// Empty stub for the `server-only` package. In production, Next.js bundles
// its own copy from next/dist/compiled and uses it as a marker that throws
// at compile time if a module is imported into a client bundle. In test
// runs (vitest) the package isn't resolvable, so we map it to this no-op.
export {};
