# DWA-A-138-1 — inventory (REFERENCE implementation)

## 0. Sources used
- Transcript: `C:\Users\Ekowai\Desktop\Guidelines\DWA-A-138-1\DWA-A_138-1_WD (5).md` (identical copies under `Desktop\Supabase data\Guidelines knowledge markdown\` and `Desktop\Blumen Forscheln\`). All table quotes below are from this file (line numbers given).
- Prod dump: `scratchpad/std/DWA-A-138-1.json` — 28 worksheets, 262 fields, 46 equations, 35 requirements.
- Repo (section 6 only): `src/lib/eval/tab9.ts`, `src/lib/eval/surface-inventory.ts`.

## 1. Table-lookup candidates (category → predetermined values)

| worksheet | selector field | dependent fields filled | guideline table | override allowed? | table in transcript? |
|---|---|---|---|---|---|
| A138-07 | `surface_inventory.rows[].tab9_value` (json row key, 30 Tab. 9 entries in tab9.ts) | `c_i`, `c_s` per row | Tab. 9 §5.3.3.5 | Yes — Tab. 9 is "Empfohlene Abflussbeiwerte"; Gl. 2 text: "C_i – Abflussbeiwert der Teilfläche, zum Beispiel gemäß Tabelle 9" (`coeff_override` flag exists) | yes (L1248–1315), see quote A |
| A138-06 | `flaechengruppe` (enum, 19 values present) | `belastungskategorie` (currently a SECOND manual enum) | Tab. 5 §5.2.2 | Yes: "Von der Kategorisierung nach Tabelle 5 kann in begründeten Fällen abgewichen werden." (L791) | yes (L800–866), quote B |
| A138-12 / A138-19 | `flaechengruppe` + `bbz_thickness` (≥20 / ≥30 cm) | `ac_as_ratio_limit` (number, desc says engine keys on Flächengruppe), `n_M_overflow_limit` (number, manual) | Tab. 6 §5.2.3.2 | Only via authority for D/S groups: "(*) … sind ggf. mit der zuständigen Behörde abzustimmen" | yes (L912–940), quote C |
| A138-06 | `flaechengruppe` | required `η_AFS63` / `η_gelöst` thresholds — NO required-value fields exist; only `eta_AFS63`, `eta_geloest` (achieved) with threshold text in validation_rules | Tab. 7 §5.2.3.3 | (*) D/S groups authority; (**) η_gelöst refers to Cu/Zn only | yes (L980–1023), quote D |
| A138-03 | `permeability_test_method` (enum 4 values: feldversuch/laborversuch/korngroessenanalyse/literaturwert) | `f_methode` (number, manual "Tab. 11 value") | Tab. 11 §5.3.3.6 | No override wording; f_K = f_Ort·f_Methode ≤ 1 (Gl. 6) | yes (L1381–1393), quote E — **enum is too coarse to look up (6 rows vs 4 values)** |
| A138-08 | (none) → `f_ort` number | — | Tab. 10 §5.3.3.6 | "im gegebenen Wertebereich von 0,3 bis 1,0 begründet zu wählen" (L1369) → SR-2 selection with justification, not a lookup | **VALUES NOT IN TRANSCRIPT** (Tab. 10 is an image, L1373) |
| A138-08 | (missing) Schutzkategorie (1)–(4) + `A_C` ≤/> 800 m² | `n` (number, REQ-08 allows {0.1,0.2,0.33,0.5}); A138-26 `T_n_Ue` | Tab. 8 §5.3.3.4 | "Hinweise zur Festlegung" — values are upper bounds "(≤ …)" | yes (L1132–1196), quote F |
| A138-12 | `soil_bodenart_tab13` (enum 2 values) when `a_s_m_determination_method = soil_estimate` | `A_S_m` | Tab. 13 §6.3.2 | "Größenordnungen … unabhängig von der Geometrie" (estimate only) | yes (L1706–1713): `Mittel-/Feinsand → 0,10 · AC`; `schluffiger Sand, sandiger Schluff, Schluff → 0,20 · AC` |
| A138-15 → 16..22 | `facility_type_selected` (enum 7) | per-type limits now hard-coded per worksheet: `h_M ≤0.30`, `freibord` vr, `basin_ki_min_check`, `basin_h_check`, `t_E ≤84`, `boeschungsneigung` | Tab. 14 §6.9 | "i. d. R." values (Regelfall) | yes (L2250–2269), quote G |
| A138-21 | `shaft_type` (typ_A/typ_B) | filter fields (`schacht_filter_thickness ≥50`, `k_f_FS ≤1e-3`) + equation choice Gl. 37 vs Gl. 40 | §6.7.1/§6.7.2 text | — | yes (text) |
| A138-02 | 7 criteria fields (`gw_clearance`, `contaminated_land_status`, `water_protection_zone`, `k_f`, `geotech_hazards`, `building_clearance_status`, `slope_risk`) | `feasibility_determination` (currently manual enum) | Tab. 3 §5.1.2 | Col 3 → "technische und planerische Maßnahmen … ggf. mit der zuständigen Genehmigungsbehörde abzustimmen" | yes (L741–755), quote H |
| A138-18 | (missing) Schüttmaterial | `q_VS` default | §6.4.2 L1869–1870 | "Liegen keine Herstellerangaben … vor, können folgende Werte näherungsweise …" | yes: `Kiessand: q_vs = 0,2 l/(s·m)`; `Kies (z. B. 16/32): q_vs = 5 l/(s·m)` |
| A138-03/05 | `permeability_test_method` | suitability warnings (Bodenart, Anlagentyp) | Tab. A.1 Anh. A (normativ) | Karten/Bodenansprache: "Ersteinschätzung; nicht für Bemessung" | yes (L2444–2466), quote I |
| A138-04 | `kostra_grid_cell` | `r_D_n_table` (json D×n) | KOSTRA-DWD-2020 (external) | DWD exact values | external dataset (already table-driven) |

Missing-field gaps (tables with values by class but no selector in prod): Tab. 8 Schutzkategorie; Tab. 11 method rows (needs 6-value enum); Schüttmaterial for q_VS; Tab. 7 required-η fields; BBZ k_f default "1·10⁻⁵ bis 5·10⁻⁵ m/s" when BBZ meets 5.2.3.2 (L1395); Bild 2 k_f-classes (image only; the 5-class table lives in DIN 18130-1 Tab. 1 — cross-standard inherit candidate). Note: A138 "Tabelle 1" is the abbreviations list, not soil classes.

Quote A — Tab. 9 (L1248–1315, condensed to value rows):
```
Dachflächen: Schrägdach – Metall, Glas, Schiefer, Faserzement 0,9 | 1,0; – Ziegel, Abdichtungsbahnen 0,9 | 1,0
Flachdach bis 3°: – Metall, Glas, Faserzement 0,9 | 1,0; – Abdichtungsbahnen 0,9 | 1,0; – Kiesschüttung 0,8 | 0,8
Begrünte Dachflächen: Extensivbegrünung >5° 0,4 | 0,7; Intensivbegrünung ≥30 cm ≤5° 0,1 | 0,2; Extensiv ≥10 cm ≤5° 0,2 | 0,4; Extensiv <10 cm 0,3 | 0,5
Betonflächen 0,9 | 1,0; Schwarzdecken (Asphalt) 0,9 | 1,0; Pflaster mit Fugenverguss 0,8 | 1,0; Gleisanlage feste Fahrbahn 0,9 | 1,0
Rampen mit Neigung zum Gebäude 1,0 | 1,0; Kunststoffflächen von Sportplätzen 0,5 | 1,0
Betonsteinpflaster in Sand/Schlacke, Platten 0,7 | 0,9; Pflaster Fugenanteil >15 % / fester Kiesbelag 0,6 | 0,7; Wassergebundene Flächen 0,7 | 0,9
Lockerer Kiesbelag, Schotterrasen 0,2 | 0,3; Verbundsteine mit Sickerfugen 0,25 | 0,4; Rasengittersteine häufig 0,2 | 0,4; ohne häufige 0,1 | 0,2
Gleisanlage Schotterbau durchlässig 0,1 | 0,2; schwach durchlässig 0,4 | 0,6
Sportflächen mit Dränung: Kunststoff 0,1 | 0,1; Tennen 0,3 | 0,3; Rasen 0,1 | 0,1
Parkanlagen/Rasen/Gärten: flach 0,1 | 0,2; steil 0,2 | 0,3; dauerhaft eingestaute Wasserflächen 1,0 | 1,0
(a) Die Abflussbeiwerte beziehen sich ausschließlich auf Flächen A_E,i, die potenziell einen Abfluss zum Entwässerungssystem haben.
```
Quote B — Tab. 5 Flächengruppe → BK (L800–866): `D → I; VW1 → I; V1 → I; VW2 → II; V2 → II; V3 → III; BG1 → I; BF → II; BL → II; BG2 → II; SD1 → II; SD2 → III; SV bzw. SVW → III; SF → III; SL → III; BG3 → III; SG → III; SA → III` (BK for V3 printed as "III" in the row `V3 & III`).

Quote C — Tab. 6 (L912–940):
```
Flächengruppe | ≥ 20 cm | ≥ 30 cm
D (BK I): (*)
VW1, V1: (keine Angabe)
BG1: bei Mulden-Rigolen: Überlauf in Rigole mit n_M max. 2/a
VW2, V2, BF, BG2 (BK II): AC/A_S,m ≤ 30, Überlauf n_M max. 1/a | AC/A_S,m ≤ 50, Überlauf n_M max. 1/a
BL, V3, BG3: AC/A_S,m ≤ 15, n_M max. 1/a | AC/A_S,m ≤ 30, n_M max. 1/a
SD1 (II), SD2, SV/SVW, SF, SL, SG, SA (III): (*)
(*) … richten sich nach den rechtlichen Anforderungen und sind ggf. mit der zuständigen Behörde abzustimmen.
```
Quote D — Tab. 7 (L980–1023): `D: (*); VW1, V1, BG1: η_AFS63 40 %, η_gelöst 50 % (**); VW2, V2, BF, BG2: 70 %, 65 % (**); BL, V3, BG3: 80 %, 75 % (**); SD1…SA: (*)`; "(**) Der Wirkungsgrad η_gelöste Stoffe bezieht sich ausschließlich auf die Referenzparameter Kupfer und Zink."

Quote E — Tab. 11 (L1381–1393):
```
Großflächige Feldversuche in Testgrube/Probeschurf (≥ 1 m²) | 1
Kleinflächige Feldversuche: kleine Testgrube/Probeschurf (< 1 m²) | 0,9; Doppelzylinder-Infiltrometer | 0,9; Open-End-Test | 0,8
Laborverfahren mit ungestörten Proben (z. B. Permeameter) | 0,7
Laborverfahren mit gestörten Proben/Sieblinienauswertung für Sandböden | 0,1
```
Quote F — Tab. 8 (L1132–1196): columns = Bemessungshäufigkeit AC ≤ 800 m² | AC > 800 m² und öffentliche Entwässerung | Überflutungshäufigkeit öffentliche Entwässerung:
```
(1) gering | (≤ 0,33/a) | (≤ 0,5/a) | (0,1/a)
(2) mäßig | (≤ 0,2/a) | (≤ 0,33/a) | (0,05/a)
(3) stark | ≥ 5 a (≤ 0,2/a) [beide Spalten] | 30 a (0,033/a)
(4) sehr stark | ≥ 10 a (≤ 0,1/a) [beide Spalten] | (0,02/a)
(a) … Bei Durchführung eines Überflutungsnachweises kann bei AC ≤ 800 m² die Bemessungshäufigkeit für AC > 800 m² angesetzt werden.
```
Quote G — Tab. 14 (L2250–2269):
```
k_f maßgebliche Bodenschicht m/s: Fläche/Mulde/MRE ≥ 1·10⁻⁶; MRS –; Rigole/Schacht ≥ 1·10⁻⁶; Becken ≥ 1·10⁻⁵
Mächtigkeit bewachsene Bodenzone cm: Fläche/Mulde/MRE/MRS ≥ 20; Rigole –; Schacht –; Becken ≥ 20
k_f bewachsene Bodenzone (langjährig): ca. 1·10⁻⁵ (oberirdisch, Becken)
Einstauhöhe cm: Fläche 0; für Mulden i. d. R. ≤ 30; Rigole/Schacht ggf. bautechnisch begrenzt; Becken i. d. R. ≥ 50
Freibord Überlauf cm: MRE/MRS ≥ 10; Becken ≥ 35; sonst –
Böschungsneigung 1:m: Mulde/MRE/MRS i. d. R. 1 : 1,5 oder flacher; Becken i. d. R. ≤ 1 : 1,5
Entleerungszeit (n = 1/a) h: Mulde/MRE/MRS ≤ 84; Becken ≤ 84
```
Quote H — Tab. 3 decision text (L752): "Eine Versickerung … ist grundsätzlich möglich, wenn alle der oben genannten Kriterien [Spalte 2] zutreffen … Wenn eine oder mehrere Kriterien [Spalte 3] zutreffen, sind technische und planerische Maßnahmen … aufzuzeigen … Wenn eines der [Spalte-4-]Kriterien zutrifft, ist eine Versickerung … in der Regel nicht zulässig". Column-2 thresholds: `MHGW ≥ 1 m`, `k_f ≥ 1·10⁻⁶ m/s`.

Quote I — Tab. A.1 (L2444–2466): `Abschätzung mit Boden-/Geodaten-Karten, Bodenansprache → k_f-Wert, "Ersteinschätzung; nicht für Bemessung"`; `Sieblinienauswertung → Sandböden; tief liegende Rigolen, Schächte`; `Laborversuche ungestörte Proben → alle/alle`; `Bohrlochmethode (Open-End) → alle; Rigolen, Schächte`; `Doppelzylinder-Infiltrometer → Infiltrationsrate k_i; schlämmungsunempfindliche, steinarme Böden; Versickerungsfläche, Mulden`; `Probeschurf ≤ 1 m² / > 1 m² → alle/alle`.

## 2. Repeatable-group candidates (one of N)

| worksheet | the thing | member fields | currently modelled | aggregation + consumers |
|---|---|---|---|---|
| A138-07 | Teilfläche | `id,label,tab9_value,area_m2,c_i,c_s,coeff_override` | json rows (DONE) | Σ area·c_i → `A_C`, `A_C_sealed`, `A_C_unsealed`; Σ area → `A_E_ba`, `A_E_nba`; `C_m`; consumers A138-10/12/13/16–22/26 |
| A138-26 | Teilfläche (Überflutungsfall) | `A_E_b_a_flood`, `C_S` | single scalars — **re-typed duplicate of the inventory rows' `c_s`** | Σ(A_E,b,a·C_S) per Gl. 10 (L699) should come from the same rows |
| A138-05 / A138-03 | Versuchsstandort (k_f test site) | site id, method (Tab. 11 row), measured k_f, depth/layer | only `kf_test_sites_count` (number) + one `k_f` | min → `k_f` ("Auf der sicheren Seite liegend wird die minimale Infiltrationsrate als k_f-Wert verwendet", L1354); count → `kf_test_density_check` (≥1 per 150 m² Sohle; +1 if length >10 m; every 25 m, L1338–1340) |
| A138-05 | Bodenschicht (layered profile) | depth from/to, Bodenart, k_f, is-BBZ | `soil_classification` text only | governing layer: "die jeweils geringere Durchlässigkeit ist maßgebend" (L1039) → `k_f`; `clearance_a` from top of exchanged layer (Bild 3) |
| A138-15 | Versickerungsanlage (facility instance) | type, geometry, n, V, A_S,m | one worksheet per TYPE (A138-16..22), `a138_anlagentyp_kandidaten` json multi-select | none/max — N facilities of the same type (e.g. two Mulden, MRS in series/parallel, Tab. 12 note 1/3) cannot be modelled today |
| A138-06 | Flächengruppe per connected surface | group per row | one `flaechengruppe` for the whole project | max (strictest): "Dabei gilt dann für alle Flächen die jeweils strengste Behandlungsanforderung" (L944) |
| A138-04 | KOSTRA row (D × n) | D, r_D(n) | `r_D_n_table` json (DONE) | iteration picks max V (Gl. 8) → `D_min`, `r_D_n` |
| A138-18 | Versickerrohr type | `az`, `d_i`, `d_a`, `az_SOE`, `A_SOE` | scalars (one pipe type, "az gleichartiger Versickerrohre") | fine as-is |

## 3. Conditional dependencies

| worksheet | driver | affected | rule + quote | encoded? |
|---|---|---|---|---|
| A138-08/09 | `A_E`, `t_f`, `n`, `q_S_AC` | `simple_method_applicable`, `design_method` | "A_E … maximal 200 ha oder … t_f … maximal 15 min"; "n ≥ 0,1/a bzw. T_n ≤ 10 a"; q_S ≥ 2 l/(s·ha) (L1088–1090) | vr text + REQ-15 |
| A138-13 | `q_S_AC` | `f_Z` | "bei … q_S,Ac ≤ 5 l/(s·ha) wird ein Zuschlagsfaktor f_z = 1,2 erforderlich" | REQ-15 `(q_S_AC > 5 OR f_Z == 1.2)` |
| A138-07 | `A_C` | `flood_check_trigger` → A138-26 | "Überflutungsnachweis … wenn … AC … größer als 800 m² ist" (p.49) | REQ-22 IF/THEN |
| A138-15 | `facility_type_selected` | visibility of A138-16..22; A_S,m equation (Gl. 16 Mulde / Gl. 17 Rigole); Tab. 14 limits | §6.1 Bild 7 (image) | `a_s_m_determination_method`, `a_s_m_needs_reconfirmation`; no visibility rule |
| A138-21 | `shaft_type` | Typ B: filter fields, Gl. 38/39; Gl. 40 when `kf_too_high_flag` | "Ist beim Schacht Typ B … k_f > 1·10⁻³ m/s … wird die Filterschicht … maßgeblich … Bemessung … mit Gl. (40)" | REQ-33 IF typ_B; Gl. 40 switch not encoded |
| A138-05 | `k_f > 1e-3` | `kf_too_high_flag` → authority | "Eine Versickerung bei k_f-Werten > 1·10⁻³ m/s ist möglich, jedoch muss … mit der Genehmigungsbehörde abgestimmt werden" (L1031) | flag vr only |
| A138-02 | `gw_clearance < 1` | `authority_coordination_required` | "Bei einem Abstand … ≥ 1 m kann in der Regel auf diese Abstimmung verzichtet werden" (L777) | REQ-04 BLOCKS at <1 m — stricter than text (text = Abstimmung, not prohibition) |
| A138-01 | `water_protection_zone` | REQ-COV-01 | "in Zone I nicht zulässig und in Zone II und III stark eingeschränkt" | encoded |
| A138-06 | above- vs underground facility | Tab. 6 (BBZ) vs Tab. 7 (dezentrale Behandlung) | "Bei Einsatz unterirdischer Versickerungsanlagen sind dezentrale Behandlungsanlagen vorzuschalten" (L967) | `treatment_required` manual boolean |
| A138-06/11 | BBZ meets 5.2.3.2 + Bild 1 | `k_f` (BBZ) 1·10⁻⁵…5·10⁻⁵, `f_methode` = neglected | L1395 | nothing |
| A138-02/22 | `residential_accessibility` | `residential_depth_check` (h + Freibord ≤ 40 cm) | §5.3.5 | vr text |
| A138-16 | facility = Fläche | `D_min_used` = 10 min (15 for large flat) | "D = 10 min … kann … auf D = 15 min vergrößert werden" | nothing |
| A138-19/20 | MRE/MRS | `n_M_Bemessung` = 1/a; `n_R` per Tab. 6+8; `n_M_overflow_limit` 2/a (BK I) or 1/a | L919–926 | limit manual |
| A138-13/26 | facility has throttle (MRS) | `Q_Dr` else 0 | Gl. 8/9/10 use Q_Dr | `Q_Dr` lives only in A138-20; no default 0 |
| A138-26 | share of Dachflächen > 70 % | `T_n_Ue` 30 → 100 a | prod vq "Bestehen die befestigten Flächen des Grundstücks z…" (truncated; verify §5.3.4.1) | vr "30 or 100" |
| A138-08 | `A_C` ≤/> 800 and Schutzkategorie | `n` column of Tab. 8 | quote F | nothing |

## 4. Derived values + inheritance
Registered equations (output ← inputs; consumers):
- Gl. 2/2c–2g (A138-07): `A_C, C_m, A_E_ba, A_E_nba, A_C_sealed, A_C_unsealed` ← `surface_inventory`; consumed by A138-10/12/13/16–22/26.
- Gl. 3 (A138-10): `Q_zu` ← r_D(n), A_C, A_VA → A138-13.
- Gl. 5, 6 (A138-11): `k_i` ← k_f, f_K; `f_K` ← f_ort, f_methode → A138-12..22, 26.
- Gl. 4, 7 (A138-12): `Q_S` ← k_i, A_S; `A_S_m` ← A_S_min, A_S_max → A138-13/15..25.
- Gl. 1, 8, 9 (A138-13): `M`; `V_VA`; `q_S_AC` → A138-15/22/24/25/26.
- Gl. 11–13 (A138-16): `A_S` (Fläche), condition k_i > r·10⁻⁷ (REQ-31).
- Gl. 14–16 (A138-17): `V_M`, `A_S_m` (Mulde). Gl. 17–25 (A138-18): `A_S_m, Q_S, V_R, s_R, L_R, q_VS`, pipe check (REQ-32). Gl. 26–29 (A138-19): `V_MR, V_R, L_R`. Gl. 30–33 (A138-20): `V_MUE, Q_MUE, L_R, Q_Dr`. Gl. 34–40 (A138-21): `A_S, V_S, h_S, erf_k_f_FS`. Gl. 41 (A138-22): `V_VA` (Becken). Gl. 10 (A138-26): `V_Rueck`.

Re-typed duplicates (same physical quantity entered/held more than once):
- `A_C` ≡ `A_C_calculated` (14) ≡ `A_C_final` (24) ≡ `A_E_b_a_flood`+`C_S` (26).
- `k_i` ≡ `k_i_calculated`; `k_f` ≡ `a138_k_f_design` ≡ `a138_k_f_min` (≈ `kf_initial_estimate`).
- `Q_S` ≡ `Q_S_calculated` ≡ `Q_S_final` ≡ `Q_S_flood`.
- `V_VA` ≡ `V_VA_calculated` ≡ `V_VA_final` ≡ `a138_V_Sp_erforderlich` ≡ `facility_specific_volume_m3`; `V_M` ≡ `V_M_MRE`; `V_R` ≡ `V_R_MRE`.
- `q_S_AC` ≡ `q_S_AC_final` ≡ `qsac_value_verified`.
- `r_D_n` ≡ `r_D_n_optimal` ≡ `r_D_n_used` ≡ `r_D_n_used_R` ≡ `r_D_n_S` ≡ `r_D_n_B` ≡ `a138_regenspende_r_DT`; `D_min` ≡ `D_optimal_min` ≡ `D_min_used` ≡ `a138_dauerstufe_D` (flood: `D_flood_min`, `r_D_30`).
- `A_VA` ≡ `A_VA_Mulde` ≡ `A_VA_MRE` ≡ `A_VA_Becken`; `d_i/d_a` ≡ `d_S_innen/d_S_aussen`.
- `n` ≡ `n_M_Bemessung` ≡ `n_R_Bemessung` ≡ `n_R` ≡ `n_R_MRS` ≡ `n_B_Bemessung`; `T_n` ≡ `kostra_design_T_n` ≡ `a138_jaehrlichkeit_T`.
- `facility_type_selected` ≡ `a138_anlagentyp_gewaehlt` ≡ `facility_type_dimensioned` ≡ `facility_type_final`; `design_method` ≡ `design_basis_final`; `flood_check_trigger` ≡ `flood_check_required_final`; `rainfall_table_ref` repeated in 7 worksheets.

## 5. Summary
Counts: table-lookup candidates 13 (1 implemented) / repeatable groups 8 (2 implemented) / conditionals 16 (6 encoded as requirement or vr) / equations 46.

Top 5 UX wins (by engineer time saved):
1. Collapse the ~40 duplicate symbols above into read-only inherited values (engineer types A_C, k_i, Q_S, V, r_D(n), D, n once — today up to 7×).
2. Derive `belastungskategorie` from `flaechengruppe` (Tab. 5) and fill Tab. 6/7 limits (`ac_as_ratio_limit`, `n_M_overflow_limit`, required η) automatically, with the "(*) behördlich" state.
3. Re-key `permeability_test_method` to the 6 Tab. 11 rows and fill `f_methode`; make k_f test sites repeatable rows with min-aggregation and density check.
4. Add Schutzkategorie selector (Tab. 8) to fill `n` (and `T_n_Ue`), keyed on A_C ≤/> 800 m².
5. Derive `feasibility_determination` from the seven Tab. 3 criteria fields (col-2 all → feasible; any col-3 → conditional; any col-4 → not feasible).

Data-quality gaps: `f_ort` Tab. 10 not in transcript (image); `s_F` desc "gravel ≈ 0.35 typical" has no source quote; REQ-04 blocks at <1 m where text only requires Abstimmung; `n_M_overflow_limit`/`f_methode`/`ac_as_ratio_limit` are number inputs for table values; `freibord` in cm while `h_M/h_B` in m; `T_n_Ue` rule quote truncated in prod; `a138_anlagentyp_kandidaten` and `a138_anlagentyp_gewaehlt` use different value keys than `facility_type_selected` (versickerungsmulde vs mulde).

## 6. Existing wizard implementation
- **Repeatable/table-driven today**: `surface_inventory` (A138-07). `surface-inventory.ts` defines `SurfaceRow {id,label,tab9_value,area_m2,c_i,c_s,coeff_override}`; `tab9.ts` is the single accessor for Tab. 9 (30 entries, groups 1/2 paved, 3 unpaved, tagged `standard:'DWA-A 138-1', edition:'2024-10'`, `lookupTab9(value)`); `normalizeSurfaceCarrier` migrates legacy `surface_type` rows (asphalt/rasen map, unique (c_m,c_s) pair match) and sets `coeff_override` when stored c_i ≠ Tab. 9 c_m; `rowMismatch` flags "abweichend"; `summarizeSurfaces` computes A_C, A_C_sealed, A_C_unsealed, A_E_ba, A_E_nba, C_m over complete rows only. This is the full pattern (a) select → fill, (b) "+" rows, (d) registered Gl. 2 sums inherited downstream.
- `r_D_n_table` (A138-04, json D×n from KOSTRA, `kostra_grid_cell` text) is table-driven data consumed by the D-iteration (`D_min`, `r_D_n` in A138-13). No field with symbol `sub_areas` exists in the A138 prod dump; the only row carrier is `surface_inventory`.
- `soil_bodenart_tab13` + `a_s_m_determination_method` (A138-12) is a selector whose engine-side fill (0,10/0,20·AC) exists per description; `ac_as_ratio_check` states (pass/fail/not_applicable/indeterminate) indicate a Tab. 6 lookup in the engine.
- **Still generic inputs although a table/category pattern applies**: A138-06 (`belastungskategorie` manual next to `flaechengruppe`; Tab. 6/7 limits only as vr text; no required-η fields); A138-03 (`f_methode` number, coarse method enum); A138-08 (`n`, `f_ort` numbers; no Schutzkategorie); A138-02 (`feasibility_determination` manual although all Tab. 3 criteria are fields); A138-05 (`kf_test_sites_count` count only; no layer/site rows); A138-15..22 (Tab. 14 limits hard-coded per worksheet instead of keyed on `facility_type_selected`; no N-facility rows); A138-18 (`q_VS` defaults by Schüttmaterial missing); A138-21 (`shaft_type` does not switch Gl. 37/40); Anhang A Tab. A.1 and Tab. 12 unused; A138-26 flood surfaces re-typed instead of read from inventory rows.
