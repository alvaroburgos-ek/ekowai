# ISO-59004 — Inventory (Circular economy — Vocabulary, principles and guidance for implementation, FDIS 2024)

## 0. Sources used
- Transcript: NO TRANSCRIPT (no markdown or text extraction). Only the PDF exists: `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 59004\ISO_FDIS_59004_N.pdf` (FDIS, pre-publication). The encoder's `_encode_report.md` in the same folder states: "Clause and term numbering, the six principles, Table 1 and the Clause 7 stage structure are taken verbatim from this FDIS copy" and "resource_management_action (13) — Table 1 R-strategy hierarchy (refuse → re-mine), descriptions verbatim." Classification below is from the prod dump only; no table values are quoted.
- Prod dump: `scratchpad/std/ISO-59004.json` — 6 worksheets, 33 fields, 0 equations, 44 requirements.
- Worktree registry: nothing wired.

## 1. Table-lookup candidates
Numeric value tables: NONE — ISO 59004 is a vocabulary/principles/guidance document; it contains no numeric tables. Its single table (Table 1, R-strategy hierarchy) is a classification list already carried as an enum.

| worksheet | selector | dependent | ref | override? | in transcript? |
|---|---|---|---|---|---|
| ISO-59004-05 | `selected_action` (enum 13: refuse, rethink, … re-mine) | `action_category` (enum 5: create_added_value, value_retention, …) — the R-strategy implies its category; today both are typed | §6.7, Table 1 | guidance ("should"); "unless a life cycle perspective indicates a better outcome" (7.repair-before-remanufacture rule) | VALUES NOT IN TRANSCRIPT (enum labels present in prod, FDIS PDF not consulted) |
| ISO-59004-04 | `selected_principle` (enum 6) | — | §5.2 | — | prod enum only |
| ISO-59004-06 | `implementation_stage` (enum 5), `implementation_level` (enum 4) | which §7 guidance applies | §7.1.3/§7.1.4 | — | prod enum only |
| ISO-59004-02 | `defined_term` (enum 23) | glossary lookup | §3.1 | — | a single-select over a glossary is not a data field (see gaps) |

Tables by class with no selector: none beyond Table 1.

## 2. Repeatable-group candidates
| worksheet | the thing | member fields | currently | aggregation + consumers |
|---|---|---|---|---|
| ISO-59004-05 | Circular-economy action | selected_action (enum 13), action_category (enum 5), feasibility_dimension (enum 6, on -06), pilot_project (boolean, -06), value_creation_model | SINGLE enum each — one action per project | register (action/R-strategy, category, feasibility dimensions checked, pilot y/n, life-cycle note); CR-018…028 evaluate per action; consumers -06 |
| ISO-59004-04 | Principle considered | selected_principle (single enum 6) + all_principles_considered (boolean) | single select | checklist of the six principles; `all_principles_considered` DERIVED from all six ticked ("Considering the integration of all the circular economy principles is important, as focusing on only one or two can undermine achievements" — CR-013 text) |
| ISO-59004-02 / -06 | Circularity aspect | circularity_aspect (text; description lists "durability, recyclability, reusability, repairability, recoverability") | text | checklist (5 aspects) — same list is an enum in ISO-59020 |
| ISO-59004-06 | Goal with intermediate targets | ce_goals (text) | text | §7.3.2 "goals with intermediate targets" → register (goal, target, year, indicator) |
| ISO-59004-06 | Circularity indicator | selected_circularity_indicator (text) | text | register linking to ISO-59020 Table 3 rows (CR-043 hands measurement to ISO 59020) |
| ISO-59004-01 | Value-chain position / solution | value_chain_position (text) | text | one row per solution/product line |
| ISO-59004-03 | Vision statement | ce_vision_statement | text | single — fine |

## 3. Conditional dependencies
| worksheet | driver | affected | rule + quote (prod description / CR text) | encoded? |
|---|---|---|---|---|
| -06 | baseline_circularity_assessment = true | measurement hands over to ISO-59020 (project must carry ISO-59020) | CR-043 "Methods to measure and assess circularity performance … are provided by ISO 59020; this document hands measurement to …" | CR-043 empty condition |
| -06 | value_creation_model set | hands over to ISO 59010 | CR-044 | empty condition |
| -05 | repair_before_remanufacture_before_recycle = false | requires life-cycle justification | CR-028 "unless a life cycle perspective indicates a better outcome" | CR-028 checks == true only |
| -04 | hazardous_substance_risk_approach | the only BLOCK gate (CR-015) | "A circular economy should not harm health of people, wildlife or the environment; a risk-based approach should be used" | CR-015 block |
| -06 | implementation_stage | which of §7.2–7.6 fields are expected (reference situation → purpose/mission → strategy → action plan → monitoring) | §7.1.4 | CR-029 presence only |
| -06 | implementation_level = across more than one level | relationships between levels considered | CR-031 | presence only |
| -01 | ce_commitment = false | nothing downstream applies | §1 | CR-002 warn |
| -05 | preliminary_action_refuse_rethink | refuse/rethink considered before others | §6.1 | CR-018 == true |

## 4. Derived values + inheritance
Equations: NONE. Derived candidates: `all_principles_considered ← all six principle rows ticked`; `action_category ← f(selected_action)` per Table 1; `stocks_flows_monitored` ← ISO-59020 inflow/outflow rows present.
Inheritance: organization_name/type CONS=["ALL"]; value_chain_position → -05/-06; system_in_focus → -06.
Re-typed duplicates: `system_in_focus` (ISO-59004-02) vs ISO-59020 `system_in_focus` (-01); `circularity_aspect` vs ISO-59020 `circularity_aspect` (enum); `selected_circularity_indicator` vs ISO-59020 `selected_core_indicator`; `baseline_circularity_assessment` vs ISO-59020 `application_purpose = baseline`; `organization_name` vs VSME B01 undertaking identity and ISO-14046 organisation fields; `interested parties` concept vs ISO-46001/59020 `interested_parties`.

## 5. Summary
Counts: table-lookups 0 numeric (1 classification Table 1 as enum) / repeatable groups 7 (0 wired; dominant: actions register, principles checklist) / conditionals 8 (1 block, rest warn/empty) / equations 0 (3 derivable flags).

TOP 5 UX wins:
1. Principles checklist (six rows) with `all_principles_considered` derived — replaces a meaningless single-select `selected_principle` and the manual boolean behind CR-013.
2. Actions register (R-strategy enum from Table 1 → category derived, feasibility dimensions, pilot flag) replacing five single-choice fields that can only describe one action per project.
3. Inherit `system_in_focus`, `circularity_aspect` and the selected indicators from ISO-59020 when both standards are on the project (CR-043 explicitly delegates measurement).
4. Goals register with intermediate targets (§7.3.2) so progress from the reference situation can be assessed instead of a free-text blob.
5. Drop `defined_term` (23-value glossary single-select) from data entry; render the glossary as reference text — it is not a worksheet fact.

Data-quality gaps: NO transcript — every enum label in prod is unverifiable under SR-1 until the FDIS PDF (or published ISO 59004:2024) is transcribed; standard status is FDIS (pre-publication) per the encode report; CR-006…012, -014, -016, -019…026, -030, -033, -038, -040, -041, -043, -044 (22 of 44) have empty conditions; units "-" throughout; ISO-59004-02 is a glossary worksheet with two real fields.
