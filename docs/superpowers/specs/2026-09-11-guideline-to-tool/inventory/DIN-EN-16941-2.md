# DIN-EN-16941-2 — inventory

## 0. Sources used
- Transcript: `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-EN-16941-2\DIN-EN-16941-2.md` (line numbers below).
- Prod dump: `scratchpad/std/DIN-EN-16941-2.json` — 5 worksheets, 76 fields, 2 equations, 19 requirements.

## 1. Table-lookup candidates (category → predetermined values)

| worksheet | selector field | dependent fields filled | guideline table | override allowed? | table in transcript? |
|---|---|---|---|---|---|
| DIN-EN-16941-2-03 | `berechnungsverfahren` = vereinfacht + `n` | per-person Ertrag 60 l/d; Bedarf WC 35, Wäsche 15, Andere 10 — NO fields today (only Gl. 1/2 inputs for the differentiated method) | Tab. A.1 (informativ) §6.2.3 | "Beispiele für typische durchschnittliche tägliche Grauwassererträge und Bedarfsmengen" (L547) — informative examples | yes (L737–749), quote A |
| DIN-EN-16941-2-03 | source type (Dusche, Badewanne, Waschmaschine, Küchenspüle, Geschirrspüler) | `Q_S`, `V_BT`, `V_WM_y`, `Q_KS`, `V_DW` — ranges only | Tab. A.2 (informativ) | "Typische Größenordnungen" → SR-2 selection inside the range, not a fixed fill; validation_rules currently HARD-LIMIT to these informative ranges | yes (L759–769), quote B |
| DIN-EN-16941-2-03 | demand type (WC, Urinal, Waschmaschine) | `V_T`, `V_U`, `V_WM_d` — ranges | Tab. A.3 (informativ) | same as above | yes (L773–781), quote C |
| DIN-EN-16941-2-04 | `vorgesehene_nutzung` (ws-01 enum) + (missing) Sprühanwendung flag | Richtwert G per parameter (E. coli, Enterokokken, Legionella, Coliforme) — NO Richtwert fields; only measured `ecoli_kbe` etc. | Tab. D.1 (informativ, BS 8525) | "Die Beispiele in Anhang D sind Mindestanforderungen. Strengere nationale oder … festgelegte Vorgaben müssen … Vorrang haben." (L681) | yes (L848–862), quote D |
| DIN-EN-16941-2-04 | same | Richtwerte Trübung, pH, Rest-Chlor, Rest-Brom | Tab. D.2 | same | yes (L866–877), quote E — prod vr fixes `< 2.0` / `< 5.0` / `< 10` for all uses (wrong for Garten / Sprüh) |
| DIN-EN-16941-2-04 | measured value vs G | `bewertung_status` (enum grün/gelb/rot — manual today) | Tab. D.3 (bacteriological) / Tab. D.4 (system) | footnote b: coliforms >10 G alone need not stop the plant if E. coli/Enterokokken/Legionella absent | yes (L881–906), quote F |
| DIN-EN-16941-2-01 | `anlagentyp` (Anhang B a–e) | allowed uses (direct use → "unterirdische Bewässerung und Anwendungen ohne Versprühen") | Anhang B (informativ) | — | yes (L788–806) |
| DIN-EN-16941-2-02 | `rueckflusssicherung_typ` (AA/AB) | free-outlet dimension A = 2×D, min 20 mm (AA) | §5.5.2 Bild 2 | — | yes: "A freier Auslauf (das Doppelte des Innendurchmessers der Zulauföffnung mit mindestens 20 mm)" (L377) |
| DIN-EN-16941-2-02 | Personenzugang ja/nein (missing flag) | `zugang_oeffnung_mm` ≥ 400 vs EN 476 | §5.4.8 | — | yes (L327) |

Missing-field gaps: Tab. A.1 simplified-method fields; Sprühanwendung flag; Richtwert fields (G) per parameter and use; Personenzugang flag; multi-select for `grauwasser_herkunft` and `behandlungsstufen` (both single enums although "eine oder mehrere" / several sources); storage cap 50 % Tagesbedarf as derived limit for `nennkapazitaet`.

Quote A — Tab. A.1 (L737–749): `1 Person | Ertrag a 60 | Bedarf: WC 35 | Wäsche waschen b 15 | Andere Nicht-Trinkwasser-Nutzungen c 10` — "a Ertrag von Dusche, Badewanne und/oder Waschbecken. b … Waschmaschinen üblicherweise 30 l bis 60 l je Zyklus … c Zum Beispiel Gartenbewässerung."

Quote B — Tab. A.2 (L759–769): `Abfluss von Duschen (Q_S) l/min 5 bis 15; Badewanne je Nutzung (keine Vollfüllung) (V_BT) l 70 bis 200; Waschmaschine je Waschvorgang (V_WM) l/Zyklus 30 bis 60; Küchenspüle (Q_KS) l/min 5 bis 15; Geschirrspüler je Spülvorgang (V_DW) l 10 bis 20`.

Quote C — Tab. A.3 (L773–781): `WC-Spülung (V_T) l/Spülung 3 bis 8; Urinal-Druckspülung (V_U) l/Spülung 1 bis 2; Waschmaschine je Waschvorgang (V_WM) l/Waschvorgang 30 bis 60`.

Quote D — Tab. D.1 (L848–862), KBE/100 ml; columns Sprühanwendung | WC-Spülung | Gartenbewässerung | Reinigung/Waschmaschine:
```
Escherichia coli | Nicht nachweisbar | 250 | 250 | Nicht nachweisbar
Intestinale Enterokokken | Nicht nachweisbar | 100 | 100 | Nicht nachweisbar
Legionella pneumophila | 10 | N/A | N/A | N/A
Gesamt Coliforme a | 10 | 1000 | 1000 | 10
```
Quote E — Tab. D.2 (L866–877): `Trübung (NTU) < 10 | < 10 | N/A | < 10; pH 5 bis 9,5 (alle); Rest-Chlor (mg/l) < 2,0 | < 2,0 | < 0,5 | < 2,0; Rest-Brom (mg/l) 0,0 | < 5,0 | 0,0 | < 5,0`.

Quote F — Tab. D.3 (L881–893): `< G → grün → System unter Kontrolle; G bis 10 G → gelb → erneute Probenahme …; > 10 G b → rot → Nutzung des Grauwassers ausschließen, bis Problem gelöst ist`. Tab. D.4 (L897–906): `< G → grün; > G → gelb`.

## 2. Repeatable-group candidates (one of N)

| worksheet | the thing | member fields | currently modelled | aggregation + consumers |
|---|---|---|---|---|
| DIN-EN-16941-2-03 | Grauwasserquelle (Ertrag) | source type, `Q_x`/`V_x`, `t_x`, `u_x` | 16 fixed scalars (Q_S,t_S,u_S, V_BT,u_BT, Q_HWB,t_HWB,u_HWB, V_WM_y,u_WM_y, Q_KS,t_KS,u_KS, V_DW,u_DW) + `n`; `grauwasser_herkunft` single enum in ws-01 | Σ per-person terms × n → `Y_G` (Gl. 1); rows for sources not connected drop out; "Zur Reduktion des Behandlungsaufwands sollte die Sammlung wie folgt präferiert werden: e) Duschen und Badewannen; f) Handwaschbecken; g) Waschmaschinen; h) Küchenspülen und/oder Geschirrspüler" (L531–535) |
| DIN-EN-16941-2-03 | Bedarfsstelle (WC type, Urinal, Waschmaschine, misc) | type, `V_x`, `u_x` | fixed scalars V_T,u_T, V_U,u_U, V_WM_d,u_WM_d, V_misc | Σ → `D_G` (Gl. 2); "Wenn mehr als ein WC-Typ angeschlossen ist, kann der Bedarf für jedes einzelne WC berechnet werden" (L597) |
| DIN-EN-16941-2-03 | Personengruppe / Grundstück | `n` per group | single `n` | Σ; hotel / >1 Grundstück → differenziertes Verfahren (L521) |
| DIN-EN-16941-2-04 | Probenahme (sample event) | date, parameter values, status | single set of measured values + one `bewertung_status` | per-sample D.3/D.4 status; "Alle Proben müssen als Stichproben während der laufenden Behandlung … entnommen werden" (L697) |
| DIN-EN-16941-2-02 | Pumpe | for gesammeltes / behandeltes Grauwasser, inside/outside, Reservepumpe | booleans only | "eine oder mehrere Pumpen" (L401); "Mehrpumpenanlagen … erforderlichenfalls mit einer Reservepumpe" (L419) |
| DIN-EN-16941-2-02 | Speichereinrichtung | Werkstoff, Nennkapazität, Zugang, Überlauf | single | "Einzelne Speichereinrichtungen dürfen miteinander verbunden werden." (L296); Σ Nennkapazität |
| DIN-EN-16941-2-02 | Behandlungsstufe | stage a)–f), order | single enum `behandlungsstufen` | multi-select (ordered) |
| DIN-EN-16941-2-05 | Betriebstagebuch-Eintrag / Wartung | date, work, contact | boolean | rows; "dem Datum der durchgeführten Überprüfungen und detaillierte Auflistung der Wartungsarbeiten" (L724) |

## 3. Conditional dependencies

| worksheet | driver | affected | rule + quote | encoded? |
|---|---|---|---|---|
| -03 | `berechnungsverfahren` | vereinfacht → Tab. A.1 per-person values, only Wohngebäude; differenziert → Gl. 1/2 | "Das vereinfachte Verfahren … ist nur für Wohngebäude anwendbar" (L543); hotel/Wohnheim/>1 Grundstück → differenziert (L521) | nothing (CR-12 always wants Y_G, D_G) |
| -01 → -03 | `grauwasser_herkunft` (should be multi) | which Gl. 1 terms are visible; Küche/Geschirrspüler → intensivere Behandlung | "Der Verschmutzungsgrad von Grauwasser aus Küchenspüle oder Geschirrspüler ist höher … und kann eine intensivere Behandlung erfordern." (L219) | nothing |
| -01 → -04 | `vorgesehene_nutzung` (+ Sprüh) | Richtwert columns D.1/D.2; Legionella only for Sprüh; Garten: Trübung N/A, Chlor < 0,5, Brom 0,0 | quotes D/E | nothing; vr hard-coded |
| -01 | `anlagentyp` = direkt | no spraying; only unterirdische Bewässerung; note §1 excludes "direkte Anwendungssysteme ohne Aufbereitung" from scope (L140) → conflict to resolve | L794 | nothing |
| -02 | Nachspeisung mit Trinkwasser | `rueckflusssicherung_typ` AA/AB, above Rückstaulinie, Geruchsverschluss if direct | L350–352, L364, L395 | CR-08 unconditional (no medium field) |
| -02 | not Schwerkraft | `pumpe_erforderlich` → pump fields | L401 | booleans |
| -02 | Speicher unterirdisch vs oberirdisch | Standsicherheit Höchstlasten vs hydrostatischer Druck; `abstand_wurzeln_m` ≥ 3 only for unterirdisch | L306–308; "Die Position der unterirdischen Speichereinrichtung muss einen Mindestabstand von 3 m …" (L633) | CR-13 unconditional (no above/below field) |
| -02 | Personenzugang | `zugang_oeffnung_mm` ≥ 400 else EN 476 | L327 | vr unconditional |
| -02 | ventilgesteuerte Zuläufe | `warnsystem_ventilzulauf` required | L360 | unconditional |
| -02 | `nennkapazitaet` | ≤ 50 % Tagesbedarf (D_G) normally sufficient; store treated water only | "wird normalerweise eine Speicherung in Höhe von bis zu 50 % des Tagesbedarfs ausreichend sein" (L511) | nothing |
| -03 | Waschmaschine both source and demand | treatment step required | L537 | nothing; `V_WM_y` and `V_WM_d` are the same 30–60 l quantity entered twice |
| -02 | `ueberlauf_kapazitaet` vs `zufluss_kapazitaet` | ≥ | L338 | CR-07 |
| -04 | measured vs G | `bewertung_status` derived (D.3/D.4) with coliform exception | quote F | manual enum |

## 4. Derived values (equations) + inheritance
- Gl. 1 `Y_G ← n, Q_S,t_S,u_S, V_BT,u_BT, Q_HWB,t_HWB,u_HWB, V_WM_y,u_WM_y, Q_KS,t_KS,u_KS, V_DW,u_DW` (ws-03).
- Gl. 2 `D_G ← n, V_T,u_T, V_U,u_U, V_WM_d,u_WM_d, V_misc` (ws-03).
- `bemessungswert_massgebend = min(Y_G, D_G)` exists only as validation text (vr) — should be a registered equation; consumers ws-02 (`nennkapazitaet` ≤ 0,5·D_G), ws-04.
- Derivable, not registered: simplified-method `Y_G = 60·n`, `D_G = (35 + 15 + 10)·n` filtered by `vorgesehene_nutzung`; Richtwert G ← use; `bewertung_status` ← value/G; free-outlet A ← D (AA).
- Cross-standard: `rueckflusssicherung_typ` ≡ DIN-1989-1 `sicherungseinrichtung_typ` (same EN 1717 AA/AB choice); pumps per EN 12056-4 / EN 12050 (DIN-EN-12056-4 encoding); `nennkapazitaet` vs DIN-1989-1 Nennvolumen concept when combined with a rainwater system (L286).
- Re-typed duplicates: `V_WM_y`/`u_WM_y` vs `V_WM_d`/`u_WM_d` (same appliance); `n` shared (fine); `zufluss_kapazitaet` could be derived from source flow rates (Dusche 0,1–0,3 l/s, Badewanne 0,4–0,5 l/s, L238).

## 5. Summary
Counts: table-lookup candidates 9 / repeatable groups 8 / conditionals 13 / equations 2 (+1 unregistered min).

Top 5 UX wins:
1. Grauwasserquellen as repeatable rows (source type → Tab. A.2 range hint, Q/V, t, u) summed into Gl. 1, driven by a multi-select `grauwasser_herkunft` — replaces 16 always-visible scalars.
2. Bedarfsstellen rows (WC types, Urinal, Waschmaschine, misc) summed into Gl. 2; register `min(Y_G, D_G)` and derive the 50 %-Tagesbedarf storage cap for `nennkapazitaet`.
3. `berechnungsverfahren` = vereinfacht → fill Tab. A.1 per-person values and hide the differentiated inputs; block vereinfacht for non-residential.
4. `vorgesehene_nutzung` (+ Sprüh flag) → fill Richtwerte G from Tab. D.1/D.2 and derive `bewertung_status` per Tab. D.3/D.4 per sample row, replacing fixed vr limits that are wrong for Garten/Sprüh.
5. Condition CR-08/CR-13/zugang on Nachspeisemedium, Speicherlage and Personenzugang so above-ground or gravity systems are not blocked by inapplicable checks.

Data-quality gaps: validation_rules hard-limit informative Tab. A.2/A.3 ranges (`Q_S >= 5 AND Q_S <= 15` etc.) — informative ranges must be hints (SR-2), not blocks; `truebung_ntu < 10`, `rest_chlor < 2.0`, `rest_brom < 5.0` ignore use-specific columns; `grauwasser_herkunft`, `behandlungsstufen` single-select; `anlagentyp = direkt` vs §1 scope exclusion; no Richtwert/G fields; `legionella_kbe` vr `>= 0` although only Sprüh has a limit; units "1/(p*d)" fine; `bemessungswert_massgebend` not an equation.
