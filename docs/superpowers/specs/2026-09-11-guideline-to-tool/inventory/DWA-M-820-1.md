# DWA-M-820-1 — Inventory (Qualität von Ingenieurleistungen optimieren, Teil 1: Vorbereitung und Vergabeverfahren, März 2020)

## 0. Sources used
- Transcript: `C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\DWA-M_820-1.md` (identical copy in `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-1\DWA-M_820-1.md`; variant `DWA-M_820_1_th_WI-04-04_2020-10.md` not used).
- Prod dump: `scratchpad/std/DWA-M-820-1.json` — 25 worksheets, 120 fields, 0 equations, 26 requirements.
- Registry (uncommitted, worktree `C:\Users\Ekowai\_wt-fll`, branch feat/fll-revision): `src/lib/eval/selection-fields.ts`, `src/lib/eval/risk-register.ts`, `src/lib/eval/mitigation-plan.ts`, editors `checklist-editor.tsx`, `structured-register-editor.tsx`, `risk-register-editor.tsx`, `mitigation-plan-editor.tsx`; dispatch in `worksheet-form.tsx` (lines 479–519, 807–832); migrations `scripts/migrations/20260801220000_m820_risk_register_structured.sql`, `…230000_m820_mitigation_plan_structured.sql`, `…240000_m820_selection_fields.sql`. None of this is on main.

### ALREADY WIRED (worktree only — NOT on main)
| symbol | worksheet | widget | source in guideline |
|---|---|---|---|
| applicable_legal_bases | M820-08 | checklist, 3 groups (Ober-/Unterschwelle/Honorar) + custom | Anh. B.1.2/B.1.3 lists |
| exclusion_124_gwb_selected | M820-12 | register (grund, vorab_bekanntgemacht) — options deliberately NOT seeded (external law) | Anh. E.1.2 |
| award_criteria_list | M820-14 | register (kriterium enum of 7 printed E.2 criteria, gewichtung %, anmerkung); note "Summe = 100 %" but NO sumColumn | Anh. E.2.2–E.2.8 |
| stakeholder_list | M820-03 | register (beteiligter w/ datalist of 5 §5 roles, rolle, verantwortung, kommunikation) | §5.1–5.6, Bild 5 |
| alternatives_considered | M820-04 | register (alternative, beschreibung, bewertung) | §4.1/§6.3 |
| quality_targets_konzept / quality_targets_projekt | M820-04 / -05 | register (ziel, anforderung) | Bild 2 / Bild 3 |
| bewertungskommission_members | M820-16 | register (name, funktion, sachverstand, stimmberechtigt) | §8.4 |
| risk_register | M820-06 | dedicated RiskRegisterEditor: 11 printed Tab. A.1 risk groups with example rows, assessors Bauherr/Planer/Betrieb, probability 0–10 and impact 0–10 anchors, mean/sdev, product | Anh. A, Tab. A.1 |
| risk_mitigation_plan | M820-07 | dedicated MitigationPlanEditor (Tab. A.2 structure, T/O/P measure types) | Anh. A, Tab. A.2 |

Everything else in the standard remains a generic single input.

## 1. Table-lookup candidates (category → predetermined values)
| worksheet | selector field | dependent fields filled | guideline table ref | override allowed? (quote) | table contents in transcript? |
|---|---|---|---|---|---|
| M820-13 (and duplicate M820-22) | `estimated_construction_cost` (M820-01, number EUR) — a band selector does NOT exist; band must be derived from the number | `liability_insurance_personenschaden`, `liability_insurance_sonstige` (number EUR, VR "from Tab. D.1 by Baukosten"); `haftpflicht_versicherungssumme` (M820-22) | Tab. D.1, Anh. D (§8.7 / E.1.4.2) | Yes — informative: "Anhaltspunkt für die Höhe der geforderten Versicherungssumme kann Tabelle D. 1 in Anhang D geben. Dabei sind für die Festlegung der Höhe auch projektspezifische Risiken abzuschätzen." and "sollte dieser im Eigeninteresse höhere Deckungsbeträge mit seiner Versicherung vereinbaren." | YES (below) |
| M820-09 | `client_organization_type` (M820-01, enum 6 incl. federal authority) | `eu_threshold_value` (number EUR; VR "either 139000 or 214000 per organization_type") | Anh. B.2.3, VO (EU) 2019/1828 | No override in the guideline (statutory), but the value is edition-dependent: "Die EU-Schwellenwerte werden von der EU-Kommission alle zwei Jahre geprüft und durch Verordnung festgelegt." → needs a dated edition override with audit | YES: "I der obersten oder oberen Bundesbehörden oder vergleichbarer Institutionen $139.000 €$, / - für alle anderen $214.000 €$." (l.1329–1330) |
| M820-23 | `electronic_transmission` (boolean) | `required_standstill_days` (number days) | §134 Abs. 2 GWB quoted in §8.10.3.6 | No (statutory) | YES: "Der Auftraggeber darf den Vertrag frühestens 15 Kalendertage (bei Versendung auf elektronischem Weg 10 Kalendertage) nach Absendung dieser Informationen schließen (§ 134 Abs. 2 GWB)." |
| M820-09 | (constants, no selector) | `lot_value_threshold_services` = 80 000, `lot_value_threshold_construction` = 1 000 000, `lot_share_threshold_pct` = 20, `eu_threshold_review_interval_years` = 2 | §3 Abs. 9 VgV (Anh. B.2.4) | No | YES: "Soweit also die Teilaufträge insgesamt $20 \%$ des Gesamtauftragswerts und je Teileauftrag 80.000 € nicht überschreiten, können diese nach nationalem Recht vergeben werden." (l.1368) — should be read-only constants, not inputs |
| M820-10 | `threshold_status` / `oberschwellig_check` | allowed set of `procurement_procedure` | §8.6 | below threshold a choice remains: "Liegt er darunter, hat der Auftraggeber ein Suchverfahren durchzuführen oder er kann …" | rule only (see §3) |

Tab. D.1 verbatim (DWA-M_820-1.md, l.1460–1472):
```
Tabelle D.1: Deckungssummen für eine Haftpflichtversicherung in Abhängigkeit zu den Herstellungskosten
Geschätzte Baukosten | Deckungssumme für Personenschäden | Deckungssumme für sonstige Schäden
bis 0,5 Mio. €  | 1,5 Mio. € | 0,25 Mio. €
bis 1,5 Mio. €  | 1,5 Mio. € | 0,5 Mio. €
bis 4,0 Mio. €  | 1,5 Mio. € | 1,0 Mio. €
bis 10 Mio. €   | 2,0 Mio. € | 2,0 Mio. €
bis 25 Mio. €   | 3,0 Mio. € | 3,0 Mio. €
bis 50 Mio. €   | 3,0 Mio. € | 5,0 Mio. €
```
Source wording: "empfiehlt das für das Bauwesen zuständige Bundesministerium in seinen Vertragsmustern … im Wesentlichen folgende Beträge" (RBBau) — a recommendation, not a DWA requirement; > 50 Mio. € is not covered.

Tables by class with NO selector in prod: Tab. A.1 (11 risk groups × 3 assessors) — covered by the wired risk editor; Anh. B legal systematics — covered by the wired checklist. No other value tables exist in Teil 1.

## 2. Repeatable-group candidates (one of N)
| worksheet | the thing | member fields | currently modelled | aggregation + consumers |
|---|---|---|---|---|
| M820-03 | Beteiligter | stakeholder_list | WIRED register (json) | count per §5 role → REQ-04 ("All 5 stakeholder categories") has an EMPTY condition; derive from register |
| M820-04/-05 | Alternative / Qualitätsziel | alternatives_considered, quality_targets_* | WIRED registers | count ≥ 2 (VR "min 2 entries") not enforced |
| M820-06/-07 | Risiko / Maßnahmenplan | risk_register, risk_mitigation_plan | WIRED dedicated editors | mean/sdev/product per row computed in the editor; REQ-05 only checks IS NOT NULL |
| M820-12 | §124-GWB-Grund | exclusion_124_gwb_selected | WIRED register | none; REQ-11 empty condition |
| M820-14 | Zuschlagskriterium | award_criteria_list + scalars `price_weight_percent`, `schluesselpersonal_count`, `qualitaets_kriterien_anzahl` (M820-11) | WIRED register, but weights are not summed and the "Preis" row weight is re-typed as `price_weight_percent` | Σ gewichtung = 100; Preis row → price_weight_percent (REQ-15); count(non-price rows) → qualitaets_kriterien_anzahl |
| M820-16 | Kommissionsmitglied | bewertungskommission_members + scalars `bewertungskommission_size`, `bewertungskommission_chairperson` | WIRED register; size re-typed | count(rows) → size (REQ-09 ≥ 2); chair = row flag |
| M820-18/-19 | Bewerber / Bieter | applicant_count, shortlisted_count, final_offers_count, winning_bidder (M820-23) | single count scalars | Anh. F §6: "Zusammenfassung der Wertung der Bewerber (üblicherweise in Tabellenform) … die Namen der Bewerber" → Bewerber register (name, §123 ok, §124 ok, Eignung score, shortlisted, final offer, winner); counts derived → REQ-19 |
| M820-20 | Verhandlungsrunde × Bieter | negotiation_rounds, verhandlung_datum, verhandlung_thema, verhandlung_ergebnis, protokoll_signiert | ONE scalar set for N rounds | "Das Verhandlungsgespräch jeden Bieters muss zusammengefasst und dargestellt werden" → register (round, bidder, date, topic, result, signed); count → negotiation_rounds (REQ-20 ≥ 1) |
| M820-09 | Los (Teilauftrag) | loseausnahme_applicable + constants | boolean + constants | lots register (lot, net value); max ≤ 80 000 and Σ(excepted lots)/Gesamtwert ≤ 20 % → loseausnahme_applicable; REQ-24 has an EMPTY condition today |
| M820-13 | Referenzanforderung | references_required_count, reference_period_years | scalars | fine as scalars (requirements, not instances) |

## 3. Conditional dependencies
| worksheet | driver | affected | rule + quote | encoded? |
|---|---|---|---|---|
| M820-09/-10 | estimated_engineering_fee vs eu_threshold_value | threshold_status, oberschwellig_check, procurement_procedure | "Erreicht oder übersteigt der geschätzte Netto-Gesamtwert den EU-Schwellenwert ist ein VgV-F-Verfahren durchzuführen. Liegt er darunter, hat der Auftraggeber ein Suchverfahren durchzuführen oder er kann …" | REQ-07 + REQ-08 (block); oberschwellig_check is a hand-entered boolean ("Computed" only in description) |
| M820-09 | client_organization_type | eu_threshold_value | see §1 quote (139.000 / 214.000) | description only |
| M820-09 | cost_estimate_uncertainty_pct (M820-04), fee, threshold | cost_estimate_near_threshold | "Die Ungenauigkeit bei den Kostenaufstellungen im Rahmen der Vorplanung kann … bei ±25 % … liegen. Dies ist bei Entscheidungen …" | description only; no equation |
| M820-10 | procurement_procedure = direktvergabe | procedure_rationale mandatory | "Die Gründe für eine Direktvergabe sind in jedem Fall zu dokumentieren" | VR "non-empty" unconditional |
| M820-16 | procurement_procedure = vgv_f | bewertungskommission_size ≥ 2 | "gemäß § 58 Abs .5 VgV mindestens zwei Personen für die Wertung einzusetzen" | REQ-09 |
| M820-17 | procurement_procedure = vgv_f | publication_date, ted_notice_id required | "muss beim Amt für Veröffentlichungen der Europäischen Union … europaweit bekannt gemacht werden" | REQ-18 |
| M820-14 | festpreis_used = true | price weight → 0 / "Preis" row removed | "Der Preis ist in diesem Fall kein Zuschlagskriterium mehr." | REQ-15 (≤ 20 OR festpreis); no visibility rule |
| M820-13 | large_long_project = true | min_annual_revenue_multiplier ≤ 1.5 (else ≤ 2.0) | "Bei großen, langlaufenden Projekten sollte das geforderte Verhältnis Jahresumsatz zu Jahresauftragswert den Faktor 1,5 nicht überschreiten." | REQ-12 checks only ≤ 2.0 |
| M820-23 | electronic_transmission | required_standstill_days 15/10; award_date ≥ letters + standstill | quote in §1 | REQ-22 tests the inequality per branch; required_standstill_days itself hand-typed |
| M820-23 | information_letters_sent false/incomplete | contract_invalidity_135_gwb_risk = true | "so regelt § 135 GWB, dass der Vertrag von Anfang an unwirksam ist" | description only; REQ-26 tests both booleans |
| M820-13/-22 | estimated_construction_cost band | insurance sums | Tab. D.1 | REQ-13 EMPTY condition |
| M820-08 | threshold_status | applicable_legal_bases group (Ober-/Unterschwelle) | Anh. B.1.2/B.1.3 | registry note text only |
| M820-04/-05 | project_type = konzept / projekt | Bedarfsplanung Konzept vs Projekt worksheets | "sowohl für das Konzept als auch für die Projekte Bedarfsplanungen durchzuführen sind" | not encoded (both always shown) |
| M820-11 | procurement_procedure = vgv_f | leistungswettbewerb_only must be true | "gemäß § 76 Abs. 1 VgV für Planungsleistungen zwingend vorgeschrieben" | REQ-17 unconditional |

## 4. Derived values + inheritance
Equations in prod: NONE (0). Fields described as computed but hand-entered: `oberschwellig_check`, `threshold_status`, `cost_estimate_near_threshold`, `required_standstill_days`, `contract_invalidity_135_gwb_risk`, `eu_threshold_value`. Candidate registered equations: EQ-A `oberschwellig_check ← estimated_engineering_fee ≥ eu_threshold_value`; EQ-B `eu_threshold_value ← f(client_organization_type, edition)`; EQ-C `required_standstill_days ← electronic_transmission ? 10 : 15`; EQ-D `cost_estimate_near_threshold ← |fee − thr| / thr ≤ cost_estimate_uncertainty_pct / 100`; EQ-E `bewertungskommission_size ← count(members)`; EQ-F `price_weight_percent ← weight of "Preis" row`; EQ-G insurance sums ← Tab. D.1 band.

Inheritance (consumer_worksheets present): project_number → M820-02/03/21/23/24/25; estimated_engineering_fee → M820-09/13; threshold_status → M820-10/11/12/17; procurement_procedure → M820-11/16/17/18/19; liability_insurance_* → M820-22.

Re-typed duplicates (same physical quantity twice):
- `estimated_engineering_fee` (M820-01) vs `auftragswert_geschaetzt` (M820-02) — both "geschätzter Netto-Gesamtwert".
- `liability_insurance_personenschaden` / `_sonstige` (M820-13) vs `haftpflicht_versicherungssumme` (M820-22).
- `bewertungskommission_size` and `_chairperson` vs rows of `bewertungskommission_members`.
- `price_weight_percent` vs "Preis" row in `award_criteria_list`; `qualitaets_kriterien_anzahl` vs non-price rows.
- `threshold_status` (enum) vs `oberschwellig_check` (boolean) — same fact.
- Cross-standard: project_number / project_name / project_location / client organisation re-typed in DWA-M-820-2 (820-2-01), DWA-M-820-3 (M8203-01) and DIN-276-01; `hoai_applicable` (M820-08) vs 820-2 `hoai_compliance`; `stakeholder_list` vs 820-2 `stakeholders_list`; `risk_register` vs 820-2-10 `risk_register` (explicitly "nach der Methodik DWA-M 820-1 Anhang A").

## 5. Summary
Counts: table-lookups 4 (+1 rule-type) / repeatable groups 11 (8 wired, 3 open: Bewerber, Verhandlungsrunden, Lose) / conditionals 14 (7 encoded as gates, 7 description-only or empty) / equations 0 (7 candidates).

TOP 5 UX wins (by engineer time saved):
1. Make the Schwellenwert chain computed: client_organization_type → eu_threshold_value → oberschwellig_check/threshold_status → allowed procurement_procedure, with a dated edition override — removes five hand-typed fields and the most common gate failure (REQ-07/08).
2. Bewerber/Bieter register (M820-18/19/23) feeding applicant_count, shortlisted_count, final_offers_count and winning_bidder — replaces four counts reconciled by hand and produces the Anh. F §6 table.
3. Tab. D.1 lookup from estimated_construction_cost into the two Deckungssummen with audited override (informative table, so override is legitimate); retire the M820-22 duplicate.
4. Verhandlungsprotokoll as a register (round × bidder) with negotiation_rounds derived — the current single scalar set cannot hold the mandatory per-bidder summary.
5. Wire the aggregates the existing registers already imply: Σ gewichtung = 100 %, Preis row → price_weight_percent, count(members) → bewertungskommission_size, count(stakeholders per §5 role) → REQ-04.

Data-quality gaps: REQ-04, REQ-11, REQ-13, REQ-14, REQ-24 have empty conditions; statutory constants (80 000 / 1 000 000 / 20 % / 2 years) are editable inputs instead of read-only; `exclusion_124_gwb_selected` has no options by design (external law) — acceptable; all wired widgets exist only in the uncommitted worktree, prod still renders the json fields generically; unit naming inconsistent ("a" vs "years").
