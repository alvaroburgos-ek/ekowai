# DIN-18130-1 — inventory

## 0. Sources used
- Transcript: `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-18130-1\DIN-18130-1.md` (also `DIN-18130-1_OCR.md`, same content). Line numbers below refer to `DIN-18130-1.md`.
- Prod dump: `scratchpad/std/DIN-18130-1.json` — 5 worksheets, 54 fields, 8 equations, 7 requirements.

## 1. Table-lookup candidates (category → predetermined values)

| worksheet | selector field | dependent fields filled | guideline table | override allowed? | table in transcript? |
|---|---|---|---|---|---|
| DIN-18130-1-01 | `durchlaessigkeitsbereich` (enum, 5 values present) — but direction is REVERSED: the class should be DERIVED from `k_10` (ws-04), not selected | (none today; should be a read-only classification of `k_10`) | Tab. 1 §3.7 | none stated ("Für bautechnische Zwecke werden fünf Durchlässigkeitsbereiche definiert") | yes (L199–212), quote A |
| DIN-18130-1-01 / -04 | `T` (ws-03, °C) | `alpha` (ws-01, number input) | Tab. 2 §5.7 | "Zwischenwerte können geradlinig eingeschaltet werden." (L327) — Gl. 6 gives the closed form, so α is fully derived; `alpha` should not be an input | yes (L316–325), quote B |
| DIN-18130-1-02 | `S_r` (ws-01, number) | `u_0` (Sättigungsdruck, number input) | Tab. 3 §6.5 | none; table has 3 discrete rows only (≥0,95 / 0,90 / 0,85) — values between rows are undefined → SR-2 selection | yes (L421–432), quote C |
| DIN-18130-1-01 | `versuchsklasse` (enum 1a/1b/2/3) — derivable from `saettigung_aufgebracht` (ws-02) + "Strömung stationär nachgewiesen" (NO field; CR-04 uses `V_w > 0 AND t > 0` as proxy) | (none; class is the output) | Tab. 4 §3.8/§6.7 | footnote *) 1b: "Aufgrund der Versuchsbedingungen … kann aber angenommen werden, daß die Strömung stationär ist." Bild 5/6/10 note: "Bei Nachweis stationärer Strömung darf der Versuch der Versuchsklasse 2 zugeordnet werden." | yes (L477–487), quote D |
| DIN-18130-1-02 | `bodenart` (ws-01, FREE TEXT today) | `versuchsanordnung` (KD/ZY/TX), `messung_wassermenge`, `statische_belastung`, need for `u_0`, achievable `versuchsklasse` | Tab. 5 §7.1.4.4 | "X geeignet, (X) bedingt geeignet, – nicht geeignet" — a suitability filter, not a fixed value | yes (L530–559), quote E |
| DIN-18130-1-01 | (missing) "bindig / grobkörnig" + `ungleichfoermig` | `A_min` (10 / 20 cm²), ratio Größtkorn:Probe 1:5 / 1:10 | §5.8 (text) | "sollte … nicht unterschreiten" | yes: "Bei bindigen Böden sollte die Querschnittsfläche mindestens A = 10 cm² betragen, bei grobkörnigen Böden mindestens A = 20 cm²" (L336); "1 : 5 bei ungleichförmigen und 1 : 10 bei gleichförmigen Böden" (L335) |
| DIN-18130-1-01 | `versuchsanordnung` + Gefälle-Messung + Volumen-Messung + SB/U0 + `versuchsklasse` | `bezeichnung` (free text today) | §4 code pattern | — | yes: "Versuch DIN 18130 - KD - ES - ST - SB - 3" (L223); examples "ZY - MS - MZ - 3" (L593), "TX - DE - MZ - SB - 2" (L688), "TX - DE - MZ - UO - 1" (L713), "ZY - DE - ST - 3" (L1233) |
| DIN-18130-1-02 | `versuchsanordnung` | suitability text per §7.x | §7.1.1/7.2.1/7.3.1/7.4.1 | — | yes: KD "feinkörnige Böden … Durchmesser von mindestens 70 mm und eine Höhe von mindestens 20 mm" (L456); ZY-Standrohre "Sande, Kiese, Sand-Kies-Gemische" (L565); TX "alle Bodenarten mit … k < 10⁻⁵ m/s" (L627); ZY "feinkörnige Böden (Tone, Schluffe)" (L727) |

Missing-field gaps: no field for "Strömung stationär nachgewiesen" (Tab. 4 second axis); no enum for Bodenart keyed to Tab. 5 rows; no "bindig" flag for §5.8; no field for Gefälle-Erzeugung arrangement (Bild 3A / Bild 6 / Bild 8–9 pressure) which selects the h-formula (§8.1 items 1–3).

Quote A — Tab. 1 (L199–212):
```
k m/s | Bereich
unter 10⁻⁸ | sehr schwach durchlässig
10⁻⁸ bis 10⁻⁶ | schwach durchlässig
über 10⁻⁶ bis 10⁻⁴ | durchlässig
über 10⁻⁴ bis 10⁻² | stark durchlässig
über 10⁻² | sehr stark durchlässig
```
Quote B — Tab. 2 (L316–325): `Temperatur T °C: 5 | 10 | 15 | 20 | 25` → `α: 1,158 | 1,000 | 0,874 | 0,771 | 0,686`.

Quote C — Tab. 3 (L421–432): `S_r ≥ 0,95 → u_0 = 300 kN/m²; 0,90 → 600; 0,85 → 900`.

Quote D — Tab. 4 (L477–487):
```
Versuchsklasse | Wassersättigung nachgewiesen und kontrolliert nach DIN 18137-2 | Strömung stationär nachgewiesen
1a | ja | ja
1b | ja | nein*)
2 | nein | ja
3 | nein | nein
```
Quote E — Tab. 5 (L530–559), condensed (columns: Bodenart | erreichbare Klasse | Zylinder/KD/Triax | Gefälle: mehrere Standrohre/ein Standrohr/Druckerzeuger | Volumen: Meßzylinder/Standrohr-Bürette/Kapillare | SB | u_0):
```
Ton, Schluff | 3 | X / X / X | x x x | (X) x – | (X) | –
Ton, Schluff | 3 | KD | – X – | – X – | X | –
Ton, Schluff | 1 | Triax | – – X | – (X) x | X | X
Feinsand | 2 | X – – | X X – | X x – | – | –
Feinsand | 2 (1) | Triax | – – X | X – – | X | X
Mittel- und Grobsand | 2 | X – – | X X – | X x – | – | –
Sand-Kies-Gemisch | 2 | X – – | x (X) – | x (X) – | – | –
Sand-Ton-Gemisch | 2 / 3 / 1 | X / X / X | (as Ton/Schluff pattern) | | (X)/X/X | –/–/X
Kies-Sand-Ton-Gemisch | 2 | X – – | – X X | (X) X – | – | –
Kies-Sand-Ton-Gemisch | 1 | Triax | – – X | (X) – x | X | x
Dabei ist: X geeignet; (X) bedingt geeignet; – nicht geeignet
```
(The OCR of Tab. 5 has merged cells; the exact per-cell mapping must be re-verified against the PDF before encoding — SR-3.)

## 2. Repeatable-group candidates (one of N)

| worksheet | the thing | member fields | currently modelled | aggregation + consumers |
|---|---|---|---|---|
| DIN-18130-1-03 | Ablesung / Messintervall (reading) | `t`, `h_2` (or `h_o`,`h_u`), `V_w`, `T`, optionally `p_o`,`p_u` | single scalars | k per reading → "Der Durchlässigkeitsversuch darf beendet werden, wenn sich aus den Messungen ein annähernd gleichbleibender k-Wert ergibt" (L526); Tab. 7 example has 26 rows and fits `k·c` by "lineare Ausgleichungsrechnung" (L934); Tab. 11 has 3 Ablesungen with k_T each → mean `k_10 = 3,48·10⁻¹⁰` (L1357). Consumer: `k`, `k_10` (ws-04) |
| DIN-18130-1-03/-04 | Versuch (test run at a stress state / Porenzahl) | `e`, `n_pore`, `rho`, `rho_d`, `S_r`, statische Belastung, resulting `k_10` | single | "mindestens drei Durchströmungsversuche mit jeweils unterschiedlichen Porenzahlen" (L246) → k(e) log-log line (§8.3); final k_10 = mean of runs (9.3: 3,84/3,74/3,74 → 3,77·10⁻⁹, L1132–1133). Consumer: `k_f` (ws-05) |
| DIN-18130-1-01 | Probe / Probekörper (sample) | `bodenart`, `bodengruppe`, `max_d`, `A`, `l_0`, Probenart | single sample per worksheet set | none; multi-sample sites need one instance per sample (feeds A138-05 test-site rows, min k_f) |
| DIN-18130-1-05 | Pflichtangaben §8.4 (report items) | 5 Versuch items + 9 Probe items | one boolean `versuchsbericht_vollstaendig` | all-true → boolean; quote: "1) Angaben zum Versuch — Bezeichnung nach Abschnitt 4 — Versuchsdauer t — Sättigungsdruck u_0 — Raumtemperatur T — Durchströmungsrichtung 2) Angaben zur Probe — Bodenart … Bodengruppe … Größtkorn … Trockendichte … Porenzahl bzw. Porenanteil … Wassergehalt vor und nach … Sättigungszahl vor und nach … Art der Probe … Maße des Probekörpers" (L833–848) |

## 3. Conditional dependencies

| worksheet | driver | affected | rule + quote | encoded? |
|---|---|---|---|---|
| -02 → -03/-04 | `gefaelle_typ` | konstant: `h`, `l`, `Q`, Gl. 8; veränderlich: `a`, `l_0`, `h_1`, `h_2`, `t`, Gl. 9; `i_bereich` (ws-05) only for veränderlich | §8.1/§8.2; "Bei Versuchen mit veränderlichem hydraulischen Gefälle ist dessen Bereich … anzugeben" (L832) | both equations exist; no visibility/choice rule |
| -03 | arrangement (Bild 6 / Bild 8–9 / Bild 3A) | formula for `h`: Bild 6 `h = h_o − h_u`; Bild 8/9 `h = (p/γ_w − Δh)` or `(p_2 − p_1)/γ_w`; Bild 3A Gl. 7; Tab. 11 `h = h_o − h_u + (p_o − p_u)/γ_w` | §8.1 items 1)–3) (L797–806); L1326 | Gl. 7 registered; pressure variant only in desc of `h` |
| -02 | `versuchsklasse` ∈ {1a,1b} | `saettigung_aufgebracht` = true, `u_0` per Tab. 3, `statische_belastung` = true | "Um den Bedingungen der Versuchsklasse 1 zu genügen, muß die Probe vor dem Versuch gesättigt werden." (L416); "Eine äußere statische Belastung ist auch bei Aufbringen eines Sättigungsdrucks … oder bei Durchströmung von unten nach oben … erforderlich" (L442) | CR-03 (saturation); SB link not encoded |
| -02 | `stroemungsrichtung` = unten_nach_oben | `statische_belastung` required | L442; "muß durch Auflast oder Einspannung verhindert werden, daß sich die Probe auflockert" (L379) | nothing |
| -02 | `versuchsanordnung` = TX | filter stone `k ≥ 10·k_Probe`; suited only `k < 10⁻⁵` | L642, L627 | nothing |
| -02 | `versuchsanordnung` = KD | sample d ≥ 70 mm, h ≥ 20 mm; Standrohr d matched (e.g. 4 mm at k < 10⁻⁹) | L456, L467 | nothing |
| -03 | `k ≥ 1·10⁻⁷` (ZY §7.4) | Meßzylinder allowed for outflow, inflow from Bürette governs | L772 | nothing |
| -01 | `ungleichfoermig` | ratio 1:5 vs 1:10 | L335 | desc only |
| -01 | Sättigungsdruck aufgebracht? | `S_ra`/`S_re` required only "falls kein Sättigungsdruck aufgebracht" | L846 | no S_ra/S_re fields (only one `S_r`) |
| -04 | `T` | `k_10` (Gl. 6) | L305 | Gl. 6 registered (inputs list includes `alpha` redundantly) |

## 4. Derived values (equations) + inheritance
- Gl. 1 `Q ← V_w, t`; Gl. 2 `v ← Q, A`; Gl. 3 `i ← h, l`; Gl. 4 `k ← v, i` (all ws-04; `i`, `k` consumed by ws-05).
- Gl. 6 `k_10 ← T, k_T (alpha)` (ws-04 → ws-05 `k_f`).
- Gl. 7 `h ← h_0, gamma_w, gamma_org` (ws-02 → ws-04).
- Gl. 8 `k ← Q, l, A, h`; Gl. 9 `k ← a, l_0, A, t, h_1, h_2` (ws-04; two equations write the same `k` — selection must follow `gefaelle_typ`).
- Cross-standard: ws-05 `k_f = k_10` "wird an die DWA-A-138 Versickerungsbemessung übergeben" → DWA-A-138-1 A138-05 `k_f` and A138-03 `permeability_test_method` = Laborverfahren ungestört (f_Methode 0,7 per A138 Tab. 11). Not linked in prod (consumer_worksheets empty).
- Re-typed duplicates: `k` ≡ `k_T` (same value, "k_T der ermittelte Durchlässigkeitsbeiwert bei der Temperatur T"); `k_10` ≡ `k_f`; `alpha` input ≡ Gl. 6 factor; `l` vs `l_0` (equal for Bild 8/9/10: "l die Höhe des Probekörpers l_0", L802); `n_pore` ≡ f(`e`) (n = e/(1+e), from Tab. 6 values 0,424 ↔ 29,8 %); `S_r` single vs required `S_ra`/`S_re`.

## 5. Summary
Counts: table-lookup candidates 8 / repeatable groups 4 / conditionals 10 / equations 8.

Top 5 UX wins:
1. Make readings a repeatable table (t, h₂ or h_o/h_u/V_w, T) with per-row k and a fitted/mean `k_T`, `k_10` — this is the core of the lab test and today is a single scalar set.
2. Derive `alpha`, `k_T`, `k_10`, `durchlaessigkeitsbereich` (Tab. 1 class) and `bezeichnung` (§4 code) instead of typing them.
3. Turn `bodenart` into a Tab. 5 enum that filters `versuchsanordnung`, `messung_wassermenge`, `statische_belastung`, `u_0` and shows the achievable `versuchsklasse`.
4. Let `gefaelle_typ` switch the visible measurement fields and the k-equation (Gl. 8 vs Gl. 9) and require `i_bereich` only for veränderlich.
5. Fill `u_0` from `S_r` (Tab. 3) and derive `versuchsklasse` from saturation + steady-flow flags (Tab. 4).

Data-quality gaps: `versuchsklasse` enum labels say "Strömung stationär angenommen" for 1b (transcript: "nein*)"); `alpha`, `A_min` are inputs for derived/class values; `S_r` unit "-" fine but no before/after pair; `T` vr `T < 40` not from source; Tab. 5 OCR has merged cells (verify against PDF); `k_f` (ws-05) has no consumer link to DWA-A-138-1; Gl. 6 input list includes `alpha` although the formula computes it.
