# Plan 3 — VSME (`vsme`) — Task 24 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 25)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `dac13d5`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write; `git stash` not used. Prod was read ONLY by (a) `node scripts/verification/prod-query.mjs --sql "…"` (the `standards` row for `%VSME%` → code `VSME`, version `2026-02-01`; the `information_schema` column lists of `fields` / `compliance_requirements` / `equations` / `worksheet_templates` / `worksheet_sections`; the per-field count of stored `project_parameters` → 1 row on `VSME-B02.000 PracticePolicyAndOrFutureInitiativeIsPubliclyAvailable`, value never selected), (b) `node scripts/regulation-tables/build-prior-snapshot.mjs VSME vsme` (read-only tx) and (c) the NEW read-only full-text capture `node scripts/verification/capture-text.mjs VSME vsme` (committed; generalised in fix round 1 — same mechanics as prod-query.mjs — `DATABASE_URL_PROD` from `.env.local` at runtime, READ ONLY transaction, full cells, no truncation). `.env.local` never printed.
- **SR-1 source: NONE — this standard has no transcript.** The ONLY quotable text is what prod already carries: the 31 `compliance_requirements` rows whose `description` quotes the VSME paragraph ("Para 29: „The undertaking shall …“") and the 9 whose `source_quote` names the printed page ("VSME B3, §29 (p.9): …") — grade **EV**, cited `[prod verification_quote (Para NN, <CR code>)]`. Quote carriers in prod (capture counts): compliance_requirements 31 / 31 with a "Para NN" description, 9 / 31 with a source_quote naming the printed page; equations 10 / 10 with a source_quote (EQ-01 … EQ-10: paras 24(e)(v) / 39(a), 30, 50 / 53, 109, 38(a) / (b), 168, 170, 63(c) — none names B2, the B7 materials mass-flow, the C3 sector list, C7 or the B8 country metric) and 0 / 10 with a verification_quote; fields 0 / 144 for both columns. Every value, list or threshold not present in that text is a sign-off entry (U-1 / F-1 / F-2); nothing was typed from memory of the standard. `VSME Standard.pdf` was NOT opened. The XLSX template was read for STRUCTURE only (§3). The inventory was used for pointers only.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `vsme` | **0** (no transcript — no seed builder, no seed migration; vsme-U-1) | 0 | 0 | **7** (all `create`: `subsidiaries` B01.100, `sites` B01.200, `policies` B02.000, `energy_carriers` B03.100, `materials` B07.300, `employees_by_country` B08.200, `human_rights_incidents` C07.000) | 0 created; 2 UPDATEs on prod enums (`keep_prod`, visibility only) | 0 | 0 | **8** rules on existing fields (3 on B01.000, 1 on B07.000, 2 on C03.300, 2 on C06.000 — every driver on the SAME worksheet) + 2 row-scope rules inside `sites` | **0 emitted** — the 14 section rules the brief asked for (12 C-worksheets ← `BasisForPreparation`, B01.100 ← `BasisForReporting`, B02.100 ← `UndertakingsLegalForm`) are STAGED (vsme-C-1 / C-2 / C-3, the 4 gated C-worksheets in G-4): nothing is inherited in VSME | **14** (B01.100 D1; B01.200 D1–D3; B02.000 D1; B03.100 D1; B03.300 D1–D4; B07.300 D1–D2; B08.200 D1–D2) | 0 replacements (EQ-01 … EQ-10 and the Plan-2a B04.100 rows untouched); the turnover / accident rates withheld (F-1) | **54** blocks (G-1 … G-4, C-1 … C-9, D-1 … D-33, F-1 / F-2, U-1, I-1, X-1 … X-3, J-1) + 5 observations | ≈ 0.47 M (tool-marker delta 15.00 M → 14.53 M at report time) | ≈ 50 |

Field entries: 31 = **23 `create`** (7 registers, 2 boolean drivers `high_climate_impact_sector` C03.300 / `governance_body_exists` C09.000, 14 derived outputs) + **8 UPDATE** (`OtherUndertakingsLegalForm` ← `UndertakingsLegalForm == 'OtherUndertakingsLegalFormMember'`; `ListOfDisclosuresForWhichNoChangesAreReported…` / `LinkToPreviousReport…` ← `ReportContainsDisclosures…Unchanged == true`; `DescriptionOfHowCircularEconomyPrinciplesAreApplied` ← `UndertakingAppliesCircularEconomyPrinciples == true`; the two C03.300 transition-plan fields ← `high_climate_impact_sector == true`; `TypeOfContentCovered…` / `SpecificationOfOtherTypesOfContent…` ← `UndertakingHasACodeOfConduct… == true`). Migration `20260917102410`: 23 `INSERT … WHERE NOT EXISTS`, 8 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes the 23 `Plan 3:` rows and restores the eight prior NULLs. No `enum_values` is written by any UPDATE (D-1). The emitters printed 0 warnings; default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  341 passed | 1 skipped (342)
      Tests  3050 passed | 1 expected fail | 1 skipped (3052)
   Start at  21:42:00
   Duration  57.64s (transform 17.52s, setup 58.58s, import 146.58s, tests 61.23s, environment 231.88s)
```
(3032 at `dac13d5` + 18 from this task: field-configs-vsme 7, equations-vsme 9, register-vsme-sites 2. No seed builder ⇒ the shared seed-index / seed-freshness pins gain no VSME row.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`, first run). `pnpm -s eslint <10 touched .ts/.tsx/.mjs files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task; run at HEAD `dac13d5` BEFORE any of this task's files existed — 37/37 — and again after):
```
pnpm vitest run --project integration tests/harness/vsme-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  37 passed (37)
   Start at  21:35:38
   Duration  5.80s (transform 839ms, setup 0ms, import 4.08s, tests 1.54s, environment 0ms)
```
The harness was NOT edited (no stale pin found; baseline and post-change runs identical). It seeds prod's VSME templates / gates / equations (`tests/harness/seed-vsme.ts`), so the new rows are not in it by construction; it proves the branch still drives every VSME gate and equation through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch probe through the real `prepareRegisterRows` / `evaluateFormula` / `parseCondition` / `evaluateCondition` / `parseFieldConfig` (`scripts/_probe_vsme.ts`, deleted): the brief's bare-boolean forms `count_rows(sites, in_biodiversity_area OR near_biodiversity_area)` and row `visible_when: high_water_stress` PARSE and compute identically to the `== true` forms (2 / pass-fail-pending) — the module uses `== true` (playbook trap Task 2 (4)) and pins the bare form; a stored `water_withdrawn_m3` under an UNTICKED `high_water_stress` is kept in the prepared row (a262e trap 2) and ignored by the Σ because the formula branches on the flag (100, not 150); a register column keyed `count` works as a row symbol; `sum_rows(materials, weight_t)` over an optional column is `manual_required` ("Unbekanntes Symbol") on the first empty cell → the totals use `if(weight_t IS NULL, 0, weight_t)` (12,5 t / 5 m³); an EMPTY register reads count 0 and Σ `manual_required` ("Keine vollständigen Zeilen"); a scalar input the worksheet does not hold reads `manual_required` ("Fehlende oder leere Eingaben: Turnover"), Turnover 0 ⇒ "Division durch Null". Field-config test: **one RED** on my own pin — the emitter REFUSED four of the fourteen section rules I fed it to prove they were emittable (`hides DescriptionOfSignificantGroupsOfProductsAndOrServicesOffered read by gate VSME-CR-C01-01`; likewise CR-C06-01 / CR-C08-01 / CR-C09-01): the C-block was split into the 10 ungated sections (C-1 / C-2 / C-3, accepted — pinned) and G-4 (the four IF-guards + section hides, refusal AND exemption pinned) → 7/7. Equations test: **two REDs** on my own pins (the missing-input message is "Fehlende oder leere Eingaben: <symbol>" when the symbol is in `input_symbols`, not "Unbekanntes Symbol"; a Σ over an empty register is manual, not 0) → 9/9. Render test: **one RED** that was a real behaviour finding — a site flagged "near" WITHOUT an area entered leaves `area_biodiversity_ha` `manual_required` (naming `area_ha`) rather than counting 0 ha: kept as the honest state (Para 33 asks the area), pinned (2,9 ha once 0,4 is typed), and written into the register note + the D2 / D3 descriptions → 2/2; migrations re-emitted. The three staged gate forms were evaluated both ways in-session (`scripts/_probe_vsme_gates.ts`, deleted): `IF NumberOfEmployees >= 50 THEN EmployeeTurnoverRate IS NOT NULL` → 60 + rate pass / 60 no rate fail / 20 no rate pass / unset pending; `IF governance_body_exists == true THEN GenderDiversityRatioInGovernanceBody IS NOT NULL` → true + ratio pass / true no ratio fail / false pass / unset pending; `IF BasisForPreparation == 'OptionB…' THEN DescriptionOfSignificantGroups… IS NOT NULL` → B + text pass / B no text fail / A pass / unset pending. Typecheck / eslint green on the first run.

## 3. Sources — every prod quote used, the template structure, absence claims

**No transcript verification** (`verify-regulation-tables.ts` not run — no tables, no builder). Every cue in the modules, the STAGED file and the sheet is a cell of the read-only full-text capture, copied by generators (`gen-vsme-quotes.mjs` → `vsme-quotes.ts`; `gen-vsme-staged.mjs` → STAGED file + sheet section; `gen-vsme-enums.mjs` → `vsme-enums.ts`), never retyped. The 18 prod quotes used (Para · CR row · cell · verbatim text):

| Para | CR (worksheet, severity) · cell | verbatim text |
|---|---|---|
| Para 24(a) | VSME-CR-B01-01 (VSME-B01.000, block) · description | Para 24(a): „The undertaking shall disclose which of the following options it has selected: OPTION A: Basic Module (only); or OPTION B: Basic Module and Comprehensive Module.“ Unbedingte Pflichtangabe (EFRAG-Template: „MISSING VALUE“ bis befüllt). |
| Para 24(c) | VSME-CR-B01-02 (VSME-B01.000, block) · source_quote | VSME B1, §24 (p.8): "The undertaking shall disclose: ... (c) whether the sustainability report has been prepared on an individual basis (i.e. the report is limited to the undertaking's information only) or on a consolidated basis (i.e. the report includes information about the undertaking and its subsidiaries);" |
| Para 24(e) | VSME-CR-B01-03 (VSME-B01.000, warn) · description | Para 24(e): „… the undertaking shall disclose … its legal form …“ Unbedingte Pflichtangabe; als Stammdatum beratend geführt (überschneidet sich mit der Registrierung). |
| Para 24(e) | VSME-CR-B01-06 (VSME-B01.000, warn) · description | Para 24(e): „… the turnover …“ Unbedingte Pflichtangabe; zugleich der Nenner der THG-Intensität (B3 para 31). Als Stammdatum beratend geführt. |
| Para 24(e) | VSME-CR-B01-07 (VSME-B01.000, warn) · description | Para 24(e): „… the number of employees in headcount or full-time equivalent (FTE) …“ Unbedingte Pflichtangabe; als Stammdatum beratend geführt. |
| Para 24(e) | VSME-CR-B01-09 (VSME-B01.200, warn) · description | Para 24(e): „… the geolocation of sites …“ Unbedingte Pflichtangabe; als Stammdatum beratend geführt. |
| Para 29 | VSME-CR-B03-01 (VSME-B03.000, block) · source_quote | VSME B3, §29 (p.9): "The undertaking shall disclose its total energy consumption in MWh, with a breakdown as per the table below, if it can obtain the necessary information to provide such a breakdown:" |
| Para 31 | VSME-CR-B03-04 (VSME-B03.300, warn) · description | Para 31: „… shall disclose its GHG intensity calculated by dividing gross GHG emissions … by turnover …“ Unbedingt, aber abgeleitet/berechnet — daher beratend, damit eine noch nicht materialisierte Berechnung keinen harten Gate auslöst. |
| Para 33 | VSME-CR-B05-01 (VSME-B05.000, warn) · description | Para 33: „The undertaking shall disclose the number and area (in hectares) of sites that it owns, has leased, or manages in or near a biodiversity sensitive area.“ Unbedingt, aber site-bezogen (mehrere Standortzeilen) — daher beratend. |
| Para 35 | VSME-CR-B06-01 (VSME-B06.000, block) · source_quote | VSME B6, §35 (p.10): "The undertaking shall disclose its total water withdrawal, i.e. the amount of water drawn into the boundaries of the organisation (or facility); in addition, the undertaking shall separately present the amount of water withdrawn at sites located in areas of high water-stress." |
| Para 37 | VSME-CR-B07-01 (VSME-B01.000, block) · description | Para 37: „The undertaking shall disclose whether it applies circular economy principles …“ Die Ja/Nein-Angabe ist unbedingt (das „… and, if so, how …“ ist die bedingte Folgeangabe). |
| Para 37 | VSME-CR-B07-01 (VSME-B01.000, block) · source_quote | VSME B7, §37 (p.10): "The undertaking shall disclose whether it applies circular economy principles and, if so, how it applies these principles." |
| Para 39 | VSME-CR-B08-01 (VSME-B08.000, warn) · description | Para 39: „… shall disclose the number of employees … for the following metrics: (a) type of employment contract (temporary or permanent) …“ Unbedingt, aber als aufgeteilte Zählung (leer ≠ null) — daher beratend. |
| Para 40 | VSME-CR-B08-03 (VSME-B08.300, warn) · description | Para 40: „If the undertaking employs 50 or more employees, it shall disclose the employee turnover rate …“ Bedingt (Schwellenwert 50). Der Auslöser (Beschäftigtenzahl) ist nicht im Gate-Grammar-Scope vergleichbar → beratend. |
| Para 41(a) | VSME-CR-B09-03 (VSME-B09.000, warn) · description | Para 41(a): „… the number and rate of recordable work-related accidents …“ Unbedingt, aber als abgeleitete Rate — daher beratend (Anzahl wird hart geprüft). |
| Para 47 | VSME-CR-C01-01 (VSME-C01.000, warn) · description | Para 47: „The undertaking shall disclose the key elements of its business model and strategy …“ Comprehensive-Modul → para 45: bei Auslassung „assumed to not be applicable“ → beratend. |
| Para 61(a) | VSME-CR-C06-01 (VSME-C06.000, warn) · description | Para 61(a): „Does the undertaking have a code of conduct or human rights policy for its own workforce? (YES/NO)“ Comprehensive-Modul (para 45) → beratend. |
| Para 65 | VSME-CR-C09-01 (VSME-C09.000, warn) · description | Para 65: „If the undertaking has a governance body in place, the undertaking shall disclose the related gender diversity ratio.“ Bedingt + Comprehensive-Modul → beratend. |

Cues that are prod worksheet titles / field labels ONLY (no paragraph text in prod — tagged `[prod worksheet_templates.title_de (…) — no paragraph text in prod; vsme-U-1]` / `prod field labels (EV)`): the registers `policies` (B02.000 "General information - Practices, policies and/or future initiatives for transitioning towards a more sustainable economy"), `materials` (B07.300 / B07.400 "… annual mass-flow of … materials used by the undertaking"), `human_rights_incidents` (C07.000 "Social - Severe negative human rights incidents"); the driver `high_climate_impact_sector` + the two C03.300 hides ("Environment - Transition plan for undertakings operating in high climate impact sectors"); the B01.000 unchanged-disclosures pair (field labels); the equations VSME-B02.000-D1, -B07.300-D1 / D2, -B08.200-D1 (`verification_quote` NULL).

**XLSX template — STRUCTURE only** (`C:\Users\Ekowai\Desktop\environmental-reporting service\01_Referenz\VSME-Digital-Template-latest.xlsx`, read zero-install with PowerShell `[System.IO.Compression.ZipFile]::OpenRead`): `xl/workbook.xml` lists 16 sheets — Language Selection, Introduction, Table of Contents & Validation, **General Information** (sheet4), **Environmental Disclosures** (sheet5), **Social Disclosures** (sheet6), Governance Disclosures, Footnotes, Fuel Converter, Fuel Conversion Parameters, Unit Of Measurement Converter, Enumeration Lists, Translations (sheet13), Licence, Technical Sheet (sheet15), Footnote (Taxonomy) Labels (sheet16). The line-item column headers exist as shared strings (`xl/sharedStrings.xml`, 10 661 entries): 9403 "Name of the subsidiary", 9415 "Registered address of the subsidiary" (B01.100); 9427 "Address of site", 9439 "Postal code of site", 9451 "City of site", 9463 "Country of site", 9475 "GPS Location of site" (B01.200; the German translation 9467 "Land des Standorts" sits in the same block); 10005 "Name of the material used", 10017 "Weight of material used", 10029 "Volume of material used" (B07.300); 10137 "Number of employees for country of employment contract", 7031 "Country of employment contract" (B08.200); 9487 "Sustainability issues addressed by practice, policy and/or future initiative" (B02.000); 10515 "Type of human right related to the confirmed incident" (C07.000). The data sheets (sheet4 / 5 / 6) reference their labels through defined-name formulas (`template_label_…`, 188 formulas on sheet4), so the header cells resolve on sheet13 (Translations: A488 → 9403, M489 → 9427, G299 → 7031), sheet15 (Technical Sheet: B256 → 9463) and sheet16 (Footnote (Taxonomy) Labels: K1 → 9475, F51 → 10005, F52 → 10017, K53 → 10029, G66 → 10137). Conclusion used: the XBRL line-item tables are one-row-per-item structures whose column names ARE the prod field labels — the register columns are named after those labels; no cell VALUE of the template was read or used.

**Absence / presence claims (amendment O — re-executable, raw output; F = `src/lib/eval/field-configs/vsme.text.prior.json`):**
- resolution (1), the 50-employee threshold IS prod text: `grep -c "50 or more employees" F` → `1` (exit 0) — CR-B08-03 Para 40 (the only reason `NumberOfEmployees >= 50` may be written; STAGED in G-1, never emitted).
- the complete set of paragraphs prod quotes: `grep -o "Para [0-9]*" F | sort -u` → `Para 24 29 30 31 33 35 37 38 39 40 41 42 43 47 61 64 65`.
- no B2 paragraph: `grep -c "Para 2[5-8]" F` → `0` (exit 1).
- no materials paragraph: `grep -c -i "material" F` → `27` (titles / labels / descriptions); `grep -i "material" F | grep -c "Para"` → `1` — that line is Para 31 "materialisiert" (CR-B03-04), not a materials sentence.
- no C7 paragraph: `grep -i "human right" F | grep -c "Para"` → `1` — Para 61(a) (C6 code of conduct).
- no by-country paragraph: `grep -i "country of employment" F | grep -c "Para"` → `0` (exit 1).
- F-1, no rate basis: `grep -c -i "turnover rate" F` → `6` (B08.300 titles, label, the Para 40 sentence); `grep -c -i "hours worked\|per 200\|per 1,000\|per million" F` → `0` (exit 1).
- F-2, no NACE sector list: `grep -c -i "high climate impact" F` → `3` (the C03.300 worksheet title_de / title_en and its section title only).
- "governance body" IS prod text: `grep -c -i "governance body" F` → `4` (C09.000 titles, label, Para 65).
- capture facts: `grep -c '"consumer_worksheets": \[' src/lib/eval/field-configs/vsme.prior.json` → `0` (exit 1; nothing inherited — pinned as `consumer_worksheets === null` on all 144 rows); `grep -c '"verification_quote": "' F` → `0` (exit 1); `grep -c '"source_quote": "' F` → `19` = 9 compliance_requirements rows + 10 equation rows (EQ-01 … EQ-10, captured into `PROD_EQUATIONS` for the record; none of them names B2, the B7 materials mass-flow, the C3 sector list, C7 or the B8 country metric — the refinements above were re-run case-insensitively over "para": material 1 / human right 1 / country of employment 0 / high climate impact 0 / practice 0 / polic 1 (the Para 61(a) line)).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code ILIKE '%VSME%'"
  → VSME · '2026-02-01' · issued_year null · 'VSME – Freiwilliger Standard für KMU'
node scripts/regulation-tables/build-prior-snapshot.mjs VSME vsme
  → wrote src\lib\eval\field-configs\vsme.prior.json: 144 field rows (0 orphan), 40 coded sections of 40, 10 equations, 31 gates (0 unparseable …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
node scripts/verification/capture-text.mjs VSME vsme
  → wrote src\lib\eval\field-configs\vsme.text.prior.json: worksheets 40, sections 40, fields 144, requirements 31, equations 10, stored parameters [{"n":1}], instances [{"n":40}]
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts vsme 20260917102410
  → wrote 31 field entries + 0 section entries for vsme -> scripts/migrations/20260917102410_field_configs_vsme.sql scripts/rollback-20260917102410-field-configs-vsme.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts vsme 20260917102420
  → wrote 14 equations for vsme -> scripts/migrations/20260917102420_equations_vsme.sql scripts/rollback-20260917102420-equations-vsme.sql
```
Both migrations are byte-pinned against a fresh emit (`field-configs-vsme.test.ts` / `equations-vsme.test.ts` against the committed `vsme.prior.json`, default refuse mode, `warnings` pinned `[]`). The equations rollback deletes the 14 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102410` → `20260917102420`; rollback in reverse. Every file touches `VSME` rows only; neither mentions `pollutant_register` / `AmountOfEmissionTo*` (pinned). **Materialisation (amendment D, noted once — vsme-I-1):** 9 of the 14 rows are register-fed on their own worksheet and materialise on save; the 4 B03.300 intensity rows and VSME-B08.200-D2 read scalars of OTHER worksheets that are not inherited today — `manual_required` ("Fehlende oder leere Eingaben") until vsme-C-7 / C-9, then form / report / PDF only (Task 17 trap 1). Server-side `computeVisibility` reads templateFields only — relevant to NO emitted rule (every emitted rule keys on its own worksheet); the [CODE] item is recorded once as vsme-X-3 for the staged C-1 / C-2 / C-3 rules.

The full-text capture script (committed as `scripts/verification/capture-text.mjs`, generalised in fix round 1 — `node scripts/verification/capture-text.mjs VSME vsme` reproduces `vsme.text.prior.json`, so R-1 holds; the generic script is shown here, the original VSME-only version is in the git history at `fd8ea8e`):
```js
#!/usr/bin/env node
// READ-ONLY full-text capture of a standard's prod TEXT cells (Plan 3 — first used by Task 24 VSME; controller ruling
// Task 24 fix round 1 generalised it for every standard WITHOUT a transcript, Tasks 27–29). Same mechanics as
// scripts/verification/prod-query.mjs (DATABASE_URL_PROD from .env.local at runtime, NEVER printed; every statement inside
// a READ ONLY transaction) but writes FULL cells (prod-query truncates at 120 chars and folding by hand is forbidden) to
// src/lib/eval/field-configs/<slug>.text.prior.json — worksheets / sections / fields (labels, descriptions,
// verification_quote, source_quote, clause_reference, …) / compliance_requirements (description, source_quote, condition +
// md5, id, …) / equations (formula + md5, source_quote, verification_quote, …) plus the stored-parameter and instance
// counts. The output is the ONLY quotable text of a transcript-less standard (grade EV) and the generators of its
// quote / enum constants and STAGED file read it; nothing in it is retyped by hand.
// Usage: node scripts/verification/capture-text.mjs <STANDARD CODE> <slug>     e.g. VSME vsme   (slug: [a-z0-9_]+)
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import postgres from 'postgres';

const [code, slug] = process.argv.slice(2);
if (!code || !slug || !/^[a-z0-9_]+$/.test(slug)) {
  console.error('usage: capture-text.mjs <STANDARD CODE> <slug>   (slug: [a-z0-9_]+)');
  process.exit(1);
}
const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..', '..');
const envText = fs.readFileSync(path.join(root, '.env.local'), 'utf8');
const m = envText.match(/^DATABASE_URL_PROD=(.+)$/m);
if (!m) { console.error('DATABASE_URL_PROD not found in .env.local'); process.exit(1); }
const url = m[1].trim().replace(/^"|"$/g, '');
const sql = postgres(url, { prepare: false, max: 1, ssl: 'require',
  connection: { default_transaction_read_only: 'on', statement_timeout: '60000' } });
const Q = {
  standard: `select id, code, version, title_de, title_en from standards where code = $1`,
  worksheets: `select w.id, w.code, w.title_de, w.title_en, w.order_index from worksheet_templates w join standards s on s.id = w.standard_id where s.code = $1 order by w.code`,
  sections: `select w.code as worksheet, ws.id, ws.code, ws.title_de, ws.title_en, ws.order_index, ws.parent_section_id from worksheet_sections ws join worksheet_templates w on w.id = ws.worksheet_template_id join standards s on s.id = w.standard_id where s.code = $1 order by w.code, ws.order_index`,
  fields: `select w.code as worksheet, f.id, f.symbol, f.label_de, f.label_en, f.data_type, f.unit, f.is_required, f.clause_reference, f.description, f.consumer_worksheets, f.order_index, f.verification_status, f.verification_quote, f.source_quote, f.active, f.default_value, f.xbrl_element_id, ws.code as section_code, case when f.enum_values is null then null else jsonb_array_length(f.enum_values) end as enum_count
from fields f join worksheet_templates w on w.id = f.worksheet_template_id join standards s on s.id = w.standard_id left join worksheet_sections ws on ws.id = f.section_id where s.code = $1 order by w.code, f.order_index, f.symbol`,
  requirements: `select w.code as worksheet, cr.id, cr.code, cr.title_de, cr.title_en, cr.condition, md5(cr.condition) as condition_md5, cr.clause_reference, cr.severity, cr.description, cr.suggestion, cr.source_quote, cr.requires_attestation
from compliance_requirements cr join worksheet_templates w on w.id = cr.worksheet_template_id join standards s on s.id = w.standard_id where s.code = $1 order by w.code, cr.code`,
  equations: `select w.code as worksheet, e.id, e.equation_number, e.formula, md5(e.formula) as formula_md5, e.formula_latex, e.input_symbols, e.output_symbol, e.output_unit, e.clause_reference, e.description, e.verification_status, e.verification_quote, e.source_quote
from equations e join worksheet_templates w on w.id = e.worksheet_template_id join standards s on s.id = w.standard_id where s.code = $1 order by w.code, e.equation_number`,
  stored: `select count(*)::int as n from project_parameters pp join fields f on f.id = pp.field_id join worksheet_templates w on w.id = f.worksheet_template_id join standards s on s.id = w.standard_id where s.code = $1`,
  instances: `select count(*)::int as n from worksheet_instances wi join worksheet_templates w on w.id = wi.worksheet_template_id join standards s on s.id = w.standard_id where s.code = $1`,
};
const out = { _meta: { command: `node scripts/verification/capture-text.mjs ${code} ${slug}`, captured_at: new Date().toISOString(), standard: code, source: 'prod (READ ONLY transaction, DATABASE_URL_PROD from .env.local)' } };
try {
  await sql.begin('read only', async (tx) => {
    for (const [k, q] of Object.entries(Q)) {
      try { out[k] = await tx.unsafe(q, [code]); } catch (e) { out[k] = { error: String(e.message || e).split('\n')[0] }; }
    }
  });
} finally { await sql.end(); }
if (!Array.isArray(out.standard) || out.standard.length === 0) { console.error(`no standards row with code ${JSON.stringify(code)} — nothing written`); process.exit(2); }
const file = path.join(root, 'src', 'lib', 'eval', 'field-configs', `${slug}.text.prior.json`);
fs.writeFileSync(file, JSON.stringify(out, null, 1) + '\n');
const n = (k) => (Array.isArray(out[k]) ? out[k].length : `ERR ${out[k]?.error}`);
console.log(`wrote ${path.relative(root, file)}: worksheets ${n('worksheets')}, sections ${n('sections')}, fields ${n('fields')}, requirements ${n('requirements')}, equations ${n('equations')}, stored parameters ${JSON.stringify(out.stored)}, instances ${JSON.stringify(out.instances)}`);
```

## 5. What was encoded

**Tables (0)** — none (no transcript; vsme-U-1 names what an extraction of `VSME Standard.pdf` unlocks: the B03 energy-breakdown rows, the B06 water-stress definition, the B08 50-employee rule as one-row tables read by `lookup()`, the B2 / B7 / C7 cues, the C3 NACE list, the rate bases).

**Registers (7, all `create`, section `<ws>-A`)** — `src/lib/eval/field-configs/vsme.ts`; every enum column copies the prior's tokens (`vsme-enums.ts`, source symbol named; pinned byte-for-byte):
- `sites` (B01.200) — `country` (enum, 256 = `CountryOfSite`, required, `sort_by_label`), `gps`, `address` (required), `postal_code`, `city`, `in_biodiversity_area` / `near_biodiversity_area` (boolean), `area_ha` (← `in_biodiversity_area == true OR near_biodiversity_area == true`), `high_water_stress` (boolean), `water_withdrawn_m3` (← `high_water_stress == true`); footer D1 / D2 / D3. Supersedes the five first-site scalars (X-2, D-3 … D-7).
- `subsidiaries` (B01.100) — `name` (required), `registered_address`; footer `subsidiaries_count`. Visibility ← `BasisForReporting == 'ConsolidatedMember'` STAGED (C-2).
- `policies` (B02.000) — `issue` (enum, 10 = `SustainabilityIssueAddressedBy…`, required), `description`, `public` / `target_set` / `senior_accountable` (boolean, per the brief; J-1 for the C02 text twin); footer `policies_public_count`.
- `energy_carriers` (B03.100) — `carrier` (enum `electricity` / `self_generated` / `fuels` = the three prod symbols, labels = their prod labels, required), `mwh` (required); footer `TotalEnergyConsumption_calc`.
- `materials` (B07.300) — `name` (required), `weight_t`, `volume_m3`; footer the two `_calc` totals.
- `employees_by_country` (B08.200) — `country` (enum, 256 = `CountryOfSite`, identical to `CountryOfPrimaryOperations…` — pinned; the template's "Country of employment contract" header), `count` (required); footer Σ / delta.
- `human_rights_incidents` (C07.000) — `type` (enum, 6 = `TypeOfHumanRight…`, required), `specification`, `actions`.
No register column is keyed `id` or shadows a prod symbol of its worksheet (pinned); no lookup / override block (no tables).

**Created drivers (2)** — `high_climate_impact_sector` (C03.300, boolean attestation; the NACE derivation is F-2) and `governance_body_exists` (C09.000, boolean; Para 65). **Derived outputs (14 `create`, widget `derived`)** — one per equation, on the equation's worksheet.

**Field `visible_when` (8, all same-worksheet, all consumer-free, none read by a gate — pinned through `gateReaders` / `producerChain`):** B01.000 `OtherUndertakingsLegalForm` ← `UndertakingsLegalForm == 'OtherUndertakingsLegalFormMember'` (Para 24(e)); B01.000 `ListOfDisclosures…` / `LinkToPreviousReport…` ← `ReportContainsDisclosures…Unchanged == true` (labels only, U-1); B07.000 `DescriptionOfHowCircularEconomyPrinciplesAreApplied` ← `UndertakingAppliesCircularEconomyPrinciples == true` (Para 37 "and, if so, how"); C03.300 the two transition-plan fields ← `high_climate_impact_sector == true` (title only, U-1 / F-2); C06.000 `TypeOfContentCovered…` / `SpecificationOfOtherTypes…` ← `UndertakingHasACodeOfConduct… == true` (Para 61(a)). **Refused / withheld and STAGED (pinned through the guards):** `EmployeeTurnoverRate` ← `NumberOfEmployees >= 50` (CR-B08-03 reads it + driver not inherited → G-1); `GenderDiversityRatioInGovernanceBody` ← `governance_body_exists == true` (CR-C09-01 → G-3); the 12 C-worksheet section rules (driver not inherited → C-1; the 4 gated worksheets refused by the gate-aware guard → G-4); B01.100 (C-2); B02.100 cooperative fields (C-3). Section rules emitted: none.

**Equations (14 new)** — `src/lib/eval/equations/vsme.ts`: `VSME-B01.100-D1 subsidiaries_count = count_rows(subsidiaries)` · `-B01.200-D1 sites_in_or_near_biodiversity = count_rows(sites, in_biodiversity_area == true OR near_biodiversity_area == true)` · `-D2 area_biodiversity_ha = sum_rows(sites, if(… OR …, area_ha, 0))` · `-D3 AmountOfWaterWithdrawnAtSitesLocatedInAreasOfHighWaterStress_calc = sum_rows(sites, if(high_water_stress == true, water_withdrawn_m3, 0))` · `-B02.000-D1 policies_public_count = count_rows(policies, public == true)` · `-B03.100-D1 TotalEnergyConsumption_calc = sum_rows(energy_carriers, mwh)` · `-B03.300-D1 … D4 GHGIntensity_{total_location, total_market, s12_location, s12_market}_calc = <TotalGross… total> / Turnover` · `-B07.300-D1 TotalMassOfMaterialUsed_calc = sum_rows(materials, if(weight_t IS NULL, 0, weight_t))` · `-D2 TotalVolumeOfMaterialUsed_calc = sum_rows(materials, if(volume_m3 IS NULL, 0, volume_m3))` · `-B08.200-D1 employees_by_country_sum = sum_rows(employees_by_country, count)` · `-D2 employees_country_delta = NumberOfEmployees - sum_rows(employees_by_country, count)` (inlined, never chained — Task 16 trap 2). None outputs a prod symbol (EQ-01 … EQ-10 keep their rows); no figure typed (pinned — the only literal is the structural 0). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows`; the standard prints no worked example in prod text): sites [in 2,5 ha + 100 m³ stress; near 1 ha + stored 50 m³ unflagged; plain; incomplete] ⇒ 2 / 3,5 / 100, empty ⇒ 0 / manual / manual; subsidiaries 2 (+1 incomplete), empty 0; policies public 1 of 3 (+1 incomplete), empty 0; energy 120,5 + 80 (+1 incomplete) ⇒ 200,5, empty manual; intensity 1200 / 3 000 000 ⇒ 0,0004 ×4, missing Turnover ⇒ manual, Turnover 0 ⇒ Division durch Null; materials ⇒ 12,5 t / 5 m³ (bare Σ over the optional column manual — pinned), empty manual; by-country 30 + 25 ⇒ 55, delta 60 ⇒ 5 / 55 ⇒ 0 / missing ⇒ manual, empty manual.

**STAGED (`scripts/verification/vsme-STAGED-plan3-rulings.sql`, 606 lines, 0 non-comment lines, 54 ☐ blocks; generated from the captures)** — G-1 CR-B08-03 IF-guard on the printed 50 + consumer edit + hide; G-2 CR-B07-01 (BLOCK, mis-hosted on B01.000) re-home to B07.000 (archive pattern, `worksheet_template_id` restore); G-3 CR-C09-01 IF-guard on `governance_body_exists` + hide; G-4 the four C-module gates wrapped in `IF BasisForPreparation == 'OptionB…' THEN <captured>` + their section hides; C-1 `BasisForPreparation` → 12 consumers + 8 ungated section hides; C-2 `BasisForReporting` → B01.100 + section hide; C-3 `UndertakingsLegalForm` → B02.100 + section hide (`== 'CooperativeMember'`); C-4 … C-9 the consumer edits that carry the register outputs / the intensity inputs / `NumberOfEmployees` to the worksheets the brief named; D-1 … D-33 the amendment-K pairs (each states data_type / unit / is_required / gate readers / data-free or not — D-13 holds prod's ONE stored value; the eight `is_required=true` scalars and the seven gate-read scalars recommend DEFER); F-1 rates; F-2 NACE; U-1 no transcript; I-1 materialisation; X-1 pollutant_register + SP-1 (e1e56f0); X-2 sites supersedes the first-site scalars; X-3 the server-side computeVisibility [CODE] item; J-1 `senior_accountable` boolean vs the C02 text. Every gate block archives the row (`compliance_requirements_archive_vsme`), guards on `md5(condition)` (ids / md5 from the capture), restores with the explicit column list; no prod equation is touched.

## 6. Inventory §5 top-wins walk

1. Gate the whole Comprehensive module (C01…C09) on `BasisForPreparation` → **partially / STAGED with reason**: the 12 section rules are written (exported `STAGED_SECTION_RULES`, emitter-accepted for the 8 ungated worksheets — pinned) but NOT emitted because `BasisForPreparation` is inherited nowhere (consumer_worksheets null on all 144 rows) and the controller instruction routes an unresolvable driver to a C-block (vsme-C-1) instead of an inert pending rule; the four gated worksheets need the IF-guards (vsme-G-4). ✓ (encoded as staged SQL) / ratification needed.
2. Sites register as the single source for B05 / B06 → **encoded**: `sites` on B01.200 with the per-site flags + figures; count / Σ ha / Σ m³ computed (D1–D3); the B05 / B06 scalars twinned (D-8 … D-11) and their inheritance staged (C-5 / C-6). ✓
3. Missing roll-ups (TotalEnergyConsumption, GHG intensities, material totals, emission by medium) → **encoded** (energy Σ, 4 intensities, 2 material totals — 7 rows) **except** F-1 (rates) and the emission-by-medium Σ, which is ALREADY the Plan-2a migration `20260916110000_vsme_b04_register_equations.sql` (X-1, untouched). The intensity rows compute only after vsme-C-7 (inputs not inherited). ✓ (except F-1)
4. Materials, subsidiaries, policies, per-country employees as true registers with sums + the employee-total consistency check → **encoded** (`materials`, `subsidiaries`, `policies`, `employees_by_country` + Σ / count / delta; the delta needs vsme-C-9). ✓
5. Printed thresholds / triggers as conditions (≥ 50 → turnover rate; boolean → description pairs B7 / C6; legal form Other / Cooperative) → **encoded for the same-worksheet half** (B7, C6, legal-form "Other", the B1 unchanged-disclosures pair, the C3 / C9 created drivers); the ≥ 50 rule and the cooperative rule are STAGED (G-1 / C-3 — guards, never weakened); the C9 hide is G-3. ✓ (guards STAGED)

Data-quality gaps of the inventory: no transcript → every cue EV, U-1; `clause_reference` "-"/null on the fields → untouched (prod hygiene); `label_de = null` tokens (OtherUndertakingsLegalFormMember, AverageDuringTheReportingPeriodMember, BusinessConductMember) → observation (generated labels fall back to the token); waste kg vs t → observation, untouched; CR-B07-01 mis-hosted → G-2; `EmployeeTurnoverRate` / accident rate without basis → F-1; raw camelCase labels → the register columns reuse them verbatim (EV), no relabelling.

## 7. Files changed

Modified: `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (vsme lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+54 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 24)" paragraph).
Created: `scripts/verification/capture-text.mjs` (read-only full-text capture, generic `<STANDARD CODE> <slug>` since fix round 1 — committed for R-1), `src/lib/eval/field-configs/vsme.prior.json` + `vsme.text.prior.json` (captured), `src/lib/eval/field-configs/vsme-enums.ts` + `vsme-quotes.ts` (generated from the captures), `src/lib/eval/field-configs/vsme.ts`, `src/lib/eval/equations/vsme.ts`, `src/lib/eval/__tests__/field-configs-vsme.test.ts`, `src/lib/eval/__tests__/equations-vsme.test.ts`, `src/components/worksheet/__tests__/register-vsme-sites.test.tsx`, `scripts/verification/vsme-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917102410_field_configs_vsme.sql` + `scripts/rollback-20260917102410-field-configs-vsme.sql`, `scripts/migrations/20260917102420_equations_vsme.sql` + `scripts/rollback-20260917102420-equations-vsme.sql`, this report (+ the copy at `.superpowers/sdd/…/task-24-report.md`).
Untouched: every other standard's file; the VSME harness files; the Plan-2a/2b VSME migrations (`20260916110000`, `20260916140000`) and `register-configs.ts`; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the scratch probes deleted; the generators (enums, quotes, STAGED + sheet, report) live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / capture / reality won)

1. **No cross-worksheet rule emitted** (brief Step 4: 12 section rules + 4 field rules keyed on B01.000 drivers): nothing is inherited in VSME; per the controller instruction those became C-1 / C-2 / C-3 / G-1 (consumer edit + rule in one staged transaction) instead of inert pending rules. The brief's "9 worksheets C01…C09" are 12 prod worksheets (C03 has three, C08 two).
2. **G-4 (new)**: the gate-aware guard refuses the section hide on the four C-worksheets that carry a warn gate (CR-C01-01 / C06-01 / C08-01 / C09-01) — found by feeding the emitter, not anticipated by the brief; staged as IF-guards + hides.
3. **Register-fed roll-ups live on the register's worksheet** (B01.200 D1–D3 instead of B05.000 / B06.000; B03.100 D1 instead of B03.000; B07.300 D1–D2 instead of B07.400) — m277e trap 2; C-4 … C-6 / C-8 carry them to the brief's worksheets. The four intensity rows stay on B03.300 as the brief places them (inputs are scalars, not registers) with C-7.
4. **Equations 14 (brief 8 + 1 staged)**: the brief's "-D1" rows expand to D1–D3 (sites), D1–D4 (intensities), D1–D2 (materials, by-country); the "1 staged" (accident rate) is F-1 with no formula, as instructed.
5. **Row conditions in the `== true` form** (the brief wrote `in_biodiversity_area OR near_biodiversity_area` / `high_water_stress`): both parse and compute identically (probed + pinned); the module follows the playbook's boolean convention.
6. **`policies.senior_accountable` boolean** (the brief) while prod's C02 twin is a text → J-1.
7. **B03.300 twin names** `GHGIntensity_{total_location, total_market, s12_location, s12_market}_calc` (the brief: `GHGIntensity_<scope>_calc`).
8. **One extra committed script** (`scripts/verification/capture-text.mjs`, generic since fix round 1): the prior capture does not carry `description` / `source_quote`, `prod-query.mjs` truncates at 120 chars and amendment A forbids folding by hand — the script is the re-executable command behind every quote (R-1); it reads only, prints no secret.
9. **Sign-off ids 54 (brief ≈ 8)**: the 33 amendment-K pairs dominate; the brief's `vsme-U-1` / `F-1` / `F-2` / `G-1` / `G-2` / `X-1` / `X-2` ids kept.
10. **Three-fragment `verification_quote` on the `sites` register** (`src/lib/eval/field-configs/vsme.ts`, Q_B01_09 — Q_B05_01 — Q_B06_01): amendment E allows a composed cue of TWO verbatim fragments; the register carries the B1 site list AND the per-site B5 / B6 facts, so its cue joins three byte-exact prod fragments (Para 24(e), Para 33, Para 35) — reviewer-accepted deviation (fix round 1, item 3), recorded, not changed.

## 9. Self-review

- Every cue in the modules, the STAGED file and the sheet is a capture cell copied by a generator (the quote table in §3 is generated from the same capture); every enum column list equals the prior's list byte-for-byte (pinned); every gate id / md5 / worksheet id in the STAGED file is read from the capture at generation time; the staged gate conditions are the CAPTURED strings wrapped in the IF guard; the 0 / 1 / 40 counts (unparseable gates / stored values / instances) come from in-session read-only queries.
- The field-config test pins: 31 entries by kind; the 8 rules with their exact drivers and that each driver is a field of the SAME worksheet; `keep_prod` on both enum UPDATEs; the driver data types (boolean / enum) behind the `== true` / token forms; token equality for every enum column and the rule literals; the presence of "50 or more employees" in prod text; the sites column set / required columns / row conditions; footers = created outputs; every create in a captured section; `consumer_worksheets === null` on all 144 rows; the 10 ungated staged section rules ACCEPTED by the emitter, the 4 gated ones REFUSED naming their gate and ACCEPTED under the IF-guarded prior; G-1 / G-3 refusals + IF-guard exemptions; G-2's mis-hosting; no created output collides with EQ-01 … EQ-10; no gate unparseable; freshness of migration + rollback; no `enum_values` UPDATE; B04.100 untouched.
- Runtime facts were probed through the real register / formula / condition contracts before the pins (probes deleted); the render test drives `sites` (two rows, conditional columns, 256 options, the flagged-without-area open Σ, the required columns) through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file: the registry lines, the playbook paragraph and the sheet section are marker-checked `split/join` splices that throw on a missing / duplicate marker; the modules, tests and generators were written with the Write tool; the two small test-pin corrections used `split/join` on exact strings.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the two registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **The headline win (Option-A relief on the 12 Comprehensive worksheets) is entirely STAGED** — C-1 (consumer edit + 8 section hides) and G-4 (4 IF-guards + 4 hides); nothing hides today. That is the fail-safe reading of "driver not inherited ⇒ C-block instead of emitting"; if the controller prefers the plan-convention reading (emit the rules, they stay pending until the consumer edit), the 14 entries are already in `STAGED_SECTION_RULES` and the 10 ungated ones re-emit without code change (the 4 gated ones must wait for G-4 in any reading).
2. **No transcript** — every cue is EV; a text extraction of `VSME Standard.pdf` (U-1) would let a fix round replace the title-only cues and seed the three threshold tables without structure changes.
3. **G-2 is a BLOCK gate that is inert today**: re-homing CR-B07-01 makes it enforce on B07.000 — a deliberate enforcement change for the owner.
4. **33 amendment-K pairs**: one holds prod's only stored value (D-13), eight are `is_required=true` scalars, seven are gate-read — all recommend DEFER until the consumer edits land.
5. **Five equations cannot compute today** (the 4 intensities, the by-country delta) — honest `manual_required` until C-7 / C-9; they are emitted because the brief places them there and the inputs are scalars, not registers.
6. **Token/time:** ≈ 0.47 M tokens in, ≈ 50 min wall-clock — below the Task 22 / 23 band (no transcript to read; the cost sits in the 33 D-pairs and the capture tooling).

## 11. Residue (what the standard demands that the worksheets still do not ask, as far as prod text shows)

B04.000 `URLOrLinkToThePubliclyAvailableDisclosure` ← `PubliclyAvailableDisclosure == true` and B11.000 convictions / fines ← "In case of convictions and fines in the reporting period" (Para 43, CR-B11-01) — natural pairs the brief does not list; no rule emitted (B11 has no driver field; B04 out of scope) — observation on the sheet. C07.000 `human_rights_incidents` rows are not gated on the two "confirmed incidents" booleans (no paragraph text carries the condition). C08.000 revenues (fixed list, EQ-10) untouched. D99.000 entity-specific disclosures untouched. The B1 `TypeOfNumberOfEmployees` / `EmployeeCountingMethodology` interpretation of every B8 count (inventory §1) is a display concern, not encodable as a rule. Everything else the inventory names is on the sign-off sheet.


---

# Fix round 1 (review verdict on `fd8ea8e`: "Approved with minors" — 0 inventions in the byte audit; the four items applied verbatim)

- **Commit:** see the reply (one commit on top of `fd8ea8e`, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — the only prod read of this round is the smoke run of the generalised capture script into a throwaway slug (`VSME vsme_probe`, read-only, file deleted); the committed `vsme.text.prior.json` was NOT re-captured. `git stash` NOT used.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling.

1. **STAGED G-3 / G-4 no longer race on CR-C09-01** (`scripts/verification/vsme-STAGED-plan3-rulings.sql`, regenerated by the scratchpad generator). Apply order is now explicit in the file header ("G-3 before G-4, C-1 before G-4"), in G-3's Note and in both "Chosen now" texts. G-4's statement for CR-C09-01 (id `fafa0b33-bf99-477f-af6c-54f6b062cfc7`) no longer re-archives the row and is guarded on **G-3's POST-state** `md5(c.condition) = '4cd9d72ce0f5bfed75477bd453e426c8'` = md5 of the exact string G-3 writes (`IF governance_body_exists == true THEN GenderDiversityRatioInGovernanceBody IS NOT NULL`; computed with node:crypto in the generator, never typed), and writes the compound guard `IF BasisForPreparation == 'OptionBBasicModuleAndComprehensiveModuleMember' AND governance_body_exists == true THEN GenderDiversityRatioInGovernanceBody IS NOT NULL` (post-state md5 `e568febab20ba8241e8f6ac3d775f152`). Grammar: `src/lib/compliance/evaluate.ts` (header: the condition grammar accepts "`AND`/`OR`/`NOT`/parentheses, `IF cond THEN cond` guards"); verified in-session through `parseCondition` / `evaluateCondition` (scratch probe, deleted): parses; B + body + ratio ⇒ pass, B + body no ratio ⇒ fail, B no body ⇒ pass, A ⇒ pass, unset ⇒ pending (missing BasisForPreparation, governance_body_exists). G-4's rollback for that row is an explicit `UPDATE … FROM compliance_requirements_archive_vsme a WHERE a.id = c.id AND c.id = 'fafa0b33…' AND md5(c.condition) IN ('4cd9d72c…', 'e568feba…') AND md5(a.condition) = '6d28c9b6…'` — restores the ORIGINAL condition from the archive row G-3 wrote, guarded on either post-state md5; the other three gates keep `RESTORE(id)`. G-4's text also states the fallback when G-3 is REJECTED (apply G-3's archive line and the plain guard on the original md5 — one line the owner edits). The sign-off sheet section was regenerated from the same block list (54 blocks, same ids) and mirrors both texts. The STAGED file stays 100 % comments (`grep -vc "^--\\|^$"` → 0).
2. **Playbook count corrected** (Task 24 traps, item (1)): "31 … descriptions + 9 CR `source_quote` cells; the 10 equation rows carry a `source_quote` too" (capture counts — the sheet / report / `vsme-quotes.ts` already carried 9 + 10 after the in-task correction).
3. **Three-fragment `verification_quote` kept, deviation recorded (§8 item 10):** the `sites` register cue composes THREE byte-exact prod fragments (Para 24(e) CR-B01-09 — Para 33 CR-B05-01 — Para 35 CR-B06-01 source_quote) because the register carries the B1 site list AND the per-site B5 / B6 facts; amendment E allows two — accepted by the reviewer, noted, not changed.
4. **Capture script generalised ([CODE], controller ruling):** `scripts/verification/vsme-capture-text.mjs` → `scripts/verification/capture-text.mjs <STANDARD CODE> <slug>` (git mv + rewrite): parameterised queries (`s.code = $1` via `tx.unsafe(q, [code])`), writes `src/lib/eval/field-configs/<slug>.text.prior.json`, `_meta.command = node scripts/verification/capture-text.mjs <CODE> <slug>`, usage error on missing args / bad slug (exit 1), exit 2 + nothing written when no `standards` row matches; READ ONLY transaction, URL never printed. Smoke (re-executable): `node scripts/verification/capture-text.mjs` → `usage: capture-text.mjs <STANDARD CODE> <slug>   (slug: [a-z0-9_]+)` (exit 1); `… NOPE nope` → `no standards row with code "NOPE" — nothing written` (exit 2); `… VSME vsme_probe` → `wrote src\\lib\\eval\\field-configs\\vsme_probe.text.prior.json: worksheets 40, sections 40, fields 144, requirements 31, equations 10, stored parameters [{"n":1}], instances [{"n":40}]` and a node comparison against the committed file → `identical apart from _meta: true` (probe file deleted). The committed `vsme.text.prior.json` was edited in ONE field only — `_meta.command` (JSON re-serialised with the script's own `JSON.stringify(out, null, 1) + '\\n'`, asserted byte-identical before the edit; `git diff` = 1 line) — so `captured_at` and every cell stay as captured; no freshness pin reads `_meta.command` (the vsme tests load the prior through `loadPriorSnapshot` / the generated modules only; `grep -rn vsme-capture-text src scripts --include=*.ts --include=*.tsx` → no hit). References updated: the playbook paragraph, this report (§0 / §4 / §7 / §8 / the embedded script), the STAGED header, the sheet section head (regenerated), the `vsme-quotes.ts` header (regenerated — data unchanged, both migrations byte-identical, freshness pins green).

## Raw output (once, before the commit)

```
node <scratchpad>/gen-vsme-quotes.mjs → wrote vsme-quotes.ts 31 requirements, 144 fields, 40 worksheets   (header only; PROD_* data unchanged)
node <scratchpad>/gen-vsme-staged.mjs → wrote STAGED (606 lines, 54 blocks) + sheet section (440 lines); grep -vc "^--\\|^$" scripts/verification/vsme-STAGED-plan3-rulings.sql → 0
pnpm vitest run --project unit <field-configs-vsme, equations-vsme, register-vsme-sites, generated-sql-freshness> → Test Files  4 passed (4) · Tests  23 passed (23) · Start at  22:00:51 · Duration  2.67s (transform 1.97s, setup 478ms, import 3.74s, tests 790ms, environment 1.51s)
pnpm test → Test Files  341 passed | 1 skipped (342) · Tests  3050 passed | 1 expected fail | 1 skipped (3052) · Start at  22:01:00 · Duration  57.82s (transform 17.61s, setup 58.13s, import 145.98s, tests 62.49s, environment 231.28s)
pnpm -s typecheck → exit 0 · pnpm -s eslint scripts/verification/capture-text.mjs src/lib/eval/field-configs/vsme-quotes.ts → exit 0
```
