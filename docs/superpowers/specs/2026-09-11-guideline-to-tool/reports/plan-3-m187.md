# Plan 3 — DWA-M-187 (`m187`) — Task 12 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 13)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `309d553`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-187 m187` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 25 worksheet titles + field counts, the section codes / titles / field counts of ten worksheets, the 15 compliance rows with ids / severities / md5(condition) / conditions, the 4 equation rows with ids / formulas / md5 / input_symbols / status, the labels / units / `is_required` / `validation_rules` / `default_value` of all 139 fields, eight field descriptions, the `information_schema` column lists of `equations` / `compliance_requirements` / `fields`). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-187\DWA-M_187_GD.md` (1252 lines; read end-to-end L1–L1252 — L1100–L1252 are the literature list and the back-matter adverts). The 97 quote spans were lifted mechanically by line range (a throwaway generator in the scratchpad, `JSON.stringify` per span — no `${ }` / backtick hazards; `grep` found none in the spans) and every seeded cell is asserted inside its span at build time (`inSpan`). The inventory was used for pointers only — its "0,2 m mit Carbonatschicht NOT found" note is refuted by L930 (m187-J-4, R-5 reversal reported). The `.txt` / `.xlsx` / `.pdf` siblings were not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m187` | 11 | 69 (69/69 verbatim) | 0 rows skipped; the 4 OCR "KSO" / "KAO" rows of TABELLE4 / TABELLE5 kept verbatim (U-2) → 2 tables `imported_unverified`, 9 `md_verified`; Tab.-2 unit "l/(s·m²)²" quoted under U-1 (not a seeded table) | 6 (all `create`: `sorptionsstufen` -09, `filterschichten` -13, `filtersegmente` -14, `indikatororganismen` -16, `teilfilterbecken` -20, `klein_rbf_elemente` -22) | 1 (`create`: `daten_vorhanden` -20) + 1 attestation (`create`: `stoffstromtrennung` -19) | 0 | 15 (all `create`: `h_FK_min_p` + 8 × `tab3_*` on -06, `h_FK_min_spur` / `q_Dr_RBF_limit_spur` / `q_Dr_RBF_vorgabe_spur` on -11, `q_Dr_RBF_limit_mikro` / `h_FK_min_mikro` on -16, `UV_dosis_min` on -18) | 30 (3 UPDATE on consumer-free existing inputs + 27 on created fields) | 0 (every field-bearing B / D section holds a consumed producer — refused; C-2) | 24 (-09 D1…D3, -13 D1…D3, -14 D1…D4, -16 D1/D2, -20 D1…D6, -22 D1…D6) | 1 deactivation (R-1 Gl. 1 / Gl. 2 on M187-09) + 11 gate blocks (G-1 … G-11) | 47 (+4 observations) | ≈ 0.62 M (tool-marker delta 15.00 M → 14.38 M at report time) | ≈ 75 |

Field entries: 51 = 48 `create` + 3 UPDATE. Migration `20260917101210`: 48 `INSERT … WHERE NOT EXISTS`, 3 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 3 UPDATE rows to the captured NULLs and deletes only the 48 `Plan 3:` rows. Derived-output fields (`widget='derived'`): 24 — one per equation. No `enum_values` is written by any UPDATE (D-1; the only select is a `create`).

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  291 passed | 1 skipped (292)
      Tests  2713 passed | 1 expected fail | 1 skipped (2715)
   Start at  06:11:19
   Duration  44.90s (transform 12.61s, setup 47.65s, import 104.57s, tests 44.22s, environment 187.30s)
```
(2684 at `309d553` + 29 from this task: seed-m187 12, field-configs-m187 7, equations-m187 8, register-m187-schichten 2; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output.

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/m187-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  17 passed (17)
   Start at  06:12:30
   Duration  5.33s (transform 636ms, setup 0ms, import 3.59s, tests 1.58s, environment 0ms)
```
The harness seeds prod's fields / equations / gates (`tests/harness/seed-m187.ts`); the new rows are not in it by construction — it proves the branch still drives every DWA-M-187 chain and gate both ways where drivable.

**TDD evidence.** Runtime facts were established BEFORE the pins with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` on an ad-hoc BILD3 table (`scripts/_probe_m187.ts`, deleted): a `lookup_value` STRING cell compares with `!=` in row scope (`material != 'Dränagekies'` → 0,7 m over three layers) and a boolean `lookup_value` with `== true` / `== false` (0,7 / 0,25); `if(gak_min IS NULL, 1, …)` yields 1 on a layer without a printed band; `floor()` computes the modulo-4 rest; a nested `if(c == 'ja', lookup(…), lookup(…))` over an enum string computes; a null table cell inside arithmetic is `manual_required: Operand ist keine Zahl: null`; `min_rows(reg, col, cond)` with `cond` over an OPTIONAL null cell is `manual_required: Fehlende Eingabe für min_rows()` → `M187-16-D1` conditions on the REQUIRED `ablauf > 0` and `filtersegmente.flaeche_m2` was made required (`A_F_segmente` is a plain Σ); `IN {gak, mitbehandlung_ka}`, `AND`, `> 3000`, `== 'ja'` conditions parse and evaluate (`pass` / `fail`). Seed builder: all 11 tables built first run with every cell `inSpan`; verifier **69/69** on its first run. Field-config test: one RED on my own estimate of the prod self-consumer count (111 → the capture says 86; pinned as captured) → 7/7; the emitter accepted all 51 entries first time (the rule set was planned against the capture) and the six refusals the test pins (UV_dosis, B_CSB, eight section rules) were asserted, not discovered. Equations test: one RED that was the standard's own boundary — L497 prints "< 5,0 m/h" and derives 1,25 m from 15 min · 5,0 m/h, so a stage at exactly 5,0 m/h reads badge 0 (pinned as J-6) → 8/8. Render test: 2/2 first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m187 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-187\DWA-M_187_GD.md"` → **`69/69 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- S5_LIMITS_P (3): faellung_filterzulauf L488 · melioration_filtermaterial L514 · nachgeschaltete_sorptionsstufe L496
- S5_LIMITS_SPUR (4): kontaktzeit L599 · gak L601 · mitbehandlung_ka L603 · nachgeschaltete_stufe L605
- S5_LIMITS_APP (5): p_rueckhalt L488 · spurenstoffe L597 · mikroorganismen L675–L677 · organische_belastung L790 · klein_rbf L926–L930
- S5_1_3_1_P (15): faellung × beta_min / s_po4p_am L489, pufferschicht_cm L505; melioration × fe_massenanteil_pct / fe_gehalt_min_pct / p_grund_beladung_max L512, feinmassenanteil_max_pct L516, fallhoehe_max_m L542; sorptionsstufe × ebct_min_min / v_filter_auf_max / h_fk_ss_m / v_filter_ab_max L497, sorptionsstufen_min L499, fe_gehalt_min_pct / p_grund_beladung_max L522
- BILD3 (4): 1 … 4 — the printed tabular L614–L617 (shared span)
- S5_4_3_ORG (12): csb_trennung L765 · wirkungsgrad L770 · q_krit / q_a_max / v_vorstufe_min L784 · b_csb_max / a_f_pro_aeb L792 · teilfilter_multiple / teilfilter_in_betrieb / foerderleistung / beschickung_pro_ereignis L794 · austritt_max L817
- S5_5_KLEIN (17): a_b_a_max_ha L859 · h_rr_min L914 · deckschicht_cm L922 · h_fk_min L926 · caco3_filter_min_pct / h_fk_caco3_min / caco3_carbo_pct / h_fk_carbonat L930 · draen_dn_min / h_draen_min L942 · h_rbf_min L954 · a_f_anteil_pct / a_f_m2_pro_ha / a_f_element_min_m2 L964 · eta_afs63_min / jahreswassermenge_min_pct L966 · b_krit L968
- S5_3_3_5_UV (2): ja / nein — L703
- TABELLE3 (3): the three variants — body span L468–L475 (transposed)
- TABELLE4 (2): L995 / L996 · TABELLE5 (2): L1007 / L1008

Status per table: `md_verified` — S5_LIMITS_P, S5_LIMITS_SPUR, S5_LIMITS_APP, S5_1_3_1_P, BILD3, S5_4_3_ORG, S5_5_KLEIN, S5_3_3_5_UV, TABELLE3 (every printed row lifted, every cell legible; BILD3's `filterwirksam` is an unprinted classification from the Tab.-2 definitions — J-5). `imported_unverified` — TABELLE4 / TABELLE5 (U-2: "KSO" / "KAO" — the OCR prints the letter O for the digit 0; kept verbatim).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-187 m187
  → wrote src\lib\eval\field-configs\m187.prior.json: 139 field rows (75 orphan), 225 coded sections of 225, 4 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m187
  → wrote seed + rollback for m187 -> scripts/migrations/20260917101200_regulation_tables_seed_m187.sql scripts/rollback-20260917101200-regulation-tables-seed-m187.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m187 20260917101210
  → wrote 51 field entries + 0 section entries for m187 -> scripts/migrations/20260917101210_field_configs_m187.sql scripts/rollback-20260917101210-field-configs-m187.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m187 20260917101220
  → wrote 24 equations for m187 -> scripts/migrations/20260917101220_equations_m187.sql scripts/rollback-20260917101220-equations-m187.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m187.test.ts` / `equations-m187.test.ts` pin the other two against the committed `m187.prior.json`). The seed migration carries 69 row upserts (`ON CONFLICT (table_id, row_key)`) and 9 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the eleven tables (no earlier DWA-M-187 table seed exists — nothing superseded). The equations rollback deletes the 24 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101200` → `20260917101210` → `20260917101220`; rollback in reverse. Every file touches `DWA-M-187` rows only. Scalar-only rows (`h_FK_SS_calc`, `V_segment_soll`, `A_F_min_ohne_daten`, `B_CSB_calc`, `teilfilter_rest`, `A_F_anteil_calc`, `h_FK_min_klein`) are not server-materialised (amendment D — expected, noted once here and as I-2); the seventeen register-fed rows materialise on save.

## 5. What was encoded

**Tables (11)** — `src/lib/eval/regulation-tables-seed-m187.ts`, builder `m187SeedTables()`, edition `'2025-09'` (title page L9 "September 2025", L12 "Entwurf", imprint L47 "Hennef 2025" = prod `standards.version` 'September 2025 (Entwurf)' — I-1). The Merkblatt prints no numbered equations and no numeric tables beyond definitions, the qualitative Tab. 3, Bild 3 and the Klimakennung; every limit is a printed sentence, so the "text-limit" tables carry one sentence per row with a `modal` column:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| S5_LIMITS_P | `variante_p` = prod `verfahrensvariante_p` tokens | `h_fk_min_m` (b: 1,00; a / c null "keine Änderungen zu DWA-A 178"), `h_fk_comparator`, `hinweis` | locked (L514 "muss") | 3 | md_verified |
| S5_LIMITS_SPUR | `variante_spur` = prod `verfahrensvariante_spurenstoffe` tokens | `q_dr_rbf` (0,01 / 0,03 / 0,03 / null), `q_dr_comparator` (null "auf" — J-2 / ≤ / ≤ / null), `q_dr_text`, `h_fk_min_m` (null / 1 / 1 / null), `h_fk_text`, `beschickungsdauer_max_h` 24, `trockenzeit_h` 24, `segmente_erfahrung` 3 (J-3), `v_segment_text`, `hinweis` | locked (L601 "ist … zu begrenzen"; O-1 proposes anhaltswert from L597 "sollten") | 4 | md_verified |
| S5_LIMITS_APP | `sonderanwendung` = prod tokens | `q_dr_rbf` (mikro 0,01 "="), `q_dr_modal`, `h_fk_min_m` (mikro 1,0 "sollte", klein 0,25), `h_fk_carbonat_m` (klein 0,2 — PRINTED at L930, J-4), `hinweis` (the two variant-driven rows and organische_belastung carry nulls + pointer) | locked (L675 "ist … sicherzustellen"; O-2 for the mikro h_FK "sollte") | 5 | md_verified |
| S5_1_3_1_P | `variante_p` × `parameter` | `wert`, `unit`, `comparator`, `modal`, `text` — β ≥ 4 / S_PO4-P,aM 0,5 / 5 cm; Fe 7 % / > 35 % / < 0,2 g/kg / > 5 % proof / ≤ 1,0 m; EBCT ≥ 15 / < 5,0 / 1,25 m / < 2 / ≥ 2 Stufen / > 35 % / < 0,2 | locked (L512 "einzuhalten"; O-3 lists the kann / sollte rows) | 15 | md_verified |
| BILD3 | `lage` '1'…'4' | `dicke_cm` 10 / 60 / 30 / 25, `material`, `gak_vol_min/max_pct` (10–20 / – / 30–40 / –), `caco3_massenanteil_pct` 20 (sand layers), `filterwirksam` (J-5) | anhaltswert (L611 "empfohlenen", caption L621 "Empfohlener Filteraufbau") | 4 | md_verified |
| S5_4_3_ORG | `parameter` | `wert` / `unit` / `comparator` / `modal` / `text` — 3.000 mg/l, 100 %, 60 l/(s·ha) (sollte), 4 m/h, 50 m³ (empfohlen), 20 g/(m²·d), 750 m²/ha, 4 / 3 Teilfilterbecken, 6 l/(m²·min), 20 l/m² (sollten), 1 m²/Öffnung (sollte) | locked (L792) | 12 | md_verified |
| S5_5_KLEIN | `parameter` | the 17 printed §5.5 figures: A_b,a < 1 ha, h_RR ≥ 0,2, 5 cm Deckschicht, h_FK ≥ 0,25 / 0,2 carbonate, CaCO3 ≥ 20 % / 80 %, h_FK,CaCO3 ≥ 0,10, DN 50, h_Drän ≥ 0,1 (sollte), h_RBF ≥ 0,6, A_F 1,0 % = 100 m²/ha, element ≥ 1,0 m² (sollte), η ≥ 95 %, > 85 %, b_krit 7 | locked (L926) | 17 | md_verified |
| S5_3_3_5_UV | `uv_eingesetzt` = prod ja / nein | `uv_dosis_min_j_m2` 200 / null | anhaltswert (L703 "sollte … nicht unterschritten werden") | 2 | md_verified |
| TABELLE3 | `variante_p` (transposed) | the eight printed Kriterien as strings | locked (L568 — informative) | 3 | md_verified |
| TABELLE4 / TABELLE5 | `gegenstand` (the two printed row heads) | 5 KS / 7 KA cells verbatim incl. "KSO" / "KAO" | locked (L982) | 2 × 2 | imported_unverified (U-2) |

**Registers / selections / lookup_fill / visibility** — `src/lib/eval/field-configs/m187.ts`:
- `filterschichten` (M187-13 C, create json; `visible_when` Spurenstoffe b / c): `lage` lookup_key BILD3 → `material`, `dicke_soll_cm`, `gak_min`, `gak_max`, `caco3_soll_pct`, `filterwirksam` lookup_values; `dicke_ist_cm` (required), `gak_vol_pct`, `caco3_pct`; badge `gak_ok = if(gak_min IS NULL, 1, if(gak_vol_pct >= gak_min AND gak_vol_pct <= gak_max, 1, 0))`; footer `h_FK_lagen`, `h_Draen_lagen`, `schichten_gak_verletzungen`; note L611 + the four printed layers.
- `sorptionsstufen` (M187-09 C, `visible_when` P c): `stufe`, `ebct_min`, `v_filter_auf`, `v_filter_ab`, derived `h_fk_ss = ebct_min * v_filter_auf / 60`, badges `ebct_ok` (≥ 15 via `lookup('S5_1_3_1_P', …)`), `v_ok` (< 5,0); footer `sorptionsstufen_count`, `ebct_min_stufe`.
- `filtersegmente` (M187-14 C, `visible_when` Spurenstoffe c): `label`, `flaeche_m2` (required), `retentionsvolumen_m3` (required), `beschickungstag` (datalist Tag 1–3), derived `v_soll_m3 = Q_T_d_aM * 86.4` (the worksheet input in row scope, G-13), badge `v_ok`; footer `segmente_count`, `V_segment_soll`, `segmente_unterdimensioniert`, `A_F_segmente`.
- `indikatororganismen` (M187-16 C, `visible_when` mikroorganismen): `organismus` enum (e_coli / enterokokken / coliphagen, L661), `einheit` enum (kbe / mpn / pbe, Tab. 2), `zulauf`, `ablauf`, derived `log_red = log10(zulauf / ablauf)`, badge `> 1,0 Log-Stufe`; footer `organismen_count`, `log_red_min`.
- `teilfilterbecken` (M187-20 C): `label`, `flaeche_m2`, `in_betrieb`, derived `foerder_min_l_min` (6 l/(m²·min) · Fläche) and `beschickung_soll_l` (20 l/m² · Fläche) from S5_4_3_ORG — "bezogen auf die jeweils beschickte Filterfläche"; footer `teilfilter_count`, `teilfilter_rest`, `A_F_gesamt`, `A_F_aktiv`.
- `klein_rbf_elemente` (M187-22 C): `label`, `a_b_a_m2`, `a_f_m2` (required), derived `anteil_pct`, badge `a_f_ok` (≥ 1,0 m² via S5_5_KLEIN), `h_rr`, `h_rbf`; footer `elemente_count`, `A_b_a_sum_klein`, `A_F_sum_klein`, `A_F_anteil_calc`, `elemente_unter_1m2`.
- Selects / attestation (create): `daten_vorhanden` (M187-20 B, ja / nein — the two printed cases of L792); `stoffstromtrennung` (M187-19 B, boolean attestation, `visible_when CSB_konzentration > 3000`, L765); `CSB_fracht_d` (M187-20 B, number, `visible_when daten_vorhanden == 'ja'`).
- lookup_fill (all created, role `limit` except the text fills): `h_FK_min_p` (M187-06) ← S5_LIMITS_P by `verfahrensvariante_p`, visible for b) only; eight `tab3_*` text fills (M187-06) ← TABELLE3; `h_FK_min_spur` (visible b / c), `q_Dr_RBF_limit_spur` + `q_Dr_RBF_vorgabe_spur` (text, the printed operator wording; visible a / b / c) (M187-11) ← S5_LIMITS_SPUR by `verfahrensvariante_spurenstoffe`; `q_Dr_RBF_limit_mikro` / `h_FK_min_mikro` (M187-16) ← S5_LIMITS_APP by the inherited `sonderanwendung`, visible for mikroorganismen; `UV_dosis_min` (M187-18) ← S5_3_3_5_UV by `uv_eingesetzt`, visible for ja.
- Field `visible_when` on existing fields (3, all consumer-free by capture): M187-07 `pufferschicht_carbonatbrechsand` / `betriebsdauer_jahre` ← Variante a) (L505 / L554); M187-09 `anzahl_sorptionsstufen` ← Variante c) (L499). Refused / withheld: `UV_dosis` (consumed by -16, C-4), `h_FK_CaCO3` / `CaCO3_massenanteil_carbo` (consumed by -22, C-3), `B_CSB` / `A_F_pro_AEb` (self-consumed, C-5), `h_FK` / `q_Dr_RBF` (multi-consumed). Section rules: none (C-2).

**Equations (24 new)** — `src/lib/eval/equations/m187.ts`: `M187-09-D1 h_FK_SS_calc = EBCT * v_filter_aufstrom / 60` · `-D2 sorptionsstufen_count` · `-D3 ebct_min_stufe = min_rows(…)` · `M187-13-D1 h_FK_lagen = sum_rows(filterschichten, if(filterwirksam == true, dicke_ist_cm, 0)) / 100` · `-D2 h_Draen_lagen` (== false) · `-D3 schichten_gak_verletzungen = count_rows(…, gak_ok == 0)` · `M187-14-D1 V_segment_soll = Q_T_d_aM * 86.4` · `-D2 segmente_count` · `-D3 segmente_unterdimensioniert` · `-D4 A_F_segmente` · `M187-16-D1 log_red_min = min_rows(indikatororganismen, log_red, ablauf > 0)` · `-D2 organismen_count` · `M187-20-D1 A_F_gesamt` · `-D2 A_F_aktiv = sum_rows(…, if(in_betrieb == true, flaeche_m2, 0))` · `-D3 teilfilter_count` · `-D4 A_F_min_ohne_daten = lookup('S5_4_3_ORG', 'a_f_pro_aeb', 'wert') * A_E_b` · `-D5 B_CSB_calc = CSB_fracht_d / A_F_gesamt` · `-D6 teilfilter_rest = teilfilter_count - 4 * floor(teilfilter_count / 4)` · `M187-22-D1 A_F_sum_klein` · `-D2 A_b_a_sum_klein` · `-D3 elemente_unter_1m2` · `-D4 A_F_anteil_calc = A_F_sum_klein * 100 / A_b_a_sum_klein` · `-D5 h_FK_min_klein = if(carbonatschicht_vorhanden == 'ja', lookup('S5_LIMITS_APP', 'klein_rbf', 'h_fk_carbonat_m'), lookup(…, 'h_fk_min_m'))` · `-D6 elemente_count`. No prod symbol is re-produced (A_F / ok_boolean keep Gl. 1 / Gl. 2). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): 15 min · 5,0 m/h / 60 = **1,25 m** (L497 "Das entspricht einer Mindesthöhe des Filterkörpers von 1,25 m"); Bild 3 10 + 60 + 30 cm = **1,00 m** (L601 "Filterschichtstärke von 1 m") and 25 cm Dränagekies = 0,25 m; the four BILD3 fills per layer; GAK 25 % in the 10–20 % layer → one violation, Σ 1,15 m; Q_T,d,aM 5 l/s → **432 m³/d**; segments 400 / 432 / 500 m³ → one under-sized, Σ 5000 m²; 10⁶ → 10⁵ = **1,0 Log-Stufe** (L667 "90 % (1,0 Log-Stufe)"), 10⁵ → 10³ = 2, Ablauf 0 excluded, min 1; four Teilfilterbecken of 500 m² → 2000 / 1500 m², rest 0 (5 → 1, 8 → 0), 3000 l/min and 10 000 l per basin; **750 m²/ha · 2 ha = 1500 m²**; 20 kg CSB/d over 2000 m² = 10 g/(m²·d), division by zero `manual_required`; elements 5000 / 3000 / 2000 m² × 50 / 30 / 0,8 m² → Σ 10 000 / 80,8 m², **0,808 %**, one element under 1,0 m², 100 m² per ha = 1,0 %; `h_FK_min_klein` **0,25 / 0,2 m** by the carbonate toggle, `manual_required` without it; every count 0 on an empty register, every Σ `manual_required` on an empty register.

## 6. Inventory §5 top-wins walk

1. `sonderanwendung` (+ the two variant selects) as the branch switch that shows only the matching worksheets and fills the per-variant `h_FK` / `q_Dr_RBF` limits → **fills encoded, gates STAGED, sections STAGED**: `h_FK_min_p` (b), `h_FK_min_spur` / `q_Dr_RBF_limit_spur` (a / b / c), `q_Dr_RBF_limit_mikro` / `h_FK_min_mikro`, `h_FK_min_klein` (carbonate toggle) are visible twins beside the three contradictory REQs; the gates that replace REQ-02 / -03 / -04 are m187-G-1; the worksheet-level hiding needs m187-C-1 (the switch reaches five worksheets) + C-2 (every section holds a consumed producer) — the mechanism cannot deliver it as data without the owner accepting the cross-branch consumer chains.
2. Inherit the DWA-A 178 base design by reference → **Phase 6** (m187-X-1; content-boundary rule — nothing filled from A-178).
3. De-duplicate ≈ 55 copies and move the misplaced blocks (UV / KBE off -07, org-load off -08, Klein-RBF off -09) → **Phase 6** (m187-X-2 lists them; REQ-05 on -08 and REQ-06 / Gl. 1 / Gl. 2 on -09 are the gate / equation consequences in G-2 / G-6 / R-1).
4. Bild-3 layer stack as a repeatable table that sums to `h_FK`, plus Teilfilterbecken / Segment row groups with count-derived in-operation area → **encoded**: `filterschichten` (BILD3 fills, `h_FK_lagen` = 1,00 m, `h_Draen_lagen`, GAK badge), `teilfilterbecken` (`A_F_gesamt`, `A_F_aktiv`, count, modulo-4 rest, per-basin 6 l/(m²·min) / 20 l/m²), `filtersegmente` (`V_segment_soll`, count, under-sized count); the register ↔ scalar pairs are D-1 … D-5.
5. Klein-RBF: `carbonatschicht_vorhanden` toggles h_FK 0,25 → 0,2 and reveals the carbonate fields; allow `A_F_anteil_Aba < 1,0` when Gl. 2 passes → **toggle encoded** (`M187-22-D5`; the 0,2 m IS printed at L930 — the inventory's "not found" residue is cleared, J-4); revealing `h_FK_CaCO3` / `CaCO3_massenanteil_carbo` is refused (consumed, C-3); the A_F < 1,0 % permission is `A_F_anteil_calc` + the STAGED G-6 (`>= 1.0 OR ok_boolean == True`), REQ-06 keeps forcing `== 1.0` until ratified.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m187` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m187 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+47 blocks, +4 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 12)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m187.ts` (86 KB incl. the 34 KB `Q` span block), `src/lib/eval/field-configs/m187.ts`, `src/lib/eval/field-configs/m187.prior.json` (captured), `src/lib/eval/equations/m187.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m187.test.ts`, `src/lib/eval/__tests__/field-configs-m187.test.ts`, `src/lib/eval/__tests__/equations-m187.test.ts`, `src/components/worksheet/__tests__/register-m187-schichten.test.tsx`, `scripts/verification/m187-STAGED-plan3-rulings.sql` (29 blocks, every line a comment), `scripts/migrations/20260917101200_regulation_tables_seed_m187.sql` + `scripts/rollback-20260917101200-regulation-tables-seed-m187.sql`, `scripts/migrations/20260917101210_field_configs_m187.sql` + `scripts/rollback-20260917101210-field-configs-m187.sql`, `scripts/migrations/20260917101220_equations_m187.sql` + `scripts/rollback-20260917101220-equations-m187.sql`, this report (+ the copy at `.superpowers/sdd/…/task-12-report.md`).
Untouched: every other standard's file; the M187 harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging); the probes `scripts/_probe_m187.ts` / `_probe2_m187.ts` deleted.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **15 compliance rows, not 21** (capture; I-3); 4 equations (2 unique) and 139 fields / 25 worksheets as stated.
2. **`S5_LIMITS` split into three key-shape tables** (`S5_LIMITS_P` / `S5_LIMITS_SPUR` / `S5_LIMITS_APP`): a `lookup_fill` needs a `from_symbol` per key column and the brief's `'-'` variant token of the three non-variant applications has none; `S5_2_3_1_SPUR` and `S5_LIMITS_SPUR` are the same table (one printed "Zu x)" paragraph per variant carries everything).
3. **The fills sit where their keys are**: P on M187-06 (its own `verfahrensvariante_p` + inherited `sonderanwendung`), Spurenstoffe on M187-11, Mikroorganismen on M187-16 — the brief's M187-01 holds neither variant driver; `h_FK_min` for Klein-RBF is a derivation (`M187-22-D5`) over one table row, not a `lookup_fill` (the toggle changes the value, and a second table keyed on the toggle would duplicate the printed constants).
4. **The "0,2 m mit Carbonatschicht" claim IS in the transcript** (L930 "In diesem Fall kann die Filterstärke auf $h_{F K} 0,2 \mathrm{~m}$ verringert werden.") — the brief's `m187-U-1` "NOT found" premise is refuted and reported as J-4 (R-5); `m187-U-1` is used for the Tab.-2 unit OCR instead (amendment F) and U-2 for the Klimakennung "KSO" / "KAO" cells.
5. **`B_CSB_calc` divides by `A_F_gesamt`** — L792 "bezogen auf die Gesamtfilterfläche", not the brief's active area; the per-basin figures (6 l/(m²·min), 20 l/m²) are per row ("jeweils beschickte Filterfläche"), not Σ.
6. **`daten_vorhanden` is a ja / nein select_one**, not a boolean attestation (booleans are not formula inputs; the STAGED gate reads the string); `stoffstromtrennung` is the created boolean attestation for L765, visible above 3.000 mg/l — the brief's `sickerwasser_in_rbf` visibility was NOT emitted (the prohibition is unconditional).
7. **Field `visible_when`: 30 (brief: 10)** — 3 on the only consumer-free existing inputs that a branch governs + 27 on created fields; the brief's `UV_dosis` / carbonate fields / `B_CSB` / `A_F_pro_AEb` are consumed (C-3 … C-5). **Section rules: 0** (C-2; prod's self-consumers, X-3, make every field-bearing section a producer section).
8. **Equations 24 new (brief 6 + 1 staged)**: added `ebct_min_stufe`, `h_Draen_lagen`, `schichten_gak_verletzungen`, `segmente_unterdimensioniert`, `A_F_segmente`, `organismen_count`, `A_F_aktiv`, `teilfilter_count`, `teilfilter_rest`, `elemente_count`, `A_F_anteil_calc`, `h_FK_min_klein`; the brief's `segmente_count` / `A_F_gesamt` / `A_F_min_ohne_daten` / `B_CSB_calc` / `h_FK_SS_calc` / `A_F_sum_klein` / `A_b_a_sum_klein` / `elemente_unter_1m2` / `log_red_min` kept; `V_segment_soll` carries the l/s → m³/d conversion (F-1).
9. **`sorptionsstufen`** carries `v_filter_ab` and per-row EBCT / v badges (the brief's `ebct_min`, `h_fk_ss`, `v_filter`); the boundary v = 5,0 m/h reads "nein" under the printed "<" (J-6).
10. **TABELLE3 is transposed** (keyed `variante_p`, eight string columns) and exposed as eight text fills on M187-06 — the brief's `kriterium × variante` rows would need a key literal per fill.
11. **Tab. 4 / 5 keyed `gegenstand`** (the two printed row heads), not `kennung`.
12. **Sign-off ids 47 (brief ≈ 12)**: the brief's ids kept where they exist (G-1 … G-6, R-1, U-1 re-purposed per item 4, J-1, X-1, X-2); every further refused / withheld / read item got its own block (G-7 … G-11, C-1 … C-5, D-1 … D-8, O-1 … O-3, U-2, J-2 … J-6, F-1 … F-5, I-1 … I-3, X-3).

## 9. Self-review

- Every `verbatim_quote` passes the verifier (69/69); every seeded cell is asserted inside its row's span at build time (`inSpan`); every quoted fragment on the 47 sign-off blocks was machine-checked against the transcript at its cited line (a scratch checker, ellipsis-aware: 60 fragments, 0 fail) — the STAGED file's evidence lines render the LaTeX for readability and say so in their header.
- The emitter guards accepted all 51 entries and 24 equations first time because the rule set was planned against the capture (consumer lists per field, the four equations, section codes); the field-config test pins the refusals (UV_dosis, B_CSB, eight section rules), the consumer lists of every withheld field, the branch switch's five-worksheet reach, the self-consumer count (86) and that every lookup_fill's keys are symbols on the fill's own worksheet (own or inherited per capture).
- G-A3 pinned: every table key column equals the driving prod enum tokens (or the created select's options).
- The runtime facts were established through the real register contract before the pins; the render test drives the BILD3 row picker and the GAK badge through `RegisterEditor` and `evaluateFormula`.
- Prod facts used in the STAGED file (15 gate ids + md5, 4 equation ids + md5, column lists, `standards.version`, VR strings, labels, `is_required`) come from the in-session read-only queries quoted in §0; no long cell was retyped (archive pattern).
- No `String.replace` with a replacement string on any repo file: the seed module was assembled from a body file and the generated quote block with `split/join` in a script file; every later edit used `split/join` with a presence check that throws before writing; the generator scripts live in the scratchpad; the probes were deleted.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns three files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **The branch switch cannot hide worksheets by data today**: `sonderanwendung` reaches five worksheets (C-1) and every field-bearing section holds a consumed producer (C-2) — the §5 win 1's "shows only the matching worksheets" is 100 % STAGED; what IS delivered is per-field visibility on created fields plus the three consumer-free inputs.
2. **Prod's self-consumer artefact (X-3, 86 of 139 fields)** blocks visibility UPDATEs on the very inputs the branches should hide (B_CSB, A_F_pro_AEb, KBE / MPN / PBE, …); the hygiene fix is one `array_remove` UPDATE, staged.
3. **The three contradictory REQs keep firing** (REQ-03 ≤ 0,03 vs REQ-04 == 0,01 on the same worksheets; REQ-02 h_FK ≥ 1,0 on every P project) until G-1 is ratified; the fills are visible twins only.
4. **REQ-05 / REQ-06 sit on P-Rückhalt worksheets** (-08 / -09) reading org-load / Klein-RBF symbols (X-2) — G-2 / G-6 / R-1 move them; until then a Klein-RBF project is gated on M187-09.
5. **`h_FK_min_klein` / `A_F_anteil_calc` / `B_CSB_calc` / `A_F_min_ohne_daten` / `teilfilter_rest` are scalar-only** (I-2): the STAGED gates on them evaluate on the form / report, not from a materialised value.
6. **`filtersegmente.flaeche_m2` is required** (a null cell would make `A_F_segmente` undecidable) — an engineer who only knows the volumes must enter areas; say so in the note if the owner prefers optional.
7. **Bundle growth:** ~34 KB of lifted spans (`Q`, 97 spans) ride in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
8. **Token/time:** ≈ 0.62 M tokens in, ≈ 75 min wall-clock — in the Task 10 / 11 range (0.57 M / 0.65 M).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The Tab.-3 comparison as a decision aid (seeded + eight text fills; no picker); §5.1.3.2 a) Carbonatbrechsand purity parameters (L507 Korngrößenverteilung, CaCO3 der Fraktionen, Zn / Cd / Cu / Pb / Ni / Cr / Fe) and §5.1.4.2 monitoring parameters (L564) as checklists; §5.1.3.4 Bandmischer / verdichtungsfreier Einbau / Fremdüberwachung (L540–L544); §5.2.4.2 the fourteen Leitparameter (L637) as a monitoring register; §5.2.4.1 the 24 h rotation as a schedule (only the day label per segment); §5.3.4.3 the frachtbezogene Wirkungsgrad with flows per event (F-5); §5.4.4 Verteilungsnetz ≤ 1 m²/Austrittsöffnung (seeded; the existing `austritts_dichte` input is self-consumed — no fill); §5.4.5 Flächenreinigung / Sedimententnahme / Wasserspiegelmessung (L835–L843); §5.5.3.2.2 Einlauf (geschlitzte Kantsteine), Einfriedung (L918), Abdichtung alternatives (L946), Überstau / Rückstau on Frei- / Grün- / Verkehrsflächen (L950); §5.5.5 Wartungsintervalle (L976); §6 Klimakennung (seeded, J-1); Anhang A (Rheinbach figures — Bild A.1–A.4 are images). Each is on the sign-off sheet with its line or listed here.
