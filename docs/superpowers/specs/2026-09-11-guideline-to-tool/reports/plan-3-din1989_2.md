# Plan 3 — DIN-1989-2 (`din1989_2`) — Task 17 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 18)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (raw `claude --version` output "2.1.260 (Claude Code)"; no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `f008ea0`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DIN-1989-2 din1989_2` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 4 worksheet titles, the 15 section titles, the 9 equation rows with ids / formulas / md5 / status / input_symbols, the 17 compliance rows with ids / severities / conditions / md5 / titles, labels / units / `is_required` / `validation_rules` of all 42 fields, the `information_schema` column lists of `equations` / `compliance_requirements` / `fields`). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-1989-2\DIN-1989-2.md` (927 lines, mathpix LaTeX; the transcript was read end-to-end at L1–L928). The inventory was used for pointers only (all its line pointers were correct). The `.pdf` / `.xlsx` / `_encode_report.md` / `_fidelity_audit.md` beside the transcript were NOT opened (not sources).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `din1989_2` | 5 | 19 (19/19 verbatim) | 0 rows skipped; 1 table kept `imported_unverified` (TAB1 — the first Funktionsprinzip column head is an IMAGE at L271, U-1; the printed "-" cell L273 is not a row); 4 tables `md_verified` | 3 (all `create`: `behaeltnisse` -02, `prueflaeufe` -03, `pruefstoffe` -03) | 1 (`create`: `sedimentationsvolumen`) | 1 (`create` json: `kennzeichnung`, 9 lifted items a–i) | 1 (`create`, enum twin `filtertyp_tab1` ← TAB1 by `funktionsprinzip` × `sedimentationsvolumen`; the existing `filtertyp` re-bind is STAGED E-1) | 29 (6 UPDATE on consumer-free, gate-free existing fields + 23 on created fields) | 0 (every candidate section holds a consumed or gate-read field) | 23 (-01 D1; -02 D1 … D5; -03 D1 … D17) | 0 replacements emitted; 4 R-blocks (R-1 Gl. 1/2, R-2 Gl. 7–9, R-3 Gl. 4/6, R-4 Gl. 3/5) + 7 gate blocks (G-1 … G-7) + 12 D-pairs STAGED | 32 (+5 observations) | ≈ 0.55 M (tool-marker delta 15.00 M → 14.45 M at report time) | ≈ 60 |

Field entries: 38 = 32 `create` (3 registers, 1 select_one, 1 select_many, 1 lookup_fill, 3 attestations / booleans, 1 number input `V_pruefmedium_l`, 23 derived outputs — 1 fewer than "23 derived" because `V_pruefmedium_l` is an input; derived-output fields = 23, one per equation) + 6 UPDATE (`rueckhalteraum_zugaenglich`, `temperaturbestaendig_fallrohr`, `V_Pruef_leist`, `V_Pruef_trenn`, `eta_Verw`, `werkstoffbezeichnung` — visibility only). Migration `20260917101710`: 32 `INSERT … WHERE NOT EXISTS`, 6 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 6 UPDATE rows to the captured NULLs and deletes only the 32 `Plan 3:` rows. No `enum_values` is written by any UPDATE (D-1). The emitter printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — the capture has none); default (refuse) mode, no `--gate-guard=warn`. No `reference` widget (the brief's `q_max_en12056` — X-1), no DN `select_one` (J-1), no S-block (no `select_many` on an existing field).

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  314 passed | 1 skipped (315)
      Tests  2893 passed | 1 expected fail | 1 skipped (2895)
   Start at  13:47:18
   Duration  51.25s (transform 14.95s, setup 53.27s, import 123.43s, tests 52.66s, environment 210.11s)
```
(2867 at `f008ea0` + 26 from this task: seed-din1989-2 6, field-configs-din1989-2 6, equations-din1989-2 11, register-din1989-2-prueflaeufe 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → first run: 2 warnings (`L227` / `L382` assigned but unused in `field-configs/din1989_2.ts`) → both cues wired into the entries they belong to (`V_Pruef_leist` / `V_Pruef_trenn` ← L227, `pruefstoffe` ← L382), field-config migration re-emitted → exit 0, no output.

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/din1989-2-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  18 passed (18)
   Start at  13:48:16
   Duration  5.55s (transform 772ms, setup 0ms, import 3.67s, tests 1.70s, environment 0ms)
```
The harness seeds prod's 9 equations / 17 gates (`tests/harness/seed-din1989-2.ts`); the new rows are not in it by construction — it proves the branch still drives every DIN-1989-2 block gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Seed builder → verifier **19/19** on its first run → seed test RED on one of my own pins (`makeTableLookup(…)` returns the row's VALUES object, not a cell — I had asserted a cell; pin corrected, code untouched) → 6/6. Runtime facts were established BEFORE the equation pins with one scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` on the seeded tables (`scripts/_probe_din1989_2.ts`, deleted): the printed Anhang A / C / D figures reproduce (0,7 · 0,8 / 0,7 / 0,75 · 1,0 / 0 / 0,5 · 1 / 1 / 1); `if(x IS NULL, 0, x)` inside `sum_rows` keeps the Verwurf Σ decidable on Typ-A rows (0); a row whose `V_pruefmedium_l` is unset has a null `masse_soll` and the Σ is `manual_required` ("Unbekanntes Symbol"); an empty register and a `min_rows` with no matching step are `manual_required`, never 0; `count_rows` over an empty register is 0; an unset boolean cell reads `false` in `belastet == false`; `max_rows(reg, x, x IS NOT NULL)` skips rows without the optional depth; a depth typed while the column is hidden stays a stored cell (a262e trap 2); `DN <= 200` / `DN > 200` / `IN {…}` / `== true` conditions pass, fail and pend as expected (missing driver ⇒ pending ⇒ visible). Equations test → **11/11 first run**. Field-config module: the emitter accepted all 38 entries first time (the rule set was planned against the capture's consumer lists, the 9 equations and the 17 gates); the test had three REDs on my own pins (29 rules not 25; an un-escaped regex parenthesis; the -02 C section is refused by the PRODUCER guard first — `Q` is consumed by -03 — not by the gate guard) → 6/6. Render test → **3/3 first run**. Typecheck green on the first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts din1989_2 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-1989-2\DIN-1989-2.md"` → **`19/19 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- TAB1 (5): fremdstoffrueckhalt|gross, fremdstoffrueckhalt|klein (one span L273); fremdstoffableitung|gross, |klein, |keines (one span L274)
- TAB2 (7): p100 … p1 — L495 … L501
- TAB3 (3): ldpe_folie L572 · pp_kugeln L573–L583 (multi-line cell) · quarzsand L584–L587
- TAB4 (2): k125_250 L606 · gt250 L607
- TAB5 (2): werkstoff L735 · masse L736

Status per table: `md_verified` — TAB2, TAB3, TAB4, TAB5 (every printed row lifted, every cell — numeric and text — legible; the TAB3 footnote a L588 and the `${ }` token are quoted as printed). `imported_unverified` — TAB1 (U-1: the "großes Sedimentationsvolumen" column head is an `![](…)` image at L271; the five TYP cells are clean).

Beyond the verifier (which checks table rows only), a scratch checker (deleted) asserted every `verification_quote` of the two modules, every `override_quote` of the five tables and every `"…" (L…)` fragment of the sheet section and the STAGED file against the whitespace-normalised transcript: **171/171 verbatim** after one fix (the `behaeltnisse` quote had joined four spans with spaces although L315 lies between L313 and L317 — now " — "-joined).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, valid_from, left(title_de,60) AS title FROM standards WHERE code LIKE 'DIN-1989-2%'"
  → DIN-1989-2 · '2004 (DIN 1989-2)' · issued_year null · valid_from null
node scripts/regulation-tables/build-prior-snapshot.mjs DIN-1989-2 din1989_2
  → wrote src\lib\eval\field-configs\din1989_2.prior.json: 42 field rows (0 orphan), 15 coded sections of 15, 9 equations, 17 gates (0 unparseable);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts din1989_2
  → wrote seed + rollback for din1989_2 -> scripts/migrations/20260917101700_regulation_tables_seed_din1989_2.sql scripts/rollback-20260917101700-regulation-tables-seed-din1989-2.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts din1989_2 20260917101710
  → wrote 38 field entries + 0 section entries for din1989_2 -> scripts/migrations/20260917101710_field_configs_din1989_2.sql scripts/rollback-20260917101710-field-configs-din1989-2.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts din1989_2 20260917101720
  → wrote 23 equations for din1989_2 -> scripts/migrations/20260917101720_equations_din1989_2.sql scripts/rollback-20260917101720-equations-din1989-2.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-din1989-2.test.ts` / `equations-din1989-2.test.ts` pin the other two against the committed `din1989_2.prior.json`, default refuse mode). The seed migration carries 19 row upserts (`ON CONFLICT (table_id, row_key)`) and 4 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the five tables (no earlier DIN-1989-2 table seed exists — nothing superseded). The equations rollback deletes the 23 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101700` → `20260917101710` → `20260917101720`; rollback in reverse. Every file touches `DIN-1989-2` rows only. Scalar-only rows (-01 D1, -02 D1 / D2, -03 D1 … D4) are not server-materialised (amendment D — expected, noted once here and as I-2); the 17 register-fed rows (-02 D3 … D5, -03 D5 … D17) materialise on save.

## 5. What was encoded

**Tables (5)** — `src/lib/eval/regulation-tables-seed-din1989_2.ts`, builder `din19892SeedTables()`, edition `'2004'` (prod `standards.version` "2004 (DIN 1989-2)"; the title page L1–L13 prints no date — I-1):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB1 | `filterart` (= prod `funktionsprinzip` tokens fremdstoffrueckhalt / fremdstoffableitung, the printed row heads) × `sedimentationsvolumen` (gross / klein / keines = the created select) | `typ` (prod `filtertyp` tokens typ_a / typ_b / typ_c), `typ_gedruckt` (TYP A / B / C) | locked (L263 "sind Filter den in Tabelle 1 aufgeführten Typen zuzuordnen") | 5 (the "-" cell L273 is not a row) | imported_unverified (U-1) |
| TAB2 | `stufe` p100 / p50 / p20 / p10 / p5 / p2_5 / p1 | `q_pct` 100 / 50 / 20 / 10 / 5 / 2,5 / 1 [%], `pruefzeit_min` 2 / 2 / 3 / 4 / 8 / 8 / 8 [min] | locked (L479 "über eine jeweils festgelegte Dauer angeströmt") | 7 | md_verified |
| TAB3 | `pruefstoff` ldpe_folie / pp_kugeln / quarzsand | `stueck` 15 / – / –, `masse_g` 150 / 150 / 200, `konzentration_g_l` 0,15 / 0,15 / 0,20 (Σ 0,50), `bezeichnung`, `masse_anteile` ("75 / 75" for PP), `fussnote` (a: 10 g/Stück, L588) | locked (L559 "muss Tabelle 3 entsprechen") | 3 | md_verified |
| TAB4 | `kornklasse` k125_250 / gt250 | `anteil_pct` 0 / 100, `kornklasse_gedruckt` | locked (L593 "sind nach Tabelle 3 und Tabelle 4 zu wählen") | 2 | md_verified |
| TAB5 | `merkmal` werkstoff / masse | `pruefgegenstand`, `anforderung_nach`, `pruefung`, `haeufigkeit` (printed text) | locked (L728 "muss Tabelle 5 entsprechen") | 2 | md_verified |

**Registers / selections / lookup_fill / visibility** — `src/lib/eval/field-configs/din1989_2.ts`:
- `sedimentationsvolumen` (-01 B, create enum) `select_one` over the three printed Tab.-1 column heads (the first from the sibling head + §5.3.2, U-1); `filtertyp_tab1` (-01 B, create enum) `lookup_fill` role value ← TAB1 by (`funktionsprinzip`, `sedimentationsvolumen`), enum_values = the three prod tokens labelled "TYP A/B/C" — the twin of the existing `filtertyp` (E-1); `filtertyp_konsistent_code` (derived, -01 D1).
- `behaeltnisse` (-02 C, create json, `visible_when filtertyp == 'typ_b'`): label, `volumen_l` (required), `masse_gefuellt_kg` (required) + badge `≤ 20 kg`, `grifftiefe_cm` (row `visible_when einbausystem == 'separat_erdeinbau'`) + badge `≤ 60 cm`, `ohne_werkzeug` boolean (L309); footer D3 / D4 / D5; note L309 + L311 + L313. Outputs `behaeltnis_volumen_sum` / `behaeltnis_masse_max` (typ_b) / `behaeltnis_grifftiefe_max` (typ_b AND Erdeinbau). `v_rueck_a_min` (typ_a) / `v_rueck_b_min` (typ_b) as the Gl.-1 / Gl.-2 minima beside the typed provided volumes. `einbau_fallrohr` (create boolean attestation) → `temperaturbestaendig_fallrohr ← einbau_fallrohr == true` (L256). `rueckhalteraum_zugaenglich ← filtertyp IN {'typ_a', 'typ_b'}` (L292 AND L329 print the sentence).
- `prueflaeufe` (-03 C, create json): `stufe` lookup_key TAB2 → `q_pct` / `t_min` lookup_values, `q_soll = Q_Zu_max * q_pct / 100` (worksheet scalar, G-13), `belastet` boolean (unset = 6.4.5), `q_zu` / `q_ab` (required), `eta_row = (q_zu - q_ab) / q_zu` (Gl. 3 / 5); footer `prueflaeufe_count`, `eta_hydr_unbel_p100` / `_min`, `eta_hydr_bel_p100` / `_min` (the last two `visible_when DN <= 200`, §5.4.3).
- `V_pruefmedium_l` (-03 B, create number, `DN <= 200`); `pruefstoffe` (-03 D, create json, `DN <= 200`): `stoff` lookup_key TAB3 → `konz` / `stueck` / `fussnote` lookup_values, `masse_soll = konz * V_pruefmedium_l`, `stueck_soll = if(stueck IS NULL, 0, stueck * V_pruefmedium_l / 1000)`, `masse_speicher_g` (required), `masse_verwurf_g` (optional, row `visible_when filtertyp == 'typ_c'`, L631); footer D10 … D15; note L631 + "LDPE: Stücke × 10 g". Outputs `m_ges_festst_calc` / `m_sp_verunr_calc` / `eta_rueck_calc` / `filtertrennwirkung_code_ab` (DN ≤ 200; the code additionally typ_a/b), `m_verw_calc` / `eta_verw_calc` / `eta_c_calc` / `filtertrennwirkung_code_c` (typ_c AND DN ≤ 200). `v_pruef_leist_min` / `v_pruef_trenn_min` / `v_pruef_leist_ok` / `v_pruef_trenn_ok` (-03 B, DN ≤ 200). `hersteller_verfahren_dokumentiert` (create attestation, `DN > 200`, L228 / L403).
- Field `visible_when` on existing fields (6, all consumer-free, no same-worksheet gate reads them, none feeds a consumed equation — pinned): -02 `rueckhalteraum_zugaenglich`, `temperaturbestaendig_fallrohr`; -03 `V_Pruef_leist` / `V_Pruef_trenn` ← `DN <= 200` (L403: the 6.4.6 / 6.5 tests do not apply above DN 200), `eta_Verw` ← `typ_c` (Gl. 8 output, feeds nothing); -04 `werkstoffbezeichnung` ← `werkstoff_filterelement == 'kunststoff'` (pending until C-1). Refused and STAGED: `V_Rueck_A` (CR-03, G-1), `V_Rueck_B` / `behaeltnis_masse` / `tiefe_gok_griff` (CR-04/05/06, G-2), `standsicherheit_eingehalten` (CR-10, G-7), `filtertrennwirkung_nachgewiesen` (CR-08, G-4), `eta_Rueck_AB` / `eta_C` / `eta_hyd_bel` / `eta_hydr_bel_doku` and `m_ges_festst` / `m_sp_verunr` / `m_verw` (producer guard, C-2). Section rules: none (-02 C holds Q consumed by -03 and four gate inputs; -03 D holds consumed outputs and the CR-08 boolean).
- `kennzeichnung` (-04 A, create json) `select_many` over the nine printed §7 items a)–i) (L692–L700), note L698.

**Equations (23 new)** — `src/lib/eval/equations/din1989_2.ts`: `-01-D1 filtertyp_konsistent_code = if(filtertyp == filtertyp_tab1, 1, 0)` · `-02-D1 v_rueck_a_min = Q * 25` · `-D2 v_rueck_b_min = Q * 2` · `-D3 behaeltnis_volumen_sum = sum_rows(behaeltnisse, volumen_l)` · `-D4 behaeltnis_masse_max = max_rows(…, masse_gefuellt_kg)` · `-D5 behaeltnis_grifftiefe_max = max_rows(…, grifftiefe_cm, grifftiefe_cm IS NOT NULL)` · `-03-D1 v_pruef_leist_min = Q_Zu_max * 90` · `-D2 v_pruef_trenn_min = Q_Zu_max * 180` · `-D3 v_pruef_leist_ok = if(V_Pruef_leist >= Q_Zu_max * 90, 1, 0)` · `-D4 v_pruef_trenn_ok = …180…` · `-D5 prueflaeufe_count = count_rows(prueflaeufe)` · `-D6 eta_hydr_unbel_p100 = min_rows(prueflaeufe, eta_row, belastet == false AND stufe == 'p100')` · `-D7 eta_hydr_bel_p100 = … belastet == true …` · `-D8 eta_hydr_unbel_min = min_rows(prueflaeufe, eta_row, belastet == false)` · `-D9 eta_hydr_bel_min` · `-D10 m_ges_festst_calc = sum_rows(pruefstoffe, masse_soll)` · `-D11 m_sp_verunr_calc = sum_rows(pruefstoffe, masse_speicher_g)` · `-D12 m_verw_calc = sum_rows(pruefstoffe, if(masse_verwurf_g IS NULL, 0, masse_verwurf_g))` · `-D13 eta_rueck_calc` (Gl. 7, Σ inline) · `-D14 eta_verw_calc` (Gl. 8) · `-D15 eta_c_calc` (Gl. 9) · `-D16 filtertrennwirkung_code_ab = if(<Gl. 7> >= 0.7, 1, 0)` · `-D17 filtertrennwirkung_code_c = if(<Gl. 9> >= 0.7, 1, 0)`. None outputs a prod symbol (Gl. 1 … 9 keep their verified rows); no -03 row names an inherited scalar (`filtertyp`, `DN`) as an input (I-2); no twin is chained on another new output. **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): Q = 5 → 125 / 10 l; Q_Zu,max = 5 → 450 / 900 l, 450 ≥ 450 ⇒ 1, 449 ⇒ 0, 800 < 900 ⇒ 0; containers 30 + 25 = 55 l, max 22 kg, max depth 55 cm, no depth ⇒ manual_required; steps (10, 2) / (5, 0,5) / (10, 3 bel.) / (2, 0,8 bel.) / (1, 0,05) → count 5, η p100 0,8 / bel. 0,7, min 0,8 / bel. 0,6, only-p50 ⇒ p100 manual_required; Tab. 3 at 1000 l → 150 / 150 / 200 g (Σ 500 g), 15 Stück, at 2000 l doubled; **Anhang A** (Σ 10, Sp 3) → η_Rück 0,7 ⇒ code_ab 1, Sp 3,02 ⇒ 0; **Anhang C** (10, 2, 7) → 0,8 / 0,7 / 0,75 ⇒ code_c 1; **Anhang D** (10, 0, 0) → 1,0 / 0 / 0,5 ⇒ 0 and (10, 0, 10) → 1 / 1 / 1 ⇒ 1; every no-row / no-V branch manual_required, never 0.

## 6. Inventory §5 top-wins walk

1. `filtertyp` gates everything (show only the Typ-A / B / C fields; Gl. 7 vs 8/9) → **partially**: the created Typ-B register + outputs, the Gl.-1 / 2 minima, `rueckhalteraum_zugaenglich` and `eta_Verw` switch on `filtertyp`; the Typ-C branch (Gl. 8 / 9 twins, Verwurf Σ, code_c) and the A/B branch (code_ab) are visible per type; the prod inputs behind CR-03 … CR-06 and the consumed outputs are STAGED (G-1, G-2, C-2) because every one of them is gate-read or consumed — B/C filters still fail the A-checks until the IF-guards land. ✓ (guards STAGED, as the brief foresaw)
2. DN as an enum with the EN 12056-3 Tab. C.1 lookup; one `Q_max` → **partially**: no DN enum (the sizes are not printed — J-1) and no lookup (external table — content boundary); the scope rules read `DN <= 200` / `DN > 200` directly; `Q` ≡ `Q_max` ≡ `Q_Zu_max` is X-1 with a consolidation proposal; the brief's `reference` widget is not the codebase widget (X-1).
3. Test steps as repeatable rows (Tab. 2 %, Q_Zu, Q_Ab, t) producing the η-curve and the documented η values → **encoded** (`prueflaeufe`, Tab.-2 fills, η per row, p100 / min twins per state; which step is documented is J-2; Gl. 3 / 5 re-point STAGED R-4; the -02 re-typed Doku copies D-8 / D-9). ✓
4. Derive the required `V_Rueck` minima and compare with the provided volume; derive `filtertrennwirkung_nachgewiesen` from η ≥ 0,7 and the DN ≤ 200 rule → **encoded**: `v_rueck_a_min` / `v_rueck_b_min` (R-1 retires the prod rows that write into the inputs), `filtertrennwirkung_code_ab` / `_c` (D16 / D17, materialised) under `DN <= 200`, `hersteller_verfahren_dokumentiert` for DN > 200; the CR-08 rewrite is G-4 / D-2. ✓
5. Prüfstoffe rows from Tab. 3 (mass per 1000 l × Prüfmedium) with recovered masses per Stoff feeding Gl. 7–9 → **encoded** (`pruefstoffe`, `V_pruefmedium_l`, Σ twins D10 … D12, η twins D13 … D15 inline; the Gl. 7–9 re-point is R-2, the scalar pairs D-10 … D-12). ✓

Data-quality gaps of the inventory: the three `Q` symbols (X-1); `V_Rueck_A/_B` mixing required and provided (R-1: twins carry the minima); CR-03 … 06 / 08 / 10 not conditioned (G-1 / G-2 / G-4 / G-7); `werkstoffbezeichnung` VR unconditional and dead (G-3 + C-1); Tab. 1 header image (U-1); `DN` unit "DN" as number (J-1 — stays).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`din1989_2` line + next example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (din1989_2 lines + next example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+32 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 17)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-din1989_2.ts`, `src/lib/eval/field-configs/din1989_2.ts`, `src/lib/eval/field-configs/din1989_2.prior.json` (captured), `src/lib/eval/equations/din1989_2.ts`, `src/lib/eval/__tests__/regulation-tables-seed-din1989-2.test.ts`, `src/lib/eval/__tests__/field-configs-din1989-2.test.ts`, `src/lib/eval/__tests__/equations-din1989-2.test.ts`, `src/components/worksheet/__tests__/register-din1989-2-prueflaeufe.test.tsx`, `scripts/verification/din1989_2-STAGED-plan3-rulings.sql` (21 ☐ blocks — G-1 … G-7, E-1, R-1 … R-4, C-1, C-2, D-1, D-2, D-3…5, D-6…9, D-10…12, X-1; every line a comment), `scripts/migrations/20260917101700_regulation_tables_seed_din1989_2.sql` + `scripts/rollback-20260917101700-regulation-tables-seed-din1989-2.sql`, `scripts/migrations/20260917101710_field_configs_din1989_2.sql` + `scripts/rollback-20260917101710-field-configs-din1989-2.sql`, `scripts/migrations/20260917101720_equations_din1989_2.sql` + `scripts/rollback-20260917101720-equations-din1989-2.sql`, this report (+ the copy at `.superpowers/sdd/…/task-17-report.md`).
Untouched: every other standard's file; the DIN-1989-2 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the two scratch scripts (`_probe_din1989_2.ts`, `_check_quotes_din1989_2.ts`) deleted.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **`reference` widget for `q_max_en12056`:** the codebase `reference` (`field-config.ts` `referenceUi`, `ReferenceField`) is a carrier-row picker — `parseFieldConfig` rejects the brief's `{ document, table, note }` ui_config, and the emitter validates every entry through it. No fourth Q symbol was created (amendment K: bind to the existing input); Gl. 1 / 2 twins read `Q` (-02), Gl. 4 / 6 twins read `Q_Zu_max` (-03, the input the prod rows already read). Recorded as X-1 with the three-symbol consolidation proposal.
2. **DN `select_one` (`dn_nennweite`) and the `dn_le_200` attestation:** neither created — the transcript prints only "DN 200" as a threshold (J-1); `DN` is inherited on -02 / -03, so every scope rule reads `DN <= 200` / `DN > 200` directly (a missing DN pends = visible). The brief's "Filtertrennwirkung block ← dn_nennweite_le200 == true" became 18 `DN <= 200` rules (17 on created fields + `V_Pruef_leist` / `V_Pruef_trenn`) plus the `DN > 200` attestation.
3. **`filtertyp` lookup_fill (brief's D-1: manual → filled):** an UPDATE on the existing enum is a widget re-bind of a consumed (-02 / -03), gate-bearing (CR-01) input → twin `filtertyp_tab1` + STAGED E-1 (amendment J). TAB1's first key is named `filterart` (the printed row head) but keyed by the prod `funktionsprinzip` tokens (G-A3), not the brief's `rueckhalt` / `ableitung`.
4. **Gl. 7 vs 8/9 switch:** the brief's `filtertrennwirkung_code = if(filtertyp == '<typ_c>', …)` would name the inherited `filtertyp` as an input — unresolvable in the save-path materialiser (I-2) and, for a Typ-A filter, `eta_C` is blank (the "nominal case needs no blank inputs" rule). Encoded as three register-only twins mirroring Gl. 7 / 8 / 9 one-to-one and two per-type codes; the switch lives in `visible_when` and in the STAGED CR-08 rewrite (G-4).
5. **`V_Rueck_A_min` / `V_Rueck_B_min` on one -02-D1:** two rows (D1 / D2); the -03 minima are `v_pruef_*_min` (D1 / D2) plus the printed-"≥" checks `v_pruef_*_ok` (D3 / D4, G-6) — the brief's `V_Pruef_*_calc` names.
6. **η twins:** the brief's `sum_rows(prueflaeufe, if(belastet == false AND stufe == 'p100', eta_row, 0))` reads 0 when no p100 row exists (phantom pass) — encoded as `min_rows(prueflaeufe, eta_row, <cond>)` (manual_required without a match, conservative on duplicates) plus the `_min` twins per state (J-2).
7. **`m_verw_calc`:** `sum_rows(pruefstoffe, if(masse_verwurf_g IS NULL, 0, masse_verwurf_g))` — the plain `sum_rows(…, masse_verwurf_g)` of the brief is undecidable on Typ-A / -B rows where the column is hidden and empty.
8. **`rueckhalteraum_zugaenglich` ← `filtertyp IN {'typ_a', 'typ_b'}`** (brief: typ_a only) — L292 and L329 print the identical sentence under §5.3.2 AND §5.3.3 (observation).
9. **`behaeltnisse` columns:** `masse_gefuellt_kg` required (L311 applies to each container), `grifftiefe_cm` optional with the Erdeinbau row rule, two badges and the `ohne_werkzeug` boolean (L309) added; a fifth -02 equation `behaeltnis_grifftiefe_max` (D5) for the CR-06 re-point.
10. **`pruefstoffe` columns:** `stueck` / `fussnote` lookup_values and `stueck_soll` added (the LDPE 15 Stück per 1000 l; footnote a shown per row); `konz` is the Tab.-3 concentration column.
11. **Equations 23 new** (brief: 6 + 2 staged): -01 D1 consistency code, -02 D3 … D5, -03 D3 / D4 checks, D5 count, D6 … D9 four η twins, D10 … D17. Staged replacements are four (R-1 … R-4), not two; gate blocks seven (G-1 … G-7); D-pairs twelve.
12. **Sign-off ids 32** (brief ≈ 8): the brief's ids kept where given (G-1 … G-4, J-1 / J-2, X-1, R-1 / R-2, D-1 / D-2 — D-1 is the Kennzeichnung pair, the brief's "D-1 manual → filled" for `filtertyp` is E-1 per the corpus convention); extra items use the skeleton letters (G-5 … G-7, R-3 / R-4, C-1 / C-2, D-3 … D-12, U-1, I-1 / I-2).
13. **`din1989_2-G-3`:** the `werkstoffbezeichnung` VR is display-dead (validation_rules enforce nothing) — recorded as an optional NEW gate, not a rewrite.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (19/19); every `verification_quote`, `override_quote` and every `"…" (L…)` fragment on the sheet and in the STAGED file was machine-checked against the transcript (171/171) after being read at the cited line in this session; prod facts (enum tokens, consumer lists, section codes, gate ids / conditions / md5, equation ids / formulas / md5, `standards.version`, labels, column lists) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted all 38 entries and 23 equations first time because the rule set was planned against the capture (consumer lists, the 9 equations, the 17 gates, section codes) — the field-config test pins every refusal (G-1, G-2, G-4, G-7 via `gateReaders`; C-2 via `producerChain` chains; both section candidates), the IF-guard exemption G-4 relies on, G-A3 for TAB1 / TAB2 / TAB3 and the twin's enum, and that no emitted rule hides a consumed, gate-read or equation-feeding symbol.
- Runtime facts were established through the real register contract before the pins (probe deleted); the render test drives `prueflaeufe` through `RegisterEditor` with a `symbolLookup` for `Q_Zu_max` (Tab.-2 fills, Soll flow, η per row, locked ⇒ no toggle, re-fill on a step change, footer twins through `evaluateFormula`).
- One engine fact drove the design and is pinned + on the sheet (I-2): the materialiser's `symbolLookup` covers the template's own fields only — no -03 equation names `filtertyp` or `DN` (test: `input_symbols` never contain them).
- No `String.replace` with a replacement string on any repo file (splices via `split/join` with a presence check that throws before writing, or the Edit tool); the module files were written with the Write tool; the scratch scripts lived in the repo root only while running and were deleted.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section (+236 lines) and the playbook paragraph (+2 lines).

## 10. Concerns (for the controller)

1. **Every Typ-switch on a prod input is STAGED** — B/C filters still meet CR-03 … CR-06 only by typing something (G-1 / G-2); CR-08 still reads the manual boolean for every DN and type (G-4); CR-10 still binds integrated filters (G-7). That is the fail-safe reading of the gate-aware guard; the seven G-blocks are IF-guard rewrites the owner can apply one by one.
2. **CR-06's guard is compound** (`filtertyp == typ_b AND einbausystem == separat_erdeinbau`) — outside the emitter's plain-compare IF-guard exemption; G-2 Option A applies the field rule by hand, or the rule is reduced to `typ_b` (then the depth shows for integrated filters too). A compound-guard exemption is a [CODE] candidate.
3. **Nothing the registers compute reaches prod's gates yet** — the Σ / η twins and the codes are visible twins; Gl. 3 / 5 / 7 / 8 / 9 keep reading the typed scalars until R-2 / R-4 / D-6 … D-12 are ratified; Gl. 1 / 2 / 4 / 6 keep writing the minimum into the typed provided volumes until R-1 / R-3.
4. **Edition token `'2004'`** is a unique-key component; the owner confirms the printed edition on the PDF cover before `20260917101700` is applied (I-1).
5. **TAB1 `imported_unverified`** for the header image (U-1) — a 1-minute PDF look at p. 8 flips it.
6. **J-2 (which step documents η)** rests on the executor's reading (100 % as the §5.4.2 Bezugsgröße, the minimum as the conservative companion) — both pinned and on the sheet; R-4 waits on it.
7. **Bundle growth:** ~3 KB of lifted spans (Task-0 observation; Task 30 measures).
8. **Token/time:** ≈ 0.55 M tokens in, ≈ 60 min wall-clock — in the Task 10 / 16 range; the transcript is short (927 lines) but the gate/producer topology (four unguarded numeric gates on the same inputs the brief wanted hidden) pushed most of the value into STAGED blocks.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The test ORDER 6.4.4 → 6.4.5 → 6.5 → 6.4.6 (L463–L467) and the 5-min backwater-free run at Q_max (L475 — `querschnitt_nicht_eingeengt` is a boolean, no duration); the 100-cycle intermittent loading (30 % Q_Zu,max × 4 min + ≥ 10 min pause, L532–L534 — no cycle counter); the 30 % × 10 min feed and the 1-min flush of §6.5.2 (L627 / L629); the cleaning-system rule during 6.4.6 (L548 — cycle-dependent cleaning off, degree-of-soiling cleaning may stay on); the 125 µm / 250 µm sieves, 105 °C drying and 0,1 g weighing (L451–L455, L631 — test-lab equipment facts); Tab. 4's grain-size split as a check on the sand actually used (seeded, no field); the Baugruppen rule of §8.2 (L715 — variants of one Erstprüfung; `filter_id` is a single text); the 10-year record retention (L720 / L741 — `pruefberichte_aufbewahrung_jahre` exists, VR ≥ 10 dead); §9's Einbau-/Betriebsanleitung content (L747 / L749 — one boolean); the Anhang-E Prüfbericht form (L894–L928). Each is a bare reference, a procedural sentence or a text rule without a computable value and stays outside this encoding (content-boundary rule) or on the sign-off sheet.
