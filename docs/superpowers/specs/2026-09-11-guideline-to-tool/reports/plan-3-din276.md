# Plan 3 — DIN-276 (`din276`) — Task 13 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 14)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `e037ce6`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DIN-276 din276` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row — code / version / 29 worksheets / 544 active fields —, the 29 worksheet titles, the section codes / titles / field counts of DIN-276-09 and -18, the labels / units / `is_required` / descriptions of 40 fields (the free-text-with-options fields, the placeholders, the Kennwert copies), the 32 compliance rows with ids / severities / md5(condition) / conditions, the 4 IDENT equation rows with ids / md5 / status, the `information_schema` column lists of `compliance_requirements` / `equations`). `.env.local` never printed.
- **SR-1 source:** every seeded cell, every cue and every sign-off quote was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-276\DIN-276.md` (English translation of DIN 276:2018-12; 1548 lines, read end-to-end L1–L1548). The 671 row spans were lifted mechanically by line range (a throwaway generator in the scratchpad walked the `\hline NNN & …` rows tracking the nested-`tabular` depth; `JSON.stringify` per span), every seeded cell is asserted inside its span at build time (`inSpan`, fragment-wise for multi-line notes), the 52 cue spans `Q` were re-keyed to their real lines by a checker (52/52), and the 70 quoted fragments on the sign-off sheet were machine-checked against their cited lines (70/70). The inventory was used for pointers only. The `.pdf` / `.xlsx` siblings were not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `din276` | 4 | 671 (671/671 verbatim) | 0 rows skipped; 4 printed-blank / odd cells kept as printed (U-1 TABLE3 row 340 unit; U-2 TABLE4 440/0, 458/6, 474/3) → TABLE3 / TABLE4 `imported_unverified`, TABLE1 / TABLE2 `md_verified` | 14 (all `create`: `flurstuecke` -04, `kennwert_quellen` -07, `sonderkosten` -08, `kg1_positionen` … `kg8_positionen` -09 … -16, `kostenstufen_matrix` -18, `vergabeeinheiten` -21, `abweichungen` -27) | 1 (`create`: `kg_selector` -24) | 0 (the two Plan-1 checklists stay as they are — M-1 / M-2) | 3 (all `create`, text: `reference_unit_einheit` / `_bezeichnung` / `_ermittlung` -24 ← TABLE2 by `kg_selector`) | 1 (UPDATE `GK_mwst_satz_pct` -23 ← `vat_treatment IN {gross, mixed}`, pending until C-1) | 0 (M-1 / M-2 select_many drivers; every KG section holds roll-up producers, C-2) | 115 (-04 D1–D2, -07 D1, -08 D1–D6, -09 … -16 4 + one per printed second-level KG = 84, -18 D1–D11, -21 D1–D5, -24 D1–D4, -27 D1–D2) | 2 equation replacements (E-1 IDENT-04, E-2 IDENT-03) + 1 (D-3: 48 roll-ups) + 3 gate blocks (G-1 … G-3) | 44 (+5 observations) | ≈ 0.68 M (tool-marker delta 15.00 M → 14.34 M at report time) | ≈ 165 |

Field entries: 134 = 133 `create` + 1 UPDATE. Migration `20260917101310`: 133 `INSERT … WHERE NOT EXISTS`, 1 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the one UPDATE row to the captured NULLs and deletes only the 133 `Plan 3:` rows. Derived-output fields (`widget='derived'`): 115 — one per equation. No `enum_values` is written by any UPDATE (D-1; the only select is a `create`).

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  295 passed | 1 skipped (296)
      Tests  2739 passed | 1 expected fail | 1 skipped (2741)
   Start at  07:46:44
   Duration  45.93s (transform 13.31s, setup 48.28s, import 109.50s, tests 46.97s, environment 190.22s)
```
(2715 at `e037ce6` + 24 from this task: seed-din276 6, field-configs-din276 8, equations-din276 7, register-din276-matrix 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`; one RED first: a `/s` regex flag in the field-config test — TS1501 under the project target — replaced by the plain `/consumed/` pin). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output.

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/din276-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  28 passed (28)
   Start at  07:47:37
   Duration  5.57s (transform 709ms, setup 0ms, import 3.59s, tests 1.81s, environment 0ms)
```
The harness seeds prod's fields / equations / gates (`tests/harness/seed-din276.ts`); the new rows are not in it by construction — it proves the branch still drives every DIN-276 chain and gate both ways where drivable.

**TDD evidence.** Runtime facts were established BEFORE the pins with two scratch scripts through the real `prepareRegisterRows` / `evaluateFormula` on the din276 tables (`scripts/_probe_din276.ts`, `_probe2_din276.ts`, deleted): a two-key `lookup('TABLE4', kg, tab4_nr, 'unit')` in row scope computes ('m'); `if(tab4_nr IS NULL, …)` on a text cell works; a lookup miss is a silent null (no diagnostic); `lookup('TABLE1', kg, 'level') == 1` switches inside `if()`; `sum_rows(reg, expr, cond)` with NO matching row is `manual_required` ("Keine vollständigen Zeilen") → every per-group Σ is guarded with `if(count_rows(…) > 0, …, 0)`, while `sum_rows(reg, if(cond, x, 0))` reads 0 by itself; `last_rows(reg, 1)` / `(…, 2)` pick the last complete rows and the unguarded "Σ last 2 − Σ last 1" reads 0 with ONE row → `stufe_vorher_gesamt` carries `count_rows(reg) >= 2` as its Σ condition (one row → `manual_required`); a matrix row with a blank KG is incomplete and enters no Σ; `count_rows(reg, flag == false)` counts a blank boolean. **The string-literal trap was found here** (X-1): `if(status == 'rechnung', rechnung, …)` read the amount column for every row (a3 = 1000 / 500 / 300 instead of 950 / 500 / 320) and `count_rows(v, status == 'angebot')` read 0 — the evaluator's `compare` node resolves a quoted RHS that names a VALUED symbol (`src/lib/expr/evaluate.ts`, the C-1 legacy rule) → the amount columns were renamed `angebot_eur` / `auftrag_eur` / `rechnung_eur` and the TABLE1 structural tokens `kg1` / `kg2` are printed "KG 300" / "KG 310" (never identifiers); pinned in `field-configs-din276.test.ts` (no literal in any row expression is a column key or a prod symbol of its worksheet). Seed builder: all four tables built first run with every cell `inSpan` after two generator fixes (the ` & ` cell splitter on `& &` blank cells; the `\multicolumn` head rows of the multirow blocks) and one assertion change (a Table-4 item row prints no KG code — its `kg` is the `\multirow` head it sits under, asserted for head rows only); verifier **671/671** on its first run. Field-config test: 8/8 first run (the refusals — `kg_311` / section "KG 310" by `cost_breakdown_depth`, `separate_calculations_per_building`, `existing_substance_value`, `cost_target_upper_limit` — were asserted, not discovered; the emitter accepted all 134 entries first time). Equations test: 7/7 first run (pins from the probes). Render test: two REDs — the `FooterState` shape (`{ label, unit, state }`, not a bare `EvalState`) and the optgroup count (exactly 50 = 1 + 8 + 41) → 3/3.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts din276 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-276\DIN-276.md"` → **`671/671 quotes verbatim`**, exit 0 (671 PASS lines, 0 FAIL). PASS counts per table · lines:

- TABLE1 (326): every printed KG row of Table 1, L464–L1010 — 8 first-level (100 … 800), 52 second-level, 266 third-level; multi-line notes rows (e.g. 121 L467–L470, 220 L505–L512, 300 L538–L543, 400 L685–L691, 500 L809–L817) keep the nested tabular in the span
- TABLE2 (8): L1030–L1037
- TABLE3 (78): L1052–L1160 — plain rows + the three `\multirow` blocks sharing their block span (370–379 L1124–L1133, 381–389 L1143–L1150, 391–399 L1152–L1160)
- TABLE4 (259): L1174–L1541 — 72 KG rows (nr "0") + 187 numbered items; the 491–499 "Like KG 490" block L1533–L1541

Status per table: `md_verified` — TABLE1 (every printed row lifted, every cell legible; 35 notes cells are printed blank), TABLE2. `imported_unverified` — TABLE3 (U-1: row 340 prints an empty unit cell, L1093), TABLE4 (U-2: 440 head row designation-only L1287, 458 item 6 unit blank L1375, 474 item 3 unit "m." L1450).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DIN-276 din276
  → wrote src\lib\eval\field-configs\din276.prior.json: 544 field rows (12 orphan), 429 coded sections of 429, 53 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts din276
  → wrote seed + rollback for din276 -> scripts/migrations/20260917101300_regulation_tables_seed_din276.sql scripts/rollback-20260917101300-regulation-tables-seed-din276.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts din276 20260917101310
  → wrote 134 field entries + 0 section entries for din276 -> scripts/migrations/20260917101310_field_configs_din276.sql scripts/rollback-20260917101310-field-configs-din276.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts din276 20260917101320
  → wrote 115 equations for din276 -> scripts/migrations/20260917101320_equations_din276.sql scripts/rollback-20260917101320-equations-din276.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-din276.test.ts` / `equations-din276.test.ts` pin the other two against the committed `din276.prior.json`). The seed migration (639 KB) carries 671 row upserts (`ON CONFLICT (table_id, row_key)`) and 2 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the four tables (no earlier DIN-276 table seed exists — nothing superseded). The equations rollback deletes the 115 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101300` → `20260917101310` → `20260917101320`; rollback in reverse. Every file touches `DIN-276` rows only. Scalar-only rows (`DIN-276-18-D9` / `-D10`, `DIN-276-24-D1` … `-D4`) are not server-materialised (amendment D — expected, noted once here and as I-2); the 109 register-fed rows materialise on save.

## 5. What was encoded

**Tables (4)** — `src/lib/eval/regulation-tables-seed-din276.ts`, builder `din276SeedTables()`, edition `'2018-12'` (L1 "Replacement for DIN 277-3:2005-04, DIN 276-1:2008-12 and DIN 276-4:2009-08", running caption L516 "DIN 276:2018-12" = prod `standards.version` 'December 2018 (DIN 276:2018-12)' — I-1). The standard prints no numbered equation and no numeric value table: Table 1 is the KG catalogue, Tables 2–4 the reference units.

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABLE1 | `kg` = `kg_<code>` (= the prod field symbols of DIN-276-09 … -16, 1:1 pinned) | `designation`, `notes` (nullable), `level` 1/2/3, `parent_kg`, `kg1` ("KG 300"), `kg2` ("KG 310", null on level 1) — the four structural columns from the printed ordinal rule (J-2) | anhaltswert (L457 "… are examples of the respective cost group. The list is not exhaustive." — composed with L215 "Cost calculations are to be organised according to the cost classification system in Section 5 and Table 1.") | 326 | md_verified |
| TABLE2 | `kg` (kg_100 … kg_800) | `unit` (m²), `designation` (GF / GFA / AF), `determination` | anhaltswert (L1022 "It is recommended that …") | 8 | md_verified |
| TABLE3 | `kg` (3xx) | `unit` (m² / m³ / m / null), `designation`, `determination` ("According to the requirements of the project" for 370–379, 381–389, 391–399) | anhaltswert (L1044) | 78 | imported_unverified (U-1) |
| TABLE4 | `kg` × `nr` ("0" = the KG row, "1"… = the printed items) | `item`, `unit` (m / St. / kW / m² / m3/h / kVA / Ah / kWp / kvar / "m."), `designation`, `determination` ("Like KG 490" for 491–499) | kann (L1166 "… can be used …"; O-1 proposes anhaltswert from L1018) | 259 | imported_unverified (U-2) |

Units: the transcript's `$\mathrm{m}^{2}$` / `$\mathrm{m}^{3}$` are seeded as "m²" / "m³" with the printed TeX token asserted in the span; every other unit verbatim.

**Registers / selections / lookup_fill / visibility** — `src/lib/eval/field-configs/din276.ts`:
- `kostenstufen_matrix` (DIN-276-18 C, create): `stufe` enum kr / ksch / kber / ka / kf (labels from prod's worksheet titles — J-3), `datum` date, `kg100` … `kg800` number EUR **required** (J-6), derived `gesamt = kg100 + … + kg800` (§3.11), `bauwerk = kg300 + kg400` (§3.12), `eigenleistung_eur` / `prognose_eur` / `risiko_eur`, `bemerkung`; footer = the 11 D-outputs. Supersedes the five stage worksheets functionally (D-1 / X-2 on ratification).
- `kg1_positionen` … `kg8_positionen` (DIN-276-09 … -16 C, create — one per KG worksheet, brief deviation): `kg` lookup_key TABLE1 (grouped by parent KG via `group_label`, 50 groups), `bezeichnung` lookup_value, `tab4_nr` text (KG 4xx items), `menge`, derived `einheit` (TABLE2 for level 1, TABLE4 `(kg, nr)` when `tab4_nr` is set, else TABLE3 — blank where no unit row exists), `kennwert` EUR/Einheit, derived `kosten_calc = menge * kennwert`, `kosten_eur` **required**, badge `abw` (typed ≠ Menge × Kennwert), derived `ebene1` / `ebene2` (the structural ancestors), badge `im_kg` (`ebene1 == 'KG N00'`), `hinweis` lookup_value (the Table-1 notes). Footer: `kgN_positionen_sum` / `_count` / `_fremd` / `_abweichend` + one `kg_NN0_from_rows` per printed second-level KG. No `override` block (J-7).
- `sonderkosten` (DIN-276-08 C): `art` enum bausubstanz / beigestellt / besondere / prognose / risiko (J-4), `kg` lookup_key TABLE1 + designation, `betrag` required, `annahmen`, `separat_ausgewiesen` boolean; footer the five Σ + `sonderkosten_nicht_separat`.
- `vergabeeinheiten` (DIN-276-21 C): `label` required, `gewerk`, `kg` + designation, `angebot_eur` / `auftrag_eur` / `rechnung_eur`, `status` enum angebot / auftrag / rechnung (L346 "offer, order or invoice") required, derived `aktuell` (the amount at the current status); footer `KA_kostenstand_calc`, counts.
- `abweichungen` (DIN-276-27 C): `kg` required + designation, `betrag` required, `pct`, `typ` text with datalist (J-1), `ursache` required, `massnahme`, `dokumentiert`; footer Σ / count.
- `flurstuecke` (DIN-276-04 C): `gemarkung`, `flur`, `flurstueck` required, `flaeche_m2` required; footer `grundstuecksflaeche_GF_calc`, count.
- `kennwert_quellen` (DIN-276-07 C): `kg` + designation, `quelle` required (datalist BKI / Vergleichsprojekt / eigene Datenbank — prod's description words), `stand` date, `region`, `preisstand`, `standard`, `bemerkung`; footer count.
- `kg_selector` (DIN-276-24 C, create select_one; options = TABLE2 keys, labels = the printed Table-2 KG designations) + three TEXT `lookup_fill` twins `reference_unit_einheit` / `_bezeichnung` / `_ermittlung` ← TABLE2 (`role: value`); the existing NUMBER `reference_unit` (IDENT-03 input) is not re-bound (amendment J, E-2 / S-1).
- Field `visible_when` (1 UPDATE): DIN-276-23 `GK_mwst_satz_pct` ← `vat_treatment IN {gross, mixed}` (L267–L272; consumer-free; `pending` until C-1 expands the range token). Refused / withheld: every third-level `kg_NNN` (transitive roll-up chain, C-2), `separate_calculations_per_building` / `existing_substance_value` (consumed, C-4), the -28 limits (consumed; refuted by L416, J-5). Section rules: none (M-1 / M-2 select_many; C-2).

**Equations (115 new)** — `src/lib/eval/equations/din276.ts`: `DIN-276-04-D1 grundstuecksflaeche_GF_calc = sum_rows(flurstuecke, flaeche_m2)` · `-D2 flurstuecke_count` · `DIN-276-07-D1 kennwert_quellen_count` · `DIN-276-08-D1 … D5 sonderkosten_<art>_sum = if(count_rows(sonderkosten, art == '<art>') > 0, sum_rows(sonderkosten, betrag, art == '<art>'), 0)` · `-D6 sonderkosten_nicht_separat = count_rows(sonderkosten, separat_ausgewiesen == false)` · per KG worksheet `-D1 kgN_positionen_sum = if(count_rows(reg, im_kg == 1) > 0, sum_rows(reg, kosten_eur, im_kg == 1), 0)` · `-D2 _count` · `-D3 _fremd = count_rows(reg, im_kg == 0)` · `-D4 _abweichend = count_rows(reg, abw == 1)` · `-D5 … kg_NN0_from_rows = if(count_rows(reg, ebene2 == 'KG NN0') > 0, sum_rows(reg, kosten_eur, ebene2 == 'KG NN0'), 0)` (52) · `DIN-276-18-D1 … D5 KR/KSch/KBer/KA/KF_gesamt_calc = sum_rows(kostenstufen_matrix, if(stufe == '<t>', gesamt, 0))` · `-D6 stufen_count` · `-D7 stufe_aktuell_gesamt = sum_rows(last_rows(m, 1), gesamt)` · `-D8 stufe_vorher_gesamt = sum_rows(last_rows(m, 2), gesamt, count_rows(m) >= 2) - sum_rows(last_rows(m, 1), gesamt)` · `-D9 stufen_abweichung = stufe_aktuell_gesamt - stufe_vorher_gesamt` · `-D10 stufen_abweichung_pct = stufen_abweichung * 100 / stufe_vorher_gesamt` · `-D11 bauwerk_aktuell = sum_rows(last_rows(m, 1), bauwerk)` · `DIN-276-21-D1 KA_kostenstand_calc = sum_rows(vergabeeinheiten, aktuell)` · `-D2 vergabeeinheiten_count` · `-D3 … D5 KA_angebote/auftraege/rechnungen_count` · `DIN-276-24-D1 KKW_bauwerk_eur_m2_BGF = building_costs / gross_floor_area_BGF` · `-D2 KKW_bauwerk_eur_m3_BRI` · `-D3 GK_kennwert_BGF_calc = GK_total / gross_floor_area_BGF` · `-D4 KKW_analyse_kg300_anteil_calc = kg_300_total * 100 / GK_total` · `DIN-276-27-D1 abweichungen_sum` · `-D2 abweichungen_count`. No prod symbol is re-produced (IDENT-01 … 04 and the 48 KGx-xx roll-ups keep their rows). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): KG 300 register with seven rows → Σ **4500** / count 5 / fremd 2 / abweichend 1, KG 310 **4250**, KG 320 **150**, KG 330 **0** (no row), empty register 0 / 0; KG 100 register kg_110 500 000 + kg_121 2 000 → `kg_110_from_rows` 500 000, `kg_120_from_rows` 2 000, Σ 502 000; matrix rows Σ **2050 / 2230** (§3.11), Bauwerk **1400 / 1550** (§3.12), a row with a blank KG incomplete, KF without row 0, aktuell 2230, vorher 2050, Abweichung **180 = 8,78 %**, one row → vorher `manual_required` (never 0), empty matrix → aktuell `manual_required`; Sonderkosten prognose **125**, bausubstanz 0, nicht separat 2; Vergabeeinheiten aktuell Σ **1770** (950 + 500 + 320), counts 1 / 1 / 1; Flurstücke Σ **1234,5 m²**; Abweichungen Σ 100 / 2; Kennwerte **1,55 EUR/m²**, 0,3875 EUR/m³, 2,23, **53,81 %**, missing `GK_total` → `manual_required` (until C-5), BGF 0 → division by zero `manual_required`.

## 6. Inventory §5 top-wins walk

1. Collapse the five stage worksheets into one stage × KG matrix with derived row totals, `*_gesamt`, current / previous stage totals and deviations → **encoded** (`kostenstufen_matrix` on DIN-276-18 + the 11 derivations); the five stage worksheets retire in Phase 6 (D-1 / X-2), the -26 / -27 typed comparison needs the consumer edit C-3 + E-1 / D-2.
2. Give every KG item quantity + reference unit (Tab. 2/3/4 by KG) and a Kennwert so `kg_NNN = Menge × Kennwert` is computed → **encoded** (eight `kgN_positionen` registers over TABLE1 with the unit from TABLE2 / 3 / 4, `kosten_calc`, the `abw` badge, Σ twins per first- and second-level KG); the typed EUR stays the required figure (J-7), the switch of the 48 roll-ups is D-3.
3. Delete the copy worksheets -17 / -25 and the `BU_*` block → **Phase 6** (X-2: 21 fields + `GK_bauwerkskosten`).
4. Sonderkosten register driving REQ-10 … 14 and the stage `*_anteil` fields → **register + sums encoded**, gates STAGED (G-2), the 15 `*_anteil` copies are D-1.
5. Gate KG worksheets and stage worksheets on the checklists and `cost_breakdown_depth` → **partially**: the drivers are `select_many` (M-1 / M-2) and every third-level input is a roll-up producer (C-2); the one emittable rule (`GK_mwst_satz_pct`) is emitted; C-1 (range tokens) is the prerequisite for all of them.

Data-quality gaps of the inventory: the "(Engineer: Label/Einheit pruefen)" placeholders → the Table-2 fills + S-1; the free-text option lists → T-1 / T-2 where printed, J-1 where not; REQ-09 / REQ-26 … 29 empty conditions → G-1 / I-3; Table 4 now seeded (259 rows) with the `tab4_nr` register column reading it.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`din276` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (din276 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+44 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 13)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-din276.ts` (299 KB incl. the 671 lifted spans), `src/lib/eval/field-configs/din276.ts`, `src/lib/eval/field-configs/din276.prior.json` (captured), `src/lib/eval/equations/din276.ts`, `src/lib/eval/__tests__/regulation-tables-seed-din276.test.ts`, `src/lib/eval/__tests__/field-configs-din276.test.ts`, `src/lib/eval/__tests__/equations-din276.test.ts`, `src/components/worksheet/__tests__/register-din276-matrix.test.tsx`, `scripts/verification/din276-STAGED-plan3-rulings.sql` (26 blocks, every line a comment), `scripts/migrations/20260917101300_regulation_tables_seed_din276.sql` + `scripts/rollback-20260917101300-regulation-tables-seed-din276.sql`, `scripts/migrations/20260917101310_field_configs_din276.sql` + `scripts/rollback-20260917101310-field-configs-din276.sql`, `scripts/migrations/20260917101320_equations_din276.sql` + `scripts/rollback-20260917101320-equations-din276.sql`, this report (+ the copy at `.superpowers/sdd/…/task-13-report.md`).
Untouched: every other standard's file; the DIN-276 harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging); the probes deleted; the generator / checkers live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **One KG-item register per KG worksheet** (`kg1_positionen` … `kg8_positionen`), not one on DIN-276-09: a register-fed equation lives on its register's worksheet and the Σ twins must sit beside the `kg_NN0_total` they replace (the eight worksheets); a `create` never sets consumers. The row picker still offers the whole Table 1; the `im_kg` badge excludes foreign rows.
2. **The matrix derivations (incl. current / previous / deviation) live on DIN-276-18**, not on -26 / -27 (same rule); C-3 / D-2 / E-1 carry the re-point. `stufe_vorher_gesamt` is guarded (`count_rows >= 2`) — the brief's "Σ last 2 − current" reads 0 with one row.
3. **TABLE2 / 3 / 4 stay three tables**: Table 4 is keyed `(kg, nr)` (72 KG rows + 187 printed items), so a `TABLE234` union keyed by `kg` alone is impossible; the register's `einheit` is one derived chain (`level == 1` → TABLE2; `tab4_nr` set → TABLE4; else TABLE3).
4. **`reference_unit` is a NUMBER** (the reference quantity, IDENT-03 input) — the brief's `lookup_fill` re-bind to the Table-2 `unit` string would write nothing; three TEXT twins + `kg_selector` instead (E-2 / S-1).
5. **The brief's `kosten_override` / `override { applies_to: ['kosten_eur'] }` is not encodable** — the mechanism's override governs `lookup_value` cells (I-4); `kosten_eur` is the required typed figure with `kosten_calc` and the `abw` badge beside it (J-7).
6. **The -28 limit visibility is refuted by L416** ("In conjunction with a target value, a range with an upper and lower limit can also be defined") — no rule (J-5).
7. **`existing_substance_value`** (consumed by -23; `construction_activity` reaches -03 / -07 only) and **`separate_calculations_per_building`** (consumers `DIN-276-09..29`) are refused → C-4; **every `kg_NNN`** is a roll-up producer → C-2 (the brief's Step-4 rows 3 / 4 / 5 are all STAGED).
8. **`vat_treatment IN {gross, mixed}`**, not `== '<brutto token>'`: L270 prints the third form ("VAT is only shown for individual cost details"), prod token `mixed`.
9. **115 equations (brief ≈ 18)**: the 52 second-level Σ twins, counts and badges per register; the brief's `KKW_analyse_eur_m2_BGF_calc = GK_total / BGF` became `GK_kennwert_BGF_calc` on -24 (GK_total not inherited → C-5) plus two Bauwerk Kennwerte that compute today (`building_costs` / BGF / BRI DO reach -24); `cost_parameter_per_AF_calc` could not be emitted (`kg_500_total` not on -24 — C-5 follow-up); `final_contract_value_calc` became `KA_kostenstand_calc` (Σ at the row's own cost status, L346) rather than Σ `auftrag` (an optional null cell breaks a plain Σ — probed).
10. **Free-text → select**: all STAGED (T-1 KA_kostenstatus, T-2 the three stage texts) or J-1 (HOAI phases, AA_typ, Prüfungs-/Freigabe-Status — not printed); no `select_one` UPDATE on a text field was emitted (a select over a `text` data_type is unproven on the branch).
11. **Sonderkosten `art` tokens are this task's own** (bausubstanz / beigestellt / besondere / prognose / risiko): prod `special_cost_flags` has NULL `enum_values` in the capture and the Plan-1 values are the German headings with "§4.2.10" (J-4).
12. **Sign-off ids 44 (brief ≈ 12)**: the brief's ids kept where they exist (M-1 / M-2, G-1 / G-2, D-1, F-1, S-1, X-1 … X-3, J-1; R-1 → E-1, R-2 → D-3 by class letter); every further refused / withheld / read item got its own block.
13. **Prod facts**: 32 compliance rows as stated, 9 with empty conditions; 12 orphan fields (the `attest_*` booleans); the KG worksheets carry a coded subsection per second-level KG ("KG 110" …) under C.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (671/671); every seeded cell is asserted inside its row's span at build time; the 52 cue spans were re-keyed by machine to their real lines (52/52) after my first hand-cited line numbers were wrong (the sed windows misled — caught before any file used them); every quoted fragment on the 44 sign-off blocks was machine-checked against the transcript at its cited line (70/70, one line fix: the kg_311 row is L545, not L539).
- TABLE1 ↔ prod: the 326 rows are exactly the 326 prod `kg_*` fields of DIN-276-09 … -16 (pinned both ways) — the catalogue and the encoding agree 1:1.
- The emitter guards accepted all 134 entries and 115 equations first time because the rule set was planned against the capture; the field-config test pins the refusals (`kg_311`, section "KG 310", `separate_calculations_per_building`, `existing_substance_value`, `cost_target_upper_limit`), the range consumer tokens, the -24 inheritance facts, and that every register footer symbol is a created derived field on the same worksheet.
- G-A3 pinned: `kg_selector` options = TABLE2 keys; register enum tokens listed; TABLE1 keys = the prod symbols.
- The string-literal trap (X-1) is pinned per register (no literal in a row expression is a column key or a prod symbol of its worksheet) and recorded in the playbook.
- Prod facts used in the STAGED file (32 gate ids + md5, 4 IDENT ids + md5, column lists, labels, descriptions, `standards.version`) come from the in-session read-only queries listed in §0; no long cell was retyped (archive pattern).
- No `String.replace` with a replacement string on any repo file: every edit used `split/join` with a presence check that throws before writing, or the Edit tool; the seed module is generated by the scratchpad script from the transcript with `JSON.stringify` per span (no heredoc for backslash-heavy content).
- `git checkout -- scripts/reasoning-map/` run before staging; `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **The engine's string-literal rule (X-1)** is a corpus-wide hazard: any register whose enum tokens coincide with a column key or a worksheet symbol silently compares against the value. Every earlier Plan-3 standard should be scanned (a data-side emitter guard "enum option token ∉ column keys ∪ worksheet symbols" would catch it without a runtime change — [CODE] candidate, not done here).
2. **Range consumer tokens (C-1)** make five of the six brief conditionals inert until ratified — C-1 is one UPDATE and the single most valuable ratification for DIN-276.
3. **C-2 cannot be ratified alone**: hiding third-level inputs nulls the roll-ups; it needs D-3 (totals from the registers) first — the sheet says so.
4. **The Kennwert twins on -24** (`GK_kennwert_BGF_calc`, `KKW_analyse_kg300_anteil_calc`) read `manual_required` until C-5; the two Bauwerk twins compute today.
5. **Seed module 299 KB / migration 639 KB** (I-4): Table 1's notes are the bulk; explicit cells + spans per the m820_3 pattern. Task 30 measures the bundle.
6. **English transcript**: Table-1 designations / notes and the Table-2/3/4 cells are seeded in English (the transcript's language); prod labels are German. The register shows English printed cells beside German labels until the owner rules on a German source (SR-3).
7. **The eight KG registers add 84 derived fields + 84 equations** — mechanical, but a large surface for the owner's browser pass; the Σ twins are the D-3 evidence.
8. **Token/time:** ≈ 0.68 M tokens in, ≈ 165 min wall-clock — above the Task 10–12 range (0.57–0.65 M / 75 min): the 671-row lift, the string-literal probe and the 44-block sheet.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§4.2.6 explanations organised in the cost breakdown (L231 — no per-KG remark field beyond the register rows); §4.3.2 the requirements-planning basis per DIN 18205 (L290–L294) as a checklist; §4.3.3–4.3.6 the per-stage input documents (L302–L307, L315–L320, L330–L337, L349–L356) — the Plan-1 `input_documents_register` exists but is not stage-keyed; §4.5 cost management decisions and measures (L392–L402) — only `abweichungen.massnahme`; §4.6.2 the cost-target range (upper + lower with a target value, L416) as a gate against `stufe_aktuell_gesamt` (needs C-3; REQ-24 checks presence only); §5.2 "predominant cause" allocation (L443) and the further subdivision levels (L445–L447); §5.3 execution-oriented breakdown by STLB-Bau / VOB C trades (L451–L453) — `vergabeeinheiten.gewerk` is free text; §6.1 the analogous application to civil engineering / infrastructure / open spaces (L1018) — no project-type-driven unit switch. Each is on the sign-off sheet or listed here with its line.
