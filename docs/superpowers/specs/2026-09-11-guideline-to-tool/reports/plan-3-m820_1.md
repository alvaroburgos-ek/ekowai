# Plan 3 — DWA-M-820-1 (`m820_1`) — Task 18 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 19)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (raw `claude --version` output "2.1.260 (Claude Code)"; no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `5444d8f`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-1 m820_1` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 25 worksheet titles, the section codes / titles / field counts of nine worksheets, the 26 compliance rows with ids / severities / md5 / lengths / titles, labels / units / `is_required` / `validation_rules` / descriptions of the 52 fields on the nine touched worksheets + 7 drivers, the `client_organization_type` enum labels, the `information_schema` column lists of `fields` / `compliance_requirements` / `project_parameters`, the 9 stored `project_parameters` rows of the standard). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-1\DWA-M_820-1.md` (2010 lines, mathpix LaTeX; title page L1–L80, §5 L599–L672, §8.4–§8.7 L841–L870, §8.10.3 L996–L1066, Anh. B L1236–L1380, Anh. D L1440–L1480, Anh. E.1.4 / E.2 L1619–L1700, Anh. F L1740–L1800 read in full; the cited lines re-printed one by one before lifting). The inventory was used for pointers only. The `.pdf` beside the transcript was NOT opened (not a source under the md-transcript ruling).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m820_1` | 6 | 20 (20/20 verbatim) | 0 rows skipped; 0 unreadable cells — all six tables `md_verified` (every displayed cell legible) | 6 (3 `create`: `lose` -09, `bewerber` -18, `verhandlungsrunden` -20; 3 UPDATE = the Plan-1 registers `stakeholder_list` -03, `award_criteria_list` -14, `bewertungskommission_members` -16 upgraded in place, column keys kept) | 0 (no printed option list without a prod select; the created enum columns live inside registers) | 0 (no S-block) | 4 (all `create`: `eu_threshold_value_anhb23` + `eu_threshold_verordnung_anhb23` -09 ← ANHB23 by `client_organization_type`; `min_annual_revenue_multiplier_max` -13 ← E1_4_1_UMSATZ by the boolean `large_long_project`; `required_standstill_days_gwb` -23 ← S8_10_3_6 by the boolean `electronic_transmission`) | 1 (on the created `price_weight_calc_pct` ← `festpreis_used == false`; 0 on existing fields — every brief target refused or refuted, §8) | 0 | 35 (-03 D1–D2; -09 D1–D14; -13 D1; -14 D1–D3; -16 D1–D4; -18 D1–D4; -20 D1–D3; -22 D1–D4) | 0 replacements (prod has 0 equations); 11 gate blocks (G-1 … G-11), 24 D-pairs, 2 E-blocks, 4 C-blocks STAGED | 47 blocks (+5 observations) | ≈ 0.60 M (tool-marker delta 15.00 M → 14.41 M at report time) | ≈ 75 |

Field entries: 45 = 42 `create` (3 registers, 4 lookup_fill, 35 derived outputs — one per equation) + 3 UPDATE (widget / ui_config of the Plan-1 registers; no `visible_when`, no `enum_values`). Migration `20260917101810`: 42 `INSERT … WHERE NOT EXISTS`, 3 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 3 UPDATE rows to the captured NULLs (the TS `SELECTION_CONFIGS` fallback takes over again — observation on the sheet) and deletes only the 42 `Plan 3:` rows. No `enum_values` is written by any UPDATE (D-1). The emitter printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once, before the two typecheck / eslint fixes — a test-only `null` typing and an unused constant, both re-covered by the focused re-run below):
```
 Test Files  318 passed | 1 skipped (319)
      Tests  2921 passed | 1 expected fail | 1 skipped (2923)
   Start at  14:47:09
   Duration  52.74s (transform 16.03s, setup 54.38s, import 129.21s, tests 54.17s, environment 215.34s)
```
(2893 at `5444d8f` + 28 from this task: seed-m820-1 6, field-configs-m820-1 7, equations-m820-1 12, register-m820-1-bewerber 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → first run: 1 error (`equations-m820-1.test.ts(93,58): TS2345` — my `thr(null)` helper typed `number`; pin rewritten with `num(…, null)`), second run: exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → first run: 1 warning (`L465` assigned but unused in `field-configs/m820_1.ts` — the Konzept/Projekt cue whose rule was withheld, J-5; constant removed, the cue lives in `equations/m820_1.ts` `KONZEPT_PROJEKT_CUE` and on the sheet), field-config migration re-emitted (byte-identical content — the constant was unused), second run: exit 0, no output.

Focused re-run after the fixes:
```
pnpm vitest run --project unit <seed-m820-1, field-configs-m820-1, equations-m820-1, register-m820-1-bewerber, generated-sql-freshness>
 Test Files  5 passed (5)
      Tests  33 passed (33)
```

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/m820-1-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  26 passed (26)
   Start at  14:48:59
   Duration  5.94s (transform 790ms, setup 0ms, import 3.85s, tests 1.91s, environment 0ms)
```
The harness seeds prod's 120 fields / 26 gates (`tests/harness/seed-m820-1.ts`); the new rows are not in it by construction — it proves the branch still drives every DWA-M-820-1 gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `evaluateFormula` / `prepareRegisterRows` / `resolveLookupFill` on the seeded tables (`scripts/_probe_m820_1.ts`, deleted): the Tab. D.1 `if()` chain returns the printed band figures at every boundary (inclusive "bis") and `manual_required` "keine Zeile in TABD1 für Schlüssel [gt50]" above 50 Mio. € (the deliberate miss in the last branch — `if()` short-circuits); an enum reaches `lookup('ANHB23', client_organization_type, …)` as its token (139 000 / 214 000); zero-input `lookup()` rows compute (80 000 / 2); a lookup_fill keyed on a prod boolean resolves `'true'` → 10 / `'false'` → 15 and `keys_missing` while unset; the lot register's row bound switches by kind, `sum_rows(if(ausnahme == true, …))` reads 0 for unset booleans, an EMPTY register leaves the Σ / code `manual_required` while `count_rows` reads 0; **a Plan-1 award row with an EMPTY weight breaks `sum_rows` ("Unbekanntes Symbol gewichtung")** and **a stakeholder row without `kategorie` makes `count_rows(reg, kategorie == …)` undecidable** — both columns were made `required` (0 stored rows in prod, checked read-only); `floor()` computes the odd-size code; `max_rows` over an empty register is `manual_required` (never 0). Seed builder → verifier **20/20** on its first run → seed test RED on one of my own pins (`makeTableLookup(…)(code, keys)` returns the row's VALUES object, not a cell — pin corrected, code untouched) → 6/6. Field-config test → **7/7 first run** (the emitter accepted all 45 entries first time; every refusal of the brief's targets was asserted through `gateReaders` / `producerChain` / `emitFieldConfigSql`, not discovered). Equations test → **12/12 first run** (pins from the probe). Render test → one RED (my `act` import from vitest instead of `@testing-library/react`) → 3/3. Typecheck / eslint: the two findings above, both mine, both in test / unused code.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m820_1 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-1\DWA-M_820-1.md"` → **`20/20 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- TABD1 (6): le0_5 L1465 · le1_5 L1466 · le4 L1467 · le10 L1468 · le25 L1469 · le50 L1470
- ANHB23 (6): bundesbehoerde L1329; municipality / utility / association / sonstige_auftraggeber / other L1330
- S8_10_3_6 (2): false / true — L1064 (one printed sentence carries both figures)
- S3_9_VGV (3): los_dienstleistung / los_bau / anteil_pct — L1366 (the quoted § 3 Abs. 9 VgV sentence)
- ANHB11 (1): pruefintervall_jahre — L1255
- E1_4_1_UMSATZ (2): false ("das Zweifache") / true ("Faktor 1,5") — L1623

Status per table: `md_verified` — all six (every printed row lifted; every displayed cell — numeric and text — legible; no image, no empty cell). No U-block.

Beyond the verifier (which checks table rows only), a scratch checker (deleted) asserted every `verification_quote` fragment of the two modules and every `override_quote` fragment of the six tables against the whitespace-normalised transcript: **136/136 verbatim**; a second one-liner checked every `"…" (L…)` fragment of the sheet section and the STAGED file: **46/46**.

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, valid_from, left(title_de,70) AS title FROM standards WHERE code LIKE 'DWA-M-820-1%'"
  → DWA-M-820-1 · 'März 2020' · issued_year null · valid_from null
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-1 m820_1
  → wrote src\lib\eval\field-configs\m820_1.prior.json: 120 field rows (39 orphan), 175 coded sections of 175, 0 equations, 26 gates (5 unparseable — symbols: [] + parse_error; …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m820_1
  → wrote seed + rollback for m820_1 -> scripts/migrations/20260917101800_regulation_tables_seed_m820_1.sql scripts/rollback-20260917101800-regulation-tables-seed-m820-1.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m820_1 20260917101810
  → wrote 45 field entries + 0 section entries for m820_1 -> scripts/migrations/20260917101810_field_configs_m820_1.sql scripts/rollback-20260917101810-field-configs-m820-1.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m820_1 20260917101820
  → wrote 35 equations for m820_1 -> scripts/migrations/20260917101820_equations_m820_1.sql scripts/rollback-20260917101820-equations-m820-1.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m820-1.test.ts` / `equations-m820-1.test.ts` pin the other two against the committed `m820_1.prior.json`, default refuse mode). The seed migration carries 20 row upserts (`ON CONFLICT (table_id, row_key)`) and 6 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the six tables (no earlier DWA-M-820-1 table seed exists — nothing superseded; the Plan-1 `20260911120000_selection_configs_DWA_M_820_1.sql` carries no tables). The equations rollback deletes the 35 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101800` → `20260917101810` → `20260917101820`; rollback in reverse. Every file touches `DWA-M-820-1` rows only. Scalar-only rows (-09 D1 … D7, -13 D1, -22 D1 … D4 = 12) are not server-materialised (amendment D — expected, noted once here and as I-2); the 23 register-fed rows (-03 D1–D2, -09 D8–D14, -14 D1–D3, -16 D1–D4, -18 D1–D4, -20 D1–D3) materialise on save.

## 5. What was encoded

**Tables (6)** — `src/lib/eval/regulation-tables-seed-m820_1.ts`, builder `m8201SeedTables()`, edition `'2020'` (title page L9 / L19 "März 2020", imprint L48 "© DWA, 1. Auflage, Hennef 2020" = prod `standards.version` 'März 2020'):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABD1 | `baukosten_band` le0_5 / le1_5 / le4 / le10 / le25 / le50 | `baukosten_max_mio` 0,5 / 1,5 / 4 / 10 / 25 / 50, `personen_mio` 1,5 / 1,5 / 1,5 / 2 / 3 / 3, `sonstige_mio` 0,25 / 0,5 / 1 / 2 / 3 / 5, `band_gedruckt` | anhaltswert (L1629 "Anhaltspunkt … kann Tabelle D. 1 … geben" — L1454 RBBau "empfiehlt … im Wesentlichen folgende Beträge") | 6 | md_verified |
| ANHB23 | `organisation` = the six prod `client_organization_type` tokens (G-A3) | `schwellenwert_eur` 139 000 (bundesbehoerde) / 214 000 (the five others — J-3), `gruppe_gedruckt`, `verordnung` "Verordnung (EU) 2019/1828 vom 30.10.2019", `gueltig_ab` "01.01.2020" (J-1) | locked (L1327 "alle zwei Jahre festgesetzt" — L1255) | 6 | md_verified |
| S8_10_3_6 | `uebermittlung` 'false' / 'true' (stringified prod boolean `electronic_transmission`, G-15) | `frist_tage` 15 / 10 Kalendertage, `gedruckt` | locked (L1064 "darf … frühestens … schließen (§ 134 Abs. 2 GWB)") | 2 | md_verified |
| S3_9_VGV | `konstante` los_dienstleistung / los_bau / anteil_pct | `wert` 80 000 / 1 000 000 / 20, `unit`, `comparator` < / < / <= (J-6), `gedruckt` | locked (L1366, the quoted statute) | 3 | md_verified |
| ANHB11 | `konstante` pruefintervall_jahre | `wert` 2 Jahre | locked (L1255) | 1 | md_verified |
| E1_4_1_UMSATZ | `grossprojekt` 'false' / 'true' (stringified prod boolean `large_long_project`) | `faktor_max` 2 / 1,5, `modal` ("darf … nicht mehr als … verlangen" / "sollte … nicht überschreiten"), `gedruckt` | anhaltswert (L1623 "sollte … den Faktor 1,5 nicht überschreiten" — O-1) | 2 | md_verified |

**Registers / lookup_fill / visibility** — `src/lib/eval/field-configs/m820_1.ts`:
- **Plan-1 registers upgraded in place (UPDATE, keys kept):** `stakeholder_list` (-03) + `kategorie` enum (auftraggeber / auftragnehmer / ausfuehrende_firmen / behoerden / oeffentlichkeit — the §5.2–§5.6 heads, required), footer `stakeholder_count` / `stakeholder_kategorien_abgedeckt`; `award_criteria_list` (-14): `kriterium` (the seven Anh. E.2.2–E.2.8 headings L1659…L1694, required), `gewichtung` required 0–100, `anmerkung`, derived badge `ist_preis = if(kriterium == 'Preis', 1, 0)` (quoted literal, Task 13b), footer Σ / Preis / count, note = L1013 + L1696 + L1698 (the Plan-1 "Summe = 100 %" / "Preis und mind. ein qualitatives Kriterium" dropped — F-1; `sum_column` retired for the equation footer, D-2b-5); `bewertungskommission_members` (-16) + `vorsitz` boolean, `name` required, footer size / Vorsitz / stimmberechtigt / ungerade.
- **Created registers:** `lose` (-09 B): `los` text, `art` enum dienstleistung / bau, `netto_wert_eur`, `ausnahme` boolean, derived `grenze_eur` (S3_9_VGV by kind), badge `unter_grenze` (strict `<`, J-6); footer the seven -09 lot outputs; note L1366 + the J-6 / J-7 readings. `bewerber` (-18 B): name, formal_ok, p123_ok, p124_ok, eignung_punkte, rang, shortlisted, angebot_punkte, final_offer, winner (Anh. F Nr. 6 / 8 / 9 L1757–L1786); footer the four counts. `verhandlungsrunden` (-20 B): runde, bieter, datum, thema, ergebnis, protokoll_signiert (§ 8.10.3.4 L1025 / L1027, Anh. F Nr. 8 L1780); footer max round / count / unsigned.
- **lookup_fill twins (amendment J — the existing inputs stay typeable):** `eu_threshold_value_anhb23` (number) + `eu_threshold_verordnung_anhb23` (text) on -09 ← ANHB23 by `client_organization_type` (E-1); `min_annual_revenue_multiplier_max` (number, role limit) on -13 ← E1_4_1_UMSATZ by `large_long_project`; `required_standstill_days_gwb` (number) on -23 ← S8_10_3_6 by `electronic_transmission` (E-2).
- **Derived outputs (35, `widget='derived'`):** -09 `eu_threshold_konsistent_code`, `oberschwellig_code`, `cost_estimate_near_threshold_code`, the four locked constants `lot_value_threshold_services_calc` / `_construction_calc` / `lot_share_threshold_pct_calc` / `eu_threshold_review_interval_years_calc`, `lose_count` / `lose_gesamt_eur` / `lose_ausnahme_sum_eur` / `lose_ausnahme_anteil_pct` / `lose_ausnahme_max_eur` / `lose_ausnahme_verletzt` / `loseausnahme_code`; -13 `min_annual_revenue_multiplier_ok`; -14 `award_weight_sum_pct` / `price_weight_calc_pct` / `qualitaets_kriterien_count`; -16 `bewertungskommission_size_calc` / `_vorsitz_count` / `_stimmberechtigt_count` / `_ungerade_code`; -18 `applicant_count_calc` / `shortlisted_count_calc` / `final_offers_count_calc` / `winner_count`; -20 `negotiation_rounds_calc` / `verhandlungsgespraeche_count` / `protokolle_unsigniert_count`; -22 `liability_personen_tabd1` / `liability_sonstige_tabd1` / `liability_personen_ok` / `liability_sonstige_ok`; -03 `stakeholder_count` / `stakeholder_kategorien_abgedeckt`.
- **Field `visible_when` (1):** `price_weight_calc_pct` ← `festpreis_used == false` (L1698 "kein Zuschlagskriterium mehr"; own-worksheet driver). Every brief target on an existing field was refused or refuted (§8). Section rules: none.

**Equations (35 new)** — `src/lib/eval/equations/m820_1.ts`: `M820-09-D1 oberschwellig_code = if(estimated_engineering_fee >= eu_threshold_value, 1, 0)` · `-D2 cost_estimate_near_threshold_code = if(abs(fee − thr) * 100 / thr <= cost_estimate_uncertainty_pct, 1, 0)` · `-D3 eu_threshold_konsistent_code = if(eu_threshold_value == eu_threshold_value_anhb23, 1, 0)` · `-D4 … D7` zero-input `lookup()` constants · `-D8 lose_count = count_rows(lose)` · `-D9 lose_gesamt_eur = sum_rows(lose, netto_wert_eur)` · `-D10 lose_ausnahme_sum_eur = sum_rows(lose, if(ausnahme == true, netto_wert_eur, 0))` · `-D11` share · `-D12 max_rows(…)` · `-D13 lose_ausnahme_verletzt = count_rows(lose, ausnahme == true AND unter_grenze == 0)` · `-D14 loseausnahme_code = if(verletzt == 0 AND share <= lookup('S3_9_VGV', 'anteil_pct', 'wert'), 1, 0)` (inline) · `M820-13-D1 min_annual_revenue_multiplier_ok` · `M820-14-D1 award_weight_sum_pct = sum_rows(award_criteria_list, gewichtung)` · `-D2 price_weight_calc_pct = sum_rows(…, if(ist_preis == 1, gewichtung, 0))` · `-D3 qualitaets_kriterien_count = count_rows(…, ist_preis == 0)` · `M820-16-D1 … D4` count / Vorsitz / stimmberechtigt / `if(n − 2·floor(n/2) == 1, 1, 0)` · `M820-18-D1 … D4` counts · `M820-20-D1 negotiation_rounds_calc = max_rows(verhandlungsrunden, runde)` · `-D2` count · `-D3 count_rows(…, protokoll_signiert == false)` · `M820-22-D1 / D2 liability_*_tabd1 = <six-step if() chain, every bound and sum a lookup('TABD1', …) × 1000000, last branch the deliberate 'gt50' miss>` · `-D3 / D4 liability_*_ok = if(liability_insurance_* >= <chain inline>, 1, 0)` · `M820-03-D1 stakeholder_count` · `-D2 stakeholder_kategorien_abgedeckt = Σ if(count_rows(stakeholder_list, kategorie == '<k>') >= 1, 1, 0)` ×5. None outputs a prod symbol; no boolean is an equation input; no twin is chained on another new output. **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): 214 000 ≥ 214 000 ⇒ 1, 213 999 ⇒ 0; near-threshold 200 000 vs 214 000 at 25 % ⇒ 1 (6,5 %), 100 000 ⇒ 0, 160 500 ⇒ 1 (exactly 25 %); constants 80 000 / 1 000 000 / 20 / 2; lots (150 000 / 30 000 A / 20 000 A / 900 000 Bau) → Σ 1 100 000, ausgenommen 50 000, 4,55 %, max 30 000, 0 violations, code 1; an excepted lot AT 80 000 ⇒ violation ⇒ code 0; 90 % share ⇒ 0; exactly 20 % ⇒ 1; empty register ⇒ counts 0, Σ / code `manual_required`; award 60 / 20 / 20 → Σ 100, Preis 20, quality 2, no Preis row ⇒ 0, a weightless row incomplete; commission 3 / 1 / 2 / odd, four ⇒ even, empty ⇒ 0; Bewerber 3 / 2 / 2 / 1, empty ⇒ 0; talks max 2 / 3 / 2 unsigned, empty ⇒ rounds `manual_required` (never 0); stakeholders 3 rows / 2 categories, all five ⇒ 5; **Tab. D.1** 400 000 → 1,5 / 0,25 Mio, 500 000 (inclusive) → same, 500 001 → 1,5 / 0,5, 4 Mio → 1,5 / 1, 10 Mio → 2 / 2, 25 Mio → 3 / 3, 50 Mio → 3 / 5, 50 000 001 ⇒ `manual_required` naming `gt50`; 13 `lookup('TABD1'` calls per chain, no band figure typed; `_ok` 1,5 Mio ≥ 1,5 Mio ⇒ 1, 1 Mio ⇒ 0.

## 6. Inventory §5 top-wins walk

1. Schwellenwert chain computed (organisation → threshold → oberschwellig → procedure) with a dated edition → **encoded as twins, chain STAGED**: ANHB23 (dated, locked, J-1 edition rule) fills `eu_threshold_value_anhb23` + the regulation text next to the typed threshold; `oberschwellig_code` and the consistency code compute; REQ-07 (pending on every project today — it sits on M820-04 where nothing resolves) and REQ-08 are G-3; the re-bind of the typed threshold is E-1. The five hand-typed fields are NOT yet removed (E-1, D-4 … D-10). ✓ (J-1 filed)
2. Bewerber / Bieter register feeding the four counts and the Anh. F table → **encoded** (`bewerber` with the Anh. F Nr. 6 / 8 / 9 columns; `applicant_count_calc` / `shortlisted_count_calc` / `final_offers_count_calc` / `winner_count` materialise on save); the manual counts stay until D-16 … D-19 / G-9. ✓
3. Tab. D.1 lookup into the Deckungssummen with audited override; retire the -22 duplicate → **encoded on M820-22** (the driver reaches -22, not -13): `liability_personen_tabd1` / `_sonstige_tabd1` (anhaltswert chain, > 50 Mio. € open — J-2) and the `_ok` comparisons; the typed requirements stay (D-1 / D-2 propose no switch — the standard asks for project-specific risks on top); the -22 duplicate is D-3; REQ-13 is G-11. ✓
4. Verhandlungsprotokoll as a register (round × bidder) with `negotiation_rounds` derived → **encoded** (`verhandlungsrunden`; `negotiation_rounds_calc` = max round, count, unsigned count); the single scalar set is D-20 … D-24 / G-10. ✓
5. Wire the aggregates the existing registers imply (Σ weights, Preis row → price weight, count(members) → size, stakeholders per §5 role → REQ-04) → **encoded** (the three Plan-1 registers upgraded in place with the badge / flag / category their aggregates need; 9 outputs); the switches are D-12 … D-15, the gates G-1 / G-5 / G-8; "= 100 %" is F-1 (not printed). ✓

Data-quality gaps of the inventory: the five empty gates (REQ-04 / -11 / -13 / -14 / -24) — three get proposed conditions (G-5 / G-11 / G-4); the "fixed" constants as editable inputs (D-7 … D-10 with locked twins); the boolean `information_letters_sent` chain (I-1 — not an equation); REQ-07 / REQ-05 / REQ-24 on M820-04 where their symbols do not resolve (new finding, G-3 / X-1 / G-4).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m820_1` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m820_1 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+47 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 18)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m820_1.ts`, `src/lib/eval/field-configs/m820_1.ts`, `src/lib/eval/field-configs/m820_1.prior.json` (captured), `src/lib/eval/equations/m820_1.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m820-1.test.ts`, `src/lib/eval/__tests__/field-configs-m820-1.test.ts`, `src/lib/eval/__tests__/equations-m820-1.test.ts`, `src/components/worksheet/__tests__/register-m820-1-bewerber.test.tsx`, `scripts/verification/m820_1-STAGED-plan3-rulings.sql` (41 ☐ blocks incl. the grouped D-1 … D-24; 401 lines, 0 non-comment lines), `scripts/migrations/20260917101800_regulation_tables_seed_m820_1.sql` + `scripts/rollback-20260917101800-regulation-tables-seed-m820-1.sql`, `scripts/migrations/20260917101810_field_configs_m820_1.sql` + `scripts/rollback-20260917101810-field-configs-m820-1.sql`, `scripts/migrations/20260917101820_equations_m820_1.sql` + `scripts/rollback-20260917101820-equations-m820-1.sql`, this report (+ the copy at `.superpowers/sdd/…/task-18-report.md`).
Untouched: every other standard's file; `selection-fields.ts` (`SELECTION_CONFIGS` — the Plan-1 configs stay as the `widget IS NULL` fallback), `register-configs.ts`; the M820-1 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the two scratch scripts deleted.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **`eu_threshold_value` / `required_standstill_days` not re-bound** (brief: lookup_fill on the existing fields): both are consumed (-10 / -24) and the first is gate-read (REQ-07) — amendment J ⇒ twins + STAGED E-1 / E-2.
2. **ONE 6-row table `ANHB23` keyed by the prod tokens** instead of the brief's 2-row `ANHB23` + 6-row `ANHB23_MAP`: a `lookup_fill` cannot chain tables and a copied figure would be a second source; each row quotes the printed line it maps to (J-3).
3. **Tab. D.1 pair on M820-22, not M820-13** (`estimated_construction_cost` consumers `["M820-09","M820-22"]` — the brief's -13 placement is unresolvable; C-4 offers the consumer extension).
4. **The four VgV constants are derived twins, not a `lookup_fill`** (the brief already foresaw it: G-12, no key symbol) — zero-input `lookup()` rows (a178 trap 4); plus a 1-row `ANHB11` table for "alle zwei Jahre" (L1255) and the boolean-keyed `E1_4_1_UMSATZ` for the Faktor-1,5 limit — a `lookup_fill` twin, because a prod BOOLEAN is not a formula input (the brief's "equation below" for `min_annual_revenue_multiplier` cannot compute).
5. **`M820-23-D1 contract_invalidity_code` not emitted** — boolean input (I-1); REQ-26 already binds both booleans.
6. **Field `visible_when`: 1 (brief: 6)** — `procedure_rationale` (consumed → C-2), `publication_date` / `ted_notice_id` (consumed → C-1), `bewertungskommission_*` (refuted by L843 → J-4; register consumed), `price_weight_percent` (REQ-15 disjunction + consumed → G-1 / C-3; the created twin carries the rule), Bedarfsplanung Konzept / Projekt (refuted by L465 / L679 → J-5). The `min_annual_revenue_multiplier` "limit" row became the lookup_fill twin + G-7.
7. **`award_criteria_list`: footer over an equation row instead of the brief's `sum_column`** (playbook D-2b-5: the legacy client sum retires where an equation exists); the Plan-1 note dropped (F-1; L1696 refutes "Preis und mind. ein qualitatives Kriterium").
8. **`stakeholder_list` gained a `kategorie` enum** (the brief's `count_rows(stakeholder_list, rolle == '<role>')` on the free-text `rolle` column would never match reliably) — the five §5 heads as tokens; `REQ-04` proposal G-5 with the executor's `>= 2` threshold (the Merkblatt prints no "all five").
9. **Lot code denominator = Σ of all lots** (L1366 "Gesamtwertes aller Lose"), not the brief's `estimated_engineering_fee` (J-7); per-lot bound strict `<` (statute) not the paraphrase's `≤` (J-6); `lose_ausnahme_verletzt` added so a violating lot is named, not only the max.
10. **Equations 35 (brief ≈ 9 + register aggregates)**: the brief's list expanded by the constants (4), the lot outputs (7), the commission extras (stimmberechtigt / ungerade), the negotiation extras (count / unsigned), the Tab. D.1 `_ok` codes (2), the threshold consistency code, the stakeholder count.
11. **Sign-off ids 47 (brief ≈ 8)** — every refused / withheld / paired item got its own block: G-1 … G-11, C-1 … C-4, D-1 … D-24 (amendment K: one per pair — the 24 pairs are the honest size of the "hand-typed twins" debt of this standard), E-1 / E-2, J-1 … J-7, O-1, F-1, I-1 / I-2, X-1. The brief's `m820_1-J-1` (edition rule), `J-2` (> 50 Mio), `J-3` (org mapping), `G-1` (REQ-12 → became G-7; G-1 is REQ-15), `G-2` … `G-5`, `D-1` … `D-3`, `X-1` were re-numbered in order of appearance in this report (the brief's letters kept where the class matches).
12. **Section codes**: prod sections are single letters (A B F J K L M); creates land in B (inputs) or F (results) — the brief's "M820-18 register" etc. mapped accordingly.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (20/20); every `verification_quote` / `override_quote` fragment of the modules (136/136) and every `"…" (L…)` fragment on the sheet and in the STAGED file (46/46) was machine-checked against the transcript after being read at the cited line in this session; prod facts (enum tokens, consumer lists, section codes, gate ids / conditions / md5, labels, VR, the 9 stored parameters, `standards.version`) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted all 45 entries and 35 equations first time because the rule set was planned against the capture (consumer lists, the 26 gates, section codes, the 0 equations); the field-config test pins every refusal of the brief's targets (REQ-15 via `gateReaders`, the consumed producers via `emitFieldConfigSql` throws, the IF-guard exemptions on REQ-09 / REQ-18 that would have passed the gate guard alone), G-A3 for ANHB23 / S8_10_3_6 / E1_4_1_UMSATZ, the Plan-1 key preservation against `SELECTION_CONFIGS`, and that no emitted rule hides a consumed or gate-read symbol.
- Runtime facts were probed through the real register / lookup_fill contract before the pins (probe deleted); the render test drives `bewerber` through `RegisterEditor` (Anh. F columns, checkbox toggles moving the counts, an incomplete third row) and the upgraded award register (Preis badge test-id, Σ / Preis / count through `evaluateFormula`).
- Two engine facts drove the design and are on the sheet: a prod boolean is never a formula input (I-1; the two boolean-keyed tables), and scalar-only outputs reading inherited drivers are not server-materialised (I-2; G-3 Step 2 / D-4 … D-10 wait).
- No `String.replace` with a replacement string on any repo file (splices via `split/join` with a presence check that throws before writing); the module files were written with the Write tool; the scratch scripts lived in `scripts/` only while running and were deleted.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section (+317 lines) and the playbook paragraph (+2 lines).

## 10. Concerns (for the controller)

1. **The five hand-typed threshold fields are not yet removed** — E-1 / E-2 (re-binds) and D-4 … D-10 (twins) are STAGED; until then the engineer sees the printed value next to the typed one and the consistency code flags a mismatch. That is the fail-safe reading of amendment J on consumed / gate-bearing inputs.
2. **REQ-07, REQ-05 and REQ-24 sit on M820-04 where none of their symbols resolves** — pending on every project today (data observation, new); G-3 / G-4 propose the moves; the risk-editor gate (REQ-05) is X-1's owner item.
3. **Nothing the registers compute reaches prod's gates yet** — every re-point (G-8 / G-9 / G-10 / G-4 / G-5 / G-11) and every switch (24 D-pairs) is STAGED; the standard remains fully manual for enforcement until the owner ratifies, by design.
4. **G-3 Step 2 / D-4 / D-6 / D-7 / D-9 depend on the engine-output materialisation workstream** (scalar-only outputs are not persisted; a consumer on M820-10 would inherit nothing) — recorded as I-2, not worked around.
5. **Edition token `'2020'`** is a unique-key component and the four legal tables are time-bound (J-1): the owner confirms the printed edition before `20260917101800` is applied and rules how a post-2020 regulation enters (new edition row + `lookup.edition` pin).
6. **J-3 (organisation mapping)** puts only `bundesbehoerde` on 139 000 € — fail-safe (the higher threshold applies longer), but "vergleichbarer Institutionen" is the owner's call.
7. **Bundle growth:** ~3 KB of lifted spans (Task-0 observation; Task 30 measures).
8. **Token/time:** ≈ 0.60 M tokens in, ≈ 75 min wall-clock — above the Task 16 / 17 band (0.55 M / 60 min) because of the 24 amendment-K pairs and the eleven gate blocks; the transcript itself is cheap (20 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The Doppelbewertungsverbot per criterion (L979 — one boolean today, no per-criterion check); the Bewerbergemeinschaft insurance rule (L900 / L1631 — sums of the members, no per-member register); the Tagesordnung and the participants of each Verhandlungsgespräch (L1027 — the register has a Thema column, no participants); the Rechtsmittelbelehrung recommendation in the Absageschreiben (L1062); the "Zuschlagskandidat namentlich" letter content (L1062 — `winning_bidder` is the name, no letter record); the Bewerbungsphase document list (L985 … — no checklist); the Planungswettbewerb branch (§ 8.11, `procurement_procedure = planungswettbewerb` has no worksheet content); Anh. A / Anh. C / Anh. F beyond Nr. 6 / 8 / 9 (bespoke editors, text). Each is a procedural sentence or a text rule without a computable value and stays outside this encoding (content-boundary rule) or on the sign-off sheet.

---

# Fix round 1 (review verdict on `bc4583f`: APPROVED with two Important items + minors)

- **Commit:** see the reply (one commit on top of `bc4583f`, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; the field-config and equations migrations were regenerated by the Task 0 emitters only.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling.

## IMPORTANT

1. **G-3 Step 1 condition parenthesised.** The staged text `IF a == 1 THEN x == 'o' AND IF a == 0 THEN x == 'u'` parsed as ONE `guard` whose body swallowed the second IF (`parseCondition(...).kind === 'guard'`, confirmed in-session) — the unterschwellig half was never enforced. `scripts/verification/m820_1-STAGED-plan3-rulings.sql` G-3 Step 1 now reads `'(IF oberschwellig_code == 1 THEN threshold_status == ''oberschwellig'') AND (IF oberschwellig_code == 0 THEN threshold_status == ''unterschwellig'')'` — prod's original REQ-07 shape; the block's CAUTION explains the parse. Sign-off G-3 block text rewritten. Parse-shape pin added to `field-configs-m820-1.test.ts`: the staged text parses to `and` of `['guard', 'guard']`, the unparenthesised text to `guard` (the defect), and prod's captured REQ-07 condition starts with `(IF `.
2. **Amendment K: the lot share reads the existing fee.** `equations/m820_1.ts`: `LOSE_DENOMINATOR = 'estimated_engineering_fee'` (the -01 input, inherited on -09, L1368 "Gesamtauftragswerts"); `M820-09-D11 lose_ausnahme_anteil_pct = Σ ausgenommen · 100 / estimated_engineering_fee` and `M820-09-D14 loseausnahme_code` (same denominator), `input_symbols = ['lose', 'estimated_engineering_fee']`; `M820-09-D9 lose_gesamt_eur` stays as the display-only register Σ; NEW `M820-09-D15 lose_gesamt_ok = if(abs(sum_rows(lose, netto_wert_eur) - estimated_engineering_fee) < 0.005, 1, 0)` (L1366 "Gesamtwertes aller Lose" — the Σ is INLINE, never chained on the new output `lose_gesamt_eur`, Task 16 trap 2; pinned `input_symbols` do not contain it) with its created derived field `lose_gesamt_ok` (-09 F, also in the register footer). `field-configs/m820_1.ts`: register note / labels / descriptions of D9 / D11 / D14 reworded to the fee as the reference; J-7 rewritten on the sheet (emitted = the fee; the register-Σ reading is the ALTERNATIVE), D-25 added (fee ↔ register Σ, no switch proposed). **Consequence recorded (I-2, J-7):** D11 / D14 / D15 are register-fed rows that name an INHERITED scalar — they compute on the form / report / snapshot but the save-path materialiser resolves own fields only, so they persist null on save (Task 17 trap 1); the previous register-Σ form was self-contained. Re-pinned: share 4,545 % at fee = Σ = 1 100 000, 5 % at fee 1 000 000, `manual_required` without the fee; code 1 / 0 / 1 with the fee supplied; D15 = 1 at |Δ| = 0,004, 0 at 100 000 difference, `manual_required` on an empty register. Migrations `20260917101810` (46 entries: 43 create / 3 update) and `20260917101820` (36 equations) re-emitted; verifier 20/20 unchanged.

## MINOR

3. `equations-m820-1.test.ts` comment "(strict, m820_1-J-5)" → J-6.
4. Sheet I-2: "20 scalar-only rows" → **12** (-09 D1 … D7, -13 D1, -22 D1 … D4), 24 register-fed rows materialised — minus the three fee-reading rows of item 2.
5. Sheet: D-7 … D-10 and D-21 … D-24 are now ONE ☐ block per pair (the STAGED file already listed them per pair); +D-25 ⇒ **54** m820_1 blocks on the sheet (47 → 54: +6 splits, +1 D-25), 42 ☐ blocks in the STAGED file.
6. Sheet O-1: added that the 2,0 row prints the statutory "darf der Auftraggeber nicht mehr als das Zweifache des geschätzten Auftragswerts verlangen" (L1623, § 45 Abs. 2 VgV) — an `anhaltswert` override of the fill can never loosen it, REQ-12 still enforces ≤ 2,0 on the typed input; only the 1,5 recommendation is overridable.

## Raw output (once, before the commit)

```
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m820_1 20260917101810
  → wrote 46 field entries + 0 section entries for m820_1 (0 warnings)
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m820_1 20260917101820
  → wrote 36 equations for m820_1
pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m820_1 "<transcript>"
 20/20 quotes verbatim in C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-1\DWA-M_820-1.md
sheet + STAGED quote fragments re-checked (scratch one-liner): 49 verbatim, 2 "…"-elided fragments verified by grep (L1366)
pnpm vitest run --project unit <seed-m820-1, field-configs-m820-1, equations-m820-1, register-m820-1-bewerber, generated-sql-freshness>
 Test Files  5 passed (5)
      Tests  33 passed (33)
pnpm test
 Test Files  318 passed | 1 skipped (319)
      Tests  2921 passed | 1 expected fail | 1 skipped (2923)
   Start at  15:16:07 · Duration  51.48s
pnpm -s typecheck → exit 0 · pnpm -s eslint <4 touched ts files> → exit 0, no output
```
(net 0 tests — the new pins sit inside existing cases.) **The integration harness was not re-run** (per the controller): this round changed emitted DATA (three formulas, one added equation + field, labels / descriptions), a STAGED comment block and sheet text — no engine, harness seed, or prod-shaped row the DWA-M-820-1 harness reads; the 26/26 run above stands. Counts after the fix round: field entries **46** (43 create / 3 update), derived fields 36, equations **36**, visibility rules 1, sign-off blocks 54, STAGED ☐ blocks 42 (0 non-comment lines).
