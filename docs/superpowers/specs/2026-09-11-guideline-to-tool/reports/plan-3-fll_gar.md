# Plan 3 — FLL-GAR-2023 (`fll_gar`) — Task 7 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 8)
- **Model / effort:** Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-17
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `fde8316`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs FLL-GAR-2023 fll_gar` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (`worksheet_templates` / `standards` column lists, the 29 worksheet titles, `standards.version`, the 4 equation rows with formula + md5, the 30 compliance rows with ids / severities / md5 — the seven conditions longer than 110 chars read in 110-char `substr` chunks —, section titles of two worksheets, the labels / units / `validation_rules` of the fields in scope). `.env.local` never printed; confirmed gitignored.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Gewässerabdichtungsrichtlinien.md` (Ausgabe 2023, 6678 lines, flattened PLAIN TEXT — not LaTeX; printed rows are split over several lines and hyphenated at line ends); the 151 quote spans were lifted mechanically by line range (a throwaway `gen-quotes.mjs` in the scratchpad, `slice/join`, never `String.replace`), the seed file assembled by `cat` head + fragment + tail; the inventory was used for pointers only (its Tab.-26 "alignment uncertain" and the brief's Tab.-26 6-column list / Tab.-28 "nein" state were refuted by the transcript — §8). The FLL-revision batch (`feat/fll-revision`, `.superpowers/sdd/fll-m2/FLL-GAR-2023__FLL-GAR-NN.json`) was read for finding IDS only (cross-references), never as a source. The PDF was not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `fll_gar` | 20 | 107 (107/107 verbatim) | 0 rows skipped; 2 CELLS seeded null (TAB28 `zero|freiflaeche` / `zero|schwimmteich` — the printed row "0" carries one cell, U-4); 2 tables kept `imported_unverified` (TAB26 U-1 alignment → PDF, TAB28 U-4); Tab. 2 garbled (U-2), Tab. 3 a requirement list (J-1), Tab. 9–11/14/15/17/19/20/21/23/29 + Anhang 3 out of scope (U-3) | 9 (all `create`) | 7 (all `create`; fix round 1 removed `standort_tab18`) | 0 | 20 (all `create`; the `nahtbreite_min_mm` re-bind is STAGED, E-2 — fix round 1) | 4 (3 on created fields + 1 UPDATE `verzinkung_dicke_um`) | 104 (12 material worksheets × 9 sections − 4 producer sections, C-2) | 19 (fix round 1 removed FLL-GAR-05-D3) | 0 replacements emitted; R-1 (Gl. 1 onto Σ), R-2 (Gl. 2b split), E-2 STAGED; 13 gate blocks (G-1…G-13), 4 consumer blocks (C-1…C-4), 6 derivation blocks (D-1…D-6) STAGED | 46 (+10 observations) | ≈ 0.80 M (incl. fix round 1) | ≈ 115 |

Field entries (after fix round 1): 60 = 59 `create` (9 registers, 7 select_one, 20 lookup_fill, 19 derived outputs, 3 scalar inputs, 1 attestation) + 1 UPDATE (`verzinkung_dicke_um` ← `stahl_typ == 'unlegiert'`) — 4 scalar (3 created + 1 UPDATE). The first commit's `nahtbreite_min_mm` re-bind (E-2) and `standort_tab18` / `s_klasse_code` / FLL-GAR-05-D3 (D-4) were withdrawn in fix round 1. Every other Step-4 target of the brief is a consumed producer (transitive guard) or driver-less on its worksheet → C-2 / C-3 / C-4 (STAGED).

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  270 passed | 1 skipped (271)
      Tests  2575 passed | 1 expected fail | 1 skipped (2577)
   Start at  23:39:01
   Duration  40.84s (transform 12.80s, setup 44.13s, import 93.13s, tests 37.97s, environment 172.53s)
```
(2542 at `fde8316` + 33 from this task: seed-fll_gar 12, field-configs-fll_gar 7, equations-fll_gar 11, register-fll-gar-lagen 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output.

Integration harness (embedded PG; the whole `fll-gar*` set + `seed-fll-gar-smoke` + `_verify-fll-gar09` + the `verify-fll-gar-*` files — 30 files; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/seed-fll-gar-smoke.integration.test.ts tests/harness/fll-gar02-scope.integration.test.ts tests/harness/fll-gar03-verify.integration.test.ts tests/harness/fll-gar04-verify.integration.test.ts tests/harness/fll-gar05-verify.integration.test.ts tests/harness/fll-gar06-verify.integration.test.ts tests/harness/fll-gar07-verify.integration.test.ts tests/harness/fll-gar08-verify.integration.test.ts
 Test Files  8 passed (8)
      Tests  30 passed (30)
   Duration  9.83s (transform 2.90s, setup 0ms, import 51.56s, tests 22.79s, environment 1ms)
pnpm vitest run --project integration tests/harness/fll-gar10-verify.integration.test.ts tests/harness/fll-gar11-verify.integration.test.ts tests/harness/fll-gar12-verify.integration.test.ts tests/harness/fll-gar-13-verify.integration.test.ts tests/harness/fll-gar15-verify.integration.test.ts tests/harness/fll-gar16-verify.integration.test.ts tests/harness/fll-gar18-verify.integration.test.ts tests/harness/fll-gar19-verify.integration.test.ts tests/harness/fll-gar20-verify.integration.test.ts tests/harness/fll-gar21-verify.integration.test.ts tests/harness/fll-gar23-verify.integration.test.ts
 Test Files  11 passed (11)
      Tests  32 passed (32)
   Duration  12.22s (transform 4.72s, setup 0ms, import 86.82s, tests 41.22s, environment 2ms)
pnpm vitest run --project integration tests/harness/fll-gar24-verify.integration.test.ts tests/harness/fll-gar26-verify.integration.test.ts tests/harness/fll-gar27-qnot.integration.test.ts tests/harness/fll-gar28-verify.integration.test.ts tests/harness/_verify-fll-gar09.integration.test.ts tests/harness/verify-fll-gar-01.integration.test.ts tests/harness/verify-fll-gar-14.integration.test.ts tests/harness/verify-fll-gar-22.integration.test.ts tests/harness/verify-fll-gar-25.integration.test.ts tests/harness/verify-fll-gar-27-agent.integration.test.ts tests/harness/verify-fll-gar-29-agent.integration.test.ts
 Test Files  11 passed (11)
      Tests  57 passed (57)
   Duration  12.25s (transform 4.50s, setup 0ms, import 108.31s, tests 17.48s, environment 4ms)
(30 files / 119 tests in three batches on 2026-09-18 — a first single-invocation run over all 30 files, started in a background shell with a `$(sed …)` argument list, never printed a vitest summary after 25 minutes and was stopped; the batched foreground runs above are the record.)
```
The harness seeds prod's fields / gates / equations (`tests/harness/seed-fll-gar.ts`, `_fll-snapshot-raw/`); the new rows are not in it by construction — it proves the branch still enforces every FLL-GAR gate both ways where drivable and that `abdichtungs_art` is overridden in memory there ("the discriminator `abdichtungs_art`, which is NOT a field here" — the harness knows what C-1 fixes).

**TDD evidence.** Runtime facts were established BEFORE encoding with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` (`scripts/_rt-check-tmp.ts`, deleted): a row `lookup('TAB1', abdichtungs_art, …)` with the symbol out of scope yields null and makes `count_rows(reg, ok == 0)` `manual_required`; `min_rows` computes without the driver; a string-valued `derived` (`hoehe_band`) feeds a two-key `lookup('TAB28', …)`; `count_rows(reg, aggressiv == true)` is decidable over unset booleans; `if(gewaesser_type IN {…}, 0, 1)` computes on an enum string; **`evaluateFormula` checks every named scalar input before evaluating** (the R-class formula with the Versatz input missing is `manual_required` even when the `if()` would never reach it — recorded, pinned, labels say "0, wenn keine"); the literal-key `lookup('TAB5', 'verguetet', …)` computes with `input_symbols = []`; the Tab.-24 AND-chain over three inputs computes; a non-printed Tab.-22 combination is a null row (`manual_required`). The first version of the scratch helper extracted string LITERALS as symbols (`missing: ['deponie', …]`) — replaced by `extractSymbols` (what the emitter uses) before reading any result. Seed builders: the row dump found two parser slips before any test existed — Tab. 12 "7 mm 15 mm" swallowed the "1" of "15" as a footnote (fixed with a whole-token lookahead; pinned) and the markdown escapes (`GU\*`, `\-`, `\>`) needed `deMd` before cell matching (pinned). Seed test written → RED on my own `makeTableLookup` signature slip (`(code, keys[])`, not variadic) → 12/12 GREEN; verifier **107/107** on its first run. Field-config test → RED on two pins of mine (the PELD/PEHD `frag()` end markers; `rissklasse` is consumed by -12 too) → 7/7; the emitter accepted all 63 entries + 104 sections on its first run (the Step-4 rule set had been planned against the capture's `consumer_worksheets` + `equations` before emitting). Equations test 11/11 and render test 3/3 GREEN first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts fll_gar "C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Gewässerabdichtungsrichtlinien.md"` → **`107/107 quotes verbatim`**, exit 0. PASS list (table · rows · span):

- TAB1 (8): mineralisch_ohne_zusatzstoffe (L1398–L1399), mineralisch_mit_zusatzstoffen (L1400–L1401), mineralisch_hydraulisch (L1402), verbundwerkstoff_gtd (L1406), bahn_bitumen (L1407), bahn_kunststoff_elastomer (L1408), fluessigkunststoff (L1409), bahn_pe (L1410)
- TAB1_ASPHALT (3): asphaltmastix (L1403), gussasphalt (L1404), asphaltbeton (L1405)
- TAB4 (2): natuerlich (L1931–L1944), industriell (L1946–L1951) · TAB5 (1): verguetet (L2231–L2246)
- TAB6 (2): le40 (L2437–L2439), gt40 (L2440) · TAB7 (5): wasserbecken_teich (L2523–L2524), bachlauf (L2525–L2526), schwimmbecken_bade (L2527–L2534), schwimmbecken_sole (L2536–L2548), pflanzenklaeranlage (L2550–L2556) · TAB8 (8): waende|* (L2578), bodenplatte|* (L2579)
- TAB12 (3): asphaltmastix (L2920), gussasphalt (L2921), asphaltbeton (L2922–L2923) · TAB13 (2): Na / Ca (L3109–L3128) · TAB16 (4): false|quellgegendruck (L3288–L3297), false|austrocknung_frost (L3299–L3301), true|quellgegendruck (L3303–L3312), true|austrocknung_frost (L3314–L3316)
- TAB18_W (3): W1-B (L3673), W2-B (L3674), W3-B (L3675) · TAB18_R (4): R0-B (L3677), R1-B (L3678–L3680), R2-B (L3682–L3684), R3-B (L3686–L3688) · TAB18_S (2): S1-B (L3692–L3693), S2-B (L3695–L3696)
- TAB22 (12): quellschweissen|EVA/PIB/PVC-P (L4109–L4116), heissluft_heizkeil|ECB/EVA/PIB/FPO/PVC-P/TPE/EPDM (L4118–L4133), heissvulkanisation|EPDM (L4134), heissluft_pbs|EPDM_PBS (L4135) · TAB22_UEBERLAPPUNG (2): false / true (L4098–L4102)
- TAB24 (7): dichte (L4471), mfr (L4473–L4474), russgehalt (L4476–L4479), russverteilung (L4480), massaenderung (L4481–L4485), nctl (L4487–L4492), oit (L4494–L4498) · TAB25 (4): gering (L4539–L4553), mittel (L4555–L4572), hoch (L4574–L4589), freiliegend (L4591–L4599)
- TAB26 (20): GE/GW/GI (L5384), SE/SW/SI (L5385), GU/GU*/GT/GT* (L5386), SU/SU*/ST/ST* (L5387), UL/UM/UA (L5388), TL/TM/TA (L5389) · TAB27 (3): swk1 (L5430–L5435), swk2 (L5437–L5445), swk3 (L5447–L5456) · TAB28 (12): ge15|* (L5744), ge10|* (L5745), ge5|* (L5746), zero|* (L5747)

Status per table: `md_verified` — TAB1, TAB1_ASPHALT, TAB4, TAB5, TAB6, TAB7, TAB8, TAB12, TAB13, TAB16, TAB18_W, TAB18_R, TAB18_S, TAB22, TAB22_UEBERLAPPUNG, TAB24, TAB25, TAB27 (every seeded row lifted, every cell legible). `imported_unverified` — TAB26 (the plan flagged the column alignment; each printed row carries exactly five cells in the head order L5350–L5382 — left to the PDF, U-1), TAB28 (the printed row "0" carries ONE cell, U-4).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs FLL-GAR-2023 fll_gar
  → wrote src\lib\eval\field-configs\fll_gar.prior.json: 174 field rows (28 orphan), 261 coded sections of 261, 4 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts fll_gar
  → wrote seed + rollback for fll_gar -> scripts/migrations/20260917100700_regulation_tables_seed_fll_gar.sql scripts/rollback-20260917100700-regulation-tables-seed-fll-gar.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts fll_gar 20260917100710
  → wrote 63 field entries + 104 section entries for fll_gar -> scripts/migrations/20260917100710_field_configs_fll_gar.sql scripts/rollback-20260917100710-field-configs-fll-gar.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts fll_gar 20260917100720
  → wrote 20 equations for fll_gar -> scripts/migrations/20260917100720_equations_fll_gar.sql scripts/rollback-20260917100720-equations-fll-gar.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-fll_gar.test.ts` / `equations-fll_gar.test.ts` pin the other two against the committed `fll_gar.prior.json`). The seed migration carries 107 row upserts (`ON CONFLICT (table_id, row_key)`) and 18 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the twenty tables (no earlier FLL-GAR-2023 table seed and no Plan-1 selection config exist for this standard — nothing superseded). The field-config migration: 61 `INSERT … WHERE NOT EXISTS`, 2 `UPDATE fields` (`nahtbreite_min_mm` widget / lookup; `verzinkung_dicke_um` visible_when), 104 `UPDATE worksheet_sections`; the rollback deletes the 61 `Plan 3:` rows and restores the two rows / 104 sections to the captured NULLs. The equations rollback deletes the 20 `Plan 3:` rows. Apply order (owner, after the schema migration incl. the plan1-D-3-1 legacy rename): `20260917100700` → `20260917100710` → `20260917100720`; rollback in reverse. Every file touches `FLL-GAR-2023` rows only.

## 5. What was encoded

**Tables (20)** — `src/lib/eval/regulation-tables-seed-fll_gar.ts`, builder `fllGarSeedTables()`, edition `'2023-12'` (title page L7 "Ausgabe 2023", imprint L133 "2. Ausgabe, 1.000 Exemplare, Bonn, Dezember 2023" = prod `standards.version` "Dezember 2023 (2. Ausgabe; Erstauflage 2005)"):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB1 | abdichtungs_art (8 prod tokens with a printed row) | neigung_max_1m (m of "1:m" — flatter = larger m), gefaelle_max_pct, gedruckt | anhaltswert (L1391 "Richtwerte" — L1383 "entbinden nicht von einer rechnerischen Überprüfung") | 8 | md_verified (E-1, J-7) |
| TAB1_ASPHALT | mischgutart (created select) | same | same | 3 | md_verified |
| TAB4 | mineral_typ (created select) | abdichtung_min_cm 30/10, lagen_min 2/–, lage_min_cm 15, lage_max_cm 20, auflast_min_cm 30/20, eignungspruefung, gedruckt | anhaltswert (L1953 "Abweichungen von 10% … zulässig" — L1921) | 2 | md_verified |
| TAB5 | typ ('verguetet') | same | anhaltswert (L2248 — L2222) | 1 | md_verified |
| TAB6 | dicke_band (le40 / gt40) | wz_max 0,60 / 0,70, zement_min_kg_m3 280 / –, fck_min C25/30 / –, gedruckt | locked (L2448 — L2428) | 2 | md_verified |
| TAB7 | anwendungsfall (prod tokens) | festigkeitsklasse, festigkeit_wechselzone (J-3), festigkeit_gedruckt, expositionsklassen (as printed), feuchtigkeitsklasse WF/WA, c_nom_mm 40/55 | locked (L2462 — L2502) | 5 | md_verified |
| TAB8 | bauteil × ausfuehrung (prod tokens) | dicke_min_mm 240/240¹/200/240² · 250/–/200/250², fussnote, gedruckt | locked (L2565 — L2580–L2581) | 8 | md_verified |
| TAB12 | mischgutart | dicke_min_mm 7/25/40, dicke_max_mm 15/40²/–, dicke_mittel_mm 10/–/–, hohlraum_max_pct –/–/3, einlagig, fussnote, gedruckt | locked (L2915 — L2924–L2925) | 3 | md_verified |
| TAB13 | bentonit_typ (Na / Ca) | mclay_min_g_m2 3600/8000, wassergehalt_max_pct 15, quellvermoegen_min_ml 24/8, montmorillonit_min_mg_g 300 | locked (L3106) | 2 | md_verified |
| TAB16 | beschichtung ('false'/'true' = String(prod boolean), I-1) × funktion (prod tokens) | auflast_min_m 0,30/0,60/0,30/0,60 (J-4), art_der_gtd | anhaltswert (L3279 — L3320) | 4 | md_verified |
| TAB18_W / _R / _S | klasse (prod tokens) | W: fuellhoehe_max_m 5/10/–, fuellhoehe_gt_m –/–/10; R: rissbreite_max_mm –/0,2/0,5/1,0, rissversatz_max_mm –/–/–/0,5, beschreibung; S: beschreibung, mit_bauwerk_verbunden | locked (L3662–L3665 — L3717–L3719) | 3 + 4 + 2 | md_verified |
| TAB22 | fuegeverfahren × material (prod tokens) | nahtbreite_min_mm 30·30·30 / 20×6·30 / 20 / 40, stoffart | locked (L4101–L4102) | 12 | md_verified |
| TAB22_UEBERLAPPUNG | polymerbitumen ('false'/'true') | ueberlappung_min_mm 40 / 60 | locked (L4098–L4100) | 2 | md_verified |
| TAB24 | eigenschaft (7 printed rows) | min, min_strict, max, unit, norm, gedruckt | locked (L4460–L4462) | 7 | md_verified |
| TAB25 | beanspruchung (prod tokens) | peld_min_mm 0,8/1,5/–/–, pehd_min_mm –/1,0/2,0/2,5, beanspruchung_text, beispiele | anhaltswert (L4531 — L4533–L4534) | 4 | md_verified |
| TAB26 | baugrund (20 DIN 18196 classes, J-6) | sand_min_cm 10/–/10/5/5/5, vlies_ok, bautenschutzmatte_ok, kunststoffbahn_ok, beton_ok, schutzlage_erforderlich, werkstoffe_text, gedruckt | locked (L5494–L5496) | 20 | imported_unverified (U-1) |
| TAB27 | swk (swk1…3, created select) | belastung_kn 8,2/17,1/43,7, nutzung, fahrzeug_max_t –/2,5/16, flaechengewicht_min_g_m2 300/500/800, werkstoff | locked (L5422–L5423 — L5472–L5473) | 3 | md_verified |
| TAB28 | hoehe_band (ge15/ge10/ge5/zero) × anwendungsfall (prod tokens) | hoehe_min_cm, zulaessig x / x_bedingt / sonder / null (J-5, U-4), fussnote, gedruckt, hinweis | locked (L5724 — L5748) | 12 | imported_unverified (U-4) |

Not seeded: Tab. 2 (garbled — U-2), Tab. 3 (requirement list — J-1), Tab. 14 (legible but no driver field — U-3), Tab. 9–11/15/17/19/20/21/23/29, Anhang 3 (U-3).

**Registers / selections / lookup_fills / visibility** — `src/lib/eval/field-configs/fll_gar.ts` (created in the captured sections C / D; `einzugsflaechen_not` in B where A and C live):
- `boeschungsabschnitte` (-07): zone (Sumpf/Flach/Tief, L1418), neigung_1m, gefaelle_pct, laenge_m, `limit_1m` (TAB1 on the worksheet `abdichtungs_art` — inert until C-1), `ok` badge; footer `boeschung_steilste_1m`, `boeschung_verletzungen`; fill `boeschungsneigung_limit` (TAB1, "Schlüssel fehlt" until C-1).
- `abdichtungslagen` (-09): position, `rolle` (discriminator: schutzlage_unten / abdichtung / schutzlage_oben / auflast), `material` (all 12 prod tokens — not a TAB1 lookup_key, which would offer 8, §8), `neigung_limit` badge, dicke_mm, flaechengewicht_g_m2, TAB26 `baugrund` lookup_key + `sand_min_cm` / `werkstoffe_tab26` lookup_values (lower layer only), TAB27 `swk` lookup_key + `fg_min` (upper layer only), `fg_ok` badge; footer `lagen_gesamtdicke_mm`, `abdichtungslagen_count`.
- `naehte` (-16): label, fuegeverfahren, material, nahtbreite_ist_mm, `nahtbreite_min` (TAB22 per row), `ok`, pruefergebnis; footer `naht_verletzungen`. `randabschnitte` (-23): label, anwendungsfall (discriminator), hoehe_cm, randbefestigung, kapillarsperre, `hoehe_band`, `zulaessig` (TAB28 per row); footer `randabschnitte_sonder`. `durchdringungen` / `pflanzenarten` (-24, footers `durchdringungen_count` / `pflanzen_aggressiv_count`), `pruefungen` (-25, capture-only), `einzugsflaechen_not` (-27: label, a_m2, c, `ac`; footer `sum_a_m2`, `sum_ac`), `wartungsmassnahmen` (-28, capture-only).
- Selects: `mineral_typ` (-10, the two Tab.-4 row labels), `mischgutart` (-13, the three Tab.-12 row labels), `neurissbildung` / `standort_tab18` (-05, Tab.-18 wording), `bahn_vorkonfektioniert` (-16, L4064–L4067), `pe_werkstoff` (-18, L4455–L4456), `baugrund_klasse_18196` (-22, 20 classes) and `swk_klasse` (-22, the three Tab.-27 rows with their Beanspruchung).
- lookup_fills (role limit unless noted): -07 `boeschungsneigung_limit` ← TAB1; -10 `schichtdicke_abdichtung_min` / `schichtdicke_auflast_min` ← TAB4 by `mineral_typ`; -12 `festigkeitsklasse_soll` / `expositionsklassen_soll` / `feuchtigkeitsklasse_soll` (text) / `c_nom_min` ← TAB7 by `anwendungsfall_concrete`, `bauteildicke_min` (mm) ← TAB8 by (`bauteil_type`, `beton_ausfuehrungsart`); -13 `asph_dicke_min` / `asph_dicke_max` ← TAB12, `asph_neigung_max_1m` ← TAB1_ASPHALT by `mischgutart`; -14 `bentonit_flaecheneinheit_min` / `quellvermoegen_min` ← TAB13 by `bentonit_type`, `gtd_auflast_min_m` ← TAB16 by (`gtd_polyolefin_beschichtung`, `gtd_auflast_funktion`); -16 `nahtbreite_min_mm` (UPDATE, E-2) ← TAB22 by (`fuegeverfahren`, `bahn_material_naht`), `naht_ueberlappung_min_mm` ← TAB22_UEBERLAPPUNG by `polymerbitumen_beschichtung`; -18 `peld_nenndicke_min_mm` / `pehd_nenndicke_min_mm` ← TAB25 by `pe_beanspruchung_klasse` (visible per `pe_werkstoff`); -22 `sl_schutzlage_unten_sand_min_cm` / `sl_schutzlage_unten_werkstoffe_tab26` (text, role value) ← TAB26 by `baugrund_klasse_18196`, `sl_schutzlage_oben_flaechengewicht_min` ← TAB27 by `swk_klasse`.
- Field `visible_when` (4): `eisdruck_randschutz_vorgesehen` ← `eisbildung_moeglich == true` (created attestation for REQ-06, G-3), the two Tab.-25 fills ← `pe_werkstoff`, `verzinkung_dicke_um` ← `stahl_typ == 'unlegiert'` (L4817–L4819). Section rules (104): every section of FLL-GAR-10 … -21 except the four producer sections C of -10 / -12 / -14 / -16 (C-2), each `abdichtungs_art == '<its token>'` — INERT until C-1 (the driver is not inherited anywhere). Withheld: the three Tab.-18 enums on -05 (C-3), `anzahl_lagen` (C-4).

**Equations (20 new)** — `src/lib/eval/equations/fll_gar.ts`: `FLL-GAR-02-D1 gewaesser_in_scope_code` (§1.1) · `-05-D1 w_klasse_code` (Tab. 18 W) · `-05-D2 r_klasse_code` (Tab. 18 R, code 9 outside — J-2) · `-05-D3 s_klasse_code` (Tab. 18 S) · `-07-D1 boeschung_steilste_1m = min_rows(…)` · `-07-D2 boeschung_verletzungen = count_rows(…, ok == 0)` · `-09-D1 lagen_gesamtdicke_mm` · `-09-D2 abdichtungslagen_count` · `-11-D1 mz_dicke_min_tab5` (literal-key lookup) · `-12-D1 wz_max` (Tab. 6 by band) · `-14-D1 groesstkorn_max_mm` (F-1) · `-16-D1 naht_verletzungen` · `-16-D2 bahnendicke_min_mm` (F-2) · `-18-D1 pe_rhizom_nachweis_code` · `-18-D2 pehd_tab24_code` (Tab. 24 AND-chain) · `-23-D1 randabschnitte_sonder` · `-24-D1 durchdringungen_count` · `-24-D2 pflanzen_aggressiv_count` · `-27-D1 sum_a_m2` · `-27-D2 sum_ac`. None outputs `g_prime`, `Delta_u` or `Q_NOT` (R-1 / R-2 STAGED). Pinned values (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): the four exclusions → 0 / four uses → 1; W 5 / 5,01 / 10 / 10,5 m → 1 / 2 / 2 / 3; R (ausgeschlossen) 0, 0,2 → 1, 0,5 → 2, 1,0 + 0,5 → 3, 1,0 + 0,6 → 9, 1,2 → 9; S aussen_frei 1 / aussen_bauwerk 2 / innen 2; slopes 3 / 1 / 1,5 against Kunststoffbahnen 1:1,5 → steepest 1, one violation (GTD 1:3 → two); no driver → limits null and D2 `manual_required`, Stahl → null (E-1); layers Σ 8 mm / 2 Abdichtung rows with Sand 5 cm (UL) and SWK 2 → 500 g/m² badge; 30 cm; 0,60 / 0,70 at 40 / 40,1 cm; 16 / 32 at U 4,9 / 5; 1,2 / 1,0; PEHD 0 / PELD 1; Tab. 24 strict 0,940; seams 30 / 30 / 40 / 20 with two violations, a non-printed combination `manual_required`; Tab. 28 (X) / -¹ / X / -¹ → 2 Sonderkonstruktionen, the empty row-0 cell `manual_required`; 2 penetrations, 2 aggressive species; the Anhang-1 example 800 m² × C 1 → Σ A 800, Σ A·C 800 → (607·800 − 316·800)/10000 = 23,28 l/s (L6485) in the summed form.

## 6. Inventory §5 top-wins walk

1. `abdichtungs_art` as master selector → **encoded but INERT**: the 104 section rules, the Tab.-1 slope fill (-07) and the Tab.-1 limit per Böschungsabschnitt are emitted, the Nenndicken tables (Tab. 4 / 5 / 8 / 12 / 25) fill on their material worksheets from their own drivers; the rhizome rule is encoded for PE only (-18, D-5). Nothing keyed on `abdichtungs_art` lights up until the consumer edit C-1 — prod's `consumer_worksheets = ["FLL-GAR-10..21"]` is not a worksheet code (the same defect keeps REQ-05 / REQ-12 … REQ-22 `pending` today).
2. Tab. 18 classification derived → **encoded** (`w_klasse_code` from `fuellhoehe_m`; `r_klasse_code` from the created `neurissbildung` + Rissbreite + Rissversatz, R-class partial per the brief — code 9 outside the printed classes; `s_klasse_code` from the created `standort_tab18`); the manual enums stay producers (D-1 / D-3 / D-4).
3. Concrete track → **encoded** (four Tab.-7 fills incl. c_nom, the Tab.-8 minimum thickness by Bauteil × Ausführungsart, `wz_max` by the Tab.-6 band); the gates stay on their literals (G-5).
4. Seams and PE → **encoded** (`nahtbreite_min_mm` now filled from Tab. 22 on its own two keys, the overlap limit 40 / 60, the `naehte` rows; Tab.-25 Nenndicke per material via the created `pe_werkstoff`; Tab.-13 minima per Bentonit type; Tab.-16 Auflast by coating × function with the boolean keyed as 'true'/'false'); gates G-8 / G-9 / G-10 STAGED.
5. Sealing build-up as repeatable layers → **encoded** (`abdichtungslagen` with Tab.-26 Baugrund → Sand / Werkstoffe on the lower protection layer and Tab.-27 SWK → Flächengewicht on the upper one, Σ thickness and count); Tab. 26 partial only in status (`imported_unverified`, U-1 — every printed row is seeded); the `sl_*` scalars keep their own Tab.-26 / Tab.-27 fills on -22.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`fll_gar` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (fll_gar lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+46 blocks, +10 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 7)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-fll_gar.ts`, `src/lib/eval/field-configs/fll_gar.ts`, `src/lib/eval/field-configs/fll_gar.prior.json` (captured), `src/lib/eval/equations/fll_gar.ts`, `src/lib/eval/__tests__/regulation-tables-seed-fll_gar.test.ts`, `src/lib/eval/__tests__/field-configs-fll_gar.test.ts`, `src/lib/eval/__tests__/equations-fll_gar.test.ts`, `src/components/worksheet/__tests__/register-fll-gar-lagen.test.tsx`, `scripts/verification/fll_gar-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917100700_regulation_tables_seed_fll_gar.sql` + `scripts/rollback-20260917100700-regulation-tables-seed-fll-gar.sql`, `scripts/migrations/20260917100710_field_configs_fll_gar.sql` + `scripts/rollback-20260917100710-field-configs-fll-gar.sql`, `scripts/migrations/20260917100720_equations_fll_gar.sql` + `scripts/rollback-20260917100720-equations-fll-gar.sql`, this report (+ the copy at `.superpowers/sdd/…/task-7-report.md`).
Untouched: every a138 / din1989_1 / a262e / m277e / m1200_1 / m1200_3 file; the Plan-1/2 migrations; `SELECTION_CONFIGS` (no FLL-GAR entry exists); the FLL harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **The master switch is not inherited by any worksheet** (`abdichtungs_art.consumer_worksheets = ["FLL-GAR-10..21"]`, matched exactly by `loadInheritedFields`) — the brief's "REQ-12…22 already guard by `abdichtungs_art`" holds in text only; every rule / fill / equation keyed on it is emitted INERT and C-1 is the enabling ruling. The same class of token (`'All'`, `'FLL-GAR-09..21'`, `'FLL-GAR-11..14'`) sits on 12 further fields (X-5).
2. **TAB1 covers 8 of 12 tokens**: the three asphalt rows depend on the Mischgutart → `TAB1_ASPHALT` keyed on the created `mischgutart` (fill on -13); `mineralisch_bitumen` / `stahl` / `alkalisilikat` / `gup` have no TAB1 row (E-1); row 12 "PEHD" is mapped to `bahn_pe` (J-7).
3. **TAB26 has FIVE value columns**, not the brief's six (Bautenschutzmatten/-platten aus Gummi- oder PE-Granulat is ONE printed column); its rows are unambiguous in the transcript (five cells each) — kept `imported_unverified` (U-1) per amendment F because the plan flagged it and the PDF decides; the register's `material` column is an enum over all 12 tokens (a TAB1 `lookup_key` would offer 8).
4. **Tab. 28 prints no "nein"** — the brief's `count_rows(randabschnitte, zulaessig == 'nein')` can never fire; `randabschnitte_sonder` counts the "-¹" cells (J-5); the row "0" has two EMPTY cells (U-4).
5. **Booleans are not formula inputs** (`engineInputValue`): the brief's R0-B "separate boolean" became the enum `neurissbildung`; the Gartenteich rule needs the enum `bahn_vorkonfektioniert`; the PEHD/PELD distinction the brief keyed on `abdichtungs_art` needs a created `pe_werkstoff` (prod has one token `bahn_pe`) — so `rhizome_required_code` on -09 is NOT emitted; `pe_rhizom_nachweis_code` on -18 is (D-5).
6. **`evaluateFormula` requires every named scalar input** — FLL-GAR-05-D2 needs Versatz even for R1/R2 (label says "0, wenn keine"); `if()` short-circuits only its evaluation, not the missing-input check.
7. **Boolean-keyed tables** (`gtd_polyolefin_beschichtung`, `polymerbitumen_beschichtung`) key on `'false'`/`'true'` (= `String(boolean)` in `resolveLookupFill`), not the brief's ohne/mit (I-1).
8. **`naht_ueberlappung_kunststoff_mm` is the engineer's overlap** (validation '>= 40 (or 60 …)') — binding a limit fill on it would replace the input; a created `naht_ueberlappung_min_mm` carries the limit instead; `nahtbreite_min_mm` ("Mindestnahtbreite", the limit) IS re-bound (E-2).
9. **Field rules 4 / section rules 104** (brief ≈ 14 / "sections of -10…21"): `wassereinwirkungsklasse` / `rissklasse` / `standortklasse` / `anzahl_lagen` are consumed producers (C-3 / C-4), the four producer sections C are withheld (C-2); `wurzel_rhizomfestigkeit_required` untouched.
10. **Equations 20 new** (brief: 6 + 2 staged): the brief's eight plus -05-D3 (S-class), -07-D1/-D2, -11-D1, -14-D1 (F-1), -16-D2 (F-2), -18-D1/-D2, -24-D1/-D2, -27-D1 (Σ A beside Σ A·C — Gl. 1's summed form needs both); `FLL-GAR-27-D1` per brief = `sum_ac` is now `-27-D2`.
11. **Tab. 7 c_nom is 40 / 55 mm** and the "(35/45) 1" parenthesis is read as the Wasserwechselzone class (J-3); Tab. 22 has 12 combinations (brief ≈ 10); Tab. 24 seeded (7 rows) with the `pehd_tab24_code` consumer; TAB18 split into three single-key tables as the brief said.
12. **cm vs mm** on -12 / -13 (`bauteildicke_cm`, `asph_dicke`) vs Tab. 8 / Tab. 12 (mm) — the created fills carry mm and say so (GAR-12 F2).
13. **The plan's Tab. 26 "alignment uncertain"** is refuted by the transcript (five cells per row) but honoured in status (U-1).
14. Edition printed (`'2023-12'`) → no numbered edition block; sign-off ids 46 (brief ≈ 16) because every refused / withheld / unreadable / refuted item got its own block.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (107/107); every span was cut by line range with the generator; every cell is a regex cut of its lifted span (`slope()`, `nenndickeCells()`, the Tab.-8/-12/-22/-26/-28 parsers) and the seed test pins each parsed number / string against the printed line; every cue in `field-configs/fll_gar.ts`, `equations/fll_gar.ts`, the STAGED file and the 46 sign-off blocks was read from the transcript in this session at the cited line; prod facts (enum tokens, consumer lists, section codes, gate ids / conditions / md5, the equation rows / md5, `standards.version`, field labels / units / validation_rules) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted the module first time because the Step-4 rule set had been planned against the capture's `consumer_worksheets` + `equations` BEFORE emitting; the test pins each withheld target's refusal message and walks every emitted rule through `producerChain`.
- G-A3 pinned: TAB1 keys ⊂ prod `abdichtungs_art`; TAB7 = `anwendungsfall_concrete`; TAB8 = `bauteil_type` × `beton_ausfuehrungsart`; TAB13 = `bentonit_type`; TAB16 second key = `gtd_auflast_funktion`; TAB18_* = the three -05 enums; TAB22 = `fuegeverfahren` × `bahn_material_naht`; TAB25 = `pe_beanspruchung_klasse`; TAB28 second key = `abschluss_anwendungsfall`; the created selects' option lists equal the seeded key vocabularies (TAB4, TAB12 / TAB1_ASPHALT, TAB26, TAB27).
- The runtime scratch check found the two semantic traps before encoding (all-inputs-required scalars; non-printed lookup combinations) and the two parser slips were caught by the row dump before any migration was emitted.
- `String.replace` with a replacement string was used only with the literal `' '` (whitespace collapse) and `''`; every other splice is `split/join` or the Edit tool; the seed file was assembled by `cat` head + fragment + tail; the two scratch scripts under `scripts/` were deleted before the commit.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` confirmed gitignored; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the unified diff of every modified file; the only non-FLL-GAR changes are the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing keyed on `abdichtungs_art` works until fll_gar-C-1** — the 104 section rules, the -07 Tab.-1 fill, the register slope limit and `boeschung_verletzungen` are inert (visible / `manual_required`), exactly like prod's own REQ-05 / REQ-12 … REQ-22 today. One consumer-edit UPDATE (the range string → 18 codes) lights everything; it is an always-sign-off class, so it is staged, not applied.
2. **Nothing the registers compute reaches prod's gates yet** — `boeschung_verletzungen`, `naht_verletzungen`, `randabschnitte_sonder`, `pflanzen_aggressiv_count`, the Σ / counts and the class codes are visible derived values; REQ-06 / -08 / -11 / -14 / -16 / -18 / -20 / -23 / -24 keep reading the literals / empty conditions until G-2 … G-13 are ratified (fail-safe reading of "never a second producer").
3. **Two tables stay `imported_unverified`** (TAB26 U-1, TAB28 U-4): a 5-minute PDF look at p. 111 and p. 118 flips them (and decides the two empty Tab.-28 cells — the proposal leaves the value to the PDF, SR-3).
4. **Scalar-only equations are not materialised** (-02-D1, -05-D1/-D2/-D3, -11-D1, -12-D1, -14-D1, -16-D2, -18-D1/-D2) — hook / report / snapshot / PDF only (amendment D).
5. **The re-bound `nahtbreite_min_mm`** (E-2) changes a consumed producer's widget: the fill writes the Tab.-22 value once per key change and leaves a typed deviation alone (locked table ⇒ no override control); a non-printed combination shows "keine Zeile" and keeps whatever was typed — the consumers on -15 / -18 read that value.
6. **Three text-derived encodings rest on the executor's reading** (J-3 "(35/45)¹", J-4 the coated `austrocknung_frost` → Frost row, J-5 the Tab.-28 cell semantics / band edges) — all pinned and on the sheet.
7. **Bundle growth:** the 151 spans (~30 KB, incl. the 42-line Tab.-26 head and the four Tab.-25 rows) ship in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
8. **Token/time:** ≈ 0.75 M tokens in, ≈ 95 min wall-clock — above Tasks 4–6 (0.53–0.60 M / 55–70 min) for the largest table set so far (20 tables, 9 registers, 21 fills, 20 equations, 104 section rules) and a plain-text transcript that needed per-table parsers instead of `cells()`.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

Tab. 2 kf / water-loss classes (U-2); Tab. 3's Einbauwassergehalt / Luftporenanteil (no prod field, J-1); the Tab.-6 footnotes 1–4 (Wassereindringwiderstand test agreements); Tab. 7's Wasserwechselzone switch (J-3); Tab. 8 footnote 1 (Fertigplatten 6–7 cm + Ortbeton ≥ 10 / 12 cm) as inputs; Tab. 9–11 (concrete curing), 14 (geotextile MA), 15 / 17 (GTD properties / raw materials), 19 / 20 / 21 / 23 (product-type lists behind free-text fields), 29 (plant list — the `pflanzenarten.art` datalist), Anhang 3 (Flüssigkunststoff classes) — U-3; the §5.5.2 Auflast ≥ 5 kN/m² ↔ 30 cm sentence (L3435–L3437) and the two-day placement rule for coated GTD (L3439–L3442); §4.5's "natürlicher Schüttwinkel ≤ 1:3" for systems with Auflast (L1375–L1378); the §6.2.2 substrate rules (Fehlstellen ≥ 5 mm, Risse > 1 mm — L4077–L4081); the §7.1.3.2 coating thickness ≥ 320 µm (L4822–L4825) beside the 100-µm galvanisation; the Kapillarsperre / Notüberlauf duties of §4.8 (L5715–L5717, L5749–L5760) beyond the register's boolean; DIN 1986-100 r5,100 / r5,5 (external — reference only, L5716); Anhang 2's β from the slope (no `atan`, X-2). Each is listed here with its line and stays outside this encoding (content-boundary rule) or on the sign-off sheet.

---

# Fix round 1 (review verdict on `c8c5453`: APPROVED with three Important items and minors)

- **Commit:** see the reply (one commit, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; every md5 / id in the STAGED blocks is from the 2026-09-17 capture.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · model Claude Opus 5 (`claude-opus-5[1m]`).

## IMPORTANT

1. **Archive pattern on every long / verified text change** (`scripts/verification/fll_gar-STAGED-plan3-rulings.sql`): G-5 (REQ-14), G-8 (REQ-16 — the 311-char condition is no longer "re-read before applying": it travels in the archive row), G-9 (REQ-20), R-1 (Gl. 1) and R-2 (Gl. 2b) now (a) copy the row into `compliance_requirements_archive_fll_gar` / `equations_archive_fll_gar` in the same transaction (`CREATE TABLE IF NOT EXISTS … AS SELECT * … WHERE false; INSERT … SELECT * … WHERE id = … AND md5(<content>) = <captured md5>`), (b) guard the UPDATE on the captured md5, (c) roll back by DELETE of the changed row by id + md5 of the NEW text and re-INSERT of the archived row with the EXPLICIT 18-column (compliance_requirements) / 22-column (equations) list, (d) state that the archive table is dropped once every archived row is rolled back or on the owner's sign-off. The preamble names exactly these blocks as archive-pattern; the gate re-points whose captured condition is short or EMPTY (G-2, G-3, G-4, G-6, G-7, G-12, G-13, D-2) quote that text in full and restore it column-exact (the m277e-accepted form). No long text is retyped anywhere.
2. **E-2 withdrawn from the migration.** The `nahtbreite_min_mm` UPDATE entry was removed from `field-configs/fll_gar.ts` (module header rewritten); `20260917100710` re-emitted (now 59 `INSERT … WHERE NOT EXISTS` + 1 `UPDATE fields` + 104 sections; rollback carries no `nahtbreite_min_mm` line — pinned). The full UPDATE (widget / ui_config / lookup, guarded `widget IS NULL`) and its archive-pattern rollback (`fields_archive_fll_gar`, restore of the four Plan-1 columns by id) sit in the new STAGED block fll_gar-E-2; the sheet block says NOT emitted and why. The field-config test pins that no entry names `nahtbreite_min_mm` and that the prod row is untouched.
3. **`standort_tab18` / `s_klasse_code` / FLL-GAR-05-D3 removed** (module, equations module, both migrations re-emitted and re-pinned): selects 7, derived outputs 19, equations 19, field entries 60 = 59 create + 1 UPDATE. Sheet: D-4 now records that the S-class is NOT derived because Tab. 18 prints no independent S input (L3692–L3696 are the two class descriptions themselves) — a select would duplicate `standortklasse` 1:1; the STAGED D-4 line says the same and applies nothing. The module / equations headers carry the sentence too.

## MINOR

4. **Cues lifted:** four new spans appended to the seed module by the generator — `Q_L1479_1480` ("den baulichen Vorgaben (z. B. Randausbildung, Anschlüsse, Durchdringungen und technische Einbauten);"), `Q_L1483` ("dem Instandhaltungsaufwand (z. B. Reinigungsart, Inspektionsintervalle);"), `Q_L2385_2393` (§5.2.3.2 Eigen-/Fremdüberwachung + §5.2.3.3 Kontrollprüfung) and `Q_L2410` ("Die Ergebnisse der Kontrollprüfung werden der Abnahme und Abrechnung zugrunde gelegt."); `durchdringungen` quotes L1479–L1480, `wartungsmassnahmen` L1483, `pruefungen` L2385–L2393 + L2410 (its description now cites those lines instead of the rhizome sentence). The seed migration is byte-identical (the spans are cues, not table rows; verifier still 107/107).
5. **D-5** names `bep_rhizomfestigkeit_erforderlich` (-24) as the third carrier (sheet, STAGED file, the -18 field description and equation description).
6. **C-1** (sheet + STAGED): one sentence that the rules / fills on -04, -05 and -07 stay `pending` / "Schlüssel fehlt" even after C-1 until FLL-GAR-09 is filled.
7. **`LAGE_NEIGUNG_EXPR`** is now `lookup('TAB1', material, 'neigung_max_1m')` — a hidden / empty `material` (non-sealing rows) yields null → "—" (checked through `prepareRegisterRows` before editing: null, `complete: true`, no diagnostics); equations test and render test re-pinned (`[null, 5, null, 1, true]` for the lower protection layer; `derived-neigung_limit` renders "—").
8. **Report §1** counts line now says "4 scalar (3 created + 1 UPDATE)" and the table columns carry the post-fix numbers.

## Raw output

```
pnpm vitest run --project unit src/lib/eval/__tests__/field-configs-fll_gar.test.ts src/lib/eval/__tests__/equations-fll_gar.test.ts src/lib/eval/__tests__/regulation-tables-seed-fll_gar.test.ts src/components/worksheet/__tests__/register-fll-gar-lagen.test.tsx scripts/__tests__/generated-sql-freshness.test.ts
 Test Files  5 passed (5)
      Tests  38 passed (38)
pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts fll_gar "<transcript>"
 107/107 quotes verbatim in C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Gewässerabdichtungsrichtlinien.md
pnpm test
 Test Files  270 passed | 1 skipped (271)
      Tests  2575 passed | 1 expected fail | 1 skipped (2577)
   Start at  00:38:37
   Duration  41.22s
pnpm -s typecheck → exit 0 · pnpm -s eslint <7 touched .ts/.tsx files> → exit 0 (two unused-import warnings from the removed S-class fixed before the run)
```
(2575 unchanged: the removed S-class pins were replaced by "not present" pins; the count of test cases is the same.) Re-emit check: `emit-seed-sql.ts fll_gar` → seed + rollback byte-identical to `c8c5453`; `emit-field-configs-sql.ts fll_gar 20260917100710` → 60 entries + 104 sections; `emit-equations-sql.ts fll_gar 20260917100720` → 19 equations; all three freshness pins green. **The integration harness was not re-run:** the fix round changed emitted DATA only (fewer created rows, one withdrawn UPDATE, a register `derived` expr, cue strings, STAGED SQL comments) — no engine, harness seed, or prod-shaped row the FLL-GAR harness reads; the 30-file / 119-test run recorded above stands.
