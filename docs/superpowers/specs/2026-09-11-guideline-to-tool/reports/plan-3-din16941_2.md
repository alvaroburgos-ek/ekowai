# Plan 3 — DIN-EN-16941-2 (`din16941_2`) — Task 15 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 16)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude (version not re-checked in this subagent session; the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `ede96bb`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DIN-EN-16941-2 din16941_2` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 5 worksheet titles + field counts, the 36 section codes / titles, the 2 equation rows with ids / formulas / md5 / status, the 5 compliance rows this task stages with ids / severities / md5(condition) / conditions, the labels / units / `is_required` / `validation_rules` of the fields with rules, the `information_schema` column lists of `standards` / `worksheet_templates` / `equations` / `compliance_requirements` / `fields`). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-EN-16941-2\DIN-EN-16941-2.md` (921 lines; read at L1–L60 title / TOC, L126–L145 scope, L207–L246 §4–§5.2, L260–L420 §5.3–§5.6, L420–L500 §5.6.2–§5.9, L500–L630 §6, L629–L727 §7–§12, L729–L921 Anhänge A–D). The 116 quote spans were lifted mechanically by line number (a throwaway generator in the scratchpad, `JSON.stringify` per span) and every seeded cell is asserted inside its span at build time (`inSpan`); the 92 quoted fragments on the sign-off sheet and in the STAGED file were machine-checked against their cited lines (92/92). The inventory was used for pointers only (its pointers were correct; its "Sprühanwendung flag" and "per-parameter G number fields" designs were replaced — §8). The `.pdf` was not opened; no worked numeric example is printed.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `din16941_2` | 9 | 36 (36/36 verbatim) | 0 rows skipped; every table `md_verified` (all cells legible; the OCR quirks "Nicht-TrinkwasserNutzungen" and the Legionella footnote marker are kept verbatim in the spans) | 4 (all `create`: `speichereinrichtungen` -02, `grauwasserquellen_16941` / `bedarfsstellen` -03, `probenahmen` -04) | 3 (all `create`: `speicher_lage`, `nachspeisung_medium_16941` -02, `richtwert_spalte` -04) + 5 attestations (`create`: `direktnutzung_scope_bestaetigt` -01, `personenzugang` / `geruchsverschluss_nachspeisung` / `ventilgesteuerte_zulaeufe` -02, `wohngebaeude_bestaetigt` -03) | 0 (S-1 — the enum → json carrier change is STAGED; a widget switch alone loses data) | 10 (all `create`, all TEXT: 2 on ANHANGB by `anlagentyp` (-01), 8 Anhang-D Richtwerte on TABD1 / TABD2 by `richtwert_spalte` (-04)) | 21 (5 UPDATE on consumer-free, gate-free existing inputs `zugang_oeffnung_mm` / `warnsystem_ventilzulauf` / `pumpensteuerung_handnot` / `legionella_kbe` / `truebung_ntu` + 16 on created fields) | 0 | 15 (-02 D1…D3, -03 D1…D8, -04 D1…D4) | 2 replacements (R-1 Gl. 1, R-2 Gl. 2) + 9 gate blocks (G-1 … G-9) + 8 D-pairs + S-1 | 32 (+4 observations) | ≈ 0.60 M (tool-marker delta 15.00 M → 14.42 M at report time) | ≈ 65 |

Field entries: 43 = 38 `create` + 5 UPDATE. Migration `20260917101510`: 38 `INSERT … WHERE NOT EXISTS`, 5 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 5 UPDATE rows to the captured NULLs and deletes only the 38 `Plan 3:` rows. Derived-output fields (`widget='derived'`): 15 — one per equation. No `enum_values` is written by any UPDATE (D-1; the three selects are `create`). The emitter printed no lint warning (quoted literals `'differenziert'` / `'vereinfacht'` / `'sprueh'` / `'gartenbewaesserung'` / `'direkt'` / `'trinkwasser'` / `'AA'` / `'Nicht nachweisbar'` / `'N/A'` / `'0,0'` collide with no column key or worksheet symbol — pinned, 37 expressions walked) and no self-consumer NOTICE (the capture has none).

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  306 passed | 1 skipped (307)
      Tests  2835 passed | 1 expected fail | 1 skipped (2837)
   Start at  11:22:43
   Duration  48.78s (transform 14.07s, setup 50.69s, import 117.88s, tests 50.16s, environment 201.09s)
```
(2806 at `ede96bb` + 29 from this task: seed-din16941-2 9, field-configs-din16941-2 8, equations-din16941-2 9, register-din16941-quellen 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). One RED before that: `Q.L378` was missing from my span generator (`TS2339` in `field-configs/din16941_2.ts` L208 and `equations/din16941_2.ts` L58) — vitest does not typecheck, so the first emit had carried the string "undefined" inside two quotes; the span was added, `Q` regenerated, all three migrations re-emitted, and both module tests now pin `not.toContain('undefined')` on every quote. `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output.

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/din16941-2-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  20 passed (20)
   Duration  5.47s (transform 740ms, setup 0ms, import 3.59s, tests 1.70s, environment 0ms)
```
The harness seeds prod's fields / equations / gates (`tests/harness/seed-din16941-2.ts`); the new rows are not in it by construction — it proves the branch still drives every DIN-EN-16941-2 chain and gate the harness covers. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` on ad-hoc tables (`scripts/_probe_16941.ts`, deleted): a `lookup()` on a MISSING row inside a row `derived` is a null cell with NO diagnostic and the row stays complete (→ the Waschbecken hint / badge is simply absent); `lookup(...) IS NULL` is a parse error ("Ausdruck nicht auswertbar (Syntax)" — IS NULL takes a symbol only) → the status expressions compare the printed TEXT cell instead; `if(tr IS NULL, 0, …)` on an OPTIONAL number column is decidable; `if(sprueh == 'ja', 'sprueh', vorgesehene_nutzung)` returns a symbol string usable as a lookup key; a missing worksheet driver leaves the derived key null and `count_rows(reg, st == 3)` `manual_required` naming the column; `count_rows` over `status_max == 3` computes 1 / 2 / 3-band statuses; `min(Y_G, D_G)`, `max(2 * D, 20)` and an enum-keyed `if()` inside a scalar formula compute (140 / 20 / 1060); `personenzugang == false` on a stored boolean is `pass` / `fail` / `pending` (unset); an `OR` rule with a missing leg is `pending` (visible). Seed builder: all 9 tables built first run with every cell `inSpan`; seed test 9/9 first run; verifier **36/36** on its first run. Field-config test: one RED on my own ordering pin (the -03 rules are listed in section order D → E; my expected list had the E twin before the D block) → pin corrected → 8/8 (the three refusals G-2 / G-3 / G-7 were asserted, not discovered; the emitter accepted all 43 entries and 15 equations first time). Equations test: one RED on my pin — with `richtwert_spalte` unset the Legionella column's row `visible_when` is `pending` = visible AND required, so the sample row is INCOMPLETE and `count_rows` reads 0 (computed), not `manual_required`; pinned as the real behaviour (`complete === false`, count 0, last-row Σ `manual_required`) → 9/9 (every hand-derived value held: 349 / 173 / 240 / 140 / 60 / 40 / 40 / 173 / 100 / 86,5 / 4000 / 2 / 20 / 50 and the D.3 / D.4 statuses 1 / 2 / 3 on 100 / 300 / 2600 vs G 250). Render test: 3/3 first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts din16941_2 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-EN-16941-2\DIN-EN-16941-2.md"` → **`36/36 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- TABA1 (4): ertrag / bedarf_wc / bedarf_waesche / bedarf_andere — the seven-line span L739–L745 (header, body row L741, footnotes a / b / c) as every row's quote
- TABA2 (5): dusche L762 · badewanne L763 · waschmaschine L764 · kuechenspuele L765 · geschirrspueler L766
- TABA3 (3): wc L776 · urinal L777 · waschmaschine L778
- GL1_LEGENDE (6): dusche L569–L575 · badewanne L576–L577 · waschbecken L578–L580 · waschmaschine L581–L582 · kuechenspuele L583–L585 · geschirrspueler L586–L587
- TABD1 (4): sprueh / wc_spuelung / gartenbewaesserung / reinigung_waschmaschine — the nine-line span L850–L858 (two header rows, four parameter rows, footnotes a / b)
- TABD2 (4): the same four keys — the seven-line span L868–L874
- TABD3 (3): lt_g L884 · g_bis_10g L885 · gt_10g L886
- TABD4 (2): lt_g L900 · gt_g L901
- ANHANGB (5): direkt L788–L794 · kurzzeit L795–L797 · physikalisch_chemisch L798–L800 · biologisch L801–L803 · biologisch_mechanisch L804–L806

Status per table: `md_verified` — all nine (every printed row lifted, every cell legible; the two OCR quirks — "Nicht-TrinkwasserNutzungen" without the second hyphen in the Tab.-A.1 header, the Legionella "N/A" cell with its footnote marker `${ }^{\text {b }}$` — are kept verbatim in the spans; the encoded cell texts are unambiguous, so no U-block).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, title_de FROM standards WHERE code ILIKE '%16941%'"
  → DIN-EN-16941-2 · 'EN 16941-2:2021' (the standard code confirmed before the capture)
node scripts/regulation-tables/build-prior-snapshot.mjs DIN-EN-16941-2 din16941_2
  → wrote src\lib\eval\field-configs\din16941_2.prior.json: 76 field rows (0 orphan), 36 coded sections of 36, 2 equations, 19 gates (0 unparseable — symbols: [] + parse_error; the emitter refuses conservatively on those worksheets);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts din16941_2
  → wrote seed + rollback for din16941_2 -> scripts/migrations/20260917101500_regulation_tables_seed_din16941_2.sql scripts/rollback-20260917101500-regulation-tables-seed-din16941-2.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts din16941_2 20260917101510
  → wrote 43 field entries + 0 section entries for din16941_2 -> scripts/migrations/20260917101510_field_configs_din16941_2.sql scripts/rollback-20260917101510-field-configs-din16941-2.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts din16941_2 20260917101520
  → wrote 15 equations for din16941_2 -> scripts/migrations/20260917101520_equations_din16941_2.sql scripts/rollback-20260917101520-equations-din16941-2.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-din16941-2.test.ts` / `equations-din16941-2.test.ts` pin the other two against the committed `din16941_2.prior.json`, default refuse mode). The seed migration carries 36 row upserts (`ON CONFLICT (table_id, row_key)`) and 9 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the nine tables (no earlier DIN-EN-16941-2 table seed exists — nothing superseded). The equations rollback deletes the 15 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101500` → `20260917101510` → `20260917101520`; rollback in reverse. Every file touches `DIN-EN-16941-2` rows only. Scalar-only rows (`freier_auslauf_A_min`, `Y_G_vereinfacht`, `D_G_vereinfacht`, `bemessungswert_massgebend_calc`, `speicher_max_50`) are not server-materialised (amendment D — expected, noted once here and as I-1); the ten register-fed rows materialise on save.

## 5. What was encoded

**Tables (9)** — `src/lib/eval/regulation-tables-seed-din16941_2.ts`, builder `din169412SeedTables()`, edition `'2021'` (printed: "Dieses Dokument (EN 16941-2:2021)" L5 / L101, CEN acceptance "20. Dezember 2020" L36 = prod `standards.version` 'EN 16941-2:2021' — no edition block):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABA1 | `posten` ertrag / bedarf_wc / bedarf_waesche / bedarf_andere (transposed body row L741) | `l_pd` 60 / 35 / 15 / 10 l/(p·d), `spalte` (printed header), `fussnote` (a / b / c) | anhaltswert (L547 "Beispiele für typische …" — L737 caption) | 4 | md_verified |
| TABA2 | `quelle` ⊂ prod `grauwasser_herkunft` (5; Waschbecken has no printed row) | `symbol`, `unit`, `min`, `max`, `groessenordnung` ("5 bis 15" …) — SR-2 hints | anhaltswert (L753 "typische Größenordnungen" — L759) | 5 | md_verified |
| TABA3 | `bedarf` wc / urinal / waschmaschine | same shape (3 bis 8 / 1 bis 2 / 30 bis 60) | anhaltswert (L753 — L773) | 3 | md_verified |
| GL1_LEGENDE | `quelle` = prod `grauwasser_herkunft` (6) | `groesse` Q / V, `unit_qv` l/min / l, `mit_dauer` 1 / 0, the three legend sentences per source | locked (L557 "muss … angewendet werden") | 6 | md_verified |
| TABD1 | `nutzung` = the created `richtwert_spalte` tokens (the four printed columns) | per parameter `<p>_g` (number, null for "Nicht nachweisbar" / "N/A"), `<p>_text` (printed cell), `verfahren_<p>` (the EN ISO test method, reference string) | locked (L681 "Mindestanforderungen … Vorrang" — L848; O-1) | 4 | md_verified |
| TABD2 | `nutzung` (same) | `truebung_max` / `_text`, `ph_min` / `ph_max` / `ph_text`, `rest_chlor_max` / `_text`, `rest_brom_max` / `_text` (0 for "0,0" — J-3), four `verfahren_*` | locked (L681 — L866) | 4 | md_verified |
| TABD3 | `band` lt_g / g_bis_10g / gt_10g | `ergebnis`, `status` (prod `bewertung_status` tokens), `status_text`, `auswertung`, `code` 1 / 2 / 3 | locked (L681 — L889 footnote b) | 3 | md_verified |
| TABD4 | `band` lt_g / gt_g | same shape, `code` 1 / 2 | locked (L681 — L902 pH note) | 2 | md_verified |
| ANHANGB | `anlagentyp` = prod tokens (5) | `bezeichnung`, `beschreibung`, `nutzungsbeschraenkung` (direkt only, L794) | anhaltswert (L787 "können … klassifiziert werden") | 5 | md_verified |

**Registers / selections / lookup_fill / visibility** — `src/lib/eval/field-configs/din16941_2.ts`:
- `grauwasserquellen_16941` (-03 D, create json, `visible_when berechnungsverfahren == 'differenziert'`): `quelle` enum over the six prod tokens (labels from the legend), derived `mit_dauer` (GL1_LEGENDE, badge "Q · t · u" / "V · u"), derived `hint` (TABA2 "5 bis 15" …, "—" for Waschbecken), `q_or_v` (required), `t_x` min (required, row `visible_when mit_dauer == 1`), `u_x` 1/(p·d) (required), derived `ertrag_row = if(mit_dauer == 1, q_or_v · t_x · u_x, q_or_v · u_x)`, badge `in_hint` (in / outside the Tab.-A.2 range — "Hinweis, kein Grenzwert"); no `override` block (nothing is filled); footer `Y_G_rows`, `quellen_count`; note L753 + the §6.2.2 collection preference + L537.
- `bedarfsstellen` (-03 E, create json, differenziert): `bezeichnung`, `bedarf` lookup_key TABA3 → `hint` lookup_value (`groessenordnung`), `v_x` l / `u_x` 1/(p·d) (required), derived `bedarf_row = v_x · u_x`, badge `in_hint`; footer `D_G_rows`, `bedarfsstellen_count`; note L597 + L619. `V_misc` stays a scalar.
- `speichereinrichtungen` (-02 D, create json): `bezeichnung`, `werkstoff` enum (the six prod `speicher_werkstoff` tokens, labels L292), `nennkapazitaet_l` (required); footer `nennkapazitaet_sum`, `speicher_count`; note L296 / L300 / L302.
- `probenahmen` (-04 E, create json): `datum`, eight measured columns (E. coli / Enterokokken / Coliforme / pH required; Legionella required under `richtwert_spalte == 'sprueh'`; Trübung required unless `gartenbewaesserung`; Chlor / Brom optional), eight `status_*` derived cells (Tab. D.3 bands `< G` / `G bis 10 G` / `> 10 G` for the bacteriological four with the "Nicht nachweisbar" / "N/A" text branches; Tab. D.4 `< G` for Trübung / Chlor / Brom, the pH range, the "0,0" Brom case; 0 = n. a. / nicht gemessen), badge `status_max` (Ampel 0 … 3); footer `probenahmen_count`, `probenahmen_rot`, `probenahmen_gelb`, `status_letzte_probe`; note L697 / L703 / L889 (footnote b not encoded — O-2).
- Selects (create): `speicher_lage` (-02 D: unterirdisch / oberirdisch, L306 / L308), `nachspeisung_medium_16941` (-02 E: trinkwasser / andere, `visible_when nachspeisung_vorhanden == true`, L345 / L350), `richtwert_spalte` (-04 E: the four printed Tab.-D.1 columns, L850–L851 / L695). Attestations (create): `direktnutzung_scope_bestaetigt` (-01 C, visible for direkt, L140 / L794), `personenzugang` (-02 D, L327), `geruchsverschluss_nachspeisung` (-02 E, visible for trinkwasser, L350 / L395), `ventilgesteuerte_zulaeufe` (-02 E, L360), `wohngebaeude_bestaetigt` (-03 C, visible under vereinfacht, L543 / L521). Created inputs: `D_zulauf` mm (-02 E, visible for `rueckflusssicherung_typ == 'AA'`, L377 / L378).
- lookup_fill (all create, all TEXT): `anlagentyp_beschreibung` / `anlagentyp_nutzungsbeschraenkung` (-01 C ← ANHANGB by `anlagentyp`, the restriction visible for direkt); `e_coli_G`, `enterokokken_G`, `legionella_G` (visible for sprueh), `coliforme_G` (← TABD1) and `truebung_G`, `ph_G`, `rest_chlor_G`, `rest_brom_G` (← TABD2), all role `limit` keyed on `richtwert_spalte` on -04 E — the printed cell verbatim ("Nicht nachweisbar", "250", "N/A", "< 10", "5 bis 9,5", "0,0"), which a number fill could not carry.
- Field `visible_when` on existing fields (5, all consumer-free, no same-worksheet gate reads them — pinned): -02 `zugang_oeffnung_mm` ← `personenzugang == false` (L327), `warnsystem_ventilzulauf` ← `ventilgesteuerte_zulaeufe == true` (L360), `pumpensteuerung_handnot` ← `pumpe_erforderlich == true` (L401 / L461); -04 `legionella_kbe` ← sprueh (L854), `truebung_ntu` ← `richtwert_spalte != 'gartenbewaesserung'` (L870). Refused / withheld: `rueckflusssicherung_typ` (CR-08, G-2), `pumpe_trockenlaufschutz` (CR-09, G-7), `abstand_wurzeln_m` (CR-13 + `speicher_lage` not inherited on -04, G-3), the 23 Gl.-1/2 scalars under vereinfacht (CR-12 chain, G-9). Section rules: none (every driver-bearing section holds a gate-read symbol or a consumed producer; the method switch lives on the created fields).

**Equations (15 new)** — `src/lib/eval/equations/din16941_2.ts`: `-02-D1 nennkapazitaet_sum = sum_rows(speichereinrichtungen, nennkapazitaet_l)` · `-D2 speicher_count` · `-D3 freier_auslauf_A_min = max(2 * D_zulauf, 20)` (L377) · `-03-D1 Y_G_rows = n * sum_rows(grauwasserquellen_16941, ertrag_row)` (Gl. 1 over rows) · `-D2 quellen_count` · `-D3 D_G_rows = n * sum_rows(bedarfsstellen, bedarf_row) + V_misc` (Gl. 2) · `-D4 bedarfsstellen_count` · `-D5 Y_G_vereinfacht = n * lookup('TABA1', 'ertrag', 'l_pd')` · `-D6 D_G_vereinfacht = n * (if(vorgesehene_nutzung == 'wc_spuelung', 35, 0) + if(== 'waesche', 15, 0) + if(gartenbewaesserung OR reinigung, 10, 0))` (Tab. A.1 by the inherited single select — J-1) · `-D7 bemessungswert_massgebend_calc = min(Y_G, D_G)` (L511 sentence) · `-D8 speicher_max_50 = 0.5 * D_G` (L511) · `-04-D1 probenahmen_count` · `-D2 probenahmen_rot = count_rows(probenahmen, status_max == 3)` · `-D3 probenahmen_gelb` · `-D4 status_letzte_probe = sum_rows(last_rows(probenahmen, 1), status_max)`. No prod symbol is re-produced (Gl. 1 / 2 keep `Y_G` / `D_G`); existing inputs are bound by their prod symbols (`n`, `V_misc`, `vorgesehene_nutzung`, `Y_G`, `D_G`). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables; hand-derived — no printed example): 4 persons · (shower 10 · 5 · 1 + bath 100 · 0,2 + basin 4 · 0,5 · 3 + washing machine 45 · 0,25) → **349 l/d**, a flow-based row without t incomplete, a 20 l/min shower flagged "außerhalb" and still counted; 4 · (WC 6 · 4 + urinal 1,5 · 2 + WM 45 · 0,25) + 20 → **173 l/d**, V_misc blank → `manual_required`; Tab. A.1 twins 240 / 140 / 60 / 40 / 40; min 173 / 100; 86,5 l; tanks 1500 + 2500 → **4000 l** / 2; A = 20 mm (D 8) / 50 mm (D 25); WC column samples 100 / 300 / 2600 vs G 250 → **grün / gelb / rot**, Trübung 12 → gelb, Legionella N/A → 0, counts rot 1 / gelb 1, last sample 3; Sprüh column: 0 KBE grün, 1 KBE rot, Legionella 5 / 50 → 1 / 2, Brom 0 / 0,1 → 1 / 2, pH 10 → gelb; Garten: Trübung hidden (0), Chlor 0,6 ≥ 0,5 → gelb; missing driver → statuses null, rows incomplete.

## 6. Inventory §5 top-wins walk

1. Grauwasserquellen as repeatable rows (source → Tab.-A.2 hint, Q/V, t, u) summed into Gl. 1 → **encoded** (`grauwasserquellen_16941` over the six Gl.-1 sources with the term form from the legend table, hints and in-range badges from TABA2, `Y_G_rows`); the multi-select `grauwasser_herkunft` driver is STAGED (S-1 — a widget switch on the enum would lose data), replacing the 16 scalars and Gl. 1 is STAGED (D-7 / R-1). ✓ (multi-select carrier STAGED, as the brief foresaw)
2. Bedarfsstellen rows summed into Gl. 2; register `min(Y_G, D_G)`; derive the 50 %-Tagesbedarf storage cap → **encoded** (`bedarfsstellen` + `D_G_rows`, `bemessungswert_massgebend_calc`, `speicher_max_50` on -03 where D_G lives); replacing the 6 scalars / Gl. 2 / the manual Bemessungswert is STAGED (D-8 / R-2 / D-5), the cap gate is G-5.
3. `berechnungsverfahren = vereinfacht` → fill Tab. A.1 per-person values, hide the differentiated inputs, block vereinfacht for non-residential → **partially**: `Y_G_vereinfacht` / `D_G_vereinfacht` (Tab. A.1 by `n` and the inherited use) and the registers switch on the method today; the 23 existing scalars stay visible (G-9 — hiding them would make CR-12 unpassable), the Wohngebäude attestation is emitted with its gate STAGED (G-8).
4. `vorgesehene_nutzung` (+ Sprüh) → Richtwerte G from Tab. D.1 / D.2 and per-sample status per Tab. D.3 / D.4 → **encoded** (eight text fills + the `probenahmen` register with per-parameter statuses and the Ampel, keyed on the created `richtwert_spalte` — the four printed columns incl. Sprüh — because `vorgesehene_nutzung` never reaches -04 and prod has no Sprüh token; D-2 stages the derivation); the use-blind `raw` limits are G-1; the manual `bewertung_status` is D-1.
5. Condition CR-08 / CR-13 / Zugang on Nachspeisemedium, Speicherlage and Personenzugang → **encoded as drivers** (`nachspeisung_medium_16941`, `speicher_lage`, `personenzugang` + the rules on the gate-free fields `zugang_oeffnung_mm`, `geruchsverschluss_nachspeisung`, `D_zulauf` / `freier_auslauf_A_min`); the three gate rewrites are STAGED (G-2 / G-3 / G-7, refused by the gate-aware guard) and the 400-mm gate is G-4. ✓ (gates STAGED, as the brief foresaw)

Data-quality gaps of the inventory: the hard-limit `raw` rules → G-1 (display-dead today — code fact); use-blind D.2 limits → G-1 + the use-keyed fills; single-select carriers → S-1; `anlagentyp = direkt` vs §1 → G-6; no Richtwert fields → the eight fills; `legionella_kbe >= 0` for every use → hidden outside Sprüh; `bemessungswert_massgebend` not an equation → -03-D7 + D-5.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`din16941_2` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (din16941_2 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+32 blocks, +4 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 15)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-din16941_2.ts` (incl. the 116 lifted spans `Q`), `src/lib/eval/field-configs/din16941_2.ts`, `src/lib/eval/field-configs/din16941_2.prior.json` (captured), `src/lib/eval/equations/din16941_2.ts`, `src/lib/eval/__tests__/regulation-tables-seed-din16941-2.test.ts`, `src/lib/eval/__tests__/field-configs-din16941-2.test.ts`, `src/lib/eval/__tests__/equations-din16941-2.test.ts`, `src/components/worksheet/__tests__/register-din16941-quellen.test.tsx`, `scripts/verification/din16941_2-STAGED-plan3-rulings.sql` (21 ☐ blocks: S-1, G-1 … G-9, R-1, R-2, D-1 … D-8; every line a comment), `scripts/migrations/20260917101500_regulation_tables_seed_din16941_2.sql` + `scripts/rollback-20260917101500-regulation-tables-seed-din16941-2.sql`, `scripts/migrations/20260917101510_field_configs_din16941_2.sql` + `scripts/rollback-20260917101510-field-configs-din16941-2.sql`, `scripts/migrations/20260917101520_equations_din16941_2.sql` + `scripts/rollback-20260917101520-equations-din16941-2.sql`, this report (+ the copy at `.superpowers/sdd/…/task-15-report.md`).
Untouched: every other standard's file; the DIN-EN-16941-2 harness files; `scripts/reasoning-map/` (checked out before staging); the probe deleted; the generator / checkers / splice scripts live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **`grauwasser_herkunft` / `behandlungsstufen` NOT re-bound to `select_many`** (brief: emit widget + ui_config, flag "encoded, not behaving"): the code path (ChecklistEditor writes json, `extractValue(p, 'enum')` reads `value_enum`) loses the selection on reload and `grauwasser_herkunft` is consumed by -02 — not fail-safe under amendment J; the whole switch is S-1.
2. **Anhang-D Richtwerte keyed on a CREATED select `richtwert_spalte` (-04), not on `vorgesehene_nutzung` + a Sprüh attestation on -01**: `vorgesehene_nutzung` is consumed by -02 / -03 only (capture), a created boolean on -01 reaches -04 no better (a `create` never sets consumers), and prod has no Sprüh token; the four PRINTED columns are the select's options, so everything on -04 computes today (D-2 stages the derivation + C-1).
3. **Text fills, not number fills** for G: the printed cells are "Nicht nachweisbar", "N/A", "< 10", "5 bis 9,5", "0,0" — a number fill would write nothing (I-1 typing) and the brief's `status = if(x < x_G, …)` over the fill would never compute; the register statuses read the table directly in row scope (text branch + number bands).
4. **TABD1 / TABD2 are one transposed table each** (m277e trap 3), not `TABD1_<PARAM>` × 4 / `TABD2_<PARAM>` × 4; TABD3 / TABD4 carry `code` 1 / 2 / 3.
5. **`quelle` is an enum column, not a `lookup_key` on TABA2**: Gl. 1 names six sources, Tab. A.2 prints five (no Waschbecken); the legend table GL1_LEGENDE (new, 6 rows) single-sources the Q·t·u / V·u form; hints via `lookup()` (J-5).
6. **No `anzahl` column on `bedarfsstellen`**: Gl. 2 multiplies per-person terms by n only (J-6).
7. **`nennkapazitaet_max = 0.5 * D_G` lives on -03 as `speicher_max_50`** (D_G is not inherited on -02; `nennkapazitaet` IS inherited on -03 — the comparison can only live there).
8. **The 23 Gl.-1/2 scalars keep no visibility rule** (brief: hide under differenziert): the CR-12 chain makes it an enforcement change → G-9; the method rules sit on the created fields.
9. **`personenzugang` rule inverted vs the brief**: L327 puts the 400 mm on the case WITHOUT Personenzugang → `zugang_oeffnung_mm ← personenzugang == false` (probed: `== false` on a stored boolean is pass / fail / pending).
10. **`pumpe_*` block**: only `pumpensteuerung_handnot` gets the rule; `pumpe_trockenlaufschutz` is CR-09-read → G-7.
11. **`speichereinrichtungen` has no `lage` / `abstand_wurzeln_m` columns**: the created scalar `speicher_lage` is the driver the STAGED CR-13 rewrite needs; a per-tank column would be a second carrier of the same fact.
12. **Equations 15 (brief 6 + 1 staged)**: counts, `speicher_count`, `bemessungswert_massgebend_calc`, `speicher_max_50` and the two Tab.-A.1 twins added; `nennkapazitaet_max` moved (7).
13. **Sign-off ids 32 (brief ≈ 9)**: the brief's ids kept where they exist (S-1, G-1 … G-5 by subject, D-1, R-1 / R-2, J-1); every refused / withheld / read item got its own block (G-6 … G-9, D-2 … D-8, O-1 / O-2, J-2 … J-7, I-1, X-1 / X-2).
14. **Prod facts**: 19 gates as stated, none with an empty or prose condition; 0 orphan fields; 0 self-consumer entries; `anlagen_id` carries the "ALL" token (X-1); the `validation_rules.raw` strings are not enforced by the form (G-1 is hygiene).
15. **Edition printed** → no edition block (the din1989_1 pattern was not needed).

## 9. Self-review

- Every `verbatim_quote` passes the verifier (36/36); every seeded cell is asserted inside its span at build time (`inSpan`); the 92 quoted fragments on the sheet and in the STAGED file were machine-checked against the transcript (three slips caught and fixed before commit: a 5-line span the checker's ±1 context could not see (verbatim on inspection — checker widened), the LaTeX `\%` of L511 dropped in the STAGED file, and my "…" truncation marker inside a quote).
- The emitter guards accepted all 43 entries and 15 equations first time because the rule set was planned against the capture (consumer lists, the 2 equations, the 19 gates, section codes); the field-config test pins the three refusals (G-2 / G-3 / G-7), the CR-12 chain behind G-9, the consumer facts behind D-2, the S-1 facts, G-A3 and the string-literal walk (37 expressions, 0 collisions).
- Runtime facts were established through the real register contract before the pins (probe deleted); the render test drives the Grauwasserquellen register through `RegisterEditor` (t column per source, hints, badges, footer Σ through `evaluateFormula`).
- One typecheck RED (`Q.L378` missing from the generator) caught by `pnpm -s typecheck` after the first green vitest run — the migrations had carried the word "undefined" inside two quotes; fixed, re-emitted, and both module tests now pin `not.toContain('undefined')`. Worth recording: a `Q.<key>` typo is invisible to vitest and to the emitter (a string is a string) — the freshness pins would have frozen the wrong quote.
- Prod facts used in the STAGED file (2 equation ids + md5, 5 gate ids + md5, column lists, `standards.version`, labels, `validation_rules`) come from the in-session read-only queries; no long cell was retyped (archive pattern).
- No `String.replace` with a replacement string on any repo file: the seed module was assembled from a body file and the generated quote block with `split/join`; every later edit used `split/join` with a presence check that throws before writing, a Write-tool script, or the Write tool. The one shell-quoted `node -e` splice that touched a `\%` mangled it — caught by the fragment checker, fixed from a file (trap 8).
- `git checkout -- scripts/reasoning-map/` run before staging; `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **S-1 is the single most consequential ruling for this standard**: the brief's "emit the widget, flag not behaving" would have shipped a data-losing config; the STAGED transaction (data_type + widget + ui_config + value migration) is the honest shape — a Plan-3-wide check for other briefs that say "make it multi-select" on an enum is worth a line in the playbook (added as trap 1).
2. **G-9 is a guard blind spot**: consumer-free inputs of a same-worksheet equation whose output a gate reads pass both the producer and the gate-aware guard, yet hiding them disarms the gate (here: unpassable rather than N.A.). Recorded as playbook trap 2; a third guard ("hides an input of an equation whose output a same-worksheet gate reads") is a [CODE] candidate for the controller.
3. **Nothing the registers compute reaches prod's gates yet** — `Y_G_rows`, `D_G_rows`, the statuses and the fills are visible twins; Gl. 1 / 2, CR-12, CR-17 keep reading the manual inputs until R-1 / R-2 / D-1 / D-5 are ratified. That is the fail-safe reading of "never a second producer".
4. **`richtwert_spalte` duplicates part of `vorgesehene_nutzung`** (D-2): two selects can disagree on screen until C-1 + the derivation land.
5. **Scalar-only twins (I-1)**: `bemessungswert_massgebend_calc`, `speicher_max_50`, the Tab.-A.1 twins and `freier_auslauf_A_min` evaluate on the form / report only; G-5 / D-5 need the materialisation workstream.
6. **O-1**: `locked` on Anhang D blocks a stricter national value from being recorded beside the fill — the owner may prefer `messwert`.
7. **Token/time:** ≈ 0.60 M tokens in, ≈ 65 min wall-clock — in the Task 4 / 14 range.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§5.2 Sammelrohrsysteme a)–d) (EN 12056-2 sizing, marking, free drainage — L248–L252) and the bypass sizing (L258); §5.4.6 Wasserdichtheit items (L312–L315); §5.4.7 Nennweiten of the connections (L319); §5.4.9 Überlauf details (Geruchsverschluss, EN 13564-1 backflow, Kleintiersperre — L333–L339; prod has the capacity gate CR-07 only); §5.5.1 the Nachspeisung control / dead-leg items (L354–L358); §5.6.2 / §5.6.3 pump details (EN 12050-2 solids, suction pipe gradient, non-return valve, submersion depth — L426–L449) as a Pumpen register (inventory §2 — booleans only today); §5.6.4 Ausdehnungsgefäß (L453–L457); §5.7 Anlagensteuerung a)–h) (L471–L479; prod has two booleans); §5.8 Wasserzähler (EN ISO 4064 reference); §5.9 Verteilung (materials, EN 806, no cross-connections — CR-11 exists); §7 Einbauanleitung items and the site factors for underground installation (L635–L652); §8 signage details (L658–L667; CR-14 exists); §9 Inbetriebnahme (Farbtest Anhang C as a procedure — CR-15 exists); §12 Betriebstagebuch entries as rows (inventory §2 — a `wartungsbuch` register with date / work / contact is a natural Task-16-class addition; not built here to keep the task at the brief's scope); Tab. D.1 / D.2 "System-Typ" column ("Einzelstandorte und kommunale Wohnbereiche", "alle Systeme, wenn verwendet") and the Tab.-D.2 footnote a (Schwebstoffe / Färbung — L874) as text only. Each is listed here with its line and stays outside this encoding (content-boundary rule).
