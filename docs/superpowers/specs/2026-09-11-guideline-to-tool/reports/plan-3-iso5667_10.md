# Plan 3 — ISO-5667-10 (`iso5667_10`) — Task 20 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 21)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (raw `claude --version` output "2.1.260 (Claude Code)"; no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `1a4544c`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs ISO-5667-10 iso5667_10` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`; the 52 section codes / titles / field counts; labels / `data_type` / units / `is_required` / VR of the 68 fields on -01 … -08; the 10 compliance rows of interest with ids / severities / md5(condition) / lengths / titles; the 3 equation rows with ids / md5 / formulas / status; the max REQ code (CR-036, 36 rows); the `information_schema` column lists of `compliance_requirements` / `equations` / `fields` / `project_parameters`; the stored-parameter count (0) and the worksheet-instance count (0) of the standard). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from the **SPANISH** transcript `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-10\ISO-5667-10.txt` (1883 lines; AENOR licence copy of the Spanish translation of ISO 5667-10:2020, pdftotext; **VC grade by ruling** — no PDF read; the `_layout.txt` / `_pages29_35.txt` / `_q34.txt` / `_ref.txt` / `.pdf` siblings were NOT opened). Read in full lines (never `cut`): title page L1–L52, §3 L259–L353, §4 L355–L520, §5 L520–L717, §6 L718–L748, §7.1 L750–L826, §7.2 L826–L1091, §7.3–§7.4 L1092–L1275, §8 L1275–L1461, §9 L1461–L1612, §11 L1694–L1783. The 113 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-iso5667_10-quotes.mjs`, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-iso5667_10.ts`, never hand-edited); every seeded cell is asserted inside its span at build time (`inSpan`). The inventory was used for pointers only (one premise refuted — O-4).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `iso5667_10` | 8 | 14 (14/14 verbatim) | 0 rows skipped; 1 glyph slip kept verbatim (Formula (2) block prints "𝐵+" — U-1); **every table `imported_unverified`** (Spanish txt = VC, nothing upgraded) | 5 (all `create`: `stichproben_qualifiziert` -01, `probenahmestellen` -02, `probenahmetermine` -03, `flaschen_ctcv` -06, `probenahmegeraete` -07) | 1 created (`composite_duration_band` -06, h2 / h24) + 5 UPDATEs on prod selects (`keep_prod`) | 0 | 1 (`create`: `composite_interval_max` -06 ← S7_2_1 by the created band, role limit) | **29** rule-bearing entries: 10 UPDATEs on existing inputs (-04 ×5, -05 ×1, -06 ×2, -07 ×2 — the -07 pair `pending` until C-1) + 19 on created fields | 0 | 18 (-01 D1–D2; -02 D1–D2; -03 D1–D5; -05 D1–D2; -06 D1–D4; -07 D1–D2; -08 D1) | 0 replacements (the three verified prod rows stay); 9 gate blocks (G-1 … G-9, incl. 2 new gates CR-037 / CR-038), 3 consumer blocks, 21 amendment-K pairs, 1 staged equation twin (G-5 variant A) | 51 blocks (+3 observations) | ≈ 0.60 M (tool-marker delta 15.00 M → 14.40 M at report time) | ≈ 80 |

Field entries: 37 = 27 `create` (5 registers, 1 select_one, 1 lookup_fill, 2 scalar inputs `flow_cv_pct` / `event_trigger_criterion`, 18 derived outputs — one per equation) + 10 UPDATE (visibility only; widget re-stated as prod's inferred widget, no `enum_values` written — D-1 `keep_prod` on the 5 selects). Migration `20260917102010`: 27 `INSERT … WHERE NOT EXISTS`, 10 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 10 UPDATE rows to the captured NULLs and deletes only the 27 `Plan 3:` rows. The emitter printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — pinned: no captured field lists its own worksheet; 0 unparseable gates); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  326 passed | 1 skipped (327)
      Tests  2970 passed | 1 expected fail | 1 skipped (2972)
   Start at  16:53:42
   Duration  55.41s (transform 17.22s, setup 57.08s, import 135.66s, tests 58.07s, environment 224.01s)
```
(2939 at `1a4544c` + 31 from this task: seed-iso5667-10 9, field-configs-iso5667-10 8, equations-iso5667-10 10, register-iso5667-10-termine 4; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`, first run). `pnpm -s eslint <11 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/iso5667-10-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  40 passed (40)
   Start at  16:55:05
   Duration  5.93s (transform 839ms, setup 0ms, import 3.81s, tests 1.93s, environment 0ms)
```
The harness seeds prod's 81 fields / 3 equations / 36 gates (`tests/harness/seed-iso5667-10.ts`); the new rows are not in it by construction — it proves the branch still drives every ISO-5667-10 block gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` / `parseCondition` on the seeded tables (`scripts/_probe_iso5667_10.ts`, deleted): the Fórmula-1 row expr reads `A` / `number_of_samples` from the worksheet scope (n = 30, A = −5 → 7,1667 / 19,3333 / 31,5; n = 12, A = −2 → 2,3333; n = 25 → the Fórmula-2 branch 2,08); a missing `A` leaves the derived cell null and the row COMPLETE (never blocks the count); `count_rows(reg, done == true)` counts an unset boolean as false; Fórmula 3 rows 200 / 600 / 1200 ml sum to V_final, an empty register is `manual_required` ("Keine vollständigen Zeilen"), a missing `V_final` is `manual_required` ("Unbekanntes Symbol v_n"); the nested `lookup()` band switch computes 0,5 / 0,5 / 0,3 for 9 / 11,9 / 12 mm; `if(<derived lookup miss> IS NULL, 1, …)` WORKS in row scope (the peristaltic row reads badge 1 with no diagnostic) while a bare `count_rows(reg, unit_volume_ml < unit_volume_min)` over that null is `manual_required` ("Fehlende Eingabe für count_rows(): unit_volume_min") — the badge form was chosen; every staged gate text parses (`guard` / `in` / `compare`) and passes / fails / pends as recorded per block. My first probe printed `r.cells` (undefined) instead of `r.values` — a scratch-script slip, fixed. Seed builder: the `inSpan` guard went RED once (`S5_SITE sewer_channel_manhole: cell "como norma general" is not inside its verbatim span` — the printed line breaks inside "como\nnorma general"; the asserted fragment became "norma general, debe situarse"), then verifier **14/14** first run → seed test **9/9** first run. Field-config module: the emitter accepted all 37 entries first time (the rule set was planned against the capture's consumer lists and the 36 gates; every refusal of the brief's targets is asserted through `gateReaders` / `emitFieldConfigSql` throws, not discovered) → **8/8** first run. Equations test **10/10** first run (pins written from the probe). Render test: before its first run I found by reading `prepareRegisterRows` that a register column keyed `id` would read the ROW IDENTITY (`row.id`) as its cell — renamed to `kennung` (fixtures updated, migration re-emitted) → **4/4** first run. Typecheck / eslint green on the first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso5667_10 "C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-10\ISO-5667-10.txt"` → **`14/14 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- S4_3_2 (2): gt25 L478–L489 (switch sentence + Fórmula (1) block + legend) · lt25 L491–L500 (Fórmula (2) sentence + block + legend)
- S7_2_1 (2): h2 / h24 — one shared span L860–L862
- S7_2_2_1_TUBE (2): ge9 L922–L923 · ge12 L928–L930 (NOTA 1); the ≥ 9 mm cue L903 is asserted in the builder and composed into the policy quote
- S7_2_2_1_PUMP (2): vacuum / inline_piston — one shared span L936–L937
- S7_2_2_4 (1): ctcv L1026–L1028
- S3_4 (1): qualified_grab L305–L307
- S9_1 (2): gt5 L1474–L1477 · le5 L1486–L1487
- S5_SITE (2): sewer_channel_manhole L555–L564 · cooling_system L705–L706

Status per table: `imported_unverified` — ALL eight (a Spanish plain-text transcript is a VC source by ruling; nothing is upgraded, no `verification_status` UPDATE in the seed migration). One glyph cell kept verbatim: the Fórmula (2) block prints "𝐵+" on its first two terms while the legend defines A only (U-1).

Beyond the verifier (which checks table rows only), a scratch checker (`scripts/_check_iso5667_10_quotes.ts`, deleted) asserted every `verification_quote` fragment of the two modules and every `override_quote` fragment of the eight tables against the whitespace-normalised transcript: **91/91 verbatim**; and every `"…" (L…)` fragment of the sheet section and the STAGED file: **158/158 verbatim** (both generated from line ranges, never retyped).

**Absence / presence claims (amendment O — commands re-executable):**
- O-4 — the inventory's "pump-volume sentence NOT located in the Spanish txt" is **refuted**: `grep -n "50 ml\|25 ml" "<transcript>"` → `936:- debe ser adecuado para garantizar un muestreo representativo (por ejemplo, al menos 50 ml para` / `937:la bomba de vacío o 25 ml para el émbolo en línea);` / `952:… 250 ml …` / `1351:representativo (25 ml como mínimo).` → S7_2_2_1_PUMP seeded.
- E-1 — peristaltic / external carry no printed minimum: `grep -n "peristáltica\|bombeo externos"` → L1319 / L1320 / L1440 — technology names only, no figure on those lines; the only figures of §7.2.2.1 are the two at L936–L937.
- §5.2 / §5.3 print no numeric site figure: `sed -n '580,668p' | grep -nE "[0-9]"` → only clause numbers (5.2, 5.1, 5.3, 7.4), page furniture (7, © ISO 2020, 2021-7-11, ISO 5667-10:2020) and figure references (A.1, A.3) → S5_SITE has no wwtp / industrial_site row.
- F-3 — "fórmulas similares para otros períodos" is not printed: `grep -n "un mes, tres meses\|seis"` → L502 only (the sentence, no formula block).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,70) AS title FROM standards WHERE code LIKE 'ISO-5667-10%'"
  → ISO-5667-10 · '2020 (ISO 5667-10:2020, second edition; ISO/TC 147/SC 6)' · issued_year null
node scripts/regulation-tables/build-prior-snapshot.mjs ISO-5667-10 iso5667_10
  → wrote src\lib\eval\field-configs\iso5667_10.prior.json: 81 field rows (0 orphan), 52 coded sections of 52, 3 equations, 36 gates (0 unparseable …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso5667_10
  → wrote seed + rollback for iso5667_10 -> scripts/migrations/20260917102000_regulation_tables_seed_iso5667_10.sql scripts/rollback-20260917102000-regulation-tables-seed-iso5667-10.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso5667_10 20260917102010
  → wrote 37 field entries + 0 section entries for iso5667_10 -> scripts/migrations/20260917102010_field_configs_iso5667_10.sql scripts/rollback-20260917102010-field-configs-iso5667-10.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso5667_10 20260917102020
  → wrote 18 equations for iso5667_10 -> scripts/migrations/20260917102020_equations_iso5667_10.sql scripts/rollback-20260917102020-equations-iso5667-10.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-iso5667-10.test.ts` / `equations-iso5667-10.test.ts` pin the other two against the committed `iso5667_10.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 14 row upserts (`ON CONFLICT (table_id, row_key)`) and 0 `verification_status` upgrades; its rollback deletes the eight tables (no earlier ISO-5667-10 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 18 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102000` → `20260917102010` → `20260917102020`; rollback in reverse. Every file touches `ISO-5667-10` rows only. **Materialisation (amendment D, noted once):** the register-fed rows (-01 D1 / D2, -02 D1 / D2, -03 D4 / D5, -06 D1 … D3, -07 D1 / D2) materialise on save — their row exprs read only OWN-worksheet scalars (`A` / `number_of_samples` on -03, `V_final` / `M3_total` on -06); the scalar-only rows (-03 D1 … D3, -05 D1 / D2, -06 D4, -08 D1) are computed on the hook / report / snapshot / PDF only — expected, not a defect.

## 5. What was encoded

**Tables (8)** — `src/lib/eval/regulation-tables-seed-iso5667_10.ts`, builder `iso566710SeedTables()`, edition `'2020'` (title page L7–L8 "Segunda edición" / "2020-11"; prod `standards.version` "2020 (ISO 5667-10:2020, second edition; ISO/TC 147/SC 6)"). The standard prints NO numeric table — every table is a printed SENTENCE rule or the printed formula switch, one row per printed alternative:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| S4_3_2 | `n_band` gt25 / lt25 (the `if(number_of_samples > 25, …)` tokens) | formula "1" / "2", period_unit días / semana, period_length 365 / 52, a_min_factor "– 365/n" / "– 52/n" | anhaltswert (L475–L476 "pueden determinarse mediante una fórmula. Un ejemplo es:" — L502–L503; brief said locked, O-1) | 2 | imported_unverified (U-1) |
| S7_2_1 | `duration` h2 / h24 (created select) | duration_h 2 / 24, max_interval_min 5 / 30 | locked (L860–L862 "No deben superar" — L864–L867) | 2 | imported_unverified |
| S7_2_2_1_TUBE | `bore_band` ge9 / ge12 (chosen in -06-D4) | bore_min_mm 9 / 12, min_velocity_m_s 0,5 / 0,3 | locked (L903 — L922–L923 "no debe ser inferior"; NOTA 1 → O-2) | 2 | imported_unverified |
| S7_2_2_1_PUMP | `pump` = prod tokens vacuum / inline_piston | min_unit_volume_ml 50 / 25, gedruckt | anhaltswert (L934–L937 "por ejemplo, al menos …"; brief said locked, O-3) | 2 (peristaltic / external: no printed figure, E-1) | imported_unverified |
| S7_2_2_4 | `criterio` ctcv | cv_max_pct 20, modal "puede aplicarse" | anhaltswert (L1026–L1028 — L1030–L1031) | 1 | imported_unverified |
| S3_4 | `definicion` = prod token qualified_grab | min_grabs 5, max_window_h 2, min_interval_min 2 (+ the printed words cinco / dos horas / dos minutos) | locked (L304–L307, definition) | 1 | imported_unverified |
| S9_1 | `volume_band` gt5 / le5 | threshold_l 5, comparator > / ≤, homogenizer = prod tokens mechanical / laboratory_manual, modal | anhaltswert (L1474–L1477 "deberían utilizarse" — L1486–L1487 "puede aplicarse") | 2 | imported_unverified |
| S5_SITE | `site` = prod tokens sewer_channel_manhole / cooling_system | restriction_min_diameters 3, depth_fraction_min 1/3, depth_fraction_max 0,5, runoff_min_s 30, modal | locked (L555–L557 — L705–L706; the softer modals → O-5) | 2 (wwtp / industrial_site print no figure) | imported_unverified |

**Registers / selections / fills / visibility** — `src/lib/eval/field-configs/iso5667_10.ts`:
- `stichproben_qualifiziert` (-01 C, `visible_when sample_type_definition == 'qualified_grab'`): nr, Uhrzeit (text HH:MM — no time arithmetic, F-2), Volumen; footer `qualified_grab_count_calc` / `qualified_grab_count_ok`; note L305–L307.
- `probenahmestellen` (-02 D, unconditional): the §4.2 documentation items (`kennung` required, Ort, Fotos, Koordinaten, Strömungsart = prod `flow_type` tokens, Zugang, Technik, gut durchmischt) + the discriminator `specific_site_type` (prod tokens) with the §5 branch columns — sewer: Abstand stromab der Drossel, Tiefe (Anteil) + badges `restriction_ok` (≥ 3 × D) / `depth_ok` (1/3 … 1/2); wwtp: Ziel (prod tokens), Bypass bewertet; cooling: Kühlsystem (prod tokens), Vorlaufzeit + badge `runoff_ok` (≥ 30 s), stromauf der Biozid-Dosierung — every badge reads `lookup('S5_SITE', …)` and is 1 (n. a.) for the other site types; footer `stellen_count` / `stellen_site_fail`; note L397–L398.
- `probenahmetermine` (-03 C, `visible_when sampling_period == 'year'`): k (required), `day_or_week` = `A + lookup('S4_3_2', if(number_of_samples > 25, 'gt25', 'lt25'), 'period_length') * k / number_of_samples` (Fórmula 1 / 2 lifted, over the worksheet's A and n — G-13), geplantes Datum, durchgeführt; footer `period_length_calc` / `A_min_calc` / `A_in_range` / `termine_count` / `termine_done`; note: the n rows are added by hand (F-1), n = 25 → Fórmula 2 (J-1).
- `flaschen_ctcv` (-06 C, `visible_when composite_mode == 'CTCV'`): n, M3n (required), `v_n = V_final * m3_n / M3_total` (Fórmula 3 lifted, over the worksheet's V_final / M3_total); footer `flaschen_count` / `v_n_sum` / `m3_n_sum`; note L998–L1003 + the single-bottle case (J-2).
- `probenahmegeraete` (-07 C, unconditional): Gerät (required), Bauart (prod `sampler_mobility` tokens), Pumpentechnologie (prod tokens, discriminator, required), Einheitsvolumen, `unit_volume_min` = `lookup('S7_2_2_1_PUMP', pump_technology, 'min_unit_volume_ml')`, badge `unit_volume_ok` (`if(unit_volume_min IS NULL, 1, if(unit_volume_ml IS NULL, 0, if(unit_volume_ml >= unit_volume_min, 1, 0)))` — J-5), gekühlt, Material kompatibel, gereinigt & geprüft; footer `geraete_count` / `geraete_unit_volume_fail`; note L936–L937 + E-1.
- Created select + fill (-06 C, both `visible_when main_sampling_type == 'composite'`): `composite_duration_band` (h2 / h24, labels "muestra mixta de 2 horas" / "… 24 horas") → `composite_interval_max` (`lookup_fill`, role limit ← S7_2_1, number min). Created inputs: `flow_cv_pct` (-05 D, `visible_when composite_mode == 'CTCV'`), `event_trigger_criterion` (-06 D text, `visible_when event_triggered_sampling == true`, L348–L351 / L1199–L1202).
- Field `visible_when` on existing inputs (10): -04 `sampling_depth_fraction` ← sewer (L560–L564), `wwtp_sampling_objective` / `bypass_flow_assessed` ← wwtp (L581–L589 / L591–L593), `cooling_system_type` / `upstream_of_biocide` ← cooling_system (L670–L671 / L706–L708); -05 `grab_method` ← grab (L719–L728); -06 `sampler_flow_linked` ← `composite_mode IN {'CVVT', 'CTVV'}` (L965–L967), `tank_mixing_maintained` ← in_storage (L374 — L1216–L1222); -07 `tank_mixing_system` / `tank_sampling_device` ← in_storage (L374 — L1415–L1428 / L1434–L1452; `pending` until C-1 — the driver is not inherited on -07). Refused and STAGED (pinned through the guards): every composite / CTCV input read by CR-016 / -017 / -018 / -019 (incl. the chain V_final / M3_total / M3_n → equation 3 V_n), the -04 CR-011 / -012 inputs, the -01 CR-002 scalars, the -03 A / k / day / week (chain → CR-008), `number_of_samples` (consumed), `homogeneity_deviation` (consumed + CR-021 + J-3), `composite_mode` (consumed — C-3). Section rules: none.

**Equations (18 new)** — `src/lib/eval/equations/iso5667_10.ts`: `ISO-5667-10-01-D1 qualified_grab_count_calc = count_rows(stichproben_qualifiziert)` · `-D2 qualified_grab_count_ok = if(count_rows(…) >= lookup('S3_4', 'qualified_grab', 'min_grabs'), 1, 0)` · `-02-D1 stellen_count` · `-D2 stellen_site_fail = count_rows(probenahmestellen, restriction_ok == 0 OR depth_ok == 0 OR runoff_ok == 0)` · `-03-D1 period_length_calc = lookup('S4_3_2', if(number_of_samples > 25, 'gt25', 'lt25'), 'period_length')` · `-D2 A_min_calc = 0 - lookup(…) / number_of_samples` · `-D3 A_in_range = if(A >= 0 - lookup(…) / number_of_samples AND A <= 0, 1, 0)` · `-D4 termine_count` · `-D5 termine_done = count_rows(probenahmetermine, done == true)` · `-05-D1 ctcv_cv_max = lookup('S7_2_2_4', 'ctcv', 'cv_max_pct')` · `-D2 ctcv_applicable = if(flow_cv_pct <= lookup(…), 1, 0)` · `-06-D1 v_n_sum = sum_rows(flaschen_ctcv, v_n)` · `-D2 m3_n_sum` · `-D3 flaschen_count` · `-D4 suction_velocity_min = lookup('S7_2_2_1_TUBE', if(tube_internal_diameter >= lookup('S7_2_2_1_TUBE', 'ge12', 'bore_min_mm'), 'ge12', 'ge9'), 'min_velocity_m_s')` · `-07-D1 geraete_count` · `-D2 geraete_unit_volume_fail = count_rows(probenahmegeraete, unit_volume_ok == 0)` · `-08-D1 homogenizer_mechanical_required = if(collected_volume > lookup('S9_1', 'gt5', 'threshold_l'), 1, 0)`. None outputs a prod symbol (equations 1 / 2 / 3 keep `sampling_day_k` / `sampling_week_k` / `V_n`); the only figure typed into a formula is the printed "unas 25" boundary (J-1). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables — the standard prints no worked example, so the pins are the printed forms on chosen inputs): Fórmula 1 with n = 30, A = −5 → 7,1667 / 19,3333 / 31,5; Fórmula 2 with n = 12, A = −2 → 2,3333 and k = n → A + 52; n = 25 → 52/25; period 365 / 52 / 52 (25) / 365 (26); A bound −365/30 and −52/12; A in range 1 (−5), 0 (−13), 0 (+1), 1 (0 — "entre – 365/n y 0" inclusive), missing A ⇒ manual; counts 3 / 1 / empty 0; Fórmula 3 → 200 / 600 / 1200 ml, Σ 2000 = V_final, Σ M3n 1000, count 3, empty Σ manual, missing V_final manual; velocity 0,5 / 0,5 / 0,3 / 0,3 / 0,5 for 9 / 11,9 / 12 / 16 / 8 mm; qualified grabs 5 → 1, 4 (fifth without time) → 0, empty 0 / 0; site badges [1,1,1] / [0,1,1] / [1,1,0] / [1,1,1], fails 2, a sewer row without figures [0,0,1], depth 0,33 → 0 and 1/3 / 0,5 → 1 / 0,51 → 0, 3 × D inclusive; CTCV 20 %: 15 → 1, 25 → 0, 20 → 1; devices [50,1] / [50,0] / [null,1] / [25,0] → count 4, fails 2, unset boolean false; homogeniser 6 l → 1, 5 l → 0, 5,01 → 1, missing ⇒ manual.

## 6. Inventory §5 top-wins walk

1. Sampling schedule generated from `number_of_samples` + `sampling_period` (Fórmula 1 / 2 chosen automatically, A drawn within its range, n dated rows) → **partially**: the register derives each row's day / week from the worksheet's A and n with the band chosen by the printed "unas 25" sentence, `A_min_calc` / `A_in_range` show and check the printed interval (A itself stays the engineer's draw — SR-2, never drawn by the system), `termine_count == n` is the completeness gate (G-9); the n rows are entered by hand (**F-1**, no row generator); inheritance of n from ISO-5667-1 is Phase 6 (X-2). ✓ (rows entered)
2. `composite_mode` drives the automatic-sampler block (CVVT / CTVV show the flow-link; CTCV shows the Fórmula-3 rows with Σ checks) → **encoded**: `sampler_flow_linked` hidden unless CVVT / CTVV; `flaschen_ctcv` + Σ Vn / Σ M3n / count hidden unless CTCV; the CTCV precondition (CV ≤ 20 %) as `flow_cv_pct` / `ctcv_cv_max` / `ctcv_applicable`; the unit-volume / velocity limits shown as created outputs. The hides of the four prod Fórmula-3 scalars and of `composite_interval` / tube / velocity / unit volume are the G-3 / G-4 / G-5 / G-7 follow-ups (gate-read). ✓
3. `specific_site_type` shows only the §5 checks for that site → **encoded twice**: on -04 the five consumer-free, gate-free inputs are hidden by site type (the two gate-read ones are G-6); on -02 the `probenahmestellen` register carries its own site-type column with the §5 branch columns and the S5_SITE badges per point. ✓
4. Limits switched by context instead of hard-coded (5 / 30 min by duration, 0,5 / 0,3 m/s by bore, 50 / 25 ml by pump type, mechanical / manual homogeniser by volume) → **encoded** as the four seeded sentence tables + `composite_duration_band` → `composite_interval_max` fill, `suction_velocity_min` (-06-D4), the per-device `unit_volume_min` / `unit_volume_ok` (-07 register), `homogenizer_mechanical_required` (-08-D1); the prod gates keep their single branch until G-2 / G-3 / G-4 / G-5 are ratified. ✓
5. One shared "Probenahme-QA" block across ISO-5667-1 / -6 / -10 and ATV-A-704E → **Phase 6** (X-1; CR-027 already encodes (5 ± 3) °C as 2–8).

Data-quality gaps of the inventory: the CR-016 / -017 / -018 single branches → G-3 / G-4 / G-5; the `k` / `sampling_day_k` / `M3_n` / `V_n` scalars for repeated data → the registers + D-12 … D-16; `qualified_grab_*` typed instead of derived → the count is derived (-01-D1 / D2), window / interval are F-2; `report_items_recorded` boolean hiding the §11.1 list → not encoded (a Spanish-only option list would render its tokens — the m820_2-E-1 class; residue); `sample_type_definition` vs the sibling enums → X-1 class, untouched.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`iso5667_10` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (iso5667_10 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+51 blocks, +3 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 20)" paragraph).
Created: `src/lib/eval/regulation-tables-quotes-iso5667_10.ts` (113 spans, generated), `src/lib/eval/regulation-tables-seed-iso5667_10.ts`, `src/lib/eval/field-configs/iso5667_10.ts`, `src/lib/eval/field-configs/iso5667_10.prior.json` (captured), `src/lib/eval/equations/iso5667_10.ts`, `src/lib/eval/__tests__/regulation-tables-seed-iso5667-10.test.ts`, `src/lib/eval/__tests__/field-configs-iso5667-10.test.ts`, `src/lib/eval/__tests__/equations-iso5667-10.test.ts`, `src/components/worksheet/__tests__/register-iso5667-10-termine.test.tsx`, `scripts/verification/iso5667_10-STAGED-plan3-rulings.sql` (51 ☐ blocks; 604 lines, 0 non-comment lines), `scripts/migrations/20260917102000_regulation_tables_seed_iso5667_10.sql` + `scripts/rollback-20260917102000-regulation-tables-seed-iso5667-10.sql`, `scripts/migrations/20260917102010_field_configs_iso5667_10.sql` + `scripts/rollback-20260917102010-field-configs-iso5667-10.sql`, `scripts/migrations/20260917102020_equations_iso5667_10.sql` + `scripts/rollback-20260917102020-equations-iso5667-10.sql`, this report (+ the copy at `.superpowers/sdd/…/task-20-report.md`).
Untouched: every other standard's file; the ISO-5667-10 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the two scratch scripts deleted; the three generators (quotes, STAGED + sheet section, playbook paragraph) live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Tables 8 (brief 5):** S3_4 (§3.4 five / two hours / two minutes — the count verdict reads it), S9_1 (§9.1 > 5 l / ≤ 5 l — the homogeniser code reads it) and S5_SITE (§5.1 3 × D, 1/3 … 1/2; §5.4 30 s — the register badges read them) added so no printed threshold is typed into a formula. The pump table IS seeded — the brief's "seed only if found, else `iso5667_10-U-1`" resolved to found (O-4); U-1 is used for the Fórmula-(2) glyph slip instead.
2. **S4_3_2 / S7_2_2_1_PUMP policies `anhaltswert`, not the brief's `locked`** — the guideline's own words ("pueden determinarse … Un ejemplo es", "por ejemplo, al menos 50 ml") decide (O-1 / O-3); S7_2_1 / S7_2_2_1_TUBE / S3_4 / S5_SITE are `locked` as the words say.
3. **Field `visible_when` 10 on existing inputs (brief 12)** plus 19 on created fields; the brief's Step-4 targets `composite_interval`, `V_final` / `M3_total` / `M3_n` / `V_n`, the Formula-1/2 scalars, the `qualified_grab_*` scalars, `homogeneity_deviation`, `composite_mode` and the two CR-011 / -012 inputs are refused by the gate-aware / producer guards — every one is a G / C block with the hide as the follow-up UPDATE.
4. **Equations 18 (brief 6 + 1 staged):** the brief's `-03-D1` triple is three rows (D1 / D2 / D3 — one output per row), plus `qualified_grab_count_ok`, `stellen_count` / `stellen_site_fail`, `flaschen_count`, `ctcv_cv_max` / `ctcv_applicable`, `geraete_count`, `homogenizer_mechanical_required`; the brief's `-07-D1 = count_rows(reg, unit_volume_ml < unit_volume_min)` became `count_rows(reg, unit_volume_ok == 0)` — the bare comparison over a lookup-miss null is `manual_required` (probed). The one staged equation is G-5's -06 twin (needs C-2).
5. **`probenahmestellen.kennung`, not `id`** (a column keyed `id` reads the row identity — playbook trap 4); `wwtp_objective` / `cooling_type` are enum columns over the prod tokens (fixed options ⇒ selection, never free text — the brief's "wwtp_objective text" was superseded by the ruling).
6. **`tube_bore_band` not created** — the brief's own alternative (the equation form, G-9 of the brief) was taken: `suction_velocity_min` reads the band inside the formula, the 12 mm bound itself from the table.
7. **`unit_volume_min` twin on -06 not created** — `pump_technology` is not inherited on -06 (C-2); the per-device check lives in the -07 register; G-5 stages both variants.
8. **Render test file name** `register-iso5667-10-termine.test.tsx` kept (brief), covering BOTH the brief's `probenahmestellen` sewer-vs-cooling case and the schedule register.
9. **Sign-off ids 51 (brief ≈ 8):** G-1 … G-9 (the brief's G-1 CR-021, G-2 CR-025 companion, G-3…5 CR-016/017/018, G-6 CR-011/012 kept; G-7 CR-019, G-8 CR-002, G-9 CR-008 added), C-1 … C-3, D-1 … D-21 (amendment K — one per pair), E-1, U-1, J-1 … J-5, F-1 … F-3, O-1 … O-5, I-1, X-1 / X-2. The brief's `iso5667_10-F-1` (generator) / `F-2` (time arithmetic) / `X-1` (QA block) ids kept.
10. **Prod section letters used verbatim** (-01 C, -02 D, -03 C, -05 D, -06 C / D, -07 C, -08 C); every created field lands in a captured section (pinned).

## 9. Self-review

- Every `verbatim_quote` passes the verifier (14/14); every `verification_quote` / `override_quote` fragment of the modules (91/91) and every `"…" (L…)` fragment on the sheet and in the STAGED file (158/158) was machine-checked against the transcript after being read at the cited lines in this session; the spans, the sheet section and the STAGED file were generated from line ranges (never retyped); prod facts (tokens, consumer lists, section codes, gate / equation ids + md5, labels, VR, `is_required`, the 0 / 0 stored-value / instance counts, `standards.version`) come from the in-session capture / queries quoted in §0 / §4 and the STAGED header.
- The emitter accepted all 37 entries and 18 equations first time because the rule set was planned against the capture; the field-config test pins every refusal of the brief's targets (`gateReaders` per gate incl. the two chain reads `→ Gl.3 V_n` / `→ Gl.1 sampling_day_k`; `emitFieldConfigSql` throws per consumer / gate), the IF-guard exemption that G-6 relies on (a guarded CR-011 makes the sewer hide pass), the G-A3 facts for every keyed table and every register enum column, the placement of every driver (incl. the `pending` -07 pair), the absence of self-consumer entries, and that no emitted UPDATE writes `enum_values`.
- Runtime facts were probed through the real register / condition contracts before the pins (probe deleted); the render test drives `probenahmestellen` (sewer vs cooling rows, discriminator-hidden columns, S5_SITE badges flipping on a typed 20 s, the required `kennung`) and `probenahmetermine` (Fórmula-1 days from `symbolLookup`, the count moving with a typed k, the empty cell without A) through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file (splices via `split/join` with a presence-and-uniqueness check that throws before writing; the modules were written with the Write tool; `sed -i` used only on two comment lines with literal patterns, checked by `grep`).
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing computed reaches prod's gates yet** — all 9 gate blocks (incl. the two-branch limits the inventory's win 4 asks for) and the 3 consumer blocks are STAGED; until the owner ratifies, CR-016 / -017 / -018 keep enforcing their single branch, CR-002 / -011 / -012 keep blocking projects the clause does not address, and the registers are documentation beside the scalars. That is the fail-safe reading: every target is gate-read or consumed, and a hide would be an enforcement change.
2. **21 amendment-K pairs** — the brief's per-point / per-device registers twin almost every -02 / -04 / -07 scalar; each is one ☐ (data-free today: 0 stored values, 0 instances). D-4 / D-18 / D-20 / D-21 recommend DEFER (the scalar is the campaign-level or consumed fact); D-3 / D-19 need a twin equation before their gate can move.
3. **G-4 / J-4** guards the §7.2.2.1 automatic-sampler rules on the automatic COMPOSITE modes — an automatic grab sampler (§6.1 a.3) would be unguarded; the alternative (keep unconditional, rewrite the operand only) is in the block.
4. **J-2** binds Fórmula (3) to the prod token `CTCV` while §7.2.2.4 recommends a single bottle for CTCV; a created `multi_bottle_sampler` driver is the alternative.
5. **I-1** — server-side visibility resolves own fields only: the inherited-driver rules (composite / CTCV on -06, in_storage on -06 / -07) hide on the form / report / PDF but are `pending` (visible) for the server materialiser / approval gate — fail-safe (nothing skipped), recorded once.
6. **Bundle growth:** ~15 KB of lifted Spanish spans (113 spans; Task 30 measures).
7. **Token/time:** ≈ 0.60 M tokens in, ≈ 80 min wall-clock — in the Task 16 / 19 band; the cost sits in the 21 D-pairs and the 9 gate rewrites, not in the transcript (14 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The §11.1 report items (13 + the 11 automatic-sampler traceability items, L1706–L1763) as a checklist — a Spanish-only option list would render its tokens (the m820_2-E-1 [CODE] gap), so the boolean `report_items_recorded` stays; the §4.2 fixed-installation facilities list (L405–L416: vehicle access, working surfaces, lighting / power, water connection, safety, flow meters) behind the boolean `fixed_installation_facilities`; §7.2.2.1's 250 ml / 5 % CV validation rule for smaller unit volumes (L950–L953 — a validation attestation); §7.2.2.5's sampler information to record (L1040–L1042); §7.2.3's identical discrete volumes for the manual composite (L1046–L1047) and its ≤ 5 % CV (L1049–L1050 — CR-024 covers the manual equipment on -07); §7.3.1's 30 cm / mid-depth (CR-020 covers it); §7.4's rinse "al menos una vez el volumen del sistema de muestreo" (L1233–L1235) and the downstream < 20 % check (L1268–L1272); §8.1's prohibited materials (L1281–L1288 — the boolean `prohibited_material_avoided`); §9.4.3's (5 ± 3) °C as a printed nominal ± tolerance (CR-027 encodes 2–8); §11.2's protocol-change history (the boolean `protocol_change_history`); Annexes A–I (not in the transcript's read range; figures / tables of the annexes were not read — content-boundary). Each is a procedural sentence, an attestation prod already carries, or a list whose rendering needs a [CODE] item, and stays outside this encoding or on the sign-off sheet.
