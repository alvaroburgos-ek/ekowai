# Plan 3 — ISO-5667-6 (`iso5667_6`) — Task 23 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 24)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `c0cdb03`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write; `git stash` not used. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs ISO-5667-6 iso5667_6` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`; the 30 compliance rows with ids / severities / md5(condition) / lengths / titles / `requires_attestation`; the 2 equation rows with ids / md5 / formulas / status; the stored-parameter count (0), the worksheet-instance count (0) and the max CR code (CR-030); labels / units / `is_required` / VR of the fields on -02 / -03 / -04 / -07 / -09 / -11 / -13; the section codes / titles / field counts of nine worksheets; the -13 `validation_rules`; the `information_schema` column lists of `compliance_requirements` / `equations`, one row per column). `.env.local` never printed (variable NAMES listed once with `grep -oE '^[A-Z_]+='`).
- **SR-1 source:** every seeded value and every cue was read in this session from the **SPANISH** transcript `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-6\ISO-5667-6-2015.txt` (2511 lines; NCh-ISO 5667/6:2015 — the Chilean adoption of ISO 5667-6:2014, INN licence copy, pdftotext in a WIDE layout: every line indented ~85 columns, page furniture interleaved; **VC grade by ruling** — no PDF exists for this standard, `docs/verification-doctrine.md` names it the one source-absent standard). Read in full lines (never `cut` on a quoted line; the `sed 's/^ \{1,84\}//'` display de-indent was used for READING only, the generator trims lines mechanically): title page L1–L60, TOC L84–L160, §5.1.1–§5.2 L628–L1005, §6 L1006–L1067, §7 L1068–L1362, §8 L1363–L1438, §9.4 L1529–L1564, §10.3 / §10.8–§10.10 L1634–L1660 / L1758–L1797, §11.2–§11.3.2 L1827–L1866, §13 L1943–L2031, §15 L2051–L2135, Anexo A L2136–L2200, Anexo B L2200–L2300 (template, glanced). The 79 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-iso5667_6-quotes.mjs`, each line trimmed / empty lines dropped, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-iso5667_6.ts`, never hand-edited); every seeded cell is asserted inside its span at build time (`inSpan`, whitespace-collapsed). The inventory was used for pointers only.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `iso5667_6` | 6 (ANNEXA, S13_1, S7_1, S5_1_3, S5_1_4, S10_8) | 23 (23/23 verbatim) | 0 rows skipped; 1 formula block kept as printed (Ecuación (A.1) loses its radical — U-1, no cell depends on it); **every table `imported_unverified`** (Spanish txt = VC, nothing upgraded) | 4 (all `create`: `sampling_points_6` -02, `heterogeneity_samples` -03, `travel_time_runs` -04, `increments` -09) + 1 `select_many` checklist (`report_items_6` -11) | 1 created (`bed_roughness_hint` -13, 2 options = ANNEXA keys) + 3 UPDATEs on prod selects (`keep_prod`) | 1 (`report_items_6`, 17 = S13_1 a) – q)) | 1 (`create`: `c_hint` -13 ← ANNEXA by `bed_roughness_hint`, role value, `anhaltswert` twin of the typed `c`) | **16** rule-bearing entries: 6 UPDATEs on existing inputs (-03 ×2, -04 ×1, -07 ×1, -08 ×2) + 10 on created fields; plus 6 row-scope rules inside `sampling_points_6` | 0 (the -13 B / C rules are refused → C-1) | **10** (-02 D1–D2; -03 D1–D3; -04 D1–D3; -09 D1–D2) | 0 replacements (A.1 and Eq. 2 untouched); 1 U-block with the A.1 rewrite on ratification (U-1), 1 deletion proposal (D-3, archive pattern), 10 gate blocks (G-1 … G-10, incl. 2 new gates CR-031 / CR-032), 1 consumer / section block (C-1), 22 amendment-K pairs | 46 blocks (+7 observations) | ≈ 0.52 M (tool-marker delta 15.00 M → 14.48 M at report time) | ≈ 55 |

Field entries: 25 = 19 `create` (4 registers, 10 derived outputs, 1 select_one, 1 lookup_fill, 1 select_many, 1 attestation boolean `bottle_contains_preservative` -09, 1 text note `legal_custody_regulations` -10) + **6 UPDATE** (`mixing_dimension` / `vertical_mixing_distance` on -03, `travel_time_extrapolation_limit` on -04 K, `bridge_position` on -07, `isokinetic_conditions` / `automatic_sampler_mode` on -08 — no consumer, no gate reads them; the emitter accepted all six). Migration `20260917102310`: 19 `INSERT … WHERE NOT EXISTS`, 6 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes only the 19 `Plan 3:` rows and restores the six prior NULLs. No `enum_values` is written by any UPDATE (D-1 — the three UPDATE selects are `keep_prod`). The emitters printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — pinned: no captured field lists its own worksheet; the 2 unparseable gates are the 2 EMPTY conditions CR-007 / CR-028, which never refuse); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  338 passed | 1 skipped (339)
      Tests  3032 passed | 1 expected fail | 1 skipped (3034)
   Start at  20:35:14
   Duration  58.83s (transform 19.14s, setup 59.00s, import 148.58s, tests 62.93s, environment 234.95s)
```
(3009 at `c0cdb03` + 23 from this task: seed-iso5667-6 7, field-configs-iso5667-6 8, equations-iso5667-6 6, register-iso5667-6-points 2; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`, first run). `pnpm -s eslint <11 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task; run at HEAD `c0cdb03` BEFORE any of this task's files existed — 16/16 — and again after):
```
pnpm vitest run --project integration tests/harness/iso5667-6-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  16 passed (16)
   Start at  20:36:40
   Duration  5.63s (transform 856ms, setup 0ms, import 3.97s, tests 1.45s, environment 0ms)
```
The harness was NOT edited (no stale pin). It seeds the 13 worksheet templates, the 6 BLOCK + 24 WARN gates and the 2 equations of prod with the fields those gates read (`tests/harness/seed-iso5667-6.ts`, header: "13 worksheet_templates, 6 BLOCK + 24 WARN gates, 2 equations"); the new rows are not in it by construction — it proves the branch still drives every ISO-5667-6 block gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch probe through the real `prepareRegisterRows` / `evaluateFormula` / `evaluateCondition` / `parseFieldConfig` on the seeded tables (`scripts/_probe_iso5667_6.ts`, deleted): the `depth_ok` row expr with `IS NULL OR` inside `if()` parses and reads 1 / 0 / 0 / 0 on [30/40, 20/40, no depths, incomplete row]; `count_rows(reg, depth_ok == 0)` counts 2 of the 3 complete rows; an EMPTY register reads 0 / 0 for both counts (computed, never manual); row-scope `visible_when` on enum / boolean / number columns keeps the visible cells (`bridge_position` 'upstream', `ice_safety` true, `vertical_mixing_distance` 120) and stores null for the hidden ones; `max_rows − min_rows` reads 0,5 over three complete rows and `manual_required` ("Keine vollständigen Zeilen") on an empty register while the count verdict reads 0; `count_rows(reg, flow IS NOT NULL)` counts ROWS (3 rows at 2 flows → 3), so the distinct-flow count is F-1; `sum_rows` 3,5 over three complete increments (an increment without duration is incomplete), `if(sum_rows(…) < lookup(…), 1, 0)` on an empty register is `manual_required` ("Fehlende Eingabe für if(): increments") — never a phantom pass; `count_rows(increments, preservative == true)` counts 1 (unset boxes false); the zero-input twin `lookup('S5_1_3', 'travel_time', 'extrapolation_max_pct')` computes 10; `lookup('ANNEXA', bed_roughness_hint, 'c_example')` computes 50 with the enum input as a string; every staged gate text parses and evaluates as recorded per block (`IF sampling_location_type == 'bridge' THEN bridge_checks_passed == true`: bridge + false ⇒ fail, bank ⇒ pass, unset ⇒ pending; the `travel_time_required` / `sampling_strategy` / `sampling_mode` / `sampling_method` / `homogeneity_status` / `bottle_contains_preservative` / `sampling_point_count_calc` / `mixing_relevant` guards likewise). Seed builder: the `inSpan` guard went RED once on my S13_1 item i) (the cell wraps around the furniture line L1984, so the joined text is not one contiguous substring) → the builder now asserts the printed FRAGMENTS per contiguous run and joins them (`s131ItemFragments`); verifier **23/23** first run → seed test **7/7** first run. Field-config module: the emitter accepted all 25 entries first time (the rule set was planned against the capture's consumer lists and the 30 gates; every refusal of the brief's targets is asserted through `gateReaders` / `emitFieldConfigSql` throws / `producerChain`, not discovered) → **8/8** first run. Equations test: one RED on my own "no figure typed" pin (the badge comparison `depth_ok == 0` is structural, the regex now strips `== 0`) → **6/6**. Render test **2/2** first run. STAGED generator: its span guard threw twice on MY line references (the CR-028 sentence is L950–L951, not L946–L948; the §10.8 "pequeños volúmenes" sentence is L1760–L1761, not L1765–L1768) — corrected, 46 blocks written. Typecheck / eslint green on the first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso5667_6 "C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-6\ISO-5667-6-2015.txt"` → **`23/23 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- ANNEXA (2): muy_irregular / liso — one shared span L2179–L2183 (the EJEMPLO); the range "15 < c < 50" (L2165) is asserted at build time and cited in the policy quote
- S13_1 (17): a L1950 · b L1952–L1953 · c L1964–L1965 · d L1967 · e L1969 · f L1971–L1972 · g L1974 · h L1976–L1977 · i L1979–L1985 (furniture line L1984 inside) · j L1987 · k L1989 · l L1991 · m L1993 · n L1995 · o L2001 · p L2003–L2004 · q L2006
- S7_1 (1): general L1165–L1169 · S5_1_3 (1): travel_time L859–L863 · S5_1_4 (1): heterogeneity L897–L905 · S10_8 (1): incremental L1765–L1768

Status per table: `imported_unverified` — ALL six (a Spanish plain-text transcript is a VC source by ruling; nothing is upgraded, no `verification_status` UPDATE in the seed migration).

Beyond the verifier (table rows only), two scratch checkers (scratchpad `check-iso5667_6-q.ts`, `check-iso5667_6-frags.mjs`) asserted every `Q` span, every module `verification_quote` fragment, every `override_quote` fragment (**136/136 verbatim**, whitespace-normalised) and every `"…" (L…)` fragment of the sheet section and the STAGED file (**122/122 verbatim**; both generated from line ranges by `gen-iso5667_6-staged.mjs`, which itself throws on a fragment outside its span, never retyped).

**Absence / presence claims (amendment O — commands re-executable, raw output):**
- D-2 — no numeric g is printed: `grep -n "9,81\|9\.81" "<transcript>"` → (empty, exit 1); `grep -n "gravedad" "<transcript>"` → `1398:… el efecto de la gravedad sobre los sólidos suspendidos` / `2167:… g    = aceleración debido a la gravedad, expresada en metros por segundo al cuadrado (m/s2);` (exit 0) — the legend prints the unit only.
- "No numeric value table" (inventory §1, confirmed): `grep -n "Tabla [0-9A-Z]" "<transcript>"` → L174 / L176 / L177 / L178 (TOC), L1207 / L1214 (Tabla 1 — the bridge upstream / downstream advantages matrix, prose cells), L2214 / L2272 (Tablas B.1 / B.2 — the example report FORM), L2487 (Tabla D.1 — Cambios editoriales) — no printed numeric table; `grep -n "Ecuación\|(A\.[0-9])" "<transcript>"` → L2147 / L2154 / L2182 / L2185 — Ecuación (A.1) is the only printed formula.
- U-1 — the radical is absent from the txt: `Q.L2152_2155` = `"l=\n(\n0,13b 2c 0,7c + 2 g   )   …   (A.1)\ngd"` (pinned: contains `0,13b 2c 0,7c + 2 g`, not `√`).
- O-1 — brief targets absent from the CAPTURE (not a transcript claim): `Object.keys(prior).filter(k => k.includes('chain_of_custody'))` → `[]`; `prior['ISO-5667-6-09 bottle_contains_preservative']` → undefined; `prior['ISO-5667-6-04 extrapolation_pct']` → undefined (all three pinned in `field-configs-iso5667-6.test.ts`).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code LIKE 'ISO-5667-6%'"
  → ISO-5667-6 · '2014 (ISO 5667-6:2014; NCh-ISO 5667/6:2015 declared identical)' · issued_year null
node scripts/regulation-tables/build-prior-snapshot.mjs ISO-5667-6 iso5667_6
  → wrote src\lib\eval\field-configs\iso5667_6.prior.json: 76 field rows (0 orphan), 64 coded sections of 64, 2 equations, 30 gates (2 unparseable — symbols: [] + parse_error; …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso5667_6
  → wrote seed + rollback for iso5667_6 -> scripts/migrations/20260917102300_regulation_tables_seed_iso5667_6.sql scripts/rollback-20260917102300-regulation-tables-seed-iso5667-6.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso5667_6 20260917102310
  → wrote 25 field entries + 0 section entries for iso5667_6 -> scripts/migrations/20260917102310_field_configs_iso5667_6.sql scripts/rollback-20260917102310-field-configs-iso5667-6.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso5667_6 20260917102320
  → wrote 10 equations for iso5667_6 -> scripts/migrations/20260917102320_equations_iso5667_6.sql scripts/rollback-20260917102320-equations-iso5667-6.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-iso5667-6.test.ts` / `equations-iso5667-6.test.ts` pin the other two against the committed `iso5667_6.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 23 row upserts (`ON CONFLICT (table_id, row_key)`) and 0 `verification_status` upgrades; its rollback deletes the six tables (no earlier ISO-5667-6 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 10 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102300` → `20260917102310` → `20260917102320`; rollback in reverse. Every file touches `ISO-5667-6` rows only. **Materialisation (amendment D, noted once):** 9 of the 10 rows are register-fed (each reads only its own worksheet's register carrier) and materialise on save; `ISO-5667-6-04-D3` (`travel_time_extrapolation_max = lookup('S5_1_3', …)`, zero inputs) is scalar-only — computed on the form / report, not server-materialised (I-1). The -13 rules keyed on the inherited `mixing_relevant` hide on the form / report / PDF but are `pending` (visible) for the server materialiser / approval gate — fail-safe (I-1).

## 5. What was encoded

**Tables (6)** — `src/lib/eval/regulation-tables-seed-iso5667_6.ts`, builder `iso56676SeedTables()`, edition `'2015'` (title page L4–L5 "Primera edición" / "2015.02.24"; reference number L28–L29; prod `standards.version` "2014 (ISO 5667-6:2014; NCh-ISO 5667/6:2015 declared identical)"). The standard prints NO numeric table; two text catalogues and four printed SENTENCE rules (so no figure is typed into a formula — pinned in the equations test):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| ANNEXA | `roughness` muy_irregular / liso (the two printed EJEMPLO descriptions = tokens of the created `bed_roughness_hint`) | c_example 15 / 50, c_min 15, c_max 50 (L2165 "15 < c < 50"), descripcion (printed), l_example_m "83 m" / "683 m" (text) | anhaltswert (L2140 "(informativo)" — L2146–L2148 "se puede calcular aproximadamente") | 2 | imported_unverified |
| S13_1 | `item` a … q (tokens of the created `report_items_6`) | letter, text (the printed item without its marker; i) = two fragments around L1984) | anhaltswert (L1946–L1948 "podrían considerar para su inclusión") | 17 | imported_unverified |
| S7_1 | `regla` general | height_above_bed_min_cm 30, depth_below_surface_min_cm 30, gedruckt, modal | anhaltswert (L1165–L1169 "suelen ser satisfactorias" — L1284–L1285) | 1 | imported_unverified |
| S5_1_3 | `regla` travel_time | flows_min 5 ("cinco"), extrapolation_max_pct 10, modal | anhaltswert (L846–L849 — L859–L863 "se deberían realizar") | 1 | imported_unverified |
| S5_1_4 | `regla` heterogeneity | flow_share_pct 90, samples_approx 6 ("aproximadamente seis"), flows_min 3 ("al menos tres"), modal | anhaltswert (L889–L891 — L897–L899 "se deberían tomar") | 1 | imported_unverified |
| S10_8 | `regla` incremental | total_time_max_min 5, comparator "<", modal | anhaltswert (L1765–L1768 "debería ser menor") | 1 | imported_unverified |

**Registers / selections / fills / visibility** — `src/lib/eval/field-configs/iso5667_6.ts`:
- `sampling_points_6` (-02 B, unconditional): `kennung` (required — amendment P: never `id`), `coordinate` (§5.1.1), `depths`, `location_type` = prod `sampling_location_type` tokens (required, discriminator; labels = the §7.2–§7.6 headings) with the branch columns — bridge: `bridge_position` (prod tokens, "aguas arriba … bevorzugt"), `bridge_checks` (a–d); wading / bank: `ppe_high_visibility` (§7.1 / §15); under_ice: `ice_safety` (§7.6 / §15); `homogeneity_status` / `mixing_relevant` / `mixing_dimension` (← `mixing_relevant == true`) / `vertical_mixing_distance` (← vertical) over the prod tokens; `depth_below_surface_cm` / `height_above_bed_cm` + badge `depth_ok` (`if(a IS NULL OR b IS NULL, 0, if(a >= lookup('S7_1', …) AND b >= lookup('S7_1', …), 1, 0))` — "not entered ⇒ 0", decidable); footer `sampling_point_count_calc` / `points_depth_fail`; note L1168–L1169 + D-4 … D-16 + J-1.
- `heterogeneity_samples` (-03 D, unconditional): `sample_no` / `flow` / `value` required, `determinant` text + datalist of the eleven printed names (an OPEN list — L939–L940 "Se deberían incluir otros determinantes"); footer `heterogeneity_samples_count_calc` / `heterogeneity_samples_ok` / `heterogeneity_spread`; note L897–L899 + L904–L905 + F-1 + J-3.
- `travel_time_runs` (-04 B, `visible_when travel_time_required == true`): `flow` (required), `method` = prod `travel_time_method` tokens (the three printed methods L846–L849), `travel_time_min` (required); footer `travel_time_flows_count_calc` / `travel_time_flows_ok` / `travel_time_extrapolation_max`; note L859–L863 + D-20 / D-21 + G-3.
- `increments` (-09 C, `visible_when sampling_mode == 'incremental'`): `time`, `mode` = the four non-incremental prod `sampling_mode` tokens (§10.4–§10.7), `bottle`, `preservative` (boolean), `duration_min` (required); footer `increment_total_time_calc` / `increment_time_ok`; note L1765–L1768 + D-23 + G-5.
- `report_items_6` (-11 C, created json, `select_many`, 17 options = S13_1 a) – q) with the printed item texts as labels); the prod single-value `report_item` stays (D-1); completeness is F-2 / G-7.
- `bed_roughness_hint` (-13 B, created enum, 2 options = ANNEXA keys) → `c_hint` (-13 B, created number, `lookup_fill` ANNEXA by `roughness`, role value, source label "Anexo A EJEMPLO (Anhaltswert)") — both `visible_when mixing_relevant == true` (inherited from -03); the typed `c` keeps its VR "c > 15 AND c < 50" (SR-2, never auto-picked), `g` stays typed (D-2), `l` / Eq. A.1 untouched (U-1).
- Created inputs: `bottle_contains_preservative` (-09 C, attestation, L1657–L1658 + L1772–L1773 — the §10.3 driver; G-2 / J-4), `legal_custody_regulations` (-10 C, text, `visible_when legal_purpose_sample == true`, L1852–L1857 — O-1: no `chain_of_custody_*` exists).
- Field `visible_when` on existing inputs (6): -03 `mixing_dimension` ← `mixing_relevant == true` (L728–L730 — L699–L706), `vertical_mixing_distance` ← `mixing_dimension == 'vertical'` (L744–L749); -04 `travel_time_extrapolation_limit` ← `travel_time_required == true` (L859–L863); -07 `bridge_position` ← `sampling_location_type == 'bridge'` (L1194–L1195 — L1210–L1212); -08 `isokinetic_conditions` / `automatic_sampler_mode` ← `sampling_method == 'continuous'` (L1402–L1404 — L1420–L1424 / L1531–L1535 — L1545–L1549). Refused and STAGED (pinned through the guards): `inlet_velocity` ← CR-017 (G-1), `travel_time_method` / `travel_time_flows_count` ← CR-009 (G-3), `cycle_coincidence_avoided` ← CR-011 (G-4), `increment_total_time` ← CR-021 (G-5), `bridge_checks_passed` ← CR-015 (G-6), the -13 sections B / C ← chain b / c / g / d → A.1 l consumed by -03 (C-1). Section rules: none.

**Equations (10 new)** — `src/lib/eval/equations/iso5667_6.ts`: `ISO-5667-6-02-D1 sampling_point_count_calc = count_rows(sampling_points_6)` · `-D2 points_depth_fail = count_rows(sampling_points_6, depth_ok == 0)` · `-03-D1 heterogeneity_samples_count_calc` · `-D2 heterogeneity_samples_ok = if(count_rows(…) >= lookup('S5_1_4', 'heterogeneity', 'samples_approx'), 1, 0)` · `-D3 heterogeneity_spread = max_rows(heterogeneity_samples, value) - min_rows(heterogeneity_samples, value)` · `-04-D1 travel_time_flows_count_calc` · `-D2 travel_time_flows_ok = if(count_rows(…) >= lookup('S5_1_3', 'travel_time', 'flows_min'), 1, 0)` · `-D3 travel_time_extrapolation_max = lookup('S5_1_3', 'travel_time', 'extrapolation_max_pct')` · `-09-D1 increment_total_time_calc = sum_rows(increments, duration_min)` · `-D2 increment_time_ok = if(sum_rows(…) < lookup('S10_8', 'incremental', 'total_time_max_min'), 1, 0)`. None outputs a prod symbol (A.1 `l` and Eq. 2 `sampling_depth_below_surface` keep their rows); no figure is typed into a formula (pinned). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables — the standard prints no worked example for these counts; the pins are the printed thresholds on chosen rows): four points [30/40 ok, 20/40 fail, no depths fail, 35/30 ok] + one without `kennung` ⇒ count 4, fails 2, empty ⇒ 0 / 0, 29,9 cm ⇒ 0; six heterogeneity samples ⇒ count 6, ok 1, spread 0,5; five ⇒ ok 0; empty ⇒ 0 / 0 / spread manual; five runs ⇒ 5 / 1, four (+ one without time) ⇒ 4 / 0, the twin 10, empty ⇒ 0 / 0; increments 1,5 + 2 + 1 ⇒ Σ 4,5 / ok 1, + 0,5 ⇒ Σ 5 / ok 0 (strict), empty ⇒ Σ manual / ok manual.

## 6. Inventory §5 top-wins walk

1. Sampling points as repeatable rows (coordinate, depth, location type, homogeneity, mixing) with `sampling_point_count` derived and the §7 location-type checks shown per row → **encoded** (`sampling_points_6` on -02 with the discriminator + branch columns per §7.2 / §7.3–7.4 / §7.6, the §7.1 badge, `sampling_point_count_calc` / `points_depth_fail`; the scalars stay — D-4 … D-16; the gate on the register verdict is G-9). ✓
2. Heterogeneity-test samples and travel-time measurements as rows so the ≥ 6 / ≥ 3 flows / ≥ 5 flows counts and `homogeneity_status` are computed → **partially**: `heterogeneity_samples` + count / ok / spread and `travel_time_runs` + count / ok are computed; the ≥ 3 DISTINCT flows are not expressible (**F-1**); `homogeneity_status` stays the engineer's decision (the standard prints no statistic — J-3); CR-008 / CR-009 onto the register verdicts are G-8 / G-3. ✓ (counts) / F-1 (distinct flows)
3. Annex A mixing distance computed once (g fixed at 9,81, c chosen from a roughness hint) → **deferred with reason**: the printed formula block loses its radical (√g vs 2g — **U-1**, the prod row A.1 reads 2·g and is not rewritten; the printed EJEMPLO reproduces under 2·√g with the conventional g — a computation, not a source); 9,81 is not printed (**D-2**, grep empty); the roughness hint IS encoded (`bed_roughness_hint` → `c_hint`, `anhaltswert` beside the typed `c`, SR-2). Inheritance of `l` by the -03 site row exists in prod (consumer). Deferred (U-1 / D-2)
4. §13.1 report checklist as a multi-select (17 items) with CR-027 evaluated on completeness → **encoded** (`report_items_6` select_many over S13_1; D-1 retires `report_item`); completeness cannot be materialised today (**F-2** [CODE]) — G-7 DEFER. ✓ (checklist) / F-2 (gate)
5. Share the transport / preservation / QA / safety block with ISO-5667-10 and ISO-5667-1 → **Phase 6** (X-1).

Data-quality gaps of the inventory: Spanish-only transcript → VC, every table `imported_unverified`; Annex A √g → U-1; `g` typed → D-2; Eq. 2 identity → D-3 (deletion proposal, archive pattern); `report_item` single-value → D-1 + the checklist; `parameters_to_analyse` free text → untouched (no printed list); `sampling_depths` scalar → D-5; unit "-" placeholders → untouched (prod hygiene); CR-009 `= ''` on an enum → named inside G-3 (the rewrite replaces it by the boolean driver).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`iso5667_6` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (iso5667_6 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+46 blocks, +7 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 23)" paragraph).
Created: `src/lib/eval/regulation-tables-quotes-iso5667_6.ts` (79 spans, generated), `src/lib/eval/regulation-tables-seed-iso5667_6.ts`, `src/lib/eval/field-configs/iso5667_6.ts`, `src/lib/eval/field-configs/iso5667_6.prior.json` (captured), `src/lib/eval/equations/iso5667_6.ts`, `src/lib/eval/__tests__/regulation-tables-seed-iso5667-6.test.ts`, `src/lib/eval/__tests__/field-configs-iso5667-6.test.ts`, `src/lib/eval/__tests__/equations-iso5667-6.test.ts`, `src/components/worksheet/__tests__/register-iso5667-6-points.test.tsx`, `scripts/verification/iso5667_6-STAGED-plan3-rulings.sql` (46 ☐ blocks; 560 lines, 0 non-comment lines), `scripts/migrations/20260917102300_regulation_tables_seed_iso5667_6.sql` + `scripts/rollback-20260917102300-regulation-tables-seed-iso5667-6.sql`, `scripts/migrations/20260917102310_field_configs_iso5667_6.sql` + `scripts/rollback-20260917102310-field-configs-iso5667-6.sql`, `scripts/migrations/20260917102320_equations_iso5667_6.sql` + `scripts/rollback-20260917102320-equations-iso5667-6.sql`, this report (+ the copy at `.superpowers/sdd/…/task-23-report.md`).
Untouched: every other standard's file; the ISO-5667-6 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the scratch probe deleted; the generators (quotes, STAGED + sheet section, the two fragment checkers, the playbook paragraph) live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Tables 6 (brief 2):** S7_1 (§7.1 30 cm / 30 cm — the `depth_ok` badge reads it), S5_1_3 (§5.1.3 five flows / 10 % — the -04 verdicts read it), S5_1_4 (§5.1.4 six samples / three flows / 90 % — the -03 verdict reads it) and S10_8 (§10.8 5 min — the -09 verdict reads it) added so no printed threshold is typed into a formula (the Task 20 pattern; pinned). ANNEXA carries `c_min` / `c_max` as the printed bounds beside the two example points.
2. **`sampling_points_6.kennung`, not `id`** (controller resolution (1) / amendment P; pinned: no register column is keyed `id` and none shadows a prod symbol of its worksheet).
3. **No `l_mixing_calc`, no A.1 rewrite** (resolution (2)) — U-1 states both readings with the EJEMPLO arithmetic; **no D-2 constant proposal as an equation** (resolution (3): "9,81" not printed — grep in §3; the block proposes a `default_value` on the typed `g`, owner ruling).
4. **`report_items_6` created json select_many** (resolution (5)); `report_item` → D-1 (retirement, not amendment N).
5. **`heterogeneity_flows_count` withheld** (resolution (6) — F-1, probed: `count_rows(reg, flow IS NOT NULL)` counts rows); `heterogeneity_spread` emitted as the brief says, with J-3 (per-determinant filter not expressible).
6. **Step-4 targets that do not exist in the capture (O-1):** `chain_of_custody_*` (-10) → a created text note under `legal_purpose_sample`; `bottle_contains_preservative` (-09) → created; `extrapolation_pct` → prod `travel_time_extrapolation_limit` (-04 K). The `cycle_coincidence_avoided` / `inlet_velocity` / `increment_total_time` / `travel_time_method` / `travel_time_flows_count` hides are gate-read → G-blocks (the brief anticipated G-1 = CR-017 and G-3 = CR-009; CR-011 → G-4, CR-021 → G-5, CR-015 → G-6 were found by the guard). The -13 section rules → C-1 (transitive guard). The brief's `equipment_washes = 0` rule → G-2 with J-4 (the sentence forbids rinsing the BOTTLES, not washing the equipment).
7. **`heterogeneity_samples.determinant` is text + datalist, not a closed enum** (L939–L940 print "otros determinantes" — an open list); `travel_time_runs.method` and `increments.mode` are ENUM columns over the prod tokens (the printed lists are closed — rule 3b), not the brief's `text`.
8. **Equations 10 (brief 5 + 2 staged):** the brief's `-02-D1` pair is two rows (D1 / D2), `-03-D1` is three (count / ok / spread — the distinct-flow count withheld), `-04-D1` is three (count / ok / the 10 % twin), `-09-D1` is two (Σ / ok); the "2 staged" are U-1 (A.1 on ratification) and D-3 (Eq. 2 deletion proposal).
9. **Sign-off ids 46 (brief ≈ 7):** G-1 … G-10, C-1, U-1, D-1 … D-25 (D-1 checklist, D-2 g, D-3 Eq. 2, D-4 … D-25 the 22 amendment-K pairs), J-1 … J-4, F-1 / F-2, O-1, I-1, X-1. The brief's `iso5667_6-U-1` / `D-1` / `D-2` / `F-1` / `G-1` / `G-2` / `G-3` / `X-1` ids kept.
10. **Edition token `'2015'`** (the transcript's printed edition — NCh-ISO 5667/6:2015, "Primera edición 2015.02.24"), while prod's `version` starts "2014 (ISO 5667-6:2014 …)": the convention lifts the title page of the TRANSCRIPT; both strings are pinned in the seed test.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (23/23); every seeded cell fragment is asserted inside its row span at build time (whitespace-collapsed); every `Q` span, `verification_quote` / `override_quote` fragment (136/136) and every `"…" (L…)` fragment on the sheet and in the STAGED file (122/122) was machine-checked against the transcript after being read at the cited lines in this session; the spans, the sheet section and the STAGED file were generated from line ranges (never retyped); the STAGED gate conditions are the CAPTURED strings wrapped in the IF guard; prod facts (tokens, consumer lists, section codes, gate / equation ids + md5, labels, units, VR, `is_required`, the 0 / 0 stored-value / instance counts, max CR code, `standards.version`, column lists) come from the in-session capture / queries listed in §0 / §4 and the STAGED header.
- The field-config test pins every refusal of the brief's targets (`gateReaders` per gate; `emitFieldConfigSql` throws per gate / per section chain; `producerChain` for b / c / g / d; the IF-guard exemption that G-6 relies on; the two empty gates never refuse; the two captured equations; the G-A3 facts for every enum column and both created selects; no `id` column and no prod-symbol shadowing; no self-consumer entries; every created field lands in a captured section; no emitted UPDATE writes `enum_values`).
- Runtime facts were probed through the real register / formula / condition contracts before the pins (probe deleted); the render test drives `sampling_points_6` (bridge vs under-ice rows, discriminator-hidden columns, the `depth_ok` badge flipping on a typed 20 cm, the required `kennung`, the PPE column appearing on a bank row, the mixing column appearing on a ticked box) through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file: the three registry lines and the playbook / sheet insertions are marker-checked `split/join` splices that throw on a missing / duplicate marker; the modules, tests and generators were written with the Write / Edit tools (heredocs only for the two scratch checkers); the `sed -i` on the two index example lines used a literal pattern, checked by `grep`.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing computed reaches prod's gates yet** — every gate block (G-1 … G-10) is STAGED; until ratified CR-009 keeps comparing an enum with `''`, CR-011 / CR-015 / CR-021 keep their OR forms (semantically fine, syntactically unguardable), CR-017 stays unconditional, and the four registers are documentation beside the scalars. Fail-safe by construction.
2. **U-1 is the one substantive Annex-A item:** the printed EJEMPLO reproduces ONLY under the 2·√g reading (83,3 / 683,5 m vs 149,7 / 904,8 m for prod's 2·g, with the conventional g) — strong evidence that the prod row A.1 is wrong, but per SR-1 / SR-3 it stays an owner ruling (the ISO 5667-6:2014 page is not on this machine); the STAGED UPDATE applies reading (a) on ratification.
3. **22 amendment-K pairs** — the register model twins most -02 / -03 / -04 / -07 / -09 scalars; each is one ☐ (data-free today). Seven recommend DEFER (gate drivers, consumed fields, the F-1 carrier).
4. **J-1** — the ≥ 30 / ≥ 30 badge is stricter than the location clauses ("alrededor de", mid-depth alternative) and §8.1 ("dentro de 30 cm"); a point sampled at mid-depth of a shallow stream reads 0.
5. **G-2 / J-4** — the brief's `equipment_washes = 0` reading conflates two printed actions; variant B (a `bottles_not_rinsed` attestation) is the printed rule but needs an INSERT-class field the inventory does not name.
6. **Spanish option labels beside German headers** (the §7 location types, the §13.1 items, the determinant datalist, the ANNEXA descriptions) until the owner rules on a German / English source (SR-3; no PDF exists — the one source-absent standard).
7. **Token/time:** ≈ 0.52 M tokens in, ≈ 55 min wall-clock — below the Task 20 / 22 band; the cost sits in the 22 D-pairs and the ten gate blocks, not in the transcript (23 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§5.1.1's deviation record ("Cualquier desviación del punto de muestreo de rutina … registrada con los resultados analíticos, junto con las nuevas coordenadas", L676–L679) — no per-point deviation column; §5.1.2's tidal-state flow / depth measurements (L733–L742) — the boolean `tidal_influence_checked` only; §5.1.4's class-wise heterogeneity testing ("al menos una ubicación de cada clase", L919) — no class column; §5.2's trend-study fixed points / same time-day-month (L986–L988) — the boolean `trend_detection_design` only; §6 a) – e) sampling-folder contents (L1023–L1040) — the boolean `sampling_folder_complete`; §6 vehicle racking (L1053–L1055) — `vehicle_racking_used`; Tabla 1 (bridge upstream / downstream advantages, L1214–L1250) — prose, informs the `bridge_position` labels only; §7.2's shallow-water alternatives (L1254–L1258); §7.5's engine-off / bow sampling (L1335–L1339) — no boat column; §8.2's floating platforms / multi-point inlets (L1427–L1433); §9 equipment (§9.1–§9.5: materials, PTFE, surface-film devices, automatic samplers) — `equipment_inert_tested` / `automatic_sampler_mode` only; §10.1 risk factors (L1578–L1619) — `risk_factors_excluded`; §10.2 arrival checks (L1620–L1633); §10.4–§10.7 the mode-specific procedures — the `sampling_mode` enum only; §10.9 preservative handling (funnel rinse, PPE, L1781–L1785); §10.10 labelling (L1787–L1794) — `sample_labelled`; §11.3.1 courier records (L1837–L1848); §12.1–§12.3 contamination / identification / QC (L1868–L1942) — the -11 booleans; §13.2 protocol change history (L2010–L2019) — `protocol_change_history_kept`; §14 certification (L2032–L2050) — `quality_management_system`; §15's three risk outcomes and the ADVERTENCIA list (L2069–L2104) — the prod enum `risk_assessment_outcome` + `unsafe_condition_present` (CR-030) already carry them; Anexo B (Tablas B.1 / B.2 — the example report form) — a form template (no value; the `report_items_6` checklist covers the §13.1 items it instantiates); Anexo C bibliography / Anexo D editorial changes — nothing to encode. Each is a procedural sentence, an attestation prod already carries, or a template, and stays outside this encoding or on the sign-off sheet.

---

# Fix round 1 (review verdict on `114b8c3`: "Approved with minors" — the four items applied verbatim)

- **Commit:** see the reply (one commit on top of `114b8c3`, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; the field-config migration was regenerated by the Task 0 emitter (one label pair changed), the seed migration re-emitted and byte-identical (the builder header comment is not part of the emitted content). `git stash` NOT used.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling.

1. **O-1 transcript absence claim carries the grep (amendment O).** `grep -n -i "custodia" "<transcript>"` → `1795:` `de custodia, sistemas de calidad y registro, ver 11.3.2 y 12.3.` (exit 0 — the ONE hit, §10.10; its preceding line L1794 reads "de ir al sitio de muestreo siguiente. Para obtener más detalles sobre problemas de trazabilidad, cadena" — a POINTER to 11.3.2 and 12.3). §11.3.2 itself (L1850–L1857) prints "Los reglamentos que se deberían seguir si las muestras se van a utilizar para propósitos legales, pueden ser mucho más complicados dependiendo del sistema legal que funciona en una jurisdicción en particular." (L1852–L1854) and the ATENCION "Cabe destacar la existencia, en algunos países de legislación local, con la que todas las personas implicadas en cualquier etapa de la toma de muestras, almacenamiento o entrega de muestras, o en la documentación asociada, debería" (L1856–L1857) — NO list of chain-of-custody items. Command + output now sit in §3 (below), in the STAGED O-1 block and in the sign-off O-1 block (evidence line L1794–L1795 added; 130/130 sheet / STAGED fragments verbatim after the regeneration).
2. **O-1 capture absence claims as shell commands + exit codes** (replacing the `Object.keys(prior)…` wording): `grep -c chain_of_custody src/lib/eval/field-configs/iso5667_6.prior.json` → `0` (exit 1); `grep -c bottle_contains_preservative src/lib/eval/field-configs/iso5667_6.prior.json` → `0` (exit 1); `grep -c '"extrapolation_pct"' src/lib/eval/field-configs/iso5667_6.prior.json` → `0` (exit 1). (The test pins on the loaded prior stay as an additional guard.)
3. **`depth_ok` badge labels read as an Anhaltswert:** both `value_labels` of the `sampling_points_6.depth_ok` column now end in "(Anhaltswert, iso5667_6-J-1)" (`src/lib/eval/field-configs/iso5667_6.ts`); `20260917102310` + rollback re-emitted (`wrote 25 field entries + 0 section entries`, freshness pin green); the render test's two label assertions updated (`register-iso5667-6-points.test.tsx`).
4. **U-2 for S13_1 item i):** the builder header (`regulation-tables-seed-iso5667_6.ts`) lists `iso5667_6-U-2` beside U-1 (the two printed fragments L1979 / L1985 around the furniture line L1984, the seeded `text` is their JOIN, each fragment asserted inside the span); a matching `iso5667_6-U-2` block (class unreadable-cell, no correction, evidence = both fragments with their lines + the furniture line) is in the STAGED file and on the sign-off sheet (47 blocks now; the sheet section was REPLACED by the generator, one `## Task 23` heading). The seed migration was re-emitted: byte-identical (`git status` shows no seed / rollback change).

Amendment-O additions to §3 (re-executable, raw output):
- O-1 (transcript) — `grep -n -i "custodia" "<transcript>"` → `1795:                                                                                     de custodia, sistemas de calidad y registro, ver 11.3.2 y 12.3.` (exit 0); L1794 (the line before, read in full): "de ir al sitio de muestreo siguiente. Para obtener más detalles sobre problemas de trazabilidad, cadena"; §11.3.2 L1852–L1854 / L1856–L1857 as quoted in item 1 — no printed list.
- O-1 (capture) — `grep -c chain_of_custody src/lib/eval/field-configs/iso5667_6.prior.json` → `0` (exit 1); `grep -c bottle_contains_preservative …prior.json` → `0` (exit 1); `grep -c '"extrapolation_pct"' …prior.json` → `0` (exit 1).

Counts after the round: 6 tables / 23 rows, 25 field entries (19 create / 6 UPDATE), 10 equations, **47** sign-off blocks (+7 observations), 47 STAGED ☐ blocks (568 lines, 0 non-comment lines).

## Raw output (once, before the commit)

```
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso5667_6 20260917102310 → wrote 25 field entries + 0 section entries for iso5667_6 (19 INSERT / 6 UPDATE; 0 warnings)
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso5667_6 → wrote seed + rollback for iso5667_6 (byte-identical — not in git status)
pnpm vitest run --project unit <seed-iso5667-6, field-configs-iso5667-6, equations-iso5667-6, register-iso5667-6-points, generated-sql-freshness, regulation-tables-seed-index> → Test Files 6 passed (6) · Tests 32 passed (32) · Duration 2.95s
verify-regulation-tables.ts iso5667_6 "<transcript>" → 23/23 quotes verbatim · check-iso5667_6-q.ts → 136/136 · check-iso5667_6-frags.mjs → 130/130 (47 blocks)
pnpm test → Test Files 338 passed | 1 skipped (339) · Tests 3032 passed | 1 expected fail | 1 skipped (3034) · Duration 56.19s (the label change and the header comment changed no test count)
pnpm -s typecheck → exit 0 · pnpm -s eslint <3 touched files> → exit 0, no output
```
