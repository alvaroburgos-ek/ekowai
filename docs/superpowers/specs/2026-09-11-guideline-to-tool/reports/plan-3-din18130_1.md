# Plan 3 — DIN-18130-1 (`din18130_1`) — Task 10 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 11)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `0197bab`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DIN-18130-1 din18130_1` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 5 worksheet titles, the 22 section codes / titles, the 8 equation rows with ids / formulas / md5 / input_symbols / status, the 7 compliance rows with ids / severities / conditions / md5, the field labels / units / `is_required` / `validation_rules`, the `information_schema` column lists of `equations` / `compliance_requirements` / `fields`). `.env.local` never printed; confirmed gitignored.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-18130-1\DIN-18130-1.md` (1359 lines; the transcript was read end-to-end at L1–L1359). The inventory was used for pointers only (several of its line pointers were off by 1–4 lines — e.g. Tab. 1 rows are L205–L209, Tab. 3 L427–L429, Tab. 5 rows L540–L551, Gl. 8 L787; every line cited below was re-read). The OCR sibling `DIN-18130-1_OCR.md` was opened once (its L610–L660) as a LEAD for the Tab.-5 Bauteil assignment and the edition header — cited in U-1 / I-1, never used as a source. The PDF was not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `din18130_1` | 7 | 36 (36/36 verbatim) | 3 Tab.-5 sub-rows NOT seeded (multirow Bauteil spans — U-1) + lower-case "x" marks read as X (U-2) → TAB5 `imported_unverified`; 6 tables `md_verified` | 2 (`create`: `ablesungen` -03, `versuche` -04) | 4 (all `create`: `bindig_grobkoernig`, `bodenart_tab5`, `messung_gefaelle`, `s_r_band`) | 1 (`create`: `pflichtangaben`, 14 lifted items in 2 printed groups) | 8 (2 UPDATE re-binds `A_min` / `u_0` + 6 `create`: `groesstkorn_verhaeltnis`, 4 × `tab5_*`, `versuchsklasse_tab4`) | 4 (3 on created fields + 1 UPDATE `i_bereich`, pending until C-1) | 0 (-04 G holds the consumed `k`, -04 H has no field — C-2) | 8 (-03 D1…D5, -04 D1…D3) | 3 replacements (R-1 k switch, R-2 Gl. 6 ← k_T_mean, R-3 k_f = k_10) + D-1 alpha retirement + 11 gates (G-1…G-11) | 34 (+4 observations) | ≈ 0.57 M (tool-marker delta 15.00 M → 14.43 M at report time) | ≈ 85 |

Field entries: 27 = 24 `create` + 3 UPDATE (`A_min` → lookup_fill limit, `u_0` → lookup_fill value, `i_bereich` visible_when). Migration `20260917101010`: 24 `INSERT … WHERE NOT EXISTS`, 3 `UPDATE fields`, 0 `UPDATE worksheet_sections`; rollback restores the three UPDATE rows to the captured NULLs and deletes only the 24 `Plan 3:` rows. Derived-output fields (`widget='derived'`): 8 — one per equation.

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  282 passed | 1 skipped (283)
      Tests  2646 passed | 1 expected fail | 1 skipped (2648)
   Start at  03:38:19
   Duration  43.01s (transform 13.27s, setup 46.40s, import 98.92s, tests 41.39s, environment 180.54s)
```
(2619 at `0197bab` + 27 from this task: seed-din18130-1 8, field-configs-din18130-1 7, equations-din18130-1 9, register-din18130-ablesungen 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → first run: 3 errors in `equations-din18130-1.test.ts` (my `num()` helper typed `number | null` while three calls pass the enum string `'veraenderlich'` / `'konstant'`) → helper widened to `number | string | null` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output (run twice: after the modules, and on the final working tree).

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/din18130-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  17 passed (17)
   Start at  03:39:49
   Duration  4.93s (transform 579ms, setup 0ms, import 3.68s, tests 1.08s, environment 0ms)
```
The harness seeds prod's 8 equations / 7 gates (`tests/harness/seed-din18130-1.ts`); the new rows are not in it by construction — it proves the branch still drives every DIN-18130-1 chain and gate both ways where drivable.

**TDD evidence.** The expression parser was probed BEFORE encoding (scratch `_probe.ts`, deleted): `1e-8` literals parse and `k = 1e-6` bands to 2 (inclusive), `T^2` / `ln()` / `floor(log10())` evaluate, the row-scope `if(gefaelle_typ == 'konstant', …)` parses. Seed test written after the builder → **8/8 GREEN first run**; verifier **36/36** on its first run (every cell asserted inside its span at build time by `inSpan`). Field-config test: one RED on my own arithmetic (27 entries, not 28) and the not-yet-emitted migration → 7/7 after the emit (the emitter guards accepted all 27 entries first time; the two refusals the test pins — `h_0 → Gl.7 h` and section G — were asserted, not discovered). Equations test: one RED on a precision pin (Tab. 9 Versuch 2 computes 2,6924·10⁻⁴ from the printed inputs, the table prints 2,693·10⁻⁴ — pinned to two decimals) → 9/9. Render test: one RED on `act` imported from vitest instead of testing-library, one on the 4-digit formatter (`32,9975` vs my `33` pin) → 3/3.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts din18130_1 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-18130-1\DIN-18130-1.md"` → **`36/36 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- TAB1 (5): 1 … 5 — L205–L209
- TAB2 (5): 5 / 10 / 15 / 20 / 25 — one two-line span L321–L322 (transposed table, the A138 TAB14 pattern)
- TAB3 (3): ge095 (L427), e090 (L428), e085 (L429)
- TAB4 (4): true|true (L480), true|false (L481), false|true (L482), false|false (L483); footnote *) L484 in `fussnote`
- TAB5 (15): ton_schluff|ZY (L540); feinsand|ZY, feinsand|KD (L543); feinsand|TX (L544); mittel_grobsand|ZY/KD/TX (L545); sand_kies|ZY/KD/TX (L546); sand_ton|ZY (L547); sand_ton|KD (L548); kies_sand_ton|ZY, kies_sand_ton|KD (L550); kies_sand_ton|TX (L551)
- S5_8 (2): bindig, grobkoernig (L336) · S5_8_KORN (2): true, false (L335)

Status per table: `md_verified` — TAB1, TAB2, TAB3, TAB4, S5_8, S5_8_KORN (every printed row lifted, every cell legible). `imported_unverified` — TAB5 (U-1: the KD / TX marks of the Ton/Schluff and Sand-Ton groups are `\multirow{3}` / `\multirow[b]{3}` spans on the first line — three sub-rows not seeded; U-2: lower-case "x").

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DIN-18130-1 din18130_1
  → wrote src\lib\eval\field-configs\din18130_1.prior.json: 54 field rows (0 orphan), 22 coded sections of 22, 8 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts din18130_1
  → wrote seed + rollback for din18130_1 -> scripts/migrations/20260917101000_regulation_tables_seed_din18130_1.sql scripts/rollback-20260917101000-regulation-tables-seed-din18130-1.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts din18130_1 20260917101010
  → wrote 27 field entries + 0 section entries for din18130_1 -> scripts/migrations/20260917101010_field_configs_din18130_1.sql scripts/rollback-20260917101010-field-configs-din18130-1.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts din18130_1 20260917101020
  → wrote 8 equations for din18130_1 -> scripts/migrations/20260917101020_equations_din18130_1.sql scripts/rollback-20260917101020-equations-din18130-1.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-din18130-1.test.ts` / `equations-din18130-1.test.ts` pin the other two against the committed `din18130_1.prior.json`). The seed migration carries 36 row upserts and 6 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the seven tables (no earlier DIN-18130-1 seed exists — nothing superseded). The equations rollback deletes the 8 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101000` → `20260917101010` → `20260917101020`; rollback in reverse. Every file touches `DIN-18130-1` rows only. Scalar-only rows (`alpha_calc`, `k_10_calc`, `bereich_code`) are not server-materialised (amendment D — expected, noted once here and as I-3); `k_T_mean` / `i_max_calc` / `i_min_calc` / `k_10_runs_mean` / `versuche_count` are register-fed and materialise on save.

## 5. What was encoded

**Tables (7)** — `src/lib/eval/regulation-tables-seed-din18130_1.ts`, builder `din181301SeedTables()`, edition `'1998-05'` (prod `standards.version` "1998-05 (Ersatz für 1989-11)"; the title page prints "Ersatz für Ausgabe 1989-11" L7 and the earlier editions L116 but no own date — I-1):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB1 | `bereich_code` '1'…'5' (caption order) | `bereich` (string), `k_lower`, `k_upper` [m/s] (nullable; bounds as printed) | locked (L196 "Für bautechnische Zwecke werden fünf Durchlässigkeitsbereiche definiert") | 5 | md_verified |
| TAB2 | `t_c` '5'…'25' | `alpha` | anhaltswert (L327 "Zwischenwerte können geradlinig eingeschaltet werden.") — display / pin table; Gl. 6 computes | 5 | md_verified |
| TAB3 | `s_r_band` ge095 / e090 / e085 (created select) | `u_0_kn_m2` 300 / 600 / 900 | anhaltswert (L1205 "S_ra = 0,88" — L1215 "u_o = 720 kN/m²": the standard's own example between the rows; O-1, brief said locked) | 3 | md_verified |
| TAB4 | `saettigung` × `stationaer` = stringified prod booleans 'true'/'false' | `versuchsklasse` (prod tokens 1a/1b/2/3), `fussnote` (L484 for 1b) | locked (L447) | 4 | md_verified |
| TAB5 | `bodenart` (6 created-select tokens) × `anordnung` (= prod `versuchsanordnung` KD/ZY/TX) | `erreichbare_klasse` (string, "2 (1)" as printed), `geeignet` / `ms` / `es` / `de` / `mz` / `st` / `kp` / `sb` / `u0` (enum of the three printed legend states) | anhaltswert (L553–L555 legend — L446; O-2) | 15 of 18 combinations (3 under U-1; "-" cells seeded as explicit `nicht geeignet` rows) | imported_unverified (U-1 / U-2) |
| S5_8 | `bodenklasse` bindig / grobkoernig (created select) | `a_min_cm2` 10 / 20 | anhaltswert (L336 "sollte … mindestens … betragen") | 2 | md_verified |
| S5_8_KORN | `ungleichfoermig` = stringified prod boolean | `verhaeltnis` "1 : 5" / "1 : 10" | anhaltswert (L335 "sollte … nicht unterschreiten") | 2 | md_verified |

**Registers / selections / lookup_fill / visibility** — `src/lib/eval/field-configs/din18130_1.ts`:
- `ablesungen` (-03 C, create json): `nr`, `t_s`, konstant columns `h_o`/`h_u`/`p_o`/`p_u`/`v_w` (row `visible_when gefaelle_typ == 'konstant'`, required), veränderlich columns `h_1`/`h_2` (`== 'veraenderlich'`), `t_c`; derived `h_row = h_o - h_u + (p_o - p_u) / gamma_w` (Tab. 11 L1326), `i_row` (Gl. 3 form / h_1/l_0), `k_row = if(konstant, v_w·l/(A·h_row·t_s), a·l_0/(A·t_s)·ln(h_1/h_2))` (Gl. 8 with Q = V_w/t L787 / Gl. 9 L813), `alpha_row` (Gl. 6 closed form on the row's T), `k10_row`, plus the L850 mantissa / exponent pairs (I-2); footer `k_T_mean`, `k_10_calc`, `i_max_calc`, `i_min_calc`; note L526. The worksheet scalars `gefaelle_typ` (inherited from -02), `gamma_w`, `l`, `l_0`, `A`, `a` are read in row scope (G-13).
- `versuche` (-04 I, create json): `nr`, `sigma_kn_m2`, `e`, derived `n_pore_row = e / (1 + e)` (F-3), `rho_d`, `s_ra`, `s_re`, `k_10_run` (required); footer `k_10_runs_mean`, `versuche_count`; note L246 (+ F-1: nested registers not built).
- Selects (all `create`): `bindig_grobkoernig` (-01 K; L336 labels) · `bodenart_tab5` (-02 E; the six Tab.-5 row heads) · `messung_gefaelle` (-02 C; ms / es / de = the Tab.-5 column triple L539, §4 code letters L222 / L593 / L688) · `s_r_band` (-02 D; the three Tab.-3 row heads, `visible_when saettigung_aufgebracht == true`, L417). `stroemung_stationaer` (-02 D, create boolean, attestation; Tab. 4 column head L479 + footnote L484 in the description). `pflichtangaben` (-05 K, create json, select_many; 14 lifted §8.4 items L834–L848 in the two printed groups).
- lookup_fill: `A_min` (-01, UPDATE, role limit) ← S5_8 by `bindig_grobkoernig` · `groesstkorn_verhaeltnis` (-01 K, create text) ← S5_8_KORN by the prod boolean `ungleichfoermig` · `tab5_erreichbare_klasse` / `tab5_geeignet` / `tab5_sb` / `tab5_u0` (-02 E, create text) ← TAB5 by (`bodenart_tab5`, `versuchsanordnung`) · `versuchsklasse_tab4` (-02 D, create text) ← TAB4 by (`saettigung_aufgebracht`, `stroemung_stationaer`) · `u_0` (-02, UPDATE, role value) ← TAB3 by `s_r_band`.
- Created inputs with visibility: `filterstein_k` (-02 E, m/s) ← `versuchsanordnung == 'TX'` (L642 / L668); `probe_durchmesser_mm` (-02 E) ← `== 'KD'` (L456).
- Field `visible_when` on an existing field: `i_bereich` (-05) ← `gefaelle_typ == 'veraenderlich'` (L832) — `pending` (visible, inert) until C-1. Refused and STAGED (C-2): every -03 measurement scalar (consumed by -04), `h_0` / `gamma_org` (chain `h_0 → Gl.7 h (consumed by DIN-18130-1-04)`), `statische_belastung` / `u_0` (consumed by -05), sections -04 G (holds `k`) / H (no field). Section rules: none.

**Equations (8 new)** — `src/lib/eval/equations/din18130_1.ts`: `-03-D1 k_T_mean = mean_rows(ablesungen, k_row)` · `-03-D2 alpha_calc = 1.359 / (1 + 0.0337 * T + 0.00022 * T^2)` (Gl. 6 L305) · `-03-D3 k_10_calc = k_T_mean * alpha_calc` · `-03-D4 i_max_calc = max_rows(ablesungen, if(gefaelle_typ == 'konstant', h_row / l, h_1 / l_0))` · `-03-D5 i_min_calc = min_rows(…, h_2 / l_0)` · `-04-D1 k_10_runs_mean = mean_rows(versuche, k_10_run)` · `-04-D2 versuche_count = count_rows(versuche)` · `-04-D3 bereich_code = if(k_10 < 1e-8, 1, if(k_10 <= 1e-6, 2, if(k_10 <= 1e-4, 3, if(k_10 <= 1e-2, 4, 5))))` (Tab. 1 bounds L205–L209). No prod symbol is re-produced (Q, v, i, k, k_10, h keep their verified rows). **Pinned against the standard's own examples** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): Tab. 11 (§9.4) rows → h = 2,321 / 2,312 / 2,020 m, i = 46,4 / 46,2 / 40,4, k_T = 4,8 / 4,5 / 4,4·10⁻¹⁰, α(20,5) = 0,762, k_10 per row 3,66 / 3,43 / 3,35·10⁻¹⁰, k_T_mean · α = **3,48·10⁻¹⁰** (L1357); Tab. 9 (§9.2) → k = 2,745·10⁻⁴ (L1088), k_10 = 2,12·10⁻⁴ at 20 °C (L1089); Tab. 7 (§9.1) → ln(0,655/0,503) = 0,2640 (L999), max. i = 33 / min. i = 25,3 (L918 "max. i = 33; min. i = 25 bzw. 27"); Tab. 10 (§9.3) → mean 3,77·10⁻⁹ (L1133), α(21,25) = 0,7485 (L1120); Tab. 2 → 1,158 / 1,000 / 0,874 / 0,771 / 0,686; the four printed (n, e) pairs satisfy e/(1+e); Tab. 1 bands on the printed inclusive bounds (1e-8 → 2, 1e-6 → 2, 1e-4 → 3, 1e-2 → 4).

## 6. Inventory §5 top-wins walk

1. Readings as a repeatable table with per-row k and a mean k_T / k_10 → **encoded** (`ablesungen`, `k_T_mean`, `k_10_calc`, per-row α / k_10; Tab. 11 reproduced); replacing the single-reading scalars and re-pointing Gl. 6 is STAGED (R-2 / D-3).
2. Derive `alpha`, `k_T`, `k_10`, `durchlaessigkeitsbereich` and `bezeichnung` → **encoded as twins** for α (`alpha_calc`), k_T (`k_T_mean`), k_10 (`k_10_calc`), class (`bereich_code`); the hand inputs stay until D-1 / D-2 / R-2; `bezeichnung` is F-2 (no string assembly in the language).
3. `bodenart` as a Tab.-5 enum that filters arrangement / measurement / SB / u_0 and shows the achievable class → **partially**: `bodenart_tab5` + four fills (class, suitability, SB, U0) for 15 of 18 combinations; Ton/Schluff KD + TX and Sand-Ton TX wait for the PDF read (U-1); the Gefälle / Volumen method suitability is seeded (ms/es/de/mz/st/kp columns) but not exposed as fills (would need three keys — residue).
4. `gefaelle_typ` switches the visible measurement fields and the k equation; `i_bereich` only for veränderlich → **encoded in the register** (row-scope column rules + the per-row switch) and `i_max_calc` / `i_min_calc`; the scalar-level switch (R-1), the scalar hiding (C-2) and the i_bereich requirement (G-9, needs C-1) are STAGED; the `i_bereich` rule is emitted but pending.
5. Fill `u_0` from S_r (Tab. 3) and derive `versuchsklasse` from saturation + steady-flow flags (Tab. 4) → **encoded**: `s_r_band` (SR-2 select) → `u_0` fill (anhaltswert, O-1); `stroemung_stationaer` + `versuchsklasse_tab4` fill on -02; the re-point of the -01 enum and CR-03 is STAGED (E-1), an interim consistency gate G-10.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`din18130_1` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (din18130_1 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+34 blocks, +4 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 10)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-din18130_1.ts`, `src/lib/eval/field-configs/din18130_1.ts`, `src/lib/eval/field-configs/din18130_1.prior.json` (captured), `src/lib/eval/equations/din18130_1.ts`, `src/lib/eval/__tests__/regulation-tables-seed-din18130-1.test.ts`, `src/lib/eval/__tests__/field-configs-din18130-1.test.ts`, `src/lib/eval/__tests__/equations-din18130-1.test.ts`, `src/components/worksheet/__tests__/register-din18130-ablesungen.test.tsx`, `scripts/verification/din18130_1-STAGED-plan3-rulings.sql` (34 blocks, every line a comment), `scripts/migrations/20260917101000_regulation_tables_seed_din18130_1.sql` + `scripts/rollback-20260917101000-regulation-tables-seed-din18130-1.sql`, `scripts/migrations/20260917101010_field_configs_din18130_1.sql` + `scripts/rollback-20260917101010-field-configs-din18130-1.sql`, `scripts/migrations/20260917101020_equations_din18130_1.sql` + `scripts/rollback-20260917101020-equations-din18130-1.sql`, this report (+ the copy at `.superpowers/sdd/…/task-10-report.md`).
Untouched: every other standard's file; the DIN-18130-1 harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Placement:** `bodenart_tab5` and the Tab.-5 fills sit on -02 (the brief: -01) — a created field never carries `consumer_worksheets`, so a fill keyed on it must sit on its worksheet (a262e trap 1); the Tab.-4 fill is a created twin `versuchsklasse_tab4` on -02 (the -01 `versuchsklasse` keeps its input, E-1); `bereich_code` is `DIN-18130-1-04-D3` on -04 where `k_10` lives (the brief's `-01-D1` could never compute: k_10 is not consumed by -01); all register-fed rows sit on the register's worksheet (-03 D1…D5 instead of the brief's -04 D1…D3 for k_T / α / k_10).
2. **Equations 8 new (brief 6 + 2 staged):** `i_max_calc` / `i_min_calc` (the printed §8.4 range, §9.1 "max. i = 33; min. i = 25") and `versuche_count` added; the brief's `-05-D1 pflichtangaben_count` is not an equation (a select_many is not a register) → G-4 uses `contains()`.
3. **TAB5 keyed with prod's upper-case `KD`/`ZY`/`TX`** (capture), not the brief's kd/zy/tx; TAB1 keys `'1'…'5'` (the stringified equation output) instead of `b1…b5`; TAB4 / S5_8_KORN keys are the stringified booleans as the brief's G-15 note foresaw.
4. **TAB5 value columns are the three printed legend states** as enum strings (geeignet / bedingt geeignet / nicht geeignet) per column, and the printed "-" Bauteil cells are seeded as explicit `nicht geeignet` rows (so a fill distinguishes "not suitable" from "no row"); 15 rows, not "≈ 10".
5. **TAB3 policy anhaltswert, not locked** (O-1) — the standard's own example applies 720 kN/m² between the rows; the fill's override affordance is the only way to reproduce it.
6. **`u_0` and `A_min` re-bound as lookup_fill UPDATEs** (their keys are same-worksheet created selects); `versuchsklasse` NOT re-bound (keys on another worksheet).
7. **Field `visible_when`: 4 (brief 8)** — every existing -03 scalar is consumed (C-2); the KD/TX-specific inputs were CREATED (`filterstein_k`, `probe_durchmesser_mm`; the brief's `probe_d_min` became a diameter input, the height is the existing `l_0` → G-7); `statische_belastung` is a gate (G-8), not visibility; `alpha` stays visible (D-1). Section rules 0 (G refused, H field-less).
8. **`versuche` columns:** `s_ra` / `s_re` (the §8.4 pair) and `sigma_kn_m2` instead of the brief's single `s_r`; `n_pore` derived from `e` (F-3).
9. **Mantissa / exponent columns** added to the readings register (the Plan-2b register formatter prints |k| < 5·10⁻⁵ as "0" — I-2; the standard's L850 asks for exactly this representation).
10. **Render test file name** `register-din18130-ablesungen.test.tsx` (brief) kept.
11. **Sign-off ids 34 (brief ≈ 8):** the brief's ids kept (R-1, D-1, G-1…G-4, J-1, D-2, U-1, F-1, F-2, X-1); extra items use the skeleton letters (R-2/R-3, D-3, E-1, C-1/C-2, G-5…G-11, U-2, J-2/J-3, F-3, I-1…I-3, O-1/O-2).

## 9. Self-review

- Every `verbatim_quote` passes the verifier (36/36); every seeded cell is asserted inside its row's span at build time (`inSpan` — a slip throws before any migration is emitted); every cue in `field-configs/din18130_1.ts`, `equations/din18130_1.ts`, the STAGED file and the 34 sign-off blocks was read from the transcript in this session at the cited line (the line numbers were re-derived with `grep -n` / `sed -n` after the inventory's pointers proved off by 1–4 lines; the citations in the sheet / STAGED file were corrected in a second pass: L186, L216, L298, L302, L387, L395, L782, L878/L879, L999, L1026–L1027, L1180/L1185).
- The emitter guards accepted all 27 entries first time because the rule set was planned against the capture (consumer lists, the Gl.-7 chain, section codes) before emitting; the field-config test pins the refusals (`h_0 → Gl.7 h`, section G with `k`), the field-free section H, every -03 scalar's consumer list, and that every lookup_fill's keys are symbols on the fill's own worksheet.
- G-A3 pinned: TAB5.anordnung = prod `versuchsanordnung` tokens; TAB5.bodenart / TAB3.s_r_band / S5_8.bodenklasse = the created selects' options; TAB4 / S5_8_KORN keys = stringified booleans over captured boolean fields; the select_many options = the 14 lifted items; TAB1's five names = prod's `durchlaessigkeitsbereich` labels in order.
- The runtime facts were established through the real register contract: a hidden column is not required (the veränderlich rows are complete without h_o…v_w), a konstant-only derived cell in a veränderlich row is null (never a number), an incomplete row does not enter `mean_rows`, an empty register is `manual_required` for the means and 0 for `count_rows`, the row key `e` shadows Euler (null cell → null, never 2,718).
- Prod facts used in the STAGED file (equation ids + md5, gate ids + md5, column lists, `standards.version`, VR strings) come from the in-session read-only queries quoted in §0; no long cell was retyped (archive pattern).
- No `String.replace` with a replacement string on any repo file (splices via `split/join` in `node -e` or the Edit tool; the Bash heredoc collapsed `\\` once on a seed-file splice — caught by the failed `includes()` guard, redone with the Edit tool); the scratch scripts live in the scratchpad; `_probe.ts` / `_probe2.ts` were deleted.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns three files there); `.env.local` confirmed gitignored; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing replaces the hand-typed chain yet** — `k_T` / `k_10` / `alpha` / `k_f` / `durchlaessigkeitsbereich` keep their inputs; the register outputs are visible twins until R-1 / R-2 / R-3 / D-1 / D-2 / D-3 are ratified (three prod equations write `k`; Gl. 6 demands the unused `alpha`).
2. **TAB5 is three rows short** (U-1) — the classic KD test on Ton/Schluff (§7.1) has no Tab.-5 row until the owner reads p. 10; the fills read "keine Zeile" for it. A 2-minute PDF look adds three `TAB5_LINES` entries.
3. **`u_0` re-bound as a lookup_fill** — while `s_r_band` is unset (or hidden because no Sättigungsdruck is applied) the field is read-only; an engineer who wants to record "u_0 = 0" (as §9.1 / §9.2 print) cannot type it. If the owner prefers the input, the alternative is a created fill twin beside the band (one-entry change, re-emit).
4. **The register formatter prints |k| < 5·10⁻⁵ as "0"** (I-2) — the mantissa / exponent columns cover the rows, but the footer values `k_T_mean` / `k_10_calc` render "0" in the register footer (the equation cards are correct). A one-line Plan-2b `fmt()` amendment (scientific notation below 1e-3) fixes both; not done inside this DATA task.
5. **`i_max_calc` / `i_min_calc` need `gefaelle_typ`, `l` AND `l_0`** — all required prod inputs, so no practical gap, but a konstant test must still carry `l_0` (and vice versa) because the engine checks named inputs before evaluating.
6. **Bundle growth:** ~12 KB of lifted quotes (Task-0 observation; Task 30 measures).
7. **Token/time:** ≈ 0.57 M tokens in, ≈ 85 min wall-clock — above the Task 9 figure (0.55 M / 50 min) because this standard needed the worked-example arithmetic (four printed examples reproduced through the register contract) and the Tab.-5 cell-by-cell mapping.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The Gefälle / Volumen measurement-method suitability per Tab.-5 row (seeded, not exposed — three-key fills); the KD Standrohr diameter rule (L467 "d = 4 mm bei k < 10⁻⁹ m/s") and the KD filter-stone absolute bound (L467 "k > 10⁻⁶ m/s" — named in G-6); the ZY §7.4 "k ≥ 1·10⁻⁷ m/s ⇒ Meßzylinder allowed, inflow from the Bürette governs" rule (L772); the TX sample sizes (L635 "A ≥ 10 cm² bzw. d ≥ 5 × max. Korndurchmesser"); §5.5 "Sättigungszahl darf sich nicht ändern" as a S_ra vs S_re check; the Bild-2 non-linear regimes (Gl. 5 k_r = f(i), L288 — a per-gradient series); the regression fit of Tab. 7 (L934 "lineare Ausgleichungsrechnung" — F-4 class); the Einspannung alternative to Auflast (L379); the §4 code assembly (F-2). Each is on the sign-off sheet with its line or listed here.
