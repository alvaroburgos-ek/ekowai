# Plan 3 — DIN-14021 (`din14021`) — Task 25 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 26)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `e9b4619`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write; `git stash` not used. Prod was read ONLY by (a) `node scripts/verification/prod-query.mjs --sql "…"` (the `standards` row for `%14021%` → code `DIN-14021`, version `2016 (ISO 14021:2016); EN ISO 14021:2016`; the 29 non-null `validation_rules` raw strings + `default_value` of the standard's fields; `max(code)` / `count(*)` of its compliance rows → REQ-50 / 50), (b) `node scripts/regulation-tables/build-prior-snapshot.mjs DIN-14021 din14021` (read-only tx) and (c) `node scripts/verification/capture-text.mjs DIN-14021 din14021` (read-only tx, full cells → `din14021.text.prior.json`: gate ids / md5 / conditions / titles / descriptions, equation ids / md5 / status, worksheet ids, the 0 stored-parameter / 0 instance counts). `.env.local` never printed (variable NAMES listed once with `grep -oE '^[A-Z_]+='`).
- **SR-1 source:** every seeded value and every cue was read in this session from the bilingual (DE / EN) markdown transcript `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-14021\DIN-EN-ISO-14021.md` (2122 lines; DIN EN ISO 14021:2016 — markdown = the SR-1 source per owner ruling 2026-09-05, **VC grade**; the GERMAN column is the primary span, the English twin sits under each German paragraph). Read in full lines (`sed -n 'A,Bp'`, never `cut` on a quoted line): title page L1–L20, Änderungen L93–L103, TOC L172–L381, §5 L753–L912, §6 L931–L1055, §7.1 L1056–L1076, §7.2–§7.17 L1097–L1899 (every German Voraussetzungen / Bewertungsverfahren sub-clause, the three printed formulas L1300–L1303 / L1480 / L1570 with their legends). The 100 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-din14021-quotes.mjs`, each line trimmed / empty lines dropped, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-din14021.ts`, never hand-edited); every seeded cell is asserted inside its span at build time (`inSpan`, whitespace-collapsed); the seed test additionally pins that no multi-line span carries an English line. The inventory and the reasoning map `tab-14021-master-per-type.md` were used for pointers / structure only (the map's claim-type list = the prod enum; every clause sentence quoted from the transcript).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `din14021` | 3 (CLAIMMAP, S6_5_3, S5_3_5_10) | 38 (38/38 verbatim) | 0 — **every table `md_verified`** (markdown transcript, every row quote lifted, every displayed cell legible) | 1 (`create`: `claims` -01) + 2 `select_many` checklists (`general_requirements_items` -03, `documentation_items` -04) | 0 (the 23-token `selected_claim_type` stays as prod's — CLAIMMAP keys = its tokens) | 2 (created json: 8 = S5_3_5_10 headings; 7 = S6_5_3 a) – g)) | 0 (no numeric table — the classification is a register `lookup_key` / `lookup_value` pair) | **1** rule-bearing entry, on the CREATED `unqualified_claim` (-05 ← `selected_claim_type IN {…}`); **0 UPDATEs on existing fields** (every brief target refused by the guards → G-19 / G-22 … G-28 / C-2 / C-3); plus 17 row-scope rules inside `claims` | 0 | **3** (-01 D1–D3, register-fed) | 0 replacements (EQ-01 … EQ-03 untouched); the brief's -06 D2 / D3 / D4 withheld (F-1 [CODE]); 18 + 2 + 4 + 1 + 3 + 3 gate blocks (G-1 … G-28, incl. 4 NEW gates REQ-51 … REQ-54), 4 C-blocks, 36 amendment-K pairs | **76** blocks (+6 observations) | ≈ 0.62 M (tool-marker delta 15.00 M → 14.38 M at report time) | ≈ 45 |

Field entries: 7 = **7 `create`** (1 register carrier `claims`, 3 derived outputs `claims_count` / `claims_type_fail` / `specific_requirements_met_code` on -01, 2 json checklists, 1 boolean attestation `unqualified_claim` on -05) + **0 UPDATE**. Migration `20260917102510`: 7 `INSERT … WHERE NOT EXISTS`, 0 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes the 7 `Plan 3:` rows. No `enum_values` is written by any UPDATE (D-1 — there is none). The emitters printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — pinned: no captured field lists its own worksheet; 0 unparseable gates); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  345 passed | 1 skipped (346)
      Tests  3068 passed | 1 expected fail | 1 skipped (3070)
   Start at  22:40:26
   Duration  57.38s (transform 18.15s, setup 58.05s, import 146.86s, tests 61.55s, environment 229.22s)
```
(3050 at `e9b4619` + 18 from this task: seed-din14021 5, field-configs-din14021 6, equations-din14021 5, register-din14021-claims 2; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

(After the full run two D-block cross-references in the module note strings were corrected — D-26 … D-35 / D-36 — and the field-config migration re-emitted; the six pin files re-run: `Test Files 6 passed (6) · Tests 27 passed (27)` at 22:45:54.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`, first run). `pnpm -s eslint <11 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task; run at HEAD `e9b4619` BEFORE any of this task's files existed — 52/52 — and again after):
```
pnpm vitest run --project integration tests/harness/din14021-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  52 passed (52)
   Start at  22:41:51
   Duration  7.36s (transform 902ms, setup 0ms, import 4.75s, tests 2.44s, environment 0ms)
```
The harness was NOT edited (no stale pin; baseline and post-change runs identical). It seeds prod's 5 gate-hosting worksheets, the 50 BLOCK gates and the 3 equations (`tests/harness/seed-din14021.ts`), so the new rows are not in it by construction; it proves the branch still drives every DIN-14021 block gate both ways through the real save path (incl. the REQ-11 cross-worksheet fallback). This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch probe through the real `prepareRegisterRows` / `evaluateFormula` / `parseCondition` / `evaluateCondition` / `emitFieldConfigSql` / `gateReaders` / `producerChain` on the seeded CLAIMMAP (`scripts/_probe_din14021.ts`, deleted): a `lookup_value` column refills `numeric_block` / `condition_text` / `comparative_by_clause` on key pick; row-scope `if()` SHORT-CIRCUITS over null cells of other blocks (a compostable row with `e` stored under the hidden column keeps the cell — a262e trap 2 — and reads `recovered_ok` 1); a derived column may read an earlier derived column (declaration order — `type_ok` over the four badges); a boolean `lookup_value` compares `== true` (`comparative_required` true + `comparative` unticked ⇒ `comparative_ok` 0); `count_rows(claims, type_ok == 0)` counts 5 of 11 complete rows and an EMPTY register reads count 0 / fails 0 / code 0 (computed, never manual, never a phantom pass); `if(count_rows(claims) > 0 AND count_rows(claims, type_ok == 0) == 0, 1, 0)` parses (no `AND (`); the visibility DSL `selected_claim_type IN {'renewable_material', …}` parses and reads pass / fail / pending; every one of the brief's 18 Step-4 targets on existing fields is REFUSED by the emitter with the producer-guard message naming -06 (the -05 numeric inputs through the EQ-01 / EQ-02 / EQ-03 chains) and `gateReaders` names REQ-14 / -46 / -15 / -16 / -11 / -48 / -20 / -21 / -22 / -23 / -24 / -25 / -49 — all pinned. A second scratch check (`scripts/_check_din14021_staged.ts`, deleted) parse-checked all **35** staged conditions (35/35) and evaluated the G-1 / G-19 / G-22 / G-23 / G-25 / G-27 / G-28 forms both ways (recorded per block). Seed builder: the `inSpan` guard went RED once on my §7.1.1 span (L1060 is 7.1.2; 7.1.1 is L1059 — corrected in the generator); the emitted field-config migration showed the English §6.5.3 g) inside the §7.1 term list (L1063–L1076 → L1063–L1074, the German list ends at L1074 and the English g) sits at L1075) → span trimmed, migration re-emitted; verifier **38/38** → seed test went RED once on my 5.8.5 line (L877 is 5.8.4; 5.8.5 is L878 — corrected, seed re-emitted) → **5/5**. Field-config module: the emitter accepted all 7 entries first time → **6/6** first run. Equations test: one RED on my own miscount (7 failing rows, not 6) → **5/5**. Render test **2/2** first run. STAGED generator: 76 blocks written first run (every fragment inside its span). Typecheck / eslint green on the first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts din14021 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-14021\DIN-EN-ISO-14021.md"` → **`38/38 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- CLAIMMAP (23): compostable L1104–L1107 · degradable L1155–L1157 · designed_for_disassembly L1192 · extended_life_product L1250 · recovered_energy L1296–L1298 · recyclable L1336–L1340 · recycled_content L1449–L1450 · pre_consumer_material L1382 · post_consumer_material L1382 · recycled_material L1416 · recovered_reclaimed_material L1420 · reduced_energy_consumption L1508–L1509 · reduced_resource_use L1551 · reduced_water_consumption L1585 · reusable L1646–L1648 · refillable L1646–L1648 · waste_reduction L1683 · renewable_material L1719–L1721 · renewable_energy L1763 · sustainable L1801 · product_carbon_footprint L1845 · carbon_neutral L1856 · other L757 (labels asserted inside L1063–L1074 / the heading lines L1384 / L1410 / L1414 / L1418 / L1616 / L1620 / L1709 / L1753 / L1797 / L1812 / L1856 / L757)
- S6_5_3 (7): a L1033 · b L1034 · c L1035 · d L1036 · e L1037 · f L1038 · g L1054 (after the English column)
- S5_3_5_10 (8): 5_3 L783 · 5_4 L789 · 5_5 L795 · 5_6 L799 · 5_7 L823 · 5_8 L878 · 5_9 L884 · 5_10 L910 (headings asserted inside L781 / L787 / L793 / L797 / L821 / L873 / L882 / L900)

Status per table: `md_verified` — ALL three (every row quote lifted, every displayed cell legible; the seed migration carries the three `verification_status` upgrades).

Beyond the verifier (table rows only), the STAGED generator asserted every `"…" (L…)` fragment of the sheet section and the STAGED file inside its span (**all 76 blocks**, whitespace-collapsed; it throws on a fragment outside its span, never retyped), and the module `verification_quote`s are the `Q` constants themselves (verbatim by construction; pinned non-empty / no `undefined`).

**Absence / presence claims (amendment O — commands re-executable, raw output; T = the transcript):**
- No numeric value table (inventory §1 confirmed): `grep -n "Tabelle [0-9]" "<T>"` → `570:Anmerkung 2 zum Begriff: Eine Liste anerkannter Treibhausgase ist angegeben in IPCC, Climate Change 2007: The Physical Science Basis, Kapitel 2, Tabelle 2.14.` (exit 0 — the one hit is a bibliographic pointer); `grep -c "Table [0-9]" "<T>"` → `1` (its English twin L591).
- The only printed worked example is NOT one of the three per-row formulas: `grep -n "times 100" "<T>"` → L1000 / L1017 (the §6.3.3 NOTE "(15 Monate − 10 Monate) / 10 Monate × 100 = 50 %"), L1302 / L1320 (§7.6.3 formula DE / EN), L1480 / L1491 (§7.8.4), L1570 / L1590 (§7.10.3), L1911 / L1928 (Annex A restating X = A/P × 100) — no R / E / P, A / P or I / N example values are printed; the equation pins use chosen rows.
- "gewöhnlich 12 Monate" IS printed: `grep -n "12 Monate" "<T>"` → `970:c) über einen angemessenen Zeitraum, gewöhnlich 12 Monate, berechnet werden.` and `1554:7.10.2.7 Wurde für eine anfängliche Dauer von 12 Monaten …`; `grep -n -i "gewöhnlich" "<T>"` → L970 / L1505 / L1582 (the other two are NOTES on wording). Prod's VR `comparison_time_interval eq 12` → G-21.
- The four "uneingeschränkte" rules: `grep -n -i "uneingeschränkte" "<T>"` → L1719 (7.14.2) / L1763 (7.15.2) / L1801 (7.16.1) / L1856 (7.17.3.2) — exactly `UNQUALIFIED_TYPES` (renewable_material / renewable_energy / sustainable / carbon_neutral); the brief's list (three) + `sustainable` (L1801).
- O-1 — the brief's / prod VR's driver does not exist in the capture: `grep -c '"DIN-14021-05 unqualified_claim"' src/lib/eval/field-configs/din14021.prior.json` → `0` (exit 1); `grep -c unqualified_claim src/lib/eval/field-configs/din14021.prior.json` → `0` (exit 1) — CREATED by 20260917102510 (also pinned on the loaded prior).
- The 20-gate tautology: pinned from the capture — 20 rows share `condition = 'selected_claim_type IS NOT NULL'` (md5 `28c4f6704f3aa59f8bfdc892f3158f68`): REQ-28 … REQ-45, REQ-47, REQ-50, all on -01, all `block`; REQ-26 is `TRUE`; REQ-25 / REQ-49 are byte-identical; 0 unparseable gates.

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code ILIKE '%14021%'"
  → DIN-14021 · '2016 (ISO 14021:2016); EN ISO 14021:2016' · issued_year null
node scripts/regulation-tables/build-prior-snapshot.mjs DIN-14021 din14021
  → wrote src\lib\eval\field-configs\din14021.prior.json: 47 field rows (0 orphan), 41 coded sections of 41, 3 equations, 50 gates (0 unparseable …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
node scripts/verification/capture-text.mjs DIN-14021 din14021
  → wrote src\lib\eval\field-configs\din14021.text.prior.json: worksheets 6, sections 41, fields 47, requirements 50, equations 3, stored parameters [{"n":0}], instances [{"n":0}]
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts din14021
  → wrote seed + rollback for din14021 -> scripts/migrations/20260917102500_regulation_tables_seed_din14021.sql scripts/rollback-20260917102500-regulation-tables-seed-din14021.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts din14021 20260917102510
  → wrote 7 field entries + 0 section entries for din14021 -> scripts/migrations/20260917102510_field_configs_din14021.sql scripts/rollback-20260917102510-field-configs-din14021.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts din14021 20260917102520
  → wrote 3 equations for din14021 -> scripts/migrations/20260917102520_equations_din14021.sql scripts/rollback-20260917102520-equations-din14021.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-din14021.test.ts` / `equations-din14021.test.ts` pin the other two against the committed `din14021.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 38 row upserts (`ON CONFLICT (table_id, row_key)`) and 3 `verification_status = 'md_verified'` upgrades; its rollback deletes the three tables (no earlier DIN-14021 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 3 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102500` → `20260917102510` → `20260917102520`; rollback in reverse. Every file touches `DIN-14021` rows only. **Materialisation (amendment D, noted once — din14021-I-1):** all three rows are register-fed on their own worksheet (-01) and materialise on save; no scalar-only row this task. The one emitted rule (`unqualified_claim` ← the inherited `selected_claim_type`) hides on the form / report / PDF but is `pending` (visible) for the server-side `computeVisibility` (templateFields only — vsme-X-3 [CODE]) — fail-safe.

## 5. What was encoded

**Tables (3)** — `src/lib/eval/regulation-tables-seed-din14021.ts`, builder `din14021SeedTables()`, edition `'2016'` (title page L8–L9 "(ISO 14021:2016);" / "Deutsche und Englische Fassung EN ISO 14021:2016"; prod `standards.version` "2016 (ISO 14021:2016); EN ISO 14021:2016"). The standard prints NO numeric table; its lookups are text classifications:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| CLAIMMAP | `claim_type` = the 23 prod `selected_claim_type` tokens in prod order (D-1 / G-A3, pinned) | `clause` (string), `numeric_block` (**structure** — none / recovered_energy / recycled_content / reduced_resource / renewable_material / renewable_energy / carbon; an executor classifier, never a printed cell), `condition_text` (= the row quote, the printed "darf nur … wenn" / "darf nicht … wenn" / binding "muss" sentence), `comparative_by_clause` (boolean, the printed "Weil … eine vergleichende Aussage ist, müssen die Anforderungen von 6.3 erfüllt sein" — asserted on exactly extended_life_product / reduced_energy_consumption / reduced_resource_use / reduced_water_consumption / waste_reduction) | locked (L1059 "darf nicht durch Verwendung ähnlicher Begriffe abgeschwächt werden" — L1297 "darf nur erfolgen, wenn") | 23 | md_verified |
| S6_5_3 | `item` a … g | `letter`, `text` (the printed item without its marker) | locked (L1032 "müssen Folgendes enthalten") | 7 | md_verified |
| S5_3_5_10 | `clause` 5_3 … 5_10 | `clause_printed`, `heading` (the printed heading), `requirement_text` (= the row quote: 5.3 L783, 5.4 L789, 5.5 L795, 5.6 L799, 5.7 intro L823, 5.8.5 L878, 5.9.2 L884, 5.10.2.4 L910 — J-3) | locked (L823 — L783) | 8 | md_verified |

Labels are the printed terms (§7.1 NOTE list L1063–L1074 for 7.2–7.13, the printed headings for the rest; the two §7.8.1.1 waste tokens map to the recycled_content block, the two material tokens to none — din14021-J-1; `other` → §5.1 L757 "oder eine sonstige Umweltaussage" — J-2).

**Registers / selections / visibility** — `src/lib/eval/field-configs/din14021.ts`:
- `claims` (-01 B, unconditional, `create` json): `claim_type` (required, `lookup_key` CLAIMMAP, discriminator), `clause` / `numeric_block` / `condition_text` / `comparative_required` (`lookup_value`, refilled on pick), `scope` (enum = prod `claim_scope` tokens + labels, pinned byte-for-byte), `channel` (= prod `communication_channel`), `symbol` (boolean — never `symbol_used`, a -01 prod symbol; amendment P / Task 22 trap 4 pinned), `mobius` (← `claim_type IN {'recyclable', 'recycled_content'}`, §5.10.2.4 L910), `explanatory` (text), `unqualified` (← the four `UNQUALIFIED_TYPES`), `comparative`, `comparison_basis` (= prod tokens) / `comparison_months` (← `comparative == true`), the §7.6.3 block `r` / `e` / `p` → `net_recovered_pct = (r - e) / ((r - e) + p) * 100` (L1300–L1303), the §7.8.4 block `a_mass` / `p_mass` → `recycled_pct = a_mass / p_mass * 100` (L1480), the §7.10.3 block `i_res` / `n_res` → `reduced_pct = (i_res - n_res) / i_res * 100` (L1570), `renewable_pct` (← the two renewable blocks), `carbon_footprint` (← carbon) / `carbon_offset_declared` (← carbon_neutral); the four printed checks as decidable badges — `recovered_ok` (L1297 R−E>0, 0 when R / E not entered), `renewable_ok` (L1719 / L1763 — the printed 100 when `unqualified`, 0 when the share is not entered), `unqualified_ok` (L1856 / L1801 — no unqualified CO2-neutral / nachhaltig), `comparative_ok` (CLAIMMAP.comparative_by_clause ⇒ the box must be ticked) — and `type_ok` over the four (declared after them); footer `claims_count` / `claims_type_fail` / `specific_requirements_met_code`; note L1059 + D-1 … D-24 + J-4 + G-1 … G-27. 17 row-scope rules (pinned).
- `general_requirements_items` (-03 C, created json, `select_many`, 8 options = the S5_3_5_10 headings — the printed lines as values, m820_2 trap 1); `documentation_items` (-04 E, created json, `select_many`, 7 options = the S6_5_3 lines "a) …"). The twelve -03 booleans and `info_documented_min` stay (D-26 … D-36).
- `unqualified_claim` (-05 C, created boolean attestation, `visible_when selected_claim_type IN {'renewable_material', 'renewable_energy', 'sustainable', 'carbon_neutral'}` — `selected_claim_type` is inherited on -05, pinned): the driver prod's VR strings name ("renewable_material_pct eq 100 when unqualified_claim") but never held (O-1); the -05 gates that read it are G-25 / G-26 / G-27.
- Field `visible_when` on existing inputs: **none** — the brief's Step-4 targets (the four -04 comparison fields ← `comparative_claim == true`; -03 `mobius_loop_used` ← the two recycling types; -03 `symbol_distinguishable` / `natural_object_link` ← `symbol_used == true`; the eleven -05 numeric fields by type) are ALL refused by the producer guard (every one consumed by -06; the -05 inputs transitively via EQ-01 / EQ-02 / EQ-03) and, where a gate reads them, by the gate-aware guard — pinned per target, routed to G-19 (the -04 block), G-23 … G-27 (the -05 blocks), G-28 (Möbius), C-2 (symbol pair), C-3 (I / N). Section rules: none.

**Equations (3 new)** — `src/lib/eval/equations/din14021.ts`: `DIN-14021-01-D1 claims_count = count_rows(claims)` · `-D2 claims_type_fail = count_rows(claims, type_ok == 0)` · `-D3 specific_requirements_met_code = if(count_rows(claims) > 0 AND count_rows(claims, type_ok == 0) == 0, 1, 0)` (inlined — never chained on D2; an EMPTY register reads 0, never a phantom pass). None outputs a prod symbol (EQ-01 … EQ-03 keep their rows on the -05 scalars — the register carries the same printed formulas per row, amendment K: D-14 / D-17 / D-20); no figure is typed into a formula (pinned). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback CLAIMMAP): R 100 / E 40 / P 60 ⇒ net 50 %; A 25 / P 100 ⇒ X 25 %; I 8 / N 6 ⇒ U 25 %; four rows of four types ⇒ 4 / 0 / 1; the twelve-row check set (R−E<0, R / E not entered, unqualified 80 %, share not entered, unqualified CO2-neutral, unqualified nachhaltig, extended-life without comparison) ⇒ 12 / 7 / 0; a row without a claim type is incomplete; empty ⇒ 0 / 0 / 0; a null `e` cell is a missing input (never Euler — pinned). Not emitted: the brief's -06 D2 / D3 / D4 (`contains()` over the checklists + the boolean `verifiable_without_confidential`) — no engine path passes json checklist carriers to `contains()` and booleans never reach scalar equations (probed; iso46001-F-2 precedent) → **din14021-F-1** with the intended formulas.

**STAGED (`scripts/verification/din14021-STAGED-plan3-rulings.sql`, 1023 lines, 0 non-comment lines, 76 ☐ blocks; generated from the captures)** — G-1 … G-18 the 18 type gates REQ-28 … REQ-45 (`IF selected_claim_type == '<token>' THEN specific_requirements_met_code == 1`, each with its §7.x quote; archive pattern, shared md5); G-19 the -04 comparison block (REQ-14 / -15 / -46 / -16 IF-guarded on `comparative_claim == true` + the four hides); G-20 REQ-26 `TRUE` → the eight-heading `contains()` completeness (applicable after F-1); G-21 the `comparison_time_interval eq 12` VR → default 12 + justification; G-22 REQ-47 / REQ-50 (the two extra presence gates); G-23 REQ-20 (`IF … == 'recovered_energy' THEN R_energy - E_energy > 0` + hides R / E / P); G-24 REQ-21 (+ hides A / P; J-1 on the IN alternative); G-25 REQ-22 + NEW REQ-51 (`IF type == 'renewable_material' AND unqualified_claim == true THEN renewable_material_pct == 100`); G-26 REQ-23 + NEW REQ-52; G-27 REQ-24 / -25 / -49 (IN guard — the footprint hide never exempt) + NEW REQ-53 (§7.17.3.2) / REQ-54 (§7.16.1); G-28 REQ-11 rewrite (needs C-4) + the REQ-48 finding (`mobius_loop_used == True` blocks every claim without the loop — variant B); C-1 `specific_requirements_met_code` → -06; C-2 the symbol pair hides; C-3 the I / N hides; C-4 `selected_claim_type` → -03; D-1 … D-36 the amendment-K pairs (D-1 … D-25 the 24 register columns ↔ their scalars incl. the three EQ outputs and the created `unqualified_claim` plus the -06 `specific_requirements_met` twin, D-26 … D-35 the ten -03 booleans ↔ the general checklist, D-36 `info_documented_min` ↔ the documentation checklist — each states data_type / is_required / gate readers / consumers, all data-free); F-1; I-1; J-1 … J-4; O-1; X-1. Every gate block archives the row (`compliance_requirements_archive_din14021`), guards on `md5(condition)` (ids / md5 from the capture), restores with the explicit column list; every staged condition parse-checked (35/35) and the guard families evaluated both ways in-session; no prod equation is touched.

## 6. Inventory §5 top-wins walk

1. `selected_claim_type` drives visibility and gating (only the -05 numeric fields of the chosen type, only the REQ-28 … REQ-45 block of that type) → **STAGED with reason**: every hide on an existing field is refused by the producer guard (-06 consumes everything; the -05 inputs feed consumed equations) and the gate-aware guard; the 18 presence tautologies + the -05 gates are enforcement changes → G-1 … G-18 / G-23 … G-27 with the hides as follow-ups; the register (top-win 2) carries the per-type switching TODAY in row scope. ✓ (guards STAGED)
2. Claims register (one row per claim with the type-specific evidence per row) → **encoded** (`claims` on -01: 23-token discriminator over CLAIMMAP, the three printed formulas per row, the four printed checks as badges, `type_ok`, counts / verdict; D-1 … D-24 for the scalars). ✓
3. Derive the three "Aggregat" booleans and the verdict → **partially**: `specific_requirements_met_code` computed (register-fed, -01; inheritance to -06 = C-1; the -06 twin D-25); `general_requirements_met_code` / `verification_requirements_met_code` / `compliance_verdict_code` withheld — `contains()` reaches no engine path, booleans no equation (**F-1** [CODE]; formulas recorded). ✓ (specific) / F-1 (general / verification / verdict)
4. The missing "unqualified vs qualified" driver so the 100 % rules and the comparative block become true conditionals → **encoded**: `unqualified_claim` created on -05 (visible for the four printed "uneingeschränkt" types) + the register column `unqualified`; the 100 % rules are computed per row (`renewable_ok`) and STAGED as gates REQ-51 / REQ-52 (G-25 / G-26), the prohibition as REQ-53 / REQ-54 (G-27); the comparative block G-19. ✓ (driver + row checks) / gates STAGED
5. Checklists for §6.5.3 a) – g) and §5.3 – §5.10 with derived completeness → **encoded** (both `select_many` over the seeded tables); the completeness codes are F-1 (G-20 for REQ-26; `info_documented_min` / the twelve booleans D-26 … D-36). ✓ (checklists) / F-1 (codes)

Data-quality gaps of the inventory: -02 has 0 fields (nothing to encode — observation); the 18 tautological gates → G-1 … G-18 (+ REQ-47 / REQ-50 → G-22); REQ-26 `TRUE` → G-20; VR pseudo-logic naming `unqualified_claim` → created (O-1) + G-25 / G-26; `comparison_time_interval eq 12` vs "gewöhnlich" → G-21; units "-" on enums → observation, untouched; REQ-25 / REQ-49 duplicates → G-27; REQ-48 `mobius_loop_used == True` (blocks every claim without the loop) → G-28 variant B.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`din14021` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (din14021 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+76 blocks, +6 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 25)" paragraph).
Created: `src/lib/eval/regulation-tables-quotes-din14021.ts` (100 spans, generated), `src/lib/eval/regulation-tables-seed-din14021.ts`, `src/lib/eval/field-configs/din14021.ts`, `src/lib/eval/field-configs/din14021.prior.json` + `din14021.text.prior.json` (captured), `src/lib/eval/equations/din14021.ts`, `src/lib/eval/__tests__/regulation-tables-seed-din14021.test.ts`, `src/lib/eval/__tests__/field-configs-din14021.test.ts`, `src/lib/eval/__tests__/equations-din14021.test.ts`, `src/components/worksheet/__tests__/register-din14021-claims.test.tsx`, `scripts/verification/din14021-STAGED-plan3-rulings.sql` (76 ☐ blocks; 1023 lines, 0 non-comment lines), `scripts/migrations/20260917102500_regulation_tables_seed_din14021.sql` + `scripts/rollback-20260917102500-regulation-tables-seed-din14021.sql`, `scripts/migrations/20260917102510_field_configs_din14021.sql` + `scripts/rollback-20260917102510-field-configs-din14021.sql`, `scripts/migrations/20260917102520_equations_din14021.sql` + `scripts/rollback-20260917102520-equations-din14021.sql`, this report (+ the copy at `.superpowers/sdd/…/task-25-report.md`).
Untouched: every other standard's file; the DIN-14021 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the two scratch probes deleted; the generators (quotes, STAGED + sheet section, registry splice, playbook splice) live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **`type_ok` split into four per-block badges** (controller resolution: "choose the split, record it as the brief deviation"): `recovered_ok` / `renewable_ok` / `unqualified_ok` / `comparative_ok` in the "not entered ⇒ 0" form, `type_ok` declared after them (derived-on-derived probed). Two badges go beyond the brief's expression: `unqualified_ok` (the printed §7.17.3.2 / §7.16.1 prohibition — L1856 / L1801) and `comparative_ok` (the printed "vergleichende Aussage ⇒ 6.3" of five types — CLAIMMAP.comparative_by_clause); both are printed rules, recorded in J-4.
2. **`unqualified` / `unqualified_claim` cover FOUR types, not three**: `sustainable` added — §7.16.1 L1801 prints the same "uneingeschränkte" rule (grep in §3).
3. **-06 D2 / D3 / D4 NOT emitted** (brief: "contains() … encodable"): the evaluator supports `contains()`, but no engine path passes json checklist carriers (materialiser: registers only; form hook / report evaluator: registers + tableLookup; gate scope: presence marker) and `verifiable_without_confidential` is a boolean (booleans never reach scalar equations) → F-1 [CODE] with the exact formulas; iso46001-F-2 precedent. The checklists themselves ARE emitted (select_many, values = the printed lines — m820_2 trap 1, not letters).
4. **Register column keys**: `symbol` (not the brief's `symbol_used` — a -01 prod symbol, Task 22 trap 4), `carbon_offset_declared` added (the -05 twin of `carbon_neutral_offset_declared`, §7.17.3.3 L1864), `clause` / `comparative_required` lookup_value columns added (the printed clause and the comparative-by-clause fact); `condition_text` kept as the brief says; `numeric_block` labelled "Zahlenblock (§7)".
5. **Equations live on -01** (the register's worksheet — m277e trap 2), not the brief's -06; the brief's `DIN-14021-06-D1` is `DIN-14021-01-D3` with the inline empty-register guard (`count_rows(claims) > 0 AND …` — the brief's `if(claims_type_fail == 0, 1, 0)` would chain on D2 AND pass on an empty register).
6. **Field visible_when 0 (brief 9 + 7)**: every target refused — see §5; the visibility story is the register's 17 row-scope rules + the one created-field rule.
7. **G-blocks 28 (brief 21)**: G-22 (REQ-47 / REQ-50 share the tautology), G-23 … G-27 (the -05 numeric gates the brief's "nine visible_when entries" would have disarmed — found by the guards), G-28 (Möbius, incl. the REQ-48 finding); four NEW gates REQ-51 … REQ-54 carry the printed 100 % / prohibition rules on the created driver. The brief's G-1 … G-18 (each with its §7.x quote), G-19, G-20, G-21 ids kept.
8. **CLAIMMAP `condition_text` is the row quote itself** (whitespace-collapsed) — the printed sentence per type; for `pre_consumer_material` / `post_consumer_material` the §7.8.1.1 a) rule (J-1), for `other` §5.1 (J-2). `comparative_by_clause` (not in the brief) is a printed cell asserted per row.
9. **Sign-off ids 76 (brief ≈ 6)**: the 36 amendment-K pairs and the 28 gate blocks dominate.
10. **Edition token `'2016'`** (the title page prints "(ISO 14021:2016)" / "EN ISO 14021:2016" — no "2016-07" is printed: the Änderungen block L93–L103 names only the earlier editions 2001-12 / 2012-04); prod's `version` starts "2016 (ISO 14021:2016) …" — consistent, pinned.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (38/38); every seeded cell is asserted inside its span at build time; every `"…" (L…)` fragment on the sheet and in the STAGED file was asserted inside its span by the generator (76 blocks); the module `verification_quote`s are the lifted `Q` spans; every staged condition parses (35/35) and the guard families were evaluated both ways in-session; prod facts (tokens + labels, consumer lists, section codes, gate / equation ids + md5 + status, worksheet ids, the 0 / 0 stored-value / instance counts, max REQ code, VR strings, `standards.version`) come from the in-session captures / queries listed in §0 / §4 and the STAGED header.
- The field-config test pins every refusal of the brief's 18 targets (gate readers per symbol, producer chain incl. the EQ-01 / EQ-02 / EQ-03 hops, `emitFieldConfigSql` throws), the 20-gate tautology set, the IF-guard exemption G-23 relies on AND the non-exemption of an IN guard (G-27 / G-28), the three captured equations, the G-A3 / D-1 facts (CLAIMMAP keys = the prod tokens; the three register enum columns = prod value + label byte-for-byte; the checklist values = the seeded rows), no `id` column and no prod-symbol shadowing, no self-consumer entries, every create in a captured section, the O-1 absence, the `symbol_used` prod symbol, the freshness of migration + rollback with 0 UPDATEs.
- Runtime facts were probed through the real register / formula / condition / emitter contracts before the pins (probes deleted); the render test drives `claims` (recycled_content vs recovered_energy rows: discriminator-hidden blocks, lookup_value refill, the printed percentages, the §7.6.3 badge flipping on a typed E > R, the new-row incompleteness, the renewable_energy pick revealing share + unqualified box, an unqualified 80 % failing the printed 100) through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file: the registry lines, the playbook paragraph and the sheet section are marker-checked `split/join` splices (CRLF-aware) written with the Write tool; the modules, tests and generators were written with the Write tool; the three small line-number corrections used `split/join` on exact strings (generator) and literal `sed` patterns checked by `grep`.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **The headline win (per-type gating) is entirely STAGED** — G-1 … G-18 replace a tautology that passes for every type by a real block on the register verdict (an enforcement change: today NOTHING is enforced by those 18; after ratification an empty register FAILS them); the -05 gates G-23 … G-27 today BLOCK every claim that is not of their type (REQ-21 / -22 / -23 / -24 / -25 / -49 demand a percentage / footprint unconditionally, REQ-20 is pending for 22 types) — the guards make them per-type. Fail-safe by construction, but the owner should see that the current -05 gate set cannot pass for any single-type project.
2. **F-1 is the one [CODE] gap this standard exposes twice** (the two checklist completeness codes and the verdict, and G-20's gate form): plumbing json checklist carriers into the four `evaluateFormula` / gate scopes is a small engine change that Task 22 already named; until then two of the three "Aggregat" booleans stay hand-typed.
3. **G-1 … G-18's verdict spans all rows**: a failing row of another type also fails a type gate (the register is the product's claim set; a per-type count would need 23 equations). Recorded in every block.
4. **36 amendment-K pairs** — 21 recommend DEFER (gate-read or gate-driving scalars, the three EQ outputs), the rest retire on ratification; all data-free (0 stored values / 0 instances).
5. **REQ-48 (`mobius_loop_used == True`) blocks every claim that does not use the Möbius loop** — a prod enforcement error found while reading the capture; the fix needs an INSERT-class attestation (G-28 variant B), not staged as SQL.
6. **`condition_text` is attested, not computed (J-4)**: only the four printed numeric / boolean checks enter `type_ok`; the textual "darf nur … wenn" conditions are shown per row for the engineer.
7. **Token/time:** ≈ 0.62 M tokens in, ≈ 45 min wall-clock — in the Task 23 / 24 band; the cost sits in the 36 D-pairs and the 28 gate blocks (generated), not in the transcript (38 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§5.7 a) – r) as eighteen separate items (prod holds five of them as booleans; the checklist ticks the clause — J-3); §5.7 k) – q) (misinterpretation, existing aspect, explanatory statement proximity m), pre-existing aspect o), absent ingredients p), reassessment q)) — no fields; §5.9 material identification / disposal / hazard words (L883–L884) — no field; §5.10.2.2 product-vs-packaging confusion (L908) — no field; §6.2.2 retention period (L939) — `evaluation_documented` only; §6.3.3 / 6.3.4 relative vs absolute wording (L972–L1002) — no field; §6.4 the precedence order (L1008) — the enum `method_selected` only, no ordering check; §6.5.2 disclosure on request (L1031) — no field; §7.2.2.2 facility type / components (L1108–L1110), §7.3.2.1 test method / degree / duration (L1155–L1157), §7.4.2.3 – 7.4.2.7 who disassembles / tools / facilities (L1194–L1220), §7.5.2.2 / 7.5.2.3 upgradability / durability figures (L1251–L1252), §7.6.2 a) – c) waste type and quantity (L1290–L1292), §7.7.2 / 7.7.4 facilities and evidence a) – c) (L1336–L1340 / L1370–L1373), §7.8.2.2 / 7.8.3.3 product-packaging separation / variable percentage (L1450 / L1455), §7.9.2.2 / 7.11.2.2 use-phase-only (L1509 / L1606), §7.10.2.5 – 7.10.2.8 resource type / increased other resource / per resource (L1552–L1555), §7.12.2.3 limited availability (L1649–L1652), §7.13.2.2 – 7.13.2.5 waste accounting rules (L1684–L1687), §7.14.2 a) / b) and §7.15.2 per product / packaging (L1720–L1721 / L1765), §7.17.3.3 a) / b) (L1860–L1862) — each is a textual condition shown per row as `condition_text` and attested (J-4), or a procedural sentence without a printed test; Anhang A (recycling system diagram, L1904–L1930) — informative, restates X = A/P × 100; the bibliography lists of test standards (L1931–L2122) — pointers (content-boundary rule; REQ-50's "anerkannte Prüfverfahren" is a cross-reference). Each stays outside this encoding or on the sign-off sheet.
