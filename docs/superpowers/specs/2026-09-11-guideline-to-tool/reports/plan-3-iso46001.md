# Plan 3 — ISO-46001 (`iso46001`) — Task 22 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 23)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `f6d1e35`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs ISO-46001 iso46001` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`; the 10 worksheet titles; the section titles B–F per worksheet; labels / units / `is_required` / VR of the -04 and -08 fields; the 14 compliance rows of interest with ids / severities / md5(condition) / lengths / titles / `requires_attestation` / descriptions; the 4 equation rows with ids / md5 / formulas / status; the `information_schema` column lists of `compliance_requirements` / `equations` / `fields` / `regulation_tables`; the stored-parameter count (0) and the worksheet-instance count (0)). `.env.local` never printed (variable NAMES listed once with `grep -oE '^[A-Z_]+='`).
- **SR-1 source:** every seeded value and every cue was read in this session from the raw plain-text transcript `C:\Users\Ekowai\Desktop\Guidelines\_site_audit\ISO-46001\_46001_raw.txt` (1287 lines; BS ISO 46001:2019 = ISO 46001:2019(E), First edition 2019-07; **ISO-8859-1 bytes, decoded as latin1** so the printed "×" / "©" survive; **VC grade** — the `.pdf` was NOT opened). `_46001_layout.txt` (2393 lines) was read ONLY to settle the Table A.1 / Table D.1 cell pairing (L1849–L1929, L2233–L2283) and is never quoted as a row source. Read in full lines (never `cut` on a quoted line): title page L26–L35, §3.2 / §3.4 / §3.28 / §3.33 L248–L256, L370–L372, L393–L394, §4.2 L428, §6.2.1–§6.3 L490–L575, §8.2 / §8.3 L646–L656, §9.1 L660–L700, §10.1 L700–L722, A.7.3–A.7.7 L797–L830, A.12 L870–L888, Table A.1 L886–L1023, Annex C L1077–L1188, Table D.1 L1186–L1222. The 86 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-iso46001-quotes.mjs`, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-iso46001.ts`, never hand-edited); every seeded cell fragment is asserted inside its span at build time (`inSpan`, whitespace-collapsed). The inventory was used for pointers only.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `iso46001` | 2 (TABLEA1, TABLED1) | 40 (40/40 verbatim — TABLEA1 39 = all 8 printed sectors × their lettered areas; TABLED1 1 = the only pairing the layout file settles) | TABLED1: 23 printed sector ↔ indicator pairs NOT settled by the layout file → not seeded (U-1 … U-23); both tables stay `imported_unverified` (raw txt = VC) | 9 (all `create`: `interested_parties_46001` -01, `objectives` + `legal_requirements` -03, `water_sources_46001` + `significant_uses` + `indicators_46001` -04, `targets` -05, `water_streams` -08, `nonconformities` -10) + 1 `select_many` checklist (`monitoring_items` -09) | 1 (`industry_sector` -04, created, 1 option = the seeded TABLED1 key) | 1 (`monitoring_items`, 7 printed §9.1 items) | 1 (`business_activity_indicator_hint` ← TABLED1 by `industry_sector`, role `value`, twin of the consumed text input) | **3** (created `baseline_adjustment_note` ← `baseline_adjustment_trigger IS NOT NULL`; UPDATE `procurement_criteria` / `supplier_informed` ← `procurement_significant == true`) + 2 row-scope rules inside `water_streams` | 0 | **20** (-01 D1; -03 D1–D2; -04 D1–D6; -05 D1; -08 D1–D8; -10 D1–D2) | 0 replacements emitted; 1 retirement block (R-1: C.1 / C.2b / C.3 / C.5 + 16 scalars, archive pattern), 6 gate blocks (G-1 … G-6 over CR-027 / CR-028 / CR-036 / CR-039 / CR-040 / CR-037 / CR-038 / CR-009 / CR-017), 2 consumer blocks, 40 amendment-K pairs (5 twins), 23 U-blocks | 83 blocks (+7 observations) | ≈ 0.55 M (tool-marker delta 15.00 M → 14.45 M at report time) | ≈ 70 |

Field entries: 37 = 35 `create` (9 registers, 20 derived outputs, 1 select_one, 1 lookup_fill, 1 select_many, 2 attestation booleans `designing_new_facilities` / `procurement_significant` on -07, 1 text note) + **2 UPDATE** (`procurement_criteria` / `supplier_informed` on -07 E — no consumer, no gate reads them; the emitter accepted both). Migration `20260917102210`: 35 `INSERT … WHERE NOT EXISTS`, 2 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes only the 35 `Plan 3:` rows and restores the two prior NULLs. No `enum_values` is written by any UPDATE (D-1). The emitters printed 0 warnings (no quoted-literal collision — pinned; no self-consumer NOTICE; the 3 unparseable gates are the 3 EMPTY conditions, which never refuse); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, after the final re-emit):
```
 Test Files  334 passed | 1 skipped (335)
      Tests  3009 passed | 1 expected fail | 1 skipped (3011)
   Start at  19:36:01
   Duration  57.36s (transform 17.81s, setup 59.22s, import 142.13s, tests 57.95s, environment 234.86s)
```
(2991 at `f6d1e35` + 18 from this task: seed-iso46001 3, field-configs-iso46001 6, equations-iso46001 6, register-iso46001-streams 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0. `pnpm -s eslint <11 touched .ts/.tsx files + the harness file>` → exit 0, no output.

Integration harness (embedded PG):
```
pnpm vitest run --project integration tests/harness/iso46001-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  55 passed (55)
   Start at  19:38:58
   Duration  5.87s (transform 823ms, setup 0ms, import 3.71s, tests 1.98s, environment 0ms)
```
**One stale pin fixed in the harness** (the only harness edit): at `f6d1e35` — verified with `git stash` before any of this task's files were in the tree — the harness already failed 1/55 on `evaluateCondition("Win == Wout") returns fail even when Win==Wout numerically (RHS → string "Wout")` (`expected 'pass' to be 'fail'`): the pin encodes the pre-T-13b reading; since `7c82243` (plan3-T-13b) a valued BARE RHS resolves as the symbol. Probed in-session through the real `evaluateCondition`: equal ⇒ `pass`, unequal ⇒ `fail`, `Wout` unset ⇒ `fail` (the string fallback survives only for an unset RHS). The pin now asserts those three states and cites the ruling; the 54 gate / equation cases are untouched. The harness seeds prod's 97 fields / 4 equations / 40 gates; the new rows are not in it by construction — it proves the branch still drives every ISO-46001 gate through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with three scratch probes through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` / `parseCondition` (`scripts/_probe_iso46001.ts`, `_probe2`, `_probe3`, all deleted): `role IN {wd, r_other_source}` works bare inside a row `if()` and a formula, `IN {'a','b'}` quoted too, and `OR` chains equally (all three forms computed 1200 on the fixture); `count_rows(reg, role IN {…})` counts 4 recycling rows; an EMPTY register is `manual_required` ("Keine vollständigen Zeilen") for every Σ / rate / difference and 0 for the counts; output-only rows give Win 0, difference −900 and "Division durch Null" for both rates; a row without its volume / label or with a negative volume (`min: 0`) is incomplete and never counted; the §3.33 scalar reads 30 for 1200 / 40, "Division durch Null" for a zero activity value and "Fehlende oder leere Eingaben: business_activity_indicator_value" when unset; every staged gate text parses and evaluates as expected (`IF designing_new_facilities == true THEN design_consideration == true`: driver false ⇒ pass, true + attestation false ⇒ fail, driver unset ⇒ pending; `IS NOT NULL` on an unset enum ⇒ fail / set ⇒ pass; `IF recycling_streams_count >= 1 THEN a >= 0 AND b >= 0` with and without parentheses); `closed == false` counts an UNSET checkbox as open (2 of 3); a hidden TEXT cell stores `''`, a hidden ENUM cell `null`; `value - baseline` with a null baseline is a silent null cell (no diagnostic) — the `if(baseline IS NULL, 0, …)` draft was replaced by it; `count_rows(reg, meter_verified_on IS NULL)` counts on a `date` column; `max_rows / sum_rows × 100` reads 60 % and "Division durch Null" on a zero Σ. Seed builder: the span guard threw once on MY generator (the D.1 indicator span ended on the copyright line L1220 instead of L1216 — `endsWith` pin RED, span corrected to L1202–L1216) and the datalist count went RED on my miscount (22 → 24 distinct areas); the verifier then passed **40/40** first run. Field-config test: RED on my §6.3 span (L570–L571 missed the action-plan items on L572 → span extended), RED on the lint (`targets.action_plan` / `improvement_verification_method` column keys shadow the -05 prod symbols → renamed `means` / `improvement_verification` / `result_verification`), then 6/6. Equations test: one RED on my own hidden-text pin (`null` vs `''`) → 6/6. Render test: one RED on my own pin (two rows without recycling give C.3 = 0 / 1000 = 0 %, computed — not `manual_required`) → 3/3. The emitter refused `create water_sources` first time (the prod text field of the same name exists) → the register became `water_sources_46001` (O-1). Two RED in the full suite after the D-block renumbering changed descriptions → re-emitted, both freshness pins green.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso46001 "C:\Users\Ekowai\Desktop\Guidelines\_site_audit\ISO-46001\_46001_raw.txt"` → **`40/40 quotes verbatim`**, exit 0. PASS list: TABLEA1 · `industries_a … f` (L903 / L907 / L909 / L911 / L913 / L917) · `hotels_a … g` (L917 / L919 / L921 / L923 / L925 / L927 / L931) · `tertiary_institutions_a … d` (L931 / L932 / L934 / L938) · `hospitals_a … d` (L938 / L940 / L942 / L946) · `workers_dormitories_a … d` (L946 / L948 / L950 / L952) · `construction_sites_a … e` (L952 / L954 / L956 / L958 / L960) · `sports_recreation_a … g` (L976 / L977 / L979 / L981 / L983 / L985 / L987) · `offices_retail_other_a … b` (L988); TABLED1 · `wafer_fabrication` (L1194–L1202).

Status: `imported_unverified` on both tables (a raw plain-text transcript is a VC source; TABLED1 additionally carries 23 unsettled pairings). Beyond the verifier (table rows only), a scratch checker (`check-iso46001-frags.mjs`, latin1 decode) asserted every `"…" (L…)` fragment of the sheet section and the STAGED file against the whitespace-normalised transcript: **236/236 verbatim** (both generated from line ranges by `gen-iso46001-staged.mjs`, which itself throws on a fragment outside its span, never retyped); every module `verification_quote` / register `note` / option label is a `Q` span or a fragment asserted inside one by the tests.

**Absence / presence claims (amendment O — commands re-executable, raw output):**
- J-1 — no legend defines WD / R1 … R3 / O1 … O4: `grep -n "WD is\|R1 is\|R2 is\|R3 is\|O1 is\|O2 is\|O3 is\|O4 is" "<transcript>"` → (empty, exit 1); Formula (C.2) prints them (L1129 "WD + R1 + R2 + R3 = O1 + O2 + O3 + O4"), the Figure C.1 key prints only "Key AHU air handling unit WWTP wastewater treatment plant" (L1126), the legends of C.1 / C.3 / C.5 define Win / Wout / Rp / Rnp / Wp / Rpp (L1114 / L1150 / L1166); `grep -n "Figure C" …` → L1115 / L1127 / L1142 / L1150 / L1177 (captions and references only — the figures are images).
- "No numeric value table" (inventory §1, confirmed): `grep -n "^Table [A-Z]\.[0-9]\|^Table [0-9]" "<transcript>"` → L899 `Table A.1 -- Areas in which water use can be monitored`, L971 `Table A.1 (continued)`, L1192 `Table D.1 -- Examples of business activity indicators for industry sectors`; `grep -n "Table" … | grep -v "Table A.1\|Table D.1"` → (empty, exit 1).
- The brief's `source_type enum [drinking, reclaimed, alternative]` is NOT a printed closed list: `grep -n -i "drinking" "<transcript>"` → **12 lines** (fix round 1 — re-run, actual output: L12 / L161 committee names; L182 "e) drinking;" — a water USE; L224 "NOTE 2 `Replace' includes substitution of drinking water with reclaimed water, sea water and rainwater wherever feasible."; L229 the ISO 24513 title; L357 §3.25 "non-drinking water"; L371 §3.28 Note 1; L419 §3.39 Note 2; L1048 Annex B.5 prose; L1233 / L1234 / L1235 bibliography) — no list of source types; the printed closed pair is Annex C a) / b) (L1084 / L1086 → `source_class` enum on `water_sources_46001`), the printed examples are an open list ("e.g." L1086; "may include but not be limited to" L801 → `source_kind` / `kind` datalist).
- J-3 — no printed formula for the comparison with the baseline (fix round 1): `grep -n -i "baseline" "<transcript>" | grep -E "Formula|="` → (empty, exit 1); `grep -n -i "baseline" "<transcript>"` → 11 lines (L96 TOC, L248 §3.2, L549 §6.2.6, L551–L553 / L557 §6.2.7, L820 A.7.5, L822–L823 A.7.6, L1028 Annex B.1 prose) — the standard prints "measured against the baseline" (L551) and no equation; cited in the J-3 block.
- Table D.1 pairing: the raw file prints all 24 sectors (L1194–L1200) BEFORE the first indicator cell (L1202) — pinned (`indexOf('Prisons') < indexOf('Number of units produced')` on the whole-table span); the layout file's same-line alignments (L2238 … L2266) read "Food processing ↔ Number of units produced", "Prisons ↔ Mass of washloads", "Power generation" and "Agriculture" beside the two halves of ONE wrapped cell — a systematic pitch drift, so only the first line (L2238 "Wafer fabrication  Number of units produced", directly under the heads L2237) is settled.

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code LIKE 'ISO-46001%'"
  → ISO-46001 · '2019 (ISO 46001:2019, First edition, 2019-07; BS ISO 46001:2019)' · issued_year null
node scripts/regulation-tables/build-prior-snapshot.mjs ISO-46001 iso46001
  → wrote src\lib\eval\field-configs\iso46001.prior.json: 97 field rows (23 orphan), 81 coded sections of 81, 4 equations, 40 gates (3 unparseable — symbols: [] + parse_error; …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso46001
  → wrote seed + rollback for iso46001 -> scripts/migrations/20260917102200_regulation_tables_seed_iso46001.sql scripts/rollback-20260917102200-regulation-tables-seed-iso46001.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso46001 20260917102210
  → wrote 37 field entries + 0 section entries for iso46001 -> scripts/migrations/20260917102210_field_configs_iso46001.sql scripts/rollback-20260917102210-field-configs-iso46001.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso46001 20260917102220
  → wrote 20 equations for iso46001 -> scripts/migrations/20260917102220_equations_iso46001.sql scripts/rollback-20260917102220-equations-iso46001.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-iso46001.test.ts` / `equations-iso46001.test.ts` pin the other two against the committed `iso46001.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 40 row upserts (`ON CONFLICT (table_id, row_key)`) and 0 `verification_status` upgrades; its rollback deletes TABLEA1 / TABLED1 (no earlier ISO-46001 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 20 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102200` → `20260917102210` → `20260917102220`; rollback in reverse. Every file touches `ISO-46001` rows only. **Materialisation (amendment D, noted once):** 19 of the 20 rows are register-fed (each reads only its own worksheet's register carrier) and materialise on save; `ISO-46001-04-D1` (`water_efficiency_indicator_calc = past_present_water_use / business_activity_indicator_value`) is scalar-only — computed on the form / report, not server-materialised (I-1).

## 5. What was encoded

**Tables (2)** — `src/lib/eval/regulation-tables-seed-iso46001.ts`, builder `iso46001SeedTables()`, edition `'2019'` (title page L29–L32 "ISO 46001" / "First edition 2019-07"; prod `standards.version` '2019 (ISO 46001:2019, First edition, 2019-07; BS ISO 46001:2019)'). The standard prints no numeric value table; both tables are informative catalogues:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABLEA1 | `area` = `<sector token>_<letter>` (`industries_a` … `offices_retail_other_b`) | `sector_token` (enum, 8), `sector_no` ("1." … "8."), `sector` (printed name; sector 8 = the two printed fragments "Offices or retail buildings, or any other building" + "not mentioned in items 1 to 7"), `letter` ("(a)" …), `description` (printed area) | anhaltswert (L888 "indicates areas within business activities where water use can be significant, and in which monitoring can be beneficial") | 39 (6 / 7 / 4 / 4 / 4 / 5 / 7 / 2 per sector — the letter sequence restarts at "(a)" per sector, asserted at build time) | imported_unverified |
| TABLED1 | `sector` (`wafer_fabrication`) | `sector_label` ("Wafer fabrication"), `indicator_text` ("Number of units produced") | anhaltswert (L1192 "Examples of business activity indicators" — L546 "identify specific business activity indicator(s) appropriate") | **1** of 24 printed (U-1 … U-23) | imported_unverified |

**Registers / selections / visibility** — `src/lib/eval/field-configs/iso46001.ts`:
- `water_streams` (-08 B): `label` (required), `role` enum (required, discriminator) over the seven Annex-C symbols `wd` / `r_other_source` / `output` / `rp` / `rnp` / `wp` / `rpp` with the printed legends as option labels, `volume_m3` (m³, required, min 0), `period`, `source_kind` (text, datalist = the printed examples L1086 + L801; row-scope `visible_when role IN {wd, r_other_source}`), `output_type` enum (the five printed classes 1) – 5), row-scope `visible_when role == 'output'`), `meter_location` (Annex C ii)), `meter_verified_on` (date, A.12), badge `balance_term` (Win / Wout / Recycling); footer `water_streams_count` / `Win_calc` / `Wout_calc` / `leak_indicator` / `recycling_streams_count` / `plant_recycling_rate_calc` / `process_recycling_rate_calc` / `meters_unverified`; note L1106–L1114 + L1100–L1104 + D-1 … D-16 / R-1 / G-4 / J-1.
- `objectives` (-03 D): `objective` (required), `what` / `resources` / `responsible` / `deadline` (date) / `evaluation_method` = §6.2.1 1) – 5); footer `objectives_count`. `legal_requirements` (-03 F): `requirement` (required), `source`, `review_interval`, `review_date` (date); footer count.
- `water_sources_46001` (-04 C): `source` (required), `source_class` enum utility / other_source (Annex C a) / b)), `kind` datalist, `m3_per_a` (optional, NO Σ — Win is produced once on -08, J-2); footer count. `significant_uses` (-04 C): `activity` (required, datalist = the 24 distinct Table-A.1 areas), `facility`, `variables`, `m3_per_a` (required), `improvement_potential`; footer `seu_count` / `seu_total` / `seu_share_max` (F-1). `indicators_46001` (-04 E): `indicator` (required, datalist = §3.4 EXAMPLE + the seeded D.1 cell), `unit`, `period`, `activity_value` (required), `water_used_m3` (required), derived `value = water_used_m3 / activity_value` (§3.33 per row), `baseline`, derived `delta_vs_baseline = value - baseline` (J-3); footer `indicators_count`.
- `industry_sector` (-04 D, created enum, select_one, 1 option = the seeded TABLED1 key) → `business_activity_indicator_hint` (-04 D, created text, `lookup_fill` TABLED1 by `sector`, role `value`, source label "Tab. D.1 (Beispiel)") — a TWIN beside the consumed, required text `business_activity_indicator` (D-26, amendment J).
- `baseline_adjustment_note` (-04 F, created text) `visible_when baseline_adjustment_trigger IS NOT NULL` (§6.2.7; the multi-select switch of the trigger is S-1).
- `targets` (-05 C): `target` (required), `function_level`, `time_frame` (REQUIRED — "Time frames shall be established"), `responsibility` / `means` / `improvement_verification` / `result_verification` = §6.3 1) – 4); footer `targets_count`.
- `designing_new_facilities` (-07 D, created boolean attestation, §8.2 scope) and `procurement_significant` (-07 E, created boolean, §8.3 scope); `procurement_criteria` / `supplier_informed` (-07 E, existing) `visible_when procurement_significant == true` (emitted — no consumer, no gate reads them); the `design_consideration` hide is refused by the gate-aware guard (CR-027) → G-1.
- `monitoring_items` (-09 C, created json, `select_many`, the seven §9.1 1) items with their printed English labels); `interested_parties_46001` (-01 B): `party` (required), `requirement`; `nonconformities` (-10 C): `nc` (required), `correction`, `cause`, `action`, `effectiveness_reviewed` (boolean), `evidence`, `closed` (boolean; unset = open); footer `nc_count` / `nc_open`.
- Section rules: none.

**Equations (20 new)** — `src/lib/eval/equations/iso46001.ts`: `ISO-46001-01-D1 interested_parties_count = count_rows(interested_parties_46001)` · `-03-D1 objectives_count` · `-03-D2 legal_requirements_count` · `-04-D1 water_efficiency_indicator_calc = past_present_water_use / business_activity_indicator_value` (§3.33 bound to the existing -04 inputs, K (3)) · `-04-D2 seu_total = sum_rows(significant_uses, m3_per_a)` · `-04-D3 seu_share_max = max_rows(significant_uses, m3_per_a) * 100 / sum_rows(significant_uses, m3_per_a)` · `-04-D4 seu_count` · `-04-D5 water_sources_count` · `-04-D6 indicators_count` · `-05-D1 targets_count` · `-08-D1 Win_calc = sum_rows(water_streams, if(role IN {'wd', 'r_other_source'}, volume_m3, 0))` (C.2 LHS) · `-08-D2 Wout_calc = sum_rows(water_streams, if(role == 'output', volume_m3, 0))` (C.2 RHS) · `-08-D3 leak_indicator = <Win expr> - <Wout expr>` (inlined, C.1 L1114) · `-08-D4 plant_recycling_rate_calc = (Σrp + Σrnp) / (Σrp + Σrnp + Σwd) * 100` (C.3 as printed) · `-08-D5 process_recycling_rate_calc = Σrp / (Σwp + Σrpp) * 100` (C.5 as printed) · `-08-D6 water_streams_count` · `-08-D7 recycling_streams_count = count_rows(water_streams, role IN {'rp', 'rnp', 'wp', 'rpp'})` (Annex C i) "if recycling is carried out") · `-08-D8 meters_unverified = count_rows(water_streams, meter_verified_on IS NULL)` (A.12) · `-10-D1 nc_count` · `-10-D2 nc_open = count_rows(nonconformities, closed == false)`. None outputs a prod symbol (C.1 / C.2b / C.3 / C.5 keep their verified rows; the four twins are amendment-J twins of consumed + gate-read quantities; R-1 proposes the retirement). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows`, the printed forms on chosen inputs): the 8-row chart (utility 1000 + rain 200 in; sewer 900 + evaporation 250 out; Rp 300 / Rnp 100 / Wp 500 / Rpp 300) → Win 1200, Wout 1150, difference 50, C.3 = 400 / 1400 × 100 = 28,5714 %, C.5 = 300 / 800 × 100 = 37,5 %, counts 8 / 4 / 6 without a meter check, badges 1 1 2 2 3 3 3 3; empty ⇒ Σ / rates / difference `manual_required`, counts 0; output-only ⇒ Win 0, difference −900, both rates "Division durch Null"; incomplete / negative rows excluded; utility 600 + Rp 400 ⇒ C.3 = 40 %, C.5 "Division durch Null", recycling count 1. §3.33 scalar 30 / "Division durch Null" / missing input. Significant uses 600 / 200 / 200 (+ one blank) ⇒ Σ 1000, largest share 60 %, count 3; zero Σ ⇒ "Division durch Null"; empty ⇒ `manual_required` / 0. Indicator rows 1200 / 40 with baseline 35 ⇒ 30 / −5; 500 / 100 ⇒ 5 / null; 500 / 0 ⇒ null / null (complete, no diagnostic). Counts: parties 2, objectives 2 of 3, legal 1, targets 1 of 2 (no time frame ⇒ incomplete), NCs 3 with 2 open (unset box = open), empty ⇒ 0.

## 6. Inventory §5 top-wins walk

1. Water-balance register (stream, type, m³) replacing the WD / R1–R3 / O1–O4 fixed slots; Win / Wout / leak check and the C.3 / C.5 rates derive from typed stream roles → **encoded** (`water_streams` on -08 with the role per row, `Win_calc` / `Wout_calc` / `leak_indicator` / the two rate twins / the counts; the nine fixed slots and the four verified equations stay until R-1 / D-1 … D-16 are ratified; CR-037 / CR-038 → G-4). ✓
2. Objectives register (§6.2.1 1) – 5) per row) replacing the single what / resources / who / when / how set, feeding CR-009 / CR-010 → **encoded** (`objectives` on -03 D, `objectives_count`; CR-009 → G-5 proposes `objectives_count >= 1`; the six scalars stay — D-17 … D-22). ✓
3. Compute `water_efficiency_indicator` from Win (or past_present_water_use) and `business_activity_indicator_value`, with sector-picked indicator text from Table D.1 → **encoded, Table D.1 partial**: `-04-D1` binds to `past_present_water_use` (K (3); the Win ≡ water-used question is X-2), the per-row §3.33 division sits in `indicators_46001`, `industry_sector` → `business_activity_indicator_hint` fills from TABLED1 — for the ONE settled row; the other 23 sectors follow U-1 … U-23 after the PDF check. ✓ (D.1: 1 of 24)
4. Targets / action-plan register (§6.3) with verification method per row → **encoded** (`targets` on -05 C with the required time frame and the 1) – 4) action-plan columns, `targets_count`; the four scalars stay — D-32 … D-35). ✓
5. Inherit site water withdrawal / consumption from VSME B06 / ISO-59020 water block → **Phase 6** (X-1).

Data-quality gaps of the inventory: "17 `attest_*` booleans duplicate CR gates" → untouched (the gates are attestations by design; G-5 replaces one, CR-009, by a count); "unit '-' on most fields" → untouched (prod hygiene); "`business_activity_indicator_value` has no unit" → the register carries a `unit` column per indicator row; "Table D.1 pairing must be re-verified from the PDF" → exactly the U-1 … U-23 mechanism; "CR-036 / CR-039 / CR-040 have empty conditions" → G-3.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`iso46001` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (iso46001 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+83 blocks, +7 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 22)" paragraph), `tests/harness/iso46001-verify.integration.test.ts` (the one stale CR-037 pin, §2).
Created: `src/lib/eval/regulation-tables-quotes-iso46001.ts` (86 spans, generated), `src/lib/eval/regulation-tables-seed-iso46001.ts`, `src/lib/eval/field-configs/iso46001.ts`, `src/lib/eval/field-configs/iso46001.prior.json` (captured), `src/lib/eval/equations/iso46001.ts`, `src/lib/eval/__tests__/regulation-tables-seed-iso46001.test.ts`, `src/lib/eval/__tests__/field-configs-iso46001.test.ts`, `src/lib/eval/__tests__/equations-iso46001.test.ts`, `src/components/worksheet/__tests__/register-iso46001-streams.test.tsx`, `scripts/verification/iso46001-STAGED-plan3-rulings.sql` (83 ☐ blocks; 897 lines, 0 non-comment lines), `scripts/migrations/20260917102200_regulation_tables_seed_iso46001.sql` + `scripts/rollback-20260917102200-regulation-tables-seed-iso46001.sql`, `scripts/migrations/20260917102210_field_configs_iso46001.sql` + `scripts/rollback-20260917102210-field-configs-iso46001.sql`, `scripts/migrations/20260917102220_equations_iso46001.sql` + `scripts/rollback-20260917102220-equations-iso46001.sql`, this report (+ the copy at `.superpowers/sdd/…/task-22-report.md`).
Untouched: every other standard's file; `scripts/reasoning-map/` (the unit run churns files there — checked out before staging); the three scratch probes deleted; the three generators (quotes, STAGED + sheet section, fragment checker) and the playbook paragraph live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **TABLED1: 1 row, not ≈ 20** — the layout file settles only the first pairing (§3); the controller's amendment (2) makes the `industry_sector` select equal the seeded keys (1 option); U-1 … U-23 carry the print-order proposal per row with the INSERT + enum-append SQL. The alternative (a 24-option select over the printed SECTOR column now, indicator cells on ratification) is recorded in the observations, not chosen.
2. **`water_streams.source_type enum [drinking, reclaimed, alternative]` → `source_kind` text datalist** (the printed examples L1086 / L801 are open "e.g." lists; the closed printed pair a) / b) is already the role) + **`output_type` enum** (the five printed output classes 1) – 5), rule 3b) — the -04 sources register carries the a) / b) pair as `source_class`.
3. **Role tokens = the brief's proposal, confirmed against the print with a delta** (J-1): only Win / Wout / Rp / Rnp / Wp / Rpp carry printed legends; WD / R1 … R3 / O1 … O4 are printed in (C.2) only (grep in §3), (C.4) prints R1 … R4 as recycled streams.
4. **`-04-D1` reads `past_present_water_use`, not `Win_calc`** (amendment K (3): bind to the existing -04 input; `Win_calc` is a -08 symbol not inherited on -04 — the reverse direction is what prod has); Win ≡ water used is X-2.
5. **`indicators_46001` computes §3.33 per row** (`activity_value` + `water_used_m3` → derived `value`) instead of the brief's typed `value`; `delta_vs_baseline` added (J-3).
6. **`targets` columns follow the printed 1) – 4)** (`responsibility` / `means` / `improvement_verification` / `result_verification`; `time_frame` required); the brief's `action_plan` / `improvement_verification_method` keys would shadow the -05 prod symbols (lint).
7. **`water_sources` → `water_sources_46001`** (the prod text field exists; O-1). `procurement_criteria` / `supplier_informed` are on **-07 E**, not -06 (capture); CR-028 is the -06 attestation → G-2 + C-1.
8. **`design_consideration` hide NOT emitted** (gate-aware guard, CR-027 — pinned) → G-1 with the created driver.
9. **Equations 20 (brief 6 rows / ≈ 10 outputs)**: the brief's -08-D1 bundle is three rows (Win_calc / Wout_calc / leak_indicator, the difference inlined), -08-D6 … D8 counts added (the G-4 / G-3 guards need them), -04 has six rows (the sources / indicators counts), -01 / -10 counts added; no per-symbol Σ twins (amendment K).
10. **Sign-off ids 83 (brief ≈ 7)**: G-1 … G-6 (the brief's G-1 … G-4 kept where they exist; G-5 = the attestation gates beside the registers, G-6 = CR-017 conditional on D-29), C-1 / C-2, R-1, 40 D-pairs (amendment K), J-1 … J-4, X-1 / X-2, S-1, F-1 / F-2, I-1, O-1, U-1 … U-23 (one per unsettled D.1 row, amendment (2)).
11. **A.1 seeded as a catalogue table AND used** — as the `significant_uses.activity` datalist (24 distinct printed areas); no register keys on it (a `lookup_key` would restrict an informative "can be monitored" list).
12. **Harness: one stale pin fixed** (§2) — the only edit outside this task's own files besides the shared registries.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (40/40); every seeded cell fragment is asserted inside its row span at build time (whitespace-collapsed); every `"…" (L…)` fragment on the sheet and in the STAGED file was machine-checked against the transcript (236/236); the spans, the sheet section and the STAGED file were generated from line ranges (never retyped); the STAGED gate conditions are the CAPTURED strings wrapped in the IF guard; prod facts (tokens, consumer lists, section codes, gate / equation ids + md5, labels, units, `is_required`, the 0 / 0 stored-value / instance counts, `standards.version`) come from the in-session capture / queries listed in §0 / §4 and the STAGED header.
- The field-config test pins every refusal of the brief's targets (`design_consideration` ← CR-027; `plant_recycling_rate` consumed; `WD` producer-chained to Win → -09; the -07 rules read no gate; the three empty gates never refuse; the four prod equations captured; the 40 amendment-K pairs against the capture; no column key shadows a prod symbol; no quoted literal collides (25 expressions walked); D-1 no `enum_values` UPDATE).
- Runtime facts were probed through the real register / formula / condition contracts before the pins (probes deleted); the render test drives `water_streams` (two rows, a typed balance, a new recycling row) and `objectives` through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file except marker-checked `split/join`-style splices that throw on a missing / duplicate marker (the three registry lines, the D-number renumbering, the column renames, the test pin) and two literal `sed` renames of span keys (`L1202_1220` → `L1202_1216`, `L570_571` → `L570_572`, verified by `grep -c`); the seed / field-config / equation modules and the harness pin were written with the Write / Edit tools; the bash-heredoc trap (m820_3 trap 7) bit once on the harness edit (backticks collapsed → marker miss, nothing written) — redone with the Edit tool.
- `git checkout -- scripts/reasoning-map/` run before staging; `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section, the playbook paragraph and the one harness `describe`.

## 10. Concerns (for the controller)

1. **Table D.1 is 1 / 24** by the letter of the brief and amendment (2); the 23 U-blocks make the ratification mechanical but the `industry_sector` select is a one-option select until then. If the owner prefers the 24-option sector select now (every label a verbatim printed cell, no pairing asserted), it is a one-line change to `INDUSTRY_SECTORS` + a re-emit (recorded in the observations).
2. **Nothing computed reaches prod's gates yet** — every gate block (G-1 … G-6) and the retirement (R-1) are STAGED; until ratified CR-037 / CR-038 keep checking the fixed-slot scalars, CR-027 / CR-028 stay unconditional, and the registers are documentation beside the scalars. Fail-safe by construction.
3. **40 amendment-K pairs** — the register model twins every ISO-46001 scalar of a prod model that asks each fact once; each is one ☐ (data-free today). D-1 … D-12 can only retire inside R-1 (inputs of the verified equations); D-13 … D-16 / D-26 / D-29 are twins of consumed / gate-bearing quantities.
4. **I-1 / G-6** — the §3.33 scalar (`-04-D1`) is not server-materialised; CR-017 onto the twin is conditional on a [CODE] materialiser change or on D-29 moving the quantity into the register row.
5. **J-1** — the role meanings of WD / R / O rest on the printed input / output lists and prod's EV labels (the figure is an image in the txt); the SR-3 PDF-page check of Figure C.1 / C.2 is the closing action.
6. **Harness pin** — the CR-037 `Win == Wout` trap pin was stale at HEAD (fails 1/55 without this task); fixed to the T-13b behaviour. If the controller prefers the harness untouched by a standard task, revert that one `describe` and record the red as pre-existing.
7. **English cells beside German labels** (Table A.1 areas, Table D.1 cells, the §9.1 items, the role legends) until the owner rules on a German source (SR-3).
8. **Token/time:** ≈ 0.55 M tokens in, ≈ 70 min wall-clock — in the Task 20 / 21 band; the cost sits in the 40 D-pairs, the 23 U-blocks and the six gate rewrites, not in the transcript (40 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§6.2.1 a) – f) per objective (consistent with the policy, measurable, monitored, communicated, updated) — no per-row attestations on `objectives`; §6.2.2 a) – d) water efficiency management plan contents — the text `water_efficiency_management_plan` only; §6.2.4 c) "segregating the used water streams with the objective of water recycling" and 3) "prioritize and record opportunities" — `improvement_potential` per significant use only, no priority rank; §6.2.4 "estimate future water use" — the prod scalar `future_water_use_estimate` (untouched); §6.3 a) – c) considerations when setting targets — not encoded; §8.2 "results of the design activity, verification and any necessary action … shall be documented" — the boolean `design_consideration` only; §8.3 "define and document water efficiency purchasing specifications" — `procurement_criteria` text only; §9.1 2) – 4) (methods, significant deviations, compliance evaluation) — the prod fields `monitoring_plan` / `significant_deviation` / `compliance_evaluation` (untouched); §9.2 internal audit programme items a) – e) and §9.3 management-review inputs a) – d) / outputs 1) – 5) — `audit_programme` / `management_review` only; A.12 monitoring-system items 1) – 5) and the data-collection frequency — not encoded; Annex C i) "recycling streams from source to destination" — the role per row, no source → destination pair (a `reference` column is the G-10 gap); Annex B scenarios — prose examples, nothing to encode. Each is a procedural sentence, an attestation prod already carries, or a list whose per-row encoding is a follow-up, and stays outside this encoding or on the sign-off sheet.

---

# Fix round 1 (review verdict on `86511b0`: "Approved with minors" — the four items applied verbatim)

- **Commit:** see the reply (one commit on top of `86511b0`, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; no migration changed (the seed / field-config / equations modules are untouched — the STAGED file and the sheet section were regenerated from the patched generator, the fragment checker re-run).
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling. `git stash` NOT used (controller rule).

1. **§3 `drinking` grep count corrected 13 → 12** — re-run in-session, the 12 lines listed with their clauses (above).
2. **J-3 amendment-O evidence added** — `grep -n -i "baseline" "<transcript>" | grep -E "Formula|="` → empty, exit 1 (plus the 11 plain "baseline" lines); cited in §3 and inside the J-3 block (STAGED + sheet).
3. **U-1 … U-23 cite the EXACT raw line of each cell** — the generator now walks both column dumps cell by cell (`locate`): every block names the sector cell's raw line and its ordinal on that line (e.g. "Semiconductor" = the 2nd cell on L1194; "Hotels" the 1st and "Prisons" the 2nd cell on L1200) and the indicator cell's raw line and ordinal (cell 2 = L1203; cells 14–19 = the 1st … 6th cell on L1215; cells 20–24 = the 1st … 5th cell on L1216); the title of each block carries both lines. The generator throws if a cell is not found in order (24 / 24 located).
4. **G-4 wording** — the CR-037 proposal is stated as a STRICT EQUALITY of the two register sums (any non-zero `leak_indicator`, excess or shortfall, warns) and the L1114 alternative is spelled out in full: `IF water_streams_count >= 1 THEN Win_calc <= Wout_calc` (≡ `leak_indicator <= 0`, only an EXCESS of input warns) — recorded, not chosen (the printed (C.1) is the equality).

Raw output (once, before the commit): `node gen-iso46001-staged.mjs` → blocks: 83 (unchanged ids); `check-iso46001-frags.mjs` → **236/236 verbatim**; STAGED non-comment lines 0; `pnpm vitest run --project unit <the four iso46001 files + generated-sql-freshness>` and `pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso46001 "<transcript>"` → 40/40; `pnpm test` once before the commit — the lines are in the reply.
