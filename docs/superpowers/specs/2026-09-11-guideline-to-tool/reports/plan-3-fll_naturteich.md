# Plan 3 — FLL-Naturteich (`fll_naturteich`) — Task 8 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 9)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `a2dcf19`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs FLL-Naturteich fll_naturteich` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 15 worksheet titles, the section codes / titles / field counts of eight worksheets, the 6 equation rows with formula + md5, the 33 compliance rows with ids / severities / md5 — the six conditions longer than 105 chars read in 105-char `substr` chunks —, the `information_schema` column lists of `equations` / `compliance_requirements`, the labels / units / `validation_rules` of the FLLNT-03 / -06 fields). `.env.local` never printed; confirmed gitignored (`git check-ignore .env.local`).
- **SR-1 source:** every seeded value and every cue was read in this session from the PRIMARY transcript `C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Guidelines natural pool.md` (2nd edition 2017, English translation; 4494 lines, flattened PLAIN TEXT — printed rows are split over several lines and hyphenated, the wide Table 1 has its five type columns interleaved); the 91 quote spans were lifted mechanically by line range (a throwaway `gen-quotes.mjs` in the scratchpad, `slice/join` + `JSON.stringify`, never `String.replace`), the seed file assembled by `cat` head + generated spans + builders; every `*_printed` cell is asserted verbatim inside its lifted span by `cell()` at build time. The SECONDARY `C:\Users\Ekowai\Desktop\FLL Guidelines PDF\FLL-Naturteich-2017_pdftotext.txt` was consulted for the Table-1 column assignment the brief names (P1207–P1250) and — recorded as fll_naturteich-J-2 — for the column span of the single-cell rows of Tab. 10 / 11 / 12 (P2418–P2455, P2495–P2535); those three tables stay `imported_unverified`. The inventory was used for pointers only (its Table-1 block matches; its "TABLE7 split per parameter" and "one `TABLE2_TYPE1` row" were superseded by the transcript — §8). The PDF was not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `fll_naturteich` | 11 | 38 (38/38 verbatim) | 0 rows skipped, 0 OCR cells; 3 tables kept `imported_unverified` (TABLE10 / TABLE11 / TABLE12 — column span of the single-cell rows assigned from the pdftotext layout, J-2); TABLE1 `md_verified` with the column assignment from the layout (J-1); Tab. 2–6 feature matrices, Tab. 13, Tab. 14, Tab. 16 not seeded (residue) | 5 (4 `create` + 1 UPDATE of the Plan-1 `plant_species_list`) | 1 (`create` `substrate_role`) | 0 | 18 (all `create`: 4 Tab. 1 on -03, 2 Tab. 8 on -04, 6 Tab. 9 on -05, 6 Tab. 10 on -09) | 4 (2 on created fills + 2 UPDATE `filter_water_column` / `filter_kf`) | 0 (fix round 1 — the type-driven hide of -09 / -10 is STAGED as C-3, all nine sections per worksheet) | 11 (all register-fed) | 0 replacements emitted; R-1 … R-5 (total area, EQ-PUWS, EQ-02 / EQ-03, EQ-04, EQ-05 + REQ-33), 7 gate blocks (G-1 … G-7), 3 consumer blocks (C-1 … C-3), E-1, D-1 / D-2, S-1 STAGED | 32 (+10 observations) | ≈ 0.62 M (tool-marker delta 15.00 M → 14.38 M at report time) | ≈ 70 |

Field entries: 37 = 34 `create` (4 registers, 1 select_one, 18 lookup_fill, 11 derived outputs) + 3 UPDATE (`FLLNT-10 filter_water_column` / `filter_kf` visible_when; `FLLNT-12 plant_species_list` register upgrade). Migration: 34 `INSERT … WHERE NOT EXISTS`, 3 `UPDATE fields`, 0 `UPDATE worksheet_sections` (fix round 1); the rollback deletes the 34 `Plan 3:` rows and restores the 3 rows to the captured NULLs.

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  274 passed | 1 skipped (275)
      Tests  2599 passed | 1 expected fail | 1 skipped (2601)
   Start at  01:32:18
   Duration  42.32s (transform 14.08s, setup 45.59s, import 97.85s, tests 40.19s, environment 176.38s)
```
(2575 at `a2dcf19` + 24 from this task: seed-fll_naturteich 8, field-configs-fll_naturteich 6, equations-fll_naturteich 7, register-fllnt-filtereinheiten 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → first run 1 `no-unused-vars` warning (`Q_L2018_2022` imported but unused in `field-configs/fll_naturteich.ts`) → the span became the third cue of `substrate_role`, migration re-emitted → second run exit 0, no output (the whole-suite run above followed the re-emit).

Integration harness (embedded PG; the whole FLL-NT set the brief names — 14 files: `seed-fll-naturteich.smoke`, `fllnt02/11/12-verify`, `verify-fll-naturteich-fllnt01/03/05/06/07/09/10/13/14/15`; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/seed-fll-naturteich.smoke.integration.test.ts tests/harness/fllnt02-verify.integration.test.ts tests/harness/fllnt11-verify.integration.test.ts tests/harness/fllnt12-verify.integration.test.ts tests/harness/verify-fll-naturteich-fllnt01.integration.test.ts tests/harness/verify-fll-naturteich-fllnt03.integration.test.ts tests/harness/verify-fll-naturteich-fllnt05.integration.test.ts
 Test Files  7 passed (7)
      Tests  41 passed (41)
   Duration  8.70s (transform 2.97s, setup 0ms, import 18.80s, tests 38.96s, environment 1ms)
pnpm vitest run --project integration tests/harness/verify-fll-naturteich-fllnt06.integration.test.ts tests/harness/verify-fll-naturteich-fllnt07.integration.test.ts tests/harness/verify-fll-naturteich-fllnt09.integration.test.ts tests/harness/verify-fll-naturteich-fllnt10.integration.test.ts tests/harness/verify-fll-naturteich-fllnt13.integration.test.ts tests/harness/verify-fll-naturteich-fllnt14.integration.test.ts tests/harness/verify-fll-naturteich-fllnt15.integration.test.ts
 Test Files  7 passed (7)
      Tests  61 passed (61)
   Duration  8.95s (transform 3.29s, setup 0ms, import 14.07s, tests 45.92s, environment 2ms)
```
(14 files / 102 tests in two foreground batches on 2026-09-18.) The harness seeds prod's fields / gates / equations (`tests/harness/seed-fll-naturteich.ts`); the new rows are not in it by construction — it proves the branch still enforces every FLL-NT gate both ways where drivable.

**TDD evidence.** Runtime facts were established BEFORE encoding with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` on the seeded TS tables (scratchpad `rt-check.ts`, kept out of the repo): a row `lookup('TABLE7', 'all', …)` with a LITERAL key computes; a row `derived` reading a worksheet symbol not in scope (`natural_pool_type` on -04) is null and makes `count_rows(wasserproben, ok == 0)` `manual_required` ("Fehlende Eingabe für count_rows(): ok"), while the same row with `type_IV` in scope decides (P 0.02 > 0.01 → violation); `if(x IS NULL, 0, x)` guards optional cells; a nested `if()` on the discriminator short-circuits a null enum cell; the share formula computes 57.14 and the submerged share divides by zero (`manual_required`) without a regeneration row; an empty register is `manual_required`; the `filtereinheiten` branches compute per kind (layer minima 10 / 50 / 40 / null / 0, feed limits 5 / 15 / 8 / null / 0) and the App.-5 Example-1 row yields **6300 m²**; a scalar `lookup('TABLE1', natural_pool_type, …)` computes 30 for type III and is `manual_required` on the null cell of type IV. Seed builders: the `cell()` assertion caught the only slip before any test existed — Tab. 12 prints "Individual verifi- cation" where Tab. 11 prints "Individual verifica- tion" (the printed fragment is now a per-table parameter, both pinned). Seed test written → RED on my own `makeTableLookup` signature slip (positional keys, not a keys object — the Task-7 slip repeated) → 8/8 GREEN; verifier **38/38** on its first run. Field-config test 6/6, equations test 7/7 and render test 3/3 GREEN first run (the emitter accepted all 37 entries + 11 sections on its first run because the Step-4 rule set had been planned against the capture's `consumer_worksheets` + `equations` before emitting; the test pins each withheld target's refusal message and walks every emitted rule through `producerChain`).

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts fll_naturteich "C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Guidelines natural pool.md"` → **`38/38 quotes verbatim`**, exit 0. PASS list (table · rows · span):

- TABLE1 (5): type_I … type_V — the whole printed Tab.-1 body L1105–L1220 as the quote of every row (transposed / interleaved table; cells asserted inside it)
- TABLE2_SUBMERGED (2): type_I (L1273–L1276), type_II (L1360–L1363)
- TABLE7 (1): all — Tab. 7 body L1906–L1932 · TABLE8 (1): all — Tab. 8 body L1940–L1971 · TABLE8_P (5): type_I … type_V — the same Tab.-8 body
- TABLE9 (3): filter_iii, filter_iv, plant — Tab. 9 body L2029–L2065
- TABLE10 (2): submergent, emersed — Tab. 10 body L2589–L2634
- TABLE11 (3): vertical_continuous_overflow, vertical_no_overflow, horizontal — Tab. 11 body L2668–L2753 · TABLE12 (3): same tokens — Tab. 12 body L2790–L2862
- TABLE15 (9): 4_6 … 22_32 — one printed line each (L4113–L4121)
- S10_4_3 (4): submerged (L3051), marsh_small (L3052), marsh_medium (L3053), lilies (L3054)

Status per table: `md_verified` — TABLE1 (J-1), TABLE2_SUBMERGED, TABLE7, TABLE8, TABLE8_P, TABLE9, TABLE15, S10_4_3 (every seeded row lifted, every cell legible in the md). `imported_unverified` — TABLE10, TABLE11, TABLE12 (J-2: the md prints the single-cell rows once and the horizontal column's "Individual verification" once per parameter group; the assignment to the two vertical columns rests on the pdftotext layout; a PDF look at pp. 47 / 50 / 52 flips them).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs FLL-Naturteich fll_naturteich
  → wrote src\lib\eval\field-configs\fll_naturteich.prior.json: 130 field rows (15 orphan), 135 coded sections of 135, 6 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts fll_naturteich
  → wrote seed + rollback for fll_naturteich -> scripts/migrations/20260917100800_regulation_tables_seed_fll_naturteich.sql scripts/rollback-20260917100800-regulation-tables-seed-fll-naturteich.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts fll_naturteich 20260917100810
  → wrote 37 field entries + 0 section entries for fll_naturteich -> scripts/migrations/20260917100810_field_configs_fll_naturteich.sql scripts/rollback-20260917100810-field-configs-fll-naturteich.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts fll_naturteich 20260917100820
  → wrote 11 equations for fll_naturteich -> scripts/migrations/20260917100820_equations_fll_naturteich.sql scripts/rollback-20260917100820-equations-fll-naturteich.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-fll_naturteich.test.ts` / `equations-fll_naturteich.test.ts` pin the other two against the committed `fll_naturteich.prior.json`). The seed migration carries 38 row upserts (`ON CONFLICT (table_id, row_key)`) and 8 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the eleven tables (no earlier FLL-Naturteich table seed exists — nothing superseded; the Plan-1 `20260911120000_selection_configs_FLL_Naturteich.sql` touches `fields.widget/ui_config` of `equipment_elements_list` / `plant_species_list` only and is untouched — the Plan-3 UPDATE of `plant_species_list` writes the same row (widget / ui_config, the Plan-1 keys kept in the superset config) — applied AFTER the Plan-1 file it supersedes it, as in the m1200_3 precedent; the Plan-3 rollback restores the captured prior, i.e. NULL, not the Plan-1 config). The equations rollback deletes the 11 `Plan 3:` rows. Apply order (owner, after the schema migration incl. the plan1-D-3-1 legacy rename; the Plan-1 selection configs step 3 BEFORE the field configs): `20260917100800` → `20260917100810` → `20260917100820`; rollback in reverse. Every file touches `FLL-Naturteich` rows only.

## 5. What was encoded

**Tables (11)** — `src/lib/eval/regulation-tables-seed-fll_naturteich.ts`, builder `fllNaturteichSeedTables()`, edition `'2017'` (imprint L236–L237 "2nd edition 2017 (German), English Translation 2023, 50 copies and download version, Bonn, May 2024"; preface L477–L479 "an accurate translation of the second edition … issued in 2017" = prod `standards.version` "2017 (2nd edition, German); English translation 2023/2024"):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABLE1 | pool_type (prod `natural_pool_type` tokens) | regeneration_share_min_pct 50 / 50 / 30 / – / –, share_comparator '>', share_printed, share_ref (Tab. 5 / Tab. 6), flow_type none / none / slow / quick / system + flow_printed, filter_operation, p_binding_required (III), substrate_filter (III, IV), hydrobotanical (I, II, III), regeneration_technique, technical_name (all `*_printed` cells verbatim) | locked (L1078–L1081 §5 "distinguished … in accordance with Table 1" — L1207–L1212) | 5 | md_verified (J-1, J-3) |
| TABLE2_SUBMERGED | pool_type (type_I, type_II) | regeneration_share_min_pct 50, submerged_share_of_regeneration_min_pct 50 ("therefrom at least half"), printed | locked (L1233–L1234) | 2 | md_verified |
| TABLE7 (p. 32) | source ('all') | ammonium_max 0.5, iron_max 0.2, p_total_max 0.03, hardness_min 1.0 (+ 5.6 °dH), conductivity_max 1000, manganese_max 0.05, nitrate_max 50.0, orthophosphate_max 0.01, ph_min 6.0, ph_max 9.0, acid_capacity_min 2 (+ 5.6 °dH), printed | anhaltswert (L1904 caption — L1973–L1978) | 1 | md_verified |
| TABLE8 | source ('all') | ammonium_max 0.3, hardness_min 1.0, conductivity_max 1000, nitrate_max 30.0, nitrite_max 0.01, ph_min 7.0, ph_max 9.0, acid_capacity_min 2, printed | anhaltswert (L1938 — L1973–L1978) | 1 | md_verified |
| TABLE8_P | pool_type (prod tokens) | p_total_max / orthophosphate_max 0.03 (I–III) / 0.01 (IV, V), printed | anhaltswert (same) | 5 | md_verified |
| TABLE9 (p. 34) | substrate_role (created: filter_iii / filter_iv / plant) | grain_max_mm 16 / 32 / 8, oversize_max_pct 15 / 15 / –, fines_max_pct 2 / 0.5 / –, kf_min 1e-4 / 1e-3 / –, frost_resistance_required + frost_printed, elutable_p_max 5 / 5 / –, column_head | locked (L2018–L2022; O-1) | 3 | md_verified |
| TABLE10 | hydrobot_type (prod tokens) | water_column_min_cm 80 / 10, water_column_max_cm – / 50, substrate_min_cm 10 / 10, substrate_max_cm 20 / 30, grain_max_mm 8, feed_rate_qmax 5, printed | locked (L2587 — L2580–L2582) | 2 | imported_unverified (J-2) |
| TABLE11 | flow_direction (prod `filter_flow_direction` tokens) | water_column_min_cm 10 / – / –, layer_min_cm 40, grain_max_mm 16, oversize_max_pct 15, fines_max_pct 2, frost true, kf_min 1e-4, tolerance_pct 10, feed_rate_qmax 5 / 8 / –, individual_verification (horizontal: every numeric cell null), printed | locked (L2666 — L2658–L2661) | 3 | imported_unverified (J-2) |
| TABLE12 | same | water_column_min_cm 0 / – / –, layer_min_cm 50, grain_max_mm 32, fines_max_pct 0.5, kf_min 1e-3, feed_rate_qmin 15 / 15 / –, individual_verification | locked (L2788 — L2784–L2787; O-2) | 3 | imported_unverified (J-2) |
| TABLE15 | grain_class (4_6 … 22_32, created register column) | pore_water_pct 43 … 47, surface_m2_m3 1400 … 250, grain_printed | anhaltswert (L4111 — L4122) | 9 | md_verified |
| S10_4_3 | plant_group (submerged / marsh_small / marsh_medium / lilies, created register column) | density_min / density_max 6–10 / 3–5 / 5–7 / –, pot_size "P 0.5 (500 cm3)", printed | anhaltswert (L3049–L3050 — L3055) | 4 | md_verified |

Not seeded: Tab. 2–6 beyond the share cells (per-type feature matrices, qualitative), Tab. 13 (operation per type — no driver field; `inspection_frequency` is free text), Tab. 14 (eluate sample sizes — no field), Tab. 16 (trophic overview — informative), App. 6 (evaporation — informative).

**Registers / selections / lookup_fills / visibility** — `src/lib/eval/field-configs/fll_naturteich.ts` (created fields land in the captured sections C / D of their worksheet):
- `zonen` (-06): label, `zone` (swimming / regeneration / supplementary — §3 definitions L735–L738 / L765–L768 as labels; discriminator), area_m2, depth_m, ground_area_m2 / wall_area_m2 (App. 5), `technique` (the prod `regeneration_technique` tokens, required for regeneration rows), `underwater_m2` derived; footer the four -06 outputs.
- `wasserproben` (-04): date, `location` (fill / swimming; discriminator), ten parameters (iron / manganese fill-only, nitrite swimming-only) + pH, per-parameter `*_ok` badges reading TABLE7 (literal key) / TABLE8 (literal key) / TABLE8_P (by `natural_pool_type` — inert until C-1), `sample_ok`; footer `sample_count`, `sample_violations`.
- `filtereinheiten` (-10): label, `unit_kind` (hydrobotanical / substrate_filter / technical; discriminator), `hydrobot_type` (prod tokens, enum — NOT a lookup_key, so the override toggle follows Tab. 15), `flow_type` / `flow_direction` (prod tokens), area_m2, layer_cm, `layer_min` / `layer_ok`, `grain_class` (TABLE15 lookup_key) → `surface_m2_m3` (lookup_value, `anhaltswert` override with the `surface_override` flag + reason), `colonized_manual_m2` (technical units, manufacturer), `colonized_m2` (EQ-02's printed form per row), feed_rate, `feed_limit` / `feed_ok`; footer `filter_colonized_surface_total`, `filter_feed_violations`.
- `ueberlaufeinrichtungen` (-11): label, `type` (rigid / flexible — L2903–L2904), edge_length_m, tolerance_mm, `tol_limit` = if(edge ≤ 1, 1, 2), `tol_ok`; footer `overflow_edge_length_total`, `overflow_tolerance_violations`.
- `plant_species_list` (-12, UPDATE of the Plan-1 register): the Plan-1 keys `art` / `zone` / `anzahl` kept (stored rows survive), `plant_group` (S10_4_3 lookup_key) → `density_min` / `density_max` (lookup_values), area_m2, `density` (the engineer's pick, SR-2), `in_range` badge, `count` = area · density; footer `plant_count_total`.
- Select: `substrate_role` (-05, the three Tab.-9 column heads as labels).
- lookup_fills (role limit unless noted): -03 `regeneration_share_min_pct` (TABLE1, hidden for IV / V), `regeneration_share_tab1_text` / `flow_tab1_text` / `filter_operation_tab1_text` (text, role value — the printed Tab.-1 cells); -04 `swimming_p_total_limit` / `swimming_orthophosphate_limit` (TABLE8_P by `natural_pool_type`, "Schlüssel fehlt" until C-1); -05 `grain_size_max_tab9` / `oversize_max_tab9` / `fines_max_tab9` / `kf_min_tab9` / `elutable_p_max_tab9` + `frost_resistance_tab9` (text) by `substrate_role`; -09 `hydrobot_water_column_min_tab10` / `_max_tab10` (emersed only) / `hydrobot_substrate_min_tab10` / `_max_tab10` / `hydrobot_grain_max_tab10` / `hydrobot_feed_qmax_tab10` by `hydrobot_type`.
- Field `visible_when` (4): `regeneration_share_min_pct` ← `natural_pool_type IN {'type_I', 'type_II', 'type_III'}` (L1559–L1560 "% indication irrelevant"), `hydrobot_water_column_max_tab10` ← `hydrobot_type == 'emersed'` (L2607), `filter_water_column` / `filter_kf` ← `filter_flow_direction != 'horizontal'` (L2695–L2699 / L2737–L2741 "Individual verification"). Section rules: NONE (fix round 1) — the producer sections B / C / D (-09) and B / C / D / F (-10) are refused by the emitter and the driver-free sections A / F / J / K / L / M hold 0 fields (a rule there is not observable: `worksheet-form.tsx` renders only sections with a visible field); the type-driven hide of both worksheets (Tab. 1 regeneration-area column L1135–L1147; L2580–L2581) is STAGED as C-3 with all nine section UPDATEs per worksheet. Withheld: `p_binding_required`, `splash_water_tank_volume` (C-2); the brief's `drainage_*` (no such field).

**Equations (11 new, all register-fed)** — `src/lib/eval/equations/fll_naturteich.ts`: `FLLNT-06-D1 total_pool_area_calc = sum_rows(zonen, area_m2)` · `-06-D2 regeneration_share_calc` = Σ regeneration · 100 / Σ (swimming + regeneration) · `-06-D3 pool_underwater_surface_calc = sum_rows(zonen, underwater_m2)` · `-06-D4 submerged_hydrobot_share_calc` · `-10-D1 filter_colonized_surface_total = sum_rows(filtereinheiten, colonized_m2)` · `-10-D2 filter_feed_violations = count_rows(filtereinheiten, feed_ok == 0)` · `-11-D1 overflow_edge_length_total` · `-11-D2 overflow_tolerance_violations` · `-12-D1 plant_count_total = sum_rows(plant_species_list, area_m2 * density)` · `-04-D1 sample_violations = count_rows(wasserproben, sample_ok == 0)` · `-04-D2 sample_count`. None outputs `pool_underwater_surface`, `filter_colonized_surface_actual`, `filter_50x_rule_met`, `filter_volume_required`, `overflow_edge_length` or `splash_water_tank_volume` (the six verified prod rows; R-2 … R-5 / G-6 STAGED). Pinned values (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): zones 30 / 20 / 20 / 10 → 80 m², 57.14 %, 95 m² (App. 5 Ex. 2), 50 %; 49 / 51 → 51 (the strict "> 50"); no regeneration row → "Division durch Null"; empty register → "Keine vollständigen Zeilen"; filter rows → layer minima [10, 50, 40, null, 0], surfaces [–, 600, 1200, 1200, –], colonized [0, **6300**, 4800, 4800, 500] (App. 5 Ex. 1), feed limits [5, 15, 8, null, 0], `feed_ok` [1, 1, 0, 1, 1] → Σ 16400, 1 violation; a quick filter at 12 < Qmin 15 and an emersed layer of 35 cm → 2 violations / two layer badges 0; overflow rows 0.3 (L2912) / 2 / 0.8 / 1 m → Σ 4.1 m, one ±1-mm violation; plants 10·8 + 5·4 + 4·1 + 2·9 = 122 with the lilies row in range and the 9 / m² marsh row flagged; a Plan-1 row (art / zone / anzahl only) is incomplete and not counted; fill samples → 0 / 3 violations, a swimming row without the type → `manual_required`, with type IV → 1 (P 0.02 > 0.01), with type I → 0; nitrite 0.02 → violation on a swimming row only.

## 6. Inventory §5 top-wins walk

1. `natural_pool_type` as master selector → **partially encoded**: the Tab.-1 share minimum (number, hidden for IV / V), the printed share / flow / filter-operation cells (text fills) on -03; the Tab.-8 P limits by type on -04 (inert until C-1). NOT delivered: the hide of the non-applicable worksheets -09 / -10 per type is STAGED (C-3 — every field-bearing section holds a consumed producer) and the P flag's visibility is C-2; `filter_flow_type` stays manual with the printed "Flow" cell beside the type (D-1).
2. Filter / hydrobotanical units as repeatable rows → **encoded** (`filtereinheiten`: technique + flow direction per row, Tab. 10 / 11 / 12 limits as read-only derived cells, Tab.-15 grain class → surface with a justified product override, colonized surface per row = EQ-02's printed form, Σ across rows, feed-rate violations); EQ-01's 50×-rule onto the Σ is G-6, EQ-02 / EQ-03 replacement R-3.
3. Zones as repeatable areas → **encoded** (`zonen` with Σ, the water-area share, the underwater surface from ground + walls, the submerged share for I / II); replacing the four typed totals and EQ-PUWS is R-1 / R-2; the supplementary-area definition is J-4.
4. Water analyses as dated rows per location with Tab. 7 / 8 targets inline → **encoded** (`wasserproben`, per-parameter badges, `sample_violations`); the P-limit switch by type decides after C-1; REQ-09 / REQ-10 onto the count is G-4 (REQ-10 sits on -03 without its symbols — X-3).
5. Plant plan rows with §10.4.3 ranges and counts → **encoded** (`plant_species_list` upgraded in place: group → range, chosen density, count, Σ); the three density scalars stay (X-2).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`fll_naturteich` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (fll_naturteich lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+32 blocks, +10 observations; C-3 / G-1 / J-5 amended in fix round 1), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 8)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-fll_naturteich.ts`, `src/lib/eval/field-configs/fll_naturteich.ts`, `src/lib/eval/field-configs/fll_naturteich.prior.json` (captured), `src/lib/eval/equations/fll_naturteich.ts`, `src/lib/eval/__tests__/regulation-tables-seed-fll_naturteich.test.ts`, `src/lib/eval/__tests__/field-configs-fll_naturteich.test.ts`, `src/lib/eval/__tests__/equations-fll_naturteich.test.ts`, `src/components/worksheet/__tests__/register-fllnt-filtereinheiten.test.tsx`, `scripts/verification/fll_naturteich-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917100800_regulation_tables_seed_fll_naturteich.sql` + `scripts/rollback-20260917100800-regulation-tables-seed-fll-naturteich.sql`, `scripts/migrations/20260917100810_field_configs_fll_naturteich.sql` + `scripts/rollback-20260917100810-field-configs-fll-naturteich.sql`, `scripts/migrations/20260917100820_equations_fll_naturteich.sql` + `scripts/rollback-20260917100820-equations-fll-naturteich.sql`, this report (+ the copy at `.superpowers/sdd/…/task-8-report.md`).
Untouched: every a138 / din1989_1 / a262e / m277e / m1200_1 / m1200_3 / fll_gar file; the Plan-1/2 migrations incl. `20260911120000_selection_configs_FLL_Naturteich.sql`; `SELECTION_CONFIGS` in `selection-fields.ts` (the TS fallback for the two FLL-NT registers stays until the Plan-1 file is applied; `equipment_elements_list` untouched); the FLL-NT harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **`natural_pool_type` is not consumed by FLLNT-04 or -05** (capture) — the brief's `wasserproben` type switch and the Tab.-8 P fills on -04 are emitted inert until C-1 (pinned); TABLE9 keys on the created `substrate_role` rather than the type (Tab. 9 prints three roles; the plant substrate exists in every type).
2. **TABLE7 / TABLE8 are single-row tables keyed `source = 'all'`** read with a literal key (m277e trap 3), not the brief's `TABLE7_<PARAM>` split; **TABLE8_P** (five rows, identical cells per group) carries the only two type-split parameters — keyed on the prod tokens (G-A3), not the brief's `type_group` i_iii / iv_v.
3. **TABLE2_SUBMERGED has two rows** (Tab. 2 type I AND Tab. 3 type II print the same "therefrom at least half" cell), not the brief's one-row `TABLE2_TYPE1`.
4. **Tab. 9 limits are CREATED (`*_tab9`) beside the created `substrate_role`**, not bound onto the existing `filter_grain_size_max` / `filter_substrate_*` / `filter_kf` — those are consumed inputs on -05 / -10 (a262e-E-3 / fll_gar-E-2 lesson) → E-1; the brief's `*_ist` twins are part of that ruling. Six Tab.-10 fills were created on -09 for the same reason (the brief's §5 "type fills").
5. **`filtereinheiten.hydrobot_type` is an enum column, not a TABLE10 `lookup_key`** — the register override binds to the FIRST lookup_key, which must be the `anhaltswert` Tab. 15 (product surface override); the Tab.-10 limits are read in the derived columns instead. Added: `colonized_manual_m2` for technical units (§10.2.4 manufacturer data), `layer_ok`, `surface_override`.
6. **`zonen.technique` is an enum over the prod `regeneration_technique` tokens** (fixed options ⇒ selection, never free text) instead of the brief's `technique text`; the fourth -06 equation (`submerged_hydrobot_share_calc`, Tab. 2 / 3 "at least half") consumes TABLE2_SUBMERGED.
7. **`total_pool_area_calc` sums ALL zones (prod's definition); `regeneration_share_calc` divides by the water area (swimming + regeneration)** — §3 says supplementary areas do not belong to the pool (J-4); the brief's `/ total_pool_area_calc` would have chained a same-pass output (the materialiser reads stored inputs only — the Σ is inlined).
8. **`plant_species_list` keeps the Plan-1 keys `art` / `zone` / `anzahl`** (m1200_3 trap 6) — the brief's `species text` became `art`; `anzahl` is relabelled as a remark column.
9. **Field `visible_when` 4 (brief: 7):** `p_binding_required` and `splash_water_tank_volume` are consumed producers (C-2, refused and pinned); the brief's `drainage_*` rule has no target field in prod; `hydrobot_water_column_max_tab10` (emersed only) and `regeneration_share_min_pct` (types I–III) are additions. **Section rules 0 (brief: "top-level sections of -09 / -10"):** every field-bearing section is a producer section (refused), the field-free ones are unobservable — the hide is C-3 (fix round 1).
10. **Equations 11 new (brief: 7 + 2 staged):** the brief's eight plus `-06-D4` (submerged share), `-11-D2` (tolerance violations), `-04-D2` (sample count); the brief's EQ-04 / EQ-05 semantics are R-4 / R-5; `FLLNT-04-D1` uses a per-row `sample_ok` AND-chain instead of an OR over `ok_* == 0` (same truth table, decidable per row).
11. **The Tab.-11 / -12 horizontal column is "Individual verification" for EVERY parameter** (feed rate included), not only water column / kf — encoded as null cells + `individual_verification = true`; only the two brief-named scalars (`filter_water_column`, `filter_kf`) get the visibility rule (the other -10 scalars are unconsumed too and could follow — observation).
12. **Prod gates live on mirror worksheets** (REQ-10 on -03, REQ-08 / -19 / -20 / -21 / -33 on -09) reading symbols not in scope there → X-3 (the m1200_3 mirror-gate class), not something this task moves.
13. **Type III prints "> 30%" in Tab. 1 and "≥ 30%" in Tab. 4** (J-3); prod REQ-07 tests `> 30`.
14. **Edition** printed (`'2017'`) → no numbered edition block; sign-off ids 32 (brief ≈ 10) because every refused / withheld / read item got its own block.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (38/38); every span was cut by line range with the generator (JSON-escaped, no `${`/backtick in any span — the generator throws otherwise); every `*_printed` cell is asserted inside its span by `cell()` at build time and the seed test pins each typed number against the printed line; every cue in `field-configs/fll_naturteich.ts`, `equations/fll_naturteich.ts`, the STAGED file and the 32 sign-off blocks was read from the transcript in this session at the cited line; prod facts (enum tokens, consumer lists, section codes, the 6 equation rows / md5, the 33 gate rows / md5, `standards.version`, field labels / VR) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted the module first time because the Step-4 rule set was planned against the capture's `consumer_worksheets` + `equations` BEFORE emitting; the field-config test pins the refusals of the withheld targets (`p_binding_required`, `splash_water_tank_volume`, `grain_specific_surface` — transitive via EQ-02 → EQ-01 → -15 —, the seven producer sections), pins that the driver-free sections hold 0 captured fields, and walks every emitted rule through `producerChain`.
- G-A3 pinned: TABLE1 / TABLE2_SUBMERGED / TABLE8_P keys = prod `natural_pool_type`; TABLE10 = `hydrobot_type`; TABLE11 / TABLE12 = `filter_flow_direction`; the register enum columns = prod token lists (`hydrobot_type`, `filter_flow_type`, `filter_flow_direction`, `regeneration_technique`); the created select's values = TABLE9 keys; the register lookup_keys bind TABLE15 / S10_4_3 with the seeded vocabularies.
- The runtime scratch check found the decidability facts before encoding (literal-key lookups, out-of-scope symbol → `manual_required`, `IS NULL` guards, `if()` short-circuit on null enums, division by zero), which is why the equations and render tests were green first run; the two REDs were my own (`cell()` catching the Tab.-12 hyphenation, `makeTableLookup`'s positional keys).
- `String.replace` was never used with a replacement string; every splice is `split/join` (node one-liners) or the Write / Edit tool; the seed file was assembled by `cat`; the scratch scripts live in the scratchpad only.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` confirmed gitignored; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the unified diff of every modified file; the only non-FLL-NT changes are the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing keyed on `natural_pool_type` works on FLLNT-04 until fll_naturteich-C-1** — the two Tab.-8 P fills read "Schlüssel fehlt" and `sample_violations` is `manual_required` for any project with a swimming-area sample (fill-water samples decide today). One consumer-edit UPDATE lights it; always-sign-off class, so staged.
2. **Nothing the registers compute reaches prod's gates yet** — `regeneration_share_calc`, `pool_underwater_surface_calc`, `filter_colonized_surface_total`, `filter_feed_violations`, `overflow_*`, `sample_violations`, `plant_count_total` are visible derived values; EQ-PUWS / EQ-01 … EQ-05, REQ-07 / -09 / -10 / -19 / -22 / -33 keep reading the manual scalars and literals until R-1 … R-5 / G-1 … G-7 are ratified (the fail-safe reading of "never a second producer").
3. **Three tables stay `imported_unverified`** (TABLE10 / TABLE11 / TABLE12, J-2): the md prints their single-cell rows once; the pdftotext layout was used beyond the brief's Table-1 allowance to assign them — a 5-minute PDF look at pp. 47 / 50 / 52 flips them, and the reviewer may prefer TABLE1 downgraded to the same status (J-1).
4. **Prod hygiene surfaced but not touched:** six gates on mirror worksheets whose symbols are not in scope (X-3 — `pending` on every project today), `swimming_area_m2` twice (X-1), EQ-04 outputting its own input field (R-4), EQ-05 / REQ-33 multiplying the colonizable underwater surface where the text says "inundated water surface" (R-5), five orphan enum-token fields (S-1), `total_pool_area_m2` "computed" without an equation (R-1).
5. **The brief's re-bind of the existing substrate / hydrobotanical inputs** (E-1) was not done — the same class the reviewer withdrew in Task 7 (E-2); the created `*_tab9` / `*_tab10` limits sit beside the inputs, which keep their widgets.
6. **Bundle growth:** the 91 spans (~28 KB, incl. the four full table bodies and the 116-line Tab.-1 body) ship in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
7. **Token/time:** ≈ 0.62 M tokens in, ≈ 70 min wall-clock — in the Task 4–6 band (0.53–0.60 M / 55–70 min) despite the plain-text transcript, because the four transposed tables were quoted by body (no per-cell parsers) and the runtime check preceded the encoding.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

Tab. 2–6 per-type care / biology / purification matrices (qualitative — `inspection_frequency` and Tab. 13 stay free text); Tab. 4's "filter feed rate > 25% of the total water volume/day, filter operation > 12 h/day, maximum downtime 1 h" (L1437–L1441) and Tab. 5 / 6's "> 80% … 24 h/day" (L1545–L1548, L1646–L1649) — no volume / operating-hours fields; Tab. 14 (eluate sample sizes); Tab. 16 (trophic overview); §9.1's drainage duty (L2359–L2360, attestation REQ-14 only); §10.2.5's physical/chemical "only … supplementary" (REQ-24 covers the technique choice); the reservoir requirements of §10.3.1 beyond the 150 l/m² (emergency overflow, animal exit, nutrient exclusion — L2949–L2957, attestation candidates); the automatic feed system rules (L2942–L2946); §10.4.3's planting times (L3035–L3039); DGfdB R 65.06 / DIN 19643-1 / DIN 18916 / DIN 18915 / DIN EN 206-1 / DIN VDE 0100-410 (external — reference only). Each is listed here with its line and stays outside this encoding (content-boundary rule) or on the sign-off sheet.

---

# Fix round 1 (review verdict on `57ebbc7`: needs fixes — SR-1 clean, six prod equations untouched)

- **Commit:** see the reply (one commit, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; every id / md5 in the STAGED blocks is from the 2026-09-17/18 capture.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · model provenance per the commit trailer; the subagent ran on Opus 5.

## IMPORTANT

1. **The 11 section rules dropped.** `field-configs/fll_naturteich.ts`: `SECTION_VISIBILITY = []` with the reason in its doc comment (the ruled sections A / F / J / K / L / M of -09 / -10 hold 0 fields — pinned against the capture in `field-configs-fll_naturteich.test.ts` — and `worksheet-form.tsx` renders only sections that contain a visible field, so nothing was observable); the cues stay lifted as `SECTION_RULE_CUES`. `20260917100810` + rollback re-emitted (37 field entries + 0 section entries; rollback carries no `worksheet_sections` line — pinned). The intended rules moved into the STAGED C-3 block as the SQL that becomes valid together with the producer sections: 18 `UPDATE worksheet_sections` (all nine captured codes A B C D F J K L M per worksheet, -09 ← types I–III, -10 ← types III / IV, type V per J-5). Report §1 / §4 / §5 / §6 win 1 / §8 (9) / §9 rewritten: the type-driven hide is STAGED (C-3), not delivered; sheet blocks C-3 and J-5 amended.

## MINOR

2. **`substrate_role` description** now states that the filter_iii / filter_iv half is NOT cross-checked against `natural_pool_type` (not inherited on -05) and that a later derivation / plausibility check needs the consumer edit FLLNT-03 → FLLNT-05 first (the C-1 pattern). Migration re-emitted (text only).
3. **G-1 latent guard conflict** recorded in the STAGED block and the sheet: the staged `consumer_worksheets = ['FLLNT-06']` on `regeneration_share_min_pct` would make the emitter refuse the emitted `visible_when` on a re-emit; functionally safe because REQ-07R repeats the same IF guard (hidden ⇒ null ⇒ not_applicable); on ratification withdraw the rule and re-emit, or keep it with REQ-07R's IF as the single guard.
4. **page_ref — reversal reported (R-5).** The review premise "marker 33 precedes §7.1.2 → Table 8 is on the following page" does not hold against the source: the TOC prints "Table 7 … 32" (L373–L374), "Table 8 … 33" (L375), "Table 9 … 34" (L376), and every page marker sits directly BEFORE its page's content, after the "This release version …" footer ("22" L1101 before Table 1 = TOC p. 22; "33" L1936 before §7.1.2 / Table 8 = TOC p. 33; "47" L2585 before Table 10 = TOC p. 47). So TABLE8 / TABLE8_P = 33 stays; the two WRONG refs under that convention were TABLE7 (was 33 → **32**, marker "32" L1881) and TABLE9 (was 35 → **34**, marker "34" L1998) — corrected in the seed builder with the marker lines in comments; `20260917100800` + rollback re-emitted (two `page_ref` cells), freshness pin green; verifier still 38/38.
5. **Report header** rewritten: model provenance per the commit trailer ("Claude Fable 5.1" by ruling); the subagent ran on Opus 5 — no contradiction.

## Raw output

```
pnpm vitest run --project unit src/lib/eval/__tests__/field-configs-fll_naturteich.test.ts src/lib/eval/__tests__/regulation-tables-seed-fll_naturteich.test.ts src/lib/eval/__tests__/equations-fll_naturteich.test.ts src/components/worksheet/__tests__/register-fllnt-filtereinheiten.test.tsx scripts/__tests__/generated-sql-freshness.test.ts
 Test Files  5 passed (5)
      Tests  29 passed (29)
pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts fll_naturteich "<transcript>"
 38/38 quotes verbatim in C:\Users\Ekowai\Desktop\Supabase data\Guidelines knowledge markdown\FLL-Guidelines natural pool.md
```
```
pnpm test
 Test Files  274 passed | 1 skipped (275)
      Tests  2599 passed | 1 expected fail | 1 skipped (2601)
   Start at  01:54:08
   Duration  41.68s
pnpm -s typecheck → exit 0 · pnpm -s eslint <3 touched .ts files> → exit 0, no output
```
(2599 unchanged: the withdrawn section pins were replaced by the 0-field pins; the case count is the same.) **The integration harness was not re-run:** the fix round changed emitted DATA only (11 section rows withdrawn, two `page_ref` cells, one description string, STAGED SQL comments, docs) — no engine, harness seed, or prod-shaped row the FLL-NT harness reads; the 14-file / 102-test run recorded above stands.
