# Plan 3 — DWA-M-1200-1 (`m1200_1`) — Task 5 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 6)
- **Model / effort:** Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-17
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `6fee3be`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-1 m1200_1` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (`standards.version`, the 20 worksheet titles, the 21 compliance rows with ids / severities / conditions incl. the 204-char CR-004 text, the one `equations` row). `.env.local` never printed; confirmed gitignored.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-1\DWA-M_1200-1_GD.md` (Gelbdruck Juli 2025, 3289 lines, mathpix LaTeX tables); the quote spans were lifted mechanically by line range (a throwaway `gen-quotes.mjs` in the scratchpad), never retyped; the inventory was used for pointers only (its pointers were correct; its CR-004 "≤ 1" claim was not — see §8). `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-2\DWA-M_1200-2_GD.md` L532–L577 was read ONLY to cross-check the OCR-damaged Tab. 8 class column (amendment: cite both lines — U-1 / U-3).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m1200_1` | 7 | 63 (63/63 verbatim) | 0 rows skipped; 6 CELLS seeded null (TAB27 class-C E. coli, C/D Trübung, D Legionella — U-2; TAB8 D-row chem cell + Zielvorgabe — U-3); 3 tables kept `imported_unverified` (TAB8 U-1/U-3, TAB27 U-2, TAB18 U-4) | 6 (all `create`) | 0 | 0 | 24 (all `create`: 14 Tab.-8 number fills + 3 note-f number fills on -09, 7 Tab.-27 text fills on -12) | 9 (all on created fields) | 0 | 8 | 0 replacements emitted; 9 gate blocks (G-1…G-9) + 2 derivations (D-1, D-2) + 1 retirement (R-1) STAGED | 28 (+11 observations) | ≈ 0.53 M (tool-marker delta 15.00 M → 14.47 M at report time) | ≈ 70 |

Field entries: 40 = 40 `create` (6 registers, 24 lookup_fills, 8 derived outputs, 2 attestations); 0 UPDATE — every existing field the brief's Step 4 names is a consumed producer (transitive guard) or has no driver in scope, so their rules are STAGED (C-1 / C-3), and the pending I-1 pair is untouched (E-1).

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  262 passed | 1 skipped (263)
      Tests  2514 passed | 1 expected fail | 1 skipped (2516)
   Start at  21:13:32
   Duration  39.96s (transform 10.34s, setup 43.24s, import 87.33s, tests 35.82s, environment 170.46s)
```
(+26 cases from this task: seed-m1200_1 9, field-configs-m1200_1 6, equations-m1200_1 8, register-m1200-1-risiko 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → first run 1 `no-unused-vars` warning (`Q_T8_B` imported but unused in `field-configs/m1200_1.ts`) → the Enterokokken fill's cue now cites the printed B row too (`Q_T8_A — Q_T8_B — Q_T8_C`), field-config migration re-emitted → second run exit 0, no output.

`pnpm vitest run --project integration tests/harness/m1200-1-verify.integration.test.ts` (embedded PG; no engine change on this task):
```
 Test Files  1 passed (1)
      Tests  18 passed (18)
   Start at  21:07:16
   Duration  5.29s (transform 509ms, setup 0ms, import 3.75s, tests 1.34s, environment 0ms)
```
The harness seeds prod's gates / EQ-001; the new rows are not in it by construction — it proves the branch still enforces every DWA-M-1200-1 gate both ways.

**TDD evidence.** Runtime facts were established BEFORE encoding with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` (scratchpad `rt-check.ts`, deleted): `lookup()` on two enum strings computes, a row-scope `visible_when` reads a same-row `derived` column, `if(x IS NULL, …)` parses in a row expr, `count*100/count` over zero rows is `manual_required` "Division durch Null.", `mean_rows` over an empty register is "Keine vollständigen Zeilen", `IN {'A','B-1','C-1'}` and `== 'ja'` conditions pass/fail as expected. Seed test written first → RED: `Transform failed … Unterminated string` — the quote generator's `String.replace(/\$\{/g, "${'$'}{")` had expanded the `$'` replacement pattern and emitted NO `${ }` escapes (the m277e trap in a new place) → generator switched to `split/join`, seed file re-assembled from head + fragment + tail by `cat` → RED on two of my slips (TAB19's header line inside the span → `grid.slice(1)`; `cells()` split on `' & '` left a trailing `&` on empty printed cells → split on `'&'`) → 9/9 GREEN; verifier 63/63 first run after that. Equations test → RED on `lookup_value` cells (`lookup.key_column` is the REGISTER column key, not the table's — read `register-rows.ts refillLookupValues`) → 8/8 GREEN; every pinned value (rank 4 / 1 / 3, Tab. 23 codes 4 / 1 / 3 / 2 / 4, Σ 20 000 m³/a, 2 areas, compliance 50 / 100 / 75 %, PFAS 100) held on the first run after that. Field-config test → RED on one pin of mine (`toBeUndefined` vs the module's explicit `null`) → 6/6; the emitter accepted all 40 entries on its first run (no producer-guard refusal — every UPDATE candidate had been planned out against the capture beforehand). Render test 3/3 first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m1200_1 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-1\DWA-M_1200-1_GD.md"` → **`63/63 quotes verbatim`**, exit 0. PASS list (table · rows · span):

- TAB8 (6): A (L1156–L1159), B-1 / B-2 (L1160–L1164, shared), C-1 / C-2 (L1165–L1166, shared), D (L1195–L1211 — the Tab. 8 (Ende) row)
- TAB8_NOTE_F (8): polstoff / mikrosieb / raumfilter (L1219), membran / mikrofiltration / ultrafiltration / nanofiltration / umkehrosmose (L1220)
- TAB7_CLASS (6): A (L984–L995), B-1 (L996–L1008), B-2 (L1018–L1026), C-1 (L1027–L1040), C-2 (L1056–L1065), D (L1066–L1077)
- TAB23 (25): A|1…A|5 (L2052), B|* (L2053), C|* (L2054), D|* (L2055), E|* (L2056)
- TAB27 (6): A (L2383), B-1 / B-2 (L2384), C-1 / C-2 (L2385), D (L2386)
- TAB19 (3): 1, 2, 3 — the printed body L1967–L1976 as the quote of each transposed row
- TAB18 (9): M-1 … S (L1891–L1899, one line each)

Status per table: `md_verified` — TAB8_NOTE_F, TAB7_CLASS, TAB23, TAB19 (every seeded row lifted, every cell legible). `imported_unverified` — TAB8 (U-1 class column images / glyphs; U-3 D-row chem cell image, "25，0" / "？5，0" sulfate cells), TAB27 (U-2 class column images, empty class-C E. coli cell), TAB18 (U-4 "0" glyph for O).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-1 m1200_1
  → wrote src\lib\eval\field-configs\m1200_1.prior.json: 126 field rows (12 orphan), 180 coded sections of 180, 1 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m1200_1
  → wrote seed + rollback for m1200_1 -> scripts/migrations/20260917100500_regulation_tables_seed_m1200_1.sql scripts/rollback-20260917100500-regulation-tables-seed-m1200-1.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m1200_1 20260917100510
  → wrote 40 field entries + 0 section entries for m1200_1 -> scripts/migrations/20260917100510_field_configs_m1200_1.sql scripts/rollback-20260917100510-field-configs-m1200-1.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m1200_1 20260917100520
  → wrote 8 equations for m1200_1 -> scripts/migrations/20260917100520_equations_m1200_1.sql scripts/rollback-20260917100520-equations-m1200-1.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m1200_1.test.ts` / `equations-m1200_1.test.ts` pin the other two against the committed `m1200_1.prior.json`). The seed migration carries 63 row upserts (`ON CONFLICT (table_id, row_key)`) and 4 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the seven tables (no earlier DWA-M-1200-1 table seed to re-emit). The field-config rollback deletes only the 40 `Plan 3:` rows (no UPDATE to restore); the equations rollback deletes the 8 `Plan 3:` rows. Apply order (owner, after the schema migration incl. the plan1-D-3-1 legacy rename): `20260917100500` → `20260917100510` → `20260917100520`; rollback in reverse. Every file touches `DWA-M-1200-1` rows only.

## 5. What was encoded

**Tables (7)** — `src/lib/eval/regulation-tables-seed-m1200_1.ts`, builder `m12001SeedTables()`, edition `'2025-07'` (title page L9 "Juli 2025" under "Entwurf" L12 = prod `standards.version` "Gelbdruck (Entwurf), Juli 2025"):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB8 | klasse (= the six prod `gueteklasse_zugeordnet` tokens) | zielvorgabe, e_coli_max 10/100/100/100/100/10 000, enterokokken_max 100/100/100/400/400/–, legionella_max 1000 (strict "<") + condition, nematoden_max 1 + condition, bsb5_max 10/–…, bsb5_note ("gemäß Richtlinie 91／271／EWG" — bare pointer), afs_max 10, truebung_max 2 (A…C-2), five log10 targets (A / B-1 / C-1), leistungsziele 1/0, validierung_pass_share_pct 90/50, routine_pass_share_pct 90 | locked (L1129 — L1151; notes d/e → P-1) | 6 (B-1/B-2 and C-1/C-2 share their printed row) | imported_unverified (U-1, U-3) |
| TAB8_NOTE_F | filtration_typ (eight prod tokens) | ntu_avg_24h 2/–, ntu_5pct 5/0,2, ntu_never 10/0,5 | anhaltswert (L1219 "sollten … nicht überschreiten") | 8 (E-2 token mapping) | md_verified |
| TAB7_CLASS | klasse | rank 1…6 (caption order), methode, anwendungsbereich_eu, karenzzeit_text, emitter_tropf_cm 25 / emitter_mikro_cm 50 (C-1), laktierend_ausgeschlossen (C true), laktierend_abtrocknung (B-1 true), vorsorge_text | locked (Tab. 4 note (*) L918) | 6 | md_verified (J-2) |
| TAB23 | wahrscheinlichkeit (A…E) × schadensausmass (1…5) — prod tokens | risikoniveau (prod tokens), risikoniveau_label (printed), risikoniveau_code 1…5 | locked (L2042) | 25, parsed from the printed row lines | md_verified |
| TAB27 | klasse | e_coli, enterokokken, bsb5, afs, truebung, legionella, nematoden — printed strings verbatim, nullable | locked (L2378 — L2372 "Mindesthäufigkeit") | 6 (5 empty cells → null, U-2) | imported_unverified (U-2) |
| TAB19 | kategorie (1/2/3) | bezeichnung, substanzen (printed cells joined), anzahl 9/5/0, grenzwert_text "Summe PFAS-20<100 ng/l", grenzwert_ng_l 100 | anhaltswert (L1965 "empfohlener") | 3 | md_verified |
| TAB18 | schutzgut_code (M-1 … S) | schutzgut, expositionsweg, bewertung_optionen, bewertung_fest ("hoch" for B, K), darstellung, code_printed | anhaltswert (L1882 "erfolgen kann") | 9, parsed from the printed lines | imported_unverified (U-4) |

Not seeded: Tab. 21 / 22 (level definitions — their printed labels are the option labels of the register enums, L2017–L2021 / L2031–L2035); Tab. 20 (Bereiche — J-3 option); Tab. 24 / 25 / 26 (empty templates — the registers); Tab. 5 / 6 (EU-only, superseded by Tab. 8 per inventory); Tab. B.1 (Leitsubstanzen — outside the brief; the metals stay manual); BBodSchV values (external law).

**Registers / lookup_fills / visibility** — `src/lib/eval/field-configs/m1200_1.ts` (all created in the captured sections B / D):
- `kulturen_tab7` (M12001-08 B): label, `klasse_sub` lookup_key TAB7_CLASS, `methode` / `karenzzeit` lookup_values, `laktierend` boolean, `klasse_rank` badge (`lookup('TAB7_CLASS', klasse_sub, 'rank')`), `laktierend_konflikt` badge (class C + lactating); footer `gueteklasse_code`. The 12 yes/no questions stay (J-1).
- `risiko_zeilen` (M12001-07 B): the brief's columns with prod tokens — gefahr (six hazard sheets), `schutzgut` lookup_key TAB18 + `expositionsweg` lookup_value, exposition (Tab. 24 column), schadensausmass / wahrscheinlichkeit (prod `1…5` / `A…E` with the printed Tab. 22 / 21 labels), Erwägungsgründe, `ausgangsrisiko` (token) + `ausgangsrisiko_code` (Tab. 23 per row), `massnahmen` / `rest_schaden` / `rest_wahrsch` required-and-visible only from `ausgangsrisiko_code >= 3` (L2061 "moderates oder hohes"), `restrisiko_code`; footer `ausgangsrisiko_max_code`, `restrisiko_max_code`.
- `stoerfaelle` (M12001-14 B): Teilelement (printed codes as placeholder), Teilprozess, Auslöser / Ereignis, Art der Gefährdung (free text — no printed list, J-3), schadensausmass / wahrscheinlichkeit, Erwägungsgründe, `risiko` + `risiko_code` (Tab. 23); footer `stoerfall_max_code`.
- `flaechenverzeichnis` (M12001-16 B): Fläche, ha, Kultur, `klasse_sub` lookup_key TAB7_CLASS, `methode_zulaessig` lookup_value, geplante Methode, `menge_m3` [m³/a], `karenzzeit` lookup_value; footer `anwendungsbereich_count_calc`, `zusatzwasserbedarf_jahr_calc`.
- `routineproben` (M12001-09 B): date, parameter (the seven Tab. 27 columns, discriminator), wert, `limit` (nested `if` over the seven created limit symbols in row scope — G-13), `relevant` (`if(limit IS NULL, 0, 1)`), `ok` (Legionella strict "<", else "≤"); footer `compliance_quote_calc`.
- `messstellen` (M12001-13 B): the five §6.6.3 mandatory items (parameter, matrix over the §6.6.1 compartments, Partei, Intervall, Ort) + Vorbelastung / Messwert / BBodSchV reference value (engineer-entered — content boundary) / Einheit; capture-only.
- lookup_fills on M12001-09 (TAB8 keyed on the inherited `gueteklasse_zugeordnet`, role limit): `e_coli_limit`, `enterokokken_limit`, `bsb5_limit`, `afs_limit`, `truebung_limit`, `legionella_limit` (← `aerosolrisiko == 'ja'`), `nematoden_limit` (← `anwendung_weide_oder_futterpflanzen == 'ja'`), five `log10_*_ziel` + `validierung_min_share_pct` (← `gueteklasse_zugeordnet IN {'A','B-1','C-1'}`), `routine_min_share_pct`; TAB8_NOTE_F keyed on the inherited `filtration_typ`: `truebung_avg_limit`, `truebung_5pct_limit`, `truebung_never_limit`. On M12001-12 (TAB27, role value, text): seven `beprobung_frequenz_<param>_tab27` (Legionella ← `aerosolrisiko == 'ja'`; Nematoden without a rule — its driver is not consumed on -12, C-1).
- attestations on M12001-09: `toc_korrelation_nachgewiesen` (note d), `truebung_kontinuierlich_ueberwacht` (note e) — P-1 / G-2.
- Field `visible_when` (9): all on created fields (listed above). Section rules: none. Withheld (C-3): the brief's rules on `legionella_value`, `nematoden_value`, `log10_*`, `validierungs_compliance_pct`, `truebung_max_value`, `bsb5_value`, `afs_value`, `indikatorchemikalien_*` (consumed producers) and on `pfas20_value` (REJECTED on L1955); `beschilderung_urbane_flaechen` (C-1, driver out of scope).

**Equations (8 new)** — `src/lib/eval/equations/m1200_1.ts`: `M12001-08-D1 gueteklasse_code = min_rows(kulturen_tab7, klasse_rank)` · `M12001-07-D1 restrisiko_max_code = max_rows(risiko_zeilen, restrisiko_code)` · `M12001-07-D2 ausgangsrisiko_max_code = max_rows(risiko_zeilen, ausgangsrisiko_code)` · `M12001-14-D1 stoerfall_max_code = max_rows(stoerfaelle, risiko_code)` · `M12001-16-D1 anwendungsbereich_count_calc = count_rows(flaechenverzeichnis)` · `M12001-16-D2 zusatzwasserbedarf_jahr_calc = sum_rows(flaechenverzeichnis, menge_m3)` (text-only, F-1) · `M12001-09-D1 compliance_quote_calc = count_rows(routineproben, ok == 1) * 100 / count_rows(routineproben, relevant == 1)` · `M12001-09-D2 pfas20_limit_ng_l = lookup('TAB19', '3', 'grenzwert_ng_l')` (scalar, not materialised — amendment D). None outputs `risikoniveau_ausgangs` (prod EQ-001 stays the single producer, R-1). Pinned values (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): rank 4 (C-1 + D), 1 (A + D), 3 (incomplete A row ignored); Tab. 23 D×4 = 4 → residual B×2 = 2, A×1 = 1, C×3 = 3 (measures required), E×5 residual D×3 = 4; Störfall max 4; 2 areas / 20 000 m³/a; compliance 50 % (2 of 4 incl. a strict-Legionella miss), 100 % under class D with non-relevant samples excluded, 75 % with a hidden Legionella limit; empty / no-relevant ⇒ `manual_required`; PFAS 100.

## 6. Inventory §5 top-wins walk

1. Tab. 7 questionnaire → strictest class, Tab. 8 limits pre-filled read-only → **encoded** (`kulturen_tab7` + `gueteklasse_code` on -08; 14 Tab.-8 fills + 3 note-f fills on -09, hidden where the class prints "–"); the questionnaire → row mapping is J-1, the manual `gueteklasse_zugeordnet` derivation D-2, CR-003 G-9.
2. Arbeitshilfe C/D as rows with the Tab.-23 matrix per row, CR-014 on max(row) → **encoded** (`risiko_zeilen` + `stoerfaelle`, both codes and labels per row, second assessment only from moderat, `restrisiko_max_code` / `ausgangsrisiko_max_code` / `stoerfall_max_code`); CR-014 rewrite G-5, scalar derivations D-1, EQ-001 retirement R-1.
3. Flächenverzeichnis rows feeding class / count / demand / CR-015 → **partially**: register + `anwendungsbereich_count_calc` + `zusatzwasserbedarf_jahr_calc` encoded; the class per row is displayed (lookup_key) but the strictest-class equation stays single-sourced on -08 (no second equation); the re-points are C-2 / G-8.
4. Tab. 27 plan for all seven parameters, Legionella / Nematoden only when relevant → **partially**: seven text fills on -12, Legionella hidden without aerosol risk; the Nematoden rule needs the consumer edit C-1; the class-C E. coli cell is unreadable (U-2); the existing enum is E-3.
5. Routine samples as rows → compliance % computed → **encoded** (`routineproben` + `compliance_quote_calc` on -09 with per-row limits, strict Legionella, non-relevant samples excluded); CR-008 / `compliance_quote_pct` are G-7 / C-2; the log10-deviation limit stays residue.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m1200_1` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m1200_1 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+28 blocks, +11 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 5)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m1200_1.ts`, `src/lib/eval/field-configs/m1200_1.ts`, `src/lib/eval/field-configs/m1200_1.prior.json` (captured), `src/lib/eval/equations/m1200_1.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m1200_1.test.ts`, `src/lib/eval/__tests__/field-configs-m1200_1.test.ts`, `src/lib/eval/__tests__/equations-m1200_1.test.ts`, `src/components/worksheet/__tests__/register-m1200-1-risiko.test.tsx`, `scripts/verification/m1200_1-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917100500_regulation_tables_seed_m1200_1.sql` + `scripts/rollback-20260917100500-regulation-tables-seed-m1200-1.sql`, `scripts/migrations/20260917100510_field_configs_m1200_1.sql` + `scripts/rollback-20260917100510-field-configs-m1200-1.sql`, `scripts/migrations/20260917100520_equations_m1200_1.sql` + `scripts/rollback-20260917100520-equations-m1200-1.sql`, this report (+ the copy at `.superpowers/sdd/…/task-5-report.md`).
Untouched: every a138 / din1989_1 / a262e / m277e file; the Plan-1/2 migrations; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **No `TAB8_CLASSMAP` / `gueteklasse_group` chain.** Every class-keyed table keys on the six prod tokens directly (amendment B: key tokens = the driving enum's value strings); a printed row covering two sub-classes is seeded twice with identical cells and the same span (a262e trap 3). One fill per limit, no created key field per worksheet.
2. **TAB8 and TAB27 are single-key tables with one value column per parameter** (m277e TABLE4_LIMITS precedent), not the brief's `TAB8_ECOLI … TAB8_LOG10_<ORG>` / `TAB27_<PARAM>` split; `TAB8_PASS_SHARE`, `TAB8_LEGIONELLA`, `TAB8_NEMATODEN`, `TAB7_RANK` are columns of TAB8 / TAB7_CLASS.
3. **`min_share_required` is a lookup_fill (`validierung_min_share_pct`), not the brief's equation M12001-09-D2**; the D2 slot went to the Tab.-19 PFAS-20 value so TAB19 has a consumer (the I-1 pair is untouched). Equations: 8 (brief: 7 + 1 staged) — `ausgangsrisiko_max_code` added; the "strictest class over the Flächenverzeichnis" was deliberately NOT added (single-source).
4. **The brief's CR-004 premise is refuted**: prod's class-D branch already reads `e_coli_value <= 10000` (in-session capture of the 204-char condition); G-3 is single-source hygiene only.
5. **Zero UPDATE entries / zero rules on existing fields**: every Step-4 target is a consumed producer (C-3) or driver-less on its worksheet (C-1); the rules live on the created twins. `bsb5_value` optionality needs `is_required` (G-2) — the two attestation booleans were created now.
6. **The Tab. 27 fills are created text twins**; the existing `beprobung_frequenz_e_coli` enum is not re-bound (E-3; the class-C cell is empty, a fill would remove the input for C-1 / C-2).
7. **`stoerfaelle.gefaehrdung` is free text** — Tab. 26 prints no "Art der Gefährdung" list (J-3); `messstellen.matrix` uses the five §6.6.1 compartments (L2360–L2364), not the brief's `[boden, wasser, pflanze]`.
8. **Yes/no drivers are prod enums** (`aerosolrisiko`, `anwendung_weide_oder_futterpflanzen` = `ja`/`nein`) → rules read `== 'ja'`, not `== true`.
9. **Prod has 21 compliance rows** (brief: 22); the Tab. 8 D-row chemical cell and the class-C E. coli cell of Tab. 27 are unreadable as the plan foresaw, plus the sulfate-reducer OCR (U-3) and the Tab. 18 "0" glyph (U-4) the plan did not.
10. **Register outputs on the register's worksheet**: `compliance_quote_calc` on -09 (not -12), `anwendungsbereich_count_calc` on -16 (not -08); the re-points are C-2.
11. Edition printed (`'2025-07'`) → no numbered edition block; sign-off ids 28 (brief ≈ 12) because every refused / withheld / unreadable item got its own block.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (63/63); every span was cut by line range with the generator (the `${ }` LaTeX token escaped as `${'$'}{ }`, fullwidth punctuation kept); every fragment used as a cell (`frag()` / `cells()` / `bullet()`) is a substring of a lifted span, and the tests pin the resulting strings against the printed lines; every cue in `field-configs/m1200_1.ts`, `equations/m1200_1.ts`, the STAGED file and the 28 sign-off blocks was read from the transcript in this session at the cited line; prod facts (enum tokens, consumer lists, section codes, gate ids / conditions, the EQ-001 row, `standards.version`) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted the module first time because the Step-4 rule set had been planned against the capture's `consumer_worksheets` BEFORE emitting (the test pins each withheld target's consumer list and walks every created rule through `producerChain`).
- G-A3 pinned: TAB8 / TAB7_CLASS / TAB27 keys = prod `gueteklasse_zugeordnet`; TAB8_NOTE_F = prod `filtration_typ`; TAB23 keys = prod `eintrittswahrscheinlichkeit` × `schadensausmass` and its level tokens = prod `risikoniveau_ausgangs`; the register enum columns carry the prod token lists.
- The `String.replace` `$'` trap was caught by the seed test (RED) and recorded as playbook trap 8; the seed file was never spliced (head + generated fragment + tail via `cat`; small edits with `split/join` in node or the Edit tool).
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` confirmed gitignored; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the unified diff of every modified file; the only non-M1200-1 changes are the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing the registers compute reaches prod's gates yet** — `gueteklasse_code`, the three risk maxima, the two Flächenverzeichnis outputs and `compliance_quote_calc` are visible derived fields; CR-003 / -004 / -006 / -008 / -014 / -015 keep reading the manual inputs until G-3…G-9 / D-1 / D-2 / C-2 are ratified. That is the fail-safe reading of "never a second producer".
2. **Three tables stay `imported_unverified` for OCR cells** (TAB8, TAB27, TAB18 — U-1…U-4): a 5-minute PDF look at p. 39–40, p. 80 and p. 64 flips them and seeds the five null cells.
3. **The withheld visibility on -09 / -10 / -12 producers (C-3)** means the engineer still sees `legionella_value`, `nematoden_value`, the log10 inputs etc. for every class — the created twins next to them say "— (kein Tab. 8-Grenzwert)" / hide, which is the honest fallback, not the brief's UX.
4. **Two text-derived encodings rest on the executor's reading**: the strictness `rank` (caption order, J-2) and the "≥ 3 = moderat" threshold (Tab. 23 level order) — both pinned and on the sheet.
5. **Scalar-only equation not materialised** (M12001-09-D2) — hook / report / snapshot / PDF only (amendment D).
6. **Bundle growth:** TAB8's four printed spans (~6 KB) and TAB7_CLASS's six (~9 KB) ship in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
7. **Token/time:** ≈ 0.53 M tokens in, ≈ 70 min wall-clock — in line with Tasks 3/4 for a standard with 6 registers, 24 fills and the heaviest OCR damage so far; the runtime scratch check and the mechanical lift paid for themselves again (equations green after one config fix, render green first run).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The maximum-deviation rules of L1143 / L1145 (one log10 step for E. coli / Legionella, 100 % of the value for Nematoden / BSB5 / AFS / Trübung — needs per-parameter deviation rows); the Tab. 8 note g) two-coliphage-types requirement (L1221); the Tab. 7 per-crop Karenzzeiten beyond the class-level text (J-2 option); the Tab. 7 footnote (**) worker-protection sentence (L1124); the Tab. 20 event catalogue (L1993–L2001) as a picker for `stoerfaelle`; Tab. B.1 Leitsubstanzen with maximum concentrations (L1933 — Anhang B not read in this task); BBodSchV Vorsorgewerte (external law — reference only); the six-year RMP revision cadence (L1937) — `rmp_aktualisierungs_intervall_jahre` exists but no gate; Tab. 16 / 17 exposure classes for groundwater / surface water (L1939); the §6.6.3 monitoring programme's interval semantics (free text); the DWA-M 590 demand method (bare pointer, L1407). Each is listed here with its line and stays outside this encoding (content-boundary rule) or on the sign-off sheet.
