# Plan 3 — DWA-M-820-3 (`m820_3`) — Task 9 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 10)
- **Model provenance:** per the commit trailer (`Co-Authored-By: Claude Fable 5.1`, controller ruling); the subagent session ran on Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `e2ed808`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-3 m820_3` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 24 worksheet titles, the section codes / titles / field counts of six worksheets, the 32 compliance rows with ids / severities / md5 / lengths, ten conditions read in 110-char chunks, the `information_schema` column lists of `worksheet_templates` / `standards` / `worksheet_sections` / `compliance_requirements` / `worksheet_instances`, the labels / VR / descriptions of the count fields, the instance / parameter counts). `.env.local` never printed; confirmed gitignored (`git check-ignore .env.local`).
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-3\DWA-M_820-3.md` (1297 lines; the ten catalogues at L687–L1190). The 193 row spans were lifted mechanically by line range (a throwaway parser in the scratchpad tracking the nested-tabular depth; every cell asserted inside its span at build time by `inSpan`); the seed module was generated with `JSON.stringify` for every string (no `String.replace`, no heredoc — the Bash heredoc on this machine collapses `\\`). The inventory was used for pointers only (its "174 printed items" is an arithmetic slip — §8). The PDF was not opened.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m820_3` | 10 | 193 (193/193 verbatim) | 0 rows skipped; 2 cells with an EMPTY printed Nr. (QE_A4, positional — U-1, table `imported_unverified`); Bild 1 is an image (U-2, no table) | 12 (all `create`, one per QE worksheet from one template) | 0 | 2 (`create`: `sektoren`, `anwendungshinweise`) | 0 | 126 (all on created fields — the 12 registers, 108 count / share outputs, the 6 annex outputs; all `pending` until C-1) | 0 (every field-bearing section holds consumed producers — refused; C-2 / C-3 STAGED) | 115 (12 × 9 from one template + 7 annex / Projektstopp) | 0 replacements (prod has 0 equations); D-1 / D-2 (ownership switches, 50 fields), G-1 … G-5, C-1 … C-5, M-1, J-1 … J-3, U-1 / U-2, I-1, X-1, O-1 STAGED | 22 (+6 observations) | ≈ 0.55 M (tool-marker delta 15.00 M → 14.45 M at report time) | ≈ 50 |

Field entries: 129 = 129 `create` (2 select_many, 12 registers, 115 derived outputs) + 0 UPDATE. Migration: 129 `INSERT … WHERE NOT EXISTS`, 0 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes the 129 `Plan 3:` rows.

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  278 passed | 1 skipped (279)
      Tests  2619 passed | 1 expected fail | 1 skipped (2621)
   Start at  02:31:02
   Duration  42.23s (transform 12.70s, setup 45.84s, import 96.60s, tests 39.58s, environment 177.52s)
```
(2599 at `e2ed808` + 20 from this task: seed-m820_3 4, field-configs-m820_3 6, equations-m820_3 6, register-m820-3-qe 4; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output (run twice: after the modules, and on the final working tree).

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/m820-3-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  25 passed (25)
   Start at  02:31:50
   Duration  5.60s (transform 569ms, setup 0ms, import 3.63s, tests 1.79s, environment 0ms)
```
The harness seeds prod's fields / gates (`tests/harness/seed-m820-3.ts`); the new rows are not in it by construction — it proves the branch still enforces every M820-3 gate both ways where drivable.

**TDD evidence.** Runtime facts were established BEFORE encoding with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` on the seeded TS tables (scratchpad `scratch-rt-m8203.ts`, deleted): a `lookup_key` row fills `kriterium` / `hinweise` from QE_B2 (an empty printed Hinweise cell fills `''`; a stored cell is not overwritten); a row `derived` `if(lookup('QE_B2', nr, 'nr_num') >= 1 AND … <= 21, 1, 0)` decides 1 / 0 per row; `count_rows(reg, rating == 'y' AND in_teil == 1)` counts 1 with a Nr.-22 row present; `count_rows` over an EMPTY register is **0 (computed)**, not `manual_required`; a share formula with a scalar denominator reads `manual_required: Fehlende oder leere Eingaben: qe63a_items_total` while the total is unset; a 67-input `if(a == 'nicht_erreicht' OR …, 1, 0)` parses, is engine-eligible (`validateEngineEligibility` → verified) and computes 0 / 1 (my first "one missing → 0" reading was a scratch-script bug — `??` swallowed the null — the equations test pins the real `manual_required` naming the symbol). Seed test written after the parser → 4/4 GREEN first run (the parser's line numbers were asserted, not guessed); verifier **193/193** on its first run. Field-config test: one RED on my own arithmetic (rule count 128 vs the real 126 = 129 − 3) → 6/6. Equations test 6/6 GREEN first run. Render test: one RED on the footer format (`6,67` vs the formatter's `6,6667`) → 4/4.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m820_3 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-3\DWA-M_820-3.md"` → **`193/193 quotes verbatim`**, exit 0. PASS list (table · rows · span lines):

- QE_A1 (15): n1 … n15 — L689–L709 (n10 L698–L701, n13 L704–L707 multi-line cells)
- QE_A2 (8): n1 … n8 — L717–L724
- QE_A3 (12): n1 … n12 — L732–L743
- QE_A4 (3): n1 L752–L753 (Nr. printed empty), n2 L754–L756 ("2"), n3 L757–L758 (Nr. printed empty)
- QE_B1 (15): n1 … n9 L769–L777, n10 … n15 L785–L793 ("B. 1 (Ende)")
- QE_B2 (40): n1 … n13 L802–L822 (Grundlagenermittlung), n14 … n21 L824–L831 (Vorplanung), n22 … n35 L833–L861 (Entwurfsplanung), n36 … n40 L872–L879 (Genehmigungsplanung)
- QE_B3 (50): n1 … n17 L890–L949 (Ausführungsplanung), n18 … n39 L951–L1005 (Vorbereiten der Vergabe), n40 … n50 L1007–L1017 (Mitwirken bei der Vergabe)
- QE_B4 (34): n1 … n34 L1027–L1131
- QE_B5 (10): n1 … n5 L1141–L1145, n6 … n10 L1155–L1165 ("B. 5 (Ende)")
- QE_B6 (6): n1 … n3 L1174–L1176, n4 … n6 L1184–L1189 ("B. 6 (Ende)")

Status per table: `md_verified` — QE_A1, QE_A2, QE_A3, QE_B1, QE_B2, QE_B3, QE_B4, QE_B5, QE_B6 (every printed row lifted, every cell legible). `imported_unverified` — QE_A4 (U-1: two of three Nr. cells printed as `\multirow{2}{*}{}`, rows keyed by printed position).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-3 m820_3
  → wrote src\lib\eval\field-configs\m820_3.prior.json: 250 field rows (0 orphan), 216 coded sections of 216, 0 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m820_3
  → wrote seed + rollback for m820_3 -> scripts/migrations/20260917100900_regulation_tables_seed_m820_3.sql scripts/rollback-20260917100900-regulation-tables-seed-m820-3.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m820_3 20260917100910
  → wrote 129 field entries + 0 section entries for m820_3 -> scripts/migrations/20260917100910_field_configs_m820_3.sql scripts/rollback-20260917100910-field-configs-m820-3.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m820_3 20260917100920
  → wrote 115 equations for m820_3 -> scripts/migrations/20260917100920_equations_m820_3.sql scripts/rollback-20260917100920-equations-m820-3.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m820_3.test.ts` / `equations-m820_3.test.ts` pin the other two against the committed `m820_3.prior.json`). The seed migration carries 193 row upserts (`ON CONFLICT (table_id, row_key)`) and 9 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the ten tables (no earlier DWA-M-820-3 table seed exists — nothing superseded; the Plan-1 `20260801300000_m820_3_applicable_lph.sql` / `SELECTION_CONFIGS.applicable_lph` are untouched). The equations rollback deletes the 115 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917100900` → `20260917100910` → `20260917100920`; rollback in reverse. Every file touches `DWA-M-820-3` rows only. Scalar-only rows (the two annex sums, the two rated sums, the three Projektstopp codes) are not server-materialised (amendment D — expected, noted once here); the 108 count / share rows are register-fed and materialise on save.

## 5. What was encoded

**Tables (10)** — `src/lib/eval/regulation-tables-seed-m820_3.ts`, builder `m8203SeedTables()`, edition `'2026'` (title page L13 / L23 "Februar 2026", imprint L51 "© DWA, 1. Auflage, Hennef 2026" = prod `standards.version` 'Februar 2026'):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| QE_A1 … QE_B6 (one per printed catalogue) | `nr` = `n<Nr.>` | `kriterium`, `hinweise` (the printed cells, OCR quirks verbatim), `nr_num`; `group_label` = the printed Leistungsphase headings of B.2 (4) / B.3 (3); `label_de` = `<Nr.> · <Kriterium>` | anhaltswert (L67 "… projektspezifisch ausgewählt und ergänzt werden müssen." — L194 "… stellen eine Auswahl an Kriterien für die Projektabwicklung dar.") | 15 / 8 / 12 / 3 / 15 / 40 / 50 / 34 / 10 / 6 = 193 | md_verified × 9; QE_A4 imported_unverified (U-1) |

Also lifted (cue constants, one line each): L67, L194, L196, L200 / L202 / L204 / L206, L307, L313, L321, L398, L550, L591, plus the ten catalogue heading lines (`heading` / `heading_line` on `QE_CATALOGUES`). Not seeded: Tabelle 1 (abbreviations, not a value table); Bild 1 (image — U-2).

**Registers / selections / visibility** — `src/lib/eval/field-configs/m820_3.ts` (all created; the twelve registers + 108 outputs are generated from `QE_WORKSHEETS`, one spec row per worksheet with `{ ws, prefix, table, annex, from, to, split }`):
- `<prefix>_items` (section C of M8203-07 … -18): `nr` lookup_key → the catalogue (grouped by `group_label` on B.2 / B.3), `kriterium` / `hinweise` lookup_values, `rating` enum y / p / n / na (prod's count scale — labels "Y — erfüllt" …), `evidence`, `remark`; on the four split worksheets a row `derived` badge `in_teil` (`lookup(T, nr, 'nr_num')` inside the printed range — "gehört zu Teil 1 (Nr. 1–21)" / "außerhalb …") that every count filters on; footer = the nine outputs; note = the L67 cue + "Vollständigkeit: `<rated> == N`"; no override block (rows are picked, never rewritten — I-1 / O-1).
- Derived outputs (section D, `widget='derived'`, number): `<prefix>_items_{y,p,n,na}_calc`, `<prefix>_items_rated`, `<prefix>_share_{y,p,n,na}_pct` (%) per worksheet; on M8203-22: `gesamt_anhang_a_items_total_calc`, `gesamt_anhang_a_items_rated`, `projektstopp_code_a`; on M8203-23 the B twins + `projektstopp_code_b`; on M8203-24 (section F) `projektstopp_code`.
- Select_many: `sektoren` (M8203-01 B; L196 — Wasserwirtschaft / Wasserbau / Abwasser / Abfall) and `anwendungshinweise` (M8203-02 C; the four printed sentences L200 / L202 / L204 / L206 without the OCR'd bullet glyph) beside the booleans they mirror (J-1). `bild1_schritte` not created (U-2).
- Field `visible_when` (126): `project_type IN {'gesamtsystem', 'both'}` on every created Anhang-A field (M8203-07 … -10, -22), `project_type IN {'einzelprojekt', 'both'}` on every created Anhang-B field (M8203-11 … -18, -23) — the captured tokens (`both`, not the brief's `beide`); `projektstopp_code` and the two select_many sets stay visible. Every rule is `pending` (inert) until C-1: `project_type.consumer_worksheets = ["ALL"]`, a token `loadInheritedFields` never matches. Section rules: NONE (every field-bearing section of -04 … -18 holds consumed producers — refused, pinned; C-2 / C-3). Rules on existing fields: NONE (246 of 250 fields are consumed; the BIM block is C-4).

**Equations (115 new)** — `src/lib/eval/equations/m820_3.ts`: per QE worksheet `<ws>-D1 … -D4 = count_rows(<reg>, rating == '<r>'[ AND in_teil == 1])`, `-D5 = count_rows(<reg>[, in_teil == 1])`, `-D6 … -D9 = count_rows(…) * 100 / <prefix>_items_total`; `M8203-22-D1` / `M8203-23-D1` = Σ of the inherited `qeNN_items_total` constants (38 / 155, computable today); `-D2` = Σ of the created `_rated` outputs (undecidable until C-5); `M8203-22-D3` / `M8203-23-D3` / `M8203-24-D1` = `if(<OR over 13 / 54 / 67 pz_*_status == 'nicht_erreicht'>, 1, 0)` (L307; `teilweise_erreicht` does not trigger — J-2). Pinned values (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): A.1 rows y / y / p / n / na / na / unrated → 2 / 1 / 1 / 2 / 6 rated, shares 13.33 / 6.67 / 6.67 / 13.33 % of 15, share `manual_required` without the total, empty register → 0 / 0 / 0 %; B.2 rows n1 / n21 / n22 / n40 → M8203-12 counts 1 y / 1 p / 2 rated (n22, n40 outside Teil 1), M8203-13 counts 1 y / 1 n / 2 rated, shares 4.76 % of 21 / 5.26 % of 19; annex sums 38 / 155 / 18; Projektstopp 0 → 1 on one `nicht_erreicht`, 0 on `teilweise_erreicht`, 0 on all `nicht_zutreffend`, `manual_required` naming the unset goal (and naming `pz_62_1_status` for an Anhang-A-only input set). No prod symbol is re-produced (the manual counts, totals, fulfilment rates, verdicts and Projektstopp booleans stay theirs — D-1 / D-2 / F-1 / G-4).

## 6. Inventory §5 top-wins walk

1. Item-level checklists per catalogue with derived counts / fulfilment / totals → **encoded (counts + shares), partially delivered**: the twelve registers seed every printed item verbatim (193 rows, picked per row with Y / P / N / NA + evidence), the counts and per-rating shares derive (materialised on save), the annex sums compute from the inherited constants; BUT the ~100 hand-typed numbers are NOT yet removed — the 48 manual counts + 2 annex totals keep their input widgets until D-1 / D-2 are ratified (the ownership switch is the deactivation class the brief itself labels D-1), and `fulfilment_pct` needs F-1 (nothing printed). REQ-15 … REQ-24 become self-fulfilling after D-1; G-3 offers interim completeness gates.
2. Gate Anhang A vs B on `project_type` → **encoded, inert**: 126 rules on the created fields; the existing scalars / sections and the block gates REQ-06 … REQ-14 are C-2 / C-3 / G-1 / G-2 STAGED — and every rule waits on C-1 (the `{ALL}` consumer list). An Einzelprojekt is still blocked by REQ-06 … -08 today.
3. Derive `projektstopp_review_triggered` / `projektstopp_required` → **encoded as codes** (`projektstopp_code_a` / `_b` / `projektstopp_code`, computing once every goal is set); the booleans stay manual and REQ-31 (empty) onto the code is G-4.
4. Aggregate verdicts from fulfilment thresholds → **F-1** (formula and bands not printed; two candidates + the bands for the owner).

   **Amendment-O evidence for "not printed"** (re-executed in the Task-30b absence pass, 2026-09-24, against the SR-1 transcript `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-3\DWA-M_820-3.md`; full lines, no truncating filter):

   ```
   $ grep -n -i "Erfüllungsgrad" DWA-M_820-3.md   → (no output)   exit=1
   $ grep -n -i "Ampel"          DWA-M_820-3.md   → (no output)   exit=1
   $ grep -n -i "gewicht"        DWA-M_820-3.md   → (no output)   exit=1
   $ grep -n -i "Gewichtung"     DWA-M_820-3.md   → (no output)   exit=1
   $ grep -n -i "gelb"           DWA-M_820-3.md   → (no output)   exit=1
   ```

   Non-empty controls, every hit classified (so the negative is not a mis-aimed regex):

   ```
   $ grep -n '%' DWA-M_820-3.md                                        → 2 hits, exit=0
     L50   Gedruckt auf 100 \% Recyclingpapier
     L1269 Fördernde DWA-Mitglieder erhalten 20 \% Rabatt auf den Ladenpreis.
   $ grep -n -i "grün" → 4 hits (L71, L638, L853, L980) · $ grep -n -i "rot" → 2 hits (L638, L1110), exit=0
     every one a substring false positive — "Gründen", "begründet", "begründenden", "protokolliert"; no colour band.
   $ grep -n -iE "Bewertung|Punkte|Skala|Score|Kennzahl|Bewertungsmaßstab"  → 14 hits, exit=0
     every one a catalogue item text or prose, e.g. L723 "Bewertung der Maßnahmen und Erstellung einer
     Prioritätenliste & Bewertungsmatrix (im Planungsteam abgestimmt und freigegeben)" — no rating scale.
   $ grep -n -iE "teilweise|nicht zutreffend|n\. z\.|entfällt"           → 2 hits (L451, L583), exit=0
     both prose — so not even the Y / P / N / NA rating legend is printed.
   ```

   **Verdict: the claim HOLDS.** No rating scale, no P-weight, no NA treatment, no verdict band appears anywhere in the transcript; the L67 / L202 sentences (re-read in full with `sed -n '67p;202p'`, exit=0) say the evaluation is the project team's. The 12 `fulfilment_pct` fields and the three verdict enums correctly stay MANUAL.
5. Inherit `applicable_lph`, identity and BIM facts from DWA-M-820-2 → **not this task** (cross-standard; the Plan-1 `applicable_lph` config with its I-1 pending pair untouched; the LPH-driven visibility of B.2 … B.6 is M-1, a select_many driver).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m820_3` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m820_3 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+22 blocks, +6 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 9)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m820_3.ts` (generated, 107 KB), `src/lib/eval/field-configs/m820_3.ts`, `src/lib/eval/field-configs/m820_3.prior.json` (captured), `src/lib/eval/equations/m820_3.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m820_3.test.ts`, `src/lib/eval/__tests__/field-configs-m820_3.test.ts`, `src/lib/eval/__tests__/equations-m820_3.test.ts`, `src/components/worksheet/__tests__/register-m820-3-qe.test.tsx`, `scripts/verification/m820_3-STAGED-plan3-rulings.sql` (22 blocks, every line a comment), `scripts/migrations/20260917100900_regulation_tables_seed_m820_3.sql` + `scripts/rollback-20260917100900-regulation-tables-seed-m820-3.sql`, `scripts/migrations/20260917100910_field_configs_m820_3.sql` + `scripts/rollback-20260917100910-field-configs-m820-3.sql`, `scripts/migrations/20260917100920_equations_m820_3.sql` + `scripts/rollback-20260917100920-equations-m820-3.sql`, this report (+ the copy at `.superpowers/sdd/…/task-9-report.md`).
Untouched: every other standard's file; `selection-fields.ts` (`applicable_lph` Plan-1 config), `register-configs.ts`; the M820-1 / -2 / -3 harness files; `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **193 printed items, not 174.** 15 + 8 + 12 + 3 = 38 and 15 + 40 + 50 + 34 + 10 + 6 = 155; the brief's own per-catalogue list sums to 193, as do the prod VR strings and the M8203-22 / -23 descriptions. 193 spans lifted.
2. **`project_type` tokens are `gesamtsystem` / `einzelprojekt` / `both`** (capture), not `beide` — every rule uses `both`.
3. **Equation outputs are CREATED twins (`*_calc`, `*_rated`, `*_share_*_pct`), not the existing `qeNN_items_y/p/n/na`** the brief names: those are consumed, required manual inputs read by REQ-15 … REQ-24, and taking them over is the deactivation class the brief labels D-1 — STAGED with the full 48 × 4 SQL (widget → derived, output re-pointed, twin retired, footer re-pointed). Consequence: the hand-typed counts are not yet removed (§6 win 1 partially delivered).
4. **Percentages added:** the brief's Step 5 lists counts only; the task's title asks for percentages — four per-rating share equations per worksheet (`count · 100 / qeNN_items_total`, the manual constant as denominator — J-3) were emitted; the weighted fulfilment stays F-1.
5. **Projektstopp: three codes, not one** — `projektstopp_code_a` (-22, 13 goals), `projektstopp_code_b` (-23, 54 goals) beside the brief's `projektstopp_code` (-24, 67 goals), because the engine requires every named input and a project of one type never sets the other annex's goals; G-4 proposes the -24 form.
6. **Annex totals: two rows per summary worksheet** — `*_items_total_calc` (Σ of the inherited constants, computes today) added beside the brief's `*_items_rated` (Σ of created outputs, undecidable until C-5).
7. **Field `visible_when`: 126 (brief: 4) — all on created fields; 0 on existing fields.** The brief's four (BIM block, `projektstopp_review_triggered` stays visible) collapse to C-4; every existing field is consumed. **Section rules: 0 (brief: M8203-04 … -18)** — refused (consumed producers) → C-2 / C-3; the field-free sections are not observable (Task 8 lesson).
8. **`bild1_schritte` not created** — Bild 1 is an `\includegraphics` figure (U-2); `sektoren` and `anwendungshinweise` created as select_many (J-1).
9. **B.2 / B.3 split:** ONE table each (as the brief says) — the halves are told apart by a row `derived` badge on the register (`in_teil`), not by the brief's `count_rows(reg, nr_num >= from AND nr_num <= to)` over a stored column (no stored `nr_num` column is needed: the badge reads the catalogue row).
10. **A.4 categories are the three rows** (`kriterium` = the category, `hinweise` = its 2 / 3 / 2 printed lines joined with " · "), not "rows with `group_label`" — a category is the item itself; `group_label` is used where the transcript prints headings (B.2 / B.3).
11. **`applicable_lph` M-1** as the brief foresaw (select_many driver); `{ALL}` consumer (C-1) applies to it too.
12. **REQ-31 sits on M8203-22 in prod** (the brief assumed M8203-24 for the Projektstopp gate); G-4 proposes the move.
13. Sign-off ids 22 (brief ≈ 8) because every refused / withheld / read item got its own block; the `X` letter is used for the mirror-gate prod hygiene item (cross-worksheet), `I` for the interface gap, `O` for the policy.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (193/193); every span was cut by line range with the parser (nested-tabular depth tracked; A.4 continuation lines folded into the row span); every cell value is asserted inside its span at build time (`inSpan` — a slip would throw before any migration is emitted); the seed test pins the per-catalogue counts with their line ranges, the B.2 / B.3 group boundaries, the first / last items, the A.3 spanning Hinweis, the A.4 empty-Nr. rows and the OCR quirks; every cue in `field-configs/m820_3.ts`, `equations/m820_3.ts`, the STAGED file and the 22 sign-off blocks was read from the transcript in this session at the cited line (the observation lines L305 / L876 / L943 / L1070 / L1089 / L1131 re-checked with `sed` before appending).
- The emitter guards accepted both modules first time because the rule set was planned against the capture (consumer lists, `{ALL}`, section codes) before emitting; the field-config test pins the refusals of every withheld target (the C / F / B sections of -04 … -18, the four BIM flags, `projektstopp_review_triggered`), pins that the field-free sections hold 0 captured fields, pins the `{ALL}` fact, and walks every emitted rule through `producerChain` (skipDirect for creates).
- G-A3 pinned: the annex rules use the captured `project_type` tokens; every register's `lookup_key` binds a seeded table whose keys are `n1 … nN`; the split ranges equal the prod VR totals (21 / 19 / 17 / 33); the rating options are prod's count scale.
- The runtime scratch check found the decidability facts before encoding (empty register → 0; scalar denominator → `manual_required`; 67-term OR eligible), which is why the equations test was green first run; the three REDs were mine (a scratch `??`, an arithmetic pin, the footer format).
- No `String.replace` with a replacement string on any repo file (the two one-line splices used `split/join` in `node -e`; the generators write whole files); the scratch scripts live in the scratchpad; the repo scratch file `scratch-rt-m8203.ts` was deleted (not staged).
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` confirmed gitignored; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines, the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing keyed on `project_type` works until m820_3-C-1** — `consumer_worksheets = ["ALL"]` is a prod token no worksheet code matches, so the 126 emitted rules are `pending` (visible, inert) and the STAGED gates G-1 / G-2 would be too; one consumer UPDATE lights everything (the fll_gar-C-1 class — the same defect sits on 12 more M8203-01 / -03 rows).
2. **The hand-typed counts are NOT removed by this task** — the counts / shares are twins beside the manual fields until D-1 (48 fields) / D-2 (2 fields) are ratified; REQ-15 … REQ-24 keep reading the manual inputs. Ratifying D-1 is a 192-statement block, generated, with the reverse for each; 0 stored parameters exist for the standard today, so the switch is data-safe now.
3. **The `_rated` annex sums and `projektstopp_code` on -24 need inputs the engineer may never set** — C-5 (consumer edit) for the rated sums; for the overall Projektstopp code a single-type project must set the other annex's 13 / 54 goals `nicht_zutreffend` (the per-annex codes compute without that).
4. **A latent guard conflict after C-5** (the fll_naturteich-G-1 class): once the twelve `_rated` outputs carry consumers, the emitter refuses their emitted `visible_when` at the next re-emit — withdraw those twelve rules then.
5. **QE_A4 stays `imported_unverified`** (two empty printed Nr. cells) — a 1-minute PDF look at p. 24 flips it.
6. **Bundle growth:** the 193 spans (~95 KB) are the largest Plan-3 seed so far (Task-0 observation; Task 30 measures).
7. **Token/time:** ≈ 0.55 M tokens in, ≈ 50 min wall-clock — below the Task 4–8 band (0.53–0.62 M / 55–75 min) because the whole encoding is one template over a 12-row spec and the spans were lifted by a parser rather than by hand.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

Project-specific additions to the catalogues ("ergänzt", L67 / L200 — I-1, free text only); the fulfilment rate and the verdict bands (not printed — F-1); the Risikoanalyse after a missed Phasenziel (L307 second sentence — no field); the Bild 1 steps as printed labels (image — U-2); the §7.2.2 … §7.5 digital goals beyond the existing booleans (L591 … — attestation candidates); the LPH-driven applicability of B.2 … B.6 (M-1); the DWA-M-820-1 / -2 inheritance of identity / LPH / BIM facts (cross-standard, inventory win 5). Each is on the sign-off sheet with its line or listed here.

---

# Fix round 1 (review verdict on `cee1e98`: APPROVED with two Important items + minors)

- **Commit:** see the reply (one commit, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; the seed / field-config / equation migrations were regenerated by the Task 0 emitters only.
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · model provenance per the commit trailer; the subagent ran on Opus 5.

## IMPORTANT

1. **Projektstopp codes trigger on BOTH printed states (J-2 reframed).** `equations/m820_3.ts`: `PROJEKTSTOPP_TRIGGER = "IN {'nicht_erreicht', 'teilweise_erreicht'}"`, `projektstoppFormula` now emits `pz IN {…}` per goal for `M8203-22-D3` / `M8203-23-D3` / `M8203-24-D1`; every one of the three keeps `verification_quote = norm(Q_L307)` — "Werden Phasenziele nicht oder nur unvollständig erreicht, ist die Prüfung eines Projektstopps erforderlich. Im Rahmen einer Risikoanalyse muss bewertet werden, ob und wie das Projekt fortgeführt werden kann." (L307, pinned verbatim in the test). Descriptions (equations + the three created fields' labels / descriptions in `field-configs/m820_3.ts`) say "nicht oder nur unvollständig erreicht" → `nicht_erreicht` / `teilweise_erreicht`. Tests re-pinned: `teilweise_erreicht` on any goal → **1** on all three codes (was 0), the formula prefix `if(pz_52_1_status IN {'nicht_erreicht', 'teilweise_erreicht'} OR …` pinned, `erreicht` / `nicht_zutreffend` everywhere → 0 unchanged. `20260917100920` + rollback re-emitted (3 formulas changed); `20260917100910` + rollback re-emitted (three label / description strings). J-2 on the sheet and in the STAGED file now states: emitted default = the printed sentence (**source-settled by the text, not an interpretation** — L307 names both states and prod's enum maps them to exactly those two tokens); the narrower nicht-only reading is the ALTERNATIVE the owner may choose (its one-line replace + reverse in the block).
2. **D-1 rollback generated, not prose.** The STAGED D-1 block now carries the **192 REVERSE statements** generated from the same template as the 192 forward ones (48 × 4, `BEGIN … COMMIT`): manual field `widget = NULL, ui_config = NULL` guarded on the forward value (`f.widget = 'derived' AND f.ui_config IS NULL`), equation output pointed back to `<sym>_calc` with `replace(formula, '<sym> = ', '<sym>_calc = ')` guarded on `e.output_symbol = '<sym>'` + the `Plan 3:` selector, twin reactivated (`active = true … AND NOT f.active` — the scope drops `f.active` because the twin is inactive after the forward), register footer restored (`replace('"<sym>"', '"<sym>_calc"')` guarded on the forward text). Counted in the file: 384 `UPDATE` lines in the block (192 + 192), 48 `SET active = true`. Every line of the STAGED file is still a comment (0 non-comment lines).

## MINOR

3. **Engineer-visible effect until C-5** (report §5 / sheet observation): the two cards `gesamt_anhang_a_items_rated` (M8203-22) / `gesamt_anhang_b_items_rated` (M8203-23) read `manual_required — Fehlende oder leere Eingaben: qe52_items_rated, qe53_items_rated, qe54_items_rated, qe55_items_rated` (resp. `qe62_items_rated, qe63a_items_rated, qe63b_items_rated, qe64a_items_rated, qe64b_items_rated, qe65_items_rated, qe66_items_rated, qe67_items_rated`) — the created outputs are not inherited there (pinned in `equations-m820_3.test.ts`: `reason(run('M8203-22-D2', {}))` contains `qe52_items_rated`).
4. **QE_A4 labels without a synthesized Nr.** `QeItem` gained `nr_printed` (the Nr. as PRINTED, `''` for the two empty cells); `qeCatalogueAsTable` builds `label_de` as `<Nr.> · <Kriterium>` only where a Nr. is printed → QE_A4 rows read `Ökonomie/Wirtschaftlichkeit` / `2 · Ökologie/Umwelt` / `Sozioökonomie/Gesellschaft` (row keys n1 / n2 / n3 unchanged, positional). Seed test pins `nr_printed` on all 193 items and the three labels; `20260917100900` + rollback re-emitted; verifier **193/193**.
5. **QE_A3 span note** added to U-1 (sheet + STAGED): the Hinweise of rows n2 … n12 come from the `\multirow[t]{12}{*}{…}` cell printed on the n1 line (L732) — the only seeded cells not contained in their own row's `verbatim_quote` (LaTeX-explicit; `inSpan` skipped for that column).

## Raw output

```
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m820_3 · emit-field-configs-sql.ts m820_3 20260917100910 · emit-equations-sql.ts m820_3 20260917100920
  → 193 rows / 129 field entries + 0 section entries / 115 equations (files regenerated)
pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m820_3 "<transcript>"
 193/193 quotes verbatim in C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-3\DWA-M_820-3.md
pnpm vitest run --project unit <seed-m820_3, field-configs-m820_3, equations-m820_3, register-m820-3-qe, generated-sql-freshness>
 Test Files  5 passed (5)
      Tests  25 passed (25)
pnpm test
 Test Files  278 passed | 1 skipped (279)
      Tests  2619 passed | 1 expected fail | 1 skipped (2621)
   Start at  02:52:59
   Duration  44.53s
pnpm -s typecheck → exit 0 · pnpm -s eslint <5 touched .ts files> → exit 0, no output
```
(2619 unchanged: the re-pinned cases replaced their predecessors.) **The integration harness was not re-run:** the fix round changed emitted DATA only (three formula strings, three label / description strings, two `label_de` cells, the STAGED / sheet / report text) — no engine, harness seed, or prod-shaped row the M820-3 harness reads; the 25/25 run recorded above stands.
