# Plan 3 — ISO-14046 (`iso14046`) — Task 26 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 27)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `6420adb`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write; `git stash` not used. Prod was read ONLY by (a) `node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code ILIKE '%14046%'"` (→ `ISO-14046`, version `ISO 14046:2014`, issued_year null), (b) `node scripts/regulation-tables/build-prior-snapshot.mjs ISO-14046 iso14046` (read-only tx) and (c) `node scripts/verification/capture-text.mjs ISO-14046 iso14046` (read-only tx, full cells → `iso14046.text.prior.json`: gate ids / md5 / conditions / titles / descriptions, the EQ-01 id / md5 / status / formula, worksheet ids, section titles, field labels / units / is_required / consumers, the 0 stored-parameter / 0 instance counts). `.env.local` never printed (variable NAMES listed once with `grep -oE '^[A-Z_]+='` — `DATABASE_URL_PROD=` present).
- **SR-1 source:** every seeded value and every cue was read in this session from the SPANISH markdown transcript `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\ISO-14046\ISO-14046.md` (1096 lines; NTC-ISO 14046 — "adopción idéntica (IDT) por traducción de la norma ISO 14046:2014", title page L13–L15; markdown = the SR-1 source per owner ruling 2026-09-05, **VC grade**; quoted in Spanish as printed). Read in full lines (`sed -n 'A,Bp'` with line numbers, never `cut` on a quoted line — amendment O): title page L1–L26, TOC L123–L175, §1–§3 L223–L404 (definitions 3.1.9 / 3.3.11–3.3.14 / 3.5.6), §4 L405–L462 (glanced), §5.1 L463–L505, §5.2 L507–L615, §5.3 L617–L735, §5.4 L737–L846, §5.5–§5.6 L848–L877, §6 L879–L996, §7 L998–L1036, Anexo A L1038–L1083, bibliography L1085–L1096. The 103 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-iso14046-quotes.mjs`, each line trimmed / empty lines dropped, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-iso14046.ts`, never hand-edited); every seeded cell is asserted inside its span at build time (`inSpan`, whitespace-collapsed) and the six printed OCR quirks of S6_2_TP are asserted PRESENT so no silent correction can slip in. The inventory was used for pointers only (its "§5.2.2 a)–j)" pointer was refuted by the transcript — J-4).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `iso14046` | 2 (S5_2_4_2_DQ, S6_2_TP) | 17 (17/17 verbatim) | 0 rows skipped; S5_2_4_2_DQ **`md_verified`** (every displayed cell legible), S6_2_TP **`imported_unverified`** (six printed OCR quirks inside its displayed sub-item cells — U-1, kept verbatim) | 5 (all `create`: `elementary_flows` -03, `impact_categories_14046` + `lci_cf_rows` -04, `significant_issues_14046` -05, `review_panel` -07) + 2 `select_many` checklists (`data_quality_items` -02, `third_party_report_items` -06) | 0 (every driver is a prod enum — tokens kept, D-1) | 2 (created json: 10 = §5.2.4.2 a) – j); 7 = §6.2 a) – g)) | 0 (no numeric table) | **8** rule-bearing entries: **1 UPDATE** on an existing input (`review_panel_members` -07 ← `critical_review_type == 'panel_review'`) + 7 on created fields (`reference_period_inventory` ← the created `baseline_conditions_present == true`; `weighting_report_note` ← `weighting_applied == true`; `third_party_report_items` ← `report_type == 'third_party'`; `review_panel` + its three outputs ← `panel_review`); plus 1 row-scope rule inside `elementary_flows` (`releases` ← `direction == 'output'`) | 0 (the brief's -04 rules on `study_type` are refused → G-1) | **15** (-03 D1–D10 flows Σ / balance / count / per type; -04 D1 Σ contributions; -05 D1 count; -07 D1–D3 panel count / independent chair / ≥ 3) | 0 replacements (EQ-01 untouched); 1 R-block (EQ-01 onto rows, archive pattern); 12 gate blocks (G-1 … G-12, incl. 3 NEW gates REQ-23 / REQ-24 / REQ-25), 3 C-blocks, 26 amendment-K pairs | **55** blocks (+6 observations) | ≈ 0.53 M (tool-marker delta 15.00 M → 14.47 M at report time) | ≈ 40 |

Field entries: 26 = **25 `create`** (5 register carriers, 15 derived outputs, 2 json checklists, 1 boolean attestation `baseline_conditions_present`, 2 text fields `reference_period_inventory` / `weighting_report_note`) + **1 UPDATE** (`review_panel_members` — no consumers, no gate reads it; the emitter accepted it). Migration `20260917102610`: 25 `INSERT … WHERE NOT EXISTS`, 1 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes the 25 `Plan 3:` rows and restores the one prior NULL. No `enum_values` is written by any UPDATE (D-1). The emitters printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — pinned: no captured field lists its own worksheet; 0 unparseable gates); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  349 passed | 1 skipped (350)
      Tests  3088 passed | 1 expected fail | 1 skipped (3090)
   Start at  23:44:24
   Duration  59.77s (transform 18.32s, setup 59.31s, import 153.47s, tests 65.72s, environment 236.85s)
```
(3068 at `6420adb` + 20 from this task: seed-iso14046 5, field-configs-iso14046 6, equations-iso14046 7, register-iso14046-flows 2; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`, first run). `pnpm -s eslint <11 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task; run at HEAD `6420adb` BEFORE any of this task's files existed — 22/22 — and again after):
```
pnpm vitest run --project integration tests/harness/iso14046-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  22 passed (22)
   Start at  23:45:29
   Duration  5.78s (transform 889ms, setup 0ms, import 3.70s, tests 1.91s, environment 0ms)
```
The harness was NOT edited (no stale pin; baseline and post-change runs identical). It seeds the 7 worksheet templates, the 52 gate-read field symbols and the 21 BLOCK gates of prod (`tests/harness/seed-iso14046.ts`; REQ-21 `TRUE` warn is excluded by design; EQ-01 is NOT seeded — its `SUM()` form is documented there as faithful-to-prose / NR-for-execution), so the new rows are not in it by construction; it proves the branch still drives every ISO-14046 block gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch probe through the real `prepareRegisterRows` / `evaluateFormula` / `parseCondition` / `evaluateCondition` / `emitFieldConfigSql` / `gateReaders` / `producerChain` (`scripts/_probe_iso14046.ts`, deleted — copy in the scratchpad): the emitter accepted all 26 entries + 15 equations first time (0 warnings); every brief target on an existing field was REFUSED as planned (producer chains naming -04 / -06 / -03 / -02, gate readers REQ-13 / REQ-22 / REQ-18 / REQ-20 / REQ-11) while `comparative_study_equivalence`, `review_panel_members` and `recycling_allocation_type` were ACCEPTED (the first and third are withheld on the transcript's words — J-2 / J-1); the -04 section rule on `study_type` was refused on C / D / E / H (the D refusal naming `lci_result → EQ-01 category_indicator_result (consumed by ISO-14046-05, ISO-14046-06)`) and accepted on the field-less A / B / F / G / K / M (inert — not emitted); a row-scope hidden TEXT cell stores `''` (iso46001 trap 6) and an INCOMPLETE row keeps a stored hidden cell (a262e trap 2); `sum_rows(reg, if(direction == 'input', quantity_m3, 0))` reads 150 / 50 / balance 100 on the fixture, 0 for the absent direction and `manual_required` ("Keine vollständigen Zeilen") on an EMPTY register while `count_rows` reads 0; `if(direction == 'input' AND resource_type == 'rainwater', …)` computes per type; `lci_value * cf` per row (null on the incomplete row) and Σ 160; the per-name Σ computes ONLY with the literal name typed (130 for 'escasez') — the F-1 fact; `count_rows(review_panel, chair == true AND independent == true)` reads 1 / 0 / 0 (unset boxes false); `if(count_rows(review_panel) >= 3, 1, 0)` reads 1 / 0 / 0 (empty ⇒ 0, never a phantom pass); every staged gate text parses (15/15) and the guard families evaluate both ways (recorded per block). Seed builder: verifier **17/17** first run → seed test ONE red on my own pin (I pinned L909 as the lead-in sentence alone; the printed line is one paragraph ending with it — pin changed to `endsWith`) → **5/5**. Field-config module: **6/6** first run. Equations test: **7/7** first run. Render test **2/2** first run. STAGED generator: 55 blocks written first run (every fragment inside its span). Typecheck / eslint green on the first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso14046 "C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\ISO-14046\ISO-14046.md"` → **`17/17 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- S5_2_4_2_DQ (10): a L596 · b L597 · c L598 · d L599 · e L600 · f L601 · g L602 · h L603 · i L604 · j L605 (lead-in / policy L595)
- S6_2_TP (7): a L910–L913 · b L914–L919 · c L920–L941 · d L942–L955 · e L956–L971 · f L972–L978 (the NOTA L980 excluded) · g L981–L984 (lead-in / policy L909)

Status per table: S5_2_4_2_DQ `md_verified` (every row quote lifted, every displayed cell legible; the seed migration carries the one `verification_status` upgrade); S6_2_TP `imported_unverified` (amendment F: the displayed `subitems` cells carry the printed OCR quirks "Ias" L916 / L973, "ii)cualquier" L923, "$y$" L926, "iii)resultado" L927 and the duplicated L940 / L941 line — kept verbatim, U-1; nothing corrected).

Beyond the verifier (table rows only), the STAGED generator asserted every `"…" (L…)` fragment of the sheet section and the STAGED file inside its span (**all 55 blocks**, whitespace-collapsed; it throws on a fragment outside its span, never retyped), and the module `verification_quote`s are the `Q` constants themselves (verbatim by construction; pinned non-empty / no `undefined`).

**Absence / presence claims (amendment O — commands re-executable, raw output; T = the transcript):**
- No printed table (inventory §1 confirmed): `grep -n "Tabla\|Table\|Tabelle" "<T>"` → (empty, exit 1); `grep -n -i -w "tabla\|table\|tabelle" "<T>"` → (empty, exit 1); `grep -c "tabular" "<T>"` → `6` (the LaTeX `\begin{tabular}` environment of the title-page correspondence box L12–L20 only). The standard prints no value table; characterisation factors come from external LCIA methods (content-boundary rule — `lcia_methodology` / `characterization_model` stay text; the register's `cf` is typed).
- The §7.4 minimum IS printed: `grep -n "tres miembros" "<T>"` → `1030:… un panel de revisión constituido por al menos tres miembros. …` (exit 0) — the literal 3 of ISO-14046-07-D3 (controller resolution 7).
- The §5.2.4.2 modal: `grep -c "deben tratar" "<T>"` → `0` (exit 1); `grep -n "deberían tratar" "<T>"` → `595:Los requisitos para la calidad de los datos deberían tratar lo siguiente:` (exit 0) → `anhaltswert` (controller resolution 2 / J-4).
- The OCR quirks: `grep -n "Ias \|Ios " "<T>"` → `916:2) Ias aplicaciones previstas;` / `973:1) Ios resultados;` (exit 0) — kept verbatim (U-1).
- No printed threshold for "significativa": `sed -n '848,860p' "<T>" | grep -n "%"` → (empty, exit 1) (J-5).
- O-1 — the brief's targets are absent from the CAPTURE (not a transcript claim): `grep -c "weighting_report_note\|reference_period_inventory\|baseline_conditions_present" src/lib/eval/field-configs/iso14046.prior.json` → `0` (exit 1) — all three CREATED by 20260917102610 (also pinned on the loaded prior, together with the absence of `review_panel` / `elementary_flows` / `lci_cf_rows`).
- The prod enum labels: `iso14046.prior.json` holds `label_de` (German) + `label_en` (English) for every enum; the transcript prints Spanish only → E-1 (tokens matched to L652–L657 / L660–L665 / L1052–L1053, labels never rewritten).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code ILIKE '%14046%'"
  → ISO-14046 · 'ISO 14046:2014' · issued_year null · 'Umweltmanagement - Wasser-Fußabdruck - Grundsätze, Anforderungen und Leitlinien'
node scripts/regulation-tables/build-prior-snapshot.mjs ISO-14046 iso14046
  → wrote src\lib\eval\field-configs\iso14046.prior.json: 73 field rows (0 orphan), 53 coded sections of 53, 1 equations, 22 gates (0 unparseable …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
node scripts/verification/capture-text.mjs ISO-14046 iso14046
  → wrote src\lib\eval\field-configs\iso14046.text.prior.json: worksheets 7, sections 53, fields 73, requirements 22, equations 1, stored parameters [{"n":0}], instances [{"n":0}]
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso14046
  → wrote seed + rollback for iso14046 -> scripts/migrations/20260917102600_regulation_tables_seed_iso14046.sql scripts/rollback-20260917102600-regulation-tables-seed-iso14046.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso14046 20260917102610
  → wrote 26 field entries + 0 section entries for iso14046 -> scripts/migrations/20260917102610_field_configs_iso14046.sql scripts/rollback-20260917102610-field-configs-iso14046.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso14046 20260917102620
  → wrote 15 equations for iso14046 -> scripts/migrations/20260917102620_equations_iso14046.sql scripts/rollback-20260917102620-equations-iso14046.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-iso14046.test.ts` / `equations-iso14046.test.ts` pin the other two against the committed `iso14046.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 17 row upserts (`ON CONFLICT (table_id, row_key)`) and 1 `verification_status = 'md_verified'` upgrade (S5_2_4_2_DQ); its rollback deletes the two tables (no earlier ISO-14046 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 15 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102600` → `20260917102610` → `20260917102620`; rollback in reverse. Every file touches `ISO-14046` rows only. **Materialisation (amendment D, noted once — iso14046-I-1):** all 15 rows are register-fed on their own worksheets and materialise on save; no scalar-only row this task. The emitted rules hide on the form / report / PDF but are `pending` (visible) for the server-side `computeVisibility` (templateFields only — vsme-X-3 [CODE]) — fail-safe.

## 5. What was encoded

**Tables (2)** — `src/lib/eval/regulation-tables-seed-iso14046.ts`, builder `iso14046SeedTables()`, edition `'2014'` (title page L13–L15 "adopción idéntica (IDT) por traducción de la norma ISO 14046:2014."; prod `standards.version` "ISO 14046:2014"). The standard prints NO numeric table; its catalogues are text lists:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| S5_2_4_2_DQ | `item` a … j | `letter`, `text` (the printed item without its marker) | anhaltswert (L595 "deberían tratar lo siguiente") | 10 | md_verified |
| S6_2_TP | `item` a … g | `letter`, `heading` (the printed heading after the marker), `subitems` (the printed 1) … / i) … lines joined, OCR quirks verbatim) | locked (L909 "debe cubrir los siguientes aspectos") | 7 | imported_unverified (U-1) |

**Registers / selections / visibility** — `src/lib/eval/field-configs/iso14046.ts`:
- `elementary_flows` (-03 C, unconditional, `create` json): `unit_process` (required), `direction` (enum input / output, required, discriminator), `quantity_m3` (m³, required, min 0), `resource_type` (enum = the six prod `flow_water_resource_type` tokens + prod labels, required), `quality`, `form_of_use` (enum = the seven prod `flow_form_of_use` tokens), `location`, `temporal`, `releases` (← `direction == 'output'`, §5.3.2 g)); footer `water_input_total` / `water_output_total` / `water_balance_diff` / `elementary_flow_count`; note L647 + L649 + D-1 … D-14. The register IS the §5.3.2 inventory ("entradas y salidas para cada proceso unitario"); the inventory phase aggregates nothing across type / quality / form / place / period (L675), so the rows stay separate and the Σ are per direction and per resource type.
- `impact_categories_14046` (-04 C, `create` json): `category` (required), `indicator_name`, `indicator_unit`, `model`, `result` (TYPED number — F-1); note L777 + L838 + D-15 … D-18 / D-26. The register is `_14046`-suffixed because prod's text field `impact_categories` occupies the natural name on the same worksheet (iso46001 trap 5).
- `lci_cf_rows` (-04 E, `create` json): `category` (required), `flow`, `lci_value` (required — never `lci_result`, a -04 prod symbol), `cf` (required), `contribution` = `lci_value * cf` (the printed definition 3.3.14 L325 / prod EQ-01 per row); footer `category_indicator_total`; note L325 + L785 + D-19 / D-20 / R-1.
- `significant_issues_14046` (-05 B, `create` json): `issue` (required), `contribution_pct` (%, 0 – 100), `mechanism`; footer `significant_issues_count`; note L851 + D-21 + J-5.
- `review_panel` (-07 E, `create` json, ← `critical_review_type == 'panel_review'` — inherited on -07, pinned): `name` (required), `affiliation`, `independent`, `chair`; footer `review_panel_members_calc` / `panel_chair_independent` / `panel_min_members_ok`; note L1030 + D-22 + G-3.
- `data_quality_items` (-02 F, created json, `select_many`, 10 options = the printed a) – j) lines); `third_party_report_items` (-06 C, created json, `select_many`, 7 options = the printed heading lines "a) aspectos generales:" …, ← `report_type == 'third_party'` — inherited on -06). The booleans `data_quality_requirements` / `third_party_report` stay (D-24 / D-25).
- `baseline_conditions_present` (-02 C, created boolean attestation, §5.2.2 n) L534 "si es aplicable") and `reference_period_inventory` (-02 C, created text ← `baseline_conditions_present == true`, NOTA L536 + §6.2 d) 8) L955); `weighting_report_note` (-04 H, created text ← `weighting_applied == true`, L844 — the brief's target absent from prod, O-1).
- Field `visible_when` on existing inputs: **one** — `review_panel_members` (-07 E) ← `critical_review_type == 'panel_review'` (§7.4 L1030; §5.2.2 p) L538). Every other brief target is refused by the producer guard and / or the gate-aware guard (pinned per target) and routed to G-1 / G-2 / G-4 / G-5 / G-6 / G-7, or withheld on the transcript's words (J-1 / J-2 / J-3). Section rules: none (the -04 `study_type` rule → G-1; see §8.3).

**Equations (15 new)** — `src/lib/eval/equations/iso14046.ts`: `ISO-14046-03-D1 water_input_total = sum_rows(elementary_flows, if(direction == 'input', quantity_m3, 0))` · `-D2 water_output_total` (output) · `-D3 water_balance_diff` (inline Σ in − Σ out — never chained on D1 / D2) · `-D4 elementary_flow_count = count_rows(elementary_flows)` · `-D5 … -D10 water_input_<token> = sum_rows(elementary_flows, if(direction == 'input' AND resource_type == '<token>', quantity_m3, 0))` for the six prod tokens · `ISO-14046-04-D1 category_indicator_total = sum_rows(lci_cf_rows, contribution)` · `ISO-14046-05-D1 significant_issues_count = count_rows(significant_issues_14046)` · `ISO-14046-07-D1 review_panel_members_calc = count_rows(review_panel)` · `-D2 panel_chair_independent = count_rows(review_panel, chair == true AND independent == true)` · `-D3 panel_min_members_ok = if(count_rows(review_panel) >= 3, 1, 0)` (the 3 = the printed "tres", L1030). None outputs a prod symbol (EQ-01 keeps its row — R-1); every row is register-fed on the register's worksheet. **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows`): 120 + 30 in / 50 out ⇒ 150 / 50 / 100 / count 3 / rainwater 30 / groundwater 120 / surface_water 0 (the 50 is an output) / fossil 0; only inputs ⇒ Σ out 0 (computed); EMPTY or all-incomplete ⇒ every Σ `manual_required` ("Keine vollständigen Zeilen"), count 0; LCI rows 100 × 0.5 / 40 × 2 / 3 × 10 ⇒ 50 / 80 / 30, Σ 160, a row without CF incomplete (null), empty ⇒ manual_required, the per-name Σ 130 only with the literal typed; issues 1 / 0; panel 3 / 1 / 1, 2 / 0 / 0, 0 / 0 / 0. Not emitted: the brief's -02 D1 / -06 D1 (`contains()` over the checklists — no engine path passes json carriers; din14021-F-1 precedent) → **iso14046-F-2** with the intended formulas; the per-category Σ by engineer-typed name → **iso14046-F-1**.

**STAGED (`scripts/verification/iso14046-STAGED-plan3-rulings.sql`, 664 lines, 0 non-comment lines, 55 ☐ blocks; generated from the captures)** — G-1 the -04 impact assessment ← `study_type` (REQ-12 / REQ-14 / REQ-15 IF-guarded + the ten section hides in one transaction; today REQ-12 blocks every inventory study — L477–L480 / L484); G-2 REQ-22 IF-guarded on `is_organization_assessment == true` + the hides of `organization_boundary` / `consolidation_method` (today every product assessment must pick a consolidation method); G-3 NEW warn gates REQ-23 (`panel_min_members_ok == 1`) / REQ-24 (`panel_chair_independent >= 1`) for a panel review (modal "debería"); G-4 REQ-13 OR→IF + the qualifier hide; G-5 REQ-18 OR→IF + the `third_party_report` hide; G-6 REQ-20 OR→IF and REQ-19 presence→IF (no hide — J-3); G-7 REQ-11 IF-guarded on the two non-avoided allocation tokens + the owner-applied hides (IN guard never exempt — pinned); G-8 NEW block gate REQ-25 `IF weighting_applied == true THEN comparative_assertion_public == false` (L846 "no deben"); G-9 REQ-10 → `elementary_flow_count >= 1 AND inventory_balance_explained == true` (after D-2 … D-5); G-10 REQ-05 deletion (archive pattern, after D-8 … D-12); G-11 REQ-16 `significant_issues IS NOT EMPTY` → `significant_issues_count >= 1` (after D-21); G-12 REQ-06 → the ten-clause `contains()` checklist form + warn (after F-2); C-1 `category_indicator_total` → -05 / -06; C-2 the ten -03 outputs → -04; C-3 `significant_issues_count` → -06; D-1 … D-26 the amendment-K pairs (D-1 … D-7 the flow scalars, D-8 … D-14 the -02 scope scalars incl. `seasonal_aspects`, D-15 … D-18 the category scalars, D-19 / D-20 the EQ-01 inputs, D-21 issues, D-22 the panel count, D-23 baseline, D-24 / D-25 the checklist booleans, D-26 the profile text — each states data_type / is_required / gate readers / consumers, all data-free); R-1 EQ-01 onto rows (archive + delete after D-18 … D-20 and F-1; md5 `92dd32e56a8cf39d593a10b33d0c75b1`, `verified_via_cross_reference`); F-1; F-2; E-1; I-1; J-1 … J-5; O-1; U-1; X-1 (`sites_14046` Phase 6); X-2 (cross-standard twins + the six scope fields). Every gate block archives the row (`compliance_requirements_archive_iso14046`), guards on `md5(condition)` (ids / md5 from the capture, re-checked with node:crypto), restores with the explicit column list; every staged condition parse-checked (15/15) and the guard families evaluated both ways in-session; the one prod equation is touched only by R-1.

## 6. Inventory §5 top-wins walk

1. Elementary-flow register in -03 (one row per unit process × flow with the seven §5.3.2 attributes), retiring the six re-typed -02 scope fields → **encoded** (`elementary_flows` with a) – g) as columns, `releases` on outputs only, Σ per direction / per type / balance / count); the retirement of the six scope fields + the seven -03 flow scalars is D-1 … D-14 (amendment K; Phase-6 pointer X-2). ✓
2. Impact-category register in -04 with EQ-01 evaluated per row and `water_footprint_profile` derived as the row set → **partially**: `lci_cf_rows` carries `lci_value × cf` per row and `category_indicator_total` (Σ over all rows) is emitted; `impact_categories_14046` holds the categories with a TYPED per-category result — the Σ by engineer-typed category name is not expressible (no grouped aggregate; **F-1** [CODE]); EQ-01 stays (R-1 staged); the profile ↔ register pair is D-26. ✓ (rows + total) / F-1 (per-category Σ)
3. Gate -04 on `study_type` so an inventory study is not blocked by REQ-12, and gate the Annex-A fields on `is_organization_assessment` → **STAGED with reason**: both are enforcement changes AND every -04 field-bearing section holds consumed producers (the guards refuse the hides) → G-1 (REQ-12 / -14 / -15 IF-guards + the section hides) and G-2 (REQ-22 IF-guard + the two hides). ✓ (guards STAGED)
4. Inherit site water volumes from ISO-46001 / VSME B06 and a sites register from VSME B01.200 → **Phase 6** (X-1 / X-2; nothing encoded across standards). deferred-with-reason
5. Checklists for §5.2.4.2 a) – j) and §6.2 a) – g) so `data_quality_requirements` / `third_party_report` are derived → **encoded** (both `select_many` over the seeded tables, the §6.2 one visible for third-party reports); the derived completeness codes are F-2 (the booleans stay — D-24 / D-25; G-12 carries the REQ-06 form). ✓ (checklists) / F-2 (codes)

Data-quality gaps of the inventory: the Spanish-only transcript → E-1 (labels EV, tokens matched); REQ-21 `TRUE` → observation (no computable form); units "-" on the dimensioned -04 numbers → `indicator_unit` column, prod untouched; VR "IMPLIES" pseudo-logic → the G-1 / G-2 IF-guards; the -02 text twins of the -03 enums → D-8 … D-14.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`iso14046` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (iso14046 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+55 blocks, +6 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 26)" paragraph).
Created: `src/lib/eval/regulation-tables-quotes-iso14046.ts` (103 spans, generated), `src/lib/eval/regulation-tables-seed-iso14046.ts`, `src/lib/eval/field-configs/iso14046.ts`, `src/lib/eval/field-configs/iso14046.prior.json` + `iso14046.text.prior.json` (captured), `src/lib/eval/equations/iso14046.ts`, `src/lib/eval/__tests__/regulation-tables-seed-iso14046.test.ts`, `src/lib/eval/__tests__/field-configs-iso14046.test.ts`, `src/lib/eval/__tests__/equations-iso14046.test.ts`, `src/components/worksheet/__tests__/register-iso14046-flows.test.tsx`, `scripts/verification/iso14046-STAGED-plan3-rulings.sql` (55 ☐ blocks; 664 lines, 0 non-comment lines), `scripts/migrations/20260917102600_regulation_tables_seed_iso14046.sql` + `scripts/rollback-20260917102600-regulation-tables-seed-iso14046.sql`, `scripts/migrations/20260917102610_field_configs_iso14046.sql` + `scripts/rollback-20260917102610-field-configs-iso14046.sql`, `scripts/migrations/20260917102620_equations_iso14046.sql` + `scripts/rollback-20260917102620-equations-iso14046.sql`, this report (+ the copy at `.superpowers/sdd/…/task-26-report.md`).
Untouched: every other standard's file; the ISO-14046 harness files; `scripts/reasoning-map/` (the unit run churns five files there — checked out before staging); the two scratch scripts (probe, STAGED generator) deleted from `scripts/` — copies with the quotes generator in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **The brief's "§5.2.2 a)–j)" data-quality list is printed under 5.2.4.2** ("Calidad de los datos", L590 / L595–L605); §5.2.2 is the scope list a) – p) (L521–L538). The table is `S5_2_4_2_DQ` (clause_reference §5.2.4.2), not the brief's `S5_2_2_DQ` — a table code naming a wrong clause would be a false pointer in prod (J-4). The brief's file names / slug / migration stamps are unaffected.
2. **`S5_2_4_2_DQ` policy `anhaltswert`** — the lead-in prints "deberían tratar" (should), `grep -c "deben tratar"` → 0 (controller resolution 2). `S6_2_TP` locked ("debe cubrir").
3. **-04 section rules NOT emitted** (brief: `visible_when` on the top-level sections of -04 ← `study_type`): `study_type` IS inherited on -04 (checked), but C / D / E / H hold consumed producers (REQ-12 / -14 / -15 read them too) and A / B / F / G / K / M hold no field — refused / inert → G-1 with the three IF-guards + the ten section hides in one transaction.
4. **Register symbols `impact_categories_14046` / `significant_issues_14046`** (brief: `impact_categories` / `significant_issues`): both names are prod TEXT fields on the same worksheets (the emitter refuses a `create` over an existing symbol); paired in D-15 / D-21. Column `lci_value` (brief: `lci_result` — a -04 prod symbol; amendment P shadow rule). `impact_categories_14046` gained `indicator_unit`; `lci_cf_rows` gained `flow`.
5. **`impact_categories_14046.result` is a typed number** (brief's "derived — per-category Σ"): implemented as the controller's resolution (1) — `contribution` per row + the worksheet-level Σ; the per-category Σ is F-1.
6. **`review_panel` keyed on `critical_review_type == 'panel_review'`**, not the brief's `comparative_assertion_public == true` (§7.4 is the printed driver; the brief lists `review_panel_members` under it too) — J-3.
7. **Three brief hides withheld on the transcript's words** (the emitter would have accepted them — pinned): `recycling_allocation_type` (L729: a closed-loop procedure itself avoids allocation — J-1), `comparative_study_equivalence` (L996 binds every "estudio comparativo" — J-2), `critical_review_performed` (also refused by the guards; L1007 / L1020–L1022 — J-3).
8. **`allocation_procedure != '<avoided token>'`** has TWO avoided tokens in prod (`avoided_subdivision`, `avoided_system_expansion`) → the driver is `allocation_procedure IN {'physical_relationship', 'other_relationship_economic'}` (G-7; the IN guard is never exempt — the hides are owner-applied inside the block).
9. **`weighting_report_note` / `reference_period_inventory` / `baseline_conditions_present` do not exist in prod** (O-1) — all three CREATED (the two text fields as the iso5667_6 `legal_custody_regulations` precedent; the boolean driver as the brief says).
10. **Equations 15 (brief 5 + 1 staged)**: the brief's -03 D1 is ten rows (Σ in / Σ out / balance / count / six per-type); -02 D1 / -06 D1 withheld (F-2); the §7.4 "≥ 3" is emitted with the printed "tres" (controller resolution 7) plus `panel_chair_independent`; `-05-D1` (count) added for G-11. The §7.4 gate proposal is warn (modal "debería"), not block.
11. **G-blocks 12 (brief 3)**: G-4 … G-12 were found by the guards / the capture (REQ-13 / -18 / -20 OR-forms, REQ-11 / -22 / -12 unconditional presence checks that block inventory studies / product assessments / avoided allocation, REQ-05 / -10 / -16 / -06 reading the scalars the registers twin, the printed weighting prohibition L846 as REQ-25).
12. **Sign-off ids 55 (brief ≈ 6)**: the 26 amendment-K pairs and the 12 gate blocks dominate.
13. **E-1 refined**: prod carries German `label_de` AND English `label_en` (both translations; the prod `verification_quote`s themselves are Spanish sentences of this transcript) — tokens matched, labels never rewritten; the printed d) list has six bullets vs prod's seven tokens (`instream_use` + `other` for the last bullet) — recorded, tokens kept.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (17/17); every seeded cell is asserted inside its span at build time (the S6_2_TP OCR quirks asserted PRESENT); every `"…" (L…)` fragment on the sheet and in the STAGED file was asserted inside its span by the generator (55 blocks); the module `verification_quote`s are the lifted `Q` spans; every staged condition parses (15/15) and the guard families were evaluated both ways in-session; prod facts (tokens + labels, consumer lists, section codes, gate / equation ids + md5 (re-checked with node:crypto) + status, worksheet ids, the 0 / 0 stored-value / instance counts, max REQ code, `standards.version`) come from the in-session captures / queries listed in §0 / §4 and the STAGED header.
- The field-config test pins every refusal of the brief's targets (gate readers per symbol, producer chains incl. the EQ-01 hop for the -04 D section, `emitFieldConfigSql` throws), the acceptance of the three withheld hides (so the withholding is a recorded judgment, not a guard artefact), the inertness of the field-less -04 sections, the IF-guard exemption G-5 relies on AND the non-exemption of the IN guard (G-7), the captured gate set (22, REQ-21 the one warn), the one captured equation, the G-A3 / D-1 facts (register enum columns = prod value + label_de byte-for-byte; the checklist values = the seeded rows; every rule literal a prod token), no `id` column and no prod-symbol shadowing (`lci_result` / `impact_categories` / `significant_issues` pinned as the collisions avoided), no self-consumer entries, every create in a captured section, the O-1 absences, the freshness of migration + rollback with exactly 1 UPDATE.
- Runtime facts were probed through the real register / formula / condition / emitter contracts before the pins (probe deleted, copy in the scratchpad); the render test drives `elementary_flows` (input vs output rows: the `releases` column only on the output row, the six-token resource select with prod labels, the Σ / balance / count / per-type equations over the stored rows, the direction switch revealing releases and moving the quantity between Σ in and Σ out, the new-row incompleteness until quantity + type are entered) through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file: the registry lines, the playbook paragraph and the sheet section are marker-checked `split/join` splices (CRLF-aware) or Write-tool files; the modules, tests and generators were written with the Write tool; the one pin correction used the Edit tool.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Two prod enforcement errors found by reading the capture, both STAGED, neither fixed today:** REQ-12 blocks every water footprint INVENTORY STUDY (§5.1: three phases, no impact assessment) and REQ-22 demands a consolidation method + organisation boundary from every product / process assessment (Annex A is for organisations). Fail-safe by construction (nothing changed), but the owner should see that today no inventory study and no non-organisation assessment can pass -04 / -02.
2. **The per-category indicator result stays hand-typed (F-1)** — the headline "EQ-01 per row" is delivered as the per-row product + the Σ over all rows; the Σ by category name needs a grouped aggregate or a cross-register row reference ([CODE]). Until then `impact_categories_14046.result` is a derived value the engineer copies (recorded, never silent).
3. **F-2 is the checklist gap this standard exposes twice** (the two completeness codes and G-12's gate form) — the same [CODE] item as din14021-F-1 / iso46001-F-2.
4. **26 amendment-K pairs** — 19 recommend DEFER (gate-read or gate-driving scalars, the EQ-01 triple, the checklist booleans), the rest retire on ratification; all data-free (0 stored values / 0 instances).
5. **S6_2_TP stays `imported_unverified` on OCR quirks alone** (every row quote is verbatim) — the PDF (SR-3) settles the six cells; a later pass may normalise them and upgrade the table.
6. **The §7.4 minimum panel size uses a formula literal (`>= 3`)** per controller resolution 7 (the printed "tres" quoted beside it, pinned against the span); a one-row constant table would have avoided the literal — noted as the alternative in the playbook paragraph.
7. **Token/time:** ≈ 0.53 M tokens in, ≈ 40 min wall-clock — in the Task 23 / 25 band; the cost sits in the 26 D-pairs and 12 gate blocks (generated), not in the transcript (17 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§4.1–§4.13 the thirteen principles as separate attestations (prod holds one boolean `principles_applied`); §5.2.2 g) assumptions / value choices / optional elements, i) which result parameters (prod `results_type` covers it), k) covered cause-effect chains + foreseeable consequences of excluded impacts, l) uncertainties, m) justifications for exclusions (L527–L533) — no fields beyond `limitations`; §5.2.3 the identification of unit processes needing primary data vs secondary data (L558–L560) — a per-process attribute the flows register could carry (`data_source` column, not added — no printed closed list); §5.2.4.1 e) land-use changes to drainage / evaporation, h) storage duration (L575 / L578) — no field; §5.2.4.2 secondary-data justification (L593) — no field; §5.3.1 a) – e) the ISO 14044 calculation procedures (L622–L628) — the two booleans `calculation_procedures_documented` / `data_validation_done` only; §5.3.3.2 the three-step hierarchy per shared process (L699–L703) — one scalar `allocation_procedure`, no per-process register; §5.3.3.3 the order of allocation bases for recycling (L732–L735) — no field; §5.4.2 the environmental mechanisms per method (L777) — `characterization_model` text only; §5.4.5 / §5.4.6 the availability / degradation qualifiers per selected category (L815–L834) — no field beyond `water_footprint_qualifier`; §5.5 b) – g) the interpretation items beyond the register (the three checks, conclusions, limitations, uncertainty, sensitivity ranges) — prod's scalars; §6.1 redundant indicators disclosure (L896), improvement initiatives (L899), consistency check for comparisons (L901) — no field; §6.2 the sub-items 1) … / i) … per aspect (in the table, not ticked individually); §7.1 the five review verifications (L1010–L1014) — no field; §7.3 / §7.4 the review statement / responses in the report (L1026 / L1034) — `critical_review_statement` text only; Annex A.3 direct vs indirect inventories and the cradle-to-grave / cradle-to-gate choice (L1070–L1083) — no field (X-1); the bibliography (L1085–L1096) — pointers (content-boundary rule). Each stays outside this encoding or on the sign-off sheet.
