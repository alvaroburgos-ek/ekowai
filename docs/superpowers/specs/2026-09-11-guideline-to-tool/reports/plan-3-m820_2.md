# Plan 3 — DWA-M-820-2 (`m820_2`) — Task 19 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 20)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (raw `claude --version` output "2.1.260 (Claude Code)"; no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `a687568`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-2 m820_2` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 28 worksheet titles, the labels / `data_type` / `is_required` / units / VR of the 67 fields on the 17 touched-or-driver worksheets, the 22 compliance rows of interest with ids / severities / md5 / lengths / titles, the max REQ code, the `information_schema` column lists of `compliance_requirements` / `project_parameters` / `fields`, the section codes / titles / field counts of -05 / -19 / -24, the stored-parameter count per field of the standard (2 rows, both on -10 booleans — none on a register) and the 140 worksheet instances). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-2\DWA-M_820-2.md` (2701 lines, mathpix LaTeX; title page L1–L80, §1 L334–L338, §4.2 L547–L566, §4.3.5–§4.3.7 L647–L713, §4.6 L890–L936, §5.2 L1028–L1073, §5.3.1–§5.3.2 L1118–L1156, §5.3.5 L1192–L1207, §5.3.8–§5.3.9 L1241–L1274, §5.4 L1307–L1383, §5.5 L1384–L1463, §5.6.4 L1553–L1572, §5.7.1 / §5.7.3 L1591–L1701, §5.8 L1784–L1847, §7.5–§7.6 L2130–L2191, §8.3.2 L2296–L2322, §8.7 L2474–L2490, Anhang A / B L2523–L2571 read in full; the cited lines re-printed one by one before lifting; the cue constants of the module were generated MECHANICALLY from the transcript lines — whole line or a regex-named sentence span). The inventory was used for pointers only. The `.pdf` / `.xlsx` beside the transcript were NOT opened (not sources under the md-transcript ruling).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m820_2` | 2 | 29 (29/29 verbatim) | 0 rows skipped; 0 unreadable cells — both outline catalogues `md_verified` (every displayed cell legible) | 7 (6 `create`: `offene_punkte` -06, `entscheidungen` -12, `dritte` -13, `auflagen` -16, `vergaben_los` -19, `gewaehrleistungen` -24; 1 UPDATE = the Plan-1 `change_orders` -21 upgraded in place, column keys kept) | 0 (the brief's `einleitung_vorhanden` is a boolean → `attestation`; 2 created attestations: `einleitung_vorhanden` -16, `bim_methode_angewendet` -27) | 2 (`create` json: `projekthandbuch_kapitel` -05 = the 8 Anhang-B chapters, `statusbericht_abschnitte` -06 = the 19 Anhang-A sections + 2 annexes, grouped) — no S-block | 0 (no numeric table; no key symbol) | **0** (every brief target is consumed by another worksheet AND / OR read by an unguarded same-worksheet gate — each is a STAGED G / C block, §8) | 0 | 14 (-06 D1–D2; -12 D1–D2; -13 D1; -16 D1–D2; -19 D1–D2; -21 D1–D3; -24 D1–D2) | 0 replacements (prod has 0 equations); 15 gate blocks (G-1 … G-15), 4 consumer blocks (C-1 … C-4), 5 D-pairs, E-1, M-1 STAGED | 33 blocks (+5 observations) | ≈ 0.55 M (tool-marker delta 15.00 M → 14.45 M at report time) | ≈ 65 |

Field entries: 25 = 24 `create` (6 registers, 2 select_many, 2 attestations, 14 derived outputs — one per equation) + 1 UPDATE (`change_orders`: widget / ui_config; no `visible_when`, no `enum_values`). Migration `20260917101910`: 24 `INSERT … WHERE NOT EXISTS`, 1 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the UPDATE row to the captured NULLs (the TS `SELECTION_CONFIGS.change_orders` fallback takes over again) and deletes only the 24 `Plan 3:` rows. No `enum_values` is written by any UPDATE (D-1). The emitter printed 0 warnings (no quoted-literal collision, no self-consumer NOTICE — pinned: no captured field lists its own worksheet); default (refuse) mode, no `--gate-guard=warn`. No `lookup_fill`, no `reference`, no `select_one`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  322 passed | 1 skipped (323)
      Tests  2939 passed | 1 expected fail | 1 skipped (2941)
   Start at  15:52:33
   Duration  53.08s (transform 16.00s, setup 54.87s, import 129.79s, tests 54.46s, environment 217.11s)
```
(2921 at `a687568` + 18 from this task: seed-m820-2 3, field-configs-m820-2 7, equations-m820-2 5, register-m820-2-gewaehrleistung 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`; one earlier run showed errors ONLY in the three scratch part files under `scripts/_m820_2_*.part.ts` — assembled into the module and deleted). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output (an earlier run on the module reported 28 `no-unused-vars` warnings on generated cue constants not referenced by the entries — pruned to the 31 used ones, then one more — `L1058`, mentioned only in the header comment — removed by hand).

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/m820-2-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  44 passed (44)
   Start at  15:54:03
   Duration  6.14s (transform 787ms, setup 0ms, import 3.73s, tests 2.21s, environment 0ms)
```
The harness seeds prod's 113 fields / 59 gates (`tests/harness/seed-m820-2.ts`); the new rows are not in it by construction — it proves the branch still drives every DWA-M-820-2 gate both ways through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `evaluateFormula` / `prepareRegisterRows` / `evalCondition` / `parseCondition` (`scripts/_probe_m820_2.ts` + `_probe2_m820_2.ts`, deleted): `sum_rows(change_orders, kosten_eur)` over a row with an EMPTY optional cell is `manual_required` ("Unbekanntes Symbol kosten_eur") while `sum_rows(reg, if(kosten_eur IS NULL, 0, kosten_eur))` reads 15 000,5; `count_rows(reg, status == 'offen')` with an UNSET enum cell is `manual_required` ("Fehlende Eingabe für count_rows(): status") — so `status` / `aenderung` became REQUIRED on the upgraded Plan-1 config (0 stored rows in prod, checked read-only); an unset boolean reads `false` (`erledigt == false` counts it); `count_rows` over an EMPTY register is 0, `sum_rows` is `manual_required` ("Keine vollständigen Zeilen"); **date cells are strings — `min_rows(gewaehrleistungen, beginn)` ⇒ "Operand ist keine Zahl: 2026-03-01"** (F-1); an empty text cell (absent or `''`) reads `''` and `IS NULL` treats it as missing (`begruendung IS NULL` counts 2 of 3); the G-1 staged text parses to `and` of two `guard`s and the unparenthesised form to ONE `guard`; `evalCondition` on G-1 / G-2 / G-3 passes and fails both ways (choice `abnahmepruefung` + planned false ⇒ pass, `testbetrieb` + false ⇒ fail, unset ⇒ pending); a bare `contains(x, 'a')` does NOT parse as a condition while `contains(x, 'a') == true` and the IF-guard pair do (completed-not-included ⇒ fail, both ⇒ pass, not completed ⇒ pass) — the M-1 form. My first probe used a bare function as the `Scope` (`ctx.scope.symbol is not a function`) — a scratch-script slip, fixed to `{ symbol, carrier }`. Seed builder → verifier **29/29** on its first run → seed test **3/3 first run**. Field-config module → the emitter accepted all 25 entries first time (the rule set was planned against the capture's consumer lists and the 59 gates; every refusal of the brief's targets is asserted through `gateReaders` / `emitFieldConfigSql` throws, not discovered) → **7/7 first run**. Equations test **5/5 first run**, render test **3/3 first run** (pins written from the probe). Typecheck / eslint: the unused-constant warnings above (generated cues), both mine.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m820_2 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-2\DWA-M_820-2.md"` → **`29/29 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- ANHANGA (21): a1 … a9_2 — the `\hline <Nr.> & <Titel> \\` rows L2532–L2550 (19); anhang1 L2556 ("Anhang 1: Finanz- und Liquiditätsplan Stand:"), anhang2 L2557
- ANHANGB (8): b1 … b8 — L2563–L2570 (the plain numbered lines)

Status per table: `md_verified` — both (every printed row lifted; every displayed cell — number and title — legible; no image, no empty cell; UTF-8 umlauts clean, checked byte-wise with `cat -A`). No U-block.

Beyond the verifier (which checks table rows only), a scratch checker (`scripts/_check_m820_2_quotes.ts`, deleted) asserted every `verification_quote` fragment of the two modules and every `override_quote` fragment of the two tables against the whitespace-normalised transcript: **89/89 verbatim**; and every `"…" (L…)` fragment of the sheet section and the STAGED file: **53/53 verbatim** (1 `…`-elided fragment checked piecewise).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, valid_from, left(title_de,70) AS title FROM standards WHERE code LIKE 'DWA-M-820-2%'"
  → DWA-M-820-2 · 'April 2023' · issued_year null · valid_from null
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-820-2 m820_2
  → wrote src\lib\eval\field-configs\m820_2.prior.json: 113 field rows (16 orphan), 252 coded sections of 252, 0 equations, 59 gates (4 unparseable — symbols: [] + parse_error; …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m820_2
  → wrote seed + rollback for m820_2 -> scripts/migrations/20260917101900_regulation_tables_seed_m820_2.sql scripts/rollback-20260917101900-regulation-tables-seed-m820-2.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m820_2 20260917101910
  → wrote 25 field entries + 0 section entries for m820_2 -> scripts/migrations/20260917101910_field_configs_m820_2.sql scripts/rollback-20260917101910-field-configs-m820-2.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m820_2 20260917101920
  → wrote 14 equations for m820_2 -> scripts/migrations/20260917101920_equations_m820_2.sql scripts/rollback-20260917101920-equations-m820-2.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m820-2.test.ts` / `equations-m820-2.test.ts` pin the other two against the committed `m820_2.prior.json`, default refuse mode). The seed migration carries 29 row upserts (`ON CONFLICT (table_id, row_key)`) and 2 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the two tables (no earlier DWA-M-820-2 table seed exists — nothing superseded; the Plan-1 `20260911120000_selection_configs_DWA_M_820_2.sql` carries no tables). The equations rollback deletes the 14 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101900` → `20260917101910` → `20260917101920`; rollback in reverse. Every file touches `DWA-M-820-2` rows only. **Every equation is register-fed and lives on its register's worksheet — all 14 outputs are materialised on save** (no scalar-only row; amendment D has nothing to note for this standard).

## 5. What was encoded

**Tables (2)** — `src/lib/eval/regulation-tables-seed-m820_2.ts`, builder `m8202SeedTables()`, edition `'2023'` (title page L9 "April 2023", imprint L48 "© DWA, 1. Auflage, Hennef 2023" = prod `standards.version` 'April 2023'):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| ANHANGA | `abschnitt` a1 … a9_2 (19 printed sections, `ebene` 1 / 2), anhang1 / anhang2 (`ebene` 0) | `nummer`, `titel`, `gedruckt` ("<Nr.> <Titel>" / "Anhang n: <Titel>" — the checklist option), `ebene`; `group_label` Inhaltsverzeichnis / Anhang | anhaltswert (L681 "Es empfiehlt sich, einen Muster-Statusbericht (Gliederungsbeispiel siehe Anhang A) … aufzubauen" — L2525 "Abhängig von den Anforderungen im Projekt kann der Statusbericht auf 1 bis 2 Seiten …") | 21 | md_verified |
| ANHANGB | `kapitel` b1 … b8 | `nummer`, `titel`, `gedruckt` | anhaltswert (L663 "… sind auf das Notwendige beschränkt. Im Anhang B ist ein Gliederungsvorschlag enthalten.") | 8 | md_verified |

Teil 2 prints NO numeric value table (inventory §1 confirmed): the Berichtsturnus / Regelkreis frequencies are default sentences (L688 / L549) and stay enums without a table.

**Registers / selections / attestations** — `src/lib/eval/field-configs/m820_2.ts`:
- **Plan-1 register upgraded in place (UPDATE, keys kept):** `change_orders` (-21, orphan, bottom): `aenderung` required, `datum` typed `date` (Plan-1: text TT.MM.JJJJ), `kosten_eur` optional (a Nachtrag without Kostenwirkung counts 0), `status` required over the Plan-1 tokens offen / genehmigt / abgelehnt / umgesetzt; footer `change_orders_count` / `change_orders_sum` / `change_orders_open`; the Plan-1 `sum_column` retired (D-2b-5); note L702 + L706 + L708 + L1571.
- **Created registers (section C):** `offene_punkte` (-06: punkt*, verantwortlich, termin date, status* offen / erledigt — L1130); `entscheidungen` (-12: nr*, datum*, entscheidung*, begruendung, entscheider — L1134 / L1146 / L1150); `dritte` (-13: leistung* with datalist Fachplanung / Gutachten / Rechtsberatung, buero, beauftragt date — L1198 / L1202); `auflagen` (-16: auflage*, genehmigung text (I-1), frist date, erledigt boolean — L1374 / L1380); `vergaben_los` (-19: los*, verfahren, zuschlag date, auftragswert* EUR ≥ 0 — L1243 / L1247 / L1666); `gewaehrleistungen` (-24: auftragnehmer*, abnahme* / beginn* / ende* dates, maengel_offen ≥ 0 — L1798 / L1807 / L1808 / L1812). Each carries a footer over its equation outputs and the printed sentences as note; no override block, no lookup column.
- **Checklists (created json `select_many`, section C):** `projekthandbuch_kapitel` (-05) — 8 options = ANHANGB `gedruckt` (pinned against the seed rows); `statusbericht_abschnitte` (-06) — 21 options = ANHANGA `gedruckt`, grouped Inhaltsverzeichnis (19) / Anhang (2). Both sit beside the existing booleans (`project_handbook_complete` read by REQ-07; REQ-08 reads the frequency) — gate proposals G-8 / G-9.
- **Attestation drivers (created boolean, section C):** `einleitung_vorhanden` (-16, L1313 / L1341 — the driver REQ-35 lacks, G-2); `bim_methode_angewendet` (-27, L2303 / L2311 — the driver the empty REQ-59 lacks, G-5).
- **Derived outputs (14, `widget='derived'`, section D):** -06 `lop_count` / `lop_open`; -12 `decisions_count` / `decisions_ohne_begruendung`; -13 `third_parties_count`; -16 `auflagen_count` / `auflagen_offen`; -19 `lots_count` / `final_contract_value_calc`; -21 `change_orders_count` / `change_orders_sum` / `change_orders_open`; -24 `warranty_count` / `warranty_open_defects`.
- **Field `visible_when`: 0. Section rules: 0.** Every brief target on an existing field is refused or refuted (§8); no created field needs a hide rule (the attestations are drivers, the registers unconditional).

**Equations (14 new)** — `src/lib/eval/equations/m820_2.ts`: `820-2-06-D1 lop_count = count_rows(offene_punkte)` · `-D2 lop_open = count_rows(offene_punkte, status == 'offen')` · `820-2-12-D1 decisions_count = count_rows(entscheidungen)` · `-D2 decisions_ohne_begruendung = count_rows(entscheidungen, begruendung IS NULL)` · `820-2-13-D1 third_parties_count = count_rows(dritte)` · `820-2-16-D1 auflagen_count` · `-D2 auflagen_offen = count_rows(auflagen, erledigt == false)` · `820-2-19-D1 lots_count` · `-D2 final_contract_value_calc = sum_rows(vergaben_los, auftragswert)` · `820-2-21-D1 change_orders_count` · `-D2 change_orders_sum = sum_rows(change_orders, if(kosten_eur IS NULL, 0, kosten_eur))` · `-D3 change_orders_open = count_rows(change_orders, status == 'offen')` · `820-2-24-D1 warranty_count` · `-D2 warranty_open_defects = sum_rows(gewaehrleistungen, if(maengel_offen IS NULL, 0, maengel_offen))`. None outputs a prod symbol; every input is the register of the same worksheet; no boolean / scalar input; no twin chained. **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): change orders 12 000 / — / 3 000,5 → 3 / 15 000,5 / 2 open, a status-less fourth row incomplete (3 / 2), empty register → 0 / 0 / Σ `manual_required`; warranty 3 rows (2 / — / 0 defects) → 3 / 2, `min_rows` / `max_rows` over the dates `manual_required` "Operand ist keine Zahl: 20…" (F-1), a row missing `ende` incomplete, empty → 0 / Σ open; LOP 3 rows → 3 / 2 open; decisions 3 rows (reason / absent / `''`) → 3 / 2 without reason; Dritte 2 / empty 0; Auflagen (true / unset / false) → 3 / 2 open; lots 250 000 + 75 000,25 → 2 / 325 000,25, a lot without Auftragswert incomplete → 0 / Σ `manual_required` (never a phantom 0 €).

## 6. Inventory §5 top-wins walk

1. REQ-46 / REQ-47 conditional on `testbetrieb_vs_abnahme_choice` → **STAGED as the gate rewrite it really is (`m820_2-G-1`)**: the brief's hide rules on `testbetrieb_planned` / `abnahme_per_bild4` are refused twice (consumed by -23 / -24; read by the unconditional block gates) — hiding would turn the gates `not_applicable`, not fix them. G-1 carries the two parenthesised IF-guard pairs (verified both ways in-session, parse shape pinned) and records the owner's alternative (REQ-47 unconditional, L1623 / L1597). ✓ (guards STAGED)
2. Gewährleistungskalender per Auftragnehmer / Firma with earliest start / latest end → **encoded** (`gewaehrleistungen`, three REQUIRED dates per row, `warranty_count` / `warranty_open_defects` materialised on save); earliest / latest dates are **F-1** (no date functions; probed) — the single pair stays (D-4 / D-5) and REQ-51's switch to `warranty_count >= 1` is G-15. ✓ (dates F-1)
3. Entscheidungs-Register replacing the boolean, REQ-25 derived → **encoded** (`entscheidungen` with Nr. / Datum / Entscheidung / Begründung / Entscheider, `decisions_count` + `decisions_ohne_begruendung` — "einschließlich der Gründe"); the boolean stays (consumed by -15 / -21), REQ-25's rewrite is G-11 (two variants). ✓
4. Vergabe-per-Los register feeding `final_contract_value` / `lot_strategy_documented` → **encoded** (`vergaben_los`; `lots_count` / `final_contract_value_calc` twin — D-1 stages the switch; REQ-31 sits on -13 and cannot read a -19 register → new gate G-14; the Teil-1 Loseausnahme feed is X-1, Phase 6). ✓
5. `project_handbook_complete` and the Anhang-A status-report gate as checklists over the printed outlines → **encoded** (both outlines seeded verbatim as text catalogues, both checklists created with exactly those options); the gates over them (all 8 chapters / the 19 sections) are G-8 / G-9 (anhaltswert outlines ⇒ warn, owner may pick a subset). ✓

Data-quality gaps of the inventory: `lph_completed` value-format mismatch → **E-1** (no UPDATE emitted, `keep_prod`; the DB checklist would render the tokens — a [CODE] item); the empty gates REQ-13 / REQ-50 (observations — no field to read) and REQ-55 / REQ-59 (sit on -25 where none of their symbols resolves — **new finding**, G-5 / G-7 propose the moves); `discharge_permit_extension` without a driver → the created `einleitung_vorhanden` + G-2; `worksheet_status` as a guideline field — untouched; the risk-register duplication across Teil 1 / Teil 2 — not this task.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m820_2` line + the example line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m820_2 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+33 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 19)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m820_2.ts`, `src/lib/eval/field-configs/m820_2.ts`, `src/lib/eval/field-configs/m820_2.prior.json` (captured), `src/lib/eval/equations/m820_2.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m820-2.test.ts`, `src/lib/eval/__tests__/field-configs-m820-2.test.ts`, `src/lib/eval/__tests__/equations-m820-2.test.ts`, `src/components/worksheet/__tests__/register-m820-2-gewaehrleistung.test.tsx`, `scripts/verification/m820_2-STAGED-plan3-rulings.sql` (26 ☐ blocks; 476 lines, 0 non-comment lines), `scripts/migrations/20260917101900_regulation_tables_seed_m820_2.sql` + `scripts/rollback-20260917101900-regulation-tables-seed-m820-2.sql`, `scripts/migrations/20260917101910_field_configs_m820_2.sql` + `scripts/rollback-20260917101910-field-configs-m820-2.sql`, `scripts/migrations/20260917101920_equations_m820_2.sql` + `scripts/rollback-20260917101920-equations-m820-2.sql`, this report (+ the copy at `.superpowers/sdd/…/task-19-report.md`).
Untouched: every other standard's file; `selection-fields.ts` (`SELECTION_CONFIGS` — the Plan-1 configs stay as the `widget IS NULL` fallback; the `lph_completed` / `included_hoai_phases` pair stays un-migrated), `register-configs.ts`; the M820-2 harness files; `scripts/reasoning-map/` (the unit run churns three files there — checked out before staging); the five scratch scripts deleted (probes, quote generator, quote checker; the three module part files were assembled and removed).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Prod worksheet codes are `820-2-NN`, not `M8202-NN`** — every entry, gate and STAGED block uses the captured codes; content sections are `C`, results `D` ("Results / Derived Values").
2. **Field `visible_when`: 0 (brief: 8).** Every target of Step 4 is consumed by another worksheet (`testbetrieb_planned` / `abnahme_per_bild4` → -23 / -24; `discharge_permit_extension`, the VOB trio → -19; `liability_clarified` → -09; `ip_rights_defined`, `bim_basics_established` → -28; `hoai_compliance` → -15 / -19; the -11 booleans → -12 / -15) and / or read by an unguarded same-worksheet gate (REQ-46 / -47 / -35 / -38 / -39 / -40 / -56 / -23) — the emitter refuses each (asserted in the test, not discovered); the only EMITTABLE candidate, `gis_data_quality` on -28, is § 8.7 Geoinformationssysteme, not BIM (J-3). The visibility story is the STAGED gate-guard list G-1 … G-7 plus the consumer blocks C-1 … C-4 — the m820_3 pattern (playbook Task 9 trap 5).
3. **The brief's cue "Ein erster Gliederungsvorschlag" is not printed** (R-5): L663 "Im Anhang B ist ein Gliederungsvorschlag enthalten." and L681 "Gliederungsbeispiel siehe Anhang A" are the cues (O-1).
4. **VOB driver for REQ-39 / REQ-40 is a reading, not printed**: only § 5.5.1 (L1406 / L1408) names the VOB/A; § 5.5.2 / § 5.5.3 name the phase — G-3 stages REQ-38 on the printed sentence, G-4 stages the brief's guard on the other two with the caveat.
5. **"BIM fields (820-2-27/28)" refuted**: none of the -27 / -28 fields is BIM-conditional in § 8.2 / § 8.3.3 / § 8.7 / § 8.8 / § 8.9; the only BIM-conditional item is `bim_basics_established` itself, whose empty gate REQ-59 sits on -25 — the created driver `bim_methode_angewendet` + G-5 (move + guard) replace the brief's hide rows.
6. **`hoai_compliance` ← `contract_type`: no printed rule** (the brief's § 2.2 / § 4.6 cue does not exist; HOAI sentences delimit Grundleistungen) — J-2, withheld.
7. **Bedarfsplanung Konzept / Projekt hide refuted by L336 / L1058** (BOTH are required) — J-1, withheld (m820_1-J-5 class).
8. **REQ-55 / REQ-59 sit on 820-2-25** (Richtlinienverwaltung) with empty conditions — the inventory placed REQ-59 on -27 / -28; G-5 / G-7 propose the moves with the archive pattern restoring `worksheet_template_id`.
9. **Equations 14 (brief ≈ 8 + register aggregates)**: `lop_count`, `decisions_ohne_begruendung` (L1146 "einschließlich der Gründe"), `auflagen_count` added; the brief's `sum_rows(change_orders, kosten_eur)` / `sum_rows(gewaehrleistungen, maengel_offen)` take the `if(x IS NULL, 0, x)` form (a bare optional column breaks the Σ — probed); the earliest / latest warranty dates are F-1 as the brief foresaw.
10. **`change_orders` is an UPDATE entry, not "unchanged config"**: the footer needs the equation outputs, `status` / `aenderung` must be required for the count to be decidable, and D-2b-5 retires the Plan-1 `sum_column`; the column keys are kept and 0 stored rows exist (read-only count).
11. **`lph_completed`: no UPDATE at all** (brief: E-1 "D-1 keeps prod") — the Plan-1 pair stays un-migrated (playbook I-1); E-1 stages the format alignment and names the [CODE] gap (the checklist renders enum values, not labels); M-1's gate is probed in the `contains(…) == true` form (a bare `contains()` does not parse).
12. **Sign-off ids 33 (brief ≈ 8)**: the brief's `m820_2-G-1` … `G-7` were re-numbered in order of appearance (G-1 REQ-46/47 · G-2 REQ-35 · G-3 REQ-38 · G-4 REQ-39/40 · G-5 REQ-59 · G-6 REQ-56 · G-7 REQ-55; the brief's "G-6 REQ-25" is G-11, "G-7 REQ-51" is G-15); C-1 … C-4 (consumer edits behind the refused hides), D-1 … D-5 (amendment K pairs — `final_contract_value`, `vergabeverfahren_used`, `zuschlag_erteilt_datum`, `warranty_start_date`, `warranty_end_date`), E-1, M-1, F-1, J-1 … J-3, I-1 (`auflagen.genehmigung` free text — no cross-register reference), O-1, X-1.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (29/29); every `verification_quote` / `override_quote` fragment of the modules (89/89) and every `"…" (L…)` fragment on the sheet and in the STAGED file (53/53) was machine-checked against the transcript after being read at the cited line in this session; the module's cue constants were generated from the transcript (never retyped) and pruned to the ones the entries use; prod facts (enum tokens, consumer lists, section codes, gate ids / conditions / md5 / titles, labels, VR, `is_required`, the stored-parameter count, `standards.version`) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter accepted all 25 entries and 14 equations first time because the rule set was planned against the capture; the field-config test pins every refusal of the brief's targets (`gateReaders` per gate; `emitFieldConfigSql` throws per consumer), the IF-guard exemption that would NOT rescue any of them (no IF-guarded gate exists on this standard), the G-A3 fact for text catalogues (checklist options ≡ seed rows), the Plan-1 key preservation against `SELECTION_CONFIGS.change_orders`, the four empty gates and their worksheets, the `lph_completed` token set, and that no emitted row carries a `visible_when`.
- Runtime facts were probed through the real register / condition contracts before the pins (probes deleted); the render test drives `gewaehrleistungen` through `RegisterEditor` (three date columns, the defects column moving the Σ, an incomplete third row) and the upgraded `change_orders` (Σ / count / open through `evaluateFormula`, `sum_column` gone).
- Two engine facts drove the design and are on the sheet: date cells are strings (F-1), and `contains()` needs the comparison form (M-1 / G-8 / G-9).
- No `String.replace` with a replacement string on any repo file (splices via `split/join` with a presence-and-uniqueness check that throws before writing; the module was assembled from Write-tool part files by `cat`); one playbook edit was LOST to bash command-substitution of backticks inside `node -e` — caught by inspection, reverted with `git checkout --`, redone from a file (recorded as trap 10 in the playbook).
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns three files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing computed reaches prod's gates yet** — all 15 gate blocks (incl. the top win G-1) and the 4 consumer blocks are STAGED; until the owner ratifies, REQ-46 / REQ-47 keep mis-firing for the model not chosen, and the registers are documentation beside the booleans. That is the fail-safe reading: every target is consumed or gate-read, and a hide would be an enforcement change.
2. **G-4 (REQ-39 / REQ-40 under VOB) and G-1's REQ-47 half rest on the brief's readings**, not on printed conditionals — both blocks say so and offer the alternative (keep unconditional).
3. **REQ-55 / REQ-59 sit on a worksheet where none of their symbols resolves** (new data observation); G-5 / G-7 move them — the archive pattern restores the worksheet id on rollback.
4. **E-1 needs a [CODE] item before it can be ratified as staged**: the ChecklistEditor renders `enum_values[].value`; a DB `select_many` over `lph_0 … lph_9` would show the tokens. Until then the Plan-1 label-string checklists are the better UX and M-1 cannot evaluate.
5. **F-1**: no date arithmetic — earliest / latest warranty dates need a [CODE] item (date-typed `min_rows` / `max_rows`).
6. **Bundle growth:** ~1 KB of lifted spans (the smallest Plan-3 seed; Task 30 measures).
7. **Token/time:** ≈ 0.55 M tokens in, ≈ 65 min wall-clock — inside the Task 16 / 17 band; the transcript is cheap (29 rows), the cost sits in the 59-gate capture reading and the 33 blocks.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The Berichtsturnus default ("in der Regel ein vierteljährlicher Turnus", L688) and the Regelkreis cadence ("zwei- bis vierwöchig", L549) as printed defaults for the frequency enums (no table — an O/J candidate at the next touch); the "mindestens fünf Arbeitstage" pre-distribution of meeting papers (L1130) and the "mindestens zwei Jahre vor Ablauf" Einleitungserlaubnis lead time (L1345) as day-count constants — printed figures without a field (candidates for a one-row table + attestation at the next touch, not encoded here because no worksheet asks a date to compare against — F-1 class); the Vertragsfortschreibung as a separate document with reference to the Ursprungsvertrag (L909 — no register); the Teilabnahmen concept (L923 / L931 — booleans only); the Montageendkontrolle / Abnahmeprotokoll contents (L1692 / L1696 — text); the LPH-9 responsibility and control frequency (L1836 — no field); the § 6 Regelwerke lifecycle (§ 6.3 — booleans); Bild 1–4 (flow schemes, images — not encodable). Each is a procedural sentence or a text rule without a computable value and stays outside this encoding (content-boundary rule) or on the sign-off sheet.
