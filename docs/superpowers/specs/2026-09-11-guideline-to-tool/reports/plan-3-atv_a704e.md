# Plan 3 — ATV-A-704E (`atv_a704e`) — Task 27 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 28)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-24
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `0ee2682`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write; `git stash` not used. Prod was read ONLY by (a) `node scripts/verification/prod-query.mjs --sql "…"` (the `standards` row for `%704%` → code `ATV-A-704E`, version `April 2007`; the `information_schema` column lists of `equations` / `compliance_requirements` / `fields` / `worksheet_sections`; the per-equation NULL checks of the provenance columns), (b) `node scripts/regulation-tables/build-prior-snapshot.mjs ATV-A-704E atv_a704e` (read-only tx) and (c) `node scripts/verification/capture-text.mjs ATV-A-704E atv_a704e` (read-only tx, full cells). `.env.local` never printed.
- **SR-1 source: NONE — this standard has NO TRANSCRIPT.** The only file on disk is the scanned PDF `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ATV A 704E\ATV-A-704E-Operating-Methods-for-Wastewater-Analysis.pdf`; `_encode_report.md` beside it is SECOND-HAND and was **not** opened as a quote source. The PDF was NOT read for content and is NOT described from memory anywhere in this task. The ONLY quotable text is prod's own — `fields.verification_quote` (**91 / 91** rows), `equations.verification_quote` + `source_quote` (**6 / 6** each), `compliance_requirements.description` (**30 / 30**) + `source_quote` (**26 / 30**) — all **grade EV** (encoded, unverified against the standard) and cited as `prod <column> (EV, unverified — no transcript)`. Every value those cells contain that the standard prints as a table is a sign-off item (`atv_a704e-U-1 … U-6`); **no regulation table is seeded** (tables 0, no seed builder, no seed migration, no `SEED_BUILDERS` entry — pinned).

## 0. The failed extraction (amendment O evidence — run ONCE, raw output)

```powershell
PS> $pdf = "C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ATV A 704E\ATV-A-704E-Operating-Methods-for-Wastewater-Analysis.pdf"
PS> Test-Path $pdf
True
PS> & "C:\Users\Ekowai\scoop\shims\pdftotext.exe" -layout $pdf "$env:TEMP\a704e_probe.txt"; Write-Output "EXIT=$LASTEXITCODE"
EXIT=0
PS> $f = Get-Item "$env:TEMP\a704e_probe.txt"; "BYTES=$($f.Length)"
BYTES=37
PS> $t = Get-Content "$env:TEMP\a704e_probe.txt" -Raw; "CHARS=$($t.Length)"; "NONWS=$(($t -replace '[\s\f]','').Length)"
CHARS=37
NONWS=0
PS> [regex]::Escape($t.Substring(0,[Math]::Min(400,$t.Length)))
\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f\f
PS> & "C:\Users\Ekowai\scoop\shims\pdfinfo.exe" $pdf | Select-String "Pages|Encrypted|Page size"
Pages:           37
Encrypted:       no
Page size:       587 x 833 pts
```

**Reading:** the extractor succeeds (exit 0) and writes **37 bytes = one form feed per page, zero non-whitespace characters** — the file is a 37-page image scan with no text layer. `pdftotext -layout` therefore CANNOT unlock a single printed value of this standard, and no `-f/-l` page range would change that. The unblock path is an **OCR by the owner** (ocrmypdf / Acrobat) or a **manual transcription of the named pages**; that is stated verbatim in each of `atv_a704e-U-1 … U-6`. The probe file was deleted.

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `atv_a704e` | **0** (no transcript — no seed builder, no seed migration, no `SEED_BUILDERS` entry; `atv_a704e-U-1 … U-6`) | 0 | 0 | **9** (all `create`: `betriebsmethoden` -01, `qs_massnahmen` -08, `einzelbestimmungen` / `verduennungsversuche` / `aufstockungsversuche` -09, `vergleichsmessungen` -10, `pruefmittel` -11, `mitarbeiter` / `abweichungen` -12) | 0 created; 1 UPDATE on a prod enum (`keep_prod`, visibility only) | 0 | 0 | **8** rules = **1 on an EXISTING field** (`photometer_check_done` -11 ← `testing_equipment == 'photometer'`; the two -06 rules the brief asks for are ACCEPTED by the emitter but WITHHELD — `atv_a704e-J-3`, fix round 1) + 7 on created fields (the -09 C register + its 3 outputs ← `multiple_determination_performed`; the -10 register + its 2 outputs ← the equivalency/parallel OR) + **4 row-scope rules** inside `pruefmittel` | **0 emitted** — the five §4.4 rules the brief asks for are STAGED (`-09 C` / `-10 C` / `-10 D` refused by the gate-aware guard → **G-2**; `-09 D` / `-09 E` ACCEPTED by the emitter but withheld, driver not inherited → **C-2**) | **12** (-08 D1; -09 D1 … D5; -10 D1 / D2; -11 D1 / D2; -12 D1 / D2) | 0 replacements (EQ-01 … EQ-06 untouched; the switch is the R-1 block) | **72** blocks (G 8, C 4, D 42, R 1, F 1, **J 3**, U 6, O 2, X 3, I 2) + 5 observations | ≈ 0.51 M (tool-marker delta 15.00 M → 14.49 M at report time) | ≈ 45 |

Field entries: 22 = **21 `create`** (9 register carriers + 12 derived outputs) + **1 UPDATE**. Migration `20260917102710`: 21 `INSERT … WHERE NOT EXISTS`, 1 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes the 21 `Plan 3:` rows and restores the one prior NULL. No `enum_values` is written by any UPDATE (D-1). The emitters printed 0 warnings; default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  352 passed | 1 skipped (353)
      Tests  3106 passed | 1 expected fail | 1 skipped (3108)
   Start at  13:30:17
   Duration  59.37s (transform 18.95s, setup 57.64s, import 156.58s, tests 67.15s, environment 231.59s)
```
(3088 at `0ee2682` + 18 from this task: field-configs-atv_a704e 7, equations-atv_a704e 9, register-a704e-bestimmungen 2. No seed builder ⇒ the shared seed-index / seed-freshness pins gain no ATV-A-704E row.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`; ONE red on the first run, on my own test line — `cfg.lookup_fill_hint` is not a key of `RegisterUiConfig`; replaced by `cfg.catalog`). `pnpm -s eslint <8 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task; run at HEAD `0ee2682` BEFORE any of this task's files existed — 39/39 — and again after):
```
pnpm vitest run --project integration tests/harness/a704e-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  39 passed (39)
   Start at  13:31:59
   Duration  5.93s (transform 887ms, setup 0ms, import 3.81s, tests 1.94s, environment 0ms)
```
The harness was NOT edited (no stale pin found; baseline and post-change runs identical). It seeds prod's ATV-A-704E templates / gates / equations (`tests/harness/seed-a704e.ts`), so the new rows are not in it by construction; it proves the branch still drives every ATV-A-704E gate and equation through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch probe through the real `prepareRegisterRows` / `evaluateFormula` / `parseCondition` / `emitFieldConfigSql` / `gateReaders` / `producerChain` (`scripts/_probe_a704e.ts`, deleted — copy in the scratchpad):

- every rule condition parses, incl. the OR form `equivalency_check_performed == true OR parallel_analysis_performed == true`;
- the determination set {100, 110, 90} reads count 3 / mean 100 / max deviation 10 % through the D1–D3 forms; an EMPTY register reads count 0 (computed) and mean / max deviation `manual_required` ("Keine vollständigen Zeilen") — never a phantom 0;
- **the filtered-aggregate finding that changed the design:** `max_rows(vergleichsmessungen, abs(dev_pct), kind == 'parallel')` with NO parallel row is `manual_required`, while the brief's `max_rows(vergleichsmessungen, if(kind == 'parallel', abs(dev_pct), 0))` reads **`computed 0`** — a false "0 % deviation" for a check that was never performed. Both forms are in the probe output and both are pinned in the equations test; the module uses the filtered form (playbook Task 27 trap 2);
- the emitter's verdict on every brief target was ASSERTED, not discovered afterwards: `photometer_check_done` ACCEPTED, and `precipitation_influence` / `storage_temperature` ACCEPTED TOO (no consumers, no gate reader) — which is why their withholding in fix round 1 is a recorded judgment (`atv_a704e-J-3`), not a guard artefact; `heating_device_deviation` REFUSED naming CR-026; `pipette_tested_volume` / `pipette_deviation_pct` REFUSED naming CR-025; `parallel_analysis_performed` REFUSED by the producer chain (`consumed by ATV-A-704E-10`); sections `-09 C` / `-10 C` / `-10 D` REFUSED naming CR-019 / CR-022 / CR-023 **and** the EQ-02 / EQ-05 / EQ-06 hops; sections `-09 D` / `-09 E` ACCEPTED (withheld as C-2).

Field-config test: **two REDs on my own pins** — (a) the "no figure in a row expression" regex forbade the printed `* 100` that EQ-02 / EQ-05 / EQ-06 themselves carry (pin narrowed to "everything except `* 100`"); (b) I pinned the brief's "23 `attest_*` booleans" and the capture holds **21** → the premise is recorded as refuted (`atv_a704e-O-2`), never rounded → 7/7. Equations test: 9/9 first run. Render test: 2/2 first run.

The staged gate conditions were parse-checked and evaluated BOTH ways in-session (`scripts/_probe_a704e_gates.ts`, deleted):
```
14/14 staged conditions parse
G-1 CR-026 IF-guard:  thermoblock + 2 °C pass · thermoblock + 5 °C fail · photometer (nothing entered) pass · unset pending (missing testing_equipment)
G-2 CR-019 IF-guard:  multi + 5 % vs 10 % pass · multi + 15 % vs 10 % fail · no multiple determination pass · unset pending
G-3 CR-025 IF-guard:  pipette 0,1 ml + 1,5 % pass · 0,1 ml + 2,5 % fail · 2,0 ml + 1,5 % fail · photometer (nothing entered) pass
G-4 / G-6 counts:     qa_measures_count 9 pass · 0 fail · mitarbeiter_count 0 fail · 2 pass
G-5 maxima:           max_dev 8 vs 10 pass · 12 vs 10 fail · CR-031 dilution 15 vs 20 pass · 25 vs 20 fail · CR-032 unset pending
```

## 3. Sources — every prod quote used (there is no transcript to verify against)

`verify-regulation-tables.ts` was NOT run: no tables, no builder, nothing to verify. Every cue in the modules, the STAGED file and the sheet is a cell of the read-only capture, copied by the generator `gen-a704e-quotes.mjs` into `src/lib/eval/field-configs/atv_a704e-quotes.ts` and referenced by name — never retyped. Grade **EV** throughout; the marker string `[prod verification_quote — EV, unverified: ATV-A-704E has NO transcript …]` is appended to every module `verification_quote` and pinned in the test.

### 3.1 The 29 `fields.verification_quote` cells the modules cite

| # | prod column | worksheet · symbol | verbatim cell text (EV — prod, unverified) |
|---|---|---|---|
| 1 | `fields.verification_quote` | `ATV-A-704E-03 application_mode` | "For the reasonable application of operating methods the following guidelines apply: When pertaining to water and wastewater fee regulations, operating methods can only replace reference methods if this is regulated by law. [...] The use of operating methods can clearly reduce the need for reference methods. So, for example, in self-monitoring - except for the parallel analysis - reference methods can be omitted. — printed p.11" |
| 2 | `fields.verification_quote` | `ATV-A-704E-09 calculated_value` | "IQC-Card 5, Sheet 1 - Dilution: 2. Single steps: a) Determination of the MEASURED VALUE ORIGINAL SAMPLE (column 3) b) Dilution c) Determination of MEASURED VALUE DILUTED SAMPLE (column 5) \| 3. Formulas for calculation: DILUTION FACTOR (column 4) = TOTAL VOLUME / SAMPLE VOLUME. CALCULATED VALUE (column 6) = DILUTION FACTOR · MEASURED VALUE DILUTED SAMPLE \| If the variation between CALCULATED VALUE and MEASURED VALUE ORIGINAL SAMPLE is smaller than given in the internal specification (IQC-Card 2), there is no error in the matrix. If it is greater, an error in the matrix is likely. — printed p.35" |
| 3 | `fields.verification_quote` | `ATV-A-704E-12 deviation_feature` | "Here, the date of the deviation is put down in column 1. The name of the person who detects the deviation (column 2), the type of distinctive features (column 3) and in which IQC-Card they are documented (column 4) are also noted. As soon as the cause is known, it is documented in column 5 with the measured taken (column 6). The result of the measures introduced is entered together with date (date 7), the result of the measures (column 8) and the name of the person who determined the result (column 9). — printed p.67" |
| 4 | `fields.verification_quote` | `ATV-A-704E-12 deviation_measure` | (the same IQC-Card 11 sentence as #3 — prod stores it on all four deviation fields) |
| 5 | `fields.verification_quote` | `ATV-A-704E-09 deviation_single_pct` | "The greatest difference in the single measured values is entered into the IQC-Card in mg/l (column 8) and in % (greatest difference of the single measured values divided by the mean value; column 9). In column 10 the result of the control measure is documented. \| IQC-Card 3 - Multiple Determinations (form header): 1 Date \| 2 Name \| 3 Sample designation \| 4 Measured value 1 [mg/l] \| 5 Measured value 2 [mg/l] \| 6 Measured value 3 [mg/l] \| 7 Mean [mg/l] \| 8 Greatest spread Difference [mg/l] \| 9 Greatest spread Col 8 x 100% / Col 7 [%] \| 10 Interpretation: Quality target achieved? — printed p.29 and 30" |
| 6 | `fields.verification_quote` | `ATV-A-704E-10 deviation_equivalency_pct` | "IQC-Card 6 - Equivalency Measurements (form header): 1 Date \| 2 Name \| 3 Sample designation \| 4 Parameter \| 5 Mean [mg/l] \| 6 Nominal value or mean of the equivalency measurements [mg/l] \| 7 Deviation of the mean from the nominal value [mg/l] \| 8 Deviation relative [%] \| 9 Interpretation: Quality target achieved? \| Usually, the deviation in the central measurement range should not exceed the quality target of < 20 % (in this regard also see IQC-Card 2: Internal Specifications). — printed p.45 and 43" |
| 7 | `fields.verification_quote` | `ATV-A-704E-10 deviation_parallel_pct` | "IQC-Card 7 - Parallel Measurements to the Reference Method (form header): 7 Referenced monitoring Measured value [mg/l] \| 8 Measured value of the operating method [mg/l] \| 9 Deviation absolute Col8-Col7 [mg/l] \| 10 Deviation relative Col9 x 100/Col7 [%] \| 11/12 Permissible deviation Condition [mg/l] [%] \| 13 Interpretation: Quality target achieved? \| Usually, the deviation in the central measuring range should not exceed the quality target of < 20 % (see also IQC-Card 2 in this regard: Internal Specifications). Especially with measuring results in the lower concentration range (e. g. Ptotal < 1 mg/l) greater percental tolerances are possible. — printed p.48 and 46" |
| 8 | `fields.verification_quote` | `ATV-A-704E-12 employee_qualification` | "Qualification: Predominantly, qualification is certified by a corresponding vocational training. A training as \"specialist on wastewater technology\" or a similar training qualification should be listed here. A qualification that was gained otherwise must be presented in a corresponding way. — printed p.62" |
| 9 | `fields.verification_quote` | `ATV-A-704E-03 expected_concentration_range` | "By referring to the criteria mentioned in Sections 3.1 and 3.2, the operator must decide for the operating method that is suitable for his specific case of application. In doing so, especially the measuring range must be selected in such way that preferably the expected result can be found in the 20 % to 80 % interval of the measuring range. From experience, this range shows the greatest analytical reliability. Suitable dilution steps are permitted. — printed p.11" |
| 10 | `fields.verification_quote` | `ATV-A-704E-12 instruction_training_record` | "Overview of instruction and training: This overview serves as a proof of training according to Section 4.3.3 of the Standard. Here the completed basic introduction as well as all internal and external successive training are recorded. Here, for example, also supportive visits by the manufacturer and DWA-courses must be registered. \| Sheet 2 - Overview of Instructions and Training: 1 Event: topic and organising party \| 2 Date \| 3 Signature supervisor — printed p.62 and 66" |
| 11 | `fields.verification_quote` | `ATV-A-704E-09 mean_value` | "From the single results a mean value must be produced, which represents the measuring result. Here, all single results must be included in the mean. \| From the single measured values the mean (add single measured values and divide them by the number of the measured values; column 7) is produced. \| IQC-Card 3 - Multiple Determinations (form header): … \| 7 Mean [mg/l] \| 8 Greatest spread Difference [mg/l] \| 9 Greatest spread Col 8 x 100% / Col 7 [%] \| 10 Interpretation: Quality target achieved? — printed p.29 and 30" |
| 12 | `fields.verification_quote` | `ATV-A-704E-10 measured_value_operating` | "IQC-Card 6 - Equivalency Measurements (form header): 1 Date \| 2 Name \| 3 Sample designation \| 4 Parameter \| 5 Mean [mg/l] \| 6 Nominal value or mean of the equivalency measurements [mg/l] \| 7 Deviation of the mean from the nominal value [mg/l] \| 8 Deviation relative [%] \| 9 Interpretation: Quality target achieved? — printed p.45" |
| 13 | `fields.verification_quote` | `ATV-A-704E-10 measured_value_reference` | "IQC-Card 7 - Parallel Measurements to the Reference Method (form header): 7 Referenced monitoring Measured value [mg/l] \| 8 Measured value of the operating method [mg/l] \| 9 Deviation absolute Col8-Col7 [mg/l] \| 10 Deviation relative Col9 x 100/Col7 [%] \| 11/12 Permissible deviation Condition [mg/l] [%] \| 13 Interpretation: Quality target achieved? — printed p.48" |
| 14 | `fields.verification_quote` | `ATV-A-704E-09 measured_value_spiked_sample` | "If the measured value MEASURED VALUE SPIKED SAMPLE deviates from the NOMINAL VALUE SPIKED SAMPLE by more than the tolerance given in the internal specification (IQC-Card 2), a matrix error is likely. — printed p.36" |
| 15 | `fields.verification_quote` | `ATV-A-704E-11 monitoring_interval` | "IQC-Card 9: Recommendations for control- and monitoring frequencies can be taken from the table. \| Testing equipment / Monitoring: BOD-respirometer biannually \| Photometer annually \| Annealing furnace annually \| Heating device/thermoblock annually \| Ion exchange annually \| Piston stroke pipettes quarterly \| Refrigerator annually \| Conductivity measuring device biannually \| pH-meter monthly \| Sampling equipment biannually \| Reagents quarterly \| Oxygen measuring device every two weeks \| Thermometer quarterly \| Thermo cabinet annually \| Drying cabinet annually \| Scales annually — printed p.54" |
| 16 | `fields.verification_quote` | `ATV-A-704E-09 NSS` | "IQC-Card 5, Sheet 3 - standard addition (general procedure): 2. Single steps: Determination of the MEASURED VALUE ORIGINAL SAMPLE (column 3), Standard addition by adding the VOLUME STANDARD (column 5) in familiar CONCENTRATION STANDARD (column 6) to the defined VOLUME SAMPLE (column 4), Determination of MEASURED VALUE SPIKED SAMPLE (column 9). \| 3. Calculation: NOMINAL VALUE SPIKED SAMPLE (NSS, column 8): NSS = [(volume sample · measured value original sample) + (volume standard · concentration standard)] / (volume sample + volume standard) — printed p.36" |
| 17 | `fields.verification_quote` | `ATV-A-704E-09 n_determinations` | "Usually it is sufficient to carry out a double determination of the sample to be analysed. A triple determination additionally increases the analytic reliability of the measuring results and should be carried out for greater variations in the measured values. — printed p.29" |
| 18 | `fields.verification_quote` | `ATV-A-704E-01 parameter_name` | "2.2 Abbreviated Terms - Abbreviation / Description: Sett. substances [Abs. Stoffe] Settable substances \| BOD5 [BSB5] Biochemical oxygen demand in five days \| COD [CSB] Chemical oxygen demand \| NH4-N Ammonium nitrogen \| NO3-N Nitrate nitrogen \| NO2-N Nitrite nitrogen \| Ptotal Total phosphorus \| TNb Total nitrogen bound \| TOC Total organic carbon — printed p.9" |
| 19 | `fields.verification_quote` | `ATV-A-704E-11 photometer_check_done` | "Monitoring of the photometer: Within the scope of testing equipment monitoring, the function of photometers must be tested regularly. The device must be checked annually according to the instructions manual. The test kit can be ordered as a test solution kit from the manufacturer of the photometer. The entire optical system of the photometers is checked. The monitoring must be documented on Sheet 1. — printed p.55" |
| 20 | `fields.verification_quote` | `ATV-A-704E-11 pipette_deviation_pct` | "The mean must be calculated from the 5 results of the weightings. [...] The mean value must lie within a defined tolerance range (see table). \| Tested volume [ml] / Deviation [%] / Tolerance range [g]: 0.100 / ≤ 2 / 0.098 - 0.102 \| 0.200 / ≤ 2 / 0.196 - 0.204 \| 0.500 / ≤ 2 / 0.490 - 0.510 \| 1.000 / ≤ 1 / 0.990 - 1.010 \| 2.000 / ≤ 1 / 1.980 - 2.020 \| 5.000 / ≤ 1 / 4.950 - 5.050 \| If the results lie outside the tolerance range, the pipette must no longer be used as testing equipment (remove designation) and should be sent to the manufacturer for testing, if necessary. — printed p.55" |
| 21 | `fields.verification_quote` | `ATV-A-704E-06 precipitation_influence` | "IQC-Card 8 - Sampling, Sampling Log: Pre-treatment: none [ ] cooled [ ] added: ______ \| Influence of precipitation: yes [ ] no [ ] — printed p.51" |
| 22 | `fields.verification_quote` | `ATV-A-704E-08 qa_minimum_frequency` | "IQC-Card 2 Sheet 1 - Recommendations by the DWA-Working Group IG-4.3, col.3 Minimum frequency: Multiple determinations 1x per month, person- and matrix-related \| Measurement of standards after each 10th sample; at least 1x per month, person-related \| Plausibility checks (spiking, dilution) if measuring results are implausible; if matrix changes; 1x per quarter \| Equivalency measurements and/or interlaboratory test (operating methods) 1x per year, person-related \| Parallel measurements to the reference method 1x per year, method-related \| Pipettes 100-1000 µl volume check 4x per year \| pH-meter check 1x per month \| Heating device/thermoblock check 1x per year — printed p.23" |
| 23 | `fields.verification_quote` | `ATV-A-704E-08 qa_measure` | "IQC-Card 2 Sheet 1 - Recommendations by the DWA-Working Group IG-4.3, col.1 Quality assurance measure: Multiple determinations \| Measurement of standards \| Plausibility checks (spiking, dilution) \| Equivalency measurements and/or interlaboratory test (operating methods) \| Parallel measurements to the reference method \| Pipettes 100-1000 µl volume check \| Pipettes > 1000 µl volume check \| pH-meter check \| Heating device/thermoblock check \| Measuring device check (e.g. photometer) — printed p.23" |
| 24 | `fields.verification_quote` | `ATV-A-704E-08 qa_quality_target_pct` | "IQC-Card 2 Sheet 1 - Recommendations by the DWA-Working Group IG-4.3, col.4 Quality target: Multiple determinations Random error < 10 % * \| Measurement of standards Adherance to the permissible value range (control limits) \| Plausibility checks (spiking, dilution) deviation < 20 % * \| Equivalency measurements Deviation < 20 % * \| Parallel measurements to the reference method Deviation < 20 % * \| Pipettes 100-1000 µl Deviation < 2 % \| Pipettes > 1000 µl Deviation < 1 % \| pH-meter Deviation < 0.2 pH \| Heating device/thermoblock Deviation < 3 °C \| * For measuring results in the lower concentration range (e. g. Ptotal < 1 mg/l) greater percental tolerances are possible. In this case their permissible deviations should be defined in mg/l. — printed p.23" |
| 25 | `fields.verification_quote` | `ATV-A-704E-09 sample_volume` | "IQC-Card 5, Sheet 1 - Dilution: … 3. Formulas for calculation: DILUTION FACTOR (column 4) = TOTAL VOLUME / SAMPLE VOLUME. CALCULATED VALUE (column 6) = DILUTION FACTOR · MEASURED VALUE DILUTED SAMPLE — printed p.35" |
| 26 | `fields.verification_quote` | `ATV-A-704E-06 sampling_method` | "IQC-Card 8 - Sampling, Sampling Log, Method of sampling: manual sampling [ ] - automatic sampling [ ] time proportional [ ] volume proportional [ ] flow proportional [ ] — printed p.51" |
| 27 | `fields.verification_quote` | `ATV-A-704E-09 single_result_i` | "IQC-Card 3 - Multiple Determinations (form header): 1 Date \| 2 Name \| 3 Sample designation \| 4 Measured value 1 [mg/l] \| 5 Measured value 2 [mg/l] \| 6 Measured value 3 [mg/l] \| 7 Mean [mg/l] \| 8 Greatest spread Difference [mg/l] \| 9 Greatest spread Col 8 x 100% / Col 7 [%] \| 10 Interpretation: Quality target achieved? — printed p.30" |
| 28 | `fields.verification_quote` | `ATV-A-704E-06 storage_temperature` | "Storage of samples: What is the optimum storage temperature to eliminate changes of the sample in the best possible way? The wastewater sample should be stored at temperatures around 4 °C for a short time. In this way, for instance, the bacterial conversion of ammonium to nitrate can be suppressed. — printed p.49" |
| 29 | `fields.verification_quote` | `ATV-A-704E-11 testing_equipment` | "IQC-Card 9: Recommendations for control- and monitoring frequencies can be taken from the table. \| Testing equipment / Monitoring: BOD-respirometer biannually \| Photometer annually \| … \| Scales annually — printed p.54" |

(The full, unabridged strings are in `src/lib/eval/field-configs/atv_a704e-quotes.ts` — generated from the capture, never retyped; the ellipses above are in this REPORT only, for width.)

### 3.2 The six `equations` rows this task re-expresses (`verification_quote` + `source_quote`, EV)

| equation | stored formula | `source_quote` (EV) |
|---|---|---|
| `ATV-A-704E-09 EQ-01` | `mean_value = SUM(single_result_i) / n_determinations` | "DWA-A 704E, IQC-Card 3 – Multiple Determinations, Documentation: \"From the single measured values the mean (add single measured values and divide them by the number of the measured values; column 7) is produced.\" (p. 29)." |
| `ATV-A-704E-09 EQ-02` | `deviation_single_pct = 100 * (single_result_i - mean_value) / mean_value` | "DWA-A 704E, IQC-Card 3 – Multiple Determinations, Documentation: \"The greatest difference in the single measured values is entered into the IQC-Card in mg/l (column 8) and in % (greatest difference of the single measured values divided by the mean value; column 9).\" (p. 29). **[RULING: printed col.9 metric is the greatest difference of the single values ÷ mean value (a max-spread), which diverges semantically from the encoded formula 100*(single_result_i − mean_value)/mean_value — evidence only, formula not changed.]**" |
| `ATV-A-704E-09 EQ-03` | `calculated_value = (total_volume / sample_volume) * measured_value_diluted_sample` | "DWA-A 704E, IQC-Card 5 …, Sheet 1 – Dilution, \"3. Formulas for calculation: DILUTION FACTOR (column 4) = TOTAL VOLUME / SAMPLE VOLUME. CALCULATED VALUE (column 6) = DILUTION FACTOR · MEASURED VALUE DILUTED SAMPLE\" (p. 35)." |
| `ATV-A-704E-09 EQ-04` | `NSS = (volume_sample * measured_value_original_sample + volume_standard * concentration_standard) / (volume_sample + volume_standard)` | "DWA-A 704E, IQC-Card 5, Sheet 3 – standard addition (general procedure), \"3. Calculation: NOMINAL VALUE SPIKED SAMPLE (NSS, column 8): NSS = [(volume sample · measured value original sample) + (volume standard · concentration standard)] / (volume sample + volume standard)\" (p. 36)." |
| `ATV-A-704E-10 EQ-05` | `deviation_equivalency_pct = 100 * (measured_value_operating - nominal_value_reference) / nominal_value_reference` | "DWA-A 704E, IQC-Card 6 – Equivalency Measurements … \"deviation of the calculated mean value from the nominal or mean value in mg/l (column 7) and in per cent (column 8).\" (p. 43)." |
| `ATV-A-704E-10 EQ-06` | `deviation_parallel_pct = 100 * (measured_value_operating - measured_value_reference) / measured_value_reference` | "DWA-A 704E, IQC-Card 7 – Parallel Analyses to the Reference Method, Documentation: \"… The difference between both measured values must be formed and must be entered into the IQC-Card in absolute (column 9) as well as percental value (column 10) with reference to the result of the external laboratory.\" (p. 46)." |

All six are `verified_against_standard` in prod and are **untouched** by this task's migrations. EQ-02's own `source_quote` carries prod's RULING note about the col.9 divergence — that note is the evidence of `atv_a704e-J-1`.

### 3.3 The `compliance_requirements` cells the STAGED blocks quote

All 30 gate rows (id, condition, md5, severity, `requires_attestation`, description, `source_quote`) are printed verbatim in the STAGED file's header and in the blocks that touch them; the 13 the blocks cite are CR-001, CR-016, CR-018, CR-019, CR-020, CR-021, CR-024, CR-025, CR-026, CR-027, CR-028, CR-029, CR-030.

### 3.4 Absence / presence claims (amendment O — commands re-executable, raw output)

- **The PDF has no text layer** — §0 above (exit 0, 37 bytes, `NONWS=0`, `Pages: 37`).
- **`attest_*` count (O-2, brief premise refuted):** `grep -oE '"ATV-A-704E-[0-9]{2} attest_[a-z0-9_]+"' src/lib/eval/field-configs/atv_a704e.prior.json | wc -l` → `21` (exit 0); the per-worksheet split from the same grep piped through `sed`/`sort`/`uniq -c` → `8 01 · 4 03 · 2 05 · 3 07 · 4 08`. The brief and the inventory both say 23. Pinned in the test.
- **CR-020 / CR-021 are NOT the dilution / spiking gates (O-1, brief premise refuted):** from the capture, both live on `ATV-A-704E-08`, are `block`, `requires_attestation true`, and their conditions are `attest_atv_a_704e_08_cr_020 == True` / `attest_atv_a_704e_08_cr_021 == True` (pinned in the test with a regex). The deviation gates of this standard are CR-019 (-09), CR-022 / CR-023 (-10) — pinned with their exact conditions. No gate of this standard checks a dilution or a spiking deviation; `atv_a704e-G-5` PROPOSES them as the new CR-031 / CR-032 (the highest existing code is CR-030).
- **Three gates carry an EMPTY condition:** `node -e "…gates… filter(parse_error)"` via the prior → `["ATV-A-704E-05 CR-029","ATV-A-704E-07 CR-028","ATV-A-704E-08 CR-030"]`, and `PROD_GATE[code].condition === ''` for all three (pinned). Empty conditions never refuse a rule (amendment M) — recorded as `atv_a704e-G-7` / `G-8`.
- **Two dead `consumer_worksheets` tokens:** the only non-code consumer entries in the whole capture are `ATV-A-704E-01 aqa_applied` and `ATV-A-704E-01 parameter_name`, both `["ALL"]` — a token `loadInheritedFields` (`code = ANY(...)`) never matches (pinned; `atv_a704e-C-4`). No rule of this task keys on either (pinned).
- **No self-consumers:** no captured field lists its own worksheet in `consumer_worksheets` (pinned) — the Task-12b class does not apply here.
- **Nothing seeded:** `SEED_BUILDERS` has no `atv_a704e` key (pinned); neither migration contains the string `regulation_table` (pinned).
- **The created symbols do not exist in prod:** all 21 created `(worksheet, symbol)` keys are absent from the prior (pinned, one assertion per symbol).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,80) AS title FROM standards WHERE code ILIKE '%704%'"
  → ATV-A-704E · 'April 2007' · issued_year null · 'Betriebsmethoden für die Abwasseranalytik (DWA-A 704E / ATV-A 704E)'
node scripts/regulation-tables/build-prior-snapshot.mjs ATV-A-704E atv_a704e
  → wrote src\lib\eval\field-configs\atv_a704e.prior.json: 91 field rows (21 orphan), 101 coded sections of 101, 6 equations,
    30 gates (3 unparseable — symbols: [] + parse_error; the emitter refuses conservatively on those worksheets);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
node scripts/verification/capture-text.mjs ATV-A-704E atv_a704e
  → wrote src\lib\eval\field-configs\atv_a704e.text.prior.json: worksheets 12, sections 101, fields 91, requirements 30,
    equations 6, stored parameters [{"n":0}], instances [{"n":12}]
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts atv_a704e 20260917102710
  → wrote 24 field entries + 0 section entries for atv_a704e -> scripts/migrations/20260917102710_field_configs_atv_a704e.sql scripts/rollback-20260917102710-field-configs-atv-a704e.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts atv_a704e 20260917102720
  → wrote 12 equations for atv_a704e -> scripts/migrations/20260917102720_equations_atv_a704e.sql scripts/rollback-20260917102720-equations-atv-a704e.sql
```

Both migrations are byte-pinned against a fresh emit (`field-configs-atv_a704e.test.ts` / `equations-atv_a704e.test.ts` against the committed `atv_a704e.prior.json`, default refuse mode, `warnings` pinned `[]`). The equations rollback deletes the 12 `Plan 3:` rows; the field-configs rollback deletes the 21 `Plan 3:` rows and restores the three prior NULLs. **Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102710` → `20260917102720`; rollback in reverse. There is no seed migration.** Every file touches `ATV-A-704E` rows only.

**Live `information_schema` column lists, re-read READ-ONLY in this session (2026-09-24)** — they are the basis of the R-1 archive rollback: `equations` **22** (`id, worksheet_template_id, equation_number, formula, formula_latex, input_symbols, output_symbol, output_unit, clause_reference, description, verification_status, audit_status, source_file, source_anchor, source_quote, audit_notes, audited_at, audited_by, verified_by_user_id, verified_at, verification_note, verification_quote`); `compliance_requirements` **18**; `fields` **31** (still WITHOUT `widget / ui_config / lookup / visible_when` — the Plan-1 schema migration is unapplied); `worksheet_sections` **7**. A read-only per-column NULL check on the six ATV-A-704E equations shows `verified_at` and `verification_note` **non-null on all six** (`formula_latex`, `source_file`, `source_anchor`, `audit_status`, `audit_notes`, `audited_at`, `audited_by`, `verified_by_user_id` all null) — so the R-1 rollback lists **all 22** columns; a shorter list would silently drop the provenance of six `verified_against_standard` rows on a ratify-then-rollback. The STAGED header prints all four lists with the re-read date.

**Materialisation (amendment D, noted once — `atv_a704e-I-1`):** all 12 equations take exactly ONE input, the register of their own worksheet, so every row materialises on save; no scalar-only row exists in this task. The emitted `visible_when` rules hide on the form / report / PDF but are `pending` (visible) for the server-side `computeVisibility`, which reads templateFields only (the [CODE] item recorded once as `vsme-X-3`) — fail-safe.

## 5. What was encoded

**Tables (0)** — none. `atv_a704e-U-1 … U-6` name the six the standard prints (IQC-Card 2 Sheet 1 frequencies + quality targets; IQC-Card 9 monitoring intervals; IQC-Card 9 Sheet 3 pipette volumes / tolerances; §2.2 reference methods per parameter; IQC-Card 8 preservation by sample type × parameter; the §4.2 20–80 % window), each with its proposed `table_code`, key / value columns, policy question and the `lookup_fill` bindings it would unlock — plus the failing-extraction evidence and the OCR / manual-transcription unblock path.

**Registers (9, all `create`)** — `src/lib/eval/field-configs/atv_a704e.ts`; every enum column copies prod's `value` + `label_de` byte-for-byte (D-1, pinned against the prior):

| register | worksheet · section | columns | footer |
|---|---|---|---|
| `betriebsmethoden` | -01 D | `parameter` (enum 9, required, discriminator), `application_mode` (enum 3), `expected_range`, `validation_coverage_pct` (%), `suitable` (bool) | — |
| `qs_massnahmen` | -08 E | `measure` (enum 9, required, discriminator), `frequency`, `target_pct` (%), `performed_count` | `qa_measures_count` |
| `einzelbestimmungen` | -09 C (← `multiple_determination_performed`) | `parameter` (required, discriminator), `date` (required), `single_result` (required), `unit` | `n_determinations_calc`, `mean_value_calc`, `max_dev_pct_calc` |
| `verduennungsversuche` | -09 D | `parameter`, `date`, `sample_volume_ml`*, `total_volume_ml`*, `measured_diluted`*, **`calculated` = EQ-03 per row**, `reference`*, **`dev_pct`** | `dilution_max_dev` |
| `aufstockungsversuche` | -09 E | `parameter`, `date`, `sample_volume_ml`*, `standard_volume_ml`*, `standard_concentration`*, `measured_original`*, `measured_spiked`*, **`nss_nominal` = EQ-04 per row**, **`dev_pct`** | `spike_max_dev` |
| `vergleichsmessungen` | -10 C (← equivalency OR parallel) | `kind` (equivalency/parallel, required, discriminator), `parameter`, `date`, `operating_value`*, `reference_value`*, **`dev_pct` = EQ-05 / EQ-06 per row** | `equivalency_max_dev`, `parallel_max_dev` |
| `pruefmittel` | -11 C | `equipment` (enum 16, required, discriminator), `interval` (enum 5), `last_check`, `result`, `pipette_volume_ml` / `pipette_dev_pct` (← pipette rows), `heating_dev_c` (← thermoblock rows), `photometer_check` (← photometer rows) | `pruefmittel_count`, `pipette_dev_max` |
| `mitarbeiter` | -12 C | `name`*, `qualification`, `instruction_date`, `training_record` | `mitarbeiter_count` |
| `abweichungen` | -12 D | `date`, `feature`*, `iqc_card_ref` (enum 11), `cause`, `measure`, `result` | `abweichungen_count` |

(* = required; **bold** = `derived` row column.) No register column is keyed `id` and none shadows a prod symbol of its worksheet (pinned — hence `sample_volume_ml` / `total_volume_ml` rather than the brief's `sample_volume` / `total_volume`, which ARE prod symbols of -09). No `lookup` binding and no `override` block anywhere: nothing is seeded.

**Field `visible_when` (8 + 4 row-scope):** 1 on an EXISTING field — `photometer_check_done` (-11) ← `testing_equipment == 'photometer'` (consumer-free and read by no gate — asserted through `gateReaders` / `producerChain`). The brief's two -06 rules (`precipitation_influence` / `storage_temperature` ← `sampling_method == 'automatic'`) are equally accepted by the emitter but WITHHELD (`atv_a704e-J-3`, fix round 1): no prod cell makes either field conditional on automatic sampling. 7 on created fields (`einzelbestimmungen` + its three outputs ← `multiple_determination_performed == true`; `vergleichsmessungen` + its two outputs ← `equivalency_check_performed == true OR parallel_analysis_performed == true`); 4 row-scope rules inside `pruefmittel`. **Refused / withheld and STAGED (pinned through the guards):** `heating_device_deviation` ← CR-026 (G-1); `pipette_tested_volume` / `pipette_deviation_pct` ← CR-025 (G-3); `parallel_analysis_performed` ← the producer chain (C-1); the five §4.4 section rules (G-2 / C-2). Section rules emitted: none.

**Equations (12 new)** — `src/lib/eval/equations/atv_a704e.ts`, every `verification_quote` **null** (controller resolution 1):

`ATV-A-704E-08-D1 qa_measures_count = count_rows(qs_massnahmen)` · `-09-D1 n_determinations_calc = count_rows(einzelbestimmungen)` · `-09-D2 mean_value_calc = mean_rows(einzelbestimmungen, single_result)` (EQ-01 over rows) · `-09-D3 max_dev_pct_calc = max(max_rows(…) − mean_rows(…), mean_rows(…) − min_rows(…)) * 100 / mean_rows(…)` (EQ-02 maximised, mean inlined — never chained on D2) · `-09-D4 dilution_max_dev = max_rows(verduennungsversuche, abs(dev_pct))` · `-09-D5 spike_max_dev = max_rows(aufstockungsversuche, abs(dev_pct))` · `-10-D1 equivalency_max_dev = max_rows(vergleichsmessungen, abs(dev_pct), kind == 'equivalency')` · `-10-D2 parallel_max_dev` (`'parallel'`) · `-11-D1 pruefmittel_count = count_rows(pruefmittel)` · `-11-D2 pipette_dev_max = max_rows(pruefmittel, pipette_dev_pct, equipment == 'piston_stroke_pipettes')` · `-12-D1 abweichungen_count = count_rows(abweichungen)` · `-12-D2 mitarbeiter_count = count_rows(mitarbeiter)`.

No row outputs a prod symbol (EQ-01 … EQ-06 keep theirs — pinned); the only figure in any formula is the printed `× 100` that EQ-02 / EQ-05 / EQ-06 themselves carry (pinned). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows`, hand-checked — the standard prints no worked example this session may read): determinations 100 / 110 / 90 (+1 incomplete) ⇒ 3 / 100 / 10 %, and 100 / 130 / 95 ⇒ mean 108,333… / 20,0 % (engine 20.000000000000004, float — pinned with `toBeCloseTo`); empty or all-incomplete ⇒ count 0, mean and max deviation `manual_required`; dilution 10 → 100 ml with 12 ⇒ 120 and +20 %, with 9 ⇒ 90 and −10 %, max 20, empty manual; standard addition 90 ml at 20 + 10 ml at 200 ⇒ NSS 38, measured 57 ⇒ +50 %, 19 ⇒ −50 %, max 50, empty manual; comparisons 110/100 equivalency +10 %, 75/100 and 130/100 parallel −25 % / +30 % ⇒ 10 and 30, no parallel row ⇒ manual (and the brief's `if(…, 0)` form ⇒ a false 0, pinned side by side); Prüfmittel 0,8 % / 1,5 % + a photometer row ⇒ count 3 and pipette max 1,5, no pipette row ⇒ count 1 and max manual; the three counting rows ⇒ 2 / 1 / 1 with one incomplete row each, 0 on empty.

**STAGED (`scripts/verification/atv_a704e-STAGED-plan3-rulings.sql`, 1 490 lines, 0 non-comment lines, 71 ☐ blocks; generated from the captures)** — G-1 CR-026 IF-guard + the thermoblock hide; G-2 CR-019 / CR-022 / CR-023 IF-guards on the §4.4 booleans + the three section hides; G-3 CR-025 IF-guard + the two pipette hides; G-4 the IQC-Card 2 / IQC-Card 9 attestation gates (CR-020 / CR-021 / CR-024 / CR-027) → register counts; G-5 the three deviation gates re-pointed onto the register maxima + two NEW warn gates CR-031 / CR-032 for the dilution and spiking trials (guarded on G-2's POST-state md5, computed with node:crypto); G-6 CR-018 → `mitarbeiter_count`; G-7 CR-028's EMPTY condition (with the explicit refusal to propose a bare `count >= 1` on a LOG register); G-8 CR-029 / CR-030's EMPTY conditions as content-boundary pointers; C-1 the `parallel_analysis_performed` producer refusal; C-2 `blank_and_standard_controlled` → -09 + the two withheld section hides; C-3 the three register counts → the gates' worksheets; C-4 the two dead `["ALL"]` tokens; D-1 … D-42 the amendment-K pairs (each states data_type / is_required / gate readers / consumers, all data-free: 0 stored `project_parameters`, 12 worksheet instances); R-1 the six prod equations onto the rows (archive + delete, **all 22 `equations` columns** in the rollback); F-1 the impossible per-row deviation column; J-1 the col-9 spread-vs-deviation divergence; J-2 the withheld second denominator column; J-3 the two withheld -06 rules; U-1 … U-6 the six unseeded tables; O-1 / O-2 the two refuted brief premises; X-1 the 21 attestations that stay; X-2 `sample_volume` vs `volume_sample`; X-3 the cross-standard sampling twins; I-1 materialisation; I-2 the unguarded emitted UPDATE (the shared emitter is NOT changed — amendment G; same item as `iso14046-I-2`). Every gate block archives the row into `compliance_requirements_archive_atv_a704e`, guards on `md5(condition)` (ids / md5 from the capture) and restores with the explicit column set; the one equation block uses the equations archive.

## 6. Inventory §5 top-wins walk

1. **IGC-Card 2 as a lookup: pick `qa_measure` → frequency and % target filled; a repeatable list so all nine measures exist per plant.** → **partially.** The repeatable list is encoded (`qs_massnahmen`, all nine prod tokens, count equation, footer). The auto-fill is NOT: the printed table cannot be seeded without a transcript, so frequency and target are engineer inputs and the table is `atv_a704e-U-1` with its full proposed schema. ✓ (register) / U-1 (values)
2. **IGC-Card 9 as repeatable Prüfmittel rows with interval and tolerance auto-filled, pipette ranges by volume.** → **partially.** `pruefmittel` is encoded with the per-type tolerance columns (row-scope `visible_when` on the discriminator), the count and the pipette maximum. The auto-fill needs `atv_a704e-U-2` (intervals) and `U-3` (volume / tolerance table); the volume-dependent 2 % / 1 % limit stays inside CR-025 until then (G-3). ✓ (register) / U-2 · U-3 (values)
3. **Multiple determinations as rows → mean, n and max deviation computed; CR-019 evaluated on the worst row.** → **encoded**, with one honest gap: the register + `n_determinations_calc` / `mean_value_calc` / `max_dev_pct_calc` compute today; CR-019 still reads the single typed `deviation_single_pct` because re-pointing a gate is an enforcement change (`atv_a704e-G-5`, evaluated both ways). The per-ROW deviation column is impossible (`atv_a704e-F-1`). ✓ (computation) / G-5 (enforcement)
4. **Dilution / standard-addition / equivalency / parallel as dated rows per parameter with the four IGC formulas computed and compared with the target inherited from WS-08.** → **encoded**: three registers, all four stored formulas as row expressions, four maxima. The comparison with `qa_quality_target_pct` is the STAGED G-5 (+ the two NEW gates CR-031 / CR-032 for dilution and spiking, which prod does not have at all — `atv_a704e-O-1`). ✓ (rows + formulas) / G-5 (comparison)
5. **Replace the `attest_*` booleans of §1–§5 with concrete evidence rows.** → **partially / Phase 6.** The evidence rows exist now (`mitarbeiter`, `abweichungen`, `qs_massnahmen`, `pruefmittel`, plus the four measurement registers); the **21** booleans (not 23 — `atv_a704e-O-2`) all stay, because they carry the entire normative text of §1–§5 through block gates. The swap is `atv_a704e-G-4` / `G-6`, deactivation is `atv_a704e-X-1` (Phase 6). ✓ (rows) / X-1 (deactivation)

Data-quality gaps of the inventory: no machine-readable transcript → every cue EV, `U-1 … U-6`; all six equation outputs are editable inputs → `R-1`; `sample_volume` vs `volume_sample` → `X-2` (kept apart deliberately); `qa_minimum_frequency` text → `D-23` + `U-1`; `successive_supervision_interval` / `max_storage_time` text where enums are plausible → observation (no printed list is quotable); `iqc_card_module` / `deviation_iqc_card_ref` share one enum in two roles → observation; unit `"-"` on many numeric fields → observation.

## 7. Files changed

Modified: `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (atv_a704e lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+71 blocks, +4 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 27)" paragraph).
Created: `src/lib/eval/field-configs/atv_a704e.prior.json` + `atv_a704e.text.prior.json` (captured, read-only), `src/lib/eval/field-configs/atv_a704e-quotes.ts` (generated from the capture), `src/lib/eval/field-configs/atv_a704e.ts`, `src/lib/eval/equations/atv_a704e.ts`, `src/lib/eval/__tests__/field-configs-atv_a704e.test.ts`, `src/lib/eval/__tests__/equations-atv_a704e.test.ts`, `src/components/worksheet/__tests__/register-a704e-bestimmungen.test.tsx`, `scripts/verification/atv_a704e-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917102710_field_configs_atv_a704e.sql` + `scripts/rollback-20260917102710-field-configs-atv-a704e.sql`, `scripts/migrations/20260917102720_equations_atv_a704e.sql` + `scripts/rollback-20260917102720-equations-atv-a704e.sql`, this report (+ the copy at `.superpowers/sdd/…/task-27-report.md`).
Untouched: every other standard's file; the ATV-A-704E harness files (`a704e-verify.integration.test.ts`, `seed-a704e.ts`, `_harness-env-a704e.ts`); the shared emitters; `scripts/reasoning-map/` (checked out before staging); the two scratch probes deleted from `scripts/` — copies with the two generators in the scratchpad.

## 8. Discrepancies vs the brief (capture / codebase / reality won)

1. **Registers 9, not the estimate's "7"** — the brief's own list names nine; all nine are encoded.
2. **Equations 12, not "6 new"** — the brief's Step-5 table groups several outputs under one `equation_number`, but `(worksheet, equation_number)` is unique, so each output needs its own row: -09 D1 … D5 and -10 D1 / D2 replace the table's "-09-D1 / -09-D2 / -10-D1".
3. **The two per-kind maxima and the pipette maximum use the FILTERED aggregate, not the brief's `if(…, …, 0)`** — the `if` form reads a phantom `0` when no row of that kind exists (probed both ways, both pinned). Same for `pipette_dev_max`.
4. **Register column keys renamed to avoid shadowing prod symbols** (amendment P / iso46001 trap 4): `sample_volume` → `sample_volume_ml`, `total_volume` → `total_volume_ml`, `volume_sample` → `sample_volume_ml` (spiking register), `volume_standard` → `standard_volume_ml`, `concentration_standard` → `standard_concentration`, `NSS` → `nss_nominal`, `measured_value_*` → `measured_*`. All are prod symbols of -09 / -10.
5. **`verduennungsversuche.reference` is REQUIRED** (the brief leaves it optional): an optional reference leaves `dev_pct` null on a COMPLETE row and turns `dilution_max_dev` `manual_required` on the first such row (m205 trap 3).
6. **`vergleichsmessungen.nominal_value` NOT created** (`atv_a704e-J-2`): the deviation reads `reference_value`, so a second denominator column would be a number the engineer types and the formula ignores. One required column serves both kinds — EQ-05 and EQ-06 have the identical shape.
7. **Section rules: 0 emitted, 5 staged.** The brief's -09/-10 rules are refused by the gate-aware guard (G-2); the -09 D / E ones are accepted by the emitter but withheld because their driver is not inherited (C-2, the vsme / iso14046 convention).
8. **`heating_device_deviation` and both pipette fields are NOT hidden** (the brief's Step-4 targets): CR-026 / CR-025 read them → G-1 / G-3, exactly as the brief anticipates for CR-026.
9. **`atv_a704e-G-2` covers CR-019 / CR-022 / CR-023 only.** The brief's "CR-019…023" implies CR-020 / CR-021 are deviation gates; the capture shows they are IQC-Card 2 attestation gates on -08 (`atv_a704e-O-1`). The dilution / spiking gates the brief expects do not exist — G-5 proposes them.
10. **21, not 23, `attest_*` booleans** (`atv_a704e-O-2`).
11. **Sign-off ids 72 (brief ≈ 10)** — the 42 amendment-K pairs and the 8 gate blocks dominate; the brief's `U-1 … U-6`, `F-1`, `R-1`, `G-1`, `G-2`, `X-1`, `X-2` ids are kept with their intended meanings.
12. **`einzelbestimmungen` / `vergleichsmessungen` carry a `visible_when`** the brief does not ask for (on the CREATED carriers and their outputs): the §4.4 drivers ARE inherited there, so this is the emittable half of the §4.4 win while the section rules wait for G-2.

## 9. Self-review

- Every cue in the modules, the STAGED file and the sheet is a capture cell copied by a generator (`gen-a704e-quotes.mjs` → `atv_a704e-quotes.ts`; `gen-a704e-staged.mjs` → the STAGED file + the sheet section); every enum column list equals the prior's list byte-for-byte (pinned); every gate id / md5 / condition and every equation id / md5 in the STAGED file is read from the capture at generation time; the two post-state md5 values G-5 guards on are recomputed with `node:crypto`, never typed. The 0 stored `project_parameters` / 12 worksheet instances come from the in-session read-only capture.
- The STAGED file is 100 % comments (`grep -vc "^--\|^$"` → `0`); `grep -c "☐ RATIFIED"` → `72` (71 blocks + the header line); `grep -c "^### atv_a704e-"` on the sheet → `71`.
- The field-config test pins: 24 entries by kind; the ten rules with their exact drivers and that each driver resolves where the rule lives; `keep_prod` on the one enum UPDATE; token equality for every register enum column and every rule literal; the four row-scope rules; no column keyed `id` and none shadowing a prod symbol of its worksheet; every create in a captured section with the right data_type; footers = created outputs; the refusal of every withheld target WITH its gate code / producer chain, and the ACCEPTANCE of the two withheld section rules; the IF-guard exemption; the 30 gates / 3 empty conditions / 6 equations / 21 attestations / two "ALL" tokens / no self-consumers; the O-1 and O-2 refutations; migration freshness with exactly 21 INSERTs, 3 UPDATEs, 0 section UPDATEs, no `enum_values` UPDATE and no `regulation_table` string.
- Runtime facts were probed through the real register / formula / condition / emitter contracts BEFORE the pins (probes deleted, copies in the scratchpad); the render test drives `einzelbestimmungen` through `RegisterEditor` (two rows → mean 105 and 4,762 %, a third row → 100 and 10 %, a fourth incomplete row → 3/4 and no change to the aggregates).
- No `String.replace` with a replacement string on any repo file: the two registry lines and the playbook paragraph are marker-checked `split/join` splices that throw on a missing / duplicate marker; the sheet section is spliced on the `## Plan 3 tooling rulings` marker and is idempotent (it removes an existing Task-27 section first); the modules, tests and generators were written with the Write tool; the two small pin corrections used the Edit tool.
- `git checkout -- scripts/reasoning-map/` run before staging; `.env.local` never read or printed; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the two registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph — `git diff --numstat` → playbook 3/0, sheet 602/0, registries 3/1 each.

## 10. Concerns (for the controller)

1. **The headline UX wins are half-delivered by construction.** Every ROW shape and every FORMULA is live; every printed VALUE the wins depend on (frequencies, quality targets, monitoring intervals, pipette tolerances, reference methods, preservation, the 20–80 % window) is a sign-off item, because a scanned PDF cannot be quoted. `atv_a704e-U-1 … U-6` are therefore the single highest-value ratification batch of this task: one OCR pass unlocks six tables, at least four `lookup_fill` bindings and the CR-025 limit rewrite.
2. **`max_dev_pct_calc` may be the wrong metric** (`atv_a704e-J-1`). It re-expresses the STORED EQ-02 (controller resolution 1), but prod's own `source_quote` on EQ-02 records that the PRINTED IQC-Card 3 column 9 is a greatest SPREAD ÷ mean. The two agree on symmetric data and diverge otherwise (100 / 130 / 95: 20,0 % vs 32,3 %). Only the PDF page settles it; nothing was silently corrected.
3. **Two prod enforcement gaps found by reading the capture, both STAGED, neither fixed:** (a) CR-025 and CR-026 apply to EVERY project regardless of which instrument is monitored — a plant that monitors only a photometer cannot pass -11 without inventing a pipette volume, a pipette deviation and a thermoblock temperature (established by driving the gate evaluator on the captured conditions; whether a worksheet-level not-applicable escape exists was NOT tested — the claim is about the gate); (b) three gates (CR-028 / CR-029 / CR-030) carry an EMPTY condition and enforce nothing at all.
4. **42 amendment-K pairs** — 25 recommend DEFER (gate-read or equation-bound scalars), the rest retire on ratification; all data-free (0 stored values today).
5. **G-5's md5 guards depend on G-2 having been applied.** The file states the order in the header, in G-2's Note and in G-5's text; applying G-5 first is a no-op (the guards match nothing), not a corruption.
6. **Token/time:** ≈ 0.51 M tokens in, ≈ 45 min — in the Task 24 / 26 band; the cost sits in the 42 D-pairs and the 8 gate blocks (generated), not in reading a transcript (there is none).

## 11. Residue (what the standard demands that the worksheets still do not ask, as far as prod text shows)

`control_measure_value` (-09) is the IQC-Card 3 column-10 INTERPRETATION and has no register column (`atv_a704e-D-5`); the IQC-Card 1 Survey Card is represented only by `survey_measure_count` (no per-period survey register); IQC-Card 4 "Measurement of Standards" has a `qa_measure` token but NO worksheet, fields or register of its own — the standard's own control-chart sheet is not encoded anywhere (the largest structural gap, outside this brief); the IQC-Card 11 columns 2 and 9 (who detected the deviation, who determined the result) are not register columns; the §4.3.2 "successive supervision at regular intervals" interval is a free text; §5.1's "all boundary conditions must be documented" (time, place, type and duration of storage, pre-treatment, method) is covered only by the -06 scalars, not per sample; the §3.2 manufacturer duties are three booleans with no evidence rows; the -06 sampling worksheet gained NO conditional at all (fix round 1: `precipitation_influence` and `storage_temperature` stay unconditional because no prod cell ties them to automatic sampling — `atv_a704e-J-3`; the IQC-Card 8 OCR that unblocks `atv_a704e-U-5` settles it); the DIN EN ISO 5667 and DIN 38xxx families are pointers (content-boundary rule, `atv_a704e-G-8` / `U-4`). Each stays outside this encoding or on the sign-off sheet.

---

# Fix round 1 (review verdict on `6588dec`: "Approved with minors" + one Important — 631/631 quotes verbatim, the extraction evidence reproduced, both refutations genuine, J-1 recomputed to the digit)

- **Commit:** see the reply (one commit on top of `6588dec`, Fable trailer, author `alvaro.burgos@ekowai.com`; `git checkout -- scripts/reasoning-map/` before staging; tree clean after; `git stash` NOT used). **Nothing applied to prod** — this round made NO prod contact at all (no query, no capture; the committed `atv_a704e.prior.json` / `atv_a704e.text.prior.json` are unchanged). The shared emitters were NOT touched.
- **Date:** 2026-09-24 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling.

1. **IMPORTANT — the two `-06` visibility rules are DROPPED and recorded as `atv_a704e-J-3`.** `precipitation_influence` and `storage_temperature` ← `sampling_method == 'automatic'` are gone from `src/lib/eval/field-configs/atv_a704e.ts`; `20260917102710_field_configs_atv_a704e.sql` and its rollback were re-emitted (**3 UPDATEs → 1**; the migration no longer mentions either symbol or the literal `sampling_method == ''automatic''` — pinned). Reason, stated in the new J-block: no prod cell ties either field to automatic sampling (the IQC-Card 8 log prints the method boxes and the precipitation box as separate lines with no rule between them; the storage-temperature cell is a general sentence about every wastewater sample), and the only basis is the inventory §3 line, which the global constraints class as a **pointer, not a source** — no transcript can settle it. Fail-safe is what ships: the fields stay visible, so a manual-sampling plant is still asked. `atv_a704e-J-3` carries all three prod cells verbatim as the evidence that does NOT settle it, the inventory §3 line as the insufficient basis, both `visible_when` strings verbatim as the proposed SQL (guarded `visible_when IS NULL`, with its rollback), and the OCR of IQC-Card 8 / §5.1 as the unblock path — mirrored on the sign-off sheet. The module keeps the rules as the exported `WITHHELD_06_RULES`, and the test now asserts the emitter ACCEPTS all three rules while only the photometer one is emitted, so the withholding is a recorded judgment rather than a guard artefact. Report counts, §5 and the residue §11 updated (field entries 24 → **22**, emitted field rules 10 → **8**, sign-off blocks 71 → **72**, J 2 → **3**, observations 4 → **5**).
2. **Test title corrected** (`field-configs-atv_a704e.test.ts`): "the 23 `attest_*` booleans that stay" → "the 21 … (the brief and the inventory say 23 — refuted, `atv_a704e-O-2`)". The assertion was already 21 and is unchanged.
3. **`atv_a704e-U-1` now states the key ambiguity a future lookup would inherit.** The same prod cell prints **TEN** rows in column 1 of IQC-Card 2 Sheet 1 while prod's `qa_measure` enum has **NINE** tokens: "Pipettes 100-1000 µl volume check" and "Pipettes > 1000 µl volume check" collapse into the single token `pipettes`, and the printed quality-target column splits them (< 2 % vs < 1 %) — exactly the split CR-025 already encodes as a volume band. A `TAB_IQC2_S1` keyed on `qa_measure` alone therefore cannot carry both target rows; the block says the key would need a second column (volume band) or the enum a tenth token, and that **`atv_a704e-D-22` keeps the enum byte-for-byte under owner ruling D-1**. Mirrored as a new sheet observation, and the register note's "alle neun QS-Maßnahmen" prose now states the nine-token selection against the ten printed rows.
4. **`src/lib/eval/equations/atv_a704e.ts` header reworded.** The blanket "each row ships a matching `atv_a704e-U-*` entry" was wrong: `-08-D1`, `-09-D1 … D5` and `-10-D1 / D2` point at `U-1`, `-11-D1` at `U-2`, `-11-D2` at `U-3`, while **`-12-D1` and `-12-D2` name none deliberately** — they are structural counts of a deviation log and a personnel list, and IQC-Card 10 / IQC-Card 11 print FORMS, not tables of values, so there is no printed source a U-block could ask for. Their descriptions already said "kein Transkript" without a pointer; the header now matches them.
5. **`atv_a704e-G-5` archives its own rows.** The block now opens with three `INSERT INTO compliance_requirements_archive_atv_a704e … WHERE c.id = … AND md5(c.condition) = '<POST-G-2 md5>'` (`82b143992cf7b2801a4d1b02ec4663bd` CR-019, `91c7fd2875f8e2d08f0bf9446c806740` CR-022, `5881d47a24bc89d6d7fe989d56b1347c` CR-023 — computed with `node:crypto` in the generator, identical to the guards of its own UPDATEs), and its rollback restores from **those** rows with an explicit `AND md5(a.condition) = '<post-G-2 md5>'` so undoing G-5 leaves G-2 in place. The closing note reads "the **THREE** restores above" and explains that the archive now holds two rows per id. The shared `RESTORE(code)` helper gained the same `md5(a.condition)` guard (the ORIGINAL md5) so every other gate block stays unambiguous against the two-row archive; the STAGED header's shorthand line documents it.
6. **`register-a704e-bestimmungen.test.tsx` gained two `pruefmittel` cases** (4 tests total): a photometer row and a pipette row through the real `RegisterEditor` — each of the four row-scope `visible_when` columns renders ONLY on its own row type (photometer checkbox 1×, the two pipette numbers 1× each, the thermoblock field 0×), and switching the first row's `equipment` to `heating_device_thermoblock` and then to `piston_stroke_pipettes` swaps exactly those columns while `ATV-A-704E-11-D1` / `-D2` keep reading the stored rows (2 / 1,5 → 2 / 2,4 after typing).
7. **Report §5 float wording:** "100 / 130 / 95 ⇒ mean 108,333… / 20 % exactly" → "**20,0 % (engine 20.000000000000004, float — pinned with `toBeCloseTo`)**".

Counts after the round: tables 0 (unchanged), registers 9 (unchanged), **22** field entries (21 create + 1 UPDATE), 12 equations (unchanged), **72** sign-off blocks (+5 observations), 72 STAGED ☐ blocks (1 535 lines, 0 non-comment lines). The equations migration is byte-identical (only its module header comment changed); the field-configs migration + rollback are the only regenerated SQL.

## Raw output (once, before the commit)

```
node <scratchpad>/gen-a704e-staged.mjs
  → wrote scripts\verification\atv_a704e-STAGED-plan3-rulings.sql (1535 lines, 72 blocks: G 8, C 4, D 42, R 1, F 1, J 3, U 6, O 2, X 3, I 2); sheet section 613 lines
grep -vc "^--\|^$" scripts/verification/atv_a704e-STAGED-plan3-rulings.sql → 0
grep -c "^### atv_a704e-" SIGN-OFF-plan-3.md → 72 · grep -c "## Task 27 — ATV-A-704E" → 1
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts atv_a704e 20260917102710
  → wrote 22 field entries + 0 section entries   (grep -c "^UPDATE fields f SET" → 1 · "^INSERT INTO fields" → 21)
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts atv_a704e 20260917102720 → byte-identical (git status clean for it)
pnpm vitest run --project unit <field-configs-atv_a704e, equations-atv_a704e, register-a704e-bestimmungen, generated-sql-freshness>
 Test Files  4 passed (4)
      Tests  25 passed (25)
   Start at  14:05:30
pnpm test
 Test Files  352 passed | 1 skipped (353)
      Tests  3108 passed | 1 expected fail | 1 skipped (3110)
   Start at  14:05:34
   Duration  64.12s (transform 20.06s, setup 63.02s, import 163.69s, tests 69.35s, environment 252.26s)
pnpm -s typecheck → exit 0 · pnpm -s eslint <4 touched files> → exit 0
pnpm vitest run --project integration tests/harness/a704e-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  39 passed (39)
   Start at  14:06:57
```
(3106 → 3108 = the two new `pruefmittel` render cases; the harness is unchanged and was re-run after the edits.)
