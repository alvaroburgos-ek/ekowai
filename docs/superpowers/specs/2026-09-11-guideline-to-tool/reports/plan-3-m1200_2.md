# Plan 3 — DWA-M-1200-2 (`m1200_2`) — Task 16 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 17)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (raw `claude --version` output "2.1.260 (Claude Code)"; no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `19cee8f`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-2 m1200_2` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`, the 19 worksheet titles + active field counts, the 4 equation rows with ids / formulas / md5 / status, the 15 compliance rows with ids / severities / md5(condition) / titles, labels / units / `is_required` / `validation_rules` of the fields in scope, the `information_schema` column lists of `equations` / `compliance_requirements` / `fields`). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-2\DWA-M_1200-2_GD.md` (Gelbdruck Juli 2025, 2641 lines, mathpix LaTeX tables; read at L1–L40 title page, L491–L600 §3.1 / Tab. 2 / Tab. 3 / §3.2, L600–L700 §3.3.1–§3.3.3, L754–L830 Tab. 4, L893–L960 §5.2, L1023–L1175 §5.4, L1257–L1330 §6.4 / Tab. 6, L1448–L1466 §8.2, L1745–L1786 Tab. B.2, L1788–L1892 Anhang C, L2140–L2200 Tab. E.1). The 110 quote spans were lifted mechanically by line number (scratchpad `gen-m1200_2-quotes.mjs`, `JSON.stringify` per span) and every seeded cell is asserted inside its span at build time (`must` / `frag` / `cells`); the inventory was used for pointers only (two of its designs were replaced — §8). The `.pdf` / `.xlsx` beside the transcript were not opened; DWA-M-1200-1's transcript was NOT read (Tab. 3 is seeded from THIS transcript under this standard's code).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m1200_2` | 10 | 58 (58/58 verbatim) | 0 rows skipped; 1 CELL seeded null (TABE1_LEISTUNG Clostridium "24,7" — U-4); 3 tables kept `imported_unverified` (TAB3 U-1 class glyphs + U-2 "mg/e" + X-3 notes not printed; TAB6 U-3 lost line break; TABE1_LEISTUNG U-4) | 5 (all `create`: `validierungsproben` -05, `routineproben_1200_2` -06, `verfahrenskette_stufen` -12, `betriebsparameter` -13, `kostenpositionen` -15) | 0 (the organism select lives inside the register; every driver is an existing prod enum / boolean) | 0 | 14 (all `create` on TAB3: 9 on -02 by the own class — 7 number limits + 2 text; 5 log10 targets on -04 by the inherited class) | 21 UPDATE on consumer-free, gate-free existing inputs + 45 on created fields (66) | 0 (C-2) | 63 (-02 D1; -05 D1 … D50; -06 D1; -12 D1 … D7; -13 D1 … D3; -15 D1) | 0 replacements emitted; 1 retirement (R-1, three verified rows) + 7 gate blocks (G-1 … G-7) + 9 D-pairs STAGED | 42 (+3 observations) | ≈ 0.67 M (tool-marker delta 15.00 M → 14.33 M at report time) | ≈ 80 |

Field entries: 103 = 82 `create` (5 registers, 14 lookup_fills, 63 derived outputs) + 21 UPDATE. Migration `20260917101610`: 82 `INSERT … WHERE NOT EXISTS`, 21 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback restores the 21 UPDATE rows to the captured NULLs and deletes only the 82 `Plan 3:` rows. No `enum_values` is written by any UPDATE (D-1 — `messhauefigkeit_integritaet` carries `keep_prod`). The emitter printed 3 quoted-literal lint warnings (X-4, pinned) and no self-consumer NOTICE (the capture has none); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once):
```
 Test Files  310 passed | 1 skipped (311)
      Tests  2867 passed | 1 expected fail | 1 skipped (2869)
   Start at  12:39:41
   Duration  53.01s (transform 16.21s, setup 54.10s, import 127.78s, tests 53.91s, environment 215.27s)
```
(2837 at `19cee8f` + 30 from this task: seed-m1200_2 11, field-configs-m1200_2 5, equations-m1200_2 11, register-m1200-2-validierung 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output.

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/m1200-2-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  15 passed (15)
   Duration  8.19s (transform 773ms, setup 0ms, import 6.43s, tests 1.58s, environment 0ms)
```
The harness seeds prod's fields / equations / gates (`tests/harness/seed-m1200-2.ts`); the new rows are not in it by construction — it proves the branch still drives every DWA-M-1200-2 chain and gate the harness covers. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` on the seeded tables (`scripts/_probe_m1200_2.ts`, deleted): the printed Anhang-C.2 pairs reproduce (LRV 6,447 → "6,45", 6,505 → "6,51", 7,158 → "> 7,16"); every aggregate accepts a row condition (`mean_rows` / `stdev_rows` / `median_rows` / `max_rows` with `organismus == '…'`, `count_rows` with `… AND erreicht == 1`) — the brief's G-16 / F-1 / F-2 premise is out of date; `stdev_rows` over one row is `manual_required` ("mindestens 2 vollständige Zeilen"), `max_rows` over no rows `manual_required`, `count_rows` 0; a `lookup()` on S3_3_3 for B-2 (no row) makes the verdict `manual_required`; a register without the class driver yields `relevant = 0` on every row and the share "Division durch Null"; two `lookup_key` columns bound to different tables work in one register and a `derived` `lookup()` on a side table without that row is a null cell with no diagnostic; an overridden row keeps its typed credit; the four visibility conditions pass / fail as expected. A second 8-line probe parse-checked the six STAGED gate texts (`NOT x IN {…}`, `IF … THEN a AND b AND (c OR d)`) — all parse, `NOT … IN {'chlor','clo2'}` fails for chlor. Seed builder: all ten tables built first run; seed test → RED on one of my slips (the Legionella condition `frag` without an end marker returned the rest of the span) → fixed in the builder → 11/11; verifier **58/58** on its first run. Field-config test: the emitter accepted all 103 entries first time; 5/5 first run (every refusal asserted, not discovered). Equations test: two REDs — my trailing-count slip (12 non-`-05` entries, not 13) and an ENGINE fact: the override flag stops the refill of EVERY `lookup_value` cell of that row (`refillLookupValues`), so an "abweichend" Verfahrenskette row must carry all three credits (pinned as the real behaviour, I-2; subtitle updated) → 11/11. Render test 3/3 first run. `pnpm -s typecheck` green on the first run (no `Q.<key>` typo — every quote is pinned `not.toContain('undefined')`).

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m1200_2 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-2\DWA-M_1200-2_GD.md"` → **`58/58 quotes verbatim`**, exit 0. PASS list (table · rows · lines):

- TAB3 (6): A L549–L552 · B-1 / B-2 L553–L554 (shared) · C-1 / C-2 L555–L556 (shared) · D L570–L573
- S3_3_3 (3): A L681 · B-1 / C-1 L683 (shared)
- ANHANGC1 (3): A L1800 · B-1 / C-1 L1802 (shared)
- GL_C2_1 (1): k L1822
- TAB4 (8): belebung L761–L764 · mbr L765–L774 (two printed rows) · uv L784–L789 · chlor L790–L794 · ozon L804 · mf_uf L805 · ro_nf L806 · schnellsand L816
- TAB6 (8): mbr L1300–L1303 · mf_uf L1304–L1310 · medienfiltration L1311 · ozon L1312–L1315 · ro L1316 · uv L1317 · uv_aop L1318 · chlor L1319
- TABB2 (12): L1753 … L1760, L1771 … L1774 (one line each)
- S8_2_KOSTEN (8): L1453 … L1458, L1464, L1465
- TABE1_STUFEN (5): L2172, L2173, L2183, L2184, L2185
- TABE1_LEISTUNG (4): L2167 … L2170

Status per table: `md_verified` — S3_3_3, ANHANGC1, GL_C2_1, TAB4, TABB2, S8_2_KOSTEN, TABE1_STUFEN (every printed row lifted, every cell legible; the MF/UF "0 d)" cell `$0{ }^{\text {dl }}$` is kept verbatim and encoded as 0 with note d). `imported_unverified` — TAB3 (U-1 class glyphs, U-2 "mg/e", X-3 notes not printed), TAB6 (U-3 MBR line break), TABE1_LEISTUNG (U-4 "24,7").

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year FROM standards WHERE code = 'DWA-M-1200-2'"
  → DWA-M-1200-2 · 'Gelbdruck (Entwurf) — Juli 2025'
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-2 m1200_2
  → wrote src\lib\eval\field-configs\m1200_2.prior.json: 84 field rows (12 orphan), 171 coded sections of 171, 4 equations, 15 gates (3 unparseable — symbols: [] + parse_error; the emitter refuses conservatively on those worksheets);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m1200_2
  → wrote seed + rollback for m1200_2 -> scripts/migrations/20260917101600_regulation_tables_seed_m1200_2.sql scripts/rollback-20260917101600-regulation-tables-seed-m1200-2.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m1200_2 20260917101610
  → WARNING M12002-06 routineproben_1200_2.limit: expr compares against the quoted literal 'e_coli' … (×3, X-4)
  → wrote 103 field entries + 0 section entries for m1200_2 -> scripts/migrations/20260917101610_field_configs_m1200_2.sql scripts/rollback-20260917101610-field-configs-m1200-2.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m1200_2 20260917101620
  → wrote 63 equations for m1200_2 -> scripts/migrations/20260917101620_equations_m1200_2.sql scripts/rollback-20260917101620-equations-m1200-2.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m1200_2.test.ts` / `equations-m1200_2.test.ts` pin the other two against the committed `m1200_2.prior.json`, default refuse mode). The seed migration carries 58 row upserts (`ON CONFLICT (table_id, row_key)`) and 7 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the ten tables (no earlier DWA-M-1200-2 table seed exists — nothing superseded). The equations rollback deletes the 63 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917101600` → `20260917101610` → `20260917101620`; rollback in reverse. Every file touches `DWA-M-1200-2` rows only. The three empty-condition gates (REQ-02 / -08 / -15) are `parse_error` in the capture but empty ⇒ never a refusal (Task 12c round 2). Scalar-only rows (`validierung_erforderlich_tab3`, `n_pass_min_tab`, `max_unterschreitung_zul`, `perzentil_erforderlich`, `k_faktor_tab`) are not server-materialised (amendment D — expected, noted once here and as I-1); the 58 register-fed rows materialise on save.

## 5. What was encoded

**Tables (10)** — `src/lib/eval/regulation-tables-seed-m1200_2.ts`, builder `m12002SeedTables()`, edition `'2025-07'` (title page L9 "Juli 2025" under L11 "Entwurf" = prod `standards.version` "Gelbdruck (Entwurf) — Juli 2025" — no edition block):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB3 | `klasse` = the six prod `wassergueteklasse` tokens | zielvorgabe, e_coli_max 10/100/100/100/100/10 000, enterokokken_max 100/100/100/400/400/–, legionella_max 1000 (strict "<") + condition, nematoden_max 1 + condition, bsb5_max 10/–…, bsb5_text (printed cell; the Richtlinie 91/271/EWG pointer never expanded), afs_max 10 (A…C-2), afs_text, truebung_max 2 (A…C-2), five log10 targets (A / B-1 / C-1), leistungsziele 1/0 | locked (L544 caption — L534 "müssen … eingehalten werden") | 6 (B-1/B-2 and C-1/C-2 share their printed row) | imported_unverified (U-1, U-2, X-3) |
| S3_3_3 | `klasse` (A, B-1, C-1 — no row for the classes Tab. 3 prints "－") | n_total 16, n_pass_min 15/8/8, max_shortfall_log10 1,0/2,0/2,0, nachweisgrenze_regel | locked (L679 — L681 "muss") | 3 | md_verified |
| ANHANGC1 | `klasse` (A, B-1, C-1) | percentile 10/50/50, kriterium | locked (L1800 "muss") | 3 | md_verified |
| GL_C2_1 | `konstante` (k) | wert 1,282, gedruckt | locked (L1822) | 1 | md_verified |
| TAB4 | `stufe` (belebung, mbr, uv, chlor, ozon, mf_uf, ro_nf, schnellsand) | bezeichnung, mechanismen, einflussfaktoren, methodik, referenz (printed text, tabular wrappers removed) | locked (L754 — L758; O-1) | 8 | md_verified |
| TAB6 | `stufe` (mbr, mf_uf, medienfiltration, ozon, ro, uv, uv_aop, chlor) | parameter_gedruckt, parameter_online, parameter_periodisch, frequenz_online, frequenz_periodisch, frequenz_gedruckt | anhaltswert (L1297 "Beispiel" — L1293; O-2) | 8 | imported_unverified (U-3) |
| TABB2 | `stufe` (twelve printed stages) | *_erreichbar (numbers), *_erwartbar_text / _min / _max per organism group (null for "systemspezifisch"), erwartbar_gemeinsam, anmerkung (the Chlor Ct condition) | anhaltswert (L1748 "Indikative" — L659 "können … herangezogen werden") | 12 | md_verified |
| S8_2_KOSTEN | `stufe` (eight printed cost items) | kosten_min/max_eur_m3, zusaetzlich, bezugsjahr 2020, gedruckt | anhaltswert (L1450 "grobe Richtwerte") | 8 | md_verified |
| TABE1_STUFEN | `stufe` (biologisch, mf_uf, ozon, bak, uv — J-5) | bezeichnung, auslegung, betriebsbedingungen | anhaltswert (L2164 "nur auszugsweise und vorläufig als Beispiel") | 5 | md_verified |
| TABE1_LEISTUNG | `organismus` (four printed) | p10_log10 6,4 / 5,1 / 4,2 / null, p10_gedruckt, proben_gedruckt, methodik | anhaltswert (L2164) | 4 | imported_unverified (U-4) |

Not seeded: Tab. 1 / 2 (abbreviations; Tab. 2 = the 1200-1 class table — X-1); Tab. 5 (qualitative process ratings — no selector in prod); Tab. B.1 (~40 reference-plant rows — no field; the register lets the engineer override a credit with such a figure); Anhang A / D; the Tab.-3 notes a)–g) (not printed — X-3).

**Registers / lookup_fills / visibility** — `src/lib/eval/field-configs/m1200_2.ts`:
- `validierungsproben` (-05 B, create json, `visible_when wassergueteklasse IN {'A','B-1','C-1'}`): `organismus` enum (five Tab.-3 organisms, discriminator), date, `x_i`, `y_i` (label: enter the detection limit when below it — L1819), `below_detection` boolean, derived `lrv_i = log10(x_i / y_i)`, `ziel` (nested `if` over the class-keyed TAB3 columns — `ZIEL_EXPR`), `erreicht` badge (`below_detection == true OR lrv_i >= ziel`), `shortfall`; footer `n_proben_validierung` + the five `validierung_ok_<org>`; note L679. 50 derived outputs on -05 D (all `visible_when` the same class rule): `n_proben_validierung`, `n_pass_min_tab`, `max_unterschreitung_zul`, `perzentil_erforderlich`, `k_faktor_tab`, and per organism `n_`, `n_erreicht_`, `max_unterschreitung_`, `mw_…_calc`, `sd_…_calc`, `p10_…_calc`, `p50_…_calc`, `validierung_ok_`, `perzentil_ok_` (stems `ecoli`, `somat_coliphagen`, `fspez_coliphagen`, `clostridium`, `sulfatreduzierer`).
- `routineproben_1200_2` (-06 B, create json): date, `parameter` (seven Tab.-3 columns, discriminator), `wert`, `limit` (nested `if` over the class-keyed TAB3 cells in row scope), `relevant` badge, `ok` badge (Legionella strict "<"); footer `konformitaet_calc`; note L1283. Undecidable until C-1 (the class does not reach -06).
- `verfahrenskette_stufen` (-12 B, create json): `reihenfolge`, `stufe` lookup_key TABB2 (discriminator), three erreichbar credits as `lookup_value` with an `override` block (`credit_abweichend`, anhaltswert — reference-plant data per row with a reason), derived erwartbar texts, Tab.-6 online / periodic parameters, Tab.-4 method, Tab.-E.1 example (null where a side table prints no row — J-5), Auslegung, Betriebsfenster min / max / Einheit; footer `stufen_count`, `credit_sum_*`; note L659.
- `betriebsparameter` (-13 B, create json): `stufe` lookup_key TAB6 (discriminator) with the printed online / periodic parameters and frequency as `lookup_value`, own parameter text, `messhaeufigkeit` enum (prod tokens), Betriebsfenster, `alarm_verzoegerung_min` (label "5 bis 30 min" — SR-2 shown, the prod-gate maximum 30 checked), badges `online_ok`, `alarm_ok`; footer `parameter_count`, `parameter_nicht_online`, `alarm_verzoegerung_max_calc`; note L1289.
- `kostenpositionen` (-15 B, create json): `stufe` lookup_key S8_2_KOSTEN with min / max as read-only hints (no override block), own `kostenkennwert`, `baupreisindex_jahr`, badge `im_richtwertbereich`; footer `kosten_summe_calc`; note L1450.
- lookup_fill (all create on TAB3, keyed on `wassergueteklasse`): -02 `zielvorgabe_aufbereitung_tab3` (text), `e_coli_limit_1200_2`, `enterokokken_limit_1200_2` (A…C-2), `bsb5_limit_1200_2` (A only), `bsb5_anforderung_tab3` (text, the Richtlinie pointer for B…D), `afs_limit_1200_2` (A…C-2), `truebung_limit_1200_2` (A…C-2), `legionella_limit_1200_2` (← `aerosol_risk == true`), `nematoden_limit_1200_2` (← `weide_oder_futter == true`); -04 five `leistungsziel_<org>_tab3` (← the class rule). Derived `validierung_erforderlich_tab3` on -02 D.
- Field `visible_when` on existing fields (21, all consumer-free, no same-worksheet gate reads them — pinned): -05 `confidence_alpha` ← `validierungsmonitoring_typ IN {'umfaenglich','umfaenglich_montecarlo'}`, `k_faktor_normal` ← `IN {'umfaenglich','umfaenglich_basis'}` (J-1), `perzentil_10_log10` ← A, `perzentil_50_log10` ← B-1 / C-1 (outputs of the prod C.2 rows — consumer-free, outside every chain); -09 `mbr_porendurchmesser` ← mf / uf / mbr, `filtergeschw_langsam` ← langsamsand, `filtergeschw_schnellsand` ← raumfilter / schnellsand; -11 `uv_dosis` / `uv_dosis_referenz` ← uv, `ozon_dosis_spez` ← ozon, `reaktor_hydraulik_charakterisiert` ← the six chemical methods (J-2), `ct_wert` ← chlor / clo2 (J-4), `clo2_restkonz` ← clo2, `perameisensaeure_konz` ← pfa; -13 `messhauefigkeit_integritaet` ← mf / uf / mbr, `transmembrandruck` / `permeatfluss` ← mf / uf / nf / uo / mbr, `uv_transmission` ← uv, `delta_sak254` ← ozon, `restchlor_freies` ← chlor; -14 `sekundaerdesinfektion_verfahren` ← `sekundaerdesinfektion_erforderlich == true` (REQ-12's own IF guard — exempt). Refused / withheld: the section rules on -04 / -05 (C-2), `flux_membran` (C-3), `legionella` / `intest_nematoden` (C-4). Section rules: none.

**Equations (63 new)** — `src/lib/eval/equations/m1200_2.ts`: `M12002-02-D1 validierung_erforderlich_tab3 = lookup('TAB3', wassergueteklasse, 'leistungsziele')` · `-05-D1 n_proben_validierung = count_rows(validierungsproben)` · `-D2 n_pass_min_tab` / `-D3 max_unterschreitung_zul` (S3_3_3 by class) · `-D4 perzentil_erforderlich` (ANHANGC1) · `-D5 k_faktor_tab = lookup('GL_C2_1', 'k', 'wert')` · `-D6 … -D50` per organism (generated `ORGANISMEN × ORG_OUTPUTS`, every formula self-contained): `n = count_rows(reg, organismus == 'x')`, `n_erreicht = count_rows(reg, … AND erreicht == 1)`, `max_unterschreitung = max_rows(reg, shortfall, …)`, `mw = mean_rows(reg, lrv_i, …)`, `sd = stdev_rows(…)`, `p10 = mean − k · sd` (Gl. C.2-1, k from GL_C2_1), `p50 = median_rows(…)` (Gl. C.2-2), `validierung_ok = if(n ≥ S3_3_3.n_total AND n_erreicht ≥ n_pass_min AND max shortfall ≤ max_shortfall_log10, 1, 0)`, `perzentil_ok = if(if(ANHANGC1.percentile == 10, p10, p50) ≥ TAB3.log10_<org>, 1, 0)` · `-06-D1 konformitaet_calc = count_rows(reg, ok == 1) * 100 / count_rows(reg, relevant == 1)` · `-12-D1 … -D3 credit_sum_<gruppe> = sum_rows(verfahrenskette_stufen, credit_<gruppe>)` · `-D4 … -D6 credit_ok_<gruppe>` vs the TAB3 target of the inherited class (viruses ← coliphagen 6,0; protozoa ← Clostridium 4,0 — J-3; bacteria ← E. coli 5,0) · `-D7 stufen_count` · `-13-D1 parameter_count` · `-D2 parameter_nicht_online = count_rows(reg, online_ok == 0)` · `-D3 alarm_verzoegerung_max_calc = max_rows(reg, alarm_verzoegerung_min, alarm_verzoegerung_min IS NOT NULL)` · `-15-D1 kosten_summe_calc = sum_rows(kostenpositionen, kostenkennwert)` (F-1). None outputs a prod symbol (`log10_reduktion`, `perzentil_10_log10`, `perzentil_50_log10`, `lrv_i` stay single-sourced — R-1). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): the printed C.2 pairs → LRV 6,45 / 6,51 / > 7,16 with the below-detection pair reached; 16 pairs with MW 6,58 and SD 0,30 (synthesised 8 × (m ± d)) → **p10 6,1954** (prints 6,20) ≥ 6,00 → `perzentil_ok` 1 for A and B-1 (median 6,58); a wider set (6,4 / 0,5) → p10 5,759 → A 0, C-1 1, B-2 `manual_required`; the §3.3.3 verdict: 16 clean pairs → 1; one miss by 0,5 log10 → A 1; one miss by 1,5 → A 0, B-1 1; two misses → A 0, C-1 1; 15 pairs → 0 for every class; a below-detection pair with x_i / y_i short of the target still counts (L681); thresholds 15 / 2,0 / 50 by class, D `manual_required`; `validierung_erforderlich_tab3` 1 / 1 / 0 / 1 / 0 / 0; routine samples under A (5 / 1000 strict / 12 / 1,5 NTU) → **50 %**, under D (E. coli 5000 ≤ 10 000, Enterokokken and Trübung "kein Wert") → 100 %, no class → `manual_required`; chain biologisch + mbr (bacteria overridden to 5) + uv → Σ 14 / 14 / 13, `credit_ok` 1 / 1 / 1, biologisch + chlor → protozoa Σ 2 < 4,0 → 0, B-2 `manual_required`; monitoring rows → 3 / 1 / 45 (a row without a delay does not count); costs 1,2 + 0,1 + 0,18 → 1,48 €/m³ with the in-range badges 1 / 0 / 1; every no-row branch `manual_required`, never 0.

## 6. Inventory §5 top-wins walk

1. Class-driven validation (`wassergueteklasse` → targets per organism, N = 16 rule, percentile choice, N/A for B-2 / C-2 / D) → **encoded**: five TAB3 target fills on -04 (visible for A / B-1 / C-1), `n_pass_min_tab` / `max_unterschreitung_zul` / `perzentil_erforderlich` from S3_3_3 / ANHANGC1 by class (no row for the "－" classes ⇒ undecidable, the printed fact), `validierung_erforderlich_tab3` on -02, the register and its 50 outputs hidden for B-2 / C-2 / D. The section rules on the EXISTING validation fields are refused (C-2); `leistungsziel_log10` stays (D-1). ✓
2. Validation samples as 16 paired rows per organism with LRV_i, MW, SD, P10 / P50 and the 15/16 · 8/16 verdict → **encoded** (`validierungsproben` + 45 per-organism outputs; the printed C.2 example reproduced; SD and median ARE computed — the brief's F-1 / F-2 gaps are refuted, the only F-block is the unprinted Monte-Carlo variant F-2); the 12 typed statistics stay as D-2 … D-5 pairs, the prod C.2 rows as R-1, REQ-04 / -05 / -06 as G-7 / G-1 / G-2. ✓
3. Verfahrenskette as repeatable stages that spawn Tab.-6 monitoring, Tab.-B.2 credits and stage-specific fields → **partially**: `verfahrenskette_stufen` (credits with override, Tab.-6 / Tab.-4 / Tab.-E.1 hints, Betriebsfenster per row, Σ credits and the class comparison) and `betriebsparameter` (Tab.-6 rows with the alarm rule) encoded; the collapse of worksheets 09 / 10 / 11 / 13 / 20 / 21 / 23 into the rows is NOT done (the stage-specific inputs stay on their worksheets, switched by the existing method selects — win 4); `verfahrenskette` text D-7, REQ-11 G-6.
4. `desinfektionsverfahren` drives the dose / CT fields and the Tab.-4 proof → **encoded** as visibility on the 7 consumer-free -11 inputs and the 6 -13 inputs (+ the -09 membrane / sand switch); the Tab.-4 method text is shown per stage in the chain register; the chlorine "nur als Restdesinfektion" gate is G-4; `flux_membran` C-3. ✓
5. Inherit class, aerosol / pasture flags and effluent values from DWA-M-1200-1 → **Phase 6** (X-1; the values are typed here, prod tokens identical).

Data-quality gaps of the inventory: the four empty worksheets (08 / 20 / 21 / 23) untouched (REQ-09 on the empty -08 is G-3); `pfas_summe_20` µg/l is consistent with THIS transcript (X-2); `verfahrenskette` free text → D-7; `log10_reduktionen` json → D-2; the opaque `rule:*` validation strings stay (display-dead, no gate reads them); Tab. B.1 / B.2 absent from prod → TABB2 seeded, B.1 not (no field; override per row instead).

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m1200_2` line + next example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m1200_2 lines + next example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+42 blocks, +3 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 16)" paragraph).
Created: `src/lib/eval/regulation-tables-seed-m1200_2.ts` (incl. the 110 lifted spans `Q`), `src/lib/eval/field-configs/m1200_2.ts`, `src/lib/eval/field-configs/m1200_2.prior.json` (captured), `src/lib/eval/equations/m1200_2.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m1200_2.test.ts`, `src/lib/eval/__tests__/field-configs-m1200_2.test.ts`, `src/lib/eval/__tests__/equations-m1200_2.test.ts`, `src/components/worksheet/__tests__/register-m1200-2-validierung.test.tsx`, `scripts/verification/m1200_2-STAGED-plan3-rulings.sql` (20 ☐ blocks: C-1, C-2, C-3, C-4, G-1 … G-7, R-1, D-1 … D-9; every line a comment), `scripts/migrations/20260917101600_regulation_tables_seed_m1200_2.sql` + `scripts/rollback-20260917101600-regulation-tables-seed-m1200-2.sql`, `scripts/migrations/20260917101610_field_configs_m1200_2.sql` + `scripts/rollback-20260917101610-field-configs-m1200-2.sql`, `scripts/migrations/20260917101620_equations_m1200_2.sql` + `scripts/rollback-20260917101620-equations-m1200-2.sql`, this report (+ the copy at `.superpowers/sdd/…/task-16-report.md`).
Untouched: every other standard's file; the DWA-M-1200-2 harness files; `scripts/reasoning-map/` (checked out before staging); the two probes deleted; the generator / assembly scripts live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **SD / median per organism ARE expressible** — every aggregate takes a row condition (G-16 closed; `src/lib/expr/evaluate.ts`); the brief's F-1 / F-2 blocks and its "5 registers per organism" fallback are not needed. One register + a generated 5 × 9 output block.
2. **ONE `TAB3` keyed on the six prod class tokens** (the m1200_1 TAB8 shape: one value column per printed parameter), not the brief's `TAB3_LOG10_<ORG>` ×4 / `TAB3_<PARAM>` split; **S3_3_3 / ANHANGC1 keyed on the class tokens** (A, B-1, C-1), not on a `klasse_group` a/b1_c1 helper — no `if()` in the key, and the classes Tab. 3 prints "－" for have no row (undecidable by construction). Tables 10 (brief 6): + GL_C2_1 (the k constant, single-sourced), S8_2_KOSTEN (the §8.2 cost ranges the cost register needs), TAB4 seeded as a text table, Tab. E.1 split in two shapes.
3. **The Tab.-3 limit fills live on -02** (class + the inherited measured values), the log10 target fills on -04 (Planungsphase, class inherited); the brief left the worksheet open. `restchlor_freies` is a -13 field in prod (the brief lists it under -11); its rule sits on -13 where `desinfektionsverfahren` IS inherited.
4. **`routineproben_1200_2` on -06 reads a class that does not reach -06** (capture: consumers -04 / -05 / -11 / -12) — emitted anyway with the undecidable state pinned and the consumer edit STAGED (C-1), fll_naturteich trap 3.
5. **`verfahrenskette_stufen` keys on TABB2 (12 stages), not on TAB6 (8)** — the chain must hold the mechanical / biological stages that carry credits; Tab. 6 / Tab. 4 / Tab. E.1 are `derived` hints sharing tokens where the printed stage is the same (J-5). `betriebsparameter` keys on TAB6, `kostenpositionen` on S8_2_KOSTEN (not TAB6 — the printed cost items).
6. **Additional -13 rules** beyond the brief (`permeatfluss`, `uv_transmission`, `delta_sak254`, `restchlor_freies`), the -09 sand rules and `reaktor_hydraulik_charakterisiert` for every chemical method (J-2) — all consumer-free and gate-free in the capture.
7. **Equation numbering:** 63 rows (brief: 8 + 2 staged) — the brief's "×5" per equation number expanded to one row per organism and output; `M12002-13-D1` of the brief (`perzentil_konformitaet_calc`) became `M12002-06-D1 konformitaet_calc` on the register's worksheet (m277e trap 2).
8. **REQ-06 / REQ-09 are dead in prod today** (on -03 / the empty -08 where their symbols never resolve) — not in the brief; G-2 / G-3 propose the moves. REQ-02 / -08 / -15 have empty conditions.
9. **The Tab.-3 notes a)–g) are not printed** in this transcript (the brief cites "Tab. 3 note f)" for the membrane-vs-sand turbidity ceilings) — X-3; nothing lifted from the 1200-1 twin.
10. **No `select_one` created** (brief: 1 — the organism select lives inside the register); no S-block (no `select_many` anywhere).
11. **Sign-off ids 42** (brief ≈ 9): every refused / withheld / read / mapped item got its own block.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (58/58); every seeded cell is asserted inside its span at build time (`must` / `frag`); every cue in the three modules, the STAGED file and the 42 sign-off blocks was read from the transcript in this session at the cited line (two off-by-one cites — L595 → L596 for the PFAS sentence, "Tab. 2 L515–L520" → L500–L520 — were caught on re-check and corrected before commit); prod facts (enum tokens, consumer lists, section codes, gate ids / conditions / md5, the four equation rows / md5, `standards.version`, labels, column lists) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted all 103 entries and 63 equations first time because the rule set was planned against the capture (consumer lists, the 4 equations, the 15 gates, section codes); the field-config test pins the three refusals (C-2 section rules, C-3, C-4), the IF-guard exemption on REQ-12, the dead gates behind G-2 / G-3, G-A3 and the quoted-literal walk (3 warnings, all on -06).
- Runtime facts were established through the real register contract before the pins (probes deleted); the render test drives the register through `RegisterEditor` with `symbolLookup` for the class (targets per organism, badge, re-targeting on a select change, footer through `evaluateFormula`).
- One engine fact surfaced by a RED and kept as the real behaviour: an overridden row is not refilled at all (I-2) — pinned, subtitle updated, playbook trap 6.
- No `String.replace` with a replacement string on any repo file: the seed module was assembled with `cat` from a head, the generated `Q` block and a tail (Write tool); every later edit used `split/join` with a presence check that throws before writing.
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns five files there); `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing the register computes reaches prod's gates yet** — the 50 -05 outputs, `konformitaet_calc`, the credit sums and the monitoring counts are visible twins; REQ-03 / -04 / -05 / -06 / -09 / -11 keep reading the typed scalars until G-1 … G-7 / C-1 are ratified. That is the fail-safe reading of "never a second producer"; REQ-06 and REQ-09 are additionally dead today (G-2 / G-3).
2. **The -05 output block is large by design** (50 created derived fields, 50 equations): five organisms × nine statistics. A leaner alternative (one register per organism, or a per-organism `organismus_auswahl` select with nine outputs) was rejected because the standard demands the proof "separat für jeden Indikatororganismus" side by side; if the owner prefers fewer fields, the `ORG_OUTPUTS` array is the one place to trim.
3. **Three tables stay `imported_unverified` for printed-cell reasons** (TAB3 U-1 / U-2, TAB6 U-3, TABE1_LEISTUNG U-4): a 5-minute PDF look at p. 16–17, p. 52 and p. 85 flips them (and decides the "24,7" cell and the missing Tab.-3 notes — X-3).
4. **`routineproben_1200_2` is inert until C-1** (the class must reach -06) — the honest fail-safe of "a create never sets consumers" and "never invent a consumer edit".
5. **Seven J-blocks rest on the executor's reading** (generic `umfaenglich` token, the chemical-method set for the reactor attestation, the Clostridium protozoa target, Ct for ClO2, the stage-token mapping, `>= 16`, the below-detection LRV as a lower bound) — all pinned and on the sheet.
6. **Scalar-only twins (I-1)** evaluate on the form / report only; **an overridden chain row is not refilled (I-2)** — a per-column override is a [CODE] candidate.
7. **Bundle growth:** the 110 spans (~40 KB incl. the multi-line Tab.-4 rows) ship in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
8. **Token/time:** ≈ 0.67 M tokens in, ≈ 80 min wall-clock — above the Task 5 / 6 range, driven by the 5 × 9 output block and the ten tables.

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The routine-sample deviation limit (L534 / L1283: one log10 step for E. coli / Legionella, 100 % of the value for other parameters — needs per-parameter deviation rows); the two-week minimum validation duration (L674 "sollte sich über mindestens zwei Wochen erstrecken" — `validierungsdauer_wochen` exists, no gate); the Monte-Carlo variant's N ≥ 10 and α = 0,1 / 0,5 rules (L1885 — F-2); the improved detection limit / spiking rules (Anhang C.4 / C.5, L1892–L1913); the §3.3.4 laboratory / sampling requirements beyond REQ-07 (L700–L735); the Referenzanlagen-Register documentation set (L735, worksheet -07 has one boolean); the §5.2 sand-filter velocity range 0,05–0,5 m/h (L941 — `filtergeschw_langsam` carries no hint); the ozone dose range 0,5–1,0 g O3/g DOC (L1099 — no hint on `ozon_dosis_spez`); the UV reference dose ≥ 40 mJ/cm² (Tab. 4 L784 — `uv_dosis_referenz` carries no gate); the Tab.-B.2 Chlor Ct ≥ 15 mg·min/l, pH ≤ 7,5, ≥ 10 °C condition (seeded in `anmerkung`, not a gate on `ct_wert`); §6.3 Störfallmanagement, §6.5 redundancy, §6.6 / §6.7 (booleans only); §7 storage sizing (L1418–L1436); §8.3 / §8.4 storage and transport costs; §9 environmental effects (REQ-15 empty); Tab. 5 and Tab. B.1. Each is listed here with its line and stays outside this encoding (content-boundary rule) or on the sign-off sheet.
