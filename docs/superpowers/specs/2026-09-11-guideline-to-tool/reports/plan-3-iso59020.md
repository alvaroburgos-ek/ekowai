# Plan 3 — ISO-59020 (`iso59020`) — Task 21 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 22) — **fix round 1 applied 2026-09-18** (see the section at the end; counts below are the post-fix-round values)
- **Model / effort:** the subagent session ran on Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched; the commit trailer carries `Co-Authored-By: Claude Fable 5.1` per the controller ruling · **CLI:** claude 2.1.260 (no update taken — the user declines updates) · **Date:** 2026-09-18
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `359e3fb`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs ISO-59020 iso59020` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (the `standards` row incl. `version`; the 9 worksheet titles; the 26 coded sections C–H of -04 … -09 with titles / field counts; labels / units / `is_required` / VR of the 62 fields on -04 … -09; the 37 compliance rows with ids / severities / md5(condition) / lengths / titles; the 13 equation rows with ids / md5 / formulas / status; the `information_schema` column lists of `compliance_requirements` / `equations` / `fields`; the stored-parameter count (0) and the worksheet-instance count (0) of the standard). `.env.local` never printed.
- **SR-1 source:** every seeded value and every cue was read in this session from the English plain-text transcript `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 59020\ISO-59020-Unlocked.txt` (4131 lines; ISO 59020:2024(en), first edition 2024-05; **VC grade** — the two `.pdf` siblings were NOT opened). Read in full lines (never `cut`): title page L1–L12, §3.3.5–§3.3.10 L360–L395, §6.4–§6.5 L827–L950, §7.3.1–§7.4 L1040–L1238 (Table 3 L1081–L1148 on both pages), §7.5 L1239–L1245, §7.6 L1253–L1400, §8.4.6 L1652–L1665, §8.6 L1738–L1812, Annex A L1820–L2540 (A.1 … A.6.3 with every printed formula block), Annex B intro + Table B.1 L2540–L2600, Annex C L3005–L3216, Annex G L3744–L3860. The 88 quote spans were lifted MECHANICALLY by line range (scratchpad `gen-iso59020-quotes.mjs`, `JSON.stringify` per span → `src/lib/eval/regulation-tables-quotes-iso59020.ts`, never hand-edited); every seeded cell fragment is asserted inside its span at build time (`inSpan`, whitespace-collapsed). The inventory was used for pointers only (two premises refuted — §8 / O-1).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `iso59020` | 1 (TABLE3) | 13 (13/13 verbatim — all 13 printed rows incl. the "continued" page A.5.2 … A.6.3) | 0 rows skipped; 5 line-end hyphenations joined (U-1) + the A.8 "⋅1000" glyph (U-2) → the table stays `imported_unverified` (txt = VC) | 7 (all `create`: `indicators` + `additional_indicators` -04, `inflows` -05, `outflows` -06, `energy_flows` -07, `data_sources` -08, `complementary_methods` -09) | 0 | 0 | 0 | **1** (created `temporal_boundary_note` -09 ← `temporal_boundary_shortened == true`) + 2 row-scope rules inside registers | 0 | **38** (-04 D1–D11; -05 D1–D7; -06 D1–D8; -07 D1–D6; -08 D1–D5; -09 D1) — the nine Σ/Σ aggregates WITHHELD (J-1, fix round 1) | 0 replacements emitted; 9 withheld aggregates (J-1, exact forms in the block); 1 retirement block (R-1: A.1 … A.8 + 8 gates + 7 consumer re-points, conditional on J-1), 6 gate blocks (G-1 … G-6, incl. 2 new gates CR-038 / CR-039 and 4 empty-condition rewrites), 1 consumer block, 25 amendment-K pairs (3 of them twins: D-4 / D-10 / D-17) | 51 blocks (+5 observations) | ≈ 0.61 M (tool-marker delta 15.00 M → 14.39 M at report time) | ≈ 75 |

Field entries: 46 = 46 `create` (7 registers, 38 derived outputs — one per equation —, 1 text input) + **0 UPDATE** (no existing field is touched: every brief target is register-driven, refuted by the text, or gate-read + producer-chained — §5). Migration `20260917102110`: 46 `INSERT … WHERE NOT EXISTS`, 0 `UPDATE fields`, 0 `UPDATE worksheet_sections`; the rollback deletes only the 46 `Plan 3:` rows. No `enum_values` is written by any statement (D-1 trivially). The emitters printed 0 warnings (no quoted-literal collision — pinned; no self-consumer NOTICE — the capture has none; 4 unparseable gates = the 4 EMPTY conditions, which never refuse by the round-2 ruling); default (refuse) mode, no `--gate-guard=warn`.

## 2. Raw test output

`pnpm test` (after all changes, once, after the re-emit):
```
 Test Files  330 passed | 1 skipped (331)
      Tests  2991 passed | 1 expected fail | 1 skipped (2993)
   Start at  18:11:16
   Duration  54.51s (transform 16.14s, setup 56.38s, import 135.05s, tests 56.12s, environment 223.31s)
```
(2970 at `359e3fb` + 21 from this task: seed-iso59020 3, field-configs-iso59020 7, equations-iso59020 8, register-iso59020-inflows 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.)

`pnpm -s typecheck` → exit 0 (first run after the modules existed). `pnpm -s eslint <11 touched .ts/.tsx files>` → exit 0, no output (first run).

Integration harness (embedded PG; no engine change on this task):
```
pnpm vitest run --project integration tests/harness/iso59020-verify.integration.test.ts
 Test Files  1 passed (1)
      Tests  38 passed (38)
   Start at  18:12:32
   Duration  5.54s (transform 810ms, setup 0ms, import 3.82s, tests 1.54s, environment 0ms)
```
The harness seeds prod's 83 fields / 13 equations / 37 gates (`tests/harness/seed-iso59020.ts`); the new rows are not in it by construction — it proves the branch still drives every ISO-59020 gate the harness covers through the real save path. This task adds DATA rows written-not-applied and claims no execution proof beyond the unit pins below.

**TDD evidence.** Runtime facts were established BEFORE the pins with one scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` / `parseCondition` (`scripts/_probe_iso59020.ts`, deleted): a BOOLEAN table cell filled into a `lookup_value` column compares `== true` (1) and NOT `== 1` (0) — the G-15 question of the brief; the module therefore reads the printed word ("Mandatory") via `lookup()`; a row with a blank REQUIRED mass is `complete: false` with null derived cells (never a silent 0) and enters no Σ; a zero-mass / zero-net-energy row is a silent null cell (division by zero is recoverable, no diagnostic) and stays complete; an unset `traceable_recycling` hides `m_reco` (row complete, `pct_reco` 0 by the `if()` short-circuit) while a ticked box with a blank `m_reco` makes the row incomplete; `unit == energy_unit_common` reads the worksheet scalar (G-13) and is 0 on every row while the scalar is unset; `sum_rows` over an empty register is `manual_required` ("Keine vollständigen Zeilen"), `count_rows` 0; `mean_rows(outflows, rlp, t_lp IS NOT NULL AND t_ialp IS NOT NULL)` computes (1,2) while the bare form is `manual_required` (`Unbekanntes Symbol "rlp"`); every staged gate text parses (`IF … THEN …`, `IS NOT NULL`, compound `AND`). A second probe (`scripts/_probe2_iso59020.ts`, deleted) parse-checked all 14 STAGED conditions read back from the STAGED file and recorded pass / fail / pending per block (results in §5 and the blocks). Seed builder: the build-time span guard and the verifier passed on the first run (**13/13**); the seed test went RED once on MY pin (I asserted a contiguous "pro- duced" fragment, but the neighbouring column's text sits between the hyphen and its continuation on the page — the pin became one fragment per line, as the builder already does) → 3/3. Equations test **8/8** first run (pins from the probe). Field-config test: the emitter accepted all 55 entries first time (46 after fix round 1); the test went RED three times on my own pins — a leading space in a collapsed quote (`.trim()`), the walked-expression count (23 column exprs / row rules, not 29 — recounted), and the guard facts I had under-stated: `mTO_X` is read by CR-015 … CR-019 (the last two through the A.5 / A.6 / A.7 chains, round 4) and hiding `EIRENE_X` trips the PRODUCER guard first (`hides EIRENE_X → A.8 pct_ECONRE_X (consumed by ISO-59020-09)`) before the gate guard names CR-020 — each resolved to the real fact → 7/7. Render test 3/3 first run. One RED in the full suite after the last edit: the G-6 → G-5 renumber changed two description strings after the emit — re-emitted (`generated-sql-freshness` / both pins green).

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso59020 "C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 59020\ISO-59020-Unlocked.txt"` → **`13/13 quotes verbatim`**, exit 0. PASS list (TABLE3 · row · lines):

- A.2.2 L1085–L1087 · A.2.3 L1088–L1092 (with the merged "Resource / Inflows" cell) · A.2.4 L1093–L1095 · A.3.2 L1096–L1099 · A.3.3 L1100–L1103 · A.3.4 L1104–L1106 · A.3.5 L1107–L1111 · A.4.2 L1112–L1116 · A.5.2 L1131–L1133 · A.5.3 L1134–L1138 · A.5.4 L1139–L1141 · A.6.2 L1142–L1145 · A.6.3 L1146–L1148 (the "Table 3 (continued)" page, L1127).

Status: `imported_unverified` (a plain-text transcript is a VC source; amendment F: the five hyphenations U-1). Beyond the verifier (table rows only), a scratch checker asserted every `"…" (L…)` fragment of the sheet section and the STAGED file against the whitespace-normalised transcript: **158/158 verbatim** (both generated from line ranges, never retyped); every module `verification_quote` / register `note` is a `Q` span by construction (typecheck pins the key names; the seed test pins no span is empty).

**Absence / presence claims (amendment O — commands re-executable, raw output):**
- O-1 — the inventory's "§3.3.7 examples 'LCA per ISO 14044, LCC, ISO 26000'" is **refuted**: `sed -n '375,378p' "<transcript>" | grep -n "14044\|LCC\|26000\|e\.g\."` → (empty, exit 1); L375–L378 print the definition only ("3.3.7 complementary method method, approach or standard that is used together with circularity measurement (3.3.2) to provide a circularity assessment (3.3.3)"). The datalist comes from Annex C.3 / C.4 / C.5 (`grep -n "^ *— ISO\|^ *— ISO/" …` → 17 bullet lines L3078 … L3198; S-LCA L3206, LCSA L3213).
- J-5 — §8.6.2 prints NO external-use condition on verifiability: `sed -n '1758,1765p' "<transcript>" | grep -in "external"` → (empty, exit 1); L1760 reads "Verifiability of all data documentation is a key criterion to ensure transparency."
- J-6 — §7.5 does not tie aggregation to a system level: `sed -n '1239,1245p' "<transcript>" | grep -in "only\|exclusively\|regional\|interorganizational"` → (empty, exit 1); L1242–L1243 "Aggregation can also be needed for higher system levels."
- Figures A.1 / A.2 ("100 % resource inflow / outflow formula") are images: `grep -n "Figure A\.[12]" "<transcript>"` → L1851, L1886 (caption), L1893, L2023, L2040 (caption) — captions and references only; the 100 % rule is encoded from the printed sentences L1850–L1854 / L2020–L2023.
- J-9 (reclassified from U-2 in fix round 1) — the A.8 multiplier: `grep -n "⋅100" "<transcript>"` → L1929 / L1953 / L1995 / L2127 / L2179 / L2231 (A.1 … A.7 "⋅100"), **L2280 "⋅1000" (A.8)**, L2359 / L2388 (A.9 / A.10), L2628 / L2677 (B.1 / B.2); the A.8 legend L2284 reads "in %".
- J-8 — no printed aggregation for RLP: `grep -n "RLP" "<transcript>"` → L2102 (the formula), L2106 (the legend) only.

## 4. Prior capture / emit commands (re-executable)

```
node scripts/verification/prod-query.mjs --sql "SELECT code, version, issued_year, left(title_de,70) AS title FROM standards WHERE code LIKE 'ISO-59020%'"
  → ISO-59020 · '2024 (First edition 2024-05)' · issued_year null
node scripts/regulation-tables/build-prior-snapshot.mjs ISO-59020 iso59020
  → wrote src\lib\eval\field-configs\iso59020.prior.json: 83 field rows (0 orphan), 88 coded sections of 88, 13 equations, 37 gates (4 unparseable — symbols: [] + parse_error …);
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso59020
  → wrote seed + rollback for iso59020 -> scripts/migrations/20260917102100_regulation_tables_seed_iso59020.sql scripts/rollback-20260917102100-regulation-tables-seed-iso59020.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso59020 20260917102110
  → wrote 46 field entries + 0 section entries for iso59020 -> scripts/migrations/20260917102110_field_configs_iso59020.sql scripts/rollback-20260917102110-field-configs-iso59020.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso59020 20260917102120
  → wrote 38 equations for iso59020 -> scripts/migrations/20260917102120_equations_iso59020.sql scripts/rollback-20260917102120-equations-iso59020.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-iso59020.test.ts` / `equations-iso59020.test.ts` pin the other two against the committed `iso59020.prior.json`, default refuse mode, `warnings` pinned `[]`). The seed migration carries 13 row upserts (`ON CONFLICT (table_id, row_key)`) and 0 `verification_status` upgrades; its rollback deletes TABLE3 (no earlier ISO-59020 table seed exists — nothing superseded; no Plan-1 selection config exists for this standard). The equations rollback deletes the 38 `Plan 3:` rows. Apply order (owner, after the schema migration incl. plan1-D-3-1): `20260917102100` → `20260917102110` → `20260917102120`; rollback in reverse. Every file touches `ISO-59020` rows only. **Materialisation (amendment D, noted once):** all 38 rows are register-fed (each reads only its own worksheet's register carrier; the one worksheet scalar in play, `energy_unit_common`, is read by a row EXPR, not by an equation input) and materialise on save; there is no scalar-only row.

## 5. What was encoded

**Table (1)** — `src/lib/eval/regulation-tables-seed-iso59020.ts`, builder `iso59020SeedTables()`, edition `'2024'` (title page L8–L9 "First edition" / "2024-05"; prod `standards.version` '2024 (First edition 2024-05)'). The standard prints ONE classification table and no numeric constant; every value is a printed formula (Annex A) or a printed sentence:

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TABLE3 | `indicator` = the 13 prod `selected_core_indicator` tokens (`A.2.2_reused_content_inflow` … `A.6.3_resource_intensity_index`, D-1 / G-A3, prod order) | `category` (printed: Resource Inflows / Resource outflows / Energy / Water / Economic), `category_token` (prod `indicator_category` — structural, J-3), `mandatory_optional` (printed "Mandatory" / "Optional"), `mandatory` (boolean), `clause`, `name`, `description`, `principle` ("Retaining / Add / Recovering / Recovers resource value", "Maintains a circular flow of resources", "Indicates resource reduction") | locked (L1068–L1071 "shall be quantified and fully balanced with the use of the mandatory indicators … If a core circularity indicator is not applicable, the organization should explain why and can count the indicator value as zero") | 13 (6 Mandatory: A.2.2 / A.2.3 / A.2.4 / A.3.3 / A.3.4 / A.3.5) | imported_unverified (U-1) |

**Registers / visibility** — `src/lib/eval/field-configs/iso59020.ts`:
- `indicators` (-04 D): `indicator` lookup_key TABLE3 (grouped by the printed category), `category` / `mandatory_optional` / `principle` lookup_values, `selected` / `not_applicable` booleans, `justification` text (row-scope `visible_when not_applicable == true`), `value` / `unit` (the unit is FREE TEXT per row because Table 3 mixes percentages (A.2.x, A.3.3 – A.3.5, A.4.2, A.5.2, A.5.3), dimensionless ratios (A.3.2, A.5.4, A.6.3) and a revenue-per-mass ratio (A.6.2, "monetary units per kg") — no single printed unit list exists), derived `category_code` (TABLE3.category_token), badges `mandatory_flag` (`lookup('TABLE3', indicator, 'mandatory_optional') == 'Mandatory'`), `ok` (a Mandatory row neither selected nor N/A ⇒ 0), `na_justified` (N/A without text ⇒ 0); footer `indicators_count` / `mandatory_missing` / `na_unjustified` / `mandatory_core_covered` / `mandatory_core_indicators_included_code`; note L1068–L1071 + F-1 / F-2 / D-1 … D-4.
- `additional_indicators` (-04 D): `name` (datalist = the ten Table-B.1 indicators), `definition`, `value`, `unit`; footer `additional_indicators_count`.
- `inflows` (-05 F): `label`, `m_ti` / `m_reui` / `m_reci` / `m_reni` (kg, required, min 0), derived `pct_reui` / `pct_reci` / `pct_reni` (Formula (A.1) – (A.3) per row), `pct_linear` (A.2.1 "subtracting the circular inflows from 100 %"), `m_linear`, badge `balanced` (circular ≤ total); footer count / Σ mTI / Σ linear / unbalanced (the four mass-weighted shares are withheld — J-1).
- `outflows` (-06 G): `label`, `m_to` / `m_reuo` / `m_reno` (required), `traceable_recycling` boolean (discriminator), `m_reco` (required, row-scope `visible_when traceable_recycling == true`), `t_lp` / `t_ialp` (optional, A.3.2), derived `pct_reuo` / `pct_reco` (A.3.4: 0 % without traceable data) / `pct_reno` / `pct_linear` / `m_linear` / `rlp` (A.4) / badge `balanced`; footer count / Σ mTO / Σ linear / unbalanced / untraceable (the four shares withheld — J-1).
- `energy_flows` (-07 C): `label`, `unit` enum MJ / kWh (= prod `energy_unit_common` tokens = the printed "e.g. MJ, kWh"), `ei_rene` / `eo_rene` / `ei_te` / `eo_te` (required), derived `net_consumed`, `pct_econre` (A.8 · 100, U-2), badge `unit_ok` (`unit == energy_unit_common`, G-13 — 0 while the worksheet unit is unset); footer count / Σ EITE / Σ EOTE / `energy_unit_mismatch` (`pct_econre_agg` withheld — J-1; the four Σ carry the §A.4.2 precondition "meaningful only at energy_unit_mismatch == 0", G-6).
- `data_sources` (-08 C): `component` (required), `source`, `origin` primary/secondary + `scope` foreground/background + `specificity` specific/generic (the three printed pairs of §7.6.2, each required — J-4; tokens = prod `data_category`), `traceable`, `documented`, `justification`, badge `conservative` (secondary or generic — "should be conservatively applied"); footer count / untraceable / secondary / generic / background.
- `complementary_methods` (-09 E): `method` (required, datalist = the 17 Annex-C.3 / C.4 standards + S-LCA / LCSA of C.5 — O-1), `aspect` enum social / environmental / economic (C.2.1), `reference`, `result`; footer count.
- Created input: `temporal_boundary_note` (-09 G, text) `visible_when temporal_boundary_shortened == true` (§6.4.2 L849–L850; the driver is inherited on -09 — the only field rule; server-side `pending` = I-1).
- Field `visible_when` on existing inputs: **0** — the -07 block gating by indicator selection is register-driven (M-1); `information_verifiable` ← external is refuted by §8.6.2 and gate-read (J-5); `aggregation_method` ← system level is consumed + "ALL"-token + not printed (J-6); every -05 / -06 / -07 single-X scalar is gate-read and producer-chained (asserted through `gateReaders` / `producerChain` / `emitFieldConfigSql` throws — R-1). Section rules: none.

**Equations (38 new; fix round 1 withdrew the nine Σ/Σ aggregates into J-1)** — `src/lib/eval/equations/iso59020.ts`, every row register-fed, every Σ inlined (never chained): `ISO-59020-04-D1 indicators_count = count_rows(indicators)` · `-D2 mandatory_missing = count_rows(indicators, ok == 0)` · `-D3 na_unjustified = count_rows(indicators, na_justified == 0)` · `-D4 mandatory_core_covered = count_rows(indicators, mandatory_flag == 1 AND ok == 1)` · `-D5 mandatory_core_indicators_included_code = if(count_rows(indicators, indicator == 'A.2.2_reused_content_inflow' AND ok == 1) == 1 AND … (six clauses, one per Mandatory token of Table 3) …, 1, 0)` (per-token exactly-once — J-2 / G-4; unparenthesised in the formula because the equations emitter's legacy `CALL` regex reads `AND (` as a call — pinned) · `-D6 … D10 <category>_indicators_selected = count_rows(indicators, category_code == '<token>' AND selected == true)` · `-D11 additional_indicators_count` · `ISO-59020-05-D1 inflows_count` · `-D2 m_ti_total = sum_rows(inflows, m_ti)` · `-D3 … D5 m_reui/reci/reni_total` · `-D6 m_linear_total = sum_rows(inflows, m_linear)` · `-D7 inflows_unbalanced = count_rows(inflows, balanced == 0)` · `ISO-59020-06-D1 … D8` the same over `outflows` with `m_reco_total = sum_rows(outflows, if(traceable_recycling == true, m_reco, 0))` and `-D8 outflows_untraceable = count_rows(outflows, traceable_recycling == false)` · `ISO-59020-07-D1 energy_flows_count` · `-D2 … D5 ei_rene/eo_rene/ei_te/eo_te_total` (each description: "nur sinnvoll bei energy_unit_mismatch == 0 (§A.4.2 …)") · `-D6 energy_unit_mismatch = count_rows(energy_flows, unit_ok == 0)` · `ISO-59020-08-D1 data_sources_count` · `-D2 data_sources_untraceable = count_rows(data_sources, traceable == false)` · `-D3 … D5 secondary / generic / background` · `ISO-59020-09-D1 complementary_methods_count`. None outputs a prod symbol (A.1 … A.13 keep their verified rows; R-1 proposes the retirement of A.1 … A.8). **Pinned values** (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback TABLE3 — the standard prints no worked example for the aggregates, so the pins are the printed forms on chosen inputs): inflows Stahl 1000 / 200 / 500 / 0 → 20 / 50 / 0 %, linear 30 % = 300 kg; Holz 400 / 0 / 0 / 300 → 0 / 0 / 75 %, linear 25 %; Σ 1400 / 200 / 500 / 300 / 400 kg (the mass-weighted **14,2857 / 35,7143 / 21,4286 / 28,5714 %** were probed and are recorded in J-1, not emitted); an over-balanced row (100 / 60 / 50 / 0) → linear −10, unbalanced 1; a blank `m_reui` → incomplete, count 3 not 4, Σ 1500; m_ti = 0 → null shares, row complete; empty → Σ manual_required, count 0. Outflows Produkt 1000 / 100 / traceable 300 / 0, 12 a / 10 a → 10 / 30 / 0 %, linear 60 %, RLP 1,2; Reststoff 500 / 0 / untraceable / 100 → 0 / 0 / 20 %, linear 80 %, `m_reco` null, complete; Σ 1500 / 100 / 300 / 100 / 1000 (6,67 / 20 / 6,67 / 66,67 % probed → J-1); untraceable 1; a traceable row with blank `m_reco` incomplete. Energy (worksheet MJ) Strom 300 / 0 / 1000 / 0 → 30 %; Wärme kWh 50 / 10 / 200 / 40 → 25 %, mismatch; Durchlauf 0 / 0 / 100 / 100 → null, complete; Σ 350 / 10 / 1300 / 140 (29,3103 % probed → J-1); mismatch 1, unset unit ⇒ 3. Indicators over TABLE3: A.2.3 unselected ⇒ missing 1; A.2.4 N/A without text ⇒ unjustified 1; covered 5 ⇒ code 0; all six Mandatory selected / justified ⇒ covered 6, missing 0, **code 1**; six + a duplicate A.2.2 ⇒ covered 7 but **code 0**; a duplicate masking a missing token ⇒ covered 6 but **code 0**; an extra Optional row ⇒ still 1 (per-token form, fix round 1); category counts inflow 1 / outflow 2 / energy 1 / water 0 / economic 0 (and 2 for the full set with A.2.4 N/A); a row without an indicator incomplete; empty register ⇒ every count 0 and code 0 (never a phantom pass). G-15: boolean cell `== true` → 1, `== 1` → 0. Data sources 2 complete / 1 incomplete → untraceable 1, secondary 1, generic 1, background 1, conservative badges 0 / 1 / null; complementary methods 2 / 1 incomplete.

## 6. Inventory §5 top-wins walk

1. Make inflows and outflows repeatable rows (one per flow X) with A.1 – A.7 evaluated per row and the linear share derived → **encoded** (`inflows` on -05, `outflows` on -06: every Annex-A form per row, the A.2.1 / A.3.1 linear share and the A.3.4 0 %-rule as row expressions, the 100 %-balance badge, mass-weighted aggregates + counts in the footer); the single-X scalars and A.1 … A.7 stay until R-1 / D-6 … D-17 are ratified. ✓
2. Indicator register from Table 3 (13 rows, mandatory flag pre-filled) with selected / applicable / justification per row → derives `mandatory_core_indicators_included` and gates the energy / water / economic blocks by selection → **encoded** as the `indicators` register (TABLE3 lookup_key, the flag from the table, per-row N/A + justification, `mandatory_core_indicators_included_code` in the per-token exactly-once form, the five per-category selection counts) with the CR-009 rewrite G-4 (six per-token clauses) and the CR-010 condition G-2; the **block gating is M-1** (register-driven ⇒ sign-off; blocks stay visible; C-1 → G-1 → R-1 → the hides). ✓ (gating = sign-off)
3. Derive `pct_linear_inflow` / `pct_linear_outflow` instead of typing them and then checking the 100 % sum → **encoded** per row (`pct_linear`, `m_linear`) and as Σ linear per worksheet (`m_linear_total` / `m_linear_out_total`; the Σ/Σ shares `pct_linear_agg` / `pct_linear_out_agg` are withheld — J-1); the typed scalars stay (D-10 / D-17, twins), CR-014 / CR-019 → G-3 (balance per row + at least one flow). ✓
4. Inherit water volumes (VAIW / VTWW / VTWU) from ISO-46001 / VSME B06 and recycled-content facts from DIN-14021 → **Phase 6** (X-1 / X-2); water and economic indicators stay scalar (site-level), A.5.1 / A.2.3 name ISO 14046 / ISO 14021 as references only.
5. Elementary-component / data-source register (§7.6.1.2, §7.6.2) replacing four booleans with rows that carry data_category and traceability → **encoded** (`data_sources` on -08 with the three printed classification pairs, traceability, documentation, justification, the conservative-use badge and five counts); the booleans stay (D-22 … D-24; CR-026 / CR-029 proposals inside the D-blocks). ✓

Data-quality gaps of the inventory: `aggregation_method` free text although §7.5 / Annex G is normative → the aggregation rule is printed only as examples b) / c) (J-1, nothing emitted); CR-008 / CR-010 / CR-036 / CR-037 empty conditions → G-2 (four conditions, one worksheet move, one attestation proposal); "unit '-' on enums" → untouched (prod hygiene, no printed unit); the A.3 "PRENI(X)" symbol → kept as the printed label of the row column; "Table 3 continued rows not re-verified" → **closed**: A.5.4 / A.6.2 / A.6.3 are lifted from L1139–L1148 and pass the verifier.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`iso59020` line + next-slug example), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (iso59020 lines + next-slug example), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+51 blocks, +5 observations, inserted before "Plan 3 tooling rulings"), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 21)" paragraph).
Created: `src/lib/eval/regulation-tables-quotes-iso59020.ts` (88 spans, generated), `src/lib/eval/regulation-tables-seed-iso59020.ts`, `src/lib/eval/field-configs/iso59020.ts`, `src/lib/eval/field-configs/iso59020.prior.json` (captured), `src/lib/eval/equations/iso59020.ts`, `src/lib/eval/__tests__/regulation-tables-seed-iso59020.test.ts`, `src/lib/eval/__tests__/field-configs-iso59020.test.ts`, `src/lib/eval/__tests__/equations-iso59020.test.ts`, `src/components/worksheet/__tests__/register-iso59020-inflows.test.tsx`, `scripts/verification/iso59020-STAGED-plan3-rulings.sql` (51 ☐ blocks; 666 lines, 0 non-comment lines), `scripts/migrations/20260917102100_regulation_tables_seed_iso59020.sql` + `scripts/rollback-20260917102100-regulation-tables-seed-iso59020.sql`, `scripts/migrations/20260917102110_field_configs_iso59020.sql` + `scripts/rollback-20260917102110-field-configs-iso59020.sql`, `scripts/migrations/20260917102120_equations_iso59020.sql` + `scripts/rollback-20260917102120-equations-iso59020.sql`, this report (+ the copy at `.superpowers/sdd/…/task-21-report.md`).
Untouched: every other standard's file; the ISO-59020 harness files; `scripts/reasoning-map/` (the unit run churns files there — checked out before staging); the two scratch probes deleted; the three generators (quotes, STAGED + sheet section, playbook paragraph) live in the scratchpad.

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Table keys are prod's tokens** (`A.2.2_reused_content_inflow` …), not the brief's `a_2_2 …` — D-1 / G-A3 as the brief itself allows ("prod `selected_core_indicator` enum has 13 values, D-1: use its tokens"); `category_token` added as a structural column (J-3) so the per-category counts key on the prod `indicator_category` tokens.
2. **`mandatory` is compared as the printed string** ("Mandatory") through `lookup()` in the row expr; the boolean column is seeded too and the G-15 fact (`== true`, not `== 1`) is pinned — the brief left the literal to the executor.
3. **`applicable` became `not_applicable`** (unset boolean = applicable; a declared N/A needs its justification): with the brief's `applicable` an untouched row would read "not applicable" and show the justification on every row.
4. **Masses in kg, not t** — the printed legend ("in kg or other mass unit") and prod's units (`mTI_X` … `EOTE_X`: kg / MJ) win.
5. **`m_reco` is hidden per row unless `traceable_recycling`** (required only when the box is ticked) instead of the brief's always-required `m_reco` with a 0-branch — the A.3.4 sentence describes missing DATA, so the input disappears rather than being typed as 0 (J-7).
6. **`rlp_mean` NOT emitted** (brief -06-D1): no printed aggregation for a ratio (J-8; the mean_rows form is probed and named as the alternative).
7. **Mass-weighted aggregates WITHHELD** (fix round 1; the brief's "only if §7.5 / Annex G prints that rule"): Annex G.2 prints EXAMPLE b) (average) and EXAMPLE c) (sum-then-divide) as options and A.2.3 prints "can be calculated and then aggregated" — choosing Σ/Σ is a ruling; the nine equations stay out of `20260917102120` with their exact forms in J-1.
8. **`data_category` split into three required enum columns** (origin / scope / specificity) over the six prod tokens — §7.6.2 prints three pairs (J-4); the brief's single `data_category enum` cannot classify a source on all three axes.
9. **Datalists from Annex C (not §3.3.7) and Table B.1** — §3.3.7 prints no examples (O-1, grep in §3).
10. **Visibility: 1 emitted rule (created field) instead of the brief's 6** — `information_verifiable` (J-5: refuted + CR-035), `consolidation` / `aggregation_method` (J-6: consumed, "ALL", not printed), the -07 blocks (M-1); the brief's `temporal_boundary_note` rule IS emitted.
11. **G-blocks 6 (brief 3)**: G-1 = the brief's CR-020 … 025 (as IF-guards on the created selection counts, after C-1), G-2 = CR-008 / 010 / 036 / 037 (CR-008 moves to -09; CR-037 needs a created attestation), G-3 = CR-014 / 019 onto the register balance, G-4 = CR-009 as six per-token exactly-once clauses (fix round 1), G-5 = NEW CR-038 (common energy unit, "shall", block), G-6 = NEW CR-039 (warn companion on the energy Σ, fix round 1).
12. **Equations 38 (brief ≈ 10)**: the brief's `-05-D1` bundle is 7 rows (one output each), likewise -06 (8) / -07 (6) / -04 (11) / -08 (5) / -09 (1) — counts, Σ masses (A.1 "absolute values with units should also be documented"), the per-category selection counts and the badge counts; the brief's `pct_*_agg` / `pct_econre_agg` (9) are withheld under J-1 (fix round 1).
13. **Sign-off ids 51 (brief ≈ 8)**: the brief's `iso59020-U-1` (continued rows — closed, re-used for the hyphenations), `M-1`, `J-1`, `R-1`, `G-1 … G-3` kept where they exist; every further refused / withheld / judged item got its own block (25 D-pairs per amendment K; fix round 1 added G-6 / J-9 / X-3 and removed U-2).
14. **Render test file name** `register-iso59020-inflows.test.tsx` kept (brief) — covers `inflows` (two rows, per-row percentages, a typed change, a new incomplete row) and `outflows` (the A.3.4 hide).
15. **Prod section letters used verbatim** (-04 D, -05 F, -06 G, -07 C, -08 C, -09 E / G); every created field lands in a captured section (pinned).

## 9. Self-review

- Every `verbatim_quote` passes the verifier (13/13); every seeded cell fragment is asserted inside its row span at build time (whitespace-collapsed, one fragment per printed line — the layout splits every cell); every `"…" (L…)` fragment on the sheet and in the STAGED file was machine-checked against the transcript (158/158); the spans, the sheet section and the STAGED file were generated from line ranges (never retyped); the STAGED gate conditions are the CAPTURED strings wrapped in the IF guard (read from `iso59020.prior.json` by the generator); prod facts (tokens, consumer lists, section codes, gate / equation ids + md5, labels, units, `is_required`, the 0 / 0 stored-value / instance counts, `standards.version`) come from the in-session capture / queries listed in §0 / §4 and the STAGED header.
- The emitters accepted all 55 entries and 47 equations first time (46 / 38 after fix round 1) because the rule set was planned against the capture; the field-config test pins every refusal of the brief's targets (CR-035, the consumed `aggregation_method`, the -05 / -06 / -07 scalars incl. the chain readers CR-018 / CR-019 and the producer-guard-first fact on `EIRENE_X`, CR-009 + consumers), that the four empty gates never refuse, the G-A3 facts for TABLE3 / the energy enum / the data-source pairs, that no column key shadows a prod symbol of its worksheet, that no quoted literal collides (61 expressions walked after the fix round), the 25 amendment-K pairs against the capture, that none of the nine withheld aggregate symbols is emitted, and that no UPDATE is emitted at all.
- Runtime facts were probed through the real register / formula / condition contracts before the pins (probes deleted); the render test drives `inflows` and `outflows` through `RegisterEditor`.
- No `String.replace` with a replacement string on any repo file except two literal `sed` renames (`iso59020-G-6` → `-G-5`, verified by `grep -c`); every other splice used `split/join` with a presence-and-uniqueness check that throws before writing, or the Edit / Write tools; the seed / field-config / equation modules were written with the Write tool.
- `git checkout -- scripts/reasoning-map/` run before staging; `.env.local` never read; one commit; author `alvaro.burgos@ekowai.com` verified before committing (not changed).
- Reviewed the diff of every modified shared file: only the three registry lines (+ the next-slug example lines), the sign-off section and the playbook paragraph.

## 10. Concerns (for the controller)

1. **Nothing computed reaches prod's gates yet** — every gate block (G-1 … G-5) and the retirement (R-1) are STAGED; until ratified CR-011 … CR-020 keep checking the single-X scalars, CR-009 the typed boolean, and the registers are documentation beside the scalars. That is the fail-safe reading: every brief target is gate-read or consumed.
2. **-04-D5 / G-4 read "exactly once" per Mandatory token** (J-2, fix round 1) — no typed figure; a duplicated mandatory row fails the code and the staged gate (F-2 closed for the mandatory rows); the owner may prefer ">= 1" (tolerating duplicates) — the alternative is in J-2.
3. **J-9 (was U-2) — Formula (A.8) prints "⋅1000"**: the per-row column (and the withheld J-1 form) use · 100 on the legend ("in %"), the seven sibling forms and prod's verified A.8 (md5 captured); the SR-3 PDF-page check is the closing action — the .pdf beside the txt was NOT opened per the task rule. Prod hygiene seen on the way (X-3): the A.8 formula LHS is `PECONRE(X)` while `output_symbol` is `pct_ECONRE_X` (same pattern on A.3 / A.5 / A.6 / A.7 / A.9 / A.10) — R-1 archives the rows as they are.
4. **25 amendment-K pairs** — the per-flow / per-indicator / per-source registers twin every scalar of a model that asks each fact once; each is one ☐ (data-free today: 0 stored values, 0 instances). D-6 … D-9 / D-11 … D-16 / D-18 … D-21 can only retire inside R-1 (they feed the verified equations).
5. **R-1 changes what -09 inherits** (seven `pct_*_X` → seven `pct_*_agg`; `RLP_X` has no aggregate) — an -09 data-shape ruling, not a mechanical re-point, and since fix round 1 conditional on J-1 (the aggregates are withheld).
6. **I-1** — the one emitted visibility rule reads an inherited driver: hidden on the form / report / PDF, `pending` (visible) for the server materialiser / approval gate — fail-safe, recorded once.
7. **English cells beside German labels** (Table 3 names / descriptions / principles, the Annex-C datalist) until the owner rules on a German source (SR-3) — the register shows the printed English text.
8. **Token/time:** ≈ 0.61 M tokens in, ≈ 75 min wall-clock — in the Task 16 / 20 band; the cost sits in the 25 D-pairs and the five gate rewrites, not in the transcript (13 rows).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

§7.3.1 "taking changes in stocks into account" (L1069) — no stock-change column on the flow registers (a `stock_change` mass would enter the balance; the sentence prints no formula); A.1 "absolute values with units should also be documented" is met per row, but the per-row UNIT of a flow is fixed to kg (a mass-unit column with conversion is an F-item); A.2.3 "the fractions that are pre-consumer and post-consumer content can be disaggregated" (L1968) — no split; A.2.4's three renewable-material criteria (L1974–L1977) and the disclosure of the verification method (L1989–L1990) — no per-row attestation; A.3.3 / A.3.4 "the assumptions and information on historical reuse / recycling rate … should be documented" (L2118–L2119, L2172–L2173) — no per-row text; A.3.5's qualifying conditions for renewable recirculation (L2214–L2222) — no per-row checklist; §7.6.1.4 mitigation of missing data (L1303–L1313) — no register of gaps; §7.6.1.5 normalisation to the common denominator (L1317–L1319) — the boolean `data_normalized` only; §7.7 documentation items a)–… (L1387 ff.) — `documentation_complete` only; §8.6.1 a)–c) resource volumes / values / quality in the report (L1749–L1753) — the statement text only; Annex B indicators (B.1 … B.6.6 formulae, L2600 ff.) — only the datalist names, no per-indicator formula (each is an additional indicator the owner may encode later); Annex G's "single quantitative reference" rule (L3761–L3770) — no functional-unit field on the registers. Each is a procedural sentence, an attestation prod already carries, or a list whose per-row encoding is a follow-up, and stays outside this encoding or on the sign-off sheet.

---

# Fix round 1 (review verdict on `a9448aa`: NEEDS FIXES — controller rulings applied verbatim)

- **Commit:** see the reply (one commit on top of `a9448aa`, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — no new prod read; the three migrations were regenerated by the Task 0 emitters only (the seed migration is byte-identical: only a comment in the generated quotes module changed).
- **Date:** 2026-09-18 · CLI claude 2.1.260 (no update taken) · the subagent ran on Opus 5; trailer per the controller ruling.

## Important

1. **The nine Σ/Σ aggregates are OUT** (`-05-D7 … D10`, `-06-D7 … D10`, `-07-D6` — the controller's list names nine rows; its "8" is a miscount, all nine listed rows are withdrawn): removed from `equations/iso59020.ts` and, with them, their nine derived output fields and footer entries from `field-configs/iso59020.ts` (a derived field without an equation would be a dangling output). The remaining rows are renumbered contiguously (-05 D1–D7, -06 D1–D8, -07 D1–D6). J-1 now quotes BOTH Annex-G.2 example forms (heading L3773–L3776, a) L3778–L3779, b) L3781, c) L3783–L3789), states that the nine equations are withheld and lists their exact intended forms (re-emit is mechanical), and records the probed values. New pin: none of the nine symbols is emitted (equation or field). The energy Σ `-07-D2 … D5` STAY with the precondition sentence "nur sinnvoll bei energy_unit_mismatch == 0 (§A.4.2 gemeinsame Einheit, L2271–L2272 — Gate STAGED iso59020-G-6)" in each description (pinned), and the new **G-6** block proposes the warn gate `(IF energy_unit_mismatch > 0 THEN energy_unit_mismatch == 0)` as INSERT-class CR-039 (rollback DELETE by worksheet + code + description prefix; parse-checked in-session: 1 ⇒ fail, 0 ⇒ pass, unset ⇒ pending).
2. **G-4 rewritten** to six parenthesised per-token clauses `(count_rows(indicators, indicator == '<token>' AND ok == 1) == 1)` joined by AND over the six seeded Mandatory keys (A.2.2 / A.2.3 / A.2.4 / A.3.3 / A.3.4 / A.3.5, L1085–L1111), with the note "cross-ref F-2 (duplicate rows) — this per-token form closes the hole without [CODE]". **`-04-D5` adopts the same per-token form** — probed and pinned through the real register / formula contract (`count_rows` with `==` on the `lookup_key` column and a quoted literal works: all six once ⇒ 1; five of six ⇒ 0; six + a duplicate ⇒ 0; a duplicate masking a missing token ⇒ 0 while the plain count reads 6; an extra Optional row ⇒ still 1; empty ⇒ 0). One engine fact found on the way: the equations emitter's legacy eligibility check (`engine-eligibility.ts` `CALL` regex `/([A-Za-z_][A-Za-z0-9_]*)\s*\(/`) reads `AND (` as a call named `AND` and REFUSES a parenthesised clause list inside a formula (`… nicht unterstützte Funktion/Aggregat im Formeltext (AND, AND, AND, AND, AND)`), so the formula form is unparenthesised (`==` binds tighter than AND — same meaning) while the gate text keeps the parentheses; pinned (`MANDATORY_ALL_ONCE` contains no `AND (`). The typed "6" is gone; J-2 now records the exactly-once reading with the ">= 1" alternative; F-2 is marked closed for the mandatory rows.
3. **D-4 / D-10 / D-17 corrected**: each block now states that the twin EXISTS (`mandatory_core_indicators_included_code`; the per-row `pct_linear` column — the cross-X `pct_linear_agg` / `pct_linear_out_agg` twins were drafted and are withheld under J-1), why (amendment J: the typed input is consumed and gate-bearing, so it is never re-bound — twin + STAGED), and that the typed input stays authoritative until ratified; the generic "no second equation for the typed value is emitted" sentence is suppressed for the three TWIN blocks.

## Minor

4. **U-2 → J-9** (judgment, not an unreadable cell): keeps the printed L2280 "⋅1000" (quoted verbatim in the block), the legend L2284 "in %", the seven ·100 siblings (L1929 … L2231) and prod A.8 (`verified_against_standard`, `* 100`, id + md5) as evidence, names the SR-3 PDF-page check as the closing action; ·100 stays encoded. References updated in the field-config module, the quotes-module comment, the report (§3 / §5 / §8 / §10) and the sheet; the STAGED file carries no U-2 any more.
5. **X-3 observation** added (prod hygiene, not this task's write): prod A.8 formula LHS `PECONRE(X)` vs `output_symbol` `pct_ECONRE_X` (the same pattern on A.3 / A.5 / A.6 / A.7 / A.9 / A.10 in the captured formulas); noted in R-1 (the archive keeps the strings as they are) and in §10.
6. **`indicators.unit` free text** — one sentence in §5: Table 3 mixes percentages, dimensionless ratios and a revenue-per-mass ratio, so no single printed unit list exists.

## Raw output (once, before the commit)

```
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts iso59020 → wrote seed + rollback (byte-identical migration; the quotes module changed one comment)
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts iso59020 20260917102110 → wrote 46 field entries + 0 section entries (0 warnings)
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts iso59020 20260917102120 → wrote 38 equations (0 warnings)
pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts iso59020 "<transcript>" → 13/13 quotes verbatim
STAGED conditions read back from the file: 15, parse fails 0; G-4 = 6 parenthesised clauses; sheet + STAGED fragments machine-checked 190/190
```
(the `pnpm vitest` / `pnpm test` / typecheck / eslint / harness lines are in the reply — run once before the commit.) TDD: the five iso59020 files went RED on the removed rows / old counts (as intended), one RED on my own new pin (D5's token digits tripped the "no typed figure" regex — the pin now strips the quoted tokens and the 1 / 0 code), then green. Counts after the round: 1 table / 13 rows, **46** field entries (all create), **38** equations, 51 sign-off blocks (+5 observations), 51 STAGED ☐ blocks (666 lines, 0 non-comment lines).
