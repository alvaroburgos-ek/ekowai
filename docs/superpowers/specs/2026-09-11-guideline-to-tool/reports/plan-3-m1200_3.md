# Plan 3 — DWA-M-1200-3 (`m1200_3`) — Task 6 report

- **Status:** DONE_WITH_CONCERNS (every concern is a sign-off block; nothing blocks Task 7)
- **Model / effort:** Claude Opus 5 (`claude-opus-5[1m]`), effort as dispatched · **CLI:** claude 2.1.260 (no update taken — user declines updates) · **Date:** 2026-09-17
- **Branch:** `feat/guideline-to-tool`, worktree `C:\Users\Ekowai\_wt-g2t`, base `faf0418`
- **Nothing applied to prod.** No `apply-migration`, `drizzle-kit`, `vercel`, or DB write. Prod was read ONLY by `node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-3 m1200_3` (read-only tx) and `node scripts/verification/prod-query.mjs --sql "<SELECT …>"` (`standards.version`, the 32 compliance rows with ids / severities / conditions — the four conditions longer than 120 chars read in 110-char `substr` chunks —, the 10 equation rows with formulas, the five M12003-05 equation rows' metadata + text lengths, the `fields` / `compliance_requirements` / `equations` column lists). `.env.local` never printed; confirmed gitignored.
- **SR-1 source:** every seeded value and every cue was read in this session from `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-3\DWA-M_1200-3_GD.md` (Gelbdruck Juli 2025, 2276 lines, mathpix LaTeX tables); the 100 quote spans were lifted mechanically by line range (a throwaway `gen-quotes.mjs` in the scratchpad, `split('${').join(…)` — never `String.replace`), the seed file assembled by `cat` head + fragment + tail; the inventory was used for pointers only (two of its claims were refuted — §8). The `.pdf` / `.xlsx` beside the transcript were not opened. DWA-M-1200-1's transcript was NOT read (Tab. 10 is cited from this standard's own lines).

## 1. Counts table

| slug | tables | rows lifted | rows unreadable | registers | select_one | select_many | lookup_fill | field visible_when | section visible_when | equations new | equations staged | sign-off entries | tokens in | minutes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `m1200_3` | 10 | 62 (62/62 verbatim) | 0 rows skipped; 1 CELL seeded null (TAB789 t8/d/mit — the empty Tab.-8 cell, U-1); 4 cells encoded from OCR variants of "x⁴⁾" (TAB13 UF / UV Befüllung + Speicherung, U-2); 2 tables kept `imported_unverified` (TAB789, TAB13); Tab. 15 not seeded (row heads are images, U-3) | 6 created + 1 upgraded (`bewaesserungstagebuch` on -18 and -04, 2 UPDATE entries) | 0 | 0 | 7 (all `create`: 3 Tab.-11 (*) limits on -08, 1 Tab.-4 hex on -12, 3 Tab.-14 doses on -22) | 4 (all on created fills) | 8 (M12003-13 13-A/C/D/F/J/K/L/M) | 9 | 0 replacements emitted; 6 gate blocks (G-1…G-6), 2 retirements (R-1, R-2), 1 re-point (E-1), 2 formula-text blocks (F-1, F-2) STAGED | 27 (+10 observations) | ≈ 0.60 M (tool-marker delta 15.00 M → 14.42 M at report time) | ≈ 55 |

Field entries: 24 = 22 `create` (6 registers, 7 lookup_fills, 9 derived outputs) + 2 UPDATE (the Plan-1 `bewaesserungstagebuch` register on M12003-18 and M12003-04). Every existing field the brief's Step 4 names is a consumed producer (transitive guard) or has no driver in scope, so their rules are STAGED (C-1 … C-6); the created fills next to the drivers carry the rules.

## 2. Raw test output

`pnpm test` (after all changes):
```
 Test Files  266 passed | 1 skipped (267)
      Tests  2542 passed | 1 expected fail | 1 skipped (2544)
   Start at  22:17:55
   Duration  40.11s (transform 10.23s, setup 43.47s, import 87.95s, tests 36.67s, environment 171.48s)
```
(2514 at `faf0418` + 28 from this task: seed-m1200_3 10, field-configs-m1200_3 7, equations-m1200_3 8, register-m1200-3-schlaege 3; the shared `regulation-tables-seed-index` / `generated-sql-freshness` / `emit-seed-sql` pins iterate the new slug.) The first full run had ONE failure — `scripts/__tests__/pass3c-validate-field-config.test.ts` line 38 assumed `TAB6` is a unique table code across every seeded standard (standard-less resolution); DWA-M-1200-3's printed Tab. 6 makes it ambiguous (as TAB5 / TAB8 / TAB13 already are between a138 and din1989_1 / m1200_1). The assertion was split into the documented behaviour (an ambiguous code is NOT resolved without a standard — "never a guess", `table TAB6 not registered`) plus the original shape check on a unique multi-key code (`TAB789`); 3 files / 15 passed on the focused re-run, then the full run above.

`pnpm -s typecheck` → exit 0 (`TYPECHECK exit=0`). `pnpm -s eslint <10 touched .ts/.tsx files>` → exit 0, no output; `pnpm -s eslint scripts/__tests__/pass3c-validate-field-config.test.ts` → exit 0.

`pnpm vitest run --project integration tests/harness/m1200-3-verify.integration.test.ts` (embedded PG; no engine change on this task):
```
 Test Files  1 passed (1)
      Tests  12 passed (12)
   Start at  22:18:42
   Duration  5.26s (transform 522ms, setup 0ms, import 3.62s, tests 1.46s, environment 0ms)
```
The harness seeds prod's gates; the new rows are not in it by construction — it proves the branch still enforces every DWA-M-1200-3 gate both ways.

**TDD evidence.** Runtime facts were established BEFORE encoding with a scratch script through the real `prepareRegisterRows` / `evaluateFormula` / `evalCondition` (`scripts/_rt-check-tmp.ts`, deleted): a nested-`if()` lookup key computes (`lookup('T789', g, if(gueteklasse == 'D', 'd', 'a_c'), …)` → 2 / 1), `if(technik IN {…}, …)` parses via `parseNumeric`, an unset boolean cell is `false` (so `steuerbarer_zugang == true` is decidable on hidden cells), a null `derived` cell in a complete row makes `count_rows(reg, abstand_ok == 0)` `manual_required` ("Fehlende Eingabe für count_rows(): abstand_ok"), `AND` with a false left leg is `fail` even when the right symbol is missing, and `null >= 72` is `pending`. Seed test written first → RED (module missing) → after assembly RED on two of my slips (Tab. 5's `\multicolumn` label rows carry ONE cell — value = last cell; the TAB13 multirow labels keep their trailing `$`) → 10/10 GREEN; verifier 62/62 on its first run. Field-config test → RED on my own miscount (24 entries, not 25 — the two Tagebuch UPDATEs are registers, not creates) and the not-yet-emitted migration → emitter accepted all 24 entries + 8 sections on its first run → 7/7 GREEN. Equations test → 8/8 GREEN on the first run (every pinned value held: 50 m for the printed L922 example, 36,5 ha, 1 violation, U-1 → `manual_required`, Tab. 11 3 / 1 exceedances by Pflanzentyp, 2000 m³, 20 mm, 2301 m³, 588,5 / 4098 kWh/a, 127 290 €, 500 m³, the Tab.-13 / Tab.-14 / thermal badges). Render test 3/3 first run.

## 3. Transcript verification

`pnpm -s tsx scripts/regulation-tables/verify-regulation-tables.ts m1200_3 "C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-3\DWA-M_1200-3_GD.md"` → **`62/62 quotes verbatim`**, exit 0. PASS list (table · rows · span):

- TAB3 (3): geschlossen (L662), offen (L663), transport (L664)
- TAB3_MAP (5): offen_ortsfest_kurz / _lang (L637), geschlossen_ortsfest_kurz / _lang (L641), transportbehaelter (L651)
- TAB789 (12): t7|* (L929–L930), t8|* (L944–L945), t9|* (L1047–L1048) — four rows per table share the two-line body
- TAB11 (17): kalium + blei (L1358), natrium + cadmium (L1359), chlorid + chrom (L1360), sulfat + eisen (L1361), nitrat + kupfer (L1362), ph + mangan (L1363), wasserhaerte + nickel (L1364), leitfaehigkeit + quecksilber (L1365), zink (L1366)
- TAB11_SALZ (2): salzempfindlich / salzunempfindlich (L1369 — the (*) note that defines the key; values from L1360 / L1364 / L1365)
- TAB6 (6): PVC_DN125_PN10 … PVC_DN400_PN10 (L843–L848, one line each)
- TAB4 (2): pantone_purple_522c (L727), pantone_purple_512c (L728)
- TAB13 (12): chlorung|* (L1557–L1560), ozonung|* (L1561–L1564), pes|* (L1565–L1568), ultrafiltration|* (L1569–L1572), uv|* (L1573–L1576), h2o2|* (L1577–L1580)
- TAB14 (2): chlorung (L1741), h2o2 (L1742)
- S_TAB5_BEISPIEL (1): normgroesse (L797–L806)

Status per table: `md_verified` — TAB3, TAB3_MAP, TAB11, TAB11_SALZ, TAB6, TAB4, TAB14, S_TAB5_BEISPIEL (every seeded row lifted, every cell legible). `imported_unverified` — TAB789 (Tab. 8 D/mit prints an EMPTY cell, L945 — U-1), TAB13 (the four "x⁴⁾" OCR variants — U-2).

## 4. Prior capture / emit commands (re-executable)

```
node scripts/regulation-tables/build-prior-snapshot.mjs DWA-M-1200-3 m1200_3
  → wrote src\lib\eval\field-configs\m1200_3.prior.json: 208 field rows (104 orphan), 225 coded sections of 225, 10 equations;
    optional columns present: {"fields":{"widget":false,"ui_config":false,"lookup":false,"visible_when":false},"worksheet_sections":{"visible_when":false}}
pnpm -s tsx scripts/regulation-tables/emit-seed-sql.ts m1200_3
  → wrote seed + rollback for m1200_3 -> scripts/migrations/20260917100600_regulation_tables_seed_m1200_3.sql scripts/rollback-20260917100600-regulation-tables-seed-m1200-3.sql
pnpm -s tsx scripts/regulation-tables/emit-field-configs-sql.ts m1200_3 20260917100610
  → wrote 24 field entries + 8 section entries for m1200_3 -> scripts/migrations/20260917100610_field_configs_m1200_3.sql scripts/rollback-20260917100610-field-configs-m1200-3.sql
pnpm -s tsx scripts/regulation-tables/emit-equations-sql.ts m1200_3 20260917100620
  → wrote 9 equations for m1200_3 -> scripts/migrations/20260917100620_equations_m1200_3.sql scripts/rollback-20260917100620-equations-m1200-3.sql
```
All three migrations are byte-pinned against a fresh emit (`generated-sql-freshness.test.ts` auto-pins the seed via `SEED_BUILDERS`; `field-configs-m1200_3.test.ts` / `equations-m1200_3.test.ts` pin the other two against the committed `m1200_3.prior.json`). The seed migration carries 62 row upserts (`ON CONFLICT (table_id, row_key)`) and 8 `verification_status` upgrades (`imported_unverified → md_verified`, never the reverse); its rollback deletes the ten tables (no earlier DWA-M-1200-3 table seed to re-emit). The field-config migration: 22 `INSERT … WHERE NOT EXISTS`, 2 `UPDATE fields` (widget / ui_config / lookup / visible_when on the two Tagebuch rows — the Plan-1 selection-config migration `20260911120000_selection_configs_DWA_M_1200_3.sql` writes the same two rows; applied after it, this one supersedes it), 8 `UPDATE worksheet_sections`; the rollback deletes the 22 `Plan 3:` rows and restores the two Tagebuch rows to the captured prior (all NULL — the Plan-1 columns are unapplied in prod) and the eight sections to NULL. The equations rollback deletes the 9 `Plan 3:` rows. Apply order (owner, after the schema migration incl. the plan1-D-3-1 legacy rename; the Plan-1 selection configs step 3 BEFORE the field configs): `20260917100600` → `20260917100610` → `20260917100620`; rollback in reverse. Every file touches `DWA-M-1200-3` rows only.

## 5. What was encoded

**Tables (10)** — `src/lib/eval/regulation-tables-seed-m1200_3.ts`, builder `m12003SeedTables()`, edition `'2025-07'` (title page L7 "Juli 2025" under "Entwurf" L10 = prod `standards.version` "Gelbdruck (Entwurf), Juli 2025"):

| table | keys | values | policy (cue) | rows | status |
|---|---|---|---|---|---|
| TAB3 | speichersystem (geschlossen / offen / transport — the printed rows) | klassen_zulaessig ("A, B" / "C, D" / "Nach Bedarf"), allows_a…d (letters cover their sub-classes; "Nach Bedarf" → all true, J-2), bedarf_hinweis | anhaltswert (L655 "werden … empfohlen") | 3 | md_verified |
| TAB3_MAP | speichertyp (the five prod tokens) | system (Tab.-3 key) — the CR-06 fix (G-1) | locked (definitional; L655) | 5 | md_verified |
| TAB789 | tabelle (t7/t8/t9) × klasse_group (a_c/d) × spritzschutz (ohne/mit) | faktor 1 / 2 / 3 / null (parsed from the printed "N-fache Wurfweite"; Tab. 7 / 9 `\multirow` → D/mit = 1; Tab. 8 D/mit EMPTY → null, U-1), gedruckt, hinweis | anhaltswert (L926 "Empfohlene Abstandsregelung" — L1040 "muss im RMP erfolgen") | 12 | imported_unverified |
| TAB11 | parameter (17 printed names) | limit_salzempfindlich / limit_salzunempfindlich (equal where no (*)), unit, comparator (<= / range / max_pair — J-3), ph_min 5,0 / ph_max 9,5, gedruckt, salzabhaengig | anhaltswert (L1352 "Toleranzbereiche" — L1376 "fallspezifisch zu ermitteln") | 17 | md_verified |
| TAB11_SALZ | pflanzentyp (prod tokens) | cl_max 250 / 500, haerte_max 30 / 60, lf_max 2000 / 3000 | as TAB11 | 2 | md_verified |
| TAB6 | leitungstyp (prod tokens) | material_eur, einbau_eur, kwh_2bar / _4bar / _6bar (parsed from the printed cells), basis_laenge_m 1000 | anhaltswert (L838 "Schematischer Überblick … Beispiel" — L851 note 1) | 6 | md_verified |
| TAB4 | farbe (the two Pantone prod tokens) | name, websafe_hex, rgb, cmyk, hex | locked (L720 "sind … als Hintergrund zu nutzen") | 2 | md_verified |
| TAB13 | methode (six prod tokens) × speichersystem (geschlossen / offen) | befuellung / speicherung / start / transport (booleans from the printed "x"), umwaelzung_bedingung (note 4 for UF / UV, U-2), gedruckt | locked (L1553 "Mögliche und zulässige Maßnahmen" — L1596) | 12 | imported_unverified |
| TAB14 | methode (chlorung / h2o2) | konzentration 30 / 0,1, konz_unit, konz_text, verweilzeit_min_h 12 / verweilzeit_max_h 24, verweilzeit_text | anhaltswert (L1738 "Empfohlene Desinfektionsmaßnahmen" — L1726) | 2 | md_verified |
| S_TAB5_BEISPIEL | groesse ('normgroesse') | flaeche_ha 10, bewaesserungshoehe_mm 20, gesamtbedarf_m3 2000, speichervolumen_min_m3 2000, pumpenleistung_m3_h 50, dauer_durchgang_h 40, zulauf_m3_h 20, dauer_befuellung_h 100 | anhaltswert (L790 "als Orientierung dienlich") | 1 | md_verified |

Not seeded: Tab. 10 (the printed reproduction of DWA-M-1200-1 Tab. 7 — X-1; the class chain reads `gueteklasse`); Tab. 12 (Systembestandteile — a documentation checklist, `monitoring_programm` json stays); Tab. 15 (row heads are images — U-3; its levels are the `exposition` enum, the §8 pathways the datalist); Tab. 1 / 2 (Abkürzungen; the Tagebuch example → register columns); Tab. A.1 (bibliography).

**Registers / lookup_fills / visibility** — `src/lib/eval/field-configs/m1200_3.ts` (all created in the captured sections `NN-B` / `NN-D`):
- `schlaege` (M12003-06 06-B): Tab.-2 Kopf columns (Schlag-Nr., Feldblock, Fläche ha, Kultur), `pflanzentyp` / `technik` (prod tokens; `technik` is the discriminator), the sprinkler-only columns `sprinkler_gruppe` (t7 / t8 / t9), `wurfweite_m`, `spritzschutz`, `abstand_ist_m` hidden in row scope for non-sprinkler methods, `steuerbarer_zugang` only for Tab. 9; `faktor` badge = `if(steuerbarer_zugang == true, 0, lookup('TAB789', sprinkler_gruppe, if(gueteklasse == 'D', 'd', 'a_c'), if(spritzschutz == true, 'mit', 'ohne'), 'faktor'))`, `min_abstand_m = faktor * wurfweite_m` (Gl-Helper-5 per row), `abstand_ok` (non-sprinkler → 1, voided → 1, else ≥), `weidegang_laktierend` + `laktierend_konflikt` badge (class C, L1282); footer `flaeche_gesamt_ha`, `abstand_verletzungen`.
- `wasseranalysen` (M12003-08 08-B): date, `parameter` (the 17 Tab.-11 names, discriminator), `wert`, `einheit` / `limit_se` / `limit_su` / `ph_min` / `ph_max` (TAB11 lookups per row), `limit` (the three (*) parameters switch on the inherited `pflanzentyp`), `ok` (pH as range, else `wert <= limit`); footer `analysen_verletzungen`. The 17 scalars `k_konz … zn_konz` stay (G-5).
- `speicher_1200_3` (M12003-10 10-B): label, `speichertyp` (prod tokens, discriminator), `speichersystem` (TAB3_MAP), `klassen_zulaessig` (TAB3), `klasse_ok` badge on the inherited `gueteklasse` (not inherited on -10 today — C-7), `volumen_m3`, `o2_saettigung_pct` + `o2_ok` (≥ 50 %, L1622), `verweilzeit_h`, `belueftung` / `umwaelzung` visible from 72 h (L1626 / L1633); footer `speichervolumen_ist_m3`.
- `druckleitungen` (M12003-11 11-B): `leitungstyp` lookup_key TAB6, `laenge_m`, `arbeitsdruck` (2 / 4 / 6 bar), `material` / `einbau` lookup_values, `kwh` by pressure, `kwh_abschnitt` / `kosten_abschnitt_eur` scaled by length / 1000 (F-2), `volumenverlust_pct` + `verlust_ok` (≤ 1 %, L755); footer `energie_kwh_a`, `leitungskosten_eur`.
- `bewaesserungstagebuch` (M12003-18 18-B, UPDATE; and the -04 copy, UPDATE without footer): the Plan-1 keys kept (start / ende text with the printed "TT.MM.JJJJ hh:mm", J-5; empfehlung_mm / real_mm / flaeche_ha / wasser_m3 / aufbereitet_m3 typed number, the Tab.-2 quantities required, kommentar); footer `wasserverbrauch_ist_m3` on -18.
- `desinfektionen` (M12003-22 22-B): date, `methode` (seven prod tokens, discriminator), `speichersystem` + `moment` (Tab. 13, hidden for thermisch), `zulaessig` badge (TAB13 cell per moment, null for thermisch), `umwaelzung_hinweis` (note 4), `konz_empf` / `konz_einheit` (TAB14 text), `konz_ist`, `verweilzeit_h` + `verweilzeit_ok` (≥ 12 h, Tab. 14), `temp_c` / `dauer_min` + `thermisch_ok` (70 °C / 3 min, L1664), `dokumentiert`.
- `expositionspfade` (M12003-24 24-B): Fläche, `technik` (prod tokens), `pfad` (datalist = the six §8 sentences L1812–L1818), `exposition` (the four Tab.-15 levels), Maßnahme — capture-only (CR-13 "tabellarisch dokumentiert").
- lookup_fills: `cl_limit` / `haerte_limit` / `lf_limit` (M12003-08, TAB11_SALZ keyed on the inherited `pflanzentyp`, role limit); `farbcode_hex_tab4` (M12003-12, TAB4 keyed on the own `kennzeichnung_farbe`, text, ← the two Pantone tokens); `chlorung_stoss_konz_tab14` (← `desinfektion_methode == 'chlorung'`), `h2o2_stoss_konz_tab14` (← `== 'h2o2'`), `verweilzeit_min_tab14` (← `IN {'chlorung','h2o2'}`) on M12003-22 next to the existing consumed `chlorung_stoss_konz` / `h2o2_stoss_konz` (E-1).
- Field `visible_when` (4): the four fills above. Section rules (8): M12003-13 13-A / 13-C / 13-D / 13-F / 13-J / 13-K / 13-L / 13-M ← `bewaesserungsverfahren IN {'beregnung_sprinkler', 'mikrosprueh'}` (L906 / L1026); 13-B withheld (C-1). Withheld field rules (C-2 … C-6): the six Karenzzeit fields + `weidegang_laktierend` (-15), `frostschutz_menge` (-17, driver not in scope either), the three hybrid booleans (-12), `chlorung_stoss_konz` / `h2o2_stoss_konz` (-22), `temp_thermisch` / `dauer_thermisch_min` (-20, driver not in scope), `abschaltung_automatisch` (-11); the brief's `kontrollfilter_120 ← gueteklasse == 'A'` was dropped — the transcript prints the control filter for A AND B/C (J-4).

**Equations (9 new)** — `src/lib/eval/equations/m1200_3.ts`: `M12003-06-D1 flaeche_gesamt_ha = sum_rows(schlaege, flaeche_ha)` · `M12003-06-D2 abstand_verletzungen = count_rows(schlaege, abstand_ok == 0)` · `M12003-08-D1 analysen_verletzungen = count_rows(wasseranalysen, ok == 0)` · `M12003-10-D1 speichervolumen_calc = bewaesserungshoehe * flaeche_gesamt_ha * 10` (the printed Tab.-5 arithmetic; `flaeche_gesamt_ha` reaches -10 after C-7; scalar, not materialised) · `M12003-10-D2 bewaesserungshoehe_tab5 = lookup('S_TAB5_BEISPIEL', 'normgroesse', 'bewaesserungshoehe_mm')` (scalar) · `M12003-10-D3 speichervolumen_ist_m3 = sum_rows(speicher_1200_3, volumen_m3)` · `M12003-11-D1 energie_kwh_a = sum_rows(druckleitungen, kwh_abschnitt)` · `M12003-11-D2 leitungskosten_eur = sum_rows(druckleitungen, kosten_abschnitt_eur)` (both F-2) · `M12003-18-D1 wasserverbrauch_ist_m3 = sum_rows(bewaesserungstagebuch, wasser_m3)`. None outputs a prod helper symbol (`speichervolumen`, `dauer_*`, `frostschutz_volumen`, `min_abstand` stay single-sourced — R-1 / R-2). The brief's `M12003-18-D2 schwermetall_fracht_pa` is F-1 (text only). Pinned values (unit test through `evaluateFormula` + `prepareRegisterRows` on the TS fallback tables): the printed L922 example (class D, Regnerkanone 25 m, no shield → factor 2 → 50 m; 40 m → violation), class B → 25 m / the L937 example 5 m, steuerbarer Zugang → 0, Tab. 8 D/mit → `manual_required` (U-1), Σ 36,5 ha; Tab. 11 exceedances 3 (salzempfindlich) / 1 (salzunempfindlich), pH 4,9 → 0, a (*) parameter without `pflanzentyp` → `manual_required`; 20 mm × 10 ha × 10 = 2000 m³; 20; 2301 m³ with the class check A / D / B-2; DN 125 / 2 bar / 500 m → 588,5 kWh/a and DN 400 / 6 bar / 2000 m → 4098, costs 127 290 €; 500 m³ over the printed Tagebuch row + one; the Tab.-13 / Tab.-14 / thermal badges.

## 6. Inventory §5 top-wins walk

1. Repeatable Schlag rows with the Tab. 7/8/9 factor → `min_abstand` per row → **encoded** (`schlaege` + `flaeche_gesamt_ha` / `abstand_verletzungen` on -06; factor per row from TAB789 on the inherited class, steuerbarer Zugang voids it); the Karenzzeit / Emitterabstand auto-fill is NOT encoded (cross-standard Tab. 10 — X-1; the -15 rules C-2); CR-09 G-3; the Gl-Helper-1 re-point R-1 / C-7.
2. Tab. 11 limits switched by `pflanzentyp`, dated sample rows, fracht Σ + BBodSchV/3 → **partially**: the three (*) fills + `wasseranalysen` with per-row limits and `analysen_verletzungen` encoded; the fracht Σ is F-1 (text only, period join missing); CR-05 G-5.
3. Collapse the 58-field M12003-05 mirror → **not this task** (Phase 6 per the brief; the five duplicate helper equations are R-2, the mirror's gates are named in G-1 / G-3 / G-5 / G-6).
4. Tab. 3 / 13 / 14 → allowed classes, allowed moments, dose, CR-06 fixed → **encoded** (`speicher_1200_3` with TAB3_MAP → TAB3 per row; `desinfektionen` with the TAB13 moment cell per row and the TAB14 dose / 12-h minimum; the three Tab.-14 fills); the CR-06 token fix itself is G-1 (enforcement-changing); the G-14 workaround (`lookup` needs a literal column) was solved with nested `if()` over the cell lookups.
5. Tagebuch as typed rows feeding actual water use → **encoded** (`bewaesserungstagebuch` upgraded on -18 / -04, `wasserverbrauch_ist_m3`); the heavy-metal load stays F-1.

## 7. Files changed

Modified: `src/lib/eval/regulation-tables-seed-index.ts` (`m1200_3` line), `src/lib/eval/field-configs/index.ts`, `src/lib/eval/equations/index.ts` (m1200_3 lines), `docs/superpowers/specs/2026-09-11-guideline-to-tool/SIGN-OFF-plan-3.md` (+27 blocks, +10 observations), `docs/superpowers/guideline-to-tool-playbook.md` ("Encoding traps (Plan 3 Task 6)" paragraph), `scripts/__tests__/pass3c-validate-field-config.test.ts` (the TAB6-uniqueness assertion, §2).
Created: `src/lib/eval/regulation-tables-seed-m1200_3.ts`, `src/lib/eval/field-configs/m1200_3.ts`, `src/lib/eval/field-configs/m1200_3.prior.json` (captured), `src/lib/eval/equations/m1200_3.ts`, `src/lib/eval/__tests__/regulation-tables-seed-m1200_3.test.ts`, `src/lib/eval/__tests__/field-configs-m1200_3.test.ts`, `src/lib/eval/__tests__/equations-m1200_3.test.ts`, `src/components/worksheet/__tests__/register-m1200-3-schlaege.test.tsx`, `scripts/verification/m1200_3-STAGED-plan3-rulings.sql`, `scripts/migrations/20260917100600_regulation_tables_seed_m1200_3.sql` + `scripts/rollback-20260917100600-regulation-tables-seed-m1200-3.sql`, `scripts/migrations/20260917100610_field_configs_m1200_3.sql` + `scripts/rollback-20260917100610-field-configs-m1200-3.sql`, `scripts/migrations/20260917100620_equations_m1200_3.sql` + `scripts/rollback-20260917100620-equations-m1200-3.sql`, this report (+ the copy at `.superpowers/sdd/…/task-6-report.md`).
Untouched: every a138 / din1989_1 / a262e / m277e / m1200_1 file; the Plan-1/2 migrations; `SELECTION_CONFIGS` (the TS fallback still serves `bewaesserungstagebuch` while `widget IS NULL`); `scripts/reasoning-map/` (churn from the unit run reverted with `git checkout --` before staging).

## 8. Discrepancies vs the brief (codebase / transcript / reality won)

1. **Prod holds 32 compliance rows and 8 `-2` rows** (brief: 39 / 20 duplicates): 5 exact twins (CR-05-2, -06-2, -07-2, -08-2, -16-2 — the delete set), CR-09-2 a stricter variant, CR-17-2 / -18-2 the only live versions of their code (CR-17 / CR-18 carry an empty condition) (G-6). CR-05 / -06 / -07 / -08 / -09 / -17 / -18 live on the mirror worksheet M12003-05 (all its fields are orphans) — the register-based gate rewrites therefore also MOVE the gate (G-3 → -06, G-5 → -08).
2. **Tab. 8 D/mit is an EMPTY cell** (L945) — the inventory's "(1-fache)" is not printed; seeded null (U-1), TAB789 `imported_unverified`; a Schlag on that combination leaves M12003-06-D2 `manual_required`.
3. **CR-05 lists neither Chlorid, Wasserhärte nor Leitfähigkeit** (the inventory's "CR-05 lists both" is refuted by the 281-char capture) — G-5 appends them via the created fills.
4. **§7.3.2 prints the 120-µm Kontrollfilter for class A AND B-1 … C-2** (L1712 / L1720) — the brief's A-only rule was dropped, nothing created (J-4).
5. **`bewaesserungstagebuch` exists on -18 AND -04** (the Plan-1 migration writes both) → two UPDATE entries, the Σ footer on -18 only; `start` / `ende` stay text (printed "17.06.2023 06:00", J-5) and the Plan-1 column keys are kept (`real_mm`, `aufbereitet_m3` rather than the brief's `gabe_mm`, `davon_aufbereitet_m3`) so stored rows survive.
6. **TAB11 is one table keyed `parameter` with both limit columns** (for the register's per-row lookups) plus **TAB11_SALZ keyed `pflanzentyp`** (for the three fills) — not the brief's `TAB11_<PARAM>` split; **TAB13 is one table keyed methode × speichersystem with four boolean moment columns** — not four `TAB13_<MOMENT>` tables; the G-14 "column name by `if()`" limit is solved by nesting the lookups inside `if()` (pinned).
7. **`desinfektionen` carries its own `speichersystem` / `moment` columns** (the storage type is not inherited on -22) and all seven prod methods (thermisch with its own 70 °C / 3 min check), instead of a TAB14 `lookup_key` limited to chlorung / h2o2; the Tab.-14 doses are shown per row as derived text and, for the scalar path, as three CREATED fills next to the existing consumed inputs (E-1) rather than re-binding them.
8. **`speichervolumen_calc` (M12003-10-D1) stays `manual_required` until C-7** (`flaeche_gesamt_ha` is a created field on -06 and a `create` never sets `consumer_worksheets`); the same edit gives `speicher_1200_3.klasse_ok` its `gueteklasse` on -10. Added beyond the brief: M12003-10-D2 (the Tab.-5 default, so S_TAB5_BEISPIEL has a consumer), M12003-10-D3 (Σ volume), M12003-11-D2 (Σ cost) — 9 equations (brief: 6 + 2 staged).
9. **Zero rules on existing fields** — every Step-4 target is a consumed producer or driver-less on its worksheet (C-1 … C-6; the emitter refusals are pinned by `producerChain` in the field-config test); the four emitted field rules sit on created fills; 8 of 9 M12003-13 sections carry the section rule (13-B → C-1).
10. **Tab. 15 not seeded** (row heads are images, U-3) — `expositionspfade` uses the §8 pathway list as a datalist and the printed levels as an enum. **Tab. 10 not seeded** (X-1, per the brief) — so the §5 win-1 Karenzzeit auto-fill is not encoded.
11. **Edition printed (`'2025-07'`)** → no numbered edition block; sign-off ids 27 (brief ≈ 10) because every refused / withheld / unreadable / refuted item got its own block.
12. **A shared importer test assumed `TAB6` was globally unique** — adjusted to the documented standard-less rule (§2); no other standard's file touched.

## 9. Self-review

- Every `verbatim_quote` passes the verifier (62/62); every span was cut by line range with the generator; every fragment used as a cell (`cells()` / `frag()` / `fache()` / `deNum()`) is a substring of a lifted span, and the tests pin the resulting numbers against the printed lines; every cue in `field-configs/m1200_3.ts`, `equations/m1200_3.ts`, the STAGED file and the 27 sign-off blocks was read from the transcript in this session at the cited line (four off-by-two cites — L912 / L853 / L1832 / L1330 — were caught on re-check and corrected to L914 / L857 / L1836 / L1329 before commit); prod facts (enum tokens, consumer lists, section codes, gate ids / conditions, the equation rows, `standards.version`) come from the in-session capture / queries quoted in §4 and the STAGED header.
- The emitter guards accepted the module first time because the Step-4 rule set had been planned against the capture's `consumer_worksheets` BEFORE emitting (the test pins each withheld target's consumer list, walks every created rule through `producerChain`, and asserts the 13-B refusal message).
- G-A3 pinned: TAB3_MAP keys = prod `speichertyp`; TAB6 = `leitungstyp`; TAB4 = the two Pantone `kennzeichnung_farbe` tokens; TAB13 / TAB14 = `desinfektion_methode` minus the rows the tables do not print; TAB11_SALZ = `pflanzentyp`; the register enum columns carry the prod token lists.
- The runtime scratch check found the one semantic trap before encoding (a null derived cell makes `count_rows` undecidable) and the encoding makes every non-U-1 branch decidable; the U-1 outcome is pinned as `manual_required`, never a silent 0.
- Staged DELETEs (R-2, G-6) archive full rows in-transaction instead of hand-typed INSERT rollbacks — `prod-query.mjs` truncates the 453-char `verification_quote` cells, so a retyped rollback would be lossy (observation on the sheet).
- `git checkout -- scripts/reasoning-map/` run before staging (the unit run churns three files there); `.env.local` confirmed gitignored; the scratch `scripts/_rt-check-tmp.ts` deleted; one commit; author `alvaro.burgos@ekowai.com` verified before committing.
- Reviewed the unified diff of every modified file; the only non-M1200-3 changes are the three registry lines, the sign-off section, the playbook paragraph and the one importer-test assertion.

## 10. Concerns (for the controller)

1. **Nothing the registers compute reaches prod's gates yet** — `abstand_verletzungen`, `analysen_verletzungen`, `klasse_ok`, the 72-h / O2 badges and the Tab.-14 checks are visible derived values; CR-05 / -06 / -09 keep reading the mirror scalars until G-1 / G-3 / G-5 are ratified; CR-06 stays broken (its 'geschlossen' / 'offen' literals never match) until G-1.
2. **Two tables stay `imported_unverified` for printed-cell reasons** (TAB789 U-1, TAB13 U-2): a 5-minute PDF look at p. 30 and p. 50 flips them (and decides the Tab.-8 D/mit cell — the U-1 proposal leaves the value blank, SR-3).
3. **`speichervolumen_calc` and `klasse_ok` are inert until C-7** (the consumer edits `flaeche_gesamt_ha` → -10 / -17 and `gueteklasse` → -10) — the honest fail-safe of "a create never sets consumer_worksheets".
4. **The §5 win-1 Karenzzeit / Emitterabstand auto-fill per Schlag is NOT delivered** — it needs Tab. 10 data, which the brief assigned to DWA-M-1200-1 (Task 5 TAB7_CLASS) and the engine resolves tables per standard (X-1 offers the two options).
5. **Three text-derived encodings rest on the executor's reading** (J-2 "Nach Bedarf" = any class; J-3 the (*) values as upper bounds; F-2 linear scaling of Tab. 6 by length) — all pinned and on the sheet.
6. **Scalar-only equations not materialised** (M12003-10-D1 / -D2) — hook / report / snapshot / PDF only (amendment D).
7. **The Tagebuch UPDATE overwrites the Plan-1 config at apply time on both worksheets** — the rollback restores NULL (the captured prior), not the Plan-1 config; re-running `20260911120000_selection_configs_DWA_M_1200_3.sql` restores that layer (its UPDATE has no `widget IS NULL` guard either).
8. **Bundle growth:** the 100 spans (~25 KB incl. the four-line Tab.-13 bodies and the Tab.-5 / Tab.-2 tables) ship in the client bundle via the seed fallback (Task-0 observation; Task 30 measures).
9. **Token/time:** ≈ 0.60 M tokens in, ≈ 55 min wall-clock — in line with Task 5 for a standard with 7 registers, 7 fills and 9 equations; the runtime scratch check and the mechanical lift paid for themselves again (equations and render tests green first run).

## 11. Residue (what the guideline demands that the worksheets still do not ask)

The Tab.-10 Karenzzeiten and Emitterabstände per Schlag (L1264 / L1265 25 / 50 cm; L1278 / L1279 / L1314 / L1315 / L1318 — X-1 / C-2); the LAWA one-third load rule (L614, F-1); the Spritzschutzhecke geometry ≥ 2 m / ≥ 1 m (L950 — the existing `spritzschutz_hoehe_m` / `spritzschutz_staerke_m` on -14 carry no gate); the C / D wind-drift stop near dwellings (L920); the surface-water distances by § 4a PflSchAnwV / § 5 DüV (L964 — external law, reference only); the 60 % retro-marking share (L712 — CR-07 already reads `markierung_oberflaeche_anteil >= 60`); the Tab.-12 documentation checklist (L1431–L1490, `monitoring_programm` json stays); the Tab.-15 row ↔ pathway matrix (U-3); the ISO 16075-2 residual chlorine 0,2–1 mg/l after 30 min for class A (L1600 — `restchlor_konz` on -22 carries no gate); the §7.3.2 B/C "mehrmalige Spülung" attestation (J-4); the Frostschutz 50 mm / 500 m³ per ha per night (L1396 — `frostschutz_menge` stays an input). Each is listed here with its line and stays outside this encoding (content-boundary rule) or on the sign-off sheet.

---

# Fix round 1 (review verdict on `829dd07`: APPROVED with two Important items and four minors)

- **Commit:** see the reply (one commit, Fable trailer; `git checkout -- scripts/reasoning-map/` before staging; tree clean after). **Nothing applied to prod** — the only new prod read was `SELECT id, md5(formula) … / SELECT id, md5(condition) …` (read-only, the md5 values only).
- **IMPORTANT 1 — duplicate count.** Prod has 8 `-2` rows, not 6: 5 exact twins (CR-05-2, -06-2, -07-2, -08-2, -16-2 — the delete set, unchanged), CR-09-2 the stricter variant, CR-17-2 / CR-18-2 the only live versions of their code (CR-17 / CR-18 carry an EMPTY condition, md5 `d41d8cd9…`). Corrected in report §8.1, the SIGN-OFF G-6 evidence line and observation, the STAGED G-6 block and the `field-configs/m1200_3.ts` header.
- **IMPORTANT 2 — staged DELETE hardening** (`scripts/verification/m1200_3-STAGED-plan3-rulings.sql`, blocks R-2 and G-6): (a) every DELETE is guarded by `md5(formula)` / `md5(condition)` read read-only from prod (five equation md5s, five compliance md5s recorded in the blocks; the archive copy uses the same guards and the DELETE joins the archive on `id` + md5 equality); (b) the rollback re-INSERT names the 22 `equations` / 18 `compliance_requirements` columns explicitly (no `SELECT *`); (c) each block states that `<table>_archive_m1200_3` is dropped by the rollback (the `DROP TABLE` is in the rollback block) or on the owner's sign-off that the deletion is final; the file header carries the same sentence. Playbook: new paragraph "Staged DELETE rollbacks" (archive in the same transaction + content guard + explicit columns + drop rule; m277e's column-exact retyped INSERTs stay acceptable).
- **MINOR 3 — `desinfektionen.konz_empf` raw LaTeX.** TAB14 now carries `konz_text` as de-LaTeXed display text ("30 mg/l" / "0,1 ml/l bzw. 1 l pro 10 m³") produced mechanically from the printed cell by a new `deLatex()` helper (`$` dropped, `mathrm{x}` → x, `~` dropped, `^{3}` → ³) and `konz_gedruckt` with the raw printed cell; the row quote is unchanged (verifier 62/62). Pinned in the seed test (both columns) and the equations test (the register column shows the cleaned text); seed migration `20260917100600` re-emitted (rollback byte-identical).
- **MINOR 4 — TAB3_MAP labels.** The kurz/lang sub-lines are now LIFTED: the two row spans are L637–L639 and L641–L643 (`Q_L637_639`, `Q_L641_643`), and every label fragment ("kurz- bis mittelfristige Speicherung", "langfristige Speicherung") is cut from the row's own span with `frag()`; the seed test asserts each label part is a substring of its row quote. Verifier still 62/62.
- **MINOR 5 — two `pflanzentyp` sources.** Observation added to the Task 6 section of the sign-off sheet: `schlaege.pflanzentyp` (row column) and the inherited scalar `pflanzentyp` (drives the Tab.-11 switch) are independent sources for the same fact; owner option to drop the column or derive the scalar.
- **MINOR 6 — U-1 proposal.** The proposed value is left blank (`faktor = <value read from the PDF>`) in the sign-off block, the STAGED block and report §10.2 — the PDF look decides (SR-3); nothing pre-written.
- Raw: focused `pnpm vitest run --project unit <seed, equations, field-configs, render, generated-sql-freshness>` → 5 files / 33 passed · verifier **62/62** · re-emit: only `20260917100600` changed (field configs / equations byte-identical) · `pnpm test` → 266 files / 2542 passed | 1 expected fail | 1 skipped · `pnpm -s typecheck` exit 0 · `pnpm -s eslint <touched files>` exit 0.
