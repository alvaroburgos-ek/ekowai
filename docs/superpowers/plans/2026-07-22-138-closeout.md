# DWA-A-138-1 Phase-4 CLOSE-OUT Package

**Status:** PLANNING + VERIFICATION ONLY. Nothing merged/deployed/prod-applied by this document.
Branch `feat/138-phase-4-facility-sizing` (worktree `_wt-138-p4`), HEAD `6a7af6e`, base `main aa656a5`.
Identity verified `alvaro.burgos@ekowai.com`. Guideline (DWA-A 138-1) is ground truth; §/Gl/Tab cited.
Verified live 2026-07-22.

---

## 1. Baseline — current shippable 138 state

### DONE (harness/#22/tsc-verified)
- **Defect #22 (A_S_m dual-role on A138-17)** — home-boundary write-back suppression, re-keyed on the reliable `field.inheritedFromWorksheet` signal (Findings A–E arc: the original `inheritedFromBySymbol` signal was empty for the injected inherited A_S_m field → never suppressed → clobber; corrected). Reproduction-grade at BOTH the logic seam (`composeEngineSuppressedSymbols`) and the production wiring (WorksheetForm render). Commits: `a1bda0a` (RED) → `8c8edc0` (GREEN) → `5a1d7e4` (seam+repro) → `e56c86e` (wiring-repro) → `649b983` (tsc fix) → `2fdbe12`/`184bbe2` → **`e3c57eb` (correct signal, the fix that actually works live)** → confirmed live Step-3 re-run (build kzzi38djz): A_S_m=943.4338711204341 derived-fresh, not clobbered.
- **Findings A–H2 fixed:**
  - **A** — `facility_type_dimensioned` now writes the TYPE ('mulde') not the worksheet CODE ('A138-17'). `184bbe2`.
  - **B** — FALSE ALARM (controller query error: read value_number on a boolean field). No bug; harmless regression test kept. VERIFY-QUERY-COLUMN-MATCH standing rule.
  - **C** — NOT A BUG (A_S_m=45 after method-save is the expected asm owner-path pass-through; the Gl.16 sweep's real trigger is an A138-17 save).
  - **E** — root-caused (write-back clobber, not computedSymbols render) + fixed on the correct signal. `e3c57eb`.
  - **F (governing volume not persisted)** — `facilityGoverningVolume` + `facilityVolumeMaterialize`; step-6b persists V_M = A_S_m·h_M derived+scoped (§6.3.2 Speichervolumen). `f6a27e5`.
  - **G1 (summary doesn't re-fire)** — `phase4SummaryChainFires` after asm materialize + `shouldOpenTransaction` incl. `isPhase4SummarySave` (worksheet.ts:558). `e5f8df5`.
  - **G2 (derived enum shows no value)** — `PHASE4_READONLY_SYMBOLS` → computedSymbols; DynamicField enum respects isComputed → locked recommendation w/ `data-readonly-recommendation` marker; editable verdict intact. `3125dd1`.
  - **H (F wrote NULL V_M live)** — client Gl.14 (not displayOnly) wrote V_M=null and clobbered step-6b's server materialize. FIX: Gl.14 (`bfe6e59a`) marked `displayOnly` → client write-back loop skips it → server materialize authoritative. `ce184d0`. **Proven through the REAL saveWorksheet** (embedded-PG harness), revert-verified.
  - **H2 (summary read its own derived meets_qsac as the "explicit override" → verdict flip impossible)** — honor ONLY `source_type='entered'` overrides. `94f1960`, revert-verified.
- **5/6 facilities materialized RED→GREEN through the real save path** (Milestone 1, commits `92ecebd`/`a71fb33`/`6d966f6`/`5c3ecb0`): 16 Fläche (footprint→a138_A_s_dim, REQ-31 real Gl.13), 18 Rigole (V_R=Gl.20 via server s_R Gl.21/22, Gl.19 displayOnly, REQ-32), 19 MRE (V_MR=V_M+V_R scoped sum Gl.26, Gl.28 displayOnly), 21 Schacht (V_S=Gl.36 via h_S-sweep Gl.37, Gl.35 displayOnly, footprint→A_S_Schacht, REQ-33 Typ-B), 22 Becken (V_B=Gl.41 sweep+f_A, Gl.41 displayOnly, vol mapping→V_B). Schacht revert-verified (reverting Gl.35 displayOnly → V_S clobber).
- **REQ-31/32/33 encoded + evaluating** (recoded from the REQ-20/21/22 collision with A138-24/25/26, `7e10a1e`): REQ-31 (A138-16 Gl.13 unconditional), REQ-32 (A138-18 Gl.25, applicability via absent-L_VS→pending), REQ-33 (A138-21 Gl.38, Typ-B enum-gated). All `block`, n:1, cross-WS k_i resolves (makeGateLookup consumer fallback). APPLIED to prod + verified. SQL under `scripts/phase4/`.
- **A138-23 summary layer** — recommendation persisted as 2 new fields `recommended_phase_4_gate` (enum PASS/CONDITIONAL/FAIL, engine-written read-only) + `phase_4_recommendation_reasons` (text); 6 support fields auto-derive; `phase_4_gate_result` stays engineer-entered (D3 precedent). Bundle A APPLIED to prod + verified (`ac8a862`).
- **Embedded-PG harness** — `tests/harness/` runs the REAL `saveWorksheet` against a disposable embedded-postgres@18 (NO prod/Docker/credentials), seeded PLT-HS-01 fixture + client-faithful payload. Closes the false-confidence gap.

### Verifying commits / verified results (this close-out, 2026-07-22)
- unit `pnpm vitest run --project unit`: **1220 passed | 1 expected fail** (128 files).
- integration harness `pnpm vitest run --project integration tests/harness`: **13/13 pass** (8 facility + finding-H + pilot 4).
- tsc by-file: **28** = worksheet-store-derived-apply.test.ts 14 + build-vsme-xlsx.test.ts 10 + pass3c-validate.test.ts 2 + export-route.integration 1 + build-workbook 1. **0 new.**
- #22 guard: **15 pass + 1 expected fail** (reproduction-grade fail-on-old intact).

### OPEN
- **MRS (A138-20)** — genuine ratification block (R1): §6.6.2 "analog zur Bemessung von MRE (6.5.2)" → MRS storage = V_MR (V_M+V_R), NOT the 2026-07-15-ratified V_MUE (Gl.30 = Überlaufvolumen/overflow). A138-20 has NO V_MR field → needs (a) mapping reversal + (b) a V_MR field re-encode (prod apply). BATCHED.
- **Phases 5–6 build (A138-24…28)** — scoped only (`2026-07-22-phases-5-6-scope.md`), no code. Findings S1–S8 → ratification R1–R5.
- **Parked close-out items:** WORKBOOK-SYNC, RETIRE-PDF-138-GATE, Tab.14 freeboard/slope thresholds, #23 benign banner.
- **Prod PLT-HS-01 is RED (V_M null) BY DESIGN** — awaits the user's browser nudge on the alias (fixed build → prod Supabase materializes V_M=283.0302 = the final human GREEN). Autonomous loop is green in the harness against a real DB.

---

## 2. Task-14 close — regression RAW (verified 2026-07-22)

| Step | Command | Result |
|---|---|---|
| Unit | `pnpm vitest run --project unit` | **128 files, 1220 passed \| 1 expected fail** ✓ |
| Integration (harness) | `pnpm vitest run --project integration tests/harness` | **3 files, 13/13 pass** ✓ |
| Integration (full project) | `pnpm vitest run --project integration` | 3 passed + **22 failed (pre-existing DATABASE_URL-gated, UNRELATED)** + 1 skipped |
| tsc by-file | `pnpm tsc --noEmit` | **28** (14+10+2+1+1), **0 new** vs baseline ✓ |
| #22 guard | 3 dual-role test files | **15 pass + 1 expected fail** ✓ |

**Branch is GREEN.** The 22 integration failures are the pre-existing files that hard-require an external `DATABASE_URL` (`_setup-env.ts:14` throws at collection); they are the documented 22, unrelated to Phase-4 work. The harness files self-provide embedded-postgres and pass.

### Deployed alias
- `curl -sL https://ekowai-138-pilot.vercel.app` → 307 /de → **200 /de/login** (live).
- `vercel alias ls`: **`ekowai-138-pilot.vercel.app` → `ekowai-wizard-preview-p2b528tcy-hannesosters-projects.vercel.app`** (aliased ~3h ago).
- **FINDING:** the alias now targets build **`p2b528tcy`**, NOT the ledger-recorded `lyioclcew`. The stable alias was re-pointed to a newer green preview since the last ledger entry (consistent with the "future green builds re-point this same alias" discipline), but the ledger was not updated. Confirm the running build's fingerprint (G2b read-only "Recommended Phase-4 gate" marker) before the user's final browser sign-off. Target=preview; prod aliases untouched.

---

## 3. Merge / deploy plan (Phase 4 + campaign) — SEQUENCE, NOT EXECUTED

Flag: every **[PROD-GO]** step requires the user's explicit GO.

**(a) Branch → main merge.**
- Includes: all code (#22 fix, F/G1/G2, H/H2 fixes, 5/6 facility materializes, phase4-summary aggregator, harness `tests/harness/*`, `vitest.config` integration project, `devDep embedded-postgres`) + the WRITTEN SQL under `scripts/phase4/` (recommendation-fields, compliance-gates, recode-31/32/33, rollbacks/verifies, `_mgmt-apply.mjs`).
- Migration state: the prod field/gate SQL is **already APPLIED to prod** (Bundle A + Bundle B recoded to REQ-31/32/33, both verified). The `scripts/phase4/` files are the applied artifacts kept for audit/rollback — they are NOT drizzle migrations and do NOT re-run on merge. No unapplied migration ships with the merge.
- 45 commits `aa656a5..6a7af6e`; tree clean.

**(b) [PROD-GO] Prod deploy + P2 alias discipline** (`reference_ekowai_wizard_deploy`).
- Single Vercel project `ekowai-wizard-preview`; NO auto-deploy on merge → deploy manually from the main worktree (`vercel --prod`).
- Re-point ALL 3 custom aliases with `vercel alias set` and confirm the source column via `vercel alias ls`. The user's URL is the custom `-hannesoster-` alias which MUST be re-pointed after every prod deploy.
- Source-column confirm + one-tab hard-reload; fingerprint-before-save before any browser action.

**(c) Campaign (encoding/audit) merge/deploy state.**
- The cross-guideline audit/DATA-fix campaign (`regulatory-audit-fix-campaign` skill, vault `encoding-audit-2026-07-01/` + `DATA-FIX-CAMPAIGN.md`) is a SEPARATE workstream (per-standard `compliance_requirements` migrations, gate-enforcement fixes). It ships alongside 138 via its OWN gated per-standard migration apply — it does NOT ride this branch. The 138 Phase-4 gate work (REQ-31/32/33) is the reference-standard hand-correction that the campaign propagates as a pattern (duplicate-code-across-worksheets is now a machine-detectable import-validator class — DEFECT-REGISTER handoff 2026-07-18). No campaign code is in this branch; coordinate the two so a full A138 re-import (campaign or workbook-sync) does not regress the Phase-4 fields.

**(d) [PROD-GO] Written-not-applied / prod-gate items — WORKBOOK-SYNC.**
- Fold into the Pass3c workbooks for source-of-truth parity (before the next full A138 re-import; they currently survive re-import via ON CONFLICT but the workbook lacks them):
  - the 2 A138-23 fields (`recommended_phase_4_gate`, `phase_4_recommendation_reasons`) → A138-23 sheet;
  - REQ-31/32/33 → A138-16/18/21 Compliance_Requirements sheets;
  - the Milestone-1 field-mapping/materialize outcomes (footprint→a138_A_s_dim/A_S_Schacht, vol→V_B, V_R/V_MR/V_S homes).
- MRS V_MR field-add + the Becken f_A/Q_Dr optional field-adds are re-encodes that also fold here once ratified.

---

## 4. Consolidated open-decisions bundle (R1–R5) — the ONE signature to unblock MRS + Phases 5–6

| # | Item | Source | Recommended |
|---|---|---|---|
| **R1** | **MRS storage remap + V_MR re-encode.** Reverse the ratified `mrs→V_MUE` mapping; add a `V_MR` field on A138-20 + materialize V_MR=V_M+V_R (Gl.26 pattern). | §6.6.2 (line 2023) "…analog zur Bemessung von Mulden-Rigolen-Elementen (siehe 6.5.2)"; Gl.30 V_MÜ = Überlaufvolumen (overflow), not storage. | **APPROVE both** — current mapping provably wrong vs source (correctness, not preference). Prod field add [PROD-GO]. |
| **R2** | **A138-25 recommended verdict + reasons (D3 parity).** Attach recommended `design_adequacy_result` + reasons (Gl.9 re-check); engineer confirms an explained recommendation. | Gl.9 §5.3.3.7 (final q_S,AC ≥ 2); Phase-4 D3 precedent. | **APPROVE, PERSISTED** (2 new A138-25 fields, prod re-encode) — a compliance recommendation belongs on the PDF/audit trail. |
| **R2b** | **REQ-21 relax to `IN {PASS, NA}`.** Current `design_adequacy_result == 'PASS'` fails a legit NA (drossel-only/Nachweisverfahren). | Gl.9; enum has NA; Phase-4 REQ-21 (Gl.25) + flood REQ-23 use applicability-conditioned `IN {…,NA}`. | **Relax to `IN {PASS, NA}`** IF NA is a real outcome here; else keep `=='PASS'` and drop NA from the enum. **Ratify which.** Prod UPDATE [PROD-GO]. |
| **R3** | **A138-28 all-gates-SET final predicate.** Replace bare REQ-30 (`final_compliance_verdict IS NOT NULL`) with a conjunction requiring verdict + P2/P3/P4 gates + P5 adequacy + flood proof all resolved-positive; wire the 4 `*_phase_complete` booleans to DERIVE from the real gate enums. | Gated-phase architecture REQ-09/16/19; §7.4 capstone. S7+S8 (user-flagged). | **APPROVE the conjunction.** NO separate `phase_5_gate_result` field — fold P5 into the conjunction (adequacy+flood carry PASS/FAIL/NA). Split into per-symbol block rows if chained-AND grammar unsupported (verify at build). Prod UPDATE [PROD-GO]. |
| **R4** | **REQ-26 source_quote fix.** Stored quote is the Gl.10 D-iteration Anmerkung, not the approvals/Section-L clause it titles. | REQ-26 title "Approvals collected per Section L" / §5.3.4.1; quote↔title↔clause mismatch (S5, encoding bug). | **Correct the source_quote** to the true clause; if no approvals clause at 5.3.4.1, re-point to §7.4 Dokumentation (line 2315). **Ratify the target clause.** Prod UPDATE [PROD-GO]. |
| **R5** | **A138-26 field-home confirms + Becken optional field-adds.** Confirm `A_VA` (Gl.10 input) + `flood_check_trigger` (AC>800 producer) inherit/derive; Becken `f_A`/`Q_Dr` optional field-adds for reduced-drain projects. | Gl.10 §5.3.4 (S3/S4); §6.8.2 "gegebenenfalls" (f_A/Q_Dr optional). | **Confirm inherit-resolution first**; escalate to re-encode only if unresolved (Milestone-1 in-layer-vs-re-encode discipline). Becken field-adds only for reduced-f_A/drain projects. |

---

## 5. What "138 DONE" means (measurable, all phases) + remaining path

**Measurable definition:**
1. All 7 facilities' primary sizing outputs persist `derived`/not-stale through the real save path; cross-WS consumers resolve (5/6 done; **MRS pending R1**).
2. #22 closed; A138-23 produces the recommended gate + REQ-19 enforces; the 3 facility gates (REQ-31/32/33) enforce. **DONE.**
3. Phases 5–6 (A138-24…28) built: A138-26 Gl.10 V_Rück server-materialize+displayOnly; A138-24/25 final-value inherit + optional recommended verdicts; A138-28 derived phase-gate roll-up + ratified final predicate. **OPEN.**
4. Full-guideline regression green (unit + harness integration + by-file tsc baseline + #22 guard) on the deployed build; 138 Phase-3 baseline re-derives byte-identical (A_S_m=45, ac_as_ratio=107.48/fail, V_VA=293.169549312).
5. Prod browser sign-off on the alias (PLT-HS-01 RED→GREEN, V_M=283.0302); all 3 aliases re-pointed + source-confirmed.

**Remaining path:** ratify the R1–R5 bundle (§4) → build Phases 5–6 + MRS (in-layer where possible, gated prod applies for R1/R2/R2b/R3/R4 field/condition changes) → full-guideline regression → [PROD-GO] merge + deploy + alias re-point + workbook-sync → user browser GREEN → ship.
