# A138-21 — Schacht-/Rohrversickerung Bemessung

**Source:** `DWA-A_138-1_WD (5).md` · audited_by `claude-code-2026-05-29`

## Summary

**20 rows audited — 20 match, 0 mismatch, 0 not_found.**

Zweitgrößte equation-WS nach A138-18, alle Gleichungen verbatim §6.7.2.

## Equations §6.7.2 (7 verbatim match)

| Gl. | DB | Befund |
|---|---|---|
| (34) | `A_S = pi * d_a^2 / 4 + pi * d_a * h_S / 2` | match |
| (35) | `V_S = (A_C * 10^-7 * r_D(n) - A_S * k_i) * D * 60 * f_Z` | match |
| (36) | `V_S = pi * d_i^2 / 4 * h_S` | match |
| (37) | `h_S = (Zähler)/(Nenner)` | match |
| (38) | `A_S_FS * k_f_FS >= A_S_Schacht * k_i` | match (≥ cap erhalten) |
| (39) | `erf_k_f_FS >= ((d_a^2 + 2 * h_S * d_a) / d_i^2) * k_i` | match (≥ cap + 10⁻³-Schwelle) |
| (40) | `h_S = (...) * 4 * D * 60 * f_Z / (d_i^2 * pi)` | match |

## Fields — alle 13 match

**Wichtig: d_S_aussen / d_S_innen (m) match against §6.7.2 Z.2110/2142.** Diese WS unterscheidet sich von A138-18 (Rigole), wo Tab. 2 (universal) `d_a / d_i mm` für Versickerrohre vorgibt. Für Versickerungsschacht definiert §6.7.2 **explizit `d_a / d_i m`** — Source-internal context-spezifische Unit-Differenzierung.

Andere matches: A_S_FS, A_S_Schacht, h_S, k_f_FS, k_i_FS, r_D_n_S, V_S, sowie schacht_d_i_check (DN 1000), schacht_filter_thickness/_check, shaft_type.

## Tally update

WS 1–21: **242 / 343 (71 %)**.
