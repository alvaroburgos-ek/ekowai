# Wave 0 triage — DIN-14071-1

**Standard.** DIN CEN ISO/TS 14071 (DIN SPEC 35803):2016-06 — Umweltmanagement — Ökobilanz —
Prozesse der Kritischen Prüfung und Kompetenzen der Prüfer: Zusätzliche Anforderungen und Anleitungen
zu ISO 14044:2006 (ISO/TS 14071:2014). Prod standard_id `2eb3e188-d202-4c98-be1b-c90bf4eed3e0`.
Encoding: 4 worksheets, 29 sections, **0 equations, 0 tables**, 17 CRs, 60 fields.
PDF: `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-14071-1\DIN-CEN-ISO-14071-1.pdf`
(pdfStatus=text, scoop pdftotext -layout, 1167 lines). Page footer = printed integer (own footer,
not inherited); §4.1 p.9, §4.2 p.11, §4.4 p.14-15, §4.5 p.15-17, §5 p.20, Anhang A p.21.

**Archetype.** Purest `engineer_input` / process-verification standard in the library — beyond
ISO-14067. States shall-do process obligations only; no numeric value, formula, or table. All 60
fields are engineer_input (booleans/text/3 closed enums). VA lives on the CR *clause quote*, never on
a field's numeric source. Structural analog: ISO-14067.

## Node census (35 typed nodes; 57 incl. 60 fields rolled into sections, ISO-14067 convention)
- section 4 · equation 0 · table 0 · CR 17 · document 6 · decision-point 8
- provenance: **VA 13 · VC 18 · NR 4 · EV 0** → **belowVA (VC+NR+EV) = 22**
- data_class: engineer_input 60 (all fields); standard_range 3 (review_type, review_timing,
  conformance_result — closed enums with values → not F-7); standard_fixed/derived/normative-as-input-reference 0.

## Findings by defect class
### dead gate — constant-TRUE condition ×3  (NEW sub-class)
- **CX-01, CX-02, CX-03**: `condition = TRUE`, severity `block`. Context markers (adds-to-ISO-14044;
  applies-to-14045/14025/14067; comparative-assertion path) wired as gates that can never fail. No
  `fired_by`, no reachable-fail. Distinct from ISO-14067 boolean-tautology (`IN {true,false}`) and
  138 phantom-field. → [[dp-din14071-cx-context-gates]].

### dead gate — tautological enum IN-set ×1
- **REQ-12** conjunct `conformance_result IN {conformant,non_conformant}`: enum's only two values are
  exactly those → always true → dead conjunct. → [[dp-din14071-req12-tautology]].

### weak/no-op gate — string-emptiness on required text ×2
- **REQ-12** `review_process_description != ''` and **REQ-13** `reviewer_signatures != ''`: non-blank
  tests on `is_required=true` text fields; duplicate field-level required-ness, add no criterion.
  REQ-13 also mis-models §4.5 (chairperson MUST sign / others SHOULD) — should re-point to existing
  `chairperson_signs_statement` boolean. → [[dp-din14071-emptiness-gates]].
  (Not counted as "dead" — they DO fire on blank, just redundantly.)

### cross-worksheet gate — condition symbol not owned by gated worksheet ×1
- **REQ-04** on **DIN-14071-1-01** reads `not_involved_in_study` + `no_vested_interest`, which are
  fields on **DIN-14071-1-03** (Anhang-B). Worksheet-local evaluator can't resolve → gate can't fire
  on them (`self_declaration_submitted`, the 3rd conjunct, IS on WT-01). First non-FLL instance of the
  FLL cross-sheet class. NOTE also REQ-09 reads `includes_data_sets` which originates on WT-01 (declared
  consumer WT-02) — confirm whether the evaluator does project-wide resolution before "fixing" either.
  → [[dp-din14071-req04-crosssheet]].

### doc-not-cited-in-source ×1  (NEW provenance finding)
- **CX-03** asserts a "DIN EN ISO 14021" comparative-assertion path, but **ISO 14021 appears nowhere
  in this PDF** — the Nationales Vorwort mapping + Literaturhinweise (p.2) list only
  14025/14040/14044/14045/ISO-TS-14067. The rule is real but belongs to ISO 14044 §6.3, not ISO 14021.
  Caps NR. (`DIN-14021` exists in prod but is a different-domain standard, not ISO 14021.) →
  [[dp-din14071-cx03-14021]].

### informative-annex ceiling ×2
- Anhang A (Formblatt, "(informativ)", p.21 → comment_type ge/ed/te) and Anhang B (Selbsterklärung,
  "Beispiel"/example → not_involved/no_vested/not_employed). Field values VC, never forced VA; the
  self-declaration *shall* (REQ-04/REQ-11) stays VA. → [[dp-din14071-annex-informativity]].

### #22 hand-enterable-derived ×0
- Structurally absent: no equations, no derived fields. Recorded so re-audit doesn't hunt for it.

### greedy-AND / inequality-as-producer
- REQ-02 (5-way AND) and REQ-11 (8-way AND) are NOT greedy-AND defects: each conjunct maps 1:1 to a
  printed §4.1 / §5 bullet; the standard enumerates them conjunctively.
- No `<`/`>`/`>=` inequality gates and no verdict-as-producer beyond the emptiness tests above.

## below-VA node list (22)
VC (18): REQ-02, REQ-04, REQ-10, REQ-12, REQ-13, CX-01, CX-02, section-04, doc-iso-14044,
doc-iso-14045, doc-iso-14025, doc-iso-ts-14067, dp-cx-context-gates, dp-req12-tautology,
dp-emptiness-gates, dp-req04-crosssheet, dp-annex-informativity, dp-iso14044-deferral.
NR (4): CX-03, doc-iso-14040, doc-iso-14021, dp-cx03-14021.

## dead gates (4)
CX-01, CX-02, CX-03 (constant-TRUE) + REQ-12 tautological-enum conjunct.
(REQ-12/REQ-13 emptiness conjuncts are weak, not dead.)

## missing-doc / cross-read dependencies
- **ISO 14044 (DIN-EN-ISO-14044) — IN LIBRARY.** The dominant dependency (five objectives, conformance
  verdict, non-conformance basis all defer to ISO 14044 §6.1). in_library:true → invariant #7 does NOT
  apply. Criterion nodes cap **VC**, VA-liftable by quoting ISO-14044 §6.1 verbatim in a follow-up
  cross-read (SR-1/SR-3). **Not an acquisition block.**
- **ISO 14040 — out of library (NR).** §4.1 review-limits + §5 competency.
- **ISO 14045 / ISO 14025 / ISO/TS 14067 — referenced-only context (VC), out of library** (national
  foreword mapping only; ISO/TS 14067 is separately encoded but not linked as a value source here).
- **ISO 14021 — out of library AND not cited in this source (NR)** — CX-03 finding.

## Tier: **fix-first**
Not acquisition-blocked (dominant dep ISO-14044 is in-library; only ISO-14040/14021 are out-of-library
and neither carries a value the worksheets numerically compute). Real gate defects must be fixed
before harness: 3 constant-TRUE dead gates (CX-01/02/03), 1 tautological-enum conjunct (REQ-12), 2
emptiness gates (REQ-12/13), 1 cross-sheet gate (REQ-04), 1 doc-not-cited misattribution (CX-03).
Once the CX-context gates are demoted/re-predicated and the emptiness/cross-sheet gates repaired, the
process CRs (REQ-01/03/05/06/07/08/09/11/14) are VA-reachable and harness-ready.

## Provenance note
All VA quotes verified in-session against the rendered PDF (printed pages cited per node). Read-only:
no prod writes, no fixes, no harness runs. ISO-14044 in-library status confirmed via prod
`standards` query (code `DIN-EN-ISO-14044`).
