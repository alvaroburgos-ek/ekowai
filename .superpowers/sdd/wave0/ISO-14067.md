# Wave-0 triage — ISO-14067 (UNE-EN ISO 14067:2019 / ISO 14067:2018)

Standard id `d0e21c0f-7c5f-47b3-b1cf-fcfd7adc9b5a`. Map:
`Obsidian/.../reasoning-maps/ISO-14067/` (60 files: _index + 59 nodes).
pdfStatus = **text** (`iso-14067-2019-af-peru.pdf`, scoop pdftotext -layout, 2727 lines).
Page convention: this PDF's own footer prints `- NN -` = the printed page (ToC numbers match);
derived from THIS pdf, not inherited. VA nodes cite verbatim quote + printed page.

Prod holds **7 worksheets, 43 sections, 75 fields, 7 equations, 30 CRs** (read-only, no writes).

## Node grades (94 counting field roll-ups)
- VA 46 · VC 26 · NR 22 · EV 0
- belowVA (VC+NR+EV) = **48**
- data_class: engineer_input 60 · standard_fixed/normative-as-input-reference 12 · derived 8 · standard_range 2
- Discrete node files: section 7 · equation 7 · table 1 · CR 30 · document 6 · decision-point 8 = 59 (+index).

## FINDINGS by defect class

### dead-gate (tautological boolean `IN {true,false}`) ×4  [FIX-FIRST]
CR-02 (rcp_pcr_used §6.2), CR-10 (use_stage_included §6.3.7), CR-11 (eol_stage_included §6.3.8),
CR-14 (timing_emissions_over_10y §6.4.8). A boolean is always in {true,false} → predicate never
false → no reachable-fail. Intended semantics = "answered/documented" (IS NOT NULL) or warn.
CR-10/11/14 fields are also is_required=false. → [[dp-iso-tautology-gates]].

### inequality/verdict-as-producer on derived field + type-mismatch ×1  [FIX-FIRST]
CR-20 `cfp_result != ''` (§6.5.1, block): (a) gate on the DERIVED output of EQ-01; (b) string-
empty compare `!= ''` on a `number` field. Fix = `cfp_result IS NOT NULL` + enforce inputs
upstream. → [[dp-iso-cr20-derived-gate]].

### #22 hand-enterable-derived ×8  [FIX-FIRST]
Every equation output is data_type=number + UI-enterable yet equation-produced: cfp_result
(EQ-01, required — the material one), E_M, E_M_D2..E_M_D6 (Anexo D). Mark computed/read-only.
→ [[dp-iso-eq-outputs-enterable]].

### informative-normativity ceiling ×6 (VC) + dead-end-derived ×5
Anexo D "(informativo)": EQ-02..EQ-07 (fórmulas D.1–D.6) capped VC, never forced VA. D.2–D.6
outputs (E_M_D2..E_M_D6) have no consumer_worksheets → computed-but-unread. → [[dp-iso-annexd-informative]].

### missing-doc value dependency ×3 chains  [ACQUISITION residue, honest]
- IPCC GWP100 → gwp_100 / gwp_other_horizon / cfp_result / CR-20 = NR (values NOT in ISO 14067,
  §6.5.1 "proporcionado por el IPCC"). [[doc-ipcc-gwp]] / [[dp-iso-gwp-ipcc-source]].
- ISO/TS 14071 → critical_review / CR-28 = NR (§8 defers). [[doc-iso-ts-14071]].
- ISO 14026 → intended_communication = NR/VC (§6.3.1 out of scope). [[doc-iso-14026]].
Other referenced-not-gated docs (VC/informational): ISO 14044, ISO/TS 14027, ISO 14064-1/-3.

### annex-normativity ruling ×1
CR-25 (cfp_limitations_documented, §A.1) is block but Anexo A is *informativo*; Anexo B/C are
*normativo* (CR-26/27 block correct). → [[dp-iso-annex-normativity]].

### block-vs-optional-field tension (minor)
CR-17 (electricity_treatment, block) / CR-26 (comparison_performed, block) / CR-27 gate optional
(is_required=false) fields — a blank leaves the gate untested. Low priority. → [[dp-iso-electricity-hierarchy]].

### greedy-AND — checked, NONE defective
CR-03 (3-field) and CR-15 (2-field) NOT-NULL ANDs both gate fields that are all is_required=true
→ correct greedy-AND, not a finding.

### F-4 var-vs-var non-enforcement — NONE (no var-vs-var conditions in this standard).
### phantom-field gate — NONE (every CR condition symbol is a field owned by its own worksheet).

### meta: stale encode-report drift (not a live map defect)
`_encode_report.md` claims 2 equations + "NR=0". Prod actually holds **7 equations** (6 Anexo D
formulas D.1–D.6 added post-report) and this map grades the IPCC/14071/14026 chains NR. Recorded
so a report re-audit doesn't re-flag the map.

## BELOW-VA LIST (the 48; grouped)
- **NR value chain (IPCC), 5:** gwp_100, gwp_other_horizon, cfp_result, eq-iso-01-cfp (value), cr-iso-20.
- **NR (ISO/TS 14071), 2:** critical_review, cr-iso-28.
- **NR/VC (ISO 14026 / 14027 / 14064 / 14044 out-of-library), 4 docs + intended_communication + rcp_pcr_reference.**
- **VC informative Anexo D, 11:** eq-iso-02..07 (6) + E_M_D2..D6 dead-end outputs (5).
- **VC normativity-pending:** cr-iso-25 (Anexo A block-on-informative), cr-iso-26 (Anexo B via 14027).
- **VC cross-ref CR-02** (rcp via 14027) — also a dead gate.
- Remaining below-VA are the field roll-ups inheriting the above caps (Anexo-D input symbols
  E_V/E_EoL/R/A/C/E_PP as VC informative-annex inputs).
(All section/CR *text* nodes not in the above are VA with printed-page refs; the belowVA set is
entirely the informative-annex block + the out-of-library value dependencies.)

## DEAD GATES (4)
cr-iso-02, cr-iso-10, cr-iso-11, cr-iso-14 — all tautological boolean `IN {true,false}`.
(CR-20 is a *defective* gate but NOT dead — it can fail on empty; counted under inequality class.)

## MISSING-DOC DEPENDENCIES (out-of-library, in_library:false)
IPCC (GWP100 — flagship calc), ISO/TS 14071 (critical review), ISO 14026 (communication),
ISO 14044, ISO/TS 14027, ISO 14064-1/-3.

## TIER = fix-first
Rationale: the dead gates (×4) and the CR-20 mistyped-derived gate are concrete, in-standard
fixes that should precede a harness run. The IPCC/14071/14026 NR caps are **honest acquisition
residue** on *value provenance*, not gate topology — the gates still evaluate; only the CFP
numeric value is source-capped (and IPCC is inherently external to every ISO-14067 study, so it
does not "block acquisition" of this standard). Body §5/§6/§7 gates are otherwise clean VA
booleans/NOT-NULLs with printed-page refs. Not acquisition-blocked (no sibling regulation gates
the structure); not harness-ready (4 dead gates + CR-20 + #22 enterable outputs to fix first).
