# Wave-0 triage — ISO-59010

**Standard:** ISO-59010 — Kreislaufwirtschaft — Anleitung zum Uebergang von Geschaeftsmodellen und
Wertschoepfungsnetzwerken (ISO 59010:2024, Circular economy — Guidance on the transition of business
models and value networks)
**Prod standard_id:** `3e8454b3-7da3-490a-bd81-83f5b89ac42d` (`vadsmshzebefjreqcicl`, read-only)
**Reasoning map:** `01-Projects/ekowai-wizard/reasoning-maps/ISO-59010/`
**PDF status:** MISSING (no `*5901*` in the ISO folder or the Sweep drop, verified 2026-07-24)
**Date:** 2026-07-24

## Encoding shape
worksheet_templates ×9 · worksheet_sections ×79 · fields ×51 · **equations ×0** ·
compliance_requirements ×23. All 23 CRs are `severity='warn'` (0 block). No CR carries a
`source_quote`. Grammar used: `==`, `IS NOT NULL`, `AND`, truthy — all SUPPORTED by evaluate.ts
(NO `eq`-operator dead gates, unlike sibling ISO-59004).

## Node counts (map)
14 nodes total: 9 section + 3 decision-point + 2 document (0 equation, 0 table, CRs enumerated
inside section nodes per ISO-59004/59020 precedent).
Provenance: EV 12 · VC 0 · VA 0 · NR 2. data_class: engineer_input 9 · derived 3 · standard_fixed 2.

## Findings by defect class
### phantom-field / cross-sheet gates (10) — DEAD
Gate lives on a worksheet that does NOT own the field it reads; evaluation is worksheet-local
→ symbol never resolves → never fires:
- CR-002 (ws-01) reads `goal_measurable` → ws-02
- CR-003 (ws-01) reads `enablers_identified` → ws-02
- CR-006 (ws-03) reads `circularity_kpi_selected`, `circularity_performance_assessed` → ws-04
- CR-007 (ws-03) reads `indicator_category` → ws-04  (ALSO missing-doc dep, see below → NR)
- CR-011 (ws-05) reads `gaps_opportunities_identified` → ws-06
- CR-012 (ws-05) reads `circular_economy_strategy`, `circular_economy_principle` → ws-06
- CR-013 (ws-05) reads `economic_rationalization_done` → ws-06
- CR-017 (ws-07) reads `shared_objectives_defined`, `consistency_verification_done` → ws-08
- CR-019 (ws-07) reads `shared_infrastructure` → ws-08
Pattern = CR row placed one worksheet BEFORE the fields it checks (Pass3c placement offset). Fix
(DATA-track, no PDF needed): relocate each CR to the owning worksheet. → dp-iso59010-phantom-field-gates

### manual dead gates (2) — DEAD
- CR-018 (§7.3, ws-07) `condition='manual'`, requires_attestation=false → silent no-op
- CR-021 (§8.1, ws-09) `condition='manual'`, requires_attestation=false → silent no-op
→ dp-iso59010-manual-nonfiring

### greedy-AND (7, compounded)
CR-001 (3× IS NOT NULL), CR-005 (2), CR-006 (2), CR-009 (3), CR-012 (2), CR-017 (2), CR-020 (2).
Any one missing input silently fails the whole gate; for the phantom ones (CR-006/012/017) this
compounds with the cross-sheet miss. CR-009's three ANDed fields are all `is_required=false`.

### all-warn non-enforcing surface (23)
Every CR `warn`; 0 `block`. No blocking gate anywhere in the standard. Defensible for a guidance
standard but ratify. → dp-iso59010-all-warn-nonenforcing

### missing-doc dependency (1)
CR-007 mandates "Use the ISO 59020 circularity indicator taxonomy" but encodes no `references::`
edge and the `indicator_category` enum domain is unreconciled. Sibling ISO-59020 map exists but its
PDF is also missing → governing taxonomy unreadable → CR-007 caps **NR**.
→ doc-iso-59020-taxonomy-ref (in_library:false)

### NOT present
No eq/inequality/enum/verdict-as-producer defects; no #22 hand-enterable-derived (0 equations, 0
derived value fields); no F-4 var-vs-var non-enforcement.

## Below-VA list (all 14 nodes)
Every node is below VA — the ISO 59010:2024 PDF is absent, so no `source_page` and no VA anywhere.
- 12 EV: section-01..09 (9), dp-phantom, dp-manual, dp-all-warn
- 2 NR: doc-iso-59010-pdf-missing, doc-iso-59020-taxonomy-ref
No VC node exists (no CR `source_quote` to promote).

## Dead gates (12)
10 phantom-field (CR-002,003,006,007,011,012,013,017,019) + 2 manual (CR-018,021).
11 CRs parse and are worksheet-local so CAN fire (CR-001,004,005,008,009,010,014,015,016,020,022,023
minus the phantom/manual = the local ones) — but all `warn`, so none blocks.

## Missing-doc deps
1. ISO 59010:2024 PDF (standard-wide acquisition cap) — doc-iso-59010-pdf-missing, in_library:false
2. ISO 59020 circularity indicator taxonomy (CR-007) — doc-iso-59020-taxonomy-ref, in_library:false

## Tier: acquisition-blocked
All 14 nodes below VA; standard-wide cap = missing ISO 59010:2024 PDF (+ CR-007's ISO 59020 taxonomy
also missing). Consistent with the PDF-missing ISO convention (cf. ISO-59004, ISO-59014, ISO-59020,
ISO-46001). Real fix-first DATA-track debt exists and is fixable WITHOUT the PDF (10 phantom-field
relocations + 2 manual gates to encode/flag), to be batched to Alvaro regardless — but the
harness-blocking driver is acquisition of the PDF(s).
