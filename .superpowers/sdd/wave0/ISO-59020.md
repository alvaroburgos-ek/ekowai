# Wave-0 triage — ISO-59020 (Kreislaufwirtschaft — Messung und Bewertung der Zirkularitaetsleistung)

- **Standard id:** `32989676-fe2a-47a3-8669-c51d893dbd2d`  (prod `vadsmshzebefjreqcicl`, READ-ONLY)
- **Version:** ISO 59020:2024 (First edition 2024-05)
- **PDF status:** **MISSING** — not in `Circular economy, sustainability and water test\` nor the Sweep drop.
- **Map:** `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-59020\` (+ `_index.md`)
- **Nodes:** 61 (9 section, 13 equation, 37 CR, 1 document, 1 decision-point)
- **Provenance:** VA 0 · VC 13 · EV 47 · NR 1 · **belowVA 61/61**
- **Tier:** **acquisition-blocked** (gated on the standard's own rendered PDF, `in_library:false`)

## Encoding shape (from prod)
- 9 worksheets ISO-59020-01..09 across phases 1→5 (registration / boundary / indicator / 3× calculation / data-acq / assessment).
- Sections are generic A–M scaffolds with NULL titles (no printed section structure encoded).
- 80 fields; all `imported_unverified`. 12 enum fields, all with populated `enum_values` (no phantom-enum-null fields).
- 13 equations (Annex A.1–A.13), all `imported_unverified`, `source_file`/`source_quote` NULL.
- 37 CRs; `source_quote` NULL on every one.

## Findings list (by defect class)
1. **missing-doc dependency (standard-wide, SR-3):** the ISO 59020:2024 PDF is out-of-library.
   Verified absent by directory search. → no VA possible; every value/equation node VC-capped,
   clause/threshold claims EV, no `source_page` derivable. `doc-iso-59020-pdf-missing` (NR).
2. **dead gate — `manual` condition (non-parsing, no fired_by, never auto-fails):** 4 →
   CR-008, CR-010, CR-036, CR-037.
3. **cross-sheet gate → dead-gate risk:** 15 → CR-011..CR-025 sit on ws-04 but reference symbols
   owned by ws-05 (`pct_REUI_X`,`mREUI_X`,`mTI_X`,…), ws-06 (`pct_REUO_X`,`mTO_X`,…) and ws-07
   (`pct_ECONRE_X`,`VCIW`,`VAIW`,`RWRR`,`RMP`,`IRII`,…). If `evaluate.ts` binds symbols
   worksheet-locally (per reference_wizard_compliance_gates) these never reach a fired-fail on ws-04.
   → `dp-iso59020-crosssheet-eqmirror-gates` (needs a harness run to confirm — Wave-0 forbids).
4. **inequality/verdict-as-producer (float `==` re-expressing an equation as a gate):** 15 →
   CR-011..CR-025, each `output == formula` equality on computed floats.
5. **#22 hand-enterable-derived:** 13 → every Annex-A output field is a hand-enterable required
   `number` (pct_REUI_X, pct_RECI_X, pct_RENI_X, RLP_X, pct_REUO_X, pct_RECO_X, pct_RENO_X,
   pct_ECONRE_X, pct_CWW, pct_CDW, RWRR, RMP, IRII) although derived by its equation.
6. **greedy-AND:** CR-007 (5-way), CR-018 (3-way), CR-027, CR-028 (2-way); plus balance-folded
   sum-to-100 in CR-014 / CR-019.
7. **verdict-as-producer self-attestation booleans (no computed backing):** ~16 incl. BLOCK gates
   CR-005, CR-006, CR-009, CR-031.
8. **clause-ref mismatch (CR clause ≠ backing field clause):** CR-005 (§4.2.3 vs §6.3),
   CR-009 (§5.4 vs §7.3), CR-027 (§7.6.1.5 vs §7.6.1.6).
9. **duplicate clause / redundant gate:** CR-018 & CR-019 both §A.3.1; CR-029 duplicates the
   `data_traceability` term of CR-028.
10. **orphaned field:** `hazardous_substances_reported` (ws-09) — its only related gate (CR-036) is
    `manual`, so the field is never machine-checked.
11. **opaque single-letter operand symbols:** A.12 `RMP = C / D`, A.13 `IRII = E / F` — unverifiable
    without the PDF; `C` collides with the runoff `C` used elsewhere in the library.

## Below-VA list
ALL 61 nodes are below VA (no PDF). Specifically:
- 13 equation nodes at **VC** (formula quotable from encoding only; no PDF page/quote).
- 9 section + 37 CR + 1 dp nodes at **EV** (encoded, unverified against source).
- 1 document node at **NR** (`in_library:false`).

## Dead gates
- **Confirmed dead (non-parsing `manual`):** 4 → CR-008, CR-010, CR-036, CR-037.
- **At-risk (cross-sheet, pending harness confirmation):** 15 → CR-011..CR-025.

## Missing-doc dependencies
- `doc-iso-59020-pdf-missing` — ISO 59020:2024 rendered PDF (the standard itself). `in_library:false`.
  Acquisition action: drop the PDF into the recorded ISO folder, re-run MAP to lift equation nodes
  VC→VA + populate source_page/source_quote, then run the fix-first remediation under SR-1.
- No encoded cross-standard `references::` edges (ISO-59020 §2 normative references — e.g. ISO 59004 —
  not materialized in the encoding; revisit post-acquisition).

## Tier rationale
**acquisition-blocked.** The primary gate is the not-in-library PDF (no VA reachable). A real
fix-first defect cluster exists (dead + cross-sheet + #22 + verdict-as-producer), but per SR-1 those
corrections cannot be source-verified until the PDF is acquired, so acquisition precedes fix.
