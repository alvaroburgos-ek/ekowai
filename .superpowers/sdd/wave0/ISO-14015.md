# Wave 0 triage — ISO-14015 (Umweltbezogene Due-Diligence-Bewertung / Environmental due diligence assessment)

standard_id `0d90e21d-28bd-4247-91e3-9bb3e89ec863` · prod `vadsmshzebefjreqcicl` (READ-ONLY)
map: `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-14015\`
Date 2026-07-24 · read-only, no prod writes, no fixes, no harness.

## PDF STATUS = MISSING (VA unreachable; SR-3)
Brief supplies `pdf: missing`. Expected under `C:\Users\Ekowai\Desktop\Circular economy,
sustainability and water test\` (doctrine §Source documents) but not readable this session; no
`Sweep Guidelines PDF\ISO-14015.pdf`. Per **SR-3**, VA is unreachable for every node (no
`source_page` derivable, page-offset convention not probeable). Value/CR nodes capped at **VC**
(only the 3 CRs carrying an encoded `source_quote`) or **EV** (all others). This is an OWN-PDF
acquisition gap, NOT a cross-standard NR dependency (no `references::` to a foreign doc in the
encoding → validator invariant #7 does not fire).

## Encoding shape (from prod)
- worksheet_templates ×8 (ISO-14015-01..08; Annex-SL-style phases 1→6)
- worksheet_sections ×43 · fields ×54 (booleans + 14 enums; ALL enum_values populated, 0 phantom
  enum-value-as-field rows)
- equations ×0 · tables ×0 · **pure attestation-checklist standard** (no numeric threshold, no
  range, no compute chain)
- compliance_requirements ×34 (ALL `severity: warn`; 0 block); 3 with `requires_attestation:true`
  + `source_quote` (CR-005/022/031); 1 with `condition: manual` (CR-028)

## Node counts (map = 44 nodes, excl. index)
section 8 · equation 0 · table 0 · CR 34 · decision-point 1 · document 0.
provenance: **VA 0 · VC 3 · EV 41 · NR 0**. data_class: standard_fixed 0 · standard_range 0 ·
derived 0 · engineer_input 44 · normative-as-input-reference 0.
belowVA = **44 (ALL — no PDF)**.

## Findings by class
- **phantom-field gate = 9** — CR-002..CR-010 are hosted on `ISO-14015-01` but their condition
  symbols are fields owned by other worksheets: CR-002/003/004/005/006/007 → `-02`
  (`evidence_verifiable`, `risk_based_approach_applied`, `fair_presentation`,
  `confidentiality_maintained`, `assessor_integrity`, `assessor_competence_demonstrated`);
  CR-008/009/010 → `-03` (`roles_responsibilities_established`, `client_need_determined`,
  `provider_roles_addressed`). Only CR-001 (`asset_identified`) is same-worksheet. Under
  worksheet-local gate evaluation the symbol is absent on -01 → vacuous-pass or perpetual-fehlend.
  → [[dp-iso14015-phantom-gates]]. Fix = re-home to -02/-03 (matches -04..-08 pattern) OR confirm
  evaluate.ts resolves project-wide.
- **dead gate = 1** — CR-028 `condition: manual` (WS06, §5.5.3): no machine-evaluable field, no
  reachable-fail; never auto-fires. (warn)
- **non-enforcing IS-NOT-NULL (F-4-adjacent) = 5** — CR-010, CR-013, CR-016, CR-023, CR-027 use
  `<field> IS NOT NULL` on `is_required:false` fields → pass on ANY value (incl. `false`); record
  presence, not compliance. (CR-010 also counts as phantom.)
- **warn-only standard = 34** — every CR `severity: warn`; 0 `block` → no gate can hard-stop
  approval. Advisory-only posture for the whole standard.
- **greedy-AND = 0** · **inequality/enum/verdict-as-producer = 0** · **#22 hand-enterable-derived
  = 0** · **F-4 var-vs-var = 0** · **cross-standard missing-doc dependency = 0** — none apply (no
  equations, ranges, tables, or external references in the encoding).

## Dead gates (no reachable-fail) = 1
CR-028 (`condition: manual`, warn, WS06). No `block` gate exists at all, so no block dead gate.
The 5 IS-NOT-NULL gates have a technical reachable-fail (null field) so are not counted as strictly
dead — flagged as non-enforcing above.

## Below-VA list = ALL 44 nodes
Uniform cause: **no ISO-14015 PDF in library** (SR-3 → VA unreachable). Ceiling = VC for the 3
quote-bearing CR nodes (CR-005/022/031), EV for the 8 sections + 1 decision-point + remaining 31
CRs + index. On PDF arrival: derive page offset from ITS OWN footer, then re-read each clause for a
verbatim quote + printed page to lift the 34 CRs (and the 3 attestation quotes) toward VA.

## Missing-doc dependencies (out-of-library)
- **The ISO-14015 PDF itself** (primary acquisition gap, VA-reachability blocker). No cross-standard
  `references::` edge encoded → no dependent value node is capped NR by a foreign document.
- **Table 1 competencies** (§7.1, referenced by CR-032) survive only as the `competence_area`
  12-value enum; there is no printed source to model a table node → named as a gap, not an NR dep.

## Tier: fix-first
The map is complete from the encoding. The load-bearing findings are **PDF-independent gate-layer
defects that should be fixed before harnessing**: 9 phantom-field gates (re-home to -02/-03 or
confirm cross-worksheet resolution), 1 dead `manual` gate (CR-028), 5 non-enforcing IS-NOT-NULL
gates, and a warn-only posture (0 block gates). These are triageable now without the PDF.
VA closure additionally requires acquiring the standard's OWN rendered PDF (then derive offset +
verbatim-quote the 34 CRs). Not `acquisition-blocked` in the cross-standard sense (no not-in-library
foreign dependency), but VA-reachability IS gated on the ISO-14015 PDF — flag both.
