# Wave 0 triage — ISO-5667-10

**Standard.** ISO-5667-10 — Wasserbeschaffenheit -- Probenahme -- Teil 10: Anleitung zur Probenahme
von Abwasser (Guidance on sampling of waste waters). Prod std id
`0b87b8c0-612e-49f3-9e7a-f261e855c5ac`. Edition: ISO 5667-10:2020(E), Second edition 2020-11.

**PDF status — CORRECTION.** Brief said `pdf: missing`. **The ledger value is WRONG.** The rendered
PDF is present and extractable:
`C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-10\ISO-5667-10.pdf`
(the ISO folder is spelled "Ciruclar … sustanability" on disk — a typo vs the doctrine's recorded
path, which is why it read as missing). 36 physical pages. Per SR-4 (reality-consistent path) this
map is built **pdfStatus=text**, VA-enabled. Extract at `.superpowers/sdd/wave0/iso5667-10-extract.txt`.

**Language.** The library copy is the SPANISH (AENOR/UNE) translation ("Calidad del agua – muestreo –
Parte 10: Guía de muestreo para aguas residuales", licensed to BIOSSEMA S.C.A. 2021-07-11). Every prod
`source_quote` is Spanish and matches the PDF verbatim, so the encoder worked from this Spanish
edition. Under SR-3 the Spanish PDF is authoritative; the German `title_de`/labels are secondary. VA
quotes in this map are Spanish. → dp-language-edition.

**Page offset.** Derived THIS pdf's own footers (never inherited): phys 8 → printed 1; phys 19 →
printed 12; phys 20 → printed 13 ⇒ **printed = physical − 7**.

**Encoding shape.** 10 worksheet_templates (ISO-5667-10-01..10, phases 1–6) · 86 fields · 3 equations
(Fórmula 1/2 §4.3.2, Fórmula 3 V_n §7.2.2.3) · 36 CRs (CR-001..036). All `equations.source_quote`
NULL; ~29/36 CR `source_quote` NULL (7 carry Spanish quotes); all field `source_anchor` NULL.

**Map.** `01-Projects/ekowai-wizard/reasoning-maps/ISO-5667-10/` (_index + 10 section + 3 eq + 3 doc +
4 dp + cr-index + 7 individual CR nodes). Reuses DWA-A-138-1 `_template-node.md` verbatim; follows FLL
conventions.

**Node counts.** total=56: section 10, equation 3, CR 36, document 3, decision-point 4.
Provenance: VA 20 / VC 19 / EV 12 / NR 5. data_class: standard_fixed 13 / engineer_input 34 /
derived 6 / standard_range 3.
**belowVA = 36** (56 − 20 VA).

## Findings by defect class

### dead gate — phantom-field via systematic off-by-one CR attachment (23)  [PRIMARY]
Every CR whose condition tests a numeric/enum field is attached to the worksheet ONE POSITION BEFORE
the worksheet that owns the field. The wizard evaluates CRs worksheet-locally
(`reference_wizard_compliance_gates`), so each shifted CR references a symbol absent from its own
worksheet instance ⇒ phantom-field / dead gate. Groups:
- CR-003/004/005/006 (attached -01, own -02)
- CR-010/011/012 (attached -03, own -04)
- CR-014/015/016/017/018/019/020/021 (attached -05, own -06)
- CR-022/023/024 (attached -05, own -07)
- CR-030/031/032/033 (attached -08, own -09)
- CR-034/035/036 (attached -09, own -10)
Fire-able (11): CR-001, CR-002, CR-007, CR-008, CR-009, CR-013, CR-025, CR-026(NR), CR-027, CR-028,
CR-029. → dp-gate-shift. Fix = re-parent 23 CRs (data-only migration on worksheet_template_id).

### range/branch collapse (SR-2) (2)
- CR-016 `composite_interval <= 30` drops the 2-branch §7.2.1 rule (5 min for the 2-h composite; 30
  min for the 24-h composite — VA p.11). → dp-composite-interval-branch.
- CR-018 `unit_volume >= 25` drops the pump-type split (50 ml vacuum / 25 ml inline piston — VA p.12);
  CR-017 `suction_velocity >= 0.5` omits the NOTA-1 0,3 m/s relaxation for tube ≥12 mm. →
  dp-unit-volume-branch.

### inequality-as-hard-bound / lost-± (1)
CR-027 encodes the printed `(5 ± 3) °C` (§9.4.3, VA p.23) as `>=2 AND <=8` — arithmetically exact but
the ± form/provenance is lost in the encoding text (non-blocking, recoverable from PDF).

### verdict/boolean-as-producer (self-attesting gate) (≈15)
Many CRs gate on pure engineer attestation booleans (`==true` / `IS NOT NULL`): CR-003, CR-004, CR-005,
CR-014, CR-022, CR-025, CR-026, CR-029, CR-030, CR-031, CR-032, CR-033, CR-034, CR-035, plus the
`IS NOT NULL` presence checks (CR-006, CR-010, CR-013, CR-023, CR-028, CR-036). The "true" value is
hand-entered and never derived — the gate enforces that the user ticked a box, not a printed number.
Not a wiring bug per se, but these are EV-grade non-computational checks; noted for enforcement audit.

### PDF-internal source inconsistency (SR-3) (1)
Fórmula (2) §4.3.2 (p.5) mixes leading symbols `B` and `A` across its terms while the legend defines
only `A` (range [−52/n, 0]). Encoding uses `A` throughout (defensible, matches legend). PDF-vs-PDF
typographic defect flagged; not an encoding error. → eq-2 node.

### missing-doc dependency (NR) (3 docs)
- ISO 5667-3 (§9.3 preservation) — NOT in library (prod `standards` empty for ISO-5667-3). CR-026 caps
  NR. → doc-iso-5667-3.
- ISO 5667-14 (§10.3 QA/QC) — NOT in library. CR-032 caps NR (also dead via gate-shift). →
  doc-iso-5667-14.
- National waste-water legislation ("Ordenanza de Aguas Residuales", §7.2.1) — inherently
  per-permit/jurisdiction, in_library:false; the composite-duration selection CR-016 depends on it. →
  doc-national-ww-legislation.

### language-edition mismatch (SR-3, policy) (1)
Encoding DE vs authoritative ES PDF vs canonical EN title. VA quotes are Spanish; any future SR-1 apply
must quote this Spanish PDF (or a procured DE/EN original). → dp-language-edition.

### unattributed encoding (provenance gap)
All equation source quotes and all field anchors are NULL; the map supplied PDF pages this session but
the encoding itself carries no source pointers (EV until backfilled).

## Below-VA list (36 nodes)
- Sections VC (6): -01, -02, -05, -07, -09, -10 (registration / type-selector / attestation sheets, no
  printed number to assert on the sheet itself).
- CRs below VA (24): EV — CR-001, CR-006, CR-007, CR-010, CR-013, CR-023, CR-028, CR-036 (presence /
  boolean, no printed threshold); VC — CR-003, CR-004, CR-005, CR-014, CR-020, CR-021, CR-022, CR-024,
  CR-025, CR-029, CR-030, CR-031, CR-033, CR-034, CR-035 (qualitative Spanish quote, no numeric page);
  NR — CR-026, CR-032.
- Documents NR (3): doc-iso-5667-3, doc-iso-5667-14, doc-national-ww-legislation.
- Decision-point VC (1): dp-gate-shift.
VA (20, NOT below): sections -03/-04/-06/-08; eq-1/eq-2/eq-3; CR-002/008/009/011/015/016/017/018/019/027;
dp-composite-interval-branch / dp-unit-volume-branch / dp-language-edition.

## Dead gates
- deadGates = **23** (phantom-field via off-by-one attachment). CR-032 is dead AND NR (double).
  All 23 have severity block/warn but cannot fire on real input worksheet-locally.

## Missing-doc deps
- ISO 5667-3 (in_library:false) — CR-026 → NR.
- ISO 5667-14 (in_library:false) — CR-032 → NR.
- National waste-water legislation (in_library:false) — CR-016 duration-branch → NR/context.

## Tier
**fix-first.** VA is reachable across the value layer — every printed threshold (§3.4, §5.1, §5.4,
§7.1.2, §7.2.1, §7.2.2.1, §7.2.2.3, §9.4.3) was quoted VA from the present PDF this session, and all 3
equations are VA. The blocker to a meaningful harness run is the **23 dead gates from the systematic
worksheet-attachment shift**, which must be re-parented first (data-only migration). Only 2 CRs are
acquisition-gated (ISO 5667-3 / ISO 5667-14) — a minority, so the standard is NOT acquisition-blocked
overall. Fix the gate shift → re-derive the branch-collapse CRs → then harness.

## Decision-points routed to Alvaro batch
- dp-iso5667-10-gate-shift (re-parent 23 phantom-field CRs one worksheet forward)
- dp-iso5667-10-composite-interval-branch (CR-016 2h/24h → 5/30 min split)
- dp-iso5667-10-unit-volume-branch (CR-017/018 pump-type 25/50 ml + 0,3/0,5 m/s)
- dp-iso5667-10-language-edition (DE encoding vs ES authoritative PDF vs EN canonical)
</content>
