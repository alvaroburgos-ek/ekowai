# Wave 0 triage — ISO-59014

Standard: **ISO-59014** — "Environmental management and circular economy — Sustainability and
traceability of secondary materials recovery — Principles and requirements"
(title_de: "Umweltmanagement und Kreislaufwirtschaft — Nachhaltigkeit und Rueckverfolgbarkeit der
Rueckgewinnung von Sekundaermaterialien — Grundsaetze und Anforderungen").
Prod standard_id `afeaacac-382f-453e-baf6-936db7451d8c` (project `vadsmshzebefjreqcicl`, read-only).
PDF: **missing** — no ISO-59014 file supplied to this task and none found in the recorded ISO folder
(`docs/verification-doctrine.md` §Source documents). **No markdown/JSON convenience extraction
either.** → per SR-3/SR-1, every value/normative node caps at **EV**; VC and VA are both unreachable.
Map: `Obsidian\...\reasoning-maps\ISO-59014\` (69 nodes + _index).

## Node tally (69)
- section 10 (one per worksheet ISO-59014-01..10) · equation **0** · table **0** · CR 54 ·
  document 3 · decision-point 2.
- **provenance:** VA 0 · VC 0 · EV 66 · NR 3.  (**belowVA = 69** — the whole map, pdf missing.)
- **data_class:** engineer_input dominant (84 fields, ~70 boolean self-attestations) ·
  standard_fixed **0** · derived **0** · standard_range **0** · normative-as-input-reference 3
  (the out-of-library LCA doc nodes).
- **Self-attestation / management-system checklist standard**: zero equations, zero lookup tables,
  zero printed numeric constants. Nothing to derive, nothing to verify against a table — the whole
  compliance surface is "did the org attest/record X (§4–§8)". Even more input-only than the
  procedural GHG standards; the closest sibling profile in the sweep is a pure checklist.

## Findings by class
### Dead gates (0)
No structurally dead CRs. All 54 conditions are boolean `== true` / `IS NOT NULL` over a field that
exists somewhere in the standard → each is reachable-fail in principle. (21 are
cross-worksheet-**blocked** — a reachability defect, F-A below, not a dead gate; 3 are
doc-blocked, F-D.)

### F-A — Cross-worksheet gate (21 CRs) — DOMINANT defect class
Gates evaluate worksheet-locally (`reference_wizard_compliance_gates`: `evaluate.ts` resolves the
CR's fields on the CR's OWN worksheet). 21 CRs read a field placed on a DIFFERENT worksheet, so the
gate resolves "fehlend" against a field the engineer fills elsewhere:
- WS-01 → WS-02 field: **CR-001, CR-002**
- WS-04 → WS-05 field: **CR-012, CR-013, CR-014, CR-015, CR-016, CR-019**
- WS-06 → WS-07 field: **CR-025, CR-026, CR-027, CR-028, CR-029, CR-030, CR-031**
- WS-06 → WS-08 field: **CR-032, CR-033, CR-034, CR-035, CR-036, CR-037, CR-038**
Ruling (relocate CR to field-owning worksheet vs. intended phase-split needing an evaluator change)
staged → [[dp-iso59014-crossworksheet-gates]].

### F-C — Greedy-AND (4) — §8 traceability list collapse
- **CR-019** 2-way AND (`logistics_facilitates_recovery` AND `adequate_packaging_used`) — also F-A.
- **CR-047** 7-way AND (upstream minimum data a–g).
- **CR-048** 3-way AND (processing h–j).
- **CR-049** 6-way AND (downstream k–p).
Each folds the Clause 8 minimum-data list into one opaque block gate; a single missing sub-item
fails the whole requirement without saying which. Verify multi-AND support at harness.

### F-B — #22 hand-enterable / no-producer number (1)
- **CR-041** gates `recycling_rate IS NOT NULL` (WS-09 `number`, §7.8) — a monitored result with no
  producer/compute trace (0 equations). Should arguably derive from WS-10 throughput
  (`processing_inputs_quantity`/`processing_outputs_quantity`/routing %). See
  [[dp-iso59014-recycling-rate]].

### F-D — Missing-doc dependency (NR) — 3 CRs, 2 out-of-library docs
§6.1 defers the "best outcome from documented life-cycle studies" determination to
**ISO 14040 / ISO 14044 / ISO 14075**. Library check (`standards`): ISO-14044 IS present
(`DIN-EN-ISO-14044`); **ISO 14040 and ISO 14075 are NOT in the library**. CRs whose satisfaction is
defined by an out-of-library doc cap at **NR**: **CR-004, CR-005, CR-016**. Docs:
[[doc-iso-14040]] (in_library:false), [[doc-iso-14075]] (in_library:false),
[[doc-iso-14044]] (in_library:true).

### Not triggered (clean)
- **Phantom-field gate:** none — every symbol in every CR condition resolves to a real field
  *somewhere* in the standard (the defect is *which* worksheet — F-A — not non-existence).
- **Inequality/enum/verdict-as-producer:** none — 0 equations, so no producer nodes at all;
  no comparative/inequality conditions in any CR.
- **F-4 var-vs-var non-enforcement:** none (no var-vs-var gates).

## Below-VA list (69 — entire map, pdf missing)
Every node is below VA because **no PDF and no markdown extraction exist** → no `source_page` can be
asserted for any clause, and the modal force (shall/should) of every §4–§8 requirement is
unverified. Representative caps:
| node class | count | grade | why |
|---|---|---|---|
| 54 CR nodes | 51 EV / 3 NR | EV/NR | condition+clause encoded (EV); CR-004/005/016 doc-blocked (NR) |
| 10 section nodes | 10 EV | EV | worksheet structure encoded; no PDF page |
| 3 document nodes | 2 NR / 1 EV | NR/EV | ISO-14040/14075 not in library (NR); ISO-14044 in library (EV) |
| 2 decision-points | 2 EV | EV | defect rulings (no source value) |

## Missing-doc deps (out-of-library, acquisition class)
1. **ISO-59014 itself** — the primary source document is not in the library (pdf missing). This is
   the governing acquisition gap: until the rendered PDF is obtained, ZERO nodes can reach VA and no
   `standard_fixed`/`normative-as-input-reference` node can be validated (invariants #1/#2 are only
   vacuously satisfied because there are 0 VA and 0 standard_fixed nodes).
2. **ISO 14040** (LCA principles & framework) — referenced §6.1, not in library → CR-004/005/016 NR.
3. **ISO 14075** (Social LCA) — referenced §6.1, not in library → CR-005 NR.
(ISO 14044 IS in library as DIN-EN-ISO-14044 and does not itself force NR.)

## Tier
**acquisition-blocked.** The ISO-59014 source PDF is not in the library, so the entire value layer
is unverifiable regardless of any fix — no clause can be lifted above EV, no modal force confirmed,
no `standard_fixed` established (indeed the standard has none: it is a pure self-attestation
checklist). Fixing the F-A cross-worksheet gates (21 CRs), the F-C greedy-AND traceability gates,
and the F-B recycling_rate producer would improve gate *reachability*, but none of that lifts the
map to harness-ready because the doctrine bars a VA claim without a PDF page (SR-3) and forbids
applying any value without a same-session verbatim source (SR-1). Acquire + render the ISO-59014 PDF
FIRST; then re-map to derive the footer page offset, lift clause refs to VA, and confirm shall/should
force per CR. The F-A/F-C/F-B fixes and the ISO 14040/14075 acquisition are the fix-first tail that
follows, not the blocker.
