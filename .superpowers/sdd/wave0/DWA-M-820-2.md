# Wave 0 triage — DWA-M-820-2 (Qualität von Ingenieurleistungen optimieren – Teil 2: Durchführung)

- **Standard id (prod):** `b7acc0ab-00c5-4393-94a5-3aaa6f20b997`
- **PDF:** `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-820-2\DWA-M_820-2.pdf` (April 2023, 1. Auflage)
- **pdfStatus:** `text` (born-digital; `pdftotext -layout` clean). Offset **`printed = physical − 2`**, derived from THIS pdf's footer and cross-checked (physical p.4 = printed 2; physical p.39 = printed 37 = §5.2 "Bedarfsplanung", which the Inhalt lists at printed 37).
- **Map:** `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\DWA-M-820-2\` (+ `_index.md`)
- **Node count:** 105 (28 section, 0 equation, 4 table/Bild, 60 CR, 12 document, 1 decision-point)
- **Provenance:** VA 30 / VC 63 / EV 0 / NR 12
- **data_class:** engineer_input 88 (fields+section owners) / derived 0 / standard_fixed 0 / normative-as-input-reference 4 (the Bilder) — plus the 12 external docs carry `normative-as-input-reference`+`in_library:false`.

## Nature of the standard (why the ceiling is VC, not a gap)
This is a **process / quality-management Merkblatt** — an *Erfolgsfaktoren* catalogue of **Qualitätsbausteine** (typische Problemstellung → Lösungsvorschlag). The Vorwort states the working group **deliberately declined to provide checklists** ("vom Auftragnehmer abzuarbeitender Einzelpunkte"). The encoding therefore has:
- **0 equations, 0 numeric tables, 0 derived values.**
- 112 fields, all `engineer_input` (boolean confirmations, enum frequencies, text/json evidence, 3 EUR/count project numbers). None is a printed constant or a computed output.
- 60 CRs, all **prose-attestation gates** over those inputs.

Because **no printed number exists anywhere**, there is nothing to source-verify per SR-1/SR-3. The honest ceiling for the CR logic is **VC** (the condition is an encoding convenience, not a printed formula). The 28 section containers lift to **VA** on their printed §-anchor + Qualitätsbaustein heading (Inhalt cross-checked). This is not a fixable gap — it is the correct grade for a non-numeric standard.

## Findings by class

### Phantom-field gate (2) — worksheet-local evaluator blind to a foreign-worksheet field
- **REQ-26** (ws 820-2-11, §5.3.3): condition `project_goals IS NOT EMPTY AND goals_stability_signoff == true`. Both fields live on **ws 820-2-02**, not ws-11. Gate cannot fire as authored. (Also greedy-AND.)
- **REQ-45** (ws 820-2-20, §5.6.4): condition `approval_procedure_defined == true`. Field lives on **ws 820-2-09**, not ws-20. Gate cannot fire as authored.

### Proxy / mismatched-condition gate (2) — condition copied from another CR, wrong signal
- **REQ-06** (ws 820-2-05, §4.3.4 "Zielkonflikt betriebliche Anforderungen vs Budget"): gated on `decision_competencies_mapped == true` — a verbatim copy of **REQ-05's** condition, unrelated to its own title/clause.
- **REQ-23** (ws 820-2-11, §5.2.4): gated on `forward_planning_done == true` — a verbatim copy of **REQ-22's** condition.

### Dead gate — prose / non-parsing condition (5)
- **REQ-13** (`Engineer attestation`), **REQ-32** (`Procurement schedule documented`), **REQ-50** (`Process artefacts present`) — free prose, `requires_attestation=true`, no parseable predicate / fired_by.
- **REQ-55** (`(innovation_scope_defined == true) OR (project does not pursue innovation)`) and **REQ-59** (`(bim_basics_established == true) OR (BIM not in scope)`) — carry a **prose OR-disjunct** no evaluator can resolve → effectively always-pass / unparseable.

### Greedy-AND (5) — compound conjunction, blocks on either conjunct
- **REQ-01** (`primary_sector IS NOT NULL AND included_hoai_phases IS NOT EMPTY`), **REQ-09** (`change_log_present AND change_impact_documented`), **REQ-10** (`master_schedule_present AND detailed_schedule_present`), **REQ-51** (`warranty_start_date != null AND warranty_end_date != null`), **REQ-60** (`data_security_measures IS NOT EMPTY AND digital_rights_clarified`). All same-worksheet (parseable), but the compound should be split into separate gates or the intent confirmed.

### Presence-as-verdict (weak) (4)
- **REQ-08 / REQ-40 / REQ-42 / REQ-57** gate on `… IS NOT NULL` presence of an enum/json rather than a substantive value — passes on any selection.

### Inequality / enum / verdict-as-producer
- None of the "value-as-producer" class (no equations, no derived producers). **REQ-35** `discharge_permit_extension IN {applied,granted,not_required}` is enum-as-selection (categorical, not F-7 numeric range) — see decision-point.

### #22 hand-enterable-derived
- **None.** There are zero derived values in the encoding, so the #22 class cannot occur.

### F-4 var-vs-var non-enforcement
- **None** (no equations / no var-vs-var comparisons).

### Missing-doc dependency (12) — out-of-library, dependents NR-capped
HOAI, DWA-M-820-1:2020, DWA-M-820-3, DIN 276, VOB, VgV, WHG, LWG, BGB, DIN 69901, VDI-Regelwerk, DWA-M-810. The HOAI LPH 0–9 phase model underlies the entire worksheet spine. **None supplies a numeric value the harness must read** — the standard only *names* these in prose. So the NR cap does not block the standard's own content (it is legal/process context, not a deferred sizing table). This is why the tier is fix-first, not acquisition-blocked.

## Below-VA list (63 VC + 12 NR = 75 nodes below VA)
- **VC (63):** all 60 CR nodes (condition is DB/markdown convenience; `source_quote` populated on only 8 CRs — REQ-06/08/13/15/23/32/42/50 — none PDF-page-attested per-node) + 3 Bild/Klimakennung nodes not individually re-quoted (bild-1, bild-3, bild-4, klimakennung — 3 of the 4 table nodes are VC; all 4 actually VC this triage).
- **NR (12):** the 12 out-of-library documents.
- Everything below VA is capped **by nature** (no printed number) or **by out-of-library** — not by a fixable encoding gap for value provenance.

## Dead gates (count = 5)
REQ-13, REQ-32, REQ-50, REQ-55, REQ-59 (prose / non-parsing conditions). REQ-26 and REQ-45 are *phantom-field* gates (separately counted) — also effectively non-firing.

## Tier: **fix-first**
Structurally complete (28 phase worksheets, 60 gates, 112 project-input fields, clean §-to-printed-page mapping, no formulas to verify). **No acquisition blocker for its own content** — the Merkblatt is fully readable and mapped; there is no printed number or table to acquire. Before a harness run is meaningful the gate layer needs repair: 2 phantom-field gates, 2 proxy/mismatched conditions, 5 prose/non-parsing dead gates, 5 greedy-AND compounds. All are encoding-logic defects the DATA track can fix **without new source**. Not acquisition-blocked (the 12 missing docs are generic HOAI/legal context the process gates only name).
