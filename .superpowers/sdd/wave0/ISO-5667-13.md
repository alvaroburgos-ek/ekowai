# Wave 0 triage — ISO-5667-13

**Standard.** ISO-5667-13 — Wasserbeschaffenheit -- Probenahme -- Teil 13: Anleitung zur Probenahme von
Schlämmen (Guidance on sampling of sludges). Prod std id `436bd1b4-0e39-47e6-8a89-3e36cdee5e86`.
Edition: **ISO 5667-13:2011(E)**, English.

**PDF status — CORRECTION.** Brief said `pdf: missing`. **The ledger value is WRONG.** The rendered PDF
is present and extractable:
`C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 5667-13\ISO-5667-13-2011.pdf`
(the ISO folder is misspelled "Ciruclar…sustanability" on disk vs the doctrine's recorded path — the
same typo that hid the ISO-5667-10 PDF). Per SR-4 (reality-consistent path) this map is built
**pdfStatus=text, VA-enabled.** Extract at `.superpowers/sdd/wave0/iso5667-13-extract.txt`
(scoop `pdftotext -layout`).

**Page offset.** Derived THIS pdf's own footers (never inherited): §2 footer = printed 1; §6.1.3/§6.1.4.2
= 6; §6.3.6 = 9; §6.4.2 = 11; §7.2/§8/§9 = 13. Front matter roman; body p.1 = §1.

**Encoding shape.** 8 worksheet_templates (ISO-5667-13-01..08) · 49 fields · 3 equations (Gl.1 t §6.1.3,
Gl.2 n §6.1.4.2, Gl.3 n_sp §6.3.6) · 23 CRs (CR-001..023). All `equations.source_quote` NULL; most CR
`source_quote` NULL (CR-001/003/018/022 carry quotes that match the PDF verbatim). All
`verification_status = imported_unverified`.

**Map.** `01-Projects/ekowai-wizard/reasoning-maps/ISO-5667-13/` (_index + 8 section + 3 eq + 8 doc + 23
CR + 3 dp). Reuses DWA-A-138-1 `_template-node.md` verbatim; follows FLL conventions.

**Node counts.** total=45: section 8, equation 3, CR 23, document 8, decision-point 3.
Provenance: VA 13 / VC 20 / NR 9 / EV 3. data_class: engineer_input 31 / derived 5 / standard_fixed 8
(no standard_range; no normative-as-input-reference).
**belowVA = 32** (45 − 13 VA).

## Findings by defect class

### dead gate (2)
- **CR-005** `condition = manual` — non-parsing predicate, no fired_by input; can never auto-fire.
  Substantive obligation depends on CEN/TR 13097 (out of library) → NR either way. → dp-03 / doc-cen.
- **CR-017** heap n_sp block gate (`n_sp >= 4 AND n_sp <= 30`) — `n_sp` is owned by WS-06 (produced by
  eq-3) but the gate is attached to **WS-05** → under worksheet-local evaluation the symbol is unresolved
  → the gate **cannot fire** (dead block, safety-tagged). The numeric bound itself is **VA-correct**
  (printed p.9 in ISO-5667-13). Defect is purely WIRING. → dp-01, dp-02.

### cross-worksheet / phantom-field gate (5, incl. CR-017)
CR condition references a field owned by a DIFFERENT worksheet template than the CR:
- CR-004 (on WS-01) → `monitoring_objective` owned by WS-03
- CR-006 (on WS-01) → `variability_type`, `concentration_target` owned by WS-03
- CR-015 (on WS-05) → `sample_volume_adequate` owned by WS-06
- CR-016 (on WS-05) → `sludge_source` owned by WS-06
- CR-017 (on WS-05) → `n_sp` owned by WS-06 (derived) [also the block dead gate]
Clause refs corroborate the mis-placement (§4.x→WS-03, §6.3.x→WS-06). The 4 warn ones are non-firing but
not counted in the strict dead total (only the block CR-017 + the non-parsing CR-005 are). Fix = re-parent
5 CRs (data-only migration on worksheet_template_id), OR confirm the evaluator resolves symbols
project-wide. → dp-01-cross-worksheet-gates.

### inequality/verdict-as-producer on derived (3)
- CR-012 `t > 0` gates on the derived output of eq-1; near-vacuous.
- CR-013 `n > 0` gates on the derived output of eq-2; near-vacuous.
- CR-017 `n_sp 4..30` gates on the derived output of eq-3.
→ dp-03-derived-verdict-gates.

### #22 hand-enterable-derived (3)
t, n, n_sp are each equation outputs AND hand-enterable `number` fields (CR-012/013/017). The derived
value can be spoofed by direct entry. → dp-03.

### eq-grammar / possible NON_PARSING (4)
CR-007, CR-008, CR-021, CR-023 use `x eq true` instead of `x == true`. Per
reference_wizard_compliance_gates the supported grammar is `==`/comparison; `eq`/`when` are called out as
UNsupported → if confirmed against evaluate.ts these 4 are non-parsing → non-enforcing. Mechanical fix
(`eq`→`==`), out of Wave-0 scope. → dp-03.

### block on attestation checkbox (3)  [VA-sourced, low value]
CR-001 (§1 WARNING, VA p.1), CR-003 (§2, VA p.1), CR-022 (§8, VA p.13) are block-severity self-attestation
booleans (`== true`). Owned + reachable-fail (not dead), but enforce only that a box was ticked.

### present-not-null on optional boolean / near-vacuous (4)
CR-010, CR-014, CR-019, CR-020 — `IS NOT NULL` on non-required booleans, satisfied by true OR false.

### greedy-AND present-not-null (3)
CR-002, CR-006, CR-011.

### encoding under-captures §2 (SR-3)
PDF §2 (p.1 VA) lists NINE normative refs: ISO 5667-1, ISO 5667-10:1992, ISO 5667-12, ISO 5667-14,
ISO 5667-15:2009, ISO 6107, ISO/TR 8363, ISO 18283, CEN/TR 13097. The encoding (CR-003 description) named
only ISO 5667-15 + ISO 6107. Map restores the other 7 as document nodes (2 newly surfaced: ISO 5667-14,
ISO/TR 8363). Non-blocking provenance gap.

### unattributed encoding (provenance gap)
All 3 equation source quotes and all field anchors are NULL in prod; this session supplied the PDF pages
(p.6/p.9) but the encoding itself carries no source pointers (EV until backfilled).

## Below-VA list (32 nodes)
- Sections EV (3): -02 (terms), -03 (plan), -04 (equipment) — §3/§4/§5 procedural sheets; no numeric
  assertion made on the sheet itself this session (could rise to VA if §3/§4/§5 text is quoted).
- CRs below VA (18): NR — CR-005, CR-014, CR-021 (obligation deferred to out-of-library CEN/18283/5667-15);
  VC — CR-002, CR-004, CR-006, CR-007, CR-008, CR-009, CR-010, CR-011, CR-012, CR-013, CR-015, CR-016,
  CR-019, CR-020, CR-023 (present-not-null / boolean / cross-sheet, no numeric page asserted).
- Documents below VA (8): NR — doc-iso-5667-15, doc-iso-6107, doc-iso-18283, doc-cen-tr-13097,
  doc-iso-5667-14, doc-iso-tr-8363 (out of library); VC — doc-iso-5667-12 (method-only non-blocking),
  doc-iso-5667-10 (in_library, not read here).
- Decision-points VC (3): dp-01, dp-02, dp-03 (ruling nodes).
VA (13, NOT below): eq-1, eq-2, eq-3; sections -01, -05, -06, -07, -08; CR-001, CR-003, CR-017, CR-018,
CR-022.

## Dead gates
- deadGates = **2**: CR-005 (`manual` non-parsing) + CR-017 (cross-worksheet block, symbol on WS-06,
  cannot fire worksheet-locally). Note: 4 MORE cross-sheet gates (CR-004/006/015/016) are non-firing but
  `warn` severity → not counted in the strict block/non-parsing dead total; flagged at-risk.

## Missing-doc deps (out of library → NR)
- ISO 5667-15:2009 (in_library:false) — §7.2 storage table; CR-009/010/021 substantive obligation → NR.
- ISO 6107 (in_library:false) — §3 vocabulary; CR-003 list completeness.
- ISO 18283 (in_library:false) — §6.2 replicate performance; CR-014 → NR.
- CEN/TR 13097 (in_library:false) — §4.1 hazard analysis; CR-005 → NR.
- ISO 5667-14 (in_library:false) — §2 QA/QC; no gate consumes it (list completeness only).
- ISO/TR 8363 (in_library:false) — §2 open-channel flow; contextual (flow_related / open_channel).
- ISO 5667-12 (in_library:false) — §6.3.6 core-sampling METHOD only; **non-blocking** (n_sp bound is VA in
  ISO-5667-13 itself).
- ISO 5667-10 (**in_library:true**) — §8 safety cross-ref; no NR cap.

## Tier
**fix-first.** VA reachable across the value layer (all 3 equations + the n_sp bound + §1/§2/§8 quotes,
source-verified from the present PDF this session). The blocker to a meaningful harness run is gate
topology: 5 cross-worksheet gates (incl. block dead gate CR-017), the CR-005 `manual` non-parsing gate,
and the 4 `eq`-grammar CRs — all data-only fixes (re-parent CRs on worksheet_template_id / normalize
`eq`→`==`). Only a minority of obligations are acquisition-gated (ISO 5667-15 storage, ISO 18283
replicate, CEN/TR 13097 hazard) — NOT acquisition-blocked overall. Fix cross-sheet gates + grammar → harness.

## Decision-points routed to Alvaro batch
- dp-01-cross-worksheet-gates (re-parent 5 phantom-field CRs to their owning worksheet, or confirm
  project-wide symbol resolution)
- dp-02-nsp-deadgate (CR-017 block: relocate to WS-06 + decide engine-only n_sp)
- dp-03-derived-verdict-gates (t>0/n>0/n_sp verdicts on derived + `eq`→`==` grammar normalization)
