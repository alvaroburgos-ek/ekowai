# Wave 0 triage — ISO-14002-2

**Standard:** ISO 14002-2:2023(E) — Environmental management systems — Guidelines for using ISO 14001
to address environmental aspects and conditions within an environmental topic area — Part 2: Water.
**Prod standard id:** `e675a07c-4b69-42a1-a1a3-b1dc50ad8a38` (project `vadsmshzebefjreqcicl`, READ-ONLY).
**Reasoning map:** `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-14002-2\`
**PDF (brief said missing — FOUND):** `C:\Users\Ekowai\Desktop\Ciruclar economy, sustanability and water test\ISO 14002-2\ISO-14002-2-2023.pdf`
(doctrine folder spelling `Circular economy, sustainability...` is misspelled on disk as
`Ciruclar economy, sustanability...`; duplicate copy at `Batch guidelines 01072026\ISO 14002-2\`).
pdfStatus = **text** (scoop `pdftotext -layout`, 1683 lines). Page offset derived from THIS pdf's own
footer: printed integer sits on the line immediately BEFORE the running `ISO 14002-2:2023(E)` footer;
body §1 Scope = printed page 1, §4.2.1 = page 3 (verified text-lines 373/485). Value/clause nodes
graded VA per SR-3.

## Encoding shape (prod read-back)
- worksheet_templates ×8, worksheet_sections ×55, fields ×51 (11 enum, ~28 boolean, ~12 text),
  equations ×0, compliance_requirements ×26 (10 block / 16 warn), enum_values on 11 fields.
- 100% `engineer_input` fields — no printed value, no formula, no binding numeric table
  (Annexes A–D are informative worked examples only). Same guidance-standard shape as ISO-14004.

## Node counts (54 total)
| type | count | provenance |
|---|---|---|
| section | 8 | VA |
| compliance_requirement | 26 | VA |
| document (external) | 6 | NR (in_library:false; ISO-14004 doc marked in_library:true but no value edge) |
| decision-point | 6 | 3 VA/VC-anchored on their CR + 3 EV structural |
| equation / table | 0 | — |
| index | 1 | VA |

Provenance tally: **VA 34** (8 sec + 26 CR), **VC 11** (enum value nodes — page not pinned per option),
**NR 6** (out-of-library docs), **EV 3** (structural/normativity decision-points).
**belowVA = 20** (11 VC + 6 NR + 3 EV).

## Findings by defect class
1. **verdict/self-attestation-as-producer ×20 (systemic)** — 20/26 CRs gate on a single hand-entered
   boolean `<step>_done == true`; the pass "producer" is the engineer's checkbox, not a derived/measured
   value. 9 of these are `block` and are satisfiable by ticking a box → enforce attestation, not
   substance. Ruling → [[dp-iso14002-boolean-verdict-gates]].
2. **IS NOT NULL presence-only gate ×4** — CR-002/005/007 (all `block`) + CR-020 (`warn`) pass on any
   non-null incl. whitespace/placeholder. → [[dp-iso14002-presence-gates]].
3. **block-on-should severity ×1 systemic (10 block CRs)** — source modal is "should" throughout
   (guidance); 10 CRs set `block`. Whether a should-only standard may hard-block (vs ISO 14001 owning
   the "shall") is an Alvaro ruling. Blocks: CR-001,002,005,007,009,012,013,015,019,021,023,024,026.
   → [[dp-iso14002-block-on-should]].
4. **enum value node no per-option page ×11 (VC ceiling, not a defect)** — 11 enum fields carry
   `regulation_reference` clause tags but no pinned page per option → VC. → [[dp-iso14002-enum-pages]].
5. **source_quote NULL on ALL 26 CRs + ALL 51 fields ×1 systemic** — only `clause_reference` populated;
   encoding ships no verbatim. VA still reachable via PDF (done in this map). → [[dp-iso14002-empty-source-quote]].
6. **Annex normativity ×1** — Annexes A–D all (informative); confirm no annex example imported as
   `standard_fixed` (current default 0 standard_fixed is correct). → [[dp-iso14002-annex-normativity]].

### Defect classes NOT present (checked, absent)
- **dead gate** — NONE. All 26 CR conditions reference a real existing field (all 26 condition symbols
  verified present in the 51 fields); each is satisfiable AND failable. No literal-`TRUE`/`FALSE`
  constant condition (contrast ISO-14004 CR-033/CR-038).
- **phantom-field gate** — NONE (all condition fields exist).
- **greedy-AND** — NONE. Every CR condition is a SINGLE predicate; no multi-field AND.
- **#22 hand-enterable-derived** — NONE (0 derived fields, 0 equations).
- **inequality/enum/numeric-comparison-as-producer** — NONE (no numeric comparisons; enum gates are
  presence-only, folded into finding #2).
- **F-4 var-vs-var non-enforcement** — NONE (no var-vs-var conditions).

## Below-VA node list (20)
- VC ×11 — enum value nodes: water_review_information_category, environmental_concern_type,
  water_input_output_category, baseline_type, risk_opportunity_type, action_type, objective_level,
  target_type, control_hierarchy_type, emergency_situation_type, performance_action_type (also
  indicator_type / kpi_measurement_type are enums but the 11-count reflects distinct value-node
  clusters per the index; per-option page not pinned).
- NR ×6 — doc-iso-14001, doc-iso-14004, doc-iso-14031, doc-iso-14046, doc-iso-14005, doc-iso-14063.
- EV ×3 — dp-iso14002-boolean-verdict-gates, dp-iso14002-presence-gates, dp-iso14002-block-on-should,
  dp-iso14002-empty-source-quote (structural decision-points with no single PDF value anchor; the
  enum-pages dp is VC and annex-normativity dp is VA, so 3 remain EV).

## Dead gates
**0.** No CR has a literal-constant/unreachable-fail or phantom-field condition.

## Missing-doc dependencies (out-of-library → invariant #7)
6 referenced siblings, ALL citation targets (not data edges) — no local value node is capped NR by a
foreign table:
- doc-iso-14001 (§1/§2 normative + CR-001 precondition; gates a LOCAL bool) — NR
- doc-iso-14046 (§4.2.2 NOTE water footprint) — NR
- doc-iso-14031 (§6.2.2 indicators) — NR
- doc-iso-14005, doc-iso-14063 (bibliography/support context) — NR
- doc-iso-14004 (companion EMS guidance; separately encoded in library but no value edge) — NR

## Tier
**fix-first.** Not acquisition-blocked (PDF is in-library/on-disk; all referenced docs are citations,
not governing-value dependencies). Not harness-ready as-is: the systemic verdict-as-producer +
presence-only gates land on `block` CRs, and the block-on-should severity question needs an Alvaro
ruling before the harness treats these 10 blocks as authoritative hard-stops. All defects are
enforcement-semantics, not structural breakage — no dead gates, no phantom fields, no broken links.
