# Wave-0 Triage — ISO-14033

**Standard:** ISO 14033 — Environmental management — Quantitative environmental information — Guidelines and examples
**Prod standard_id:** `fe3a798b-f09f-4929-9400-cdb321a9fa68` (Supabase `vadsmshzebefjreqcicl`, read-only)
**PDF status:** **MISSING** — no `*14033*` file in `C:\Users\Ekowai\Desktop\Circular economy, sustainability and water test\`; brief `pdf: missing`.
**Map:** `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-14033\` (+ `_index.md`)
**Generated:** 2026-07-24. Read-only; NO prod writes, NO fixes, NO harness run (Wave 0).

## Encoding shape
- 8 worksheet_templates (ISO-14033-01..08), PDCA-structured (Scope / Terms & Data / Principles /
  Plan / Do / Quantitative-processing / Check / Act).
- 40 fields (30 engineer_input, plus 4 Clause-3 definitional enums, 3 Eq.1 fields, several booleans).
- 1 equation (WS-06 Eq.1: `parameter_value = activity_data * emission_removal_factor`, §6.2.3,
  `source_quote: null`, `imported_unverified`).
- 27 compliance_requirements — **ALL severity `warn`** (zero `block`).
- 0 printed numeric tables encoded (guidance/PDCA text standard).

## Provenance / cap (SR-3)
**PDF missing → the whole standard is uniformly VC-capped.** No node can be VA (invariant #1) and no
node can be standard_fixed (invariant #2 needs a source_page; none derivable — no PDF footer, the
138/GAR `physical−2` offset is NOT inherited). All `source_quote` are null → SR-1 verbatim blocked too.
Headline node grade: **VA 0 / VC 38 / EV 8 / NR 0**. (EV = the 8 pure-procedural section shells that
lack even a convenience extraction anchor; VC = everything with an encoding anchor.)

## Findings (by defect class)
1. **Dead gate (`condition: manual`)** — CR-025 (§6.4.3), CR-026 (§6.4.2.2). Literal string `manual`,
   no evaluable predicate, no `fired_by`, can never fire. **2 dead gates.**
2. **Always-true enum-membership gate (soft dead-gate)** — CR-002 (`application_scope IN
   {internal,external,comparison}`) and CR-005 (`data_origin_category IN {primary,secondary}`) each
   list the field's ENTIRE enum domain → a required enum can never fail → never warns. **2 tautology gates.**
3. **Equation-as-condition / verdict-as-producer + F-4 + #22** — CR-019
   (`parameter_value == activity_data * emission_removal_factor`) restates Eq.1 as a gate the app's
   condition grammar cannot enforce as an arithmetic identity (F-4 var-vs-var non-enforcement); it also
   makes the derived `parameter_value` compare against its own inputs. **1 node, triple-tagged.**
4. **#22 hand-enterable-derived** — `parameter_value` is Eq.1's output yet is encoded as a hand-enterable
   `number` field with no engine write-back. **1 field.**
5. **Cross-sheet / phantom-field gate** — CR-008 (on WS-02) reads eight `principle_*` fields owned by
   WS-03; CR-003 (on WS-02) reads `principle_comparability` owned by WS-03; CR-019 (on WS-05) reads
   `parameter_value/activity_data/emission_removal_factor` owned by WS-06. **3 cross-sheet gates.**
6. **Greedy-AND** — CR-008 ANDs 8 `principle_*` booleans into one gate (no per-principle attribution). **1.**
7. **All-warn / no enforcement surface** — 27/27 CRs are `warn`; ZERO block gates. The gate layer is
   advisory only. Consistent with a guidance standard, but the harness must not expect a block to fire.
8. **Missing source_quote everywhere** — every equation + CR has `source_quote: null` (compounds the
   missing PDF; SR-1 blocked). Standard-wide.

## Below-VA list
**ALL 46 headline nodes are below VA** — uniform cause: missing OWN PDF (SR-3). Highest-value on PDF
arrival (would become VA/standard_fixed once a page is quoted): `eq-1-parameter` (normativity call),
the 4 Clause-3 definitional enums (`fld-data-type-classification`, `fld-data-origin-category`,
`fld-specific-generic-category`, `fld-foreground-background-category`), the 8 §5 principle booleans
(`section-03`), and `fld-emission-removal-factor` (invariant #8 wants a source_page — unmet).

## Dead gates
**2** — CR-025, CR-026 (both `manual`). (CR-002 / CR-005 are always-true tautology gates — listed
separately as soft dead-gates, not counted in the hard-dead-gate total.)

## Missing-doc dependencies (out-of-library, in_library:false)
- **ISO 14050** (vocabulary, normative ref §2/§3) → consumed by CR-004 (boolean attest → degrades to VC).
- **relevant ISO 14000-family V&V standard** (§6.4.2.2) → consumed by CR-026 (already a dead `manual` gate).
Neither is a value transfer, so neither hard-NRs a computation; the missing OWN PDF dominates.

## Decision-points staged for Alvaro (never guessed)
Eq.1 normative-vs-example; CR-019 gate existence; CR-025/026 manual conversion; CR-002/005 tautology;
`parameter_value` derived-vs-input (#22); §5 principles modal (should) binding-vs-advisory.

## Tier
**acquisition-blocked** — the standard is gated on a not-in-library document: its OWN rendered PDF is
missing, so every value/equation/CR node is VC-capped and nothing can reach VA. Additionally two CRs
depend on out-of-library ISO 14050 / an ISO 14000-family V&V standard. The encoding-layer defects
(2 dead gates, 2 tautology gates, CR-019 equation-gate, #22 parameter_value, cross-sheet gates) are
real fix-first items, but they cannot be verified or fixed to VA without acquiring the PDF first →
acquisition is the primary blocker.
