# Wave-0 triage — DWA-M-1200-2

Merkblatt DWA-M 1200-2 — Wasserwiederverwendung Teil 2: Anforderungen an die weitergehende
Wasseraufbereitung (Gelbdruck/Entwurf, Juli 2025).

- Standard id: `89c47ec6-eee8-45e7-aa98-d23a404550d3` (prod `vadsmshzebefjreqcicl`, read-only).
- Reasoning map: `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\DWA-M-1200-2\`
  (`_index.md` + 50 node files).
- PDF: `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-1200-2\DWA-M_1200-2_GD.pdf` (101 physical pages,
  `pdftotext -layout` via scoop). **pdfStatus = text.**
- **Page offset derived THIS session from THIS pdf's own footer: PRINTED = PHYSICAL (offset 0).**
  Confirmed: phys p.2→printed 2, p.11→11 (§2 Begriffe), p.14→14 (§3 heading), p.22→22 (footer literal,
  §3.3.3 16-Proben body). NOT inherited from 138 (−2) or 1200-3.

## Encoding shape (prod)
- worksheet_templates ×19 (M12002-01..15 + 20/21/23/25).
- worksheet_sections ×171 (A–M boilerplate scaffold, ~9 per WS; no real content sections encoded).
- fields ×84 (**12 section-less**). M12002-08/20/21/23 have **0 own fields**.
- equations ×4 (ALL on M12002-05: Gl.(1) log10, Anhang C.2-1/2/3). All `needs_engineer_review`,
  **no source_page** in encoding, output_unit NULL.
- compliance_requirements ×15 (REQ-01..15).

## Node counts (50 total, excl. index)
section 19 · equation 4 · table 3 · CR 15 · document 5 (in_library:false) · decision-point 4.
Provenance (frontmatter tally, source of truth): **VA 15 · VC 17 · EV 13 · NR 5** → **belowVA = 35**.
data_class: standard_fixed 27 · derived 7 · engineer_input 10 · standard_range 5 ·
normative-as-input-reference 1.

## VA nodes (PDF-page-attested this session)
- Gl.(1) `log10_reduktion = log10(C_Zulauf/C_Ablauf)` — §3.3.2, **printed p.19**.
- Gl.(C.2) `10. Perzentil = MW – 1,282·SD` + `50. Perzentil = Median` + `LRV_i = log10(x_i/y_i)` —
  Anhang C.2, **printed p.75**.
- REQ-05 16-Proben ("…EU-Badegewässerrichtlinie 2006/7/EG … je 16 korrespondierende Proben …") —
  **printed p.22**.
- REQ-06 Klasse-A 10.Perzentil / B-1,C-1 50.Perzentil ≥ Leistungsziel — Anhang C.1, **printed p.74**.
- REQ-09 turbidity ≤ 2 NTU (Klassen A–C) — §5.2/Tab.3, **printed p.32** (also p.34/35/41).
- REQ-10 desinfektionsverfahren — §5.4.3, **printed p.40**.
- REQ-14/15 DIN 276 Kostenermittlung — §8.1, **printed p.57**.
- M12002-05, M12002-08, M12002-11, M12002-15 section nodes (attested via the CRs above).

## Findings by defect class
### Dead gates (4) — prose-condition / producer-less
1. **REQ-02** (M12002-03, warn) `alle Parameter <= Klassen-Grenzwert` — natural-language, no discrete
   parameter fields / Grenzwert constants → never fires.
2. **REQ-08** (M12002-08, warn) `Anzahl(Aufbereitungsstufen) >= Klassen-Mindest` — prose count, no
   countable field, no encoded per-class minimum → never fires.
3. **REQ-15** (M12002-15, warn, requires_attestation=true) `manual` — literal placeholder; attestation-
   only, non-firing for the harness (may be intentional DIN-276 sign-off — confirm).
4. **REQ-03** (M12002-13, block) `perzentil_konformitaet >= 90` — 90-% constant source-fixed but
   `perzentil_konformitaet` is a bare hand-entered number with **no producer** → no reachable
   computed-fail path (borderline dead / #22).

### Cross-worksheet gate topology (6)
- REQ-04 (05) ⇐ `leistungsziel_log10` (04).
- REQ-06 (03) ⇐ `perzentil_10/50_log10` (05) + `leistungsziel_log10` (04) + `wassergueteklasse` (02).
- REQ-09 (08) ⇐ `wassergueteklasse` (02) + `truebung_ablauf` (06, section-less).
- REQ-10 (08) ⇐ `desinfektionsverfahren` (11).
- REQ-12 (13) ⇐ `sekundaerdesinfektion_*` (14).
- REQ-13 (13) ⇐ `ausgleichsvolumen`/`speicherbauform` (14) + `notueberlauf`/`reinigungskonzept` (13) — split.
M12002-08 is **field-less** yet hosts REQ-08/09/10 — every input is cross-worksheet.

### #22 hand-enterable derived (4 + 1)
Gl.(1) `log10_reduktion`, C.2-1 `perzentil_10_log10`, C.2-2 `perzentil_50_log10`, C.2-3 `lrv_i` are
engine-computable outputs that ALSO exist as UI-enterable number fields on M12002-05. Plus
`perzentil_konformitaet` (REQ-03), a `derived`-class value that is hand-entered with no equation.

### Greedy-AND / chained IF-THEN (5)
REQ-06 (two chained IF-THEN joined by bare AND, no parens; classes B-2/C-2/D uncovered), REQ-07 (3-way
AND), REQ-09 (class-gated ≤2), REQ-11 (2-way AND; 30-min bound not source-attested), REQ-13 (IF>0 THEN
3-way AND consequent). Single missing conjunct → whole-panel undefined.

### F-4 var-vs-var non-enforcement (3)
REQ-04 `log10_reduktion >= leistungsziel_log10`, REQ-06 `perzentil ≥ leistungsziel`, REQ-03
`perzentil_konformitaet >= 90` (producer-less). Reduction/percentile side is derived-but-hand-enterable
and the target is a class-dependent input → both sides operator-editable, gate enforces nothing until the
derived side is made read-only.

### SR-2 ranges (surfaced OK, ratification pending)
`wassergueteklasse` A–D, `validierungsmonitoring_typ` (4), `messhauefigkeit` (4),
`desinfektionsverfahren` (8) — correctly encoded as engineer-selected enums (not auto-picked). Confirm
class-selection drives the correct Tab.3 limit set.

### Gelbdruck ratification (all 4 equations + Tab.3 numerics)
All equations `needs_engineer_review`, **no encoded source_page** (derived this session). Tab.3 log10
Leistungsziele + turbidity are DRAFT numeric values; Anhang C.2 is a worked method → **exemplary vs
binding** ambiguity. Ratify equation source_pages, C.2 normative status, and draft Tab.3 numbers.

## Below-VA list (35 = VC 17 + EV 13 + NR 5)
- **VC (17):** section-01, -02, -03, -06, -13; tab-02-gueteklassen, tab-06-monitoring;
  cr-01-req01, cr-05-req04, cr-06-req07, cr-11-req11, cr-13-req12; dp-prose, dp-crossworksheet,
  dp-greedy-and, dp-ranges-varvar, (tab-03 is VA). [count from frontmatter]
- **EV (13):** section-07, -09, -10, -12, -14, -20, -21, -23, -25; cr-03-req02, cr-08-req08,
  cr-13-req03, cr-13-req13. (No source_quote / no content rows.)
- **NR (5):** doc-eu-2020-741, doc-din-276, doc-eu-badegewaesser-2006-7, doc-dwa-m-1200-1,
  doc-dwa-regelwerk (all `in_library:false`).

## Missing-doc dependencies (5, all in_library:false)
EU 2020/741 (WasserWVVO), DIN 276 (Kosten im Bauwesen), EU-Badegewässerrichtlinie 2006/7/EG,
DWA-M 1200-1 (sibling part), DWA-Regelwerk misc. cross-refs.
**No CR is NR-capped**: each CR's numeric substance is restated in THIS standard's own PDF; the
referenced docs are comparative/method/context refs, not the governing table a gate reads. So the
harness is **not acquisition-blocked** on any of them.

## Tier: **fix-first**
Real encoding defects gate a meaningful harness run: 4 dead/non-firing gates (REQ-02/08/15 prose +
REQ-03 producer-less), 6 cross-worksheet gate topologies (3 on a field-less worksheet), 4+1
hand-enterable derived outputs, 3 var-vs-var non-enforcing gates, and a full Gelbdruck-draft equation set
with no encoded source_page. The standard PDF is in-library and the 5 missing docs cap only their own
document nodes (NR) without governing any gate — so NOT acquisition-blocked.
