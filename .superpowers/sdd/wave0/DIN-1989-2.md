# Wave 0 triage — DIN-1989-2 (Regenwassernutzungsanlagen Teil 2: Filter)

standard_id `150aa8bf-1ad2-4599-a413-8bf9031b898f` · prod `vadsmshzebefjreqcicl` (read-only)
PDF `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-1989-2\DIN-1989-2.pdf`
Reasoning map: `…\reasoning-maps\DIN-1989-2\` (39 nodes + _index)

## pdfStatus
`text` but **OCR-degraded** (mojibake: ü→Ã¼, ß→ÃŸ; no clean running footer). Printed
page numbers ARE recoverable (standalone digit lines beside the `DIN 1989-2:2004-08`
header — verified **p.7** at Tab.1/§5.3.1, **p.13** at §6.4.5) + the front-matter TOC
lists a printed page per clause. Page offset derived from THIS PDF (not inherited);
total 26 pages. VA nodes cite verbatim quote + printed page. Gl.(3) formula, Gl.(1)
formula, Tab.1, Tab.2 confirmed verbatim in body; the rest capped VC/NR with the gap
named (SR-3).

## Node tally (39)
sections 4 · equations 9 · tables 3 · CRs 17 · docs 4 · decision-points 2
provenance: VA 6 · VC 22 · NR 11 · EV 0 → **belowVA = 33**
data_class: derived 26 · standard_fixed 9 · engineer_input 4

## Findings by class

### Dead gates (2)
- **CR-16** (§3/§1, block) — `condition: TRUE`. Can never fail → inert block gate.
  Pure DIN 1989-1 cross-reference masquerading as enforceable. Also references
  out-of-library DIN 1989-1 → NR.
- **CR-17** (§5.1/§6.1, block) — `condition: DN IS NOT NULL`, but `DN` is
  `is_required:true` → never fails. Title/intent is the ">DN 200 Sonderregel"
  (self-declaration of Trennwirkung + Dauerbelastungs-Wirkungsgrad when no test
  method exists); the condition tests neither the >200 branch nor the documentation
  obligation. Dead + condition/title mismatch.

### Near-dead NOT-NULL gates (3) — block on a required field, only fails if blank
- **CR-01** `filtertyp IS NOT NULL` (filtertyp is required).
- **CR-11** `eta_hydr_unbel_doku IS NOT NULL` (field is required); does not reconcile
  documented vs measured eta_hydr (Gl.3).
- (CR-17 also in this shape.)

### #22 hand-enterable-derived (2) + F-4 var-vs-var
- **V_Rueck_A** / **V_Rueck_B** — `number` fields the engineer can hand-enter, yet
  each equals its own equation output (Gl.1 `Q*25`, Gl.2 `Q*2`). Gates **CR-03**
  (`V_Rueck_A >= Q*25`) and **CR-04** (`V_Rueck_B >= Q*2`) compare the field to the
  same product it is defined as → tautological if auto-computed, #22 if hand-entered.
  Staged to [[dp-01-vrueck-hand-vs-derived]] (min-volume-to-be-met reading vs
  materialized-derived reading — not guessed).

### Verdict-as-producer / phantom threshold (1)
- **CR-08** — §5.5/§6.5.3 require Trennwirkung **≥ 0,7**, but the gate tests a human
  boolean `filtertrennwirkung_nachgewiesen == true`, NOT the computed eta_Rueck_AB
  (Gl.7) / eta_C (Gl.9). The 0,7 threshold is never machine-enforced; the eta
  producers feed no gate. Staged to [[dp-02-trennwirkung-verdict-gate]].

### Provenance/source gaps (VC caps, per SR-3)
- Gl.(4),(5),(6),(7),(8),(9) source_quotes are the encoder's **paraphrase**, not a
  verbatim PDF transcription; §6.4.6/§6.5.1/§6.5.3.1/§6.5.3.2 bodies not re-read
  verbatim this run (OCR noise) → capped VC. GAP: verbatim re-read on p.14–16.
- **Tab.2** values partly OCR-garbled (2,5 %/1 % rows); **Tab.3** (Prüfstoff masses,
  §6.5.1) not encoded as any field/enum and not transcribed → VC + encoding gap
  (its masses are standard_fixed inputs to Gl.7/8/9).
- CR-06/07/09/12/14/15/17 body sentences present in encoding but not re-located
  verbatim in the OCR extract → VC; GAP named on each node.

## Below-VA list (33 nodes — VC 22 + NR 11)
NR (11): doc-din-1989-1, doc-din-1989-3, doc-din-en-12056-3, doc-din-en-iso-9001,
eq-gl1 (Q→12056-3), eq-gl2 (Q→12056-3), cr-03 (Q→12056-3), cr-04 (Q→12056-3),
cr-10 (→1989-3), cr-13 (→ISO 9001), cr-16 (→1989-1, also dead).
VC (22): section-01/02/04, eq-gl4/5/6/7/8/9, tab-03, cr-02/06/07/08/09/11/12/14/15/17,
dp-01, dp-02.

## Missing-doc dependencies (out-of-library → NR)
- **DIN 1989-1** — governing Begriffe (§3; CR-16).
- **DIN 1989-3:2003-08** — Behälter-Standsicherheit 4.4.4 (§5.7; CR-10).
- **DIN EN 12056-3:2001-01** — defines input `Q` (70 % Füllung, Gefälle 1 %, Tab.C.1);
  governs the Typ-A/B sizing chain Gl.(1)/Gl.(2) + CR-03/CR-04.
- **DIN EN ISO 9001** — WPK basis (§8.3; CR-13).
(Note: DIN 1989-1 IS separately encoded in prod as standard DIN-1989-1, but its
definitional rows are not rendered/quoted here, so it remains in_library:false for
verification purposes.)

## Tier: **fix-first**
Rationale: 2 dead gates + 2 #22 hand-enterable-derived + 1 verdict-as-producer + 3
near-dead NOT-NULL gates are concrete pre-harness fixes. The 4 missing docs cap
*provenance* (NR) on the sizing chain but do NOT block the gate logic from running —
the standard's thresholds (20 kg, 60 cm, ≥0,7) and efficiency calcs are self-contained
once the gate defects are repaired. Not acquisition-blocked. Two rulings
(dp-01 V_Rueck class, dp-02 Trennwirkung gate) must go to Alvaro before those two fix
areas can be applied.
