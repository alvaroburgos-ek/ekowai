# Wave 0 triage — DIN-EN-16941-2 (Vor-Ort-Anlagen für Nicht-Trinkwasser Teil 2: behandeltes Grauwasser)

standard_id `0f58d951-35ea-4bf5-ba42-1f733d9e0600` · prod `vadsmshzebefjreqcicl` (read-only)
PDF `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-EN-16941-2\DIN-EN-16941-2.pdf`
Reasoning map: `…\reasoning-maps\DIN-EN-16941-2\` (72 nodes + _index)
Edition: DIN EN 16941-2:2021-11 (Deutsche Fassung EN 16941-2:2021), 33 pages.

## pdfStatus
`text` — clean extraction, clean running footer. **Page offset derived from THIS pdf**
(not inherited): footer integer = form-feed index − 1 (ff[20] prints 19, ff[31] prints 30);
ff[2] = garbled CEN cover = printed 1. Every VA cites verbatim quote + PRINTED page.
**SR-3 residue:** `pdftotext -layout` cleanly extracts all PROSE and every equation SYMBOL
LEGEND, but COLLAPSES the two equation BODIES (Gl.1 → stray `D` on p.19; Gl.2 →
`U U WM WM ) + mi` on p.20). So the equation defining-sentences + legends are VA, but the
assembled `formula` STRINGS cannot be char-for-char PDF-confirmed → both equations capped **VC**
on the formula string. Named per node; not a defect.

## Node tally (72)
sections 5 · equations 2 · tables 5 · CRs 19 · documents 12 (all in_library:false) ·
decision-points 6 · value/field nodes 23
provenance: VA 30 · VC 21 · NR 21 · EV 0 → **belowVA = 42**
data_class: engineer_input 34 · standard_range 8 · standard_fixed 4 · derived 3 ·
normative-as-input-reference 2

## Findings by class

### Dead gates (0)
Every CR has a defined `fired_by` input field that EXISTS (no phantom-field gate), is reachable,
and has a real fail path (`== false` / wrong enum / inequality-fail). No `condition: TRUE`, no
NOT-NULL-on-required tautology. This encoding is materially cleaner than DIN-1989-2.

### greedy-AND-of-presence (2)
- **CR-12** (§6.1, block) — `Y_G IS NOT NULL AND D_G IS NOT NULL AND bemessungswert_massgebend
  IS NOT NULL`. Three presence checks; does NOT enforce `bemessungswert == min(Y_G,D_G)` nor the
  "≤50 % Tagesbedarf" storage rule. Staged [[dp-cr12-bemessungswert-22]].
- **CR-17** (§11, block) — `probenahmestelle_im_verteilsystem IS NOT NULL AND bewertung_status
  IS NOT NULL`. Passes even when `bewertung_status = 'rot'` (Tab.D.3 = "Nutzung ausschließen").
  Should assert `== gruen` / `!= rot`. Staged [[dp-cr17-greedy-and]].

### #22 hand-enterable-derived (1)
- **bemessungswert_massgebend** — §6.1 makes it `min(Y_G, D_G)` (a derivation) but it is encoded
  as a free hand-enterable `number` field. Should be read-only `derived` with compute trace.
  Mapped as `normative-as-input-reference` (normative §6.1 + consumed by WS-02/WS-04). Tied to CR-12.

### F-4 var-vs-var non-enforcement (1)
- **CR-07** (§5.4.9, block) — `ueberlauf_kapazitaet >= zufluss_kapazitaet`; both engineer_input,
  neither derived → self-satisfiable. No formula in the standard to derive either (SR-1: no source).
  Staged [[dp-cr07-var-vs-var]].

### verdict-as-producer / enum-verdict-as-gate (2)
- **CR-15** (§9, block) — `querverbindungstest_ergebnis == bestanden`. CORRECTLY enforcing
  (asserts the pass value), but the verdict is self-attested (no upstream producer). Noted only.
- **CR-17 / bewertung_status** — the Tab.D.3 Ampel verdict feeds CR-17 (see greedy-AND above);
  its thresholds are BS 8525-derived (out of library).

### self-attested boolean gate (14 — well-formed, noted not fixed)
CR-01/02/03/04/05/06/09/10/11/13/14/16/18/19 are `x == true` attestations. Structurally valid,
reachable, fail path = false. Representative node: [[cr-01-getrennte-sammlung]]. NOT dead gates.

### informative-annex normativity ceiling (2 decision-points)
- **Anhang A** (Tab.A.1/A.2/A.3, printed p.25) is **(informativ)** — the example ranges in the
  field descriptions (5–15 l/min, 70–200 l, 3–8 l/Spülung) are exemplary, not binding → VC /
  standard_range. [[dp-anhang-a-informative]]. Do NOT force to standard_fixed/VA.
- **Anhang D** (printed p.29) is **(informativ)** and defers to **BS 8525** → all quality-parameter
  value nodes cap VC/NR. [[dp-anhang-d-informative]].

### missing equation for a stated branch (1 decision-point)
- `berechnungsverfahren` enum {vereinfacht, differenziert} (§6.2.1): only **differenziert**
  (Gl.1/Gl.2) is encoded. The **vereinfacht** path (§6.2.3, Wohngebäude, Tab.A.1) has NO encoded
  equation → choosing it leaves Y_G/D_G unbacked. [[dp-berechnungsverfahren-branch]].

## Below-VA list (42 nodes — VC 21 + NR 21)
**NR (21):** the 12 out-of-library document nodes (doc-bs-8525, doc-en-1717, doc-en-13076,
doc-en-13077, doc-en-12056-2, doc-en-12056-5, doc-en-806-4, doc-en-1610, doc-en-12566-3,
doc-en-13564-1, doc-en-476, doc-iso-7010, doc-en-iso-4064 — clustered) + the 8 BS 8525-derived
quality value nodes (ecoli_kbe, enterokokken_kbe, gesamt_coliforme_kbe, legionella_kbe,
truebung_ntu, ph_wert, rest_chlor, rest_brom) + dp-anhang-d.
**VC (21):** section-01 (registration, no printed number); eq-gl1, eq-gl2 (formula-string collapse);
tab-a1, tab-a2, tab-a3 (Anhang A informativ), tab-d1, tab-d3 (Anhang D informativ→BS 8525);
bewertung_status; the Anhang-A-ranged Gl.1/Gl.2 input value nodes carried as informative-bounded;
dp-anhang-a. (VA 30 = the 19 CR clause quotes reachable at VA + 3 section clause quotes +
eq defining-sentences + §6.1/§5.2/§7/§8/§10/§12 verbatim.)

## Missing-doc dependencies (out-of-library → NR; none block gate logic)
- **BS 8525** (series) — Anhang D quality Richtwerte + Tab.D.3 Ampel (printed p.29). Dominant cap.
- **EN 1717 / EN 13076 / EN 13077** — Kategorie-5 backflow + freier Auslauf AA/AB (CR-08, p.6/p.13).
- **EN 12056-2 / -5**, **EN 806-4**, **EN 1610** — collection sizing + tightness test (CR-15, p.9/p.22).
- **EN 12566-3** — storage material (p.7/p.10). **EN 13564-1** — overflow backflow (p.12).
- **EN 476** — access sizing with man-entry (p.11). **ISO 7010** — P005 marking (CR-14, p.22).
- **EN ISO 4064** (-1..-5) — water meters (p.15).
All cap *value/spec* provenance only; every CR gate condition is enforceable from the in-document
DIN clause (all VA-quotable), so the standard is NOT acquisition-blocked.

## Tier: **fix-first**
Rationale: 0 dead gates and a clean, well-sourced encoding, BUT three concrete pre-harness
enforcement fixes: (1) CR-12 greedy-AND + reclass `bemessungswert_massgebend` to derived min()
(#22); (2) CR-17 greedy-AND that passes on a 'rot' verdict → tighten to `== gruen`; (3) CR-07
var-vs-var (accept or add attestation). Three rulings to Alvaro before applying: dp-cr12 (is ≤50 %
storage a hard gate? modal "normalerweise"), dp-cr17 (gate the verdict vs acquire BS 8525),
dp-berechnungsverfahren (encode simplified eq or restrict enum). The 12 out-of-library docs cap
provenance only; not acquisition-blocked.
