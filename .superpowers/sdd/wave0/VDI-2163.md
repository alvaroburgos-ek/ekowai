# Wave 0 triage — VDI-2163

**Standard:** VDI 2163 (Innenraumlufthygiene – Raumluftqualität in Abfallbehandlungsanlagen durch
Raumlufttechnik; März 2006). prod standard id `7bc49735-932e-48a4-b795-fa97ed134af5`, code `VDI-2163`.
**Map:** `Obsidian/SecondBrain/01-Projects/ekowai-wizard/reasoning-maps/VDI-2163/` (64 nodes incl. index).
**PDF:** `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\VDI-2653\VDI-2163-2006-03.pdf` — pdfStatus=TEXT.
**Page offset:** printed = physical (offset 0), derived THIS session from the running header
`VDI 2163 –N–` (p.2→–2–, p.25→–25–, p.44→–44–). Not inherited.

## Number reconciliation (RESOLVED)
Encode report flagged folder `VDI-2653` vs source-file `VDI-2163`. The rendered PDF header carries
`VDI 2163` on every page → **VDI-2163 is VA**. Prod is encoded as `VDI-2163`. Folder name is stale.
No number invented. Not a finding — closed.

## Node counts
| type | count |
|---|---|
| index | 1 |
| section | 8 |
| equation | 0 (source has none; §4.4.2 states no design equations exist — nothing invented) |
| table | 4 |
| compliance_requirement | 32 |
| document (out-of-library) | 13 |
| decision-point | 6 |
| **total** | **64** |

| provenance | count |
|---|---|
| VA | 17 |
| VC | 13 |
| NR | 19 |
| EV | 15 |

**belowVA = 47** (nodes not at VA). **deadGates = 0.**

## Findings by defect class

### Dead gates (0)
No `condition = TRUE` / tautological gate. All 32 CRs reference at least one field. (Contrast
VDI-3477, which had 3 `condition=TRUE` dead gates.)

### Presence-only weak gates (3)  — `IS NOT NULL`
- **CR-28** `luftfeuchte_zuluftleitung IS NOT NULL` — title "Keine dauerhaften Feuchteniederschläge
  in Luftleitungen" (§5.2.8) but condition only checks the value EXISTS. Fires trivially; never
  enforces the no-condensate requirement. → [[dp-vdi2163-presence-gates]]
- **CR-29** `messparameter_mikroorganismen IS NOT NULL` (§6.2.1) — presence-only + method deferred to
  TRBA 405 → NR.
- **CR-30** `naehrboden_typ IS NOT NULL` (§6.2.3) — presence-only + genormte Analytik per external
  norms → NR.

### Greedy-AND (4)
- **CR-07** `unterweisungen AND wartungsplan AND hautschutzplan AND hygieneplan AND kennzeichnung`
  (§4.2) — 5 distinct booleans, one fail message.
- **CR-09** `staubemission_vermieden AND abgaserfassung_entstehungsstelle` (§4.3) — 2 requirements.
- **CR-20** `aussenluftdurchlass_pruefintervall <= 12 AND kammerzentrale_wasser_intervall <= 6 AND
  filterwechsel_intervall_stufe1 <= 12` (§5.3 / Tab.4) — 3 distinct intervals bundled.
- **CR-21** `rueckkuehlwerk_reinigung_intervall >= 2 AND gesamtkoloniezahl_umlaufwasser <= 10000`
  (§5.2.9) — frequency + concentration threshold bundled.
→ [[dp-vdi2163-greedy-and]]

### var-vs-var non-enforcement (F-4) (1) + enum-verdict (2)
- **CR-05** `schalldruckpegel <= beurteilungspegel` (§3.4) — both fields engineer_input; the real
  limit (Beurteilungspegel) lives in UVV Lärm (out-of-library). F-4 + NR. → [[dp-vdi2163-var-vs-var]]
- **CR-26** `zuluft_keimgehalt_verhaeltnis == kleiner_gleich_aussenluft` (§6.2.4) — enum whose value is
  itself a human verdict, not a measured number. → [[dp-vdi2163-enum-verdict]]
- **CR-27** `klima_grenzbereich IN {behaglichkeit,ertraeglichkeit}` (§6.3) — near-dead: the enum has
  exactly these two allowed values, so BOTH pass → the gate can never fail on a valid value.
  → [[dp-vdi2163-enum-verdict]]

### #22 hand-enterable-derived (0)
No equations → no derived fields → no #22 class. (Confirmed correct: [[dp-vdi2163-no-equations]].)

### inequality/enum/verdict-as-producer
N/A (no equations to produce values). The verdict-style constructs are all in CR conditions
(CR-26/CR-27, see above), not equation outputs.

### phantom-field gate (0)
Every CR condition symbol resolves to a prod `fields` row (checked against the 68-field set).

### SR-2 range not surfaced (1)
- **CR-10** `aussenluftstrom_pro_person >= 40` (§4.4.2) transcribes the ASR-5 range "40 m³/h bis
  60 m³/h je Person" but the gate pins the lower bound 40 as a hard floor. Per SR-2 the 40–60 range
  should be surfaced as an engineer selection rather than silently point-picked at 40. Flagged in
  [[dp-vdi2163-value-origin]]. (Not below-VA — the value itself is printed/VA; the flag is the
  range-selection mechanism.)

## Below-VA list (47) with reason
- **Sections (8, EV):** structural map nodes, not value claims.
- **Decision-points (6, EV):** staged rulings, no value.
- **Documents (13, NR):** out-of-library — no `standards` row (only VDI-2163, VDI-3477, VDI-3814
  exist). ASR 5, TRBA 211, TRBA 405, TRGS 403, BioStoffV, UVV Lärm, VDI 6022, DIN 33403, ISO 11731,
  EN 779/1822, DIN EN 12942, VDI 3802, LV 15.
- **CRs NR (6):** CR-03, CR-04 (BioStoffV), CR-05 (UVV Lärm), CR-06 (TRGS 403), CR-29 (TRBA 405),
  CR-30 (ISO 11731 / genormte Analytik) — requirement deferred WHOLESALE to an out-of-library doc.
- **CRs VC (11):** CR-02, CR-07, CR-08, CR-09, CR-13, CR-15, CR-17, CR-18, CR-19, CR-26, CR-27 —
  engineer_input presence/organisational gates whose substance is not a VDI-2163 printed threshold,
  or defers to a cross-ref not fully in-library. Verifiable against the markdown extraction (VC) but
  not a printed numeric value.
- **CR EV (1):** CR-28 (presence-only, no printed threshold to attest).
- **Tables VC (2):** Tab.1 (TRBA/TRGS reproduction), Tab.3 (Nährböden → external norms).

**VA (17), for contrast:** CR-01,10,11,12,14,16,20,21,22,23,24,25,31,32 + Tab.2, Tab.4 + index.
All numeric control values (TKW 5·10⁴, Endotoxine <50 EU/m³, Legionellen 1 KBE/ml, Gesamtkeimzahl
1000, Umlaufwasser 10000, Außenluftströme 40/60/65/+20, Querschnitt ≥1 m², Intervalle) are quoted
verbatim from VDI-2163's OWN printed pages this session → VA, modelled as
`normative-as-input-reference` (source_page + `references::` origin doc) where the value originates
in ASR 5 / TRBA 211.

## Missing-doc dependencies (13, none in library)
ASR 5 · TRBA 211 · TRBA 405 · TRGS 403 · BioStoffV · UVV Lärm · VDI 6022 · DIN 33403 · ISO 11731 ·
EN 779/1822 · DIN EN 12942 · VDI 3802 · LV 15.
NONE is acquisition-blocking for the compliance core: the numeric thresholds VDI-2163 enforces are
printed verbatim in VDI-2163 itself (VA). Missing docs only cap the 6 wholesale-deferral CRs at NR.

## Tier
**fix-first.** No dead gates and a VA-reachable numeric compliance core (WS-06/WS-08), so NOT
acquisition-blocked. But there are enforce-level defects to fix before harnessing: 3 presence-only
gates (CR-28/29/30), 4 greedy-AND bundles (CR-07/09/20/21), 1 F-4 var-vs-var (CR-05), 1 near-dead
enum-verdict (CR-27), and 1 SR-2 range-not-surfaced (CR-10). All staged as decision-points; none
auto-fixed (findings-over-fixes).
