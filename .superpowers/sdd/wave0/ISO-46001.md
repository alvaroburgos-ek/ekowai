# Wave 0 triage — ISO-46001 (Wassereffizienz-Managementsysteme)

standard_id `f5f134ee-4285-4755-84c0-8ffc9040189b` · prod `vadsmshzebefjreqcicl` (READ-ONLY)
map: `C:\Users\Ekowai\Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-46001\`
Date 2026-07-24 · read-only, no prod writes, no fixes, no harness.

## PDF STATUS = MISSING (acquisition-blocked)
ISO 46001 is NOT in `C:\Users\Ekowai\Desktop\Circular economy, sustainability and water test\`
(dir listed this session; doctrine listed it as expected but the file is absent). No
`Sweep Guidelines PDF\ISO-46001.pdf`. Per **SR-3**, VA is unreachable for every node; value/
equation/standard_fixed nodes are capped at VC/EV with the gap named. This standard is
**acquisition-blocked**: it cannot be promoted past VC until the rendered PDF is placed and each
equation + NULL-quote CR is re-read for a verbatim quote + printed page.

## Encoding shape (from prod)
- worksheet_templates ×10 (ISO-46001-01..10; Annex-SL phase 1→5)
- worksheet_sections ×81 · fields ×74 (30 attest-booleans, 1 enum, 16 WS08 Annex-C numerics)
- equations ×4 (all WS08 Annex C: C.1 balance, C.2b outflow, C.3 plant recycling %, C.5 process %)
- compliance_requirements ×40 (33 block + 7 warn)

## Node counts (map = 24 nodes)
index 1 · section 10 · equation 4 · CR-catalogue 1 · decision-point 8.
provenance: VA 0 · VC 18 · EV 6 · NR 0. data_class: standard_fixed 0 · derived 4 · engineer_input 70 (fields) · normative-as-input-reference 0.
belowVA = 24 (ALL — no PDF).

## Findings by class
- **#22 hand-enterable-derived = 4** — Win, Wout, plant_recycling_rate, process_recycling_rate are
  eq outputs (C.1/C.2b/C.3/C.5) stored as plain `number` engineer_input fields on WS08. Engineer can
  type a value contradicting the equation. (validator invariant #4)
- **inequality/verdict-as-producer = 4** — CR-017 `water_efficiency_indicator >= 0`, CR-018
  `baseline_water_efficiency_indicator >= 0` (near-dead: fails only on negative; presence-proxy, not
  the §6.2.6/6.2.7 obligation), CR-037 `Win == Wout` (balance identity used AS condition —
  tautological when engine-derived), CR-038 recycling `>=0` pair.
- **greedy-AND = 1** — CR-038 `plant_recycling_rate >= 0 AND process_recycling_rate >= 0`.
- **cross-worksheet gate (phantom-symbol) = 2** — CR-037 & CR-038 live on WS06 but read WS08-only
  symbols (Win, Wout, plant_recycling_rate, process_recycling_rate) → won't resolve under
  worksheet-local lookup → non-firing/phantom. Fix = move to WS08.
- **dead gate (manual condition) = 3** — CR-040 (§2/§3, WS01), CR-036 (§9.1, WS09), CR-039 (A.12,
  WS09) `condition='manual'`; never auto-produce a reachable-fail (all warn).
- **missing-source (source_quote NULL) = 16 CRs + 4 equations** — CR-004,005,011,017,018,021,023,
  027,029,031,033,035,036,037,038,039,040 (17 rows; the boolean/inequality/manual set) + eqs
  C.1/C.2b/C.3/C.5. These cannot reach VC-on-own-text; the 4 eqs + WS08 numerics sit at EV.
- **F-4 var-vs-var non-enforcement** — CR-037 `Win==Wout` is the only var-vs-var; it is a
  tautology when both engine-derived, and non-enforcing in practice because it lives on the wrong
  worksheet (cross-WS finding). Counted under inequality/verdict + cross-WS above.

## Dead gates (no reachable-fail) = 3
CR-040, CR-036, CR-039 (`condition='manual'`, warn). No `block` CR is literal-TRUE; no `block` dead
gate. (CR-017/018/037/038 are near-dead/tautological but technically have a reachable-fail, so not
counted in the strict dead-gate figure; flagged as inequality/verdict.)

## Below-VA list = ALL 24 nodes
Cause is uniform: **no ISO-46001 PDF in library** (SR-3 → VA unreachable). Ceiling VC for
clause/CR/formula-structure nodes; EV for the 4 equations + WS08 numeric worksheet (source_quote
NULL compounds the missing PDF).

## Missing-doc dependencies (out-of-library)
- **The ISO-46001 PDF itself** (primary acquisition gap). No cross-standard `references::` edge is
  encoded (unlike ISO-14004's ISO-14001/19011 markers), so **no dependent value node is capped NR by
  a foreign document** — everything is capped VC/EV by the missing OWN PDF. Annex A/C references are
  internal to ISO 46001.

## Tier: acquisition-blocked
Gated on a not-in-library doc (the ISO-46001 rendered PDF). Even setting the PDF aside, the
cross-worksheet CR-037/038 misfiling + #22 hand-enterable derived + manual dead gates are
**fix-first** items — but the dominant blocker is acquisition. On PDF arrival: re-verify 4 eqs +
enum + 16 NULL-quote CRs, then address the fix-first defects, then harness.
