# Wave-0 triage — ISO-14050 (Umweltmanagement — Begriffe / Environmental management — Vocabulary)

standard_id: `db1c8091-62b8-4f2e-82ea-8e7da44519dd` · prod `vadsmshzebefjreqcicl` (read-only)
version: 2020 (Fourth edition, ISO 14050:2020(E))
map: `Obsidian/SecondBrain/01-Projects/ekowai-wizard/reasoning-maps/ISO-14050/`
pdfStatus: **MISSING** (searched `C:\Users\Ekowai\Desktop\Circular economy, sustainability and water test\` for `*1405*` → 0 hits; not in Sweep drop)

## Encoding shape
- worksheet_templates ×13 (ISO-14050-01..13: 01 registration + 02..13 = definition clusters §3.1–§3.12);
  worksheet_sections ×78 (= 13×6 archetype scaffold: Purpose / Input / Core-Defined-Terms / Output /
  Notes / Workflow — NOT content-bearing clause sections); fields ×25; **equations ×0**; **tables ×0**;
  compliance_requirements ×6 (CR-001..CR-006).
- 25 fields = 12 `term__3_x` enum term-pickers (one per cluster §3.1–§3.12) + 13 numeric quantity fields.
- Nodes generated: **18** (13 section + 1 document + 4 decision-point). CRs enumerated within the
  section/DP nodes per the ISO-59004/ISO-59020 precedent (no per-CR files).

## Provenance / data_class
- **EV 17 · VC 0 · NR 1 (doc-missing) · VA 0.**
- data_class: engineer_input 13 (sections) · derived 4 (DPs) · standard_fixed 1 (doc-missing).
- No VA asserted; no source_page asserted — PDF absent (SR-3). Invariants #1/#2 respected by omission.
- **No VC either:** ISO-14050 carries ZERO `source_quote` values (all 6 CRs + all 25 fields have
  `source_quote = null`), so there is not even a convenience-extraction quote to grade VC. Whole standard
  sits at EV below the acquisition gate.

## Findings by defect class
- **phantom-numeric fields (13):** 13 numeric quantity fields collecting project VALUES in a pure
  vocabulary/Begriffe standard — `maturity_level` (§3.3.5), `product_system_value_indicator` (§3.5.6),
  `global_warming_potential` (§3.9.2), `co2_equivalent` (§3.9.3), `ghg_emission_factor` (§3.9.20),
  `ghg_removal_factor` (§3.9.24), `ghg_activity_data` (§3.9.31), `ghg_emission_reduction` (§3.9.17),
  `ghg_removal_enhancement` (§3.9.23), `water_footprint` (§3.10.1), `carbon_footprint_of_a_product`
  (§3.11.1), `monetary_value` (§3.12.7), `material_distribution_percentage` (§3.12.32). Standard has 0
  equations and 0 CRs reading them → encoding-archetype mismatch. → dp-iso14050-phantom-numeric-fields.
- **sentinel / non-resolving consumer edges (13, F-4-adjacent):** every one of those 13 numeric fields
  declares `consumer_worksheets = ["ALL_calculation"]`, a placeholder that resolves to no real worksheet
  → typed-link resolution FAILS for all 13. → dp-iso14050-sentinel-consumer-edges.
- **non-enforcing manual/warn CR surface (6 = ALL):** CR-001..CR-006 are ALL `condition='manual'`,
  `severity='warn'`, `source_quote=null`, `requires_attestation=false`. Zero block gates, zero
  machine-evaluable conditions in the standard. Defensible for a Begriffe standard (governance/
  interpretation notes) but ratify. → dp-iso14050-manual-warn-nonenforcing.
- **missing cross-standard edges (3, latent):** CR-005 (§3.4.48) names ISO/IEC 17000:2020 as owner of
  "certification"; CR-003 (§3.9.35) & CR-004 (§3.9.36) name ISO 14064-3 as owner of GHG
  verification/validation procedures — all prose-only, no encoded `references::` document dependency.
  Currently `manual` → no NR cap forced. Latent IF meant to bind. → dp-iso14050-missing-crossref-edges.
- **bounds gaps (2, minor):** `monetary_value` has NO `validation_rules` at all; `material_distribution_
  percentage` is a % field with only `>= 0` (no `<= 100` upper bound). Folded into phantom-numeric decision.
- **dead gate / eq-operator / greedy-AND / inequality-enum-verdict-as-producer / #22 hand-enterable-derived
  / F-4 var-vs-var:** NONE. Standard has 0 equations (no derived-value/verdict-as-producer/var-vs-var
  class) and 0 machine-evaluable CR conditions (every CR is `manual` → no eq/AND parse path, no dead
  block-gate).

## Below-VA list (all 18 nodes — PDF absent)
section-iso14050-01..-13 (13, EV); doc-iso-14050-pdf-missing (NR); dp-iso14050-phantom-numeric-fields,
dp-iso14050-sentinel-consumer-edges, dp-iso14050-manual-warn-nonenforcing, dp-iso14050-missing-crossref-edges
(4, EV). No VC-grade quote node exists (no `source_quote` anywhere). Nodes most needing the PDF to reach
VA: the 13 numeric concept fields (confirm they are definitions, not measured quantities) + the 12
`term__3_x` clusters (attach `source_page`).

## Dead gates
**0.** All 6 CRs are `manual`/warn by design (human attestation for a terminology standard); none was
meant to enforce a machine pass/fail, so none is a dead gate. No block-severity or machine-evaluable CR
anywhere.

## Missing-doc dependencies
- **ISO 14050:2020(E) PDF** — the standard's own rendered PDF, not in library → standard-wide EV cap
  (the acquisition driver). doc-iso-14050-pdf-missing (in_library:false).
- (latent) ISO/IEC 17000:2020 — named in CR-005 prose only; no encoded dependency edge.
- (latent) ISO 14064-3 — named in CR-003/CR-004 prose only; no encoded dependency edge.

## Tier
**acquisition-blocked** — all 18 nodes below VA; the missing own-PDF is the standard-wide provenance cap
(ledger convention: PDF-missing → acquisition-blocked, cf. ISO-59004, ISO-46001, ISO-59014, ISO-14033).
Real fix-first DATA-track debt exists and is PDF-independent (13 phantom-numeric-field demotions/drops +
13 `["ALL_calculation"]` sentinel-consumer resolutions + 2 bounds gaps + optional CR modal-verb
promotions) — batch to Alvaro regardless — but the harness-blocking driver is PDF acquisition.
