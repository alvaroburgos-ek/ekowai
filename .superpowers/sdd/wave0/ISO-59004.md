# Wave-0 triage — ISO-59004 (Kreislaufwirtschaft — Terminologie, Grundsaetze und Leitfaden fuer die Umsetzung)

standard_id: `98737198-0988-49c6-80cb-bb077ff29fb1` · prod `vadsmshzebefjreqcicl` (read-only)
map: `Obsidian/SecondBrain/01-Projects/ekowai-wizard/reasoning-maps/ISO-59004/`
pdfStatus: **MISSING** (not in `C:\Users\Ekowai\Desktop\Circular economy, sustainability and water test\`, `*590*`/`*59004*` → 0 hits; not in Sweep drop)

## Encoding shape
- worksheet_templates ×6 (ISO-59004-01..06); worksheet_sections (generic A/B/.. scaffolds + §-mapped);
  fields ×33; **equations ×0**; **tables ×0** (§6.7 "Table 1" named in a section title, not encoded);
  compliance_requirements ×44 (CR-001..CR-044).
- Nodes generated: 10 (6 section + 1 document + 3 decision-point). CRs enumerated within section/DP
  nodes per the ISO-59020 precedent (that sibling map also did not emit per-CR files).

## Provenance / data_class
- EV 8 · VC 1 (section-04, carries the sole CR `source_quote`) · NR 1 (doc-missing) · **VA 0**.
- data_class: engineer_input 6 · derived 3 (DPs) · standard_fixed 1 (doc-missing).
- No VA asserted; no source_page asserted — PDF absent (SR-3). Invariants #1/#2 respected by omission.

## Findings by defect class
- **dead gate / eq-operator (10):** CR-002, CR-004, CR-005, CR-013, CR-017, CR-018, CR-027, CR-028,
  CR-032, CR-042. `eq` is NOT in `src/lib/compliance/evaluate.ts` (equality = `==`/`!=` only). `X eq true`
  tokenizes `aref(X)` + leftover `eq true` → `Parser.parse()` fails length check → `manual` → never fires.
  Verified against the parser source this session.
- **phantom-field / cross-sheet gate (3):** CR-003 (`ce_vision_statement IS NOT NULL`), CR-004, CR-005 —
  attached to ws-01 but read fields OWNED by ws-03. Worksheet-local resolution → CR-003 fires false-fail
  for a value entered on the correct sheet; CR-004/005 compounded with the eq dead-gate. ws-03 itself has
  0 CRs.
- **greedy-AND (1):** CR-032 `reference_situation_assessed eq true AND baseline_circularity_assessment eq true`
  (also eq-dead).
- **non-enforcing manual/warn surface:** 25 CRs `condition='manual'` (warn). Only CR-015 is `block`
  (`hazardous_substance_risk_approach == true`, §5.3.4) and it parses/fires — the sole live gate.
  Manual CRs are non-enforcing-by-design (attestation), NOT counted as dead gates.
- **missing cross-standard edge (2, latent):** CR-043 names ISO 59020, CR-044 names ISO 59010 as prose;
  no `references::` document dependency encoded. Currently `manual`, so no NR cap is forced. Flag if these
  are meant to bind.
- **inequality/enum/verdict-as-producer / #22 hand-enterable-derived / F-4 var-vs-var:** NONE — the
  standard has no equations, so no derived-value gates, no verdict-as-producer, no var-vs-var class.

## Below-VA list (all 10 nodes — PDF absent)
section-iso59004-01, -02, -03, -04 (VC, CR-015 quote), -05, -06; doc-iso-59004-pdf-missing (NR);
dp-iso59004-eq-operator, dp-iso59004-phantom-field-gates, dp-iso59004-manual-warn-nonenforcing.
The single node most needing the PDF to reach VA: **section-04** (CR-015 §5.3.4 hazardous-substance
`source_quote`).

## Dead gates
**10** (eq-operator class). CR-015 = only live enforceable gate.

## Missing-doc dependencies
- **ISO 59004:2024 PDF** — the standard's own rendered PDF, not in library → standard-wide EV/VC/NR cap
  (the acquisition driver). [[doc-iso-59004-pdf-missing]]
- (latent) ISO 59020, ISO 59010 — named in CR-043/CR-044 prose only; no encoded dependency edge.

## Tier
**acquisition-blocked** — all nodes below VA; missing own-PDF is the standard-wide provenance cap
(ledger convention: PDF-missing → acquisition-blocked, cf. ISO-46001, ISO-59014). Real fix-first
DATA-track debt exists and is PDF-independent (10 `eq`→`==`, 3 phantom relocations, CR-032 split) — batch
to Alvaro regardless — but the harness-blocking driver is PDF acquisition.
