# Wave-0 triage — ISO-5667-6

Water quality — Sampling — Part 6: Guidance on sampling of rivers and streams
(title_de: Wasserbeschaffenheit - Probenahme - Teil 6: Anleitung zur Probenahme aus Fluessen und Baechen).
standard_id `2fcde791-8adb-4fab-bfb8-963a11edb24f`, prod `vadsmshzebefjreqcicl` (read-only).
Reasoning map: `Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\ISO-5667-6\`.
Date 2026-07-24. NO fixes, NO harness, NO prod writes.

## PDF status: MISSING (SR-3 gap — whole standard capped VC)
No rendered PDF in library. Doctrine ISO path is mistyped on disk
(`Ciruclar economy, sustanability and water test`); that folder holds only a **Spanish
convenience `.txt`** `ISO-5667-6-2015.txt` (270 639 chars, no `.pdf` sibling). Doctrine already
names ISO-5667-6 as the one inventory-time gap. Per SR-3: no PDF page ⇒ no VA anywhere; the six
printed numerics degrade from `standard_fixed` to `normative-as-input-reference` capped VC; value
nodes not located in the `.txt` cap EV. `source_page` empty on every node (gap named).

Values read verbatim in the `.txt` this session (→ VC): 30 cm above bed / 30 cm below surface
(§7); inlet velocity 0,5–3,0 m/s (§8.2); transport (5 ± 3) °C (§6); increment < 5 min (§10.8);
Annex A Eq.(A.1) `0,13 b² c (0,7c+2g)/(gd)` origin ISO 555-2, worked example 83 m; "al menos dos
sitios de muestreo" (§5.1.2).

## Node counts
- nodeCount = 108 (13 section + 2 equation + 8 notable value + 30 CR + 5 document + 5 decision-point
  + 45 remaining field/enum value nodes rolled up under their sections).
- Provenance: VA 0 / VC 24 / EV 79 / NR 5.
- belowVA = 108 (nothing reaches VA — SR-3 no-PDF ceiling).

## Findings by defect class
### Dead gates — non-parsing condition (14)  [dead=14]
Grammar supports `== != >= <= > < AND OR IS NOT NULL` only.
- `eq true` (never parses) ×11: CR-003, CR-005, CR-012, CR-013, CR-018, CR-019, CR-022, CR-023,
  CR-024, CR-026, CR-027.
- single `=` assignment ×1: CR-009 (`travel_time_method = '' OR travel_time_flows_count >= 5`).
- literal `manual` (placeholder, no expression) ×2: CR-007, CR-028.
→ [[dp-iso5667-6-dead-gates]]. None fire; a source-violating input passes silently.

### Cross-worksheet gate / phantom-field-in-worksheet (12)
CR reads a symbol owned by a different worksheet (gates are worksheet-local → symbol undefined):
CR-010, CR-011 (on -03, read -05 fields); CR-015, CR-016, CR-017, CR-018 (on -06, read -07/-08);
CR-029, CR-030 (on -11, read -12); CR-006 (on -03, own field but `is_required:false`).
Root cause: A/B/C/D/J/K sections are generic scaffold buckets, not §-subsections; CRs parked on
wrong worksheet in Pass3c. → [[dp-iso5667-6-cross-worksheet-gates]].

### #22 hand-enterable-derived (2)
- EQ-07-2 `sampling_depth_below_surface = preferred_subsurface_depth` — no-op rename; output is
  also a hand-enterable field on -07.
- Annex A `l` — `derived` output but a plain editable `number` on -13 (engineer can bypass Eq.A.1).

### F-7 range with no SR-2 selection (8)
inlet 0.5–3.0 (CR-017); transport 5±3 (CR-014); Chezy c 15–50 (Eq.A.1); bottle 50 ml–3 L;
depth ≥30 cm (CR-016); confluence ≥2 (CR-006); travel-time flows ≥5 (CR-009); heterogeneity ≥6
(CR-008). Enforced as inequality gates; no in-range selection field. → [[dp-iso5667-6-ranges]].

### Greedy-OR / optional-field vacuous-pass
CR-008/009/011/015/021/030 use `X != 'lit' OR Y`; blank/optional guard ⇒ dead THEN branch.
CR-006/016/017/021 gate on `is_required:false` fields ⇒ silent pass when blank.

### Modal-verb block-vs-warn (5 block CRs on "debería/should" text)
CR-001 (scope exclusion), CR-010, CR-011, CR-015, CR-030. Spanish `.txt` modal force ≠ normative
EN "shall/should"; needs PDF to finalize. → [[dp-iso5667-6-modal-verb]].

### Inequality/enum/verdict-as-producer
None as equations (only 2 equations, both arithmetic). CR-030 uses an enum verdict
(`risk_assessment_outcome != 'do_not_sample'`) as a gate input — fine as a gate, but the enum field
is engineer_input, not a producer; no F-4 var-vs-var non-enforcement found.

## Below-VA list (all 108)
Every node is below VA. Reason is uniform: SR-3 no-PDF ceiling (no derivable `source_page`).
- 24 VC: values/quotes read in the Spanish `.txt` (the 6 numerics, Annex A eq, dead-gate
  source_quotes CR-001/002/005/010/011/015/029/030, the 30cm/2-sites/5-min rules, plus section
  nodes whose content the `.txt` confirms).
- 79 EV: encoded-only fields/enums/attestation CRs with no `.txt`-confirmed scalar.
- 5 NR: nodes depending on out-of-library docs (below).

## Dead gates
14 (see above). deadGates = 14.

## Missing-doc dependencies (out-of-library → dependents NR)
- ISO 5667-3 (preservation/transport §11, CR-023) — [[doc-iso-5667-3]]
- ISO 5667-14 (QC/QA §12.3) — [[doc-iso-5667-14]]
- ISO 5667-23 (passive sampling, §1 exclusion, CR-001) — [[doc-iso-5667-23]]
- ISO 555-2 (Annex A Eq.(A.1) origin) — [[doc-iso-555-2]]
- ISO/IEC 17025 (§14 accreditation) — [[doc-iso-iec-17025]]
(ISO 5667-1 IS in library — encoded — so not a missing dep.)

## Tier: acquisition-blocked
Two compounding blockers: (1) **no rendered PDF** → nothing exceeds VC, VA-grade harness
impossible until an ISO 5667-6 PDF is acquired (SR-3); (2) 5 out-of-library referenced docs cap
key nodes NR. The 14 dead gates + 12 cross-worksheet gates are also fix-first, but the binding
ceiling is acquisition — even a full gate re-encode cannot lift the standard above VC without the
PDF. → [[dp-iso5667-6-no-pdf]] is the governing ruling.
</content>
