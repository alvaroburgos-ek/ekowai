# Wave-0 triage — DWA-M-179-1

**Standard.** DWA-M-179-1 — Dezentrale Anlagen zur Niederschlagswasserbehandlung, Teil 1:
Allgemeines sowie Einleitung ins Oberflaechengewaesser (Entwurf/Gelbdruck, September 2024).
Prod standard_id `6ca32ff4-0aa6-456d-8731-d77721e45637` (read-only, vadsmshzebefjreqcicl).
PDF: `C:\Users\Ekowai\Desktop\Guidelines\DWA-M-179-1\DWA-M_179-1_GD.pdf` (41 phys pages, text,
`pdftotext -layout`). **Page offset `printed = physical` (offset 0)** — derived this session from
the running footer (odd pages number far-right, even pages far-left; verified p.5/8/11/17/22/37).

Reasoning map: `…/reasoning-maps/DWA-M-179-1/` (_index + 76 node files).

## Counts
- nodeCount = 76 (17 section, 6 equation, 6 table/Bild, 33 CR, 8 document, 6 decision-point)
- provenance: VA=40, VC=26, NR=10, EV=0
- data_class: standard_fixed=24, engineer_input=24, derived=19, standard_range=7, normative-as-input-reference=2
- belowVA = 36 (all VC + NR nodes)
- deadGates = 9

## pdfStatus
text (searchable). Equations Gl.(1)–(5) and the §8.2/§8.3/§7.2 thresholds VA-pinned verbatim:
- Gl.(1) `rkrit = 0,1201 · e^(0,0655·ηhyd)` p.17 — matches encoding.
- Gl.(2) `ηhyd = ηges / ηBV` p.22 — matches.
- Gl.(3) `Qkrit = Ab,a · Ψs · rkrit / 10.000` p.23 — matches.
- Gl.(4) `Ased = 3,6 · Qkrit / qA,max` p.23 — matches.
- Gl.(5) `AF = Ab,a · 0,01` (1 %) p.23 — matches.
- §8.2: AFS63 ≤ 280 kg/(ha·a); ηges ≥47 % (Kat II) / ≥63 % (Kat III); Ψs=1; Kat-I-Vermischungsverbot — p.22–23.
- Fall-4 Mindestabfluss 5 l/s (DWA-A 166) — p.18. Tab.6 Summe ≤1 / ≥3 — p.25. ηhyd<50 % "unguenstig" — p.15.
- Bild 4 regression `ηBV = a·e^(−b·qA,max)`: caption p.19–20 VA, but a/b coefficients are chart-read
  (not printed as closed numbers) → coefficients cap **VC**.

## FINDINGS by defect class

### phantom-field gate (host worksheet cannot resolve) — 1 systemic (16 CRs)
M179-08 ("Hydraulischer Wirkungsgrad") hosts REQ-08..23 (16 CRs) whose conditions read symbols owned
by OTHER worksheets: `r_krit`(M179-09), `Q_krit`(M179-10), `A_sed`/`throughput_deviation_pct`(M179-11),
`A_F`(M179-12), `treatment_method`/`flow_split_case`(M179-05), `q_A_max`/`v_F`/`filter_thickness`/
`filter_AFS63_max_load`/`sludge_storage_min`/`eta_BV`(M179-06), `A_b_a`/`Psi_s`(M179-02). Gate
evaluated worksheet-locally → cannot resolve on host. → dp-m179-phantom-field-gates.

### phantom field (enum-value literal materialised as scalar number field) — 6
`II`,`III` on M179-03; `sedimentation`,`filtration_oberflaeche`,`fall_4`,`vollstrom` on M179-08.
No clause_reference/unit/consumers — importer condition-parser created fields for enum-value RHS
literals. → dp-m179-phantom-enum-fields. (fields table = 100 rows incl. these 6 junk.)

### dead gate (no machine-evaluable fail) — 9
- REQ-03 `lookup(Tab.3, catchment, target_compartment) matches selected rule` — lookup prose.
- REQ-11 `… OR r_krit matches Tab.5 lookup` — lookup disjunct (the abs(...)<tol conjunct also F-4).
- REQ-21 `… OR reduced per pre-treatment` — prose disjunct.
- REQ-28 `monitoring_planned IN {True, False}` — TAUTOLOGY, always passes.
- REQ-29 `Betriebsanweisung prepared` — attestation prose (defers DWA-A 199, NR).
- REQ-30 `funktionspruefung_done == True AND betriebsanweisung_status == ready` — RHS value `ready`
  is NOT in the `betriebsanweisung_status` enum {not_started,in_progress,draft,final} → gate can
  never be true (enum-value mismatch → effectively dead-block).
- REQ-31 `Waste disposal pathway documented per DWA-M 369` — attestation prose (defers DWA-M 369, NR).
- REQ-32 `Dynamic cost-comparison performed per KVR-Leitlinien` — attestation prose (defers KVR, NR).
- REQ-33 `Climate classification noted in project record` — attestation prose, no field.
(REQ-29/31/32/33 carry requires_attestation=true so are acceptable AS attestation gates; still
non-enforcing as automated booleans. REQ-03/11/21/28/30 are meant to enforce but do not.)
→ dp-m179-dead-prose-gates.

### greedy-AND / unparenthesised IF-THEN → vacuous PASS on guard-false — 11
REQ-05/06/07/13/14/16/18/19/20/22/23 use `IF guard THEN req` (REQ-14/18 also `THEN a AND b`) with no
parenthesisation. Under greedy parseOr the guard-false branch = vacuous PASS (FLLNT-03/M363 class).
Plus REQ-26 partitions site_factor_sum as ≤1 | (2 ungated) | ≥3. → dp-m179-greedy-and.

### F-4 var-vs-var non-enforcement (undeclared tolerance) — 5
REQ-10/11/12/17/21 encode `abs(x − <formula>) < tol` where `tol` is NOT a declared field on any
worksheet → residual-equality check cannot evaluate deterministically. → dp-m179-tol-and-verdict.

### #22 hand-enterable-derived (single-source violation) — systemic
Gl.(1)–(5) outputs re-declared as editable `is_required number` fields: M179-08 `eta_hyd`,
M179-09 `r_krit`, M179-10 `Q_krit`, M179-11 `A_sed`, M179-12 `A_F`; plus `_ref`/`_target`/`_summary`/
`_final` duplicate editable copies on M179-07/09/10/13/15. Validator invariant #4. → dp-m179-hand-enterable-derived.

### verdict-as-producer
M179-17 `compliance_verdict` (enum) and `eta_ges_target_met` (boolean) are hand-entered, not derived
from the block-gate outcomes → editable verdict can contradict gate results. → dp-m179-tol-and-verdict.

### SR-2 range never auto-picked (F-7) — 4
`q_A_max` (Bild 4, 1–10 m/h), `v_F` (0,05–2,5 m/h), `regression_a_AFS63`/`regression_b_AFS63` and
`eta_BV` (Bild 4 curve) are point values inside printed ranges/charts with NO selection record.
→ dp-m179-tol-and-verdict / eq-m179-bild4-etabv.

### clause_reference drift
- REQ-08 (`eta_hyd >= 50`) cites §6.1, but the 50 % floor is printed §7.1/Bild 2 (p.15).
- REQ-09 (`eta_hyd <= 100`) cites §8.2 — definitional bound, not a printed rule.

### invariant #2 self-flag (standard_fixed without source_page)
REQ-09, REQ-16, REQ-19, REQ-20 classed standard_fixed but exact printed page not pinned this session
(VC). Findings until page-pinned or reclassified (REQ-09 likely definitional not standard_fixed).

## BELOW-VA list (36)
- Sections VC (7): M179-01, 06, 07, 13, 15, 16, 17.
- CRs VC (10): REQ-03, 09, 15, 16, 18, 19, 20, 27, 28, 30.
- CRs NR (4): REQ-29, 31, 32, 33 (defer to out-of-library docs).
- Bild-4 regression node VC (1): eq-m179-bild4-etabv (chart-read coefficients).
- Document nodes VC (2, in-library): doc-dwa-a-102-2, doc-dwa-a-138-1.
- Document nodes NR (6, out-of-library): doc-din-1986-100, doc-dwa-a-166, doc-dwa-a-199,
  doc-dwa-m-369, doc-kvr-leitlinien, doc-ristwag.
- Decision-point nodes VC (6): dp-m179-* (meta, not value-bearing).

## DEAD GATES (9)
REQ-03, REQ-11, REQ-21, REQ-28, REQ-29, REQ-30, REQ-31, REQ-32, REQ-33.

## MISSING-DOC dependencies (6 out-of-library)
- DIN 1986-100:2016 (Ψs = Cs definition; M179-1 prints Ψs=1 so REQ-24 stays VA, other Ψs values NR)
- DWA-A 166 (Fall-4 5 l/s justification; value printed in M179-1 → REQ-22 VA, DWA-A 166 detail NR)
- DWA-A 199 (Betriebsanweisung; REQ-29 → NR)
- DWA-M 369 (Entsorgung; REQ-31 → NR)
- KVR-Leitlinien (Kostenvergleich; REQ-32 → NR)
- RiStWag (Strassen in Wasserschutzgebieten; informative, no binding gate)
In library (NOT missing): DWA-A-102-2 (governing parent), DWA-A-138-1.

## TIER
**fix-first.** No acquisition blocker on the core dimensioning chain — the equations and the §8.2/§8.3
thresholds are all VA against M179-1's own printed text (DWA-A-102-2 governing content is reprinted in
M179-1 and the parent is in library). The missing out-of-library docs gate only the operational
attestation CRs (REQ-29/31/32/33), which are attestation-typed anyway. Blocking issues before harness:
(1) the M179-08 phantom-field gate topology (16 CRs read foreign symbols); (2) 6 phantom enum-value
scalar fields; (3) 9 dead gates incl. REQ-30 enum-value mismatch (`ready` not in enum) and REQ-28
tautology; (4) greedy-AND on 11 IF-THEN CRs; (5) undeclared `tol` on 5 residual-equality gates;
(6) #22 hand-enterable derived outputs + hand-set compliance_verdict; (7) SR-2 unpicked ranges.
None are irreversibles; all are DATA-track fixes + rulings for Alvaro's batch. Not acquisition-blocked.
