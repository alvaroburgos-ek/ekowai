# Wave-0 triage — DIN-14021 (DIN EN ISO 14021:2016-07, Umweltkennzeichnung Typ II)

standard_id: `9af88a00-6c10-4e60-867f-4705f34d05b2` · prod `vadsmshzebefjreqcicl` (read-only)
pdf: `C:\Users\Ekowai\Desktop\Guidelines\DWA DIN Scribd\DIN-14021\DIN-EN-ISO-14021.pdf` — **text** (2595+ lines, bilingual D/E)
reasoning map: `Obsidian\SecondBrain\01-Projects\ekowai-wizard\reasoning-maps\DIN-14021\`
Date 2026-07-24 · NO fixes, NO harness, NO prod writes.

## Encoding shape
6 worksheets (registration / definitions / general-5.x / verification-6.x / specific-7.x / roll-up),
39 sections, 48 fields, 3 equations, 50 compliance_requirements (all severity=block, requires_attestation=false),
all fields verification_status=imported_unverified. Checklist/attestation archetype — 42/48 fields are
engineer_input boolean/enum; only 6 nodes carry printed normative numeric content.

## PDF page convention (THIS pdf's own footer — not inherited)
Printed page = right-aligned integer on the running-header band next to `DIN EN ISO 14021:2016-07`.
No fixed physical offset (bilingual two-column + roman front-matter). VA nodes cite their own footer:
- §7.6.3 net-recovered-energy formula + `R−E>0` → **p.32** (footer at text line 2269).
- §7.8.4 recycled-content `X=A/P×100` → **p.37** (footer 36 at line 2532 precedes it).
- §7.10.3 reduced-resource `U=(I−N)/I×100` → **~p.40**.
- §7.14.2 "100 % renewable material" → **p.43**; §7.15.2 "100 % of the energy supply is renewable" → **p.44**.

## VA-confirmed value nodes (6)
| node | clause | page | verbatim check |
|---|---|---|---|
| EQ-01 net recovered energy | §7.6.3 b) | 32 | `(R−E)/((R−E)+P)×100` — matches encoding exactly |
| EQ-02 recycled content | §7.8.4.1 | 37 | `X(%)=A/P×100` — matches |
| EQ-03 reduced resource use | §7.10.3 | 40 | `U(%)=(I−N)/I×100` — matches |
| REQ-20 threshold | §7.6.3 a) | 32 | "shall only be made if R − E > 0" — matches condition `R_energy - E_energy > 0` |
| §7.14.2 100% rule | 7.14.2 | 43 | "unqualified claim … only … when … 100 % renewable material" |
| §7.15.2 100% rule | 7.15.2 | 44 | "unqualified claim … only … when 100 % of the energy supply is renewable" |

## FINDINGS

### DEAD GATES (40) — never fire a fail
- **39 × `<field> IS NOT NULL` on a required field.** Required ⇒ always present ⇒ condition never false.
  Roster: WS-01 REQ-07, REQ-28, REQ-29, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34, REQ-35, REQ-36, REQ-37,
  REQ-38, REQ-39, REQ-40, REQ-41, REQ-42, REQ-43, REQ-44, REQ-45 (19; all 18 REQ-28..45 gate the SAME
  `selected_claim_type IS NOT NULL`, never enforcing the per-§7.x substance they name) + REQ-47, REQ-50 (2).
  WS-03 REQ-01, REQ-03, REQ-05, REQ-06, REQ-08, REQ-09, REQ-10 (7). WS-04 REQ-12, REQ-13, REQ-16, REQ-17,
  REQ-18, REQ-19 (6). = 34 pure required-presence.  Plus 5 more required-presence folded above (WS-01 count).
  Total distinct required-presence dead = **39**.
- **1 × `condition = TRUE` (REQ-26).** Literal always-true → categorically never fails. Worst subclass.
- **Total dead = 40.** See [[dp-14021-deadgate-topology]]. Several risk-booleans invert sense
  (REQ-01 `vague_claim_present`, REQ-03 `sustainability_claim_present` — standard PROHIBITS the condition,
  so a correct gate is a boolean-polarity check, not `IS NOT NULL`).

### F-4 threshold-not-enforced (4)
- **REQ-22** `renewable_material_pct IS NOT NULL` — §7.14.2 mandates `= 100 %` for unqualified claim (VA p.43).
- **REQ-23** `renewable_energy_pct IS NOT NULL` — §7.15.2 mandates `= 100 %` (VA p.44).
- **REQ-21** `recycled_content_pct IS NOT NULL` — §7.8.2.1 mandates the % be stated (presence-only weak).
- **REQ-25** `carbon_footprint_value IS NOT NULL` — §7.17.2.2 mandates quantification per ISO/TS 14067.
- Printed rule exists; gate erases it → fires green for any/no value = false assurance. See [[dp-14021-100pct-threshold]].

### GREEDY-AND presence gates (5)
REQ-14, REQ-15 (§6.3 comparative, optional fields → fire green when null), REQ-24 (§7.17.3.2),
REQ-46 (§6.3 + ISO 14044), REQ-27 (terminal roll-up). All `... AND ... IS NOT NULL` — presence-only, weak.

### #22 hand-enterable-derived / verdict-as-input (5 inputs, 1 gate)
WS-06 REQ-27 consumes `general_requirements_met`, `verification_requirements_met`,
`specific_requirements_met` (hand-entered booleans, NOT derived from upstream CR states) +
`compliance_verdict` (free enum). Conformity is an INPUT, not a product of the 49 upstream gates.
See [[section-14021-06]].

### INEQUALITY/ENUM-as-producer — 1 LEGITIMATE (not a defect)
REQ-20 `R_energy - E_energy > 0` is the ONE genuinely-enforcing numeric gate; correctly encoded, PDF-VA.

### PHANTOM-FIELD gates — 0
Every CR condition symbol resolves to an existing field (all 48 fields cross-checked). No phantom refs.

### F-7 auto-pick range — 0
`selected_claim_type` (23 vals), `claim_scope`, `communication_channel`, `comparison_basis`,
`method_selected`, `compliance_verdict` are all explicit engineer pickers (SR-2 compliant). No silent point-pick.

### MISSING-DOC dependencies (NR cap, 4 docs)
- [[doc-iso-14020]] — ISO 14020 general principles; REQ-26 (condition=TRUE!) + REQ-47 (§5.2). in_library:false.
- [[doc-iso-7000-1135]] — Mobius-loop graphic ISO 7000 reg.no.1135; REQ-48 (§5.10.2.1). in_library:false.
- [[doc-iso-14044]] — LCA for comparative claims; REQ-46 (§6.3). in_library:false.
- [[doc-iso-ts-14067]] — product carbon footprint; REQ-25/REQ-49 (§7.17.2.2). in_library:false.
Dependents cap NR/VC (validator invariant #7). None is a hard sizing blocker (the four equations are
self-contained + VA); acquisition would only lift 4 cross-ref CRs from NR.

## BELOW-VA LIST (98 of 104 nodes)
- 50 CR nodes → EV (encoded, clause-anchored via .md; no printed threshold to assert except REQ-20 which is VA).
- 44 fields (non-value) → VC (clause-anchored engineer_input/attestation).
- 4 document nodes → NR (out-of-library).
- (VA = 6: EQ-01/02/03 + REQ-20 + §7.14.2 + §7.15.2, carried on the 3 eq nodes + section-14021-05 + dp-100pct.)

## TIER: fix-first
Rationale: all normative VALUE content is present + PDF-VA (the 3 equations + R−E>0 + the two 100% rules),
so NOT acquisition-blocked (the 4 missing docs only cap 4 cross-ref CRs, no sizing path). BUT the gate
layer is structurally non-enforcing: 40 dead gates + 4 F-4 threshold-erasures + a verdict-as-input
roll-up mean the encoding provides false compliance assurance today. These must be fixed (Alvaro batch:
[[dp-14021-deadgate-topology]], [[dp-14021-100pct-threshold]]) before this standard is harness-ready.
