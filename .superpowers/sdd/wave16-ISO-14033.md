# Wave-16 — ISO-14033:2019 (Quantitative environmental information — Guidelines & examples)

**Ledger header** — model `claude-opus-4-8` (fable-5 orchestrated subagents) · CLI `2.1.218` ·
effort max · 2026-07-28 · worktree `_wt-fll` @ `feat/fll-revision` · prod `vadsmshzebefjreqcicl`.
Workflow run `wf_1e2d35d0-bdb` / task `wdf0wxkn9` · 3 agents · 255,267 subagent tokens · 77 tool calls.

Standard `fe3a798b-f09f-4929-9400-cdb321a9fa68` — 8 worksheets / 48 fields / **1 equation** / 27 CRs.
Source `ISO-14033-2019.pdf`, 73 pages, English, text layer. **Page offset = +6** (triple-confirmed p.1/11/23).

## HEADLINE — strong coverage; the richest fix set of the 3-run (re-homes + retags + prose + eq).

- **BIDIRECTIONAL COVERAGE strong** — every normative element YES except definitional-only §3.2 terms
  (no field, acceptable) and informative worked-example annexes A–E. 12+ field spot-checks FAITHFUL.
- **The 1 equation RAN** (proof mandate): `parameter_value = activity_data * emission_removal_factor`
  → `evaluateFormula` returns **computed, value 20** (5×4). Internally consistent (RHS symbols = declared
  inputs; LHS = output_symbol; no class-a mismatch). It is an INFORMATIVE Annex A.2.4 c) 1) example
  (p.23), normative basis §6.1.2.2.4 (p.11) — NOT the §6.2.3 it was tagged.

## SOURCE-SETTLED FIXES APPLIED (migration `20260728210000`, 4 guarded sections, rollback written)

1. **5 mis-homed WARN gates re-homed** (dead-reference class c; settled warn-split): CR-005/006/007 →
   ws02, CR-008 → ws03, CR-019 ws05 → ws06. Each operand's sole home confirmed by prod query.
   **Reproduction (real engine):** CR-005 pending→pass · CR-006 FAIL(red)→pass · CR-007 pending→pass ·
   CR-008 pending(8)→pass · CR-019 pending→pass(consistent)/fail(inconsistent). All warn → zero
   enforcement change. (CR-003 is genuinely cross-sheet — one operand ws01, one ws03 — NOT re-homed → T-1.)
2. **§6.2.3 misattribution retag** (dead-reference class c): the multiplication's real home is Annex
   A.2.4 c) 1) / §6.1.2.2.4, not §6.2.3 — corrected on the equation, the field emission_removal_factor,
   and CR-019 (`§6.2.3` → `§6.1.2.2.4, Annex A.2.4 c) 1)`).
3. **ws03 factual count**: "The **ten** guiding principles" → "**nine**" (the description enumerates nine
   and the standard prints nine, Clause 5.2–5.10).
4. **ws06 false-normative description**: dropped the claim it is "the only quantitative relationship
   stated in normative text" and corrected the clause "6.2.3 c) 1)" → "informative Annex A.2.4 c) 1)".

## MAP FIXES (write-back same wave)
- **rule-8 (3 ERRORS cleared)** — source_page backfills: doc-iso-14050 → p.1 (sole normative ref, VC→VA
  with verbatim); fld-emission-removal-factor → p.11/p.23; doc-iso-14000-vv → p.10.
- **rule-2 (2 WARNs cleared)** — eq node `produces:: [[fld-parameter-value]]` (a node-id) → `produces::
  parameter_value` (the DB output symbol; matches the `A_C (derived)` convention). DB was correct.
- eq-1-parameter freshened: clause retag, PDF-now-acquired, normative-vs-example DECIDED (informative),
  CR-019 re-home recorded, `fixed::` on all 4 nodes.
- Snapshot refreshed; validator **ISO-14033 0 errors** (2 warns = CR-025/026, the tracked rule-14
  manual class already on the sheet).

## WORKFLOW METRIC
- **1 equation: RAN** (computed 20). ws06 chain computes end-to-end at engine level; CR-019 identity
  gate fires both ways post-re-home. All 27 CRs warn → nothing blocks. 8 worksheets **expected to run**.
- **Deployed-build execution UNPROVEN** — element 7 owner-blocked (unchanged). Static + engine-level only.

## SIGN-OFF SHEET (Section T, +2 decisions → 79 open)
T-1 CR-003 cross-sheet gate (ruling) · T-2 doc-iso-14000-vv normative→informative reclass · T-3
CR-025/026 fold into corpus S-2 manual sweep · T-4 eq-1 #22 hand-enterable-derived (note).

## CONVERGENCE (close of the 3-standard run)
| state | count |
|---|---|
| **fully treated** | **0** of 71 (deployed-build proof still owner-blocked, element 7) |
| in progress | 18 (ISO-14033 advanced: coverage✔ eq-RAN✔ re-homes✔ retags✔ map✔; owed: deployed-build exec) |
| untouched | 53 |

## 3-RUN SUMMARY (waves 14–16)
ISO-14015 · ISO-59004 · ISO-14033 all advanced to "in progress, deployed-build-exec owed". 17 warn
gates re-homed corpus-total across the three (9+3+5), all engine-proven. 1 equation execution-proven.
Two reusable assets minted: the **warn-vs-block mis-home split** (settled) and **validator rule 14**
(dead-manual, 70-CR corpus class). Sheet grew 70→79. The per-standard cost fell each wave as the
patterns hardened (the flywheel working).

## RESUME
Re-rank the live queue and dispatch the next standard. Element 7 (authenticated deployed-build harness
on the campaign alias) remains the single blocker keeping the fully-treated column at 0 — owner-gated.
