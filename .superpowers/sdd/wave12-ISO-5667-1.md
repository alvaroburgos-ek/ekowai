# Wave-12 — ISO 5667-1 / NTC-ISO 5667-1:1995 (Water Sampling — programme design) — done

**Ledger header** — `claude-fable-5` · 2026-07-28 · run `wf_a6282f65-47b` (6 agents, 495k
tokens) · water-sampling technical std, final — fixes apply. Offset **printed = PDF − 1**
(unnumbered cover = PDF sheet 1). Edition confirmed NTC-ISO 5667-1:1995 = verbatim ISO
5667/1:1980 (DB version string faithful).

## Fidelity — strong
13+ field spot-checks all FAITHFUL. All **3 equations verbatim-verified**: eq3
`n=(2·K·σ/L)²` **computes 61.47**, matching the printed worked example (n≈61); eq1 `s` /
eq2 `L` transcribe faithfully but use `sqrt()`/`SUM()` → `manual_required` **by design** (the
engine has no such functions — a rewrite would be a ruling, not a source-settled fix).

## APPLIED (source-settled, execution-proven)
1. **CR-001 + CR-010 `eq`→`==`** (migration `20260728150000`). Both used the unparseable
   operator `eq` → dead gate (`manual` both ways); sibling CR-003 uses `==`. Probe:
   `eq`→manual/manual, `==`→pass(true)/fail(false). Both warn → zero blocking change; a dead
   advisory gate becomes live. Verified by query.
2. **dp-01-source-edition `source_page = 1`** — §1 scope/edition on printed p.1 (verified);
   cleared the `2.fixed-has-page` validator error → **standard validator-clean**.

## NOT ready-to-use — rulings (sheet O)
- **K-table not encoded (SEV-2, functional gap).** The printed confidence-level→K table (p.11,
  §16.4: 99→2,58 … 50→0,67) is not a `regulation_tables` entry; `K` is a free numeric input
  with no coupling to `confidence_level`. Capture + wire (like GAP-1 elsewhere).
- **Mis-homed statistical gates:** CR-024 (`L>0`) on ws-06 but `L` on ws-07 → permanently
  pending; CR-023 (`n>0`) similar. Re-home.
- **Safety-weakness (F-4):** CR-008/009/014/016 encode boolean `shall` safety requirements as
  `X IS NOT NULL`, so answering *false* still PASSES a **block** gate. Strengthen to `== true`
  (enforcement change → ruling).
- **Over-strong:** CR-012 makes `pipe_nominal_bore≥25` a **block** from a *"por ejemplo"*
  example; CR-016 applies the groundwater purge/depth block unconditionally across all 16
  situation types (should be guarded by `water_situation_type=groundwater`).

## Workflow metric (assessed)
ws-01 registration + ws-08 flow selection complete via presence gates; ws-07 partially computes
(n auto, s & L manual by design). Most block gates are presence-only → thin enforcement.
Deployed-build harness NOT run (element 7 owner-blocked) — UNPROVEN.

## Seven-element status
Bidirectional walk ✅ · equations ✅ (3/3) · tables — K-table gap (ruling) · gates both-ways ✅
· source-settled fixes ✅ (2 applied+proven) · map write-back ✅ (dp + 2 CR nodes) · deployed
harness ⏳ (element-7). Judgment: O-D1..O-D4 on the sheet.
