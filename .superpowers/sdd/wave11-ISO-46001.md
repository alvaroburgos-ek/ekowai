# Wave-11 — ISO 46001:2019 (Water Efficiency Management Systems) — audit complete, fix applied

**Ledger header** — `claude-fable-5` · 2026-07-28 · run `wf_c0b81fb3-6cc` (9 agents, 649k
tokens) · HLS management-system requirements + water-balance computation, final — fixes apply.
Offset (arabic body) **printed = PDF − 10**, high confidence; roman front = PDF − 2.

## The strong half — coverage essentially complete
Every printed shall in the normative HLS clauses 4–10 maps to a field and, for block-worthy
items, a CR, with **severities correctly matching the modal** (shall→block, should→warn).
Clauses 1–3 + Annexes A/B/D informative, correctly held as notes/refs. One minor coverage
gap: §7.5.2 (creating/updating documented info) folded into a generic field — judgment item.

## APPLIED (source-settled, execution-proven in-session)
**Eq C.1 (Win) fix** (migration `20260728140000`). The row merged THREE printed statements
(`Win = WD+R1+R2+R3 ; Wout = O1+O2+O3+O4 ; Win = Wout`) into one formula — a `;`/multi-`=`
construct the engine can't evaluate, so **Win never computed**. Printed Formula C.2 (p.34):
`Win = Wout`, `WD+R1+R2+R3 = O1+O2+O3+O4` → `Win = WD+R1+R2+R3` (input side), structurally
determined. Probe: **manual_required → computed 135**. (The static audit predicted an `error`
on `;`; the real engine returns `manual_required` — my in-session probe is authoritative,
logged.) Wout has its own evaluable row C.2b; C.3/C.5 recycling rates faithful. Also stripped
the placeholder dangling wikilink → **standard now validator-clean**.

## NOT ready-to-use — rulings (sheet N)
Three mis-homed gates (operand on a different worksheet than the CR → worksheet-local lookup →
never evaluate):
- **CR-005** (`leadership_commitment == true`, §5.1, **block**) homed on ws01, operand on ws02
  → pending, **never blocks** (a real F-4 dead block gate).
- **CR-037** (`Win == Wout`) + **CR-038** (recycling ≥ 0) homed on ws06, operands on ws08 →
  pending. Also CR-037 hits the **bare-symbol-RHS always-fail trap** (`Win == Wout` → RHS
  becomes string `"Wout"`) — needs the subtraction form `Win - Wout == 0`. Both warn, so no
  enforcement lost, but the balance check never actually runs.

## Workflow metric (assessed, not harness-executed)
Management-system worksheets 01–07/09/10 expected to run (co-located operands, gates enforce).
WS-08 (Annex-C water balance): **Win now computes** after the C.1 fix (was broken); its two
balance gates stay dead until re-homed (sheet N). Deployed-build harness NOT run (element 7
owner-blocked) — UNPROVEN.

## Follow-up (minor)
Win/Wout/plant_recycling_rate eq outputs lack a `produces::` map-node link (eq-node coverage
WARN); §7.5.2 coverage fold.

## Seven-element status
Bidirectional walk ✅ · equations ✅ (4/4, C.1 fixed) · tables n/a · gates both-ways ✅ ·
source-settled fixes ✅ (applied+proven) · map write-back ✅ (C.1 + wikilink) · deployed
harness ⏳ (element-7 blocked). Judgment: N-D1 on the sheet.
