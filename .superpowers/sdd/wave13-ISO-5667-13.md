# Wave-13 — ISO 5667-13:2011 (Water Sampling — Part 13: sludge) — done

**Ledger header** — `claude-fable-5` · 2026-07-28 · run `wf_c696f18b-ef6` (6 agents, 407k
tokens) · sludge-sampling GUIDANCE standard (predominantly *should*), final — fixes apply.
Offset **printed = PDF − 6**. 8 ws / 46 fields / 3 eq / 23 CRs.

## Fidelity — strong
All **3 equations FAITHFUL**: `t = 60·m/(q·n)` (§6.1.3), `n = (1,96·s/E)²` (§6.1.4.2),
`n_sp = √V/2` (§6.3.6). n_sp uses `sqrt()` → `manual_required` **by design** (engine has no
sqrt — a rewrite would be a ruling).

## APPLIED (source-settled, execution-proven / verified)
1. **4× `eq`→`==`** (migration `20260728160000`): CR-007/008/021/023 used the unparseable
   `eq` → dead gates; all warn. Same class/proof as ISO-5667-1. Verified by query.
2. **§9 a) field relabel** (migration `20260728170000`): `sample_record_field` option a) was
   labelled "Sample identification"/"Probenkennung"; printed §9 a) (p.13, verified in-session)
   reads **"the name of the plant"**. Relabelled en→"Name of the plant", de→"Name der Anlage"
   (display only; stable value key preserved). Class (b) value contradicting the page.
3. **doc-iso-5667-10 reclassified** (map): was `standard_fixed` with no page on a *reference*
   to sibling ISO 5667-10 → reference-doc, `in_library:false`, `source_page=1` (§2 normative
   refs; also cited §3.5 p.2, §8 p.13). Cleared the validator error → **standard clean**.

## NOT ready-to-use — rulings (sheet P)
- **Mis-homed gates:** CR-004/006 (operands on ws-03, gate on ws-01 → permanent spurious
  warn-fail); **CR-015/016/017 (operands on ws-06, gate on ws-05)**. **CR-017 is a safety
  BLOCK → currently NON-ENFORCING** (F-4). Re-home.
- **CR-005 placeholder:** condition is the bare token `manual` → pending forever
  (missingSymbols:['manual']). Rewrite to a real expression or a manual-attestation node.
- **IS-NOT-NULL safety weakness:** CR-002 etc. gate boolean `shall` items as `X IS NOT NULL`
  → answering *false* still passes.
- **Severity over-escalation:** CR-001/003 are `block` drawn from guidance ("should"/"WARNING"),
  not a numbered shall.

## Workflow metric (assessed)
Registration/plan worksheets work via presence gates; ws-06 partially computes (n auto, n_sp
manual by design). Deployed-build harness NOT run (element 7 owner-blocked) — UNPROVEN.

## Seven-element status
Bidirectional walk ✅ · equations ✅ (3/3) · tables n/a · gates both-ways ✅ · source-settled
fixes ✅ (3 applied) · map write-back ✅ (doc reclassify + eq stamps pending in eq-node coverage) ·
deployed harness ⏳ (element-7). Judgment: P-D1..P-D4 on the sheet.
