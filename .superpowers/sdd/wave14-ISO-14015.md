# Wave-14 — ISO-14015:2022 (EASO — Environmental assessment of sites & organizations)

**Ledger header** — model `claude-opus-4-8` (fable-5 orchestrated subagents) · CLI `2.1.218` ·
effort max · 2026-07-28 · worktree `_wt-fll` @ `feat/fll-revision` · prod `vadsmshzebefjreqcicl`.
Workflow run `wf_e74db548-02e` / task `wna5catav` · 2 agents · 235,728 subagent tokens · 68 tool calls.

Standard `0d90e21d-28bd-4247-91e3-9bb3e89ec863` — 8 worksheets / 54 fields / 34 CRs / **0 equations**.
Source `ISO 14015-2022-BS-EN-ISO-Environmental-Management-Guidelines.pdf`, 38 pages, text layer present.
**Page offset = +10** (printed P = awk-page P+10; verified on '1 Scope'→footer, cross-checked pp.10/17/18).

## HEADLINE — a clean standard. Coverage complete, fields faithful, modal mapping correct.

- **BIDIRECTIONAL COVERAGE: COMPLETE.** Every printed normative element maps onto a worksheet:
  ws2=§4 Principles · ws3=§5.2 Roles · ws4=§5.3 Planning · ws5=§5.4 Info-gathering · ws6=§5.5
  Evaluation · ws7=§6 Reporting · ws8=§7 Competence (incl. Table 1). **No source→encoding gap.**
  Informative annexes A–D correctly surfaced through enums, not separately encoded.
- **MODAL MAPPING CORRECT.** Guidance standard: 0 `shall`, 80 `should`, 1 non-normative `must`.
  All 34 CRs at `severity=warn` is the RIGHT mapping — the usual over-block defect is **ABSENT**.
- **FIELD FIDELITY EXCELLENT.** 16 of 54 spot-checked (all 13 enum fields — highest risk — plus
  representative booleans): every one FAITHFUL, verbatim page backing, per-value
  regulation_references accurate. **Zero field defects.**
- **eq-SWEEP VERIFIED.** All 34 conditions: 30× `== true`, 5× `IS NOT NULL`, 1× `manual`. Zero
  residual `eq`. The `==` gates evaluate correctly (engine tokenizer → equals()). Confirmed by probe.

## SOURCE-SETTLED FIX APPLIED — mis-homed warn gates (dead-reference class c)

**Defect.** CR-002..007 reference operand fields on ws02 (Principles); CR-008..010 reference operand
fields on ws03 (Roles) — but all 9 were attached to ws01 (Registration). Engine lookup is
worksheet-LOCAL → operand never in ws01's value map → gate resolves `pending` **forever**. The
Principles+Roles conformance panel was functionally dead where displayed. CR-001 (`asset_identified`,
truly on ws01) is the control — correctly homed, untouched.

**Zero-interpretation basis** (prod field-home query, this session): each CR's sole operand field has
exactly ONE worksheet home; there is one and only one correct destination. No candidate choice.

**Reproduction — real `evaluateCondition`, this session (probe deleted after):**
```
operand ABSENT  (ws01 ctx):  evidence_verifiable == true  -> {pending, missing:[evidence_verifiable]}
operand PRESENT (ws02 ctx):  answered true  -> {pass}      answered false -> {fail}
```
Broken (permanent pending) BEFORE → working both-ways gate AFTER.

**Enforcement UNCHANGED** — all 9 are `warn`; warn never blocks. This is *distinct* from rule-13
mis-homed BLOCK gates (where re-home = enforcement change = ruling). Re-homing a warn gate only makes
the advisory display truthful → source-settled, not a ruling.

**Migration** `scripts/migrations/20260728190000_iso14015_rehome_cr002_010.sql` (guarded per-row to the
broken ws01 home, aborts unless exactly 9 rows). Rollback
`scripts/rollback-20260728190000-iso14015-rehome.sql`. Applied HTTP 201; verified: CR-002..007 on ws02,
CR-008..010 on ws03, CR-001 on ws01.

**Map write-back (same wave):** 9 CR nodes updated (owner_worksheet, condition `eq`→`==` drift fixed,
phantom-gate finding→RESOLVED, `gated_by::` re-pointed, `fixed::` appended); DP node
`dp-iso14015-phantom-gates` Option 1 marked applied + dangling `[[reference_wizard_compliance_gates]]`
wikilink (a memory ref, not a map node) stripped to plain text. Snapshot re-exported; validator:
**ISO-14015 0 errors / 0 warnings, 44 nodes.**

## WORKFLOW METRIC

- **0 equations** — no cross-worksheet compute chains. All 8 worksheets are self-contained
  data-collection/verification sheets → each **expected to run** end-to-end through the save path.
- **9 warn gates: engine-proven both-ways** post-fix (pending→pass/fail). 20 correctly-homed `== true`
  gates already both-ways (CR-001 representative). No worksheet is BLOCKED (all warn).
- **Deployed-build execution: UNPROVEN** — element 7 (authenticated deployed-build harness) remains
  owner-blocked (needs an owner-created campaign test user). Static + engine-level only this wave.
  Metric therefore honestly labelled "expected to run", not "ran", for the browser dimension.

## SIGN-OFF SHEET (judgment items — accumulate, never block)

1. **CR-028 dead `manual` placeholder** (§5.5.3, ws06). Engine parses `manual` as a truthy lookup of a
   nonexistent symbol → `pending` forever (proven). What §5.5.3 *should* check is not source-determined
   → needs a real condition or removal. **Ruling.**
2. **IS-NOT-NULL-on-boolean weakness** — CR-010/013/016/023/027. Proven: answering *false* still →
   `pass` (records presence, not conformance). Strengthening to `== true` is a modal reading → **ruling.**
3. **Cosmetic** — CR-005/CR-022/CR-031 `clause_reference` lack the `§` prefix. Trivial, deferred.
4. **Owner browser pass** — queued once element 7 unblocks: `[campaign alias] · ISO-14015 · check
   ws02/ws03 Principles+Roles gates now resolve to pass/fail after answering`.

## CONVERGENCE

| state | count |
|---|---|
| **fully treated** | **0** of 71 (deployed-build proof still owner-blocked, element 7) |
| in progress | 16 (ISO-14015 advanced: coverage✔ fields✔ gates-fixed✔ map✔; owed: deployed-build exec) |
| untouched | 55 |

ISO-14015 has every element EXCEPT the deployed-build execution proof (owner-blocked, not a defect of
the standard). Source-settled fix applied + map-written + validator green.

## RESUME
Next in the 3-standard run: **ISO-59004** (ISO/FDIS 59004:2024 Circular economy — Vocabulary/principles,
62pp bilingual EN/FR, source pre-staged at `scratchpad/iso59004.txt`), then **ISO-14033**.
