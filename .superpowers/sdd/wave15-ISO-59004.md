# Wave-15 — ISO/FDIS 59004:2024 (Circular economy — Vocabulary, principles & guidance)

**Ledger header** — model `claude-opus-4-8` (fable-5 orchestrated subagents) · CLI `2.1.218` ·
effort max · 2026-07-28 · worktree `_wt-fll` @ `feat/fll-revision` · prod `vadsmshzebefjreqcicl`.
Workflow run `wf_7256b3c4-f12` / task `wqsfdopwm` · 2 agents · 170,835 subagent tokens · 34 tool calls.

Standard `98737198-0988-49c6-80cb-bb077ff29fb1` — 6 worksheets / 33 fields / 44 CRs / **0 equations**.
Source `ISO_FDIS_59004_N.pdf` (IMANOR bilingual EN/FR copy of ISO/FDIS 59004:2024), 62 pages, text layer.
**Page offset = +7** (printed folio 1 on the 8th form-feed page; body = folio + 7; cross-checked at §4/§5).
Verified against the **English** column.

## HEADLINE — clean behavioural coverage; a recurring re-home + a corpus-wide 'manual' discovery.

- **BIDIRECTIONAL COVERAGE broadly complete** for normative BEHAVIOURAL content (Scope, six principles
  §5.2, §5.3 considerations, Clause 6 actions + Table 1 R-strategies, Clause 7). 12/12 field spot-checks
  FAITHFUL. **Zero field defects.** One real coverage gap: the **vocabulary** itself (below).
- **eq-sweep verified** — no `eq` remnants; all boolean gates `== true`; IS-NOT-NULL only on text/enum
  carriers (correct).

## SOURCE-SETTLED FIX APPLIED — mis-homed warn gates (phantom-field topology recurred as predicted)

CR-003/004/005 attached to ws-01 (Registration) but their operands (`ce_vision_statement`,
`systems_thinking_applied`, `principles_integrated`) all home on ws-03 (Vision). Worksheet-local lookup
→ CR-003 (`IS NOT NULL`) permanent **fail** (red), CR-004/005 (`== true`) permanent **pending**. ws-03
(the owning sheet) had zero CRs. **Reproduction (real engine, this session):** `ce_vision_statement IS
NOT NULL` absent→fail / present→pass; `systems_thinking_applied == true` absent→pending / present→pass.

**Migration** `20260728200000_iso59004_rehome_cr003_005.sql` (guarded to broken home ws-01, aborts
unless exactly 3). Rollback written. Applied HTTP 201; verified all 3 now on ws-03. Enforcement
unchanged (all warn). Per the wave-14 settled split, a warn mis-home is source-settled (not a ruling).

**Map write-back:** `dp-iso59004-phantom-field-gates` marked RESOLVED + `fixed::`; `section-iso59004-03`
phantom note → resolved + `gated_by::` added; both dangling `reference_wizard_compliance_gates`
wikilinks stripped. Snapshot refreshed; validator ISO-59004 **0 errors** (24 warns = the tracked
rule-14 manual class, all on the sheet).

## MAJOR CORRECTED FINDING (EXECUTED, refutes the subagent's static read)

The audit agent claimed the 24 `condition='manual'` CRs reach the engine's friendly `{kind:'manual'}`
bucket. **Probe refuted it:** literal `manual` → `{pending, missingSymbols:['manual']}` forever; only an
EMPTY condition yields `{kind:'manual'}`. So all 24 render as a broken "fehlend: manual", not a manual
attestation. **Corpus survey: 70 such CRs across 21 standards** — a systematic encoder mis-convention.
- **New validator rule 14** `14.dead-manual-condition` registered this wave; cross-validates the SQL
  count at exactly 70.
- **Disposition = RULING (sheet S-2), not auto-sweep.** Clearing `manual`→'' is *semantics-shifting*
  (result kind pending→manual), unlike the semantics-preserving eq→== sweep. Recommended as a single
  Alvaro decision covering all 70. **New standing lesson: semantics-preserving → auto-sweep;
  semantics-shifting → sheet.**

## WORKFLOW METRIC
- **0 equations** — all 6 worksheets self-contained → **expected to run** end-to-end.
- 3 warn gates now engine-proven both-ways post-re-home; ws-03 now gated.
- **Deployed-build execution UNPROVEN** — element 7 owner-blocked (unchanged). Static + engine-level only.

## SIGN-OFF SHEET (Section S, +4 → 77 open)
S-1 CR-015 block-vs-warn on §5.3.4 'should' (safety-exception design) · **S-2 corpus `manual`-dead
sweep (70 CRs)** · S-3 vocabulary coverage gap §3.2–§3.6 (~69 terms, encode extension) · S-4 PDF now
acquired → EV→VA lift possible.

## CONVERGENCE
| state | count |
|---|---|
| **fully treated** | **0** of 71 (deployed-build proof still owner-blocked, element 7) |
| in progress | 17 (ISO-59004 advanced: coverage✔ fields✔ re-home✔ map✔; owed: deployed-build exec + vocab extension) |
| untouched | 54 |

## RESUME
Final of the 3-standard run: **ISO-14033:2019** (Quantitative environmental information — Guidelines &
examples, 73pp, source staged `scratchpad/iso14033.txt`; note: this one likely HAS equations/worked
examples). Then re-rank the queue.
