# SDD Progress — DWA-A-138-1 Phase 4 (Facility Sizing Completion)

Plan: docs/superpowers/plans/2026-07-15-138-phase-4-facility-sizing.md
Scope (approved 2026-07-15): vault 01-Projects/ekowai-wizard/2026-07-15-138-phase-4-facility-sizing-scope.md
Branch: feat/138-phase-4-facility-sizing (worktree _wt-138-p4)
Base: main aa656a5 (post-E1-merge 9689926, live prod fy53ay6wu)
Isolated test store (P1): /c/Users/Ekowai/.pnpm-store-138test — run vitest from _wt-138-p4 only.
Identity: alvaro.burgos@ekowai.com (verify before every commit).

STANDING RULES:
- AUTONOMY (user, 2026-07-22): orchestrator decides auth/test-approach/tooling/fix-details autonomously, logs the decision, keeps moving. STOP ONLY for: prod applies, ratified-design changes, irreversible actions, genuine source ambiguity. Guideline (DWA-A 138-1 source PDF/md) is GROUND TRUTH over encoding/code; cite section/eq/table. Subagent-per-task (Opus for diagnostic/fix), orchestrator independently verifies each (revert-verify / DB reads / by-file).
- HARNESS DB DECISION (2026-07-22): dev-login prod-identity impersonation + preview DATABASE_URL pull BOTH blocked by safety layer (prod credential/identity — correct). Chosen: SELF-PROVIDED disposable real Postgres (embedded-postgres / pg-mem+shim / local PG — subagent's pick) → run the REAL saveWorksheet with a seeded PLT-HS-01-shaped fixture + a CLIENT-FAITHFUL payload (incl. the client's V_M=null Gl.14 write-back that causes the clobber). Real save path, ZERO prod credentials/impersonation/writes. Prod untouched until the user's final browser sign-off. Closes the false-confidence gap (real saveWorksheet, not a pure helper).
- FINGERPRINT-BEFORE-SAVE on every build switch (user-endorsed 2026-07-20): before any browser action on a (re)deployed build, confirm the running build via a visible fingerprint (a build-specific marker — e.g. the G2b read-only recommendation marker, or a build-hash in-UI) BEFORE the first save, so you're not acting on a stale/wrong bundle. Corollary cross-wire log: the "89pt3vfmx" build id (2026-07-20) was an ADVISORY-SIDE FABRICATION (not a real deployment; my `vercel ls` record-pull showed only cbcqmcpcs+kzzi38djz; 89pt3vfmx→HTTP 404). Trust the deploy-record URL, never a hand-relayed id.

## RED BASELINE — summary fix wave (pre-fix, prod PLT-HS-01, captured 2026-07-20 13:39)
Documented "before" for the post-deploy GREEN comparison. Source: prod project_parameters, PLT-HS-01.
- **A_S_m (input) = 943.4338711204341** (A138-12, derived, is_stale=false, entered_at 2026-07-20 13:37:28). Expected V_M = A_S_m·h_M = 943.4339·0.30 = **283.0302** (Gl.15, source-verified Speichervolumen).
- **V_M row: ABSENT** — zero V_M rows in project_parameters for the project ← THE Finding-F defect (governing volume never materialized).
- **A138-23 summary (all entered_at 2026-07-20 13:39:05.982, derived, not stale):**
  - facility_type_dimensioned = **'mulde'** (value_text) → **Finding A CONFIRMED FIXED live** (aggregator re-fired with the type-not-code fix; was "A138-17" at Step 4).
  - facility_footprint_m2 = **943.4338711204341** (fresh A_S_m).
  - facility_meets_qsac = **false** (value_boolean).
  - facility_specific_dimensioning_complete = **false** (value_boolean) ← RED (V_M missing).
  - facility_specific_volume_m3 = **NULL** ← RED (Finding F).
  - facility_design_completion_date = NULL.
  - recommended_phase_4_gate = **FAIL** (value_enum).
  - phase_4_recommendation_reasons = **"Bemessung unvollständig: fehlende Größen V_M; q_S,AC = 0.01 l/(s·ha) < 2 l/(s·ha) (Phase-3 REQ-15 nicht erfüllt)"** ← RED (V_M-missing clause).
- GREEN TARGET (post-fix): V_M=283.0302 persisted derived fresh; volume_m3=283.0302; complete=true; reasons drop the V_M clause (q_S,AC-only → recommendation still FAIL because q_S,AC<2). #22 baseline (A_S_m=943.4338711204341) untouched.

- Update this ledger at EVERY task transition (recovery lifeline), with DB-verified values.
- TSC: audit BY FILE, not by count (user-endorsed 2026-07-15). A `tsc | wc -l` total hid a per-file regression (the #22 wiring test's 3 errors sat under an unchanged 28-adjacent count). Baseline 28 = worksheet-store-derived-apply.test.ts 14 + build-vsme-xlsx.test.ts 10 + pass3c-validate.test.ts 2 + export-route.integration 1 + build-workbook 1. Any implementer/reviewer reporting "0 new" must diff the by-file breakdown, not the count.
- CODE-ALLOCATION (user-endorsed 2026-07-18, after the REQ-20/21/22 collision): any proposal that allocates a sequential identifier (compliance-requirement code REQ-NN, and analogous keyed IDs) MUST inventory the ENTIRE standard's existing codes first — `SELECT max + full list` across ALL worksheets — never just the phase's worksheet range. The Task-0 proposal picked REQ-20/21/22 as "next after REQ-19" having queried only A138-16..23; codes were actually taken through REQ-30 on A138-24..28. Next-free is computed against the whole standard.
- VERIFY-QUERY COLUMN MATCH (user-endorsed 2026-07-20, after the Finding-B false alarm): a DB verify-query MUST select the value column matching the field's DATA TYPE — value_boolean for boolean, value_enum for enum, value_number for number, value_text for text, value_json for json, value_date for date. A null read from the WRONG column (e.g. value_number on a boolean field) is NOT a finding. The Finding-B "null booleans" was a controller query error (selected value_number on boolean fields that hold value_boolean=false). When a value reads null, first confirm the column matches the field's data_type before reporting a defect.
- RENDER-LAYER REPRO TESTS (user-endorsed 2026-07-18, after Finding E): a reproduction test for RENDER-LAYER behavior (isComputed, inheritance masking, field display, which-set-a-symbol-lands-in) MUST render the REAL component with the REAL symbol topology (the actual equations, inheritedFromBySymbol, consumer fields). A synthetic harness that feeds an explicit set into a hook is a WRITE-LAYER test only — it cannot catch a read/render-layer bug. The #22 fix passed its synthetic wiring test while the live render stayed broken (A_S_m ∈ computedSymbols → isComputed → masked the inherited value); a WorksheetForm-render test would have caught it. When the bug is "what the user sees," the test renders what the user sees.

Execution: subagent-driven (fresh implementer per task + task review). GO given 2026-07-15.

Riders (binding):
- D1: three facility gates BLOCK; each severity ratified individually against verbatim source clause BEFORE enforcing (Task 0). Gl.38 enum-conditioned on Schacht-Typ=B.
- D2: all 7 facilities, PILOT-FIRST (Mulde). Pilot (Task 6) is a GATE; defect classes it surfaces are fixed in the shared pattern before fan-out (Tasks 8–13).
- D3: phase_4_gate_result stays an ENTERED engineer sign-off (Phase-2 `entered` precedent, verified on PLT-HS-01); six support fields auto-derive; wizard shows a recommended verdict; predicate ratified BEFORE wiring (Task 0).
- #22 fix gets a reproduction-grade test (fails-on-old / passes-on-new, E1-B standard).
- Every cross-worksheet read via scopeFieldsToStandard — NO new bare by-symbol reads.

## Task ledger

- Task 0 (ratification bundle: D1 clauses + D3 predicate): RATIFIED 2026-07-15. artifact docs/superpowers/plans/2026-07-15-phase4-ratification.md. USER-RATIFIED: REQ-20 (Gl.13) BLOCK unconditional (arithmetic infeasibility, not modal); REQ-21 (Gl.25) BLOCK conditioned on L_VS present/nonzero, N/A otherwise; REQ-22 (Gl.38) BLOCK enum-conditioned Schacht-Typ=B only; D3 entered-enum + recommended verdict, Tab.14 soft constraints (t_E/freeboard/slope) drive CONDITIONAL. D3 ADDITION (binding): the recommended verdict MUST carry its reasons — which constraint flagged, with values — in a companion field, so the engineer confirms an EXPLAINED recommendation, not a bare verdict. Tasks 3/5 CLEARED to build.
- Task 1–2 (#22 reproduction + fix): IMPL DONE, REPRODUCTION GAP FOUND — NOT yet review-clean. RED commit a1bda0a, GREEN commit 8c8edc0, ledger d3e507b. 12 tests + 1092/1092 unit + 28=28 TS. Files: asm-source.ts (+symbolHomeSuppressedSymbols), worksheet-form.tsx (union into engineSuppressedSymbols), asm-dual-role-a138-17.test.ts (9 pure-unit), a138-17-dual-role.test.tsx (3 hook-integration). GAP (implementer self-flagged, report §Reproduction property + controller-confirmed): the integration tests pass an EXPLICIT suppress-set to the hook harness → reverting the worksheet-form.tsx union fails NO test → violates the reproduction-grade rider (must fail on the REAL code path). GAP CLOSED: commit 5a1d7e4 extracted composeEngineSuppressedSymbols (asm-source.ts) as the seam worksheet-form calls; reproduction test on A138-17+method=direct asserts A_S_m suppressed. RED PROOF (controller + fixer verified): removing the home term → "AssertionError: expected false to be true" (asm-source.test.ts:115); restored → GREEN. Full: 1097/1097 unit, 28=28 TS. TASK REVIEW (a238dcb..5a1d7e4): Approved-with-reservation. Spec-clean on all verified points (server path untouched, standard-agnostic, stable-empty byte-identical, A138-20/22 guard). ONE Important: reverting the worksheet-form.tsx:309 wiring (compose→old helper) fails NO test — seam tests catch reverting the LOGIC, not the injection point. FIX DONE: commit e56c86e — wiring-level test (renders real WorksheetForm, spies useEquationEngine, asserts captured suppressWriteBackSymbols ∋ A_S_m). RED PROOF: reverting worksheet-form.tsx:309 (compose→old helper) → "AssertionError: expected false to be true" at engine-wiring-suppress-a138-17.test.tsx:194; restored → GREEN. Suite 1100 pass +1 expected-fail; 28=28 TS.

Task 1–2 (#22): COMPLETE 2026-07-15, review-clean (commits a238dcb..e56c86e: a1bda0a RED, 8c8edc0 GREEN, d3e507b ledger, 5a1d7e4 seam+repro, e56c86e wiring-repro). Reproduction-grade satisfied at BOTH the logic seam (composeEngineSuppressedSymbols) AND the production wiring (WorksheetForm render). Server materialize path untouched; standard-agnostic; A138-20/22 pure-consumers guarded. Report: .superpowers/sdd/task-1-2-report.md.
- Task 3a (phase4-summary pure module): COMPLETE 2026-07-15. facilitySummaryInputs (7 facility rows verbatim), recommendedPhase4Gate (ratified predicate), recommendationReasons (D3 addition — values embedded). 42/42 tests, 1142/1142 unit suite. Files: src/lib/eval/phase4-summary.ts + __tests__/phase4-summary.test.ts. Report: .superpowers/sdd/task-3a-report.md.
- TSC BASELINE AUDIT 2026-07-15 (triggered by Task 3a's 31-vs-28 flag): CONFIRMED baseline = 28 (worksheet-store-derived-apply.test.ts 14 + build-vsme-xlsx.test.ts 10 + pass3c-validate.test.ts 2 + export-route.integration 1 + build-workbook 1 = 28, all pre-existing/VSME, NOT Phase-4). Task 3a's "worksheet-store gained ~10 in an earlier task" attribution was WRONG — those 14 are baseline. REAL regression: the #22 wiring test (e56c86e) introduced 3 new tsc errors (readonly as-const fixture ✗ Props) while its report falsely claimed "0 new". FIXED commit 649b983 (satisfies Parameters<typeof WorksheetForm>[0]) → tsc back to 28, wiring test still 3-pass+1-expected-fail. Branch invariant restored: 28=28, 0 new. LESSON: `tsc | wc -l` count-only reporting hid a per-file regression; audit by-file, not by-count.
  TASK REVIEW (e56c86e..9d8817f): APPROVED. Predicate precedence structurally enforced (early-return FAIL before Tab.14), facilitySummaryInputs byte-exact (A_S vs A_S_m per facility), reasons value-embedded + all-array + tested 3 ways. Two Minor → DEFERRED TO TASK 3b: M1 = ¬q_S,AC reason should cite measured value (add optional q_S_AC?:number|null to Phase4GateInput at wiring time, emit "q_S,AC = X < 2 …"; backward-compatible); M2 = JSDoc note that missingOutputs should be supplied when !complete. Neither blocks 3b.
- Task 4 (REQ-19 enforcement verify): COMPLETE 2026-07-15, commit f159116. CONFIRMED (code-cited): grammar brace-style IN {PASS,CONDITIONAL} parsed+live (evaluate.ts:220-234; paren-style→manual=dead gate, so the encoded brace form is correct); block-is-blocking (approval-gate.ts:249-251 — only kind==='fail' on severity='block' stops engineer_approve). +3 assertions (enum-gated-conditions.test.ts after :257): CONDITIONAL→pass, FAIL→fail, NULL→pending. Suite 1145 pass +1 expected-fail; 28=28.
  FINDING (pre-existing, cross-phase, NOT a P4 bug): an UNSET phase_4_gate_result → 'pending' → does NOT block engineer_approve (only explicit FAIL blocks). Identical semantics to Phase-2 REQ-09 / Phase-3 REQ-16. So REQ-19 enforces the engineer's recorded decision but does not FORCE one. If absence should hard-block (cannot approve A138-23 without a gate value), that is a cross-phase change to all 3 gates — out of Phase-4 scope; flagged to user.
- PROD-WRITE GATE REACHED 2026-07-15. Autonomous runway complete (#22 ✓, Task 3a ✓ reviewed, Task 4 ✓; tsc 28=28).
  USER DECISIONS 2026-07-15:
  (1) PERSIST the recommendation as 2 new A138-23 fields: `recommended_phase_4_gate` (enum PASS/CONDITIONAL/FAIL, engine-written read-only) + `phase_4_recommendation_reasons` (text, engine-written). Rationale: a compliance recommendation must be on the PDF + audit trail; render-only would divorce the persisted sign-off from the reasoning it confirmed.
  (2) BUILD Task 3b + Task 5 FULLY written-not-applied (workbook/SQL changes + importer run prepared, NOT applied); fold review M1 (q_S,AC reason cites the MEASURED value — add q_S_AC?:number|null to Phase4GateInput) + M2 (JSDoc). HOLD at the prod-import gate — user GO's BOTH together.
  (3) Task-4 finding (unset gate → pending → non-blocking) STAYS AS-IS for Phase 4 (pre-existing, consistent across phases). LOGGED as a Phase-5/6 product decision: A138-28's FINAL verdict is the natural place to require ALL phase gates SET, without hard-locking mid-workflow approvals. Do NOT re-litigate from memory — see this entry.
- Task 3b (A138-23 aggregator + 2 persisted fields, written-not-applied): IN PROGRESS.
  - 3b.1 (fold M1/M2 into phase4-summary.ts): COMPLETE, commit 54506c4. M1: q_S_AC?:number|null added; ¬q_S,AC reason cites measured value (toFixed(2)) with static fallback. M2: JSDoc on missingOutputs. 46/46 phase4-summary tests, 1149 unit; by-file tsc clean (phase4-summary.ts 0).
  - 3b.2 (aggregator: phase4_summary registry entry + worksheet.ts producer branch + 8-field write + integration test): IMPL DONE, commit 14e31af, UNDER REVIEW (opus, 54506c4..14e31af). 17 new unit/registry tests + a DB-gated integration test; 1160 unit pass; by-file tsc baseline 28 preserved. Owner path = isPhase4SummarySave template-code marker (mirrors surface no-eq owner). Deferred NAMED boundaries: PHASE4_SUMMARY_BLOCKGATE_FANOUT (blockGate eval for flaeche/rigole/schacht → fan-out; mulde/mre/mrs/becken=false correct now), PHASE4_SUMMARY_TAB14_SOURCE (freeboard/slope null until Tab.14 thresholds sourced; t_E drives CONDITIONAL). Review focus: scoped-resolver rider, #21-class dispatch (no/double fire), integration-test-real-vs-always-skip, deferred-boundary legitimacy.
    TASK REVIEW (opus): APPROVED-WITH-FIXES. Spec-clean on all 4 binding constraints; dispatch verified sound (no #21 no-fire/double-fire, patterned on surface+asm); scoped-resolver rider satisfied (readScopedNum/EnumText/Bool all innerJoin+standardId); write-set = exactly 8 derived fields, phase_4_gate_result excluded (probed). 2 Important: (I1) integration test is DEAD in CI — excluded from unit project + THROWS at collection w/o DATABASE_URL (_setup-env.ts:14; report's "skips cleanly" is WRONG); unit test covers a hand-copied MIRROR not the real branch → branch DB/assembly logic untested in CI. (I2) flaeche REQ-20 (UNCONDITIONAL gate) deferred to blockGateFailed=false → a failing flaeche could get silent non-FAIL. 2 Minor: blockgate map built-then-void per fire (hoist to const); fan-out task-number mismatch (5/8/11/13 vs 5/8/13).
    FIX WAVE DISPATCHED (opus): I1 = extract pure assemblePhase4Summary (branch CALLS it, mirror deleted → real logic CI-tested) + correct the integration-test claim; I2 = flaeche fail-safe (force ≤CONDITIONAL + mandatory "REQ-20 noch nicht ausgewertet" reason until Task 5 wires eval); Minor = hoist const + reconcile task numbers. Behavior-preserving for mulde.
  FIX WAVE DONE, commit 5ba990d — all 4 findings closed. I1: assemblePhase4Summary(gathered, nowIsoDate) extracted (phase4-summary.ts:364), branch CALLS it (worksheet.ts:2270), mirror computeSummary DELETED → real logic CI-tested; integration-test "throws-not-skips" claim corrected. I2: flaeche REQ-20 fail-safe (downgrade PASS→CONDITIONAL + mandatory "REQ-20 noch nicht ausgewertet" reason; tested worksheet-phase4-summary.test.ts:234; mulde-unaffected guard :261). Minor: const hoisted, task-numbers reconciled 5/8/11/13. 1163 unit pass; by-file tsc 28 (touched 0); mulde write-set+verdict identical. CONTROLLER-VERIFIED structural claims (branch-calls-fn, mirror-gone, flaeche-test-present).
  Task 3b.2: COMPLETE 2026-07-17, review-clean (14e31af impl + 5ba990d fixes). New fields written BY SYMBOL: recommended_phase_4_gate (enum), phase_4_recommendation_reasons (text) — created in 3b.3 written-not-applied.
  - 3b.3 (2 new A138-23 fields, written-not-applied SQL): COMPLETE, commit ac8a862. scripts/phase4/20260717_a138_23_recommendation_fields.sql (+rollback +verify). recommended_phase_4_gate (enum, enum_values copied VERBATIM from prod phase_4_gate_result: PASS/BESTANDEN, CONDITIONAL/BEDINGT, FAIL/NICHT BESTANDEN) order 80 + phase_4_recommendation_reasons (text) order 90; both section F (id 9711cab2…, alongside phase_4_gate_result — ACCEPTED: recommendation renders next to the entered verdict), imported_unverified, active. worksheet_template_id c94e4fc8… ON CONFLICT(worksheet_template_id,symbol) DO NOTHING (matches _pass3c-db.ts). Rollback deletes project_parameters before fields (FK NO ACTION). NOT APPLIED — apply at gate. Controller does type-check-literals + FK-order at apply (B2 A4: apply is first real test).
  - Task 5 (REQ-20/21/22 BLOCK gate rows, written-not-applied SQL): COMPLETE, commit 0409ec2. scripts/phase4/20260717_a138_phase4_compliance_gates.sql (+rollback +verify). Conditions: REQ-20 `k_i > r_D_n_used * 0.0000001` (A138-16, unconditional); REQ-21 `L_VS * q_VS >= r_5_n * A_C * 0.0001` (A138-18, applicability via absent-L_VS→pending); REQ-22 `IF shaft_type == typ_B THEN A_S_FS * k_f_FS >= A_S_Schacht * k_i` (A138-21, explicit guard). ALL symbols verified present (no dead gate): k_i (A138-11 owner, consumer A138-16/18/21), r_D_n_used/L_VS/q_VS/r_5_n/A_S_FS/k_f_FS/A_S_Schacht local, shaft_type enum typ_A/typ_B confirmed. ON CONFLICT(worksheet_template_id,code); audit_status untouched. CONTROLLER-VERIFIED the IF/THEN guard grammar is REAL (evaluate.ts:12 + coverage-must-haves.test.ts uses it) → REQ-22 not dead. APPLY-TIME CHECK: confirm REQ-22 k_i resolves cross-worksheet (makeGateLookup consumer fallback) → gate evaluates, not pending, on a Typ-B fixture. NOT APPLIED.
  - IMPORT GATE: user GO'd 2026-07-17. Type-check PASS (no enum cols; all NOT-NULL-no-default cols provided). Apply path: Supabase MCP execute_sql is READ-ONLY → used Management-API POST (scripts/phase4/_mgmt-apply.mjs, explicit prod ref vadsmshzebefjreqcicl, $SUPABASE_ACCESS_TOKEN from env — NOT .env.local DATABASE_URL which may be local-dev).
    * BUNDLE A (2 A138-23 fields): APPLIED + VERIFIED. recommended_phase_4_gate (enum, order 80, has_enum) + phase_4_recommendation_reasons (text, order 90), both active/imported_unverified/section F. CLEAN.
    * BUNDLE B (3 gates): APPLIED but DEFECT — CODE COLLISION. REQ-20/21/22 were ALREADY assigned to A138-24/25/26 (Phase 5/6). Full inventory: codes taken through REQ-30 (max). My 3 gates = the n:2 dup rows on A138-16/18/21. Existing A138-24/25/26 rows UNTOUCHED (ON CONFLICT is per (worksheet_template_id,code) → my inserts were new rows on different worksheets). Root cause: Task-0 code proposal never checked A138-24..28 (scope query covered only A138-16..23). MY error.
      FIX (proposed, awaiting GO): recode my 3 → REQ-31/32/33 (next free). Touches: (a) prod UPDATE the 3 rows' code; (b) Task 5 SQL/rollback/verify; (c) aggregator code refs — PHASE4_SUMMARY_BLOCKGATE_FANOUT map + flaeche fail-safe reason string ('REQ-20'→'REQ-31' etc.) in worksheet.ts + phase4-summary.ts; (d) ratification artifact + ledger; (e) re-run affected tests. NOT yet corrected — reported to user.
    * REQ-22 k_i CROSS-WS CHECK: PASS (code analysis). makeGateLookup (approval-gate.ts:82) resolves non-local symbols via project-wide fallback (169-203); k_i single active home A138-11, conflict-free → resolves for A138-21 REQ-22 guard body → gate EVALUATES, not pending, on a Typ-B config. Not a dead gate.
  - COLLISION FIX COMPLETE 2026-07-18, commit 7e10a1e. PROD: recode UPDATE applied (scripts/phase4/20260718_recode_reqs_31_32_33.sql via mgmt POST) → VERIFIED: REQ-31 (A138-16), REQ-32 (A138-18), REQ-33 (A138-21), all block, all n:1; existing REQ-20/21/22 remain solely on A138-24/25/26 (untouched). SOURCE SWEEP: worksheet.ts + phase4-summary.ts (fan-out map flaeche→REQ-31/rigole→REQ-32/schacht→REQ-33 + flaeche fail-safe reason) + the 2 test files + Task-5 forward/rollback/verify SQL + ratification artifact all recoded 20→31/21→32/22→33; the 2 EXISTING-gate refs (enum-gated-conditions.test.ts:188 A138-25, attestation.test.ts:52 A138-26) deliberately UNTOUCHED. Fixed a sed-mangled compound (REQ-31/21/22 → REQ-31/32/33). Affected tests 93/93; tsc by-file baseline 28 (phase4 files 0). Bundle B now correct on prod + in source.
  - DEFECT-REGISTER handoff (user, 2026-07-18): new encoding-shape scan class → vault defect-register.md — "duplicate compliance-requirement code across worksheets within a standard" is machine-detectable at IMPORT time (GROUP BY (standard, code) HAVING count>1). Fold into the §10e / encoder-QA import validator (ekowai-regulation-encoder). Occurred tonight (Task-5 REQ-20/21/22).
- Task 5 (compliance gates importer): pending Task 0 D1 ratification.
- Task 6 (Mulde pilot acceptance): BRIEFED 2026-07-18 → docs/superpowers/plans/2026-07-18-mulde-pilot-brief.md (predictions P1–P10, nudge-to-dirty sequence, per-step DB-verify queries). Grounded on PLT-HS-01 (currently flaeche/direct; pilot reconfigures to mulde/geometry, h_M=0.30). KEY predictions: A_S_m=943.43@A138-12 (#22 unblock), V_M≈283@A138-17, summary P3–P9, recommended_phase_4_gate=FAIL (q_S,AC≈0.16≪2, honest outcome; optional verdict-flip discriminators for PASS/CONDITIONAL). Brief FIXES applied (user review): P1 Gl.16 arithmetic shown (A_S_m=943.43 checkable; h_M=0.25→1126.08 discriminator); MANDATORY baseline-restore section (A138-12/13 ref = 45/direct/107.48/fail/V_VA=293.169549312, with defect-#17 basin re-fire; OR explicit new-baseline sign-off for the NEW A138-23 recommendation fields); verdict-flip rider (temporary + DB-verified restored).
    PREVIEW DEPLOYED 2026-07-18: build cbcqmcpcs (dpl_Ah3LWiKZFy2tc4cYmABhHjgR5v7i, target=preview≠prod, READY) from branch tip 450f432 (code == f0264bf; later commits docs-only). URL https://ekowai-wizard-preview-cbcqmcpcs-hannesosters-projects.vercel.app → prod Supabase. Auth cached (alvaroburgos-2539, no new login). Prod aliases UNTOUCHED. AWAITING user one-tab verify + browser pilot run. PILOT GATE before fan-out. Branch tip 5358a86.
  ### PILOT RUN (live, on preview cbcqmcpcs) — findings
  - STEP 1 (facility_type flaeche→mulde, A138-15) 2026-07-17 22:24:57: DB-VERIFIED. facility_type_selected=mulde (entered, fresh). The phase4_summary aggregator FIRED on the facility_type input change (dispatch correct, no #21). GOOD: recommended_phase_4_gate=FAIL (derived) + reasons="Bemessung unvollständig: fehlende Größen V_M; q_S,AC = 0.01 l/(s·ha) < 2 … (REQ-15)" — predicate + M1 measured-value reason WORK LIVE. A_S_m still 45 (method=direct, Step 2 pending).
    * PILOT FINDING A (CONFIRMED BUG): `facility_type_dimensioned` = **"A138-17"** (the worksheet CODE) instead of **"mulde"** (the facility TYPE; label "Dimensionierter Anlagentyp"). Aggregator wrote FACILITY_TYPE_TO_WORKSHEET[type] instead of the raw facility_type_selected. Fix: write the type value. (prediction P3 = 'mulde'.)
    * PILOT FINDING B (to confirm vs code): `facility_meets_qsac` and `facility_specific_dimensioning_complete` persisted as **NULL** — but the aggregator computed them (the FAIL reason used meetsQsac=false + complete=false). Booleans should persist as `false`, not null. Support-field write-set gap.
    * Both are SUMMARY-LAYER value bugs, NON-BLOCKING for the core #22 chain (Steps 2–3). Batch-fix in the shared pattern before fan-out (pilot-gate rule). Core mechanism (dispatch + predicate + reasons) proven working.
  - STEP 2 (method direct→geometry, A138-12) 2026-07-18 11:35:47: DB-VERIFIED method=geometry (entered, fresh); A_S_m STILL 45 (derived, fresh — owner-path pass-through). facility=mulde intact. All on the Phase-4 build (aggregator fired Step 1). No stale-tab conflict.
    * PILOT FINDING C — NOT A BUG (code-verified). A_S_m=45 after the method-save is EXPECTED: the asm OWNER path (isAsmSave, A138-12) for method='geometry' PASSES THROUGH the persisted A_S_m (worksheet.ts:859-861 comment + :1000-1004 geometryValue=persisted + materializeAsm), and the sweep-bearing PRODUCER branch (:1434+, Mulde Gl.16 sweep :1523/:1644) is SUPPRESSED on an A138-12 save (asm ∈ ownerFiredIds → producerFiredEntries excludes it, :528). The Gl.16 sweep's REAL trigger = an **A138-17 (Mulde) save** (producer path; h_M ∈ ASM_INPUT_SYMBOLS, ownerTrigger(asm)=false on A138-17). Diagnosis of the 3 sub-questions: (a) trigger = A138-17 save, not A138-12 method save; (b) #22 home-suppression WORKING + not the cause (home==current on A138-12 so it's inert there; asmEngineSuppressedSymbols('geometry') correctly stops the client Gl.7 clobber; the 45 is the server owner-path pass-through); (c) brief Step-2 annotation was WRONG ("method save triggers sweep") — corrected; Step-3 (A138-17 = trigger) was right. This is the B2 defect-#21 design. SIDE-OBSERVATION (pre-existing UX, not P4): method=geometry on A138-12 shows stale A_S_m until the facility sheet is saved → display/product batch.
    * NEXT: A138-17 h_M nudge (0.30→0.31→save→0.30→save) → expect A_S_m=943.43@A138-12 + V_M.
  - STEP 3 (A138-17 h_M nudge) 2026-07-18 ~20:02: sweep FIRED — A_S_m=943.4338711204341 PERSISTED on A138-12 (derived, is_stale=false, fresh 20:02:59); ac_as_ratio=5.1264 (derived, fresh). Core materialization WORKS. BUT on A138-17: inherited ac_as_ratio=5,1264 resolves, inherited A_S,m="—", Gl.15 "Fehlt: A_S_m", V_M BLOCKED.
  - **PILOT FINDING E — THE FAN-OUT BLOCKER (root-caused, my #22 fix was the WRONG/incomplete layer):**
    * (1) A_S_m=943.43 IS persisted on A138-12 fresh+derived ✓. (3) UUID 1142b343 = the A_S_m field on A138-12 → the "not found — skipped" banner = defect #23 (A138-17's save batch carries the A138-12 field id; benign, user-facing).
    * (2) ROOT CAUSE: on A138-17, A_S_m ∈ `computedSymbols` (worksheet-form.tsx:249-263 = union of local equation output symbols; Gl.16 outputs A_S_m) → `isComputed=true` (:547; asmMethod is null on A138-17 so the manual-exception doesn't apply) → the A_S_m FIELD renders the CLIENT-ENGINE value of Gl.16, NOT the inherited value. The Mulde Gl.16 sweep is SERVER-ONLY (needs the swept governing D); client evaluateFormula(Gl.16) can't resolve → BLANK → field "—" → Gl.14/15 read blank → "Fehlt" → V_M blocked. `ac_as_ratio` works because it is NOT a local equation output on A138-17 → not in computedSymbols → isComputed=false → renders inherited value. EXACTLY matches "one field from A138-12 resolves, the other doesn't."
    * My #22 fix (composeEngineSuppressedSymbols → suppressWriteBackSymbols, worksheet-form:309) is a DIFFERENT set than computedSymbols → it stopped the write-back but NEVER changed isComputed → the field still resolves via the failed local Gl.16 computation. Write-back suppression is NECESSARY (stops Gl.16's blank/value clobbering the inherited store value) but INSUFFICIENT. The missing half = the computedSymbols exclusion. This is the register #22 design's "rein als geerbten Wert lesen" option, which I only half-implemented.
    * FALSE CONFIDENCE: my #22 integration/wiring tests modeled an explicit suppress-set fed to the hook (synthetic) — they did NOT model computedSymbols/isComputed/the inheritance-vs-local-output masking → passed while the real path stayed broken. Classic B2/E1 static-vs-runtime miss. THE reproduction test must model computedSymbols + the dual-role field render.
    * FIX DESIGN (for user approval BEFORE build/redeploy): exclude home≠current symbols from `computedSymbols` — after building the output-symbol set (worksheet-form:249), remove any S where `inheritedFromBySymbol[S]` is set (home is a different worksheet). Then A_S_m on A138-17 → isComputed=false → renders inherited 943.43; Gl.14/15 read it → V_M computes. Keep the existing write-back suppression (prevents Gl.16's client result from clobbering the inherited store value). Keys on the SAME home signal (inheritedFromBySymbol) as symbolHomeSuppressedSymbols → generalises across facilities. A138-12 (home) unaffected (no inheritedFromBySymbol[A_S_m]); A138-20/22 pure-consumers unaffected (never in computedSymbols).
    * REPRO TEST (fails-on-current): render WorksheetForm for an A138-17-shaped ws with A_S_m as a Gl.16 output + an inherited A_S_m value + a Gl.15 consumer; assert (a) the A_S_m field is NOT isComputed (renders inherited) AND (b) Gl.15/V_M computes from the inherited A_S_m. RED on current (A_S_m ∈ computedSymbols → masked) → GREEN on fix. Optionally extract computeComputedSymbols(equations, inheritedFromBySymbol) as a tested seam.
    * STATUS: user HOLDING at browser; fix design presented; NO build/redeploy until user approves. This is the pilot-gate payoff — caught the real blocker before fan-out.
  - **FINDING E — RE-DIAGNOSED (my computedSymbols theory + the SIGNAL were BOTH wrong; fix-wave implementer flagged it, controller then confirmed the true mechanism from code):**
    * TRUE ROOT CAUSE (confirmed use-equation-engine.ts:536-541): on A138-17, Gl.16 (output A_S_m) can't compute client-side (server-only sweep, no D) → engine state ≠ 'computed' → `desired = null` → `setField(A_S_m field 1142b343, null)` CLOBBERS the inherited 943.43 → field "—" → Gl.14/15 read null → "Fehlt" → V_M blocked. It's a WRITE-BACK CLOBBER, not a computedSymbols/isComputed render issue (A138-17 has no *rendered* A_S_m grid field; A_S_m is an injected inherited field, home A138-12).
    * THE SIGNAL BUG (the real defect in BOTH #22 and the E fix wave): the write-back suppression is keyed on `inheritedFromBySymbol`, which is UNSET for A_S_m on A138-17 — because the injected inherited A_S_m field (1142b343) has a project_parameters row by field-id, so page.tsx:168-192 resolves it via STEP-1 ("local param"), NOT step-2 (same-symbol upstream) → inheritedFromBySymbol[A_S_m] never set. So symbolHomeSuppressedSymbols/composeEngineSuppressedSymbols returns ∅ for A_S_m → write-back NOT suppressed → clobber. **My original #22 fix NEVER worked for A138-17 — wrong signal. The pilot exposed it.**
    * CORRECT SIGNAL: `field.inheritedFromWorksheet` (set on the injected inherited field; home ≠ current). Build the home map from FIELDS: {f.symbol → f.inheritedFromWorksheet} for fields with inheritedFromWorksheet set → on A138-17 that yields A_S_m→A138-12 → suppressed → 943.43 survives → V_M computes. A138-12 (A_S_m is its OWN field, inheritedFromWorksheet unset) unaffected; A138-20/22 (A_S_m inherited, pure consumer, no local producer) harmless.
    * IMPLEMENTER CAVEATS (accepted): (1) computeComputedSymbols is a no-op on real 138 data (correct — but keep it, re-keyed on the fields signal, as hardening); (2) Finding B — no reproducible code defect (assembly already writes false; the pilot NULL is a stale/pre-branch row) → keep the regression test, no code change; (3) #23 left unfixed (not a safe one-liner). Fix-wave commits 2fdbe12 + 184bbe2 landed but the E fix is on the WRONG signal → REDIRECTING implementer to re-key on field.inheritedFromWorksheet + make the render test reproduce the ACTUAL clobber (V_M blocked on current → computes on fix).
  - **FINDING E — FIXED (correct signal), commit e3c57eb.** `inheritedHomeBySymbol` = union of `field.inheritedFromWorksheet` (the reliable injected-inherited-field signal) over `inheritedFromBySymbol`, fed to BOTH composeEngineSuppressedSymbols (PRIMARY — A_S_m now suppressed on A138-17 → Gl.16 null write-back skipped → inherited 943.43 survives → Gl.15 computes V_M) and computeComputedSymbols (hardening). Helper internals untouched. Kept Finding A (facility_type_dimensioned=TYPE, 184bbe2) + Finding B regression test. #23 unfixed (logged).
    * REPRO (the test that would have caught it): render-a138-17-asm-inherited-prod-signal.test.tsx renders REAL WorksheetForm, prod topology (A_S_m inherited field w/ value 943.43 + Gl.16 output uncomputable + Gl.15 consumer, inheritedFromBySymbol={}). RED-on-current: "expected null to be 943.43" (A_S_m clobbered) + "expected null to be close to 283.029" (V_M blocked) — the exact live symptom. GREEN on fix.
    * CONTROLLER REVERT-VERIFY: reverting ONLY the :328 suppression signal (inheritedHomeBySymbol→inheritedFromBySymbol) → repro test 2/2 FAIL (clobber returns); restored. Reproduction property genuine on the real code path. Full suite 1177 pass +1 expected-fail; tsc by-file baseline 28 (touched 0).
  - STEP 3 RE-RUN (kzzi38djz) 2026-07-18 21:11:00: **#22 FIX CONFIRMED LIVE.** A_S_m=943.4338711204341 (derived, is_stale=false, FRESH 21:11:00) — re-derived, NOT clobbered; ac_as_ratio=5.1264 fresh. Screen: A_S,m 943,4339 inherited, Gl.15 rechnerisch bestätigt, V_M=283,0302. The clobber is gone. Core #22 chain proven end-to-end live.
  - **PILOT FINDING F (real Phase-4 blocker, engine-output-materialization gap): V_M NOT PERSISTED.** V_M=283.03 computes on screen (client engine) but there is NO V_M row in project_parameters (A138-17 HAS a V_M field de5f9fee, active — so it's not a missing-field issue; it's the known gap: saveWorksheet does not persist derived engine outputs without a server materialize). CONSEQUENCE: the A138-23 summary aggregator reads the governing volume (V_M for mulde) FROM project_parameters → null → facility_specific_volume_m3=null + facility_specific_dimensioning_complete=false → recommendation stuck FAIL ("V_M fehlt") even though V_M exists on screen. AFFECTS ALL FACILITIES (every governing volume V_M/V_R/V_MR/V_MUE/V_S/V_VA is a client-only engine output). The Task-3b.2 aggregator design assumed persisted governing volume — wrong. FIX (needed before fan-out): persist the facility governing volume (a facility-volume materialize, mirroring asm/basin/loading) so the summary can consume it. Related: [[project_engine_output_materialization]].
  - STEP 4 (A138-23, user set phase_4_gate_result=FAIL + saved) 2026-07-18: **DB shows ZERO change** — all A138-23 fields still entered_at 22:24:57 (Step-1); phase_4_gate_result NOT persisted (no row); aggregator did NOT re-fire. Synthesis of the A138-23 SUMMARY-LAYER finding cluster (pilot-gate payoff — Task 3b needs a fix wave before fan-out):
    * **Finding B — FALSE ALARM (controller query error, retracted).** facility_meets_qsac + facility_specific_dimensioning_complete ARE `false` (in value_BOOLEAN); my Step-1 query selected value_number → saw null → wrongly reported "null booleans". No bug. (Implementer was right; Finding-B "fix" was a no-op regression test, harmless.)
    * **Finding F (confirmed) — V_M governing volume not persisted** → summary reads null → volume=null, complete=false, recommendation stuck FAIL. Affects all facilities.
    * **Finding G1 (dispatch/chaining) — summary doesn't re-fire when its consumed values materialize.** phase4_summary producer-fire checks changedSymbols ∩ inputSymbols; A_S_m/V_M are materialized SERVER-side by other saves (asm/facility), never in the user batch → summary never refreshes after Step-1. AND worksheet.ts:558 tx-open condition EXCLUDES isPhase4SummarySave → a no-dirty A138-23 re-save can't force it (that's why Step 4 was a no-op: the enum didn't dirty → savedCount=0 → tx never opened). Same class as defect #17 (V_VA non-refire).
    * **Finding G2 (render) — recommended_phase_4_gate (derived enum) shows NO highlighted option** (user-observed). enum_values are valid (3 opts PASS/CONDITIONAL/FAIL on BOTH fields) → it's a derived-enum render issue (the widget doesn't show a read-only/derived enum's value as selected). Separately phase_4_gate_result (editable) didn't persist — consistent with the enum not dirtying, or the #15b field-cluster (two adjacent similar enums) → user may have touched the read-only one.
    * Finding A (facility_type_dimensioned=type) STILL UNCONFIRMED — the fix is deployed but the aggregator hasn't re-fired on fresh data (stale value "A138-17" is Step-1, not a fix failure).
  - CONCLUSION: the A138-23 summary layer is not working end-to-end. Needs a focused FIX WAVE: (F) persist facility governing volume (materialize); (G1) re-fire the summary when A_S_m/V_M/facility values materialize + add isPhase4SummarySave to the :558 tx-open; (G2) render the derived recommended enum's value. Then re-verify Step 4 (confirms Finding A + volume + complete + PASS/CONDITIONAL flips). Design + repro tests for user approval before redeploy (established discipline).
  - SUMMARY WAVE DESIGN APPROVED 2026-07-20 (rulings: F=persist Gl.15 A_S_m·h_M, SOURCE-VERIFIED §6.3.2 = the Mulde Speichervolumen; G2a+b this wave, card-redesign→display batch; verdict-persistence acceptance step added; #22 regression-guarded). Design doc docs/superpowers/plans/2026-07-20-summary-layer-fix-design.md.
  - FIX-WAVE BUILD BLOCKED (NEEDS_CONTEXT, correct escalation) 2026-07-20: F+G1 are DB-transaction behaviors; their real-topology RED needs a live Postgres — NONE available (no DATABASE_URL, local Supabase down, no Docker; only the PROD PAT, must not fake fixtures). Implementer refused to write untested F/G1 server code. #22 guard GREEN, tsc 28, no commits. KEY: the real-topology RED for F/G1 was ALREADY captured LIVE = the pilot Step-4 DB-verify (volume=null, summary stale on prod DB). RECOMMEND: build F/G1 with RED-first DB-FREE logic tests (dispatch chain-fire; :558 tx-open+isPhase4SummarySave; write-set includes persisted facility volume) + a written DB-gated integration test; G2 fully in-repo; the PILOT re-run (Step 4) = live integration GREEN. Keeps F+G1 together, no local DB. AWAITING user ratification of this test approach before build.
  - APPROACH RATIFIED + AUTONOMOUS BUILD AUTHORIZED 2026-07-20. BUILT TDD (logic-layer RED-first): F f6a27e5 (facilityGoverningVolume mulde=A_S_m·h_M + facilityVolumeMaterialize; worksheet.ts step-6b persists V_M derived+scoped; masking integration test REPLACED with faithful h_M-only one), G1 e5f8df5 (phase4SummaryChainFires after asm materialize + shouldOpenTransaction incl. isPhase4SummarySave @:558), G2 3125dd1 (PHASE4_READONLY_SYMBOLS→computedSymbols; DynamicField enum respects isComputed → locked recommendation w/ data-readonly-recommendation marker; editable verdict intact). RED proofs captured. Suite 1195 pass+1 expected-fail; tsc by-file 28 (touched 0). #22 GUARD GREEN.
  - DEPLOYED preview build **74fxny48z** (dpl_9J1YdnYLyqKkPA11ErEkiVr1hhot, target=preview READY, curl 200) from tip 3125dd1. URL https://ekowai-wizard-preview-74fxny48z-hannesosters-projects.vercel.app (scope hannesosters-projects).
  - SERVER-SIDE TRIGGER BLOCKED (honest residue): saveWorksheet is 'use server' server action, no REST route, no DB conn → browser-only; did NOT fake a prod write. RED re-verified holds (V_M 0 rows, volume null, complete false; A_S_m=943.4338711204341 #22 intact). LIVE GREEN = browser pilot + controller DB-verify.
  - FLIPS LIMITATION: PASS/CONDITIONAL need meetsQsac=true but q_S,AC<2 on PLT-HS-01 (derived, not UI-settable) → PASS/CONDITIONAL are UNIT-proven; live pilot proves FAIL + mechanics.
  - **PILOT FINDING H — F wrote NULL V_M live (root-caused 2026-07-20).** User h_M-nudged on 74fxny48z; DB: V_M row EXISTS (A138-17, derived) but value=NULL (not 283.03); summary read null → still "fehlende V_M". G1 chain-fire WORKED (summary fresh, type='mulde'). ROOT CAUSE (code-confirmed): the CLIENT engine writes V_M via **Gl.14 (bfe6e59a) which is NOT displayOnly** (Gl.15 IS displayOnly) → client Gl.14 can't compute (server-only swept D) → writes **V_M=null**; V_M is a LOCAL A138-17 field → NOT in any suppress set → the client null lands in the save/autosave batch and CLOBBERS step-6b's server-materialized 283.03 (12s-later timestamp = the debounced-autosave clobber, defect-#19 class). The 283.0302 the user saw on screen is Gl.15 (displayOnly, screen-only, never persisted). asmOutP.A_S_m + muldeHmForVolume at :2040 are BOTH fine when the sweep runs — step-6b DID persist 283.03; it's overwritten. Same clobber class as #22, but for V_M (local field, so the inheritedHomeBySymbol signal doesn't catch it).
  - FALSE-CONFIDENCE OWNED: my "acceptance" NEVER verified 283.03 persisted — the server-side trigger was BLOCKED (documented). The only "green" was the PURE facilityGoverningVolume unit test (283.03) + my PREDICTED expected-GREEN column. Neither exercises the CLIENT write-back or saveWorksheet → both blind to the clobber. Exactly the false-confidence class. Corrective standing rule below.
  - STANDING RULE (user-endorsed 2026-07-20): a fix that persists a value the CLIENT ALSO writes must be proven through THE REAL SAVE PATH (saveWorksheet with the client's actual batch), not a pure helper + a prediction. A pure-function unit green + an expected-value table is NOT acceptance for a persist that a client write-back can clobber.
  - FIX DESIGN (within approved F design): suppress V_M's client write-back on facility worksheets (server materialize is authoritative) — add facility governing-volume symbols to a suppress set (or mark Gl.14 displayOnly on A138-17 like Gl.15). Then no client null → step-6b's 283.03 stands. REPRO through the real save path needs an in-process Postgres (pglite/pg-mem) — NONE in repo; adding it is INFRA OUTSIDE the summary layer → STOP for user decision.
    * REDEPLOYED preview 2026-07-18: build **kzzi38djz** (from tip dd63a73, target=preview≠prod, READY). URL https://ekowai-wizard-preview-kzzi38djz-hannesosters-projects.vercel.app → prod Supabase. Supersedes cbcqmcpcs. Prod aliases UNTOUCHED. NEXT: user resumes pilot Step 3 (A138-17 h_M nudge 0.30→0.31→save→0.30→save) on kzzi38djz expecting A_S,m=943.43 rendered/inherited on A138-17 + V_M≈283.03 computes; then Step 4 (A138-23 summary re-verify with Finding-A facility_type_dimensioned='mulde' + Finding-B booleans) → baseline restore → pilot close.

## Phase-4 CLOSE-OUT items (parked; not blocking the pilot)
- WORKBOOK-SYNC (user-accepted 2026-07-17): fold the 2 new A138-23 fields (recommended_phase_4_gate, phase_4_recommendation_reasons) + the 3 gates (REQ-31/32/33 on A138-16/18/21) into the A138-23/16/18/21 Pass3c workbook sheets so the workbook stays source-of-truth (they currently survive re-import via ON CONFLICT DO NOTHING/UPDATE, but the workbook doesn't yet contain them). Do before the next full A138 re-import.
- RETIRE-PDF-138-GATE (E1 post-merge item 4): switch 138 PDF from PDF_138_FROZEN_GATE to route-all-minus-deny at end of Phase 4; re-verify PLT-HS-01 PDF vs Step-6.3.
- Tab.14 freeboard/slope thresholds (PHASE4_SUMMARY_TAB14_SOURCE): source the numeric ranges → wire freeboardOk/slopeOk (currently null; t_E drives CONDITIONAL).
- Task 7 (pilot close-out): pending.
- Tasks 8–13 (fan-out Rigole/MRE/MRS/Schacht/Becken/Fläche): pending pilot sign-off.
- Task 14 (full regression + deploy + close-out): pending.
