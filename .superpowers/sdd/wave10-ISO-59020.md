# Wave-10 — ISO 59020:2024 (Circular Economy — circularity measurement) — audit complete, fix applied

**Ledger header** — `claude-fable-5` · 2026-07-28 · run `wf_c621a042-255` (23 agents, 1.58M
tokens) · COMPUTATIONAL standard (13 eq), final — fixes apply. Offset (arabic body)
**printed = PDF − 10**, high confidence (6 footers + TOC); front matter roman = PDF − 2.

## The strong half
Encoding structurally faithful and broad (9 ws / 83 fields / 13 eq / 37 CRs). **All 13
equations A.1–A.13 verbatim-verified against the printed page**; every normative clause
(1–8 + Annex A core indicators) maps to worksheet content. No multi-producer collision (13
distinct outputs), no comma-subscript FN_LIKE trap.

## APPLIED (source-settled, execution-proven in-session)
**A.1 (%REUI) + A.2 (%RECI) LHS fix** (migration `20260728130000`). Their formula LHS began
with `%` — the engine's `rhs()` LHS-strip can't match it, so `%` hit the tokenizer and
**hard-errored**. Declared `output_symbol` is `pct_REUI_X`/`pct_RECI_X`; set the LHS to that.
Execution: `error("Unerwartetes Zeichen %") → computed 30 / 25`. Verified by query. **These
are 2 of the 3 MANDATORY resource-inflow core indicators (Table 3, printed p.17)** — WS
ISO-59020-05 (Resource Inflow) was broken and is now unblocked. Same Gl.13/C.1 class. A.3
already used a letter-prefixed LHS and worked. Also stripped the dangling wikilink
(`[[reference_wizard_compliance_gates]]` → memory ref) — cleared the standard's 1 validator error.

## NOT ready-to-use — the ruling (sheet M)
**15 arithmetic CRs (CR-011..025) are mis-homed on WS-04** (Indicator Selection) while every
operand lives on WS-05/06/07. ComplianceBlock resolves worksheet-locally → all 15 render
`pending` and **never enforce** — silently killing the two BLOCK-severity 100 %-balance gates
the standard demands verbatim (CR-014 inflow, CR-019 outflow; §6.3/p.11 "shall balance",
p.18 "the sum is 100 %") plus every formula-verification gate. Classic F-4. Re-homing changes
enforcement (dead→live) → ruling. Note: these use float `==` on computed division (fragile
even re-homed).

## Workflow metric (assessed, not harness-executed)
WS-06 (outflow), WS-07 (energy/water/economic) compute end-to-end; **WS-05 (inflow) now
computes A.1/A.2/A.3** after the fix (was only A.3). WS-01/02/03/04/08/09 are
attestation/selection sheets with locally-correct gates. Deployed-build harness NOT run
(element 7 owner-blocked) — UNPROVEN.

## Minor (for a follow-up pass)
Derived-as-required dual-role on pct_* fields (single-source doctrine: should be read-only);
pct_linear_inflow/outflow required but no equation (= 100 − circular, could be an equation);
CR-008/010/036/037 condition='manual' parses as a truthy test on a symbol 'manual'; CR-009
clause_reference drift (§5.4 → §7.3.1/Table 3).

## Seven-element status
Bidirectional walk ✅ · equations ✅ (13/13, A.1/A.2 fixed) · tables n/a · gates both-ways ✅
· source-settled fixes ✅ (applied+proven) · map write-back ✅ (2 eq nodes + wikilink) ·
deployed harness ⏳ (element-7 blocked). Judgment: M-D1 on the sheet.
