# Wave-9 — DIN CEN ISO/TS 14071-1 (LCA critical-review requirements) — audit complete

**Ledger header** — `claude-fable-5` · 2026-07-28 · run `wf_dd0376ba-97a` (2 agents, 232k
tokens) · requirements/process standard, 0 eq / 0 tables. Offset **printed = PDF − 4**
(§4.1 confirmed printed p.10 = PDF 14). Final standard — fixes apply.

## The strong half
**Field fidelity strong** — 17 of 60 fields spot-checked against printed clauses, all
content-FAITHFUL (no wrong-content defect). **13 of 17 CRs ENFORCE** with correct
modal-severity match: every REQ-01..14 maps to a printed *shall*, and *should* items
(changes_documented §4.3.1, no_subcontracting §4.7.2) are correctly left un-gated. §5 reviewer
competencies fully gated (REQ-11, comprehensive AND). This is a well-built requirements encoding.

## APPLIED (verified in-session)
1. **dp-din14071-scope-enums source_page = 10** — cleared the `1.va-has-page` validator error;
   §4.1 options a–d confirmed on printed p.10 (PDF 14, footer "10").
2. **3 dead-gate nodes annotated** (CX-01/02/03) — honest DEAD-GATE notes; NOT silenced with a
   fake gated_by link (the `1.cr-fired-or-dead` errors correctly remain as ruling L-D1).

## NOT ready-to-use — rulings (sheet L)
- **CX-01/02/03 are dead** — DB condition literally `TRUE` → always-pass block gates over
  framing content, referencing zero fields. Revive-with-a-predicate or remove.
- **Coverage holes (printed shall, un-gated):** §4.7.1 chairperson 8-bullet duty list
  (largest hole — only signing captured); §4.2.3 replacement shalls (min_reviewers_maintained,
  replacement_documented — fields exist, no CR); §4.5 chairperson-signature shall (only
  reviewer_signatures gated in REQ-13) + "sign as individuals not organizations";
  §4.5 non-conformance-basis-6.1 shall.
- **Modal mismatch:** REQ-06 gates the §4.3.3 iteration content (*should*) under `block`;
  REQ-13 mixed shall/should.

## Pre-authorised, NOT yet done (next action)
Quote-backfill `source_quote` on all **60 fields + 17 CRs** (currently NULL,
imported_unverified) from verbatim PDF pp.11–28 + correct `clause_reference` drift
(WS1 fields blanket-§4.1 → actual §4.4/§4.5/Annex B; no_subcontracting §5 → §4.7.2). A
dedicated evidence pass (each re-verified in-session per R-2).

## Honest residue
**Workflow metric NOT executed** — static audit only, no harness/deployed-build run.
Worksheets are structurally coherent and expected-to-run but that is **UNPROVEN**.
